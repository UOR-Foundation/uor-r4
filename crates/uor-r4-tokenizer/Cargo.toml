[package]
name = "uor-r4-tokenizer"
license = "MIT"
version = "0.1.0"
edition = "2021"
description = "Shared byte-level BPE engine and stable tokenizer identity"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
blake3 = "=1.5.0"
