[package]
name = "uor-r4-core"
version = "0.1.0"
edition = "2021"
description = "Pure mathematics of the R4 Tangent Space Router: zeta-zero embeddings, Hopf coordinates, prime/QIMC identity layer, state metrics — no wasm, no UOR-framework dependencies"

[dependencies]
sha2 = "0.10.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4.3"
