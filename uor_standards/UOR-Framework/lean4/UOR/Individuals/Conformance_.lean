-- @generated by uor-lean from uor-ontology — do not edit manually
--
-- UOR Foundation — Conformance_ individual constants.

import UOR.Structures

open UOR.Primitives

namespace UOR.Bridge.Conformance_

-- Exactly one root term is required. Range is schema:Term.
def compileUnit_rootTerm_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/reduction/rootTerm" : String))
  constraintRange := some (("https://uor.foundation/schema/Term" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("root_term" : String))
  surfaceProduction := some (("program" : String))
}

-- At least one target verification domain is required. maxCount 0 means unbounded.
def compileUnit_targetDomains_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/reduction/targetDomains" : String))
  constraintRange := some (("https://uor.foundation/op/VerificationDomain" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((0 : Nat))
  surfaceKeyword := some (("target_domains" : String))
  surfaceProduction := some (("domain-set" : String))
}

-- Exactly one thermodynamic budget is required. Shape validates presence and type; the BudgetSolvencyCheck preflight validates the value against the Landauer bound.
def compileUnit_thermodynamicBudget_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/reduction/thermodynamicBudget" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#decimal" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("thermodynamic_budget" : String))
  surfaceProduction := some (("decimal-literal" : String))
}

-- Exactly one quantum level is required. Range is schema:WittLevel.
def compileUnit_unitWittLevel_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/reduction/unitWittLevel" : String))
  constraintRange := some (("https://uor.foundation/schema/WittLevel" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("witt_level_ceiling" : String))
  surfaceProduction := some (("name" : String))
}

-- Shape validating that a CompileUnit carries all required properties before reduction admission. The unitAddress property is NOT required — it is computed by stage_initialization after shape validation passes.
def CompileUnitShape : UOR.Bridge.Conformance_.Shape UOR.Prims.Standard := {
  targetClass := some (("https://uor.foundation/reduction/CompileUnit" : String))
  requiredProperty := #[UOR.Bridge.Conformance_.compileUnit_rootTerm_constraint, UOR.Bridge.Conformance_.compileUnit_unitWittLevel_constraint, UOR.Bridge.Conformance_.compileUnit_thermodynamicBudget_constraint, UOR.Bridge.Conformance_.compileUnit_targetDomains_constraint]
  surfaceForm := some (("compile-unit-decl" : String))
}

-- Exactly one predicate term selecting this dispatch rule.
def dispatch_predicate_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/predicate/dispatchPredicate" : String))
  constraintRange := some (("https://uor.foundation/schema/Term" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("predicate" : String))
  surfaceProduction := some (("term" : String))
}

-- Non-negative integer evaluation order; lower values evaluate first.
def dispatch_priority_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/predicate/dispatchPriority" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#nonNegativeInteger" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("priority" : String))
  surfaceProduction := some (("integer-literal" : String))
}

-- The resolver class invoked when the predicate holds.
def dispatch_target_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/predicate/dispatchTarget" : String))
  constraintRange := some (("https://uor.foundation/resolver/Resolver" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("target_resolver" : String))
  surfaceProduction := some (("name" : String))
}

-- Shape instance validating predicate:DispatchRule declarations against the dispatch-rule-decl grammar.
def DispatchShapeInstance : UOR.Bridge.Conformance_.Shape UOR.Prims.Standard := {
  targetClass := some (("https://uor.foundation/predicate/DispatchRule" : String))
  requiredProperty := #[UOR.Bridge.Conformance_.dispatch_predicate_constraint, UOR.Bridge.Conformance_.dispatch_target_constraint, UOR.Bridge.Conformance_.dispatch_priority_constraint]
  surfaceForm := some (("dispatch-rule-decl" : String))
}

-- Lexical or session scope within which the lease is valid.
def lease_leaseScope_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/state/leaseScope" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#string" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("lease_scope" : String))
  surfaceProduction := some (("string-literal" : String))
}

-- Site coordinate allocated linearly by this lease.
def lease_linearSite_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/state/linearSite" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#nonNegativeInteger" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("linear_site" : String))
  surfaceProduction := some (("integer-literal" : String))
}

-- Shape instance for state:ContextLease declarations.
def LeaseShapeInstance : UOR.Bridge.Conformance_.Shape UOR.Prims.Standard := {
  targetClass := some (("https://uor.foundation/state/ContextLease" : String))
  requiredProperty := #[UOR.Bridge.Conformance_.lease_linearSite_constraint, UOR.Bridge.Conformance_.lease_leaseScope_constraint]
  surfaceForm := some (("lease-decl" : String))
}

-- IRI of a proof of pairwise disjointness of the partition components.
def parallel_disjointnessWitness_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/parallel/disjointnessWitness" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#string" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("disjointness_witness" : String))
  surfaceProduction := some (("string-literal" : String))
}

-- The site partition this parallel product is over.
def parallel_sitePartition_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/parallel/sitePartition" : String))
  constraintRange := some (("https://uor.foundation/partition/Partition" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("site_partition" : String))
  surfaceProduction := some (("name" : String))
}

-- Shape instance for parallel:ParallelProduct declarations.
def ParallelShapeInstance : UOR.Bridge.Conformance_.Shape UOR.Prims.Standard := {
  targetClass := some (("https://uor.foundation/parallel/ParallelProduct" : String))
  requiredProperty := #[UOR.Bridge.Conformance_.parallel_sitePartition_constraint, UOR.Bridge.Conformance_.parallel_disjointnessWitness_constraint]
  surfaceForm := some (("parallel-decl" : String))
}

-- The evaluator term producing a boolean.
def predicate_evaluator_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/predicate/evaluatorTerm" : String))
  constraintRange := some (("https://uor.foundation/schema/Term" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("evaluator" : String))
  surfaceProduction := some (("term" : String))
}

-- Input type the predicate evaluates over.
def predicate_inputType_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/predicate/evaluatesOver" : String))
  constraintRange := some (("https://uor.foundation/type/TypeDefinition" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("input_type" : String))
  surfaceProduction := some (("type-expr" : String))
}

-- IRI of a proof:Proof attesting that the evaluator halts on all inputs.
def predicate_terminationWitness_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/predicate/terminationWitness" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#string" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("termination_witness" : String))
  surfaceProduction := some (("string-literal" : String))
}

-- Shape instance for predicate:Predicate declarations.
def PredicateShapeInstance : UOR.Bridge.Conformance_.Shape UOR.Prims.Standard := {
  targetClass := some (("https://uor.foundation/predicate/Predicate" : String))
  requiredProperty := #[UOR.Bridge.Conformance_.predicate_inputType_constraint, UOR.Bridge.Conformance_.predicate_evaluator_constraint, UOR.Bridge.Conformance_.predicate_terminationWitness_constraint]
  surfaceForm := some (("predicate-decl" : String))
}

-- Prelude re-export for reduction:CompileUnit.
def preludeExport_CompileUnit : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/reduction/CompileUnit" : String))
  exportRustName := none
}

-- Prelude re-export for conformance:CompileUnitBuilder.
def preludeExport_CompileUnitBuilder : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/conformance/CompileUnitBuilder" : String))
  exportRustName := none
}

-- Prelude re-export for type:CompleteType.
def preludeExport_CompleteType : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/type/CompleteType" : String))
  exportRustName := none
}

-- Prelude re-export for cert:CompletenessCertificate.
def preludeExport_CompletenessCertificate : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/cert/CompletenessCertificate" : String))
  exportRustName := none
}

-- Prelude re-export for type:ConstrainedType.
def preludeExport_ConstrainedType : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/type/ConstrainedType" : String))
  exportRustName := none
}

-- Prelude re-export for schema:Datum.
def preludeExport_Datum : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/schema/Datum" : String))
  exportRustName := none
}

-- Prelude re-export for state:GroundedContext.
def preludeExport_GroundedContext : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/state/GroundedContext" : String))
  exportRustName := none
}

-- Prelude re-export for cert:GroundingCertificate.
def preludeExport_GroundingCertificate : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/cert/GroundingCertificate" : String))
  exportRustName := none
}

-- Prelude re-export for cert:InhabitanceCertificate (v0.2.1).
def preludeExport_InhabitanceCertificate : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/cert/InhabitanceCertificate" : String))
  exportRustName := none
}

-- Prelude re-export for cert:LiftChainCertificate.
def preludeExport_LiftChainCertificate : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/cert/LiftChainCertificate" : String))
  exportRustName := none
}

-- Prelude re-export for conformance:ShapeViolationReport.
def preludeExport_ShapeViolationReport : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/conformance/ShapeViolationReport" : String))
  exportRustName := none
}

-- Prelude re-export for schema:Term.
def preludeExport_Term : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/schema/Term" : String))
  exportRustName := none
}

-- Prelude re-export for the foundation enforcement TermArena type. Backed by conformance:WitnessDatum since TermArena has no direct OWL class but is the term-storage container.
def preludeExport_TermArena : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/conformance/WitnessDatum" : String))
  exportRustName := some (("TermArena" : String))
}

-- Prelude re-export for conformance:ValidatedWrapper (exposed in Rust as `Validated`).
def preludeExport_ValidatedWrapper : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/conformance/ValidatedWrapper" : String))
  exportRustName := some (("Validated" : String))
}

-- Prelude re-export for conformance:ValidationResult.
def preludeExport_ValidationResult : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/conformance/ValidationResult" : String))
  exportRustName := none
}

-- Prelude re-export for schema:WittLevel.
def preludeExport_WittLevel : UOR.Bridge.Conformance_.PreludeExport UOR.Prims.Standard := {
  exportsClass := some (("https://uor.foundation/schema/WittLevel" : String))
  exportRustName := none
}

-- IRI of a proof of stream productivity (coinductive well-foundedness).
def stream_productivityWitness_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/stream/productivityWitness" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#string" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("productivity_witness" : String))
  surfaceProduction := some (("string-literal" : String))
}

-- Function from current seed to (head, next_seed).
def stream_step_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/stream/stepTerm" : String))
  constraintRange := some (("https://uor.foundation/schema/Term" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("step" : String))
  surfaceProduction := some (("term" : String))
}

-- Initial seed value from which the stream unfolds.
def stream_unfoldSeed_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/stream/unfoldSeed" : String))
  constraintRange := some (("https://uor.foundation/schema/Term" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("unfold_seed" : String))
  surfaceProduction := some (("term" : String))
}

-- Shape instance for stream:ProductiveStream declarations.
def StreamShapeInstance : UOR.Bridge.Conformance_.Shape UOR.Prims.Standard := {
  targetClass := some (("https://uor.foundation/stream/ProductiveStream" : String))
  requiredProperty := #[UOR.Bridge.Conformance_.stream_unfoldSeed_constraint, UOR.Bridge.Conformance_.stream_step_constraint, UOR.Bridge.Conformance_.stream_productivityWitness_constraint]
  surfaceForm := some (("stream-decl" : String))
}

-- Bit width must equal 8·(k+1) for some non-negative integer k.
def wittLevel_bitWidth_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/schema/bitsWidth" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#positiveInteger" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("bit_width" : String))
  surfaceProduction := some (("integer-literal" : String))
}

-- Cycle size must equal 2^bit_width.
def wittLevel_cycleSize_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/schema/cycleSize" : String))
  constraintRange := some (("http://www.w3.org/2001/XMLSchema#nonNegativeInteger" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("cycle_size" : String))
  surfaceProduction := some (("integer-literal" : String))
}

-- The predecessor WittLevel individual whose nextWittLevel will be updated to point at this new level.
def wittLevel_predecessorLevel_constraint : UOR.Bridge.Conformance_.PropertyConstraint UOR.Prims.Standard := {
  constraintProperty := some (("https://uor.foundation/schema/wittLevelPredecessor" : String))
  constraintRange := some (("https://uor.foundation/schema/WittLevel" : String))
  minCount := some ((1 : Nat))
  maxCount := some ((1 : Nat))
  surfaceKeyword := some (("predecessor_level" : String))
  surfaceProduction := some (("name" : String))
}

-- Shape instance validating schema:WittLevel declarations against the witt-level-decl grammar.
def WittLevelShapeInstance : UOR.Bridge.Conformance_.Shape UOR.Prims.Standard := {
  targetClass := some (("https://uor.foundation/schema/WittLevel" : String))
  requiredProperty := #[UOR.Bridge.Conformance_.wittLevel_bitWidth_constraint, UOR.Bridge.Conformance_.wittLevel_cycleSize_constraint, UOR.Bridge.Conformance_.wittLevel_predecessorLevel_constraint]
  surfaceForm := some (("witt-level-decl" : String))
}

end UOR.Bridge.Conformance_
