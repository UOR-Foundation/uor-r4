-- @generated by uor-lean from uor-ontology — do not edit manually
--
-- UOR Foundation — Schema individual constants.

import UOR.Structures

open UOR.Primitives

namespace UOR.Kernel.Schema

-- The unique generator of R_n under successor. Value = 1 at every Witt level. Under iterated application of succ, π₁ generates every element of the ring.
def pi1 : UOR.Kernel.Schema.Datum UOR.Prims.Standard := {
  value := some ((1 : Nat))
  wittLength := none
  stratum := none
  spectrum := none
  element := none
}

-- 
def term_AA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("glyph(x)" : String))
}

-- 
def term_AA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[braille(x[0:5]), braille(x[6:11]), ...]" : String))
}

-- 
def term_AA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("braille(a ⊕ b)" : String))
}

-- 
def term_AA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("braille(a) ⊕ braille(b)" : String))
}

-- 
def term_AA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("glyph(bnot(x))" : String))
}

-- 
def term_AA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("complement each Braille character of glyph(x)" : String))
}

-- 
def term_AA_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AA_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("glyph(add(x, y))" : String))
}

-- 
def term_AA_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≠ f(glyph(x), glyph(y)) in general" : String))
}

-- 
def term_AA_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AA_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("liftable operations" : String))
}

-- 
def term_AA_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{xor, and, or, bnot}; NOT {add, sub, mul, neg, succ, pred}" : String))
}

-- 
def term_AA_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AA_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(x) = succ(bnot(x))" : String))
}

-- 
def term_AA_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot lifts, succ does not" : String))
}

-- 
def term_AD_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AD_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("addresses(glyph(d))" : String))
}

-- 
def term_AD_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d" : String))
}

-- 
def term_AD_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AD_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("glyph(ι(addresses(a)))" : String))
}

-- 
def term_AD_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι_addr(a)" : String))
}

-- 
def term_AG_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AG_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("μ_u" : String))
}

-- 
def term_AG_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∉ D_{2^n}" : String))
}

-- 
def term_AG_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AG_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Aff(R_n)" : String))
}

-- 
def term_AG_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("R_n× ⋉ R_n" : String))
}

-- 
def term_AG_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AG_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|Aff(R_n)|" : String))
}

-- 
def term_AG_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2^{2n-1}" : String))
}

-- 
def term_AG_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AG_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("D_{2^n}" : String))
}

-- 
def term_AG_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊂ Aff(R_n), u ∈ {±1}" : String))
}

-- 
def term_AM_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AM_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_addr(a, b)" : String))
}

-- 
def term_AM_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_i popcount(braille_i(a) ⊕ braille_i(b))" : String))
}

-- 
def term_AM_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AM_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_addr(glyph(x), glyph(y))" : String))
}

-- 
def term_AM_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_H(x, y)" : String))
}

-- 
def term_AM_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AM_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_addr" : String))
}

-- 
def term_AM_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("does NOT preserve d_R in general" : String))
}

-- 
def term_AM_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AM_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(x, y)" : String))
}

-- 
def term_AM_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|d_R(x,y) − d_addr(glyph(x), glyph(y))|" : String))
}

-- 
def term_AP_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AP_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sat(stage(k+1))" : String))
}

-- 
def term_AP_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some ((">= sat(stage(k))" : String))
}

-- 
def term_AP_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AP_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("quality(epoch(n+1))" : String))
}

-- 
def term_AP_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some ((">= quality(epoch(n))" : String))
}

-- 
def term_AP_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AP_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("deferred(q)" : String))
}

-- 
def term_AP_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("processed(q) | dropped(q)" : String))
}

-- 
def term_AR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("adiabatic schedule" : String))
}

-- 
def term_AR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("decreasing freeRank × cost-per-site order" : String))
}

-- 
def term_AR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Cost_adiabatic" : String))
}

-- 
def term_AR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_i cost(C_{σ(i)}) where σ is optimal" : String))
}

-- 
def term_AR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Cost_adiabatic" : String))
}

-- 
def term_AR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ n × k_B T × ln 2" : String))
}

-- 
def term_AR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("η = (n × ln 2) / Cost_adiabatic" : String))
}

-- 
def term_AR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ 1" : String))
}

-- 
def term_AR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("greedy vs adiabatic difference" : String))
}

-- 
def term_AR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ 5% for n ≤ 16" : String))
}

-- 
def term_AS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(δ ∘ ι) ∘ κ" : String))
}

-- 
def term_AS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ ∘ (ι ∘ κ)" : String))
}

-- 
def term_AS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(ι ∘ α) ∘ λ" : String))
}

-- 
def term_AS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι ∘ (α ∘ λ)" : String))
}

-- 
def term_AS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(λ ∘ κ) ∘ δ" : String))
}

-- 
def term_AS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("λ ∘ (κ ∘ δ)" : String))
}

-- 
def term_AS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("associator(A,B,C) ≠ 0" : String))
}

-- 
def term_AS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ mediating read-write interleaving" : String))
}

-- 
def term_AU_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AU_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Aut(R_n)" : String))
}

-- 
def term_AU_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{μ_u : x ↦ mul(u, x) | u ∈ R_n×}" : String))
}

-- 
def term_AU_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AU_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Aut(R_n)" : String))
}

-- 
def term_AU_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≅ R_n× ≅ Z/2 × Z/2^{n−2}" : String))
}

-- 
def term_AU_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AU_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|Aut(R_n)|" : String))
}

-- 
def term_AU_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2^{n−1}" : String))
}

-- 
def term_AU_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AU_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Aut(R_n) ∩ D_{2^n}" : String))
}

-- 
def term_AU_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{id, neg}" : String))
}

-- 
def term_AU_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_AU_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Aff(R_n)" : String))
}

-- 
def term_AU_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⟨D_{2^n}, μ_3⟩" : String))
}

-- 
def term_BM_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BM_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(C)" : String))
}

-- 
def term_BM_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(n − freeRank(C)) / n" : String))
}

-- 
def term_BM_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BM_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(nerve(C))" : String))
}

-- 
def term_BM_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ(−1)^k β_k(nerve(C))" : String))
}

-- 
def term_BM_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BM_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σκ_k − χ" : String))
}

-- 
def term_BM_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_residual / ln 2" : String))
}

-- 
def term_BM_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BM_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("J_k (pinned site k)" : String))
}

-- 
def term_BM_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_BM_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BM_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(x, y) > 0" : String))
}

-- 
def term_BM_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("carry(x + y) ≠ 0" : String))
}

-- 
def term_BM_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BM_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("metric tower" : String))
}

-- 
def term_BM_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ → {σ, J_k} → β_k → χ → r" : String))
}

-- 
def term_BR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("measureValue(stepMeasurePost(s))" : String))
}

-- 
def term_BR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("< measureValue(stepMeasurePre(s))" : String))
}

-- 
def term_BR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("depth(RecursionTrace(R))" : String))
}

-- 
def term_BR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ m" : String))
}

-- 
def term_BR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("terminates(R)" : String))
}

-- 
def term_BR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("true" : String))
}

-- 
def term_BR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("initialMeasure(R)" : String))
}

-- 
def term_BR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("structuralSize(T)" : String))
}

-- 
def term_BR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_BR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("eval(p, state) = true" : String))
}

-- 
def term_BR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("measureValue = 0" : String))
}

-- 
def term_B_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(x, or(y, z))" : String))
}

-- 
def term_B_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("or(and(x, y), and(x, z))" : String))
}

-- 
def term_B_11_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_11_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(and(x, y))" : String))
}

-- 
def term_B_11_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("or(bnot(x), bnot(y))" : String))
}

-- 
def term_B_12_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_12_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(or(x, y))" : String))
}

-- 
def term_B_12_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(bnot(x), bnot(y))" : String))
}

-- 
def term_B_13_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_13_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(bnot(x))" : String))
}

-- 
def term_B_13_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_B_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(x, xor(y, z))" : String))
}

-- 
def term_B_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(xor(x, y), z)" : String))
}

-- 
def term_B_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(x, 0)" : String))
}

-- 
def term_B_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_B_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(x, x)" : String))
}

-- 
def term_B_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_B_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(x, and(y, z))" : String))
}

-- 
def term_B_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(and(x, y), z)" : String))
}

-- 
def term_B_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(x, 2^n - 1)" : String))
}

-- 
def term_B_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_B_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(x, 0)" : String))
}

-- 
def term_B_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_B_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("or(x, or(y, z))" : String))
}

-- 
def term_B_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("or(or(x, y), z)" : String))
}

-- 
def term_B_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("or(x, 0)" : String))
}

-- 
def term_B_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_B_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_B_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(x, or(x, y))" : String))
}

-- 
def term_B_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_CA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x,y)_k" : String))
}

-- 
def term_CA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(x_k, xor(y_k, c_k(x,y)))" : String))
}

-- 
def term_CA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("c_{k+1}(x,y)" : String))
}

-- 
def term_CA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("or(and(x_k,y_k), and(xor(x_k,y_k), c_k))" : String))
}

-- 
def term_CA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("c(x, y)" : String))
}

-- 
def term_CA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("c(y, x)" : String))
}

-- 
def term_CA_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CA_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("c(x, 0)" : String))
}

-- 
def term_CA_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_CA_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CA_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("c(x, neg(x))_k" : String))
}

-- 
def term_CA_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1" : String))
}

-- 
def term_CA_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CA_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(x, y) > 0" : String))
}

-- 
def term_CA_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ k : c_k(x,y) = 1" : String))
}

-- 
def term_CB_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CB_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("min convergenceRate" : String))
}

-- 
def term_CB_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1 site per iteration" : String))
}

-- 
def term_CB_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CB_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("max convergenceRate" : String))
}

-- 
def term_CB_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n sites in 1 iteration" : String))
}

-- 
def term_CB_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CB_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("expected rate (residue)" : String))
}

-- 
def term_CB_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⌊log_2(m)⌋ sites per constraint" : String))
}

-- 
def term_CB_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CB_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("convergenceRate < 1 for 2 iterations" : String))
}

-- 
def term_CB_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraint set may be insufficient" : String))
}

-- 
def term_CB_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CB_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∪_i pins(C_i) = {0,...,n−1}" : String))
}

-- 
def term_CB_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraint set closes budget" : String))
}

-- 
def term_CB_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CB_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("iterations for k constraints" : String))
}

-- 
def term_CB_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ min(k, n)" : String))
}

-- 
def term_CC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolution(x, T)" : String))
}

-- 
def term_CC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(1) for CompleteType T" : String))
}

-- 
def term_CC_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CC_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("completeness(T)" : String))
}

-- 
def term_CC_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("implies completeness(T')" : String))
}

-- 
def term_CC_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CC_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sitesClosed(W₁) + sitesClosed(W₂)" : String))
}

-- 
def term_CC_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= n for valid concat(W₁, W₂)" : String))
}

-- 
def term_CC_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CC_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CompletenessResolver" : String))
}

-- 
def term_CC_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("fix(ψ-pipeline, CompletenessCandidate)" : String))
}

-- 
def term_CC_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CC_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert.verified = true" : String))
}

-- 
def term_CC_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("implies χ(N(C)) = n ∧ ∀k: β_k = 0" : String))
}

-- 
def term_CC_COST_SITE_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CC_COST_SITE_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|pinsSites(CarryConstraint(p))|" : String))
}

-- 
def term_CC_COST_SITE_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("popcount(p) + 1" : String))
}

-- 
def term_CC_PINS_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CC_PINS_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinsSites(CarryConstraint(p))" : String))
}

-- 
def term_CC_PINS_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{k : p(k)=1} union {first-zero(p)}" : String))
}

-- 
def term_CDI_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CDI_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("carry(residue(T))" : String))
}

-- 
def term_CDI_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("depth(T)" : String))
}

-- 
def term_CF_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CF_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CurvatureFlux(γ)" : String))
}

-- 
def term_CF_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ |d_R(x_i, x_{i+1}) − d_H(x_i, x_{i+1})|" : String))
}

-- 
def term_CF_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CF_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ResolutionCost(T)" : String))
}

-- 
def term_CF_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ CurvatureFlux(γ_opt)" : String))
}

-- 
def term_CF_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CF_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CurvatureFlux(x, succ(x))" : String))
}

-- 
def term_CF_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("trailing_ones(x) for t < n; n−1 for x = 2^n−1" : String))
}

-- 
def term_CF_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CF_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_{x ∈ R_n} CurvatureFlux(x, succ(x))" : String))
}

-- 
def term_CF_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2^n − 2" : String))
}

-- 
def term_CG_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CG_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("advance_to(s)" : String))
}

-- 
def term_CG_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("requires eval(g, currentState) = true" : String))
}

-- 
def term_CG_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CG_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("advance_from(s)" : String))
}

-- 
def term_CG_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("requires eval(g, currentState) = true, then apply(e)" : String))
}

-- 
def term_CIC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CIC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("valid(T) ∧ T: Transform" : String))
}

-- 
def term_CIC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: TransformCertificate. certifies(c, T)" : String))
}

-- 
def term_CIC_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CIC_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isometry(T) ∧ metric-preserving(T)" : String))
}

-- 
def term_CIC_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: IsometryCertificate. certifies(c, T)" : String))
}

-- 
def term_CIC_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CIC_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("f(f(x)) = x ∀ x ∈ R_n" : String))
}

-- 
def term_CIC_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: InvolutionCertificate. certifies(c, f)" : String))
}

-- 
def term_CIC_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CIC_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(C) = 1 ∧ freeRank = 0" : String))
}

-- 
def term_CIC_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: GroundingCertificate. certifies(c, C)" : String))
}

-- 
def term_CIC_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CIC_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("AR_1-ordered ∧ DC_10-selected trace" : String))
}

-- 
def term_CIC_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: GeodesicCertificate. certifies(c, trace)" : String))
}

-- 
def term_CIC_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CIC_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_vN = L_cost at β* = ln 2" : String))
}

-- 
def term_CIC_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: MeasurementCertificate. certifies(c, event)" : String))
}

-- 
def term_CIC_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CIC_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("P(k) = |α_k|² verified" : String))
}

-- 
def term_CIC_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: BornRuleVerification. certifies(c, event)" : String))
}

-- 
def term_CL_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CL_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Constraint/≡" : String))
}

-- 
def term_CL_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2^{[n]}" : String))
}

-- 
def term_CL_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CL_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("A ∨ B" : String))
}

-- 
def term_CL_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(A, B)" : String))
}

-- 
def term_CL_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CL_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pins(A ∧ B)" : String))
}

-- 
def term_CL_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pins(A) ∩ pins(B)" : String))
}

-- 
def term_CL_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CL_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(A ∨ B) ∧ C" : String))
}

-- 
def term_CL_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(A ∧ C) ∨ (B ∧ C)" : String))
}

-- 
def term_CL_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CL_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("A ∧ A̅ = ε, A ∨ A̅ = Ω" : String))
}

-- 
def term_CL_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ A̅ (complement)" : String))
}

-- 
def term_CM_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CM_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("C_i redundant in {C_1,...,C_k}" : String))
}

-- 
def term_CM_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pins(C_i) ⊆ ∪_{j≠i} pins(C_j)" : String))
}

-- 
def term_CM_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CM_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("minimal cover" : String))
}

-- 
def term_CM_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("irredundant sub-collection (greedy removal)" : String))
}

-- 
def term_CM_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CM_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("min constraints to cover n sites" : String))
}

-- 
def term_CM_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⌈n / max_k pins_per_constraint_k⌉" : String))
}

-- 
def term_COEFF_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_COEFF_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("standard coefficient ring for psi-pipeline" : String))
}

-- 
def term_COEFF_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Z/2Z" : String))
}

-- 
def term_CPT_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CPT_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("siteBudget(A ⊠ B)" : String))
}

-- 
def term_CPT_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("siteBudget(A) + siteBudget(B)" : String))
}

-- 
def term_CPT_2a_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CPT_2a_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Π(A ⊠ B)" : String))
}

-- 
def term_CPT_2a_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CartesianPartitionProduct(Π(A), Π(B))" : String))
}

-- 
def term_CPT_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CPT_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C(A ⊠ B)))" : String))
}

-- 
def term_CPT_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C(A))) · χ(N(C(B)))" : String))
}

-- 
def term_CPT_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CPT_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(A ⊠ B)" : String))
}

-- 
def term_CPT_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_{i+j=k} β_i(A) · β_j(B)" : String))
}

-- 
def term_CPT_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CPT_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(A ⊠ B)" : String))
}

-- 
def term_CPT_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(A) + S(B)" : String))
}

-- 
def term_CPT_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CPT_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("A ⊠ (B + C)" : String))
}

-- 
def term_CPT_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(A ⊠ B) + (A ⊠ C)" : String))
}

-- 
def term_CR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(ResidueConstraint(m, r))" : String))
}

-- 
def term_CR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("step_m = 2 × ((−2^n) mod m) + 1" : String))
}

-- 
def term_CR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(CarryConstraint(p))" : String))
}

-- 
def term_CR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("popcount(p)" : String))
}

-- 
def term_CR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(DepthConstraint(d_min, d_max))" : String))
}

-- 
def term_CR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(residue) + cost(carry)" : String))
}

-- 
def term_CR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(compose(A, B))" : String))
}

-- 
def term_CR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ cost(A) + cost(B)" : String))
}

-- 
def term_CR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("optimal resolution order" : String))
}

-- 
def term_CR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("increasing cost order" : String))
}

-- 
def term_CS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(stage(k))" : String))
}

-- 
def term_CS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(1)" : String))
}

-- 
def term_CS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(pipeline)" : String))
}

-- 
def term_CS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sum(cost(stage(k)))" : String))
}

-- 
def term_CS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(rollback)" : String))
}

-- 
def term_CS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(forward)" : String))
}

-- 
def term_CS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(preflight)" : String))
}

-- 
def term_CS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(1)" : String))
}

-- 
def term_CS_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CS_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(reduction)" : String))
}

-- 
def term_CS_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n * max(cost(stage(k)))" : String))
}

-- 
def term_CS_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CS_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("thermodynamicBudget(U) < bitsWidth(unitWittLevel(U)) × ln 2" : String))
}

-- 
def term_CS_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("reject(U) at BudgetSolvencyCheck" : String))
}

-- 
def term_CS_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CS_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("unitAddress(U)" : String))
}

-- 
def term_CS_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("address(canonicalBytes(transitiveClosure(rootTerm(U))))" : String))
}

-- 
def term_CT_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CT_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("catastrophe boundaries" : String))
}

-- 
def term_CT_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("g = 2^k for 1 ≤ k ≤ n−1" : String))
}

-- 
def term_CT_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CT_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("odd prime catastrophe" : String))
}

-- 
def term_CT_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ResidueConstraint(p, •) transitions visibility" : String))
}

-- 
def term_CT_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CT_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CatastropheThreshold(g)" : String))
}

-- 
def term_CT_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("step_g / n" : String))
}

-- 
def term_CT_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CT_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("composite catastrophe g = p⋅q" : String))
}

-- 
def term_CT_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("max(step_p, step_q) / n" : String))
}

-- 
def term_CY_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CY_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("generate(k)" : String))
}

-- 
def term_CY_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(x_k, y_k) = 1" : String))
}

-- 
def term_CY_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CY_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("propagate(k)" : String))
}

-- 
def term_CY_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(x_k, y_k) = 1 ∧ c_k = 1" : String))
}

-- 
def term_CY_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CY_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("kill(k)" : String))
}

-- 
def term_CY_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("and(x_k, y_k) = 0 ∧ xor(x_k, y_k) = 0" : String))
}

-- 
def term_CY_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CY_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(x, y)" : String))
}

-- 
def term_CY_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|carryCount(x+y) − hammingDistance(x, y)|" : String))
}

-- 
def term_CY_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CY_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("argmin_enc Σ d_Δ(enc(s_i), enc(s_j))" : String))
}

-- 
def term_CY_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("enc where carry significance ≅ domain dependency" : String))
}

-- 
def term_CY_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CY_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("min d_Δ site ordering" : String))
}

-- 
def term_CY_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("high-significance sites → most informative observables" : String))
}

-- 
def term_CY_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_CY_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("T(carry_chain(n))" : String))
}

-- 
def term_CY_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(log n) via prefix computation on (g_k, p_k) pairs" : String))
}

-- 
def term_C_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_C_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pins(compose(A, B))" : String))
}

-- 
def term_C_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pins(A) ∪ pins(B)" : String))
}

-- 
def term_C_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_C_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(A, B)" : String))
}

-- 
def term_C_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(B, A)" : String))
}

-- 
def term_C_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_C_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(compose(A,B), C)" : String))
}

-- 
def term_C_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(A, compose(B,C))" : String))
}

-- 
def term_C_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_C_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(A, A)" : String))
}

-- 
def term_C_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("A" : String))
}

-- 
def term_C_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_C_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(A, ε)" : String))
}

-- 
def term_C_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("A" : String))
}

-- 
def term_C_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_C_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(A, Ω)" : String))
}

-- 
def term_C_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Ω" : String))
}

-- 
def term_DA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CD(R, i)" : String))
}

-- 
def term_DA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("C" : String))
}

-- 
def term_DA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CD(C, j)" : String))
}

-- 
def term_DA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H" : String))
}

-- 
def term_DA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CD(H, l)" : String))
}

-- 
def term_DA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O" : String))
}

-- 
def term_DA_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DA_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("dim(normed_div_alg)" : String))
}

-- 
def term_DA_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∈ {1, 2, 4, 8}" : String))
}

-- 
def term_DA_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DA_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("algebra(L_k)" : String))
}

-- 
def term_DA_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("division_algebra[k]" : String))
}

-- 
def term_DA_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DA_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[a,b] = 0" : String))
}

-- 
def term_DA_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isCommutative(algebra(L_k))" : String))
}

-- 
def term_DA_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DA_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[a,b,c] = 0" : String))
}

-- 
def term_DA_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isAssociative(algebra(L_k))" : String))
}

-- 
def term_DC_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("optimal next constraint" : String))
}

-- 
def term_DC_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("argmax J_k over free sites" : String))
}

-- 
def term_DC_11_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_11_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_{x} J_k(x)" : String))
}

-- 
def term_DC_11_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≈ (2^n - 2)/n for each k" : String))
}

-- 
def term_DC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∂_R f(x)" : String))
}

-- 
def term_DC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("f(succ(x)) - f(x)" : String))
}

-- 
def term_DC_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∂_H f(x)" : String))
}

-- 
def term_DC_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("f(bnot(x)) - f(x)" : String))
}

-- 
def term_DC_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∂_H id(x)" : String))
}

-- 
def term_DC_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(x) - x = -(2x + 1) mod 2^n" : String))
}

-- 
def term_DC_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[neg, bnot](x)" : String))
}

-- 
def term_DC_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∂_R neg(x) - ∂_H neg(x)" : String))
}

-- 
def term_DC_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∂_R f - ∂_H f" : String))
}

-- 
def term_DC_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ carry contributions" : String))
}

-- 
def term_DC_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("J_k(x)" : String))
}

-- 
def term_DC_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∂_R f_k(x) where f_k = site_k" : String))
}

-- 
def term_DC_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("J_{n-1}(x)" : String))
}

-- 
def term_DC_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("may differ from lower sites" : String))
}

-- 
def term_DC_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("rank(J(x))" : String))
}

-- 
def term_DC_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= d_H(x, succ(x)) - 1 for generic x" : String))
}

-- 
def term_DC_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DC_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("κ(x)" : String))
}

-- 
def term_DC_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_k J_k(x)" : String))
}

-- 
def term_DD_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DD_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ(q, R)" : String))
}

-- 
def term_DD_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ(q, R)" : String))
}

-- 
def term_DD_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DD_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("dom(R)" : String))
}

-- 
def term_DD_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{q | ∃r. δ(q,R)=r}" : String))
}

-- 
def term_DIS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DIS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isExhaustive(D) ∧ isMutuallyExclusive(D)" : String))
}

-- 
def term_DIS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("true" : String))
}

-- 
def term_DIS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DIS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("dispatch(D, T)" : String))
}

-- 
def term_DIS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("exactly one Resolver" : String))
}

-- 
def term_DV_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DV_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("div(a, 1)" : String))
}

-- 
def term_DV_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("a" : String))
}

-- 
def term_DV_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DV_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("div(0, b)" : String))
}

-- 
def term_DV_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_DV_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DV_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("div(mul(a, b), b)" : String))
}

-- 
def term_DV_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("a" : String))
}

-- 
def term_DV_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_DV_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("a" : String))
}

-- 
def term_DV_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(mul(div(a, b), b), mod(a, b))" : String))
}

-- 
def term_D_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_D_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("succ^{2^n}(x)" : String))
}

-- 
def term_D_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_D_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_D_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(succ(neg(x)))" : String))
}

-- 
def term_D_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pred(x)" : String))
}

-- 
def term_D_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_D_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(neg(x))" : String))
}

-- 
def term_D_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pred(x)" : String))
}

-- 
def term_D_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_D_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("D_{2^n}" : String))
}

-- 
def term_D_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{succ^k, neg ∘ succ^k : 0 ≤ k < 2^n}" : String))
}

-- 
def term_D_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_D_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(rᵃ sᵖ, rᵇ sᵠ)" : String))
}

-- 
def term_D_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("r^((a + (-1)ᵖ b) mod 2ⁿ) s^(p xor q)" : String))
}

-- 
def term_D_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_D_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(r^a s^p)^(-1)" : String))
}

-- 
def term_D_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("r^(-(−1)^p a mod 2^n) s^p" : String))
}

-- 
def term_D_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_D_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ord(r^k s^1)" : String))
}

-- 
def term_D_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2" : String))
}

-- 
def term_EA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("free(epoch(n+1))" : String))
}

-- 
def term_EA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("free(epoch(0))" : String))
}

-- 
def term_EA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("grounded(epoch(n))" : String))
}

-- 
def term_EA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("grounded(epoch(n+1))" : String))
}

-- 
def term_EA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|context(w)|" : String))
}

-- 
def term_EA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("windowSize(w)" : String))
}

-- 
def term_EA_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EA_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("admit(epoch(n))" : String))
}

-- 
def term_EA_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("datum | refinement" : String))
}

-- 
def term_EC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Ω⁶" : String))
}

-- 
def term_EC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("−1" : String))
}

-- 
def term_EC_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("conj(conj(z))" : String))
}

-- 
def term_EC_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("z" : String))
}

-- 
def term_EC_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[q_A, q_B]^inf" : String))
}

-- 
def term_EC_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1" : String))
}

-- 
def term_EC_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[q_A, q_B, q_C]^inf" : String))
}

-- 
def term_EC_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_EC_4a_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_4a_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("||[a,b,c]_{n+1}||" : String))
}

-- 
def term_EC_4a_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("<= ||[a,b,c]_n||" : String))
}

-- 
def term_EC_4b_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_4b_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("steps_to_zero([a,b,c])" : String))
}

-- 
def term_EC_4b_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("<= |three_way_sites|" : String))
}

-- 
def term_EC_4c_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_4c_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[a,b,c] = 0" : String))
}

-- 
def term_EC_4c_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("associative(resolved_site_space)" : String))
}

-- 
def term_EC_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EC_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("max_level(convergence_tower)" : String))
}

-- 
def term_EC_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("L3_Self" : String))
}

-- 
def term_EF_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EF_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_ι(f)" : String))
}

-- 
def term_EF_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι ∘ f ∘ ι⁻¹ on Im(ι)" : String))
}

-- 
def term_EF_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EF_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_ι(f ∘ g)" : String))
}

-- 
def term_EF_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_ι(f) ∘ F_ι(g)" : String))
}

-- 
def term_EF_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EF_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_ι(id_{R_n})" : String))
}

-- 
def term_EF_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("id_{Im(ι)}" : String))
}

-- 
def term_EF_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EF_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_{ι₂ ∘ ι₁}" : String))
}

-- 
def term_EF_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_{ι₂} ∘ F_{ι₁}" : String))
}

-- 
def term_EF_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EF_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_ι(ring isometry)" : String))
}

-- 
def term_EF_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ring isometry at level m" : String))
}

-- 
def term_EF_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EF_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_ι(D_{2^n})" : String))
}

-- 
def term_EF_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊆ D_{2^m} as subgroup" : String))
}

-- 
def term_EF_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EF_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F_ι(R_n×)" : String))
}

-- 
def term_EF_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊆ R_m× as subgroup" : String))
}

-- 
def term_ER_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ER_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("advance(k, k+1)" : String))
}

-- 
def term_ER_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("guard(k) = true" : String))
}

-- 
def term_ER_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ER_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(effect(k))" : String))
}

-- 
def term_ER_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("atomic(effect(k))" : String))
}

-- 
def term_ER_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ER_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("eval(guard(k), s)" : String))
}

-- 
def term_ER_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("s (state unchanged)" : String))
}

-- 
def term_ER_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ER_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(e1; e2)" : String))
}

-- 
def term_ER_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(e2; e1)" : String))
}

-- 
def term_EXP_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EXP_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("carrier(C) is monotone" : String))
}

-- 
def term_EXP_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("all residues of C = modulus - 1, no Carry/Depth" : String))
}

-- 
def term_EXP_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EXP_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("count of monotone ConstrainedTypes at Q_n" : String))
}

-- 
def term_EXP_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2^n" : String))
}

-- 
def term_EXP_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_EXP_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("carrier(SumType(A,B))" : String))
}

-- 
def term_EXP_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("coproduct(carrier(A), carrier(B))" : String))
}

-- 
def term_FA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pending(q)" : String))
}

-- 
def term_FA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("reaches_gate(q)" : String))
}

-- 
def term_FA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("admitted(q, epoch)" : String))
}

-- 
def term_FA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("served(q, epoch + k)" : String))
}

-- 
def term_FA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lease_alloc(p1 + p2)" : String))
}

-- 
def term_FA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lease_alloc(p1) + lease_alloc(p2)" : String))
}

-- 
def term_FLR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FLR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("result(P)" : String))
}

-- 
def term_FLR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∈ {Success, Failure}" : String))
}

-- 
def term_FLR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FLR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("result(T)" : String))
}

-- 
def term_FLR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Success" : String))
}

-- 
def term_FLR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FLR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("result(A ⊗ B)" : String))
}

-- 
def term_FLR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Failure(A)" : String))
}

-- 
def term_FLR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FLR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("result(A ∥ B)" : String))
}

-- 
def term_FLR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Failure(A) (left component)" : String))
}

-- 
def term_FLR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FLR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("result(apply(recoveryEffect(r), failureState(f)))" : String))
}

-- 
def term_FLR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ComputationResult" : String))
}

-- 
def term_FLR_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FLR_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("recoveryEffect(rollback(f))" : String))
}

-- 
def term_FLR_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("PhaseEffect(conjugate)" : String))
}

-- 
def term_FL_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FL_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊥" : String))
}

-- 
def term_FL_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("all sites free (freeRank = n)" : String))
}

-- 
def term_FL_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FL_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊤" : String))
}

-- 
def term_FL_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("all sites pinned (pinnedCount = n)" : String))
}

-- 
def term_FL_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FL_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("join(S₁, S₂)" : String))
}

-- 
def term_FL_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("union of pinnings from S₁ and S₂" : String))
}

-- 
def term_FL_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FL_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lattice height" : String))
}

-- 
def term_FL_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n" : String))
}

-- 
def term_FPM_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∈ Unit" : String))
}

-- 
def term_FPM_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("site_0(x) = 1 (x is odd)" : String))
}

-- 
def term_FPM_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∈ Exterior" : String))
}

-- 
def term_FPM_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∉ carrier(T)" : String))
}

-- 
def term_FPM_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∈ Irreducible" : String))
}

-- 
def term_FPM_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∉ Unit ∪ Exterior AND no non-trivial factorization" : String))
}

-- 
def term_FPM_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∈ Reducible" : String))
}

-- 
def term_FPM_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∉ Unit ∪ Exterior ∪ Irreducible" : String))
}

-- 
def term_FPM_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x = 2^{v(x)} ⋅ u" : String))
}

-- 
def term_FPM_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("u odd, v(x) = min position of 1-bit" : String))
}

-- 
def term_FPM_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|{x: v(x) = k}|" : String))
}

-- 
def term_FPM_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2^{n−k−1} for 0 < k < n; 1 for k = n" : String))
}

-- 
def term_FPM_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("base type partition" : String))
}

-- 
def term_FPM_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|Unit| = 2^{n−1}; |Irr| = 2^{n−2}; |Red| = 2^{n−2}" : String))
}

-- 
def term_FPM_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|Irr| + |Red| + |Unit| + |Ext|" : String))
}

-- 
def term_FPM_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2ⁿ" : String))
}

-- 
def term_FPM_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FPM_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∈ Ext(T)" : String))
}

-- 
def term_FPM_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x ∉ carrier(T)" : String))
}

-- 
def term_FS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("site_k(x)" : String))
}

-- 
def term_FS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(x >> k) AND 1" : String))
}

-- 
def term_FS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("site_0(x)" : String))
}

-- 
def term_FS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x mod 2 (parity)" : String))
}

-- 
def term_FS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("site_k(x) given sites 0..k−1" : String))
}

-- 
def term_FS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("determines x mod 2^{k+1}" : String))
}

-- 
def term_FS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sites 0..k together" : String))
}

-- 
def term_FS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("determine x mod 2^{k+1}" : String))
}

-- 
def term_FS_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FS_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("all n sites" : String))
}

-- 
def term_FS_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("determine x uniquely" : String))
}

-- 
def term_FS_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FS_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("stratum(x)" : String))
}

-- 
def term_FS_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("v_2(x) = min{k : site_k(x) = 1}" : String))
}

-- 
def term_FS_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FS_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("depth(x)" : String))
}

-- 
def term_FS_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ v_2(x) for irreducible elements" : String))
}

-- 
def term_FX_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FX_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(postContext(e))" : String))
}

-- 
def term_FX_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(preContext(e)) − 1" : String))
}

-- 
def term_FX_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FX_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(postContext(e))" : String))
}

-- 
def term_FX_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(preContext(e)) + 1" : String))
}

-- 
def term_FX_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FX_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(postContext(e))" : String))
}

-- 
def term_FX_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(preContext(e))" : String))
}

-- 
def term_FX_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FX_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(A ; B, ctx)" : String))
}

-- 
def term_FX_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(B ; A, ctx)" : String))
}

-- 
def term_FX_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FX_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRankDelta(E₁ ; E₂)" : String))
}

-- 
def term_FX_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRankDelta(E₁) + freeRankDelta(E₂)" : String))
}

-- 
def term_FX_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FX_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(e, apply(e⁻¹, ctx))" : String))
}

-- 
def term_FX_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ctx" : String))
}

-- 
def term_FX_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_FX_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRankDelta(e)" : String))
}

-- 
def term_FX_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("declared freeRankDelta in EffectShape" : String))
}

-- 
def term_F_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_F_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinned site" : String))
}

-- 
def term_F_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cannot be unpinned" : String))
}

-- 
def term_F_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_F_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pin operations to close" : String))
}

-- 
def term_F_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ n" : String))
}

-- 
def term_F_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_F_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinnedCount + freeRank" : String))
}

-- 
def term_F_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("totalSites = n" : String))
}

-- 
def term_F_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_F_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isClosed" : String))
}

-- 
def term_F_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank = 0 ⇔ pinnedCount = n" : String))
}

-- 
def term_GC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("shared-frame grounding of symbol s" : String))
}

-- 
def term_GC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ c: GroundingCertificate. certifies(c, map)" : String))
}

-- 
def term_GD_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GD_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isGeodesic(T)" : String))
}

-- 
def term_GD_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("AR_1-ordered(T) ∧ DC_10-selected(T)" : String))
}

-- 
def term_GD_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GD_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ΔS_step(i) on geodesic" : String))
}

-- 
def term_GD_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ln 2" : String))
}

-- 
def term_GD_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GD_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Cost_geodesic(T)" : String))
}

-- 
def term_GD_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank_initial × k_B T × ln 2" : String))
}

-- 
def term_GD_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GD_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Cost(T) for geodesic T" : String))
}

-- 
def term_GD_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= Cost(T') for any geodesic T' on same type" : String))
}

-- 
def term_GD_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GD_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isSubgeodesic(T)" : String))
}

-- 
def term_GD_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ i: J_k(step_i) < max_{free} J_k(state_i)" : String))
}

-- 
def term_GD_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GD_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isGeodesic(trace)" : String))
}

-- 
def term_GD_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isAR1Ordered(trace) ∧ isDC10Selected(trace)" : String))
}

-- 
def term_GL_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GL_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(T)" : String))
}

-- 
def term_GL_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lower_adjoint(T)" : String))
}

-- 
def term_GL_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GL_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("r(T)" : String))
}

-- 
def term_GL_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1 − upper_adjoint(T)" : String))
}

-- 
def term_GL_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GL_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("upper(lower(T))" : String))
}

-- 
def term_GL_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("T (fixpoint: σ=1, r=0)" : String))
}

-- 
def term_GL_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GL_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("T₁ ≤ T₂ in type lattice" : String))
}

-- 
def term_GL_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("site(T₂) ⊆ site(T₁)" : String))
}

-- 
def term_GO_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GO_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinsSites(killing constraint for obstruction c)" : String))
}

-- 
def term_GO_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("superset of pinsSites(C_i) cap pinsSites(C_j)" : String))
}

-- 
def term_GR_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("finalState(R, P_1, Q)" : String))
}

-- 
def term_GR_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= finalState(R, P_2, Q) for any P_1, P_2: ExecutionPolicy" : String))
}

-- 
def term_GR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(B_{i+1})" : String))
}

-- 
def term_GR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ freeRank(B_i)" : String))
}

-- 
def term_GR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("b.datum resolves under b.constraint" : String))
}

-- 
def term_GR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("in O(1) iff Binding b is sound" : String))
}

-- 
def term_GR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ i: freeRank(B_i) = 0" : String))
}

-- 
def term_GR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Session S converges" : String))
}

-- 
def term_GR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bindings(C_fresh) ∩ bindings(C_prior)" : String))
}

-- 
def term_GR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= ∅ after SessionBoundary" : String))
}

-- 
def term_GR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ContradictionBoundary" : String))
}

-- 
def term_GR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("iff ∃ b, b': same address, different datum, same constraint" : String))
}

-- 
def term_GR_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(q) after grounding" : String))
}

-- 
def term_GR_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sites of q not in BindingAccumulator" : String))
}

-- 
def term_GR_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sigma after re-entry with query q" : String))
}

-- 
def term_GR_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("min(sigma, 1 - freeRank(q)/n)" : String))
}

-- 
def term_GR_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(S_A, S_B) valid at Q_k" : String))
}

-- 
def term_GR_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∀ j ≤ k: ∀ a ∈ pinnedSites(S_A, Q_j) ∩ pinnedSites(S_B, Q_j): datum(S_A, a, Q_j) = datum(S_B, a, Q_j)" : String))
}

-- 
def term_GR_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GR_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("leasedSites(L_A) ∩ leasedSites(L_B)" : String))
}

-- 
def term_GR_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= ∅" : String))
}

-- 
def term_GS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("T_ctx(C)" : String))
}

-- 
def term_GS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(C) × ln 2 / n" : String))
}

-- 
def term_GS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(C)" : String))
}

-- 
def term_GS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(n − freeRank(C)) / n" : String))
}

-- 
def term_GS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(B_{i+1})" : String))
}

-- 
def term_GS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ σ(B_i)" : String))
}

-- 
def term_GS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(C) = 1" : String))
}

-- 
def term_GS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(C) = 0 ↔ S(C) = 0 ↔ T_ctx(C) = 0" : String))
}

-- 
def term_GS_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GS_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("stepCount(q, C) at freeRank(C) = 0" : String))
}

-- 
def term_GS_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_GS_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GS_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("effectiveBudget(q, C)" : String))
}

-- 
def term_GS_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("max(0, siteBudget(q.type) − |pinnedSites(C) ∩ q.siteSet|)" : String))
}

-- 
def term_GS_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_GS_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Cost_saturation(C)" : String))
}

-- 
def term_GS_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n × k_B T × ln 2" : String))
}

-- 
def term_HA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("N(C)" : String))
}

-- 
def term_HA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("simplicial complex on constraints" : String))
}

-- 
def term_HA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolution stalls" : String))
}

-- 
def term_HA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⟺ H_k(N(C)) ≠ 0 for some k > 0" : String))
}

-- 
def term_HA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_residual" : String))
}

-- 
def term_HA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ Σ_k β_k × ln 2" : String))
}

-- 
def term_HG_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HG_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("additive holonomy" : String))
}

-- 
def term_HG_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("trivial (abelian ⇒ path-independent)" : String))
}

-- 
def term_HG_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HG_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{neg, bnot, succ, pred} holonomy" : String))
}

-- 
def term_HG_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("D_{2^n}" : String))
}

-- 
def term_HG_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HG_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{mul(•, u) : u ∈ R_n×} holonomy" : String))
}

-- 
def term_HG_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("R_n× ≅ Z/2 × Z/2^{n−2}" : String))
}

-- 
def term_HG_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HG_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Hol(R_n)" : String))
}

-- 
def term_HG_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Aff(R_n) = R_n× ⋉ R_n" : String))
}

-- 
def term_HG_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HG_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Hol(R_n) decomposition" : String))
}

-- 
def term_HG_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("D_{2^n} ⋅ {mul(•,u) : u ∈ R_n×, u ≠ ±1}" : String))
}

-- 
def term_HO_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HO_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("value(c)" : String))
}

-- 
def term_HO_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("contentHash(referencedCertificate(c))" : String))
}

-- 
def term_HO_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HO_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(output(app))" : String))
}

-- 
def term_HO_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(applicationTarget(app))" : String))
}

-- 
def term_HO_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HO_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(f ∘ g)" : String))
}

-- 
def term_HO_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(f) ∧ cert(g) ∧ range(g) = domain(f)" : String))
}

-- 
def term_HO_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HO_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("p" : String))
}

-- 
def term_HO_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ApplicationMorphism(partialBase(p), boundArguments(p))" : String))
}

-- 
def term_HP_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HP_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_7 ∘ ψ_1" : String))
}

-- 
def term_HP_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Kan promotion of nerve" : String))
}

-- 
def term_HP_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HP_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_8(τ≤k) restricted" : String))
}

-- 
def term_HP_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_3(C≤k)" : String))
}

-- 
def term_HP_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HP_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_9 detects convergence" : String))
}

-- 
def term_HP_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("spectral sequence converges at E_{d+2}" : String))
}

-- 
def term_HP_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HP_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HomotopyResolver time" : String))
}

-- 
def term_HP_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(n^{d+1})" : String))
}

-- 
def term_HT_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("N(C)" : String))
}

-- 
def term_HT_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("KanComplex" : String))
}

-- 
def term_HT_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("π₀(N(C))" : String))
}

-- 
def term_HT_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Z^{β₀}" : String))
}

-- 
def term_HT_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("π₁(N(C)) → D_{2^n}" : String))
}

-- 
def term_HT_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HolonomyGroup factorization" : String))
}

-- 
def term_HT_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("π_k(N(C)) for k > dim(N(C))" : String))
}

-- 
def term_HT_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_HT_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("τ_{≤1}(N(C))" : String))
}

-- 
def term_HT_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("FlatType/TwistedType classification" : String))
}

-- 
def term_HT_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("κ_k trivial for all k > d" : String))
}

-- 
def term_HT_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("spectral sequence collapses at E_{d+2}" : String))
}

-- 
def term_HT_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[α, β] ≠ 0 in π_{p+q−1}" : String))
}

-- 
def term_HT_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("LiftObstruction non-trivial" : String))
}

-- 
def term_HT_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_HT_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("π_k(N(C)) ⊗ Z" : String))
}

-- 
def term_HT_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H_k(N(C); Z) for smallest k with π_k ≠ 0" : String))
}

-- 
def term_IH_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IH_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("InhabitanceCertificate(T).verified" : String))
}

-- 
def term_IH_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("carrier(T) ≠ ∅" : String))
}

-- 
def term_IH_2a_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IH_2a_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(TwoSatDecider, T)" : String))
}

-- 
def term_IH_2a_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(n + m)" : String))
}

-- 
def term_IH_2b_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IH_2b_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(HornSatDecider, T)" : String))
}

-- 
def term_IH_2b_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(n + m)" : String))
}

-- 
def term_IH_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IH_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("carrier(reduce(T))" : String))
}

-- 
def term_IH_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("carrier(T)" : String))
}

-- 
def term_IN_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(A,B)" : String))
}

-- 
def term_IN_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("interaction_cost(A,B)" : String))
}

-- 
def term_IN_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("disjoint_leases(A,B)" : String))
}

-- 
def term_IN_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("commutator(A,B) = 0" : String))
}

-- 
def term_IN_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("shared_sites(A,B) ≠ ∅" : String))
}

-- 
def term_IN_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("commutator(A,B) > 0" : String))
}

-- 
def term_IN_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("SR_8_session(A,B)" : String))
}

-- 
def term_IN_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("commutator(A,B,t+1) ≤ commutator(A,B,t)" : String))
}

-- 
def term_IN_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("converged_negotiation(A,B)" : String))
}

-- 
def term_IN_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("U(1) ⊂ SU(2)" : String))
}

-- 
def term_IN_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("outcome_space(pairwise_negotiation)" : String))
}

-- 
def term_IN_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S²" : String))
}

-- 
def term_IN_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("converged_mutual_model(A,B,C)" : String))
}

-- 
def term_IN_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H ⊂ O" : String))
}

-- 
def term_IN_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(interaction_nerve)" : String))
}

-- 
def term_IN_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("coupling_complexity(k)" : String))
}

-- 
def term_IN_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IN_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_2(nerve) × max_disagreement" : String))
}

-- 
def term_IN_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("upper_bound(associator_norm)" : String))
}

-- 
def term_IO_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IO_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("type(resultDatum(e))" : String))
}

-- 
def term_IO_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("conformsTo(sourceType(s))" : String))
}

-- 
def term_IO_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IO_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("type(emittedDatum(e))" : String))
}

-- 
def term_IO_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("conformsTo(sinkType(s))" : String))
}

-- 
def term_IO_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IO_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(g, ingest(s))" : String))
}

-- 
def term_IO_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Datum in R_n" : String))
}

-- 
def term_IO_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IO_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(p, d)" : String))
}

-- 
def term_IO_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("surface symbol conforming to sinkType(s)" : String))
}

-- 
def term_IO_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IO_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("effect:effectTarget(e)" : String))
}

-- 
def term_IO_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("non-empty EffectTarget" : String))
}

-- 
def term_IR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinnedCount(state_{i+1})" : String))
}

-- 
def term_IR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ pinnedCount(state_i)" : String))
}

-- 
def term_IR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("iterations to converge" : String))
}

-- 
def term_IR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ n" : String))
}

-- 
def term_IR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("convergenceRate" : String))
}

-- 
def term_IR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinnedCount / iterationCount" : String))
}

-- 
def term_IR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraint set spans all sites" : String))
}

-- 
def term_IR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("loop terminates" : String))
}

-- 
def term_IT_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IT_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C))" : String))
}

-- 
def term_IT_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_k (-1)^k β_k" : String))
}

-- 
def term_IT_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IT_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C))" : String))
}

-- 
def term_IT_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_k (-1)^k dim(H_k)" : String))
}

-- 
def term_IT_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IT_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("λ_1(N(C))" : String))
}

-- 
def term_IT_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lower bounds convergence rate" : String))
}

-- 
def term_IT_7a_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IT_7a_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ κ_k - χ(N(C))" : String))
}

-- 
def term_IT_7a_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= S_residual / ln 2" : String))
}

-- 
def term_IT_7b_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IT_7b_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_residual" : String))
}

-- 
def term_IT_7b_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= (Σ κ_k - χ) × ln 2" : String))
}

-- 
def term_IT_7c_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IT_7c_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolution cost" : String))
}

-- 
def term_IT_7c_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ n - χ(N(C))" : String))
}

-- 
def term_IT_7d_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_IT_7d_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolution complete" : String))
}

-- 
def term_IT_7d_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⟺ χ(N(C)) = n and all β_k = 0" : String))
}

-- 
def term_LN_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LN_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ targetCount(site_i)" : String))
}

-- 
def term_LN_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n" : String))
}

-- 
def term_LN_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LN_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("status(f, postContext(e))" : String))
}

-- 
def term_LN_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinned" : String))
}

-- 
def term_LN_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LN_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("target(e′) = f" : String))
}

-- 
def term_LN_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("forbidden" : String))
}

-- 
def term_LN_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LN_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("remainingCount(budget after L)" : String))
}

-- 
def term_LN_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("remainingCount(budget before L) − k" : String))
}

-- 
def term_LN_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LN_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("remainingCount(budget after expiry)" : String))
}

-- 
def term_LN_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("remainingCount(budget before expiry) + leaseCardinality(L)" : String))
}

-- 
def term_LN_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LN_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("G" : String))
}

-- 
def term_LN_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("LinearTrace" : String))
}

-- 
def term_LS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinned(suspend(lease))" : String))
}

-- 
def term_LS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pinned(lease)" : String))
}

-- 
def term_LS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resources(expire(lease))" : String))
}

-- 
def term_LS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_LS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("restore(restore(checkpoint))" : String))
}

-- 
def term_LS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("restore(checkpoint)" : String))
}

-- 
def term_LS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_LS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resume(suspend(lease))" : String))
}

-- 
def term_LS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lease" : String))
}

-- 
def term_MC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σᵢ freeRank(leasedSites(L_i))" : String))
}

-- 
def term_MC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= freeRank(C)" : String))
}

-- 
def term_MC_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(B_{i+1} |_L)" : String))
}

-- 
def term_MC_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ freeRank(B_i |_L)" : String))
}

-- 
def term_MC_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(compose(S_A, S_B))" : String))
}

-- 
def term_MC_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(S_A) + freeRank(S_B) − |pinnedSites(S_A) ∩ pinnedSites(S_B)|" : String))
}

-- 
def term_MC_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank(compose(S_A, S_B))" : String))
}

-- 
def term_MC_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= freeRank(S_A) + freeRank(S_B)" : String))
}

-- 
def term_MC_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("finalBindings(R, P_1, Q)" : String))
}

-- 
def term_MC_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= finalBindings(R, P_2, Q)" : String))
}

-- 
def term_MC_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(compose(S_1, …, S_k))" : String))
}

-- 
def term_MC_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= 1 (FullGrounding)" : String))
}

-- 
def term_MC_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("stepCount(q, C*)" : String))
}

-- 
def term_MC_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= 0" : String))
}

-- 
def term_MC_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MC_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("max_i stepCount(S_i to convergence within L_i)" : String))
}

-- 
def term_MC_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ ⌈n/k⌉" : String))
}

-- 
def term_MD_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("site(ModuliTowerMap, T)" : String))
}

-- 
def term_MD_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("empty iff TwistedType at every level" : String))
}

-- 
def term_MD_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("dim(M_n)" : String))
}

-- 
def term_MD_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("basisSize(T)" : String))
}

-- 
def term_MD_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^0(Def(T))" : String))
}

-- 
def term_MD_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Aut(T) ∩ D_{2^n}" : String))
}

-- 
def term_MD_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^1(Def(T))" : String))
}

-- 
def term_MD_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("T_T(M_n)" : String))
}

-- 
def term_MD_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^2(Def(T))" : String))
}

-- 
def term_MD_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("LiftObstruction space" : String))
}

-- 
def term_MD_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("FlatType stratum codimension" : String))
}

-- 
def term_MD_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_MD_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("TwistedType stratum codimension" : String))
}

-- 
def term_MD_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ 1" : String))
}

-- 
def term_MD_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("VersalDeformation existence" : String))
}

-- 
def term_MD_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("guaranteed when H^2 = 0" : String))
}

-- 
def term_MD_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("familyPreservesCompleteness" : String))
}

-- 
def term_MD_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^2(Def(T_t)) = 0 along path" : String))
}

-- 
def term_MD_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MD_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("site(ModuliTowerMap, T) dimension" : String))
}

-- 
def term_MD_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1 when obstructionTrivial" : String))
}

-- 
def term_MN_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HolonomyGroup(T)" : String))
}

-- 
def term_MN_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ D_{2^n}" : String))
}

-- 
def term_MN_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HolonomyGroup(T) for additive constraints" : String))
}

-- 
def term_MN_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{id} (trivial: T is FlatType)" : String))
}

-- 
def term_MN_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HolonomyGroup(T) with neg + bnot in closed path" : String))
}

-- 
def term_MN_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("D_{2^n} (full dihedral holonomy)" : String))
}

-- 
def term_MN_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HolonomyGroup(T) ≠ {id}" : String))
}

-- 
def term_MN_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⟹ β₁(N(C(T))) ≥ 1" : String))
}

-- 
def term_MN_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CompleteType (IT_7d) ⟹ β₁ = 0 ⟹ holonomy" : String))
}

-- 
def term_MN_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("trivial ⟹ FlatType" : String))
}

-- 
def term_MN_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("monodromy(p₁ · p₂)" : String))
}

-- 
def term_MN_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("monodromy(p₁) · monodromy(p₂) in D_{2^n}" : String))
}

-- 
def term_MN_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("TwistedType T ⟹ H²(N(C(T')); ℤ/2ℤ)" : String))
}

-- 
def term_MN_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("non-zero class (non-trivial LiftObstruction)" : String))
}

-- 
def term_MN_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MN_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("holonomyClassified(T)" : String))
}

-- 
def term_MN_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isFlatType(T) xor isTwistedType(T)" : String))
}

-- 
def term_MO_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MO_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("I ⊗ A" : String))
}

-- 
def term_MO_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("A" : String))
}

-- 
def term_MO_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MO_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(A⊗B)⊗C" : String))
}

-- 
def term_MO_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("A⊗(B⊗C)" : String))
}

-- 
def term_MO_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MO_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(A⊗B)" : String))
}

-- 
def term_MO_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(A) ∧ cert(B)" : String))
}

-- 
def term_MO_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MO_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("σ(A⊗B)" : String))
}

-- 
def term_MO_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("max(σ(A), σ(B))" : String))
}

-- 
def term_MO_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MO_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("r(A⊗B)" : String))
}

-- 
def term_MO_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("min(r(A), r(B))" : String))
}

-- 
def term_MR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ModuliResolver boundary" : String))
}

-- 
def term_MR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("MorphospaceBoundary" : String))
}

-- 
def term_MR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("StratificationRecord coverage" : String))
}

-- 
def term_MR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("every CompleteType in exactly one stratum" : String))
}

-- 
def term_MR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ModuliResolver complexity" : String))
}

-- 
def term_MR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(n × basisSize²)" : String))
}

-- 
def term_MR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("achievabilityStatus = Achievable" : String))
}

-- 
def term_MR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("signature in some HolonomyStratum" : String))
}

-- 
def term_MS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β₀(N(C))" : String))
}

-- 
def term_MS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ 1" : String))
}

-- 
def term_MS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C))" : String))
}

-- 
def term_MS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ n" : String))
}

-- 
def term_MS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C + c))" : String))
}

-- 
def term_MS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ χ(N(C))" : String))
}

-- 
def term_MS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("achievable(χ*, β_k*, n)" : String))
}

-- 
def term_MS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("→ achievable(χ*, β_k*, n+1)" : String))
}

-- 
def term_MS_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_MS_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("verified SynthesisSignature set" : String))
}

-- 
def term_MS_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("→ exact morphospace boundary in the limit" : String))
}

-- 
def term_NV_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_NV_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("nerve(C₁ ∪ C₂)" : String))
}

-- 
def term_NV_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("nerve(C₁) ∪ nerve(C₂)" : String))
}

-- 
def term_NV_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_NV_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(C₁ ∪ C₂)" : String))
}

-- 
def term_NV_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(C₁) + β_k(C₂) − β_k(C₁ ∩ C₂)" : String))
}

-- 
def term_NV_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_NV_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Δβ_k" : String))
}

-- 
def term_NV_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∈ {−1, 0, +1}" : String))
}

-- 
def term_NV_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_NV_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(C ∪ {c})" : String))
}

-- 
def term_NV_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ β_k(C)" : String))
}

-- 
def term_OA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|2|_2 · |2|_∞" : String))
}

-- 
def term_OA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1 in Q×" : String))
}

-- 
def term_OA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CrossingCost(p=2)" : String))
}

-- 
def term_OA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ln 2 = −ln|2|_2" : String))
}

-- 
def term_OA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β* in Cost_Landauer" : String))
}

-- 
def term_OA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CrossingCost(p=2)" : String))
}

-- 
def term_OA_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OA_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("P(outcome k) = |α_k|_∞²" : String))
}

-- 
def term_OA_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Archimedean image of 2-adic amplitude" : String))
}

-- 
def term_OA_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OA_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Information cost of delta (division by 2); grounds MultiplicationCertificate" : String))
}

-- 
def term_OA_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ln 2 nats per MultiplicationCertificate sub-multiplication" : String))
}

-- 
def term_OB_C1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_C1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[neg, bnot](x)" : String))
}

-- 
def term_OB_C1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2" : String))
}

-- 
def term_OB_C2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_C2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[neg, add(•,k)](x)" : String))
}

-- 
def term_OB_C2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("−2k mod 2^n" : String))
}

-- 
def term_OB_C3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_C3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("[bnot, xor(•,k)](x)" : String))
}

-- 
def term_OB_C3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_OB_H1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_H1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("closed additive path monodromy" : String))
}

-- 
def term_OB_H1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("trivial (abelian ⇒ path-independent)" : String))
}

-- 
def term_OB_H2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_H2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("closed {neg,bnot} path monodromy" : String))
}

-- 
def term_OB_H2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∈ D_{2^n}" : String))
}

-- 
def term_OB_H3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_H3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("succ-only path WindingNumber" : String))
}

-- 
def term_OB_H3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("path length / 2^n" : String))
}

-- 
def term_OB_M1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_M1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_R(x, z)" : String))
}

-- 
def term_OB_M1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ d_R(x, y) + d_R(y, z)" : String))
}

-- 
def term_OB_M2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_M2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_H(x, z)" : String))
}

-- 
def term_OB_M2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ d_H(x, y) + d_H(y, z)" : String))
}

-- 
def term_OB_M3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_M3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(x, y)" : String))
}

-- 
def term_OB_M3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|d_R(x, y) − d_H(x, y)|" : String))
}

-- 
def term_OB_M4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_M4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_R(neg(x), neg(y))" : String))
}

-- 
def term_OB_M4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_R(x, y)" : String))
}

-- 
def term_OB_M5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_M5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_H(bnot(x), bnot(y))" : String))
}

-- 
def term_OB_M5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_H(x, y)" : String))
}

-- 
def term_OB_M6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_M6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_R(succ(x), succ(y))" : String))
}

-- 
def term_OB_M6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_R(x, y) but d_H may differ" : String))
}

-- 
def term_OB_P1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_P1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("PathLength(p₁ ⋅ p₂)" : String))
}

-- 
def term_OB_P1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("PathLength(p₁) + PathLength(p₂)" : String))
}

-- 
def term_OB_P2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_P2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("TotalVariation(p₁ ⋅ p₂)" : String))
}

-- 
def term_OB_P2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ TotalVariation(p₁) + TotalVariation(p₂)" : String))
}

-- 
def term_OB_P3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OB_P3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ReductionLength(c₁ ; c₂)" : String))
}

-- 
def term_OB_P3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ReductionLength(c₁) + ReductionLength(c₂)" : String))
}

-- 
def term_OE_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OE_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("stage(k); stage(k+1)" : String))
}

-- 
def term_OE_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("fused(k, k+1)" : String))
}

-- 
def term_OE_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OE_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("effect(a); effect(b)" : String))
}

-- 
def term_OE_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("effect(b); effect(a)" : String))
}

-- 
def term_OE_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OE_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lease(A); lease(B)" : String))
}

-- 
def term_OE_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lease(A) || lease(B)" : String))
}

-- 
def term_OE_4a_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OE_4a_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sem(fused(k, k+1))" : String))
}

-- 
def term_OE_4a_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sem(stage(k)); sem(stage(k+1))" : String))
}

-- 
def term_OE_4b_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OE_4b_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("outcome(a; b)" : String))
}

-- 
def term_OE_4b_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("outcome(b; a)" : String))
}

-- 
def term_OE_4c_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OE_4c_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("coverage(A || B)" : String))
}

-- 
def term_OE_4c_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("coverage(A) + coverage(B)" : String))
}

-- 
def term_OO_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OO_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("benefit(C_i, S)" : String))
}

-- 
def term_OO_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|pins(C_i) ∖ S|" : String))
}

-- 
def term_OO_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OO_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cost(C_i)" : String))
}

-- 
def term_OO_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("step_{m_i} or popcount(p_i)" : String))
}

-- 
def term_OO_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OO_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("greedy selection" : String))
}

-- 
def term_OO_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("argmax benefit(C_i, S) / cost(C_i)" : String))
}

-- 
def term_OO_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OO_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("greedy approximation" : String))
}

-- 
def term_OO_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(1 − 1/e) ≈ 63% of optimal" : String))
}

-- 
def term_OO_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OO_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("equal cost tiebreaker" : String))
}

-- 
def term_OO_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("prefer vertical (residue) before horizontal (carry)" : String))
}

-- 
def term_OP_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OP_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("siteCount(F(G))" : String))
}

-- 
def term_OP_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F.sites + Σ_i G_i.sites" : String))
}

-- 
def term_OP_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OP_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("grounding(F(G(x)))" : String))
}

-- 
def term_OP_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F.ground(G.ground(x))" : String))
}

-- 
def term_OP_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OP_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(F(G))" : String))
}

-- 
def term_OP_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(F) ∘ G + F ∘ d_Δ(G)" : String))
}

-- 
def term_OP_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OP_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Table(Tuple(fields))" : String))
}

-- 
def term_OP_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Sequence(Tuple(fields))" : String))
}

-- 
def term_OP_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_OP_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Tree(Symbol(leaves))" : String))
}

-- 
def term_OP_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Graph(Symbol(leaves), acyclic)" : String))
}

-- 
def term_PAR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PAR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(A ⊗ B, ctx)" : String))
}

-- 
def term_PAR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(B ⊗ A, ctx)" : String))
}

-- 
def term_PAR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PAR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRankDelta(A ∥ B)" : String))
}

-- 
def term_PAR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRankDelta(A) + freeRankDelta(B)" : String))
}

-- 
def term_PAR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PAR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ |component_i|" : String))
}

-- 
def term_PAR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n" : String))
}

-- 
def term_PAR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PAR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("finalContext(σ(A, B))" : String))
}

-- 
def term_PAR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("finalContext(A ⊗ B)" : String))
}

-- 
def term_PAR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PAR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(A ∥ B)" : String))
}

-- 
def term_PAR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("cert(A) ∧ cert(B) ∧ DisjointnessCertificate(A, B)" : String))
}

-- 
def term_PA_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PA_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("α(b₁,α(b₂,C))" : String))
}

-- 
def term_PA_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("α(b₂,α(b₁,C))" : String))
}

-- 
def term_PA_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PA_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sites(α(b,C))" : String))
}

-- 
def term_PA_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sites(C) ∪ {b.site}" : String))
}

-- 
def term_PA_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PA_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraints(α(b,C))" : String))
}

-- 
def term_PA_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraints(C) ∪ constraints(b)" : String))
}

-- 
def term_PA_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PA_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("α(b,C)|_{pinned(C)}" : String))
}

-- 
def term_PA_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("C|_{pinned(C)}" : String))
}

-- 
def term_PA_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PA_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("α(∅, C)" : String))
}

-- 
def term_PA_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("C" : String))
}

-- 
def term_PD_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PD_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("phase space" : String))
}

-- 
def term_PD_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{(n, g) : n ∈ Z₊, g constraint boundary}" : String))
}

-- 
def term_PD_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PD_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("phase boundaries" : String))
}

-- 
def term_PD_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("g | (2^n − 1) or g = 2^k" : String))
}

-- 
def term_PD_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PD_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("parity boundary" : String))
}

-- 
def term_PD_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|Unit| = 2^{n−1}, |non-Unit| = 2^{n−1}" : String))
}

-- 
def term_PD_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PD_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resonance lines" : String))
}

-- 
def term_PD_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n = k ⋅ ord_g(2)" : String))
}

-- 
def term_PD_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PD_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("phase count at level n" : String))
}

-- 
def term_PD_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ 2^n (typical O(n))" : String))
}

-- 
def term_PE_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PE_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("state(ψ, 0)" : String))
}

-- 
def term_PE_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1" : String))
}

-- 
def term_PE_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PE_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_1(q)" : String))
}

-- 
def term_PE_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ(q, R)" : String))
}

-- 
def term_PE_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PE_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_2(r)" : String))
}

-- 
def term_PE_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("G(r)" : String))
}

-- 
def term_PE_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PE_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_3(a)" : String))
}

-- 
def term_PE_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Π(a)" : String))
}

-- 
def term_PE_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PE_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_4(c)" : String))
}

-- 
def term_PE_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("α*(c)" : String))
}

-- 
def term_PE_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PE_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_5(b)" : String))
}

-- 
def term_PE_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("P(b)" : String))
}

-- 
def term_PE_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PE_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ_5 ∘ ψ_4 ∘ ψ_3 ∘ ψ_2 ∘ ψ_1 ∘ ψ_0" : String))
}

-- 
def term_PE_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ" : String))
}

-- 
def term_PI_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PI_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι(ι(s,C),C)" : String))
}

-- 
def term_PI_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι(s,C)" : String))
}

-- 
def term_PI_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PI_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("type(ι(s,C))" : String))
}

-- 
def term_PI_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("consistent(C)" : String))
}

-- 
def term_PI_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PI_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι(s,C)" : String))
}

-- 
def term_PI_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("P(Π(G(s,C)))" : String))
}

-- 
def term_PI_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PI_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("complexity(ι(s,C))" : String))
}

-- 
def term_PI_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(|C|)" : String))
}

-- 
def term_PI_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PI_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("roundTrip(P(Π(G(s))))" : String))
}

-- 
def term_PI_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("s" : String))
}

-- 
def term_PK_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PK_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("valid(κ(S₁,S₂))" : String))
}

-- 
def term_PK_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("disjoint(S₁,S₂)" : String))
}

-- 
def term_PK_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PK_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolve(s, κ(S₁,S₂))" : String))
}

-- 
def term_PK_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolve(s, S₁) ∨ resolve(s, S₂)" : String))
}

-- 
def term_PL_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PL_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Lᵢ ∩ Lⱼ" : String))
}

-- 
def term_PL_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∅" : String))
}

-- 
def term_PL_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PL_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⋃ᵢ Lᵢ" : String))
}

-- 
def term_PL_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S" : String))
}

-- 
def term_PL_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PL_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|{i | f ∈ Lᵢ}|" : String))
}

-- 
def term_PL_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1" : String))
}

-- 
def term_PM_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PM_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("phase(stage_k)" : String))
}

-- 
def term_PM_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Ω^k" : String))
}

-- 
def term_PM_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PM_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("gate(k)" : String))
}

-- 
def term_PM_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("phase(k) == Ω^k" : String))
}

-- 
def term_PM_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PM_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("fail(gate(k))" : String))
}

-- 
def term_PM_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("rollback(z → z̄)" : String))
}

-- 
def term_PM_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PM_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("conj(conj(z))" : String))
}

-- 
def term_PM_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("z" : String))
}

-- 
def term_PM_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PM_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sat(epoch_n)" : String))
}

-- 
def term_PM_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sat(epoch_{n+1})" : String))
}

-- 
def term_PM_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PM_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("context(window)" : String))
}

-- 
def term_PM_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("base_context" : String))
}

-- 
def term_PM_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PM_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ(s₀)" : String))
}

-- 
def term_PM_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ(s₀)" : String))
}

-- 
def term_PP_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PP_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("κ(λₖ(α*(ι(s,·))), C)" : String))
}

-- 
def term_PP_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolve(s, C)" : String))
}

-- 
def term_PR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("eval(p, x)" : String))
}

-- 
def term_PR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∈ {true, false}" : String))
}

-- 
def term_PR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("state(eval(p, x, s))" : String))
}

-- 
def term_PR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("s" : String))
}

-- 
def term_PR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("dispatch(D, x)" : String))
}

-- 
def term_PR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("exactly one DispatchRule" : String))
}

-- 
def term_PR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("eval(M)" : String))
}

-- 
def term_PR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("armResult(first matching arm)" : String))
}

-- 
def term_PR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("advance(k, guardTarget(g))" : String))
}

-- 
def term_PR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("requires guardPredicate(g) = true" : String))
}

-- 
def term_PT_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PT_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("siteBudget(A × B)" : String))
}

-- 
def term_PT_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("siteBudget(A) + siteBudget(B)" : String))
}

-- 
def term_PT_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PT_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("partition(A × B); grounds PartitionCertificate" : String))
}

-- 
def term_PT_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("partition(A) ⊗ partition(B)" : String))
}

-- 
def term_PT_2a_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PT_2a_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Π(A × B)" : String))
}

-- 
def term_PT_2a_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("PartitionProduct(Π(A), Π(B))" : String))
}

-- 
def term_PT_2b_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PT_2b_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Π(A + B)" : String))
}

-- 
def term_PT_2b_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("PartitionCoproduct(Π(A), Π(B))" : String))
}

-- 
def term_PT_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PT_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C(A × B)))" : String))
}

-- 
def term_PT_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C(A))) + χ(N(C(B)))" : String))
}

-- 
def term_PT_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PT_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(A × B)" : String))
}

-- 
def term_PT_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(A) + S(B)" : String))
}

-- 
def term_PW_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PW_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pow(a, 0)" : String))
}

-- 
def term_PW_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1" : String))
}

-- 
def term_PW_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PW_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pow(a, 1)" : String))
}

-- 
def term_PW_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("a" : String))
}

-- 
def term_PW_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_PW_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pow(a, add(b, c))" : String))
}

-- 
def term_PW_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(pow(a, b), pow(a, c))" : String))
}

-- 
def term_QL_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(bnot(x))" : String))
}

-- 
def term_QL_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("succ(x) in Z/(2ⁿ)Z" : String))
}

-- 
def term_QL_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|D_{2ⁿ}|" : String))
}

-- 
def term_QL_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2ⁿ⁺¹ for all n ≥ 1" : String))
}

-- 
def term_QL_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("P(j) = 2^{-j}" : String))
}

-- 
def term_QL_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Boltzmann distribution at β* = ln 2, all n ≥ 1" : String))
}

-- 
def term_QL_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("siteBudget(PrimitiveType, n)" : String))
}

-- 
def term_QL_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= n (one site per bit)" : String))
}

-- 
def term_QL_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolution(CompleteType, n)" : String))
}

-- 
def term_QL_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(1) for all n ≥ 1" : String))
}

-- 
def term_QL_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("contentAddress(x, n)" : String))
}

-- 
def term_QL_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bijection for all n ≥ 1" : String))
}

-- 
def term_QL_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ-pipeline(ConstrainedType, n)" : String))
}

-- 
def term_QL_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("valid ChainComplex for all n ≥ 1" : String))
}

-- 
def term_QL_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QL_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("wittLevelPredecessor(nextWittLevel(Q_k))" : String))
}

-- 
def term_QL_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Q_k" : String))
}

-- 
def term_QM_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QM_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_vonNeumann(ψ)" : String))
}

-- 
def term_QM_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Cost_Landauer(collapse(ψ))" : String))
}

-- 
def term_QM_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QM_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("collapse(ψ)" : String))
}

-- 
def term_QM_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("apply(ResidueConstraint, collapsed_site)" : String))
}

-- 
def term_QM_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QM_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_vN(ψ)" : String))
}

-- 
def term_QM_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∈ [0, ln 2]" : String))
}

-- 
def term_QM_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QM_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("collapse(collapse(ψ))" : String))
}

-- 
def term_QM_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("collapse(ψ)" : String))
}

-- 
def term_QM_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QM_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σᵢ |αᵢ|²" : String))
}

-- 
def term_QM_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("1" : String))
}

-- 
def term_QM_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_QM_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("amplitude index set of SuperposedSiteState over T" : String))
}

-- 
def term_QM_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("monotone pinning trajectories consistent with T" : String))
}

-- 
def term_RC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("reversible pinning of site k" : String))
}

-- 
def term_RC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("store prior state in ancilla site k'" : String))
}

-- 
def term_RC_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RC_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("reversible pinning entropy" : String))
}

-- 
def term_RC_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ΔS_total = 0" : String))
}

-- 
def term_RC_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RC_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("deferred Landauer cost" : String))
}

-- 
def term_RC_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n × k_B T × ln 2 at ancilla erasure" : String))
}

-- 
def term_RC_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RC_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("reversible total cost" : String))
}

-- 
def term_RC_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("= irreversible total cost (redistributed)" : String))
}

-- 
def term_RC_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RC_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("quantum UOR" : String))
}

-- 
def term_RC_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("superposed sites, cost ∝ winning path" : String))
}

-- 
def term_RC_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RC_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("normalize(ψ)" : String))
}

-- 
def term_RC_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ / sqrt(Σ |αᵢ|²)" : String))
}

-- 
def term_RD_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RD_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("same type T and constraint sequence" : String))
}

-- 
def term_RD_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("unique resolved partition" : String))
}

-- 
def term_RD_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RD_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("complete constraint set, any order" : String))
}

-- 
def term_RD_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("same final partition" : String))
}

-- 
def term_RE_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RE_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Π_D(T)" : String))
}

-- 
def term_RE_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Π_C(T) = Π_E(T)" : String))
}

-- 
def term_RG_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RG_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("workingSetRegions(W)" : String))
}

-- 
def term_RG_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("computable from N(C(T)) and stage k site targets" : String))
}

-- 
def term_RG_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RG_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∀ a, b ∈ R: d_R(a, b)" : String))
}

-- 
def term_RG_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ diameter(R)" : String))
}

-- 
def term_RG_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RG_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ workingSetSize(stage_k)" : String))
}

-- 
def term_RG_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ totalAddressableSpace(quantumLevel)" : String))
}

-- 
def term_RG_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_RG_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("addresses accessed by resolver at stage k" : String))
}

-- 
def term_RG_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊆ addresses(W_k)" : String))
}

-- 
def term_R_A1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_A1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, add(y, z))" : String))
}

-- 
def term_R_A1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(add(x, y), z)" : String))
}

-- 
def term_R_A2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_A2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, 0)" : String))
}

-- 
def term_R_A2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_R_A3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_A3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, neg(x))" : String))
}

-- 
def term_R_A3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_R_A4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_A4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, y)" : String))
}

-- 
def term_R_A4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(y, x)" : String))
}

-- 
def term_R_A5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_A5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sub(x, y)" : String))
}

-- 
def term_R_A5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, neg(y))" : String))
}

-- 
def term_R_A6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_A6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(neg(x))" : String))
}

-- 
def term_R_A6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_R_M1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_M1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(x, mul(y, z))" : String))
}

-- 
def term_R_M1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(mul(x, y), z)" : String))
}

-- 
def term_R_M2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_M2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(x, 1)" : String))
}

-- 
def term_R_M2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x" : String))
}

-- 
def term_R_M3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_M3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(x, y)" : String))
}

-- 
def term_R_M3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(y, x)" : String))
}

-- 
def term_R_M4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_M4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(x, add(y, z))" : String))
}

-- 
def term_R_M4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(mul(x, y), mul(x, z))" : String))
}

-- 
def term_R_M5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_R_M5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("mul(x, 0)" : String))
}

-- 
def term_R_M5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_SB_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SB_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraints(T₁)" : String))
}

-- 
def term_SB_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊇ constraints(T₂)" : String))
}

-- 
def term_SB_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SB_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolutions(T₁)" : String))
}

-- 
def term_SB_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("⊆ resolutions(T₂)" : String))
}

-- 
def term_SB_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SB_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("N(C(T₂))" : String))
}

-- 
def term_SB_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sub-complex of N(C(T₁))" : String))
}

-- 
def term_SB_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SB_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F(T₁)" : String))
}

-- 
def term_SB_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ F(T₂)" : String))
}

-- 
def term_SB_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SB_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("F(T₂)" : String))
}

-- 
def term_SB_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ F(T₁)" : String))
}

-- 
def term_SB_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SB_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("latticeDepth" : String))
}

-- 
def term_SB_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n" : String))
}

-- 
def term_SD_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("quantize(value, range, bits)" : String))
}

-- 
def term_SD_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ring element with d_R ∝ |v₁ − v₂|" : String))
}

-- 
def term_SD_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("encoding(alphabet)" : String))
}

-- 
def term_SD_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("argmin_{e} Σ d_Δ(e(a), e(b))" : String))
}

-- 
def term_SD_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Seq(T)" : String))
}

-- 
def term_SD_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Free(T) with backbone ordering" : String))
}

-- 
def term_SD_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sites(Tuple(f₁,...,fₖ))" : String))
}

-- 
def term_SD_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σᵢ sites(fᵢ)" : String))
}

-- 
def term_SD_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("nerve(Graph(V,E))" : String))
}

-- 
def term_SD_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraint_nerve(Graph(V,E))" : String))
}

-- 
def term_SD_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(Set(a,b,c))" : String))
}

-- 
def term_SD_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(Set(σ(a,b,c)))" : String))
}

-- 
def term_SD_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_1(Tree(V,E))" : String))
}

-- 
def term_SD_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_SD_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SD_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Table(S)" : String))
}

-- 
def term_SD_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Seq(Tuple(S))" : String))
}

-- 
def term_SE_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SE_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("EvaluationResolver" : String))
}

-- 
def term_SE_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("directly computes set-theoretic partition" : String))
}

-- 
def term_SE_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SE_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("DihedralFactorizationResolver" : String))
}

-- 
def term_SE_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("orbit decomposition yields same partition" : String))
}

-- 
def term_SE_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SE_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CanonicalFormResolver" : String))
}

-- 
def term_SE_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("confluent rewrite → same partition" : String))
}

-- 
def term_SE_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SE_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Π_D(T) = Π_C(T) = Π_E(T)" : String))
}

-- 
def term_SE_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("all compute same set-theoretic partition" : String))
}

-- 
def term_SF_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SF_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n ≡ 0 (mod ord_g(2))" : String))
}

-- 
def term_SF_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("factor g has optimal resolution at level n" : String))
}

-- 
def term_SF_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SF_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraints with smaller step_g" : String))
}

-- 
def term_SF_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("are more constraining, apply first" : String))
}

-- 
def term_SP_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SP_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolve_superposition(classical(x))" : String))
}

-- 
def term_SP_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolve_classical(x)" : String))
}

-- 
def term_SP_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SP_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("collapse(resolve_superposition(ψ))" : String))
}

-- 
def term_SP_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolve_classical(collapse(ψ))" : String))
}

-- 
def term_SP_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SP_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("amplitudeVector(resolve_superposition(ψ))" : String))
}

-- 
def term_SP_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("normalized" : String))
}

-- 
def term_SP_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SP_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("P(collapse to site k)" : String))
}

-- 
def term_SP_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("|α_k|²" : String))
}

-- 
def term_STR_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_STR_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("reduction(e_k) converges to π" : String))
}

-- 
def term_STR_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("true" : String))
}

-- 
def term_STR_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_STR_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("saturation(continuationContext(b))" : String))
}

-- 
def term_STR_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("saturation(postContext(e_k))" : String))
}

-- 
def term_STR_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_STR_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("computationTime(P)" : String))
}

-- 
def term_STR_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_{i=0}^{k−1} computationTime(epoch_i)" : String))
}

-- 
def term_STR_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_STR_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("epoch_0.context" : String))
}

-- 
def term_STR_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("seed" : String))
}

-- 
def term_STR_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_STR_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("epoch_{k+1}.context" : String))
}

-- 
def term_STR_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("continuationContext(boundary(e_k))" : String))
}

-- 
def term_STR_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_STR_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("linearBudget(epoch_{k+1})" : String))
}

-- 
def term_STR_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("linearBudget(epoch_k) + leaseCardinality(L)" : String))
}

-- 
def term_ST_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(A + B)" : String))
}

-- 
def term_ST_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(A) + β_k(B)" : String))
}

-- 
def term_ST_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("siteBudget(A + B)" : String))
}

-- 
def term_ST_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("max(siteBudget(A), siteBudget(B))" : String))
}

-- 
def term_ST_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(A + B)" : String))
}

-- 
def term_ST_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ln 2 + max(S(A), S(B))" : String))
}

-- 
def term_ST_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("chi(N(C(A+B)))" : String))
}

-- 
def term_ST_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("chi(N(C(A))) + chi(N(C(B)))" : String))
}

-- 
def term_ST_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("beta_k(N(C(A+B)))" : String))
}

-- 
def term_ST_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("beta_k(N(C(A))) + beta_k(N(C(B)))" : String))
}

-- 
def term_ST_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CompleteType(A + B)" : String))
}

-- 
def term_ST_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CompleteType(A) and CompleteType(B) and Q(A)=Q(B)" : String))
}

-- 
def term_ST_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃! tagSite(A + B)" : String))
}

-- 
def term_ST_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("uniqueSite ∉ dataSites(A) ∪ dataSites(B) ∧ carries ln 2 entropy (ST_2)" : String))
}

-- 
def term_ST_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraints(A + B)" : String))
}

-- 
def term_ST_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraints(A) ∪ {tag=0} ∪ constraints(B) ∪ {tag=1}" : String))
}

-- 
def term_ST_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("disjoint(N(C(A)), N(C(B)))" : String))
}

-- 
def term_ST_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("true" : String))
}

-- 
def term_ST_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_ST_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C(A + B)))" : String))
}

-- 
def term_ST_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C(A))) + χ(N(C(B)))" : String))
}

-- 
def term_SW_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SW_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("memory(window(w))" : String))
}

-- 
def term_SW_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(w)" : String))
}

-- 
def term_SW_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SW_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("saturated(slide(w))" : String))
}

-- 
def term_SW_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("saturated(w)" : String))
}

-- 
def term_SW_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SW_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resources(evict(e))" : String))
}

-- 
def term_SW_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_SW_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_SW_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("size(window)" : String))
}

-- 
def term_SW_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some ((">= 1" : String))
}

-- 
def term_TH_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("type resolution" : String))
}

-- 
def term_TH_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("measurement; cost ≥ entropy removed" : String))
}

-- 
def term_TH_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(state)" : String))
}

-- 
def term_TH_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("freeRank × ln 2" : String))
}

-- 
def term_TH_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(⊥)" : String))
}

-- 
def term_TH_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n × ln 2" : String))
}

-- 
def term_TH_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S(⊤)" : String))
}

-- 
def term_TH_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_TH_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("total resolution cost" : String))
}

-- 
def term_TH_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n × k_B T × ln 2" : String))
}

-- 
def term_TH_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β*" : String))
}

-- 
def term_TH_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ln 2" : String))
}

-- 
def term_TH_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("constraint application" : String))
}

-- 
def term_TH_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("removes entropy; convergenceRate = cooling rate" : String))
}

-- 
def term_TH_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CatastropheThreshold" : String))
}

-- 
def term_TH_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("temperature of partition phase transition" : String))
}

-- 
def term_TH_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("step_g" : String))
}

-- 
def term_TH_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("free-energy cost of constraint boundary" : String))
}

-- 
def term_TH_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TH_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("computational hardness" : String))
}

-- 
def term_TH_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("type incompleteness (high temperature)" : String))
}

-- 
def term_TJ_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TJ_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("all_or_nothing(fail)" : String))
}

-- 
def term_TJ_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("rollback" : String))
}

-- 
def term_TJ_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TJ_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("best_effort(partial_fail)" : String))
}

-- 
def term_TJ_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("commit(succeeded)" : String))
}

-- 
def term_TJ_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TJ_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("scope(transaction)" : String))
}

-- 
def term_TJ_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("single_epoch" : String))
}

-- 
def term_TS_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ForbiddenSignature(sigma)" : String))
}

-- 
def term_TS_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("no ConstrainedType with <= n constraints realises sigma" : String))
}

-- 
def term_TS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("nerve(T, target)" : String))
}

-- 
def term_TS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∃ ConstrainedType T over R_n realising target" : String))
}

-- 
def term_TS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("basisSize(T, IT_7d target)" : String))
}

-- 
def term_TS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("n" : String))
}

-- 
def term_TS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(N(C + constraint))" : String))
}

-- 
def term_TS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ χ(N(C))" : String))
}

-- 
def term_TS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("steps(TypeSynthesisResolver, target)" : String))
}

-- 
def term_TS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ n" : String))
}

-- 
def term_TS_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("SynthesizedType achieves IT_7d" : String))
}

-- 
def term_TS_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("iff CompletenessResolver certifies CompleteType" : String))
}

-- 
def term_TS_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("E[steps, Jacobian-guided synthesis]" : String))
}

-- 
def term_TS_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("O(n log n) vs O(n²) uninformed" : String))
}

-- 
def term_TS_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β₀(N(C)) for non-empty C" : String))
}

-- 
def term_TS_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≥ 1" : String))
}

-- 
def term_TS_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("min constraints for beta_1 = k" : String))
}

-- 
def term_TS_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2k + 1" : String))
}

-- 
def term_TS_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_TS_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("TypeSynthesisResolver terminates" : String))
}

-- 
def term_TS_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("within 2^n steps" : String))
}

-- 
def term_T_A1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_A1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("g ∈ D_{2^n} on Constraint C" : String))
}

-- 
def term_T_A1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("g⋅C (transformed constraint)" : String))
}

-- 
def term_T_A2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_A2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("g ∈ D_{2^n} on Partition" : String))
}

-- 
def term_T_A2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("permutes components" : String))
}

-- 
def term_T_A3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_A3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("D_{2^n} orbits on R_n" : String))
}

-- 
def term_T_A3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("determine irreducibility boundaries" : String))
}

-- 
def term_T_A4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_A4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("fixed points of neg" : String))
}

-- 
def term_T_A4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("{0, 2^{n−1}}; bnot has none (n > 0)" : String))
}

-- 
def term_T_C1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_C1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(id, f)" : String))
}

-- 
def term_T_C1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("f" : String))
}

-- 
def term_T_C2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_C2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(f, id)" : String))
}

-- 
def term_T_C2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("f" : String))
}

-- 
def term_T_C3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_C3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(f, compose(g, h))" : String))
}

-- 
def term_T_C3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("compose(compose(f, g), h)" : String))
}

-- 
def term_T_C4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_C4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("f composesWith g" : String))
}

-- 
def term_T_C4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("target(f) = source(g)" : String))
}

-- 
def term_T_E1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_E1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι(x) = ι(y)" : String))
}

-- 
def term_T_E1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x = y" : String))
}

-- 
def term_T_E2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_E2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι(add(x,y))" : String))
}

-- 
def term_T_E2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(ι(x), ι(y)); ι(mul(x,y)) = mul(ι(x), ι(y))" : String))
}

-- 
def term_T_E3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_E3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ι₂ ∘ ι₁ : R_n → R_k" : String))
}

-- 
def term_T_E3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("is an embedding (transitivity)" : String))
}

-- 
def term_T_E4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_E4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("glyph ∘ ι ∘ addresses" : String))
}

-- 
def term_T_E4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("well-defined" : String))
}

-- 
def term_T_I1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_I1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_R(neg(x), neg(y))" : String))
}

-- 
def term_T_I1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_R(x, y)" : String))
}

-- 
def term_T_I2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_I2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_H(bnot(x), bnot(y))" : String))
}

-- 
def term_T_I2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_H(x, y)" : String))
}

-- 
def term_T_I3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_I3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("succ = neg ∘ bnot" : String))
}

-- 
def term_T_I3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("preserves d_R but not d_H" : String))
}

-- 
def term_T_I4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_I4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ring isometries" : String))
}

-- 
def term_T_I4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("form a group under composition" : String))
}

-- 
def term_T_I5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_T_I5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CurvatureObservable" : String))
}

-- 
def term_T_I5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("measures failure of ring isometry to be Hamming isometry" : String))
}

-- 
def term_U_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_U_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("R_n×" : String))
}

-- 
def term_U_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Z/2 × Z/2^{n-2}" : String))
}

-- 
def term_U_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_U_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("R_1× ≅ {1}; R_2× ≅ Z/2" : String))
}

-- 
def term_U_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("special cases for small n" : String))
}

-- 
def term_U_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_U_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ord(u)" : String))
}

-- 
def term_U_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("lcm(ord((-1)^a), ord(3^b))" : String))
}

-- 
def term_U_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_U_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ord_g(2)" : String))
}

-- 
def term_U_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("divides φ(g)" : String))
}

-- 
def term_U_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_U_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("step_g" : String))
}

-- 
def term_U_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("2 * ((g - (2^n mod g)) mod g) + 1" : String))
}

-- 
def term_WC_10_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_10_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ(x) on W_n(F_2)" : String))
}

-- 
def term_WC_10_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x (identity, F_2 perfect)" : String))
}

-- 
def term_WC_11_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_11_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("V(x) on W_n(F_2)" : String))
}

-- 
def term_WC_11_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, x) in Z/(2ⁿ)Z" : String))
}

-- 
def term_WC_12_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_12_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ(x) on W_n(F_2)" : String))
}

-- 
def term_WC_12_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("(x − mul(x,x)) / 2" : String))
}

-- 
def term_WC_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("a_k(x)" : String))
}

-- 
def term_WC_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("x_k (k-th bit of x)" : String))
}

-- 
def term_WC_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_k − x_k − y_k (mod 2)" : String))
}

-- 
def term_WC_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("c_k(x,y)" : String))
}

-- 
def term_WC_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CA_2 recurrence" : String))
}

-- 
def term_WC_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("S_{k+1} ghost equation at p=2" : String))
}

-- 
def term_WC_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ_k(x+y) correction" : String))
}

-- 
def term_WC_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("c_{k+1}(x,y)" : String))
}

-- 
def term_WC_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("obstruction_trivial = false" : String))
}

-- 
def term_WC_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ_k ≠ 0 for some pair" : String))
}

-- 
def term_WC_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("d_Δ(x,y) > 0" : String))
}

-- 
def term_WC_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ghost defect nonzero" : String))
}

-- 
def term_WC_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("succ^{2ⁿ}(x) = x" : String))
}

-- 
def term_WC_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("r^{2ⁿ} = 1 in Witt-Burnside ring" : String))
}

-- 
def term_WC_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(succ(neg(x)))" : String))
}

-- 
def term_WC_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("srs = r⁻¹ relation" : String))
}

-- 
def term_WC_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WC_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(neg(x)) = pred(x)" : String))
}

-- 
def term_WC_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Product of Witt-Burnside reflections" : String))
}

-- 
def term_WLS_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WLS_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("WittLift T' is CompleteType" : String))
}

-- 
def term_WLS_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("iff spectral sequence collapses at E_2" : String))
}

-- 
def term_WLS_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WLS_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("non-trivial LiftObstruction location" : String))
}

-- 
def term_WLS_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("specific site at bit position n+1" : String))
}

-- 
def term_WLS_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WLS_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("basisSize(T') for trivial lift" : String))
}

-- 
def term_WLS_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("basisSize(T) + 1" : String))
}

-- 
def term_WLS_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WLS_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("spectral sequence convergence page" : String))
}

-- 
def term_WLS_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("≤ E_{d+2}" : String))
}

-- 
def term_WLS_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WLS_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("universallyValid identity in R_{n+1}" : String))
}

-- 
def term_WLS_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("holds with lifted constraint set" : String))
}

-- 
def term_WLS_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WLS_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ψ-pipeline ChainComplex(T')" : String))
}

-- 
def term_WLS_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("valid and restricts to ChainComplex(T) on base nerve" : String))
}

-- 
def term_WT_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("LiftChain(Q_j, Q_k) valid" : String))
}

-- 
def term_WT_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("every chainStep has trivial or resolved LiftObstruction" : String))
}

-- 
def term_WT_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("obstructionCount(chain)" : String))
}

-- 
def term_WT_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("<= chainLength(chain)" : String))
}

-- 
def term_WT_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("resolvedBasisSize(Q_k)" : String))
}

-- 
def term_WT_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("basisSize(Q_j) + chainLength + obstructionResolutionCost" : String))
}

-- 
def term_WT_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("isFlat(chain)" : String))
}

-- 
def term_WT_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("obstructionCount = 0 iff HolonomyGroup trivial at every step" : String))
}

-- 
def term_WT_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("LiftChainCertificate exists" : String))
}

-- 
def term_WT_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("CompleteType at Q_k satisfies IT_7d with witness chain" : String))
}

-- 
def term_WT_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("QT_3 with chainLength=1, cost=0" : String))
}

-- 
def term_WT_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("reduces to QLS_3" : String))
}

-- 
def term_WT_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("flat chain resolvedBasisSize(Q_k)" : String))
}

-- 
def term_WT_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("basisSize(Q_j) + (k - j)" : String))
}

-- 
def term_WT_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ObstructionChain length from Q_j to Q_k" : String))
}

-- 
def term_WT_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("<= (k-j) * C(basisSize(Q_j), 3)" : String))
}

-- 
def term_WT_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_WT_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("TowerCompletenessResolver terminates" : String))
}

-- 
def term_WT_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("within QT_8 bound" : String))
}

-- 
def term_X_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_X_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(x)" : String))
}

-- 
def term_X_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sub(0, x)" : String))
}

-- 
def term_X_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_X_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(x)" : String))
}

-- 
def term_X_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(x, 2^n - 1)" : String))
}

-- 
def term_X_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_X_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("succ(x)" : String))
}

-- 
def term_X_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, 1)" : String))
}

-- 
def term_X_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_X_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pred(x)" : String))
}

-- 
def term_X_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("sub(x, 1)" : String))
}

-- 
def term_X_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_X_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(x)" : String))
}

-- 
def term_X_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(bnot(x), 1)" : String))
}

-- 
def term_X_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_X_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("bnot(x)" : String))
}

-- 
def term_X_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pred(neg(x))" : String))
}

-- 
def term_X_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_X_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("xor(x, y)" : String))
}

-- 
def term_X_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("add(x, y) - 2 * and(x, y)" : String))
}

-- 
def term_boundarySquaredZero_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_boundarySquaredZero_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("∂_k(∂_{k+1}(c))" : String))
}

-- 
def term_boundarySquaredZero_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_coboundarySquaredZero_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_coboundarySquaredZero_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("δ^{k+1}(δ^k(f))" : String))
}

-- 
def term_coboundarySquaredZero_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("0" : String))
}

-- 
def term_criticalIdentity_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_criticalIdentity_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("neg(bnot(x))" : String))
}

-- 
def term_criticalIdentity_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("succ(x)" : String))
}

-- 
def term_deRhamDuality_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_deRhamDuality_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^k(K; R)" : String))
}

-- 
def term_deRhamDuality_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Hom(H_k(K), R)" : String))
}

-- 
def term_indexBridge_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_indexBridge_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("χ(K)" : String))
}

-- 
def term_indexBridge_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Σ_k (-1)^k β_k" : String))
}

-- 
def term_jsat_CC_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_jsat_CC_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("jointSat(Carry(p1), Carry(p2))" : String))
}

-- 
def term_jsat_CC_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("p1 AND p2 conflict-free" : String))
}

-- 
def term_jsat_CR_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_jsat_CR_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("jointSat(Carry(p), Res(m,r))" : String))
}

-- 
def term_jsat_CR_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("pin-site intersection residue-class compatible" : String))
}

-- 
def term_jsat_RR_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_jsat_RR_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("jointSat(Res(m1,r1), Res(m2,r2))" : String))
}

-- 
def term_jsat_RR_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("gcd(m1,m2) | (r1 - r2)" : String))
}

-- 
def term_localGlobalPrinciple_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_localGlobalPrinciple_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^1(K; F) = 0" : String))
}

-- 
def term_localGlobalPrinciple_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("all local sections glue" : String))
}

-- 
def term_phi_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_phi_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₁(neg, ResidueConstraint(m,r))" : String))
}

-- 
def term_phi_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ResidueConstraint(m, m-r)" : String))
}

-- 
def term_phi_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_phi_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₂(compose(A,B))" : String))
}

-- 
def term_phi_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₂(A) ∪ φ₂(B)" : String))
}

-- 
def term_phi_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_phi_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₃(closed site state)" : String))
}

-- 
def term_phi_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("unique 4-component partition" : String))
}

-- 
def term_phi_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_phi_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₄(T, x)" : String))
}

-- 
def term_phi_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₃(φ₂(φ₁(T, x)))" : String))
}

-- 
def term_phi_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_phi_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₅(neg)" : String))
}

-- 
def term_phi_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("preserves d_R, may change d_H" : String))
}

-- 
def term_phi_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_phi_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("φ₆(state, observables)" : String))
}

-- 
def term_phi_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("RefinementSuggestion" : String))
}

-- 
def term_psi_1_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_1_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("N(constraints)" : String))
}

-- 
def term_psi_1_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("SimplicialComplex" : String))
}

-- 
def term_psi_2_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_2_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("C(K)" : String))
}

-- 
def term_psi_2_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ChainComplex" : String))
}

-- 
def term_psi_3_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_3_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H_k(C)" : String))
}

-- 
def term_psi_3_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ker(∂_k) / im(∂_{k+1})" : String))
}

-- 
def term_psi_4_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_4_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("β_k(K)" : String))
}

-- 
def term_psi_4_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("rank(H_k(K))" : String))
}

-- 
def term_psi_5_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_5_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("C^k" : String))
}

-- 
def term_psi_5_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("Hom(C_k, R)" : String))
}

-- 
def term_psi_6_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_6_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^k(C)" : String))
}

-- 
def term_psi_6_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("ker(δ^k) / im(δ^{k-1})" : String))
}

-- 
def term_psi_7_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_7_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("KanComplex(N(C))" : String))
}

-- 
def term_psi_7_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("PostnikovTower" : String))
}

-- 
def term_psi_8_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_8_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("PostnikovTower(τ≤k)" : String))
}

-- 
def term_psi_8_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HomotopyGroups(π_k)" : String))
}

-- 
def term_psi_9_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_psi_9_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("HomotopyGroups(π_k)" : String))
}

-- 
def term_psi_9_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("KInvariants(κ_k)" : String))
}

-- 
def term_sheafCohomologyBridge_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_sheafCohomologyBridge_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^k(K; F_R)" : String))
}

-- 
def term_sheafCohomologyBridge_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("H^k(K; R)" : String))
}

-- 
def term_surfaceSymmetry_forAll : UOR.Kernel.Schema.ForAllDeclaration UOR.Prims.Standard := {
  boundVariables := #[]
  quantifierKind := none
}

-- 
def term_surfaceSymmetry_lhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("P(Π(G(s)))" : String))
}

-- 
def term_surfaceSymmetry_rhs : UOR.Kernel.Schema.LiteralExpression UOR.Prims.Standard := {
  literalValue := some (("s' where type(s') ≡ type(s) under F.constraint" : String))
}

-- The additive identity of the ring. Value = 0 at every Witt level. op:add(x, zero) = x for all x in R_n.
def zero : UOR.Kernel.Schema.Datum UOR.Prims.Standard := {
  value := some ((0 : Nat))
  wittLength := none
  stratum := none
  spectrum := none
  element := none
}

end UOR.Kernel.Schema
