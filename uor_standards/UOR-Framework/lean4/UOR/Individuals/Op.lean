-- @generated by uor-lean from uor-ontology — do not edit manually
--
-- UOR Foundation — Op individual constants.

import UOR.Structures
import UOR.Individuals.Schema

open UOR.Primitives

namespace UOR.Kernel.Op

-- Braille glyph encoding: 6-bit blocks to Braille characters.
def AA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AA_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Braille XOR homomorphism: Braille encoding commutes with XOR.
def AA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AA_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Braille complement: glyph of bnot(x) is character-wise complement of glyph(x).
def AA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AA_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Addition does not lift to address space: no glyph homomorphism for add.
def AA_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AA_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AA_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AA_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Liftable operations are exactly the Boolean operations.
def AA_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AA_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AA_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AA_5_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Negation decomposes into liftable bnot and non-liftable succ.
def AA_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AA_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AA_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AA_6_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Addressing bijection: addresses(glyph(d)) = d. Round-trip from datum through glyph and back is identity.
def AD_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AD_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AD_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AD_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding coherence: glyph(ι(addresses(a))) = ι_addr(a). The addressing diagram commutes through embeddings.
def AD_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AD_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AD_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AD_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Scaling not dihedral: μ_u ∉ D_[2^n] for u ≠ ±1.
def AG_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AG_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AG_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AG_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Affine group: Aff(R_n) = R_n× ⋉ R_n.
def AG_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AG_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AG_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AG_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Affine group order: |Aff(R_n)| = 2^[2n-1].
def AG_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AG_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AG_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AG_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Subgroup inclusion: D_[2^n] ⊂ Aff(R_n) with u ∈ [±1].
def AG_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AG_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AG_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AG_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Address metric is sum of per-character Hamming distances.
def AM_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AM_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AM_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AM_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Address metric equals Hamming metric on ring elements.
def AM_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AM_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AM_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AM_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Address metric does not preserve ring metric in general.
def AM_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AM_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AM_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AM_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Address incompatibility metric.
def AM_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AM_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AM_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AM_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Partial saturation is monotonically non-decreasing across stages.
def AP_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AP_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AP_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AP_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Approximation quality improves with additional epochs.
def AP_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AP_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AP_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AP_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Deferred queries are eventually processed or explicitly dropped.
def AP_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AP_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AP_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AP_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Adiabatic schedule: decreasing freeRank, cost-per-site ordering.
def AR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AR_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Adiabatic cost is sum of constraint costs in optimal order.
def AR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AR_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Adiabatic cost satisfies Landauer bound.
def AR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AR_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Adiabatic efficiency η ≤ 1.
def AR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AR_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Greedy vs adiabatic cost difference: ≤ 5% for n ≤ 16.
def AR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AR_5_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- δ-ι-κ non-associativity: δ reads registry written by κ.
def AS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AS_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ι-α-λ non-associativity: λ reads lease state written by α.
def AS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AS_2_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- λ-κ-δ non-associativity: δ reads state written by κ.
def AS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AS_3_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Root cause: non-associativity from read-write interleaving through mediating entity.
def AS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AS_4_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Automorphism group consists of unit multiplications.
def AU_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AU_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AU_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AU_1_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Automorphism group is isomorphic to the unit group.
def AU_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AU_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AU_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AU_2_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Automorphism group order is 2^{n−1}.
def AU_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AU_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AU_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AU_3_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Intersection of automorphism group with dihedral group is {id, neg}.
def AU_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AU_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AU_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AU_4_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Affine group is generated by D_{2^n} and μ_3.
def AU_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_AU_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_AU_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_AU_5_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- σ(C) = (n − freeRank(C)) / n. The saturation metric is the complement of free site ratio. Derives from SC_2.
def BM_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BM_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BM_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BM_1_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- χ = Σ(−1)^k β_k. The Euler characteristic of the constraint nerve. Derives from IT_2.
def BM_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BM_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BM_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BM_2_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Index theorem: Σκ_k − χ = S_residual / ln 2. Links all six metrics. Derives from IT_7a.
def BM_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BM_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BM_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BM_3_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- J_k = 0 for pinned sites. The Jacobian vanishes on resolved sites.
def BM_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BM_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BM_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BM_4_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- d_Δ > 0 iff carry ≠ 0. The metric discrepancy equals the Witt defect. Derives from WC_6.
def BM_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BM_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BM_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BM_5_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Metric composition tower: d_Δ → {σ, J_k} → β_k → χ → r. Each metric derives from previous ones.
def BM_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BM_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BM_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BM_6_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every recursive step strictly decreases the descent measure.
def BR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BR_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Recursion depth is bounded by the initial measure value.
def BR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BR_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every bounded recursion terminates.
def BR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BR_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Structural recursion’s measure is the input type’s structural size.
def BR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BR_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The base predicate is satisfied exactly when the measure reaches zero.
def BR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_BR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_BR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_BR_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- XOR associativity: xor(x, xor(y, z)) = xor(xor(x, y), z).
def B_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- AND distributes over OR: and(x, or(y, z)) = or(and(x, y), and(x, z)).
def B_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_10_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- De Morgan 1: bnot(and(x, y)) = or(bnot(x), bnot(y)).
def B_11 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_11_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_11_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_11_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- De Morgan 2: bnot(or(x, y)) = and(bnot(x), bnot(y)).
def B_12 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_12_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_12_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_12_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Bnot involution: bnot(bnot(x)) = x.
def B_13 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_13_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_13_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_13_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- XOR identity: xor(x, 0) = x.
def B_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- XOR self-inverse: xor(x, x) = 0.
def B_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- AND associativity: and(x, and(y, z)) = and(and(x, y), z).
def B_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- AND identity: and(x, 2^n - 1) = x.
def B_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- AND annihilation: and(x, 0) = 0.
def B_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- OR associativity: or(x, or(y, z)) = or(or(x, y), z).
def B_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- OR identity: or(x, 0) = x.
def B_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_8_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Absorption: and(x, or(x, y)) = x.
def B_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_B_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_B_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_B_9_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Addition decomposition: add(x,y)_k = xor(x_k, xor(y_k, c_k(x,y))).
def CA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CA_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Carry recurrence: c_[k+1](x,y) = or(and(x_k,y_k), and(xor(x_k,y_k), c_k)).
def CA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CA_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Carry commutativity: c(x, y) = c(y, x).
def CA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CA_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Zero carry: c(x, 0) = 0 at all positions.
def CA_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CA_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CA_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CA_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Negation carry: c(x, neg(x))_k = 1 for k > v_2(x).
def CA_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CA_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CA_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CA_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Carry-incompatibility link: d_Δ(x, y) > 0 iff ∃ k : c_k(x,y) = 1.
def CA_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CA_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CA_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CA_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Minimum convergence rate: 1 site per iteration (worst case).
def CB_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CB_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CB_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CB_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Maximum convergence rate: n sites in 1 iteration (best case).
def CB_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CB_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CB_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CB_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Expected residue constraint rate: ⌊log_2(m)⌋ sites per constraint.
def CB_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CB_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CB_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CB_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Stall detection: convergenceRate < 1 for 2 iterations suggests insufficiency.
def CB_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CB_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CB_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CB_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Sufficiency criterion: pin union covers all site positions.
def CB_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CB_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CB_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CB_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Iteration bound for k constraints: at most min(k, n) iterations.
def CB_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CB_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CB_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CB_6_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Completeness implies O(1) resolution: a CompleteType T satisfies ∀ x ∈ R_n, resolution(x, T) terminates in O(1).
def CC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CC_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Completeness is monotone: if T ⊆ T' (T' has more constraints), then completeness(T) implies completeness(T').
def CC_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CC_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CC_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CC_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Witness composition: concat(W₁, W₂) is a valid audit trail iff W₁.sitesClosed + W₂.sitesClosed = n.
def CC_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CC_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CC_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CC_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- The CompletenessResolver is the unique fixed point of the ψ-pipeline applied to a CompletenessCandidate.
def CC_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CC_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CC_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CC_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- CompletenessCertificate soundness: cert.verified = true implies χ(N(C)) = n and for all k: β_k = 0.
def CC_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CC_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CC_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CC_5_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Carry-constraint cost-to-site count: the number of sites pinned by a CarryConstraint equals popcount plus one for the stopping position.
def CC_COST_SITE : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CC_COST_SITE_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CC_COST_SITE_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CC_COST_SITE_forAll)
  verificationDomain := #[.enumerative]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Carry-constraint site-pinning map: pinsSites(CarryConstraint(p)) equals the set of bit positions where p has a 1 plus the first-zero stopping position.
def CC_PINS : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CC_PINS_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CC_PINS_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CC_PINS_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Constraint-depth invariant: carry complexity of the residue representation equals the type depth.
def CDI : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CDI_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CDI_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CDI_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Curvature flux is the sum of incompatibility along a path.
def CF_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CF_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CF_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CF_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Resolution cost is bounded below by curvature flux of optimal path.
def CF_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CF_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CF_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CF_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Successor curvature flux: trailing_ones(x) for most x, n−1 at maximum.
def CF_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CF_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CF_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CF_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Total successor curvature flux over R_n equals 2^n − 2.
def CF_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CF_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CF_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CF_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Entry guard must be satisfied to enter a stage.
def CG_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CG_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CG_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CG_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Exit guard must be satisfied, then the stage effect is applied.
def CG_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CG_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CG_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CG_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate issuance: every valid Transform admits a TransformCertificate attesting correct source-to-target mapping.
def CIC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CIC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CIC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CIC_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate issuance: every metric-preserving Transform admits an IsometryCertificate attesting distance preservation.
def CIC_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CIC_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CIC_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CIC_2_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate issuance: every involutive operation f where f(f(x)) = x admits an InvolutionCertificate.
def CIC_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CIC_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CIC_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CIC_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate issuance: full saturation (σ = 1, freeRank = 0) admits a GroundingCertificate.
def CIC_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CIC_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CIC_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CIC_4_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate issuance: an AR_1-ordered and DC_10-selected trace admits a GeodesicCertificate.
def CIC_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CIC_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CIC_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CIC_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate issuance: a MeasurementEvent verifying the von Neumann–Landauer bridge at β* = ln 2 admits a MeasurementCertificate.
def CIC_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CIC_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CIC_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CIC_6_forAll)
  verificationDomain := #[.quantumThermodynamic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate issuance: a MeasurementEvent verifying P(outcome k) = |α_k|² admits a BornRuleVerification certificate.
def CIC_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CIC_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CIC_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CIC_7_forAll)
  verificationDomain := #[.quantumThermodynamic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Constraint quotient lattice isomorphism to power set.
def CL_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CL_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CL_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CL_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Lattice join equals constraint composition.
def CL_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CL_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CL_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CL_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Lattice meet pins the intersection of component pins.
def CL_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CL_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CL_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CL_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint lattice distributivity.
def CL_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CL_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CL_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CL_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint lattice complement existence.
def CL_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CL_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CL_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CL_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint redundancy criterion.
def CM_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CM_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CM_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CM_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Minimal cover via greedy irredundant removal.
def CM_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CM_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CM_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CM_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Minimum constraint count to cover n sites.
def CM_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CM_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CM_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CM_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Standard coefficient ring: the coefficient ring for all psi-pipeline cohomology computations in uor.foundation is Z/2Z, consistent with MN_7.
def COEFF_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_COEFF_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_COEFF_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_COEFF_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Cartesian partition product site additivity: siteBudget(A ⊠ B) = siteBudget(A) + siteBudget(B). UOR sites are bit widths; |A ⊠ B| = 2^{n_A} · 2^{n_B} = 2^{n_A + n_B}, so sites add even though cardinalities multiply.
def CPT_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CPT_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CPT_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CPT_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Cartesian partition product partition map: Π(A ⊠ B) = CartesianPartitionProduct(Π(A), Π(B)).
def CPT_2a : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CPT_2a_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CPT_2a_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CPT_2a_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Cartesian partition product Euler multiplicativity: χ(N(C(A ⊠ B))) = χ(N(C(A))) · χ(N(C(B))). Distinguishes CartesianPartitionProduct (multiplicative χ, Künneth) from PartitionProduct (additive χ, site-disjoint union).
def CPT_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CPT_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CPT_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CPT_3_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Cartesian partition product Künneth formula: Betti numbers compose via the Künneth convolution — β_k(A ⊠ B) = Σ_{i+j=k} β_i(A) · β_j(B). Routed through primitive_cartesian_nerve_betti when a CartesianProductShape marker is declared on the combined shape.
def CPT_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CPT_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CPT_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CPT_4_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Cartesian partition product entropy additivity: S(A ⊠ B) = S(A) + S(B). Matches PT_4's additive Shannon convention for independent subsystems and is consistent pointwise with IT_7a/b on the combined system (no cross-factor bilinearity required).
def CPT_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CPT_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CPT_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CPT_5_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Cartesian partition product distributes over PartitionCoproduct: A ⊠ (B + C) ≡ (A ⊠ B) + (A ⊠ C) with equality at the siteBudget, SITE_COUNT, Euler, and entropy levels simultaneously. Does NOT extend to PartitionProduct — distributing ⊠ over × fails at the site-budget level because it duplicates the outer factor.
def CPT_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CPT_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CPT_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CPT_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Residue constraint cost is the step formula.
def CR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CR_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Carry constraint cost is the popcount of the pattern.
def CR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CR_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Depth constraint cost is sum of residue and carry costs.
def CR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CR_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Composite constraint cost is subadditive.
def CR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CR_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Optimal resolution order is increasing cost.
def CR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CR_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Each stage has bounded cost.
def CS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CS_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Pipeline cost = sum of stage costs.
def CS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CS_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Rollback cost is at most forward cost.
def CS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CS_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Preflight cost is O(1).
def CS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CS_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Total reduction cost bounded by n × stage_max_cost.
def CS_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CS_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CS_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CS_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Budget solvency rejection: a CompileUnit whose declared thermodynamicBudget is strictly less than the Landauer minimum (bitsWidth(Q_k) × ln 2) is rejected at the BudgetSolvencyCheck preflight.
def CS_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CS_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CS_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CS_6_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Unit address identity: the unitAddress of a CompileUnit is the u:Element computed by hashing the canonical byte serialization of the root term’s transitive closure.
def CS_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CS_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CS_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CS_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Catastrophe boundaries are powers of 2.
def CT_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CT_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CT_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CT_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Odd prime catastrophe transitions visibility via residue constraints.
def CT_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CT_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CT_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CT_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Catastrophe threshold is normalized step cost.
def CT_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CT_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CT_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CT_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Composite catastrophe threshold is max of component thresholds.
def CT_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CT_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CT_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CT_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Carry generates at site k iff and(x_k, y_k) = 1. Extends CA_1 (addition decomposition) and WC_2 (Witt sum correction).
def CY_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CY_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CY_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CY_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Carry propagates at site k iff xor(x_k, y_k) = 1 and c_k = 1. Extends CA_2 (carry recurrence) and WC_3 (Witt polynomial recurrence).
def CY_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CY_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CY_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CY_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Carry kills at site k iff and(x_k, y_k) = 0 and xor(x_k, y_k) = 0. Complement of CY_1 and CY_2.
def CY_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CY_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CY_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CY_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- d_Δ(x,y) = |carryCount(x+y) − hammingDistance(x,y)|. The metric incompatibility IS the discrepancy between carry count and Hamming distance. Strengthens WC_6.
def CY_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CY_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CY_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CY_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Optimal encoding theorem: the encoding that minimizes Σ d_Δ over observed pairs is the one where the carry chain’s significance hierarchy matches the domain’s dependency structure.
def CY_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CY_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CY_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CY_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Site ordering theorem: d_Δ is minimized when high-significance sites (upstream in the carry chain) encode the most structurally informative observables.
def CY_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CY_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CY_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CY_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Carry lookahead: the carry chain for n sites is computable in O(log n) using prefix computation on generate/propagate pairs.
def CY_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_CY_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_CY_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_CY_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Constraint pin union: pins of a composite constraint equal the union of component pins.
def C_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_C_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_C_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_C_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint composition commutativity.
def C_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_C_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_C_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_C_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint composition associativity.
def C_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_C_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_C_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_C_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint composition idempotence.
def C_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_C_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_C_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_C_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint composition identity element.
def C_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_C_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_C_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_C_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint composition annihilator.
def C_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_C_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_C_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_C_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Cayley-Dickson R→C: adjoin i with i²=−1, conjugation (a+bi)* = a−bi.
def DA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DA_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Cayley-Dickson C→H: adjoin j with j²=−1, ij=k, ji=−k, k²=−1.
def DA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DA_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Cayley-Dickson H→O: adjoin l, Fano plane products, associativity fails.
def DA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DA_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Adams theorem: no normed division algebra of dimension 16 exists.
def DA_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DA_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DA_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DA_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Convergence level k lives in k-th division algebra: L0 in R, L1 in C, L2 in H, L3 in O.
def DA_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DA_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DA_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DA_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Commutator vanishes iff algebra at that level is commutative.
def DA_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DA_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DA_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DA_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Associator vanishes iff algebra at that level is associative.
def DA_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DA_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DA_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DA_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Ring derivative: ∂_R f(x) = f(succ(x)) - f(x).
def DC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Curvature-weighted ordering: optimal next constraint maximizes J_k over free sites.
def DC_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_10_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Curvature equipartition: Σ_{x} J_k(x) ≈ (2^n - 2)/n for each k.
def DC_11 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_11_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_11_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_11_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Hamming derivative: ∂_H f(x) = f(bnot(x)) - f(x).
def DC_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Hamming derivative of identity: ∂_H id(x) = -(2x + 1) mod 2^n.
def DC_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Commutator from derivatives: [neg, bnot](x) = ∂_R neg(x) - ∂_H neg(x).
def DC_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Carry dependence: the difference ∂_R f - ∂_H f decomposes into carry contributions.
def DC_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_5_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Jacobian definition: J_k(x) = ∂_R f_k(x) where f_k = site_k.
def DC_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_6_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Top-site anomaly: J_{n-1}(x) may differ from lower sites.
def DC_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_7_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Rank-curvature identity: rank(J(x)) = d_H(x, succ(x)) - 1 for generic x.
def DC_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_8_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Total curvature from Jacobian: κ(x) = Σ_k J_k(x).
def DC_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DC_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DC_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DC_9_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dispatch determinism: same query and same registry always yield the same resolver.
def DD_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DD_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DD_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DD_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Dispatch coverage: every query in the registry domain has a matching resolver.
def DD_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DD_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DD_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DD_2_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The root resolver dispatch table is exhaustive and mutually exclusive over all TypeDefinitions.
def DIS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DIS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DIS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DIS_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Resolver dispatch is deterministic for every type.
def DIS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DIS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DIS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DIS_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Division right-identity: div(a, 1) = a.
def DV_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DV_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DV_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DV_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Division left-absorbing: div(0, b) = 0 for b ≠ 0.
def DV_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DV_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DV_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DV_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Division-of-multiplication recovery: div(mul(a, b), b) = a for b ≠ 0 in the unit cone.
def DV_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DV_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DV_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DV_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Euclidean compatibility: a = add(mul(div(a, b), b), mod(a, b)) for b ≠ 0.
def DV_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_DV_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_DV_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_DV_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Rotation order: succ^[2^n](x) = x.
def D_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_D_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_D_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_D_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Conjugation: neg(succ(neg(x))) = pred(x).
def D_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_D_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_D_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_D_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Reverse composition: bnot(neg(x)) = pred(x).
def D_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_D_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_D_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_D_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Group closure: D_[2^n] = [succ^k, neg ∘ succ^k : 0 ≤ k < 2^n].
def D_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_D_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_D_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_D_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dihedral composition rule: (rᵃ sᵖ)(rᵇ sᵠ) = r^(a + (-1)ᵖ b mod 2ⁿ) s^(p xor q).
def D_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_D_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_D_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_D_7_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Dihedral inverse formula: the inverse of r^a s^p in D_(2^n) is r^(-(−1)^p a mod 2^n) s^p.
def D_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_D_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_D_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_D_8_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Dihedral reflection order: every reflection element r^k s^1 in D_(2^n) has order 2.
def D_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_D_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_D_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_D_9_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Epoch boundary resets free sites.
def EA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EA_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Grounding carries across epochs.
def EA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EA_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Service window bounds context size.
def EA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EA_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Epoch admits one datum or one refinement pass.
def EA_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EA_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EA_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EA_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Ω⁶ = −1: reduction converges in 6 stages (phase half-turn).
def EC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Complex conjugate rollback involutory: (z̄)̄ = z.
def EC_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Pairwise convergence: commutator converges to identity.
def EC_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Triple convergence: associator converges to zero.
def EC_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_4_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Associator monotonicity: associator norm non-increasing.
def EC_4a : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_4a_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_4a_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_4a_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Associator finiteness: reaches 0 in bounded steps.
def EC_4b : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_4b_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_4b_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_4b_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Associator vanishing implies associativity on resolved site space.
def EC_4c : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_4c_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_4c_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_4c_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Adams termination: no convergence level beyond L3_Self.
def EC_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EC_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EC_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EC_5_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Embedding functor action on morphisms.
def EF_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EF_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EF_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EF_1_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding functor preserves composition.
def EF_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EF_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EF_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EF_2_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding functor preserves identities.
def EF_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EF_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EF_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EF_3_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding functor composition is functorial.
def EF_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EF_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EF_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EF_4_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding functor preserves ring isometries.
def EF_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EF_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EF_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EF_5_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding functor maps dihedral subgroup into target dihedral group.
def EF_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EF_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EF_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EF_6_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding functor maps unit group into target unit group.
def EF_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EF_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EF_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EF_7_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Stage transition requires guard satisfaction.
def ER_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ER_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ER_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ER_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Effect application is atomic.
def ER_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ER_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ER_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ER_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Guard evaluation is side-effect-free.
def ER_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ER_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ER_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ER_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Effect composition is order-independent within a stage.
def ER_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ER_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ER_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ER_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Monotone carrier characterization: a ConstrainedType has an upward-closed carrier iff every ResidueConstraint has residue = modulus - 1 and no CarryConstraint or DepthConstraint appears.
def EXP_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EXP_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EXP_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EXP_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Monotone constraint count: the number of expressible monotone ConstrainedTypes at quantum level Q_n is 2^n, corresponding to the principal filter count.
def EXP_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EXP_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EXP_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EXP_2_forAll)
  verificationDomain := #[.enumerative]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SumType carrier semantics: the carrier of a SumType is the coproduct (disjoint union) of component carriers, not the set-theoretic union.
def EXP_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_EXP_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_EXP_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_EXP_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every pending query eventually reaches a stage gate.
def FA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FA_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- No starvation under bounded epoch admission.
def FA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FA_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Fair lease allocation under disjoint composition.
def FA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FA_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every partial computation produces exactly one of Success or Failure.
def FLR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FLR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FLR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FLR_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- A total computation always succeeds.
def FLR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FLR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FLR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FLR_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Sequential failure propagation: if A fails, B is not evaluated.
def FLR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FLR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FLR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FLR_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Parallel failure independence: one component’s failure does not prevent the other’s success.
def FLR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FLR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FLR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FLR_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Recovery produces a new ComputationResult.
def FLR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FLR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FLR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FLR_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The reduction’s existing rollback mechanism is a Recovery whose effect is the conjugate phase rotation.
def FLR_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FLR_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FLR_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FLR_6_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Site lattice bottom: all sites free.
def FL_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FL_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FL_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FL_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site lattice top: all sites pinned.
def FL_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FL_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FL_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FL_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site lattice join is union of pinnings.
def FL_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FL_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FL_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FL_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site lattice height equals n.
def FL_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FL_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FL_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FL_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Unit partition membership: x is a unit iff site_0(x) = 1 (x is odd).
def FPM_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Exterior partition membership: x is exterior iff x is not in the carrier of T.
def FPM_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Irreducible partition membership: x is irreducible iff x is not a unit, exterior, or reducible.
def FPM_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Reducible partition membership: x is reducible iff x is not a unit, exterior, or irreducible.
def FPM_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- 2-adic decomposition: every element factors as 2^{v(x)} times an odd unit.
def FPM_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Stratum size formula.
def FPM_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Base type partition cardinalities.
def FPM_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Partition exhaustiveness: the four component cardinalities sum to the ring size.
def FPM_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_8_forAll)
  verificationDomain := #[.enumerative]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Exterior membership criterion: x is exterior iff x is not in the carrier of T.
def FPM_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FPM_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FPM_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FPM_9_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Site extraction: site_k(x) is the k-th bit of x.
def FS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FS_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site 0 is parity.
def FS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FS_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Progressive site determination: site_k given lower sites determines x mod 2^{k+1}.
def FS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FS_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Cumulative site determination: sites 0..k together determine x mod 2^{k+1}.
def FS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FS_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Complete site determination: all n sites determine x uniquely.
def FS_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FS_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FS_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FS_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Stratum from sites: v_2(x) is the minimum k where site_k(x) = 1.
def FS_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FS_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FS_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FS_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Depth bound: depth(x) ≤ v_2(x) for irreducible elements.
def FS_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FS_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FS_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FS_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Pinning decrements free count by exactly 1.
def FX_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FX_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FX_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FX_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Unbinding increments free count by exactly 1.
def FX_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FX_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FX_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FX_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Phase effects preserve site budget.
def FX_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FX_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FX_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FX_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Disjoint effects commute.
def FX_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FX_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FX_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FX_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Composite free-count delta is additive.
def FX_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FX_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FX_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FX_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every ReversibleEffect has an inverse (PinningEffect⁻¹ = UnbindingEffect on same site, PhaseEffect⁻¹ = conjugate phase).
def FX_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FX_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FX_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FX_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- External effects must match their declared shape.
def FX_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_FX_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_FX_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_FX_7_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Site monotonicity: a pinned site cannot be unpinned.
def F_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_F_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_F_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_F_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site budget upper bound: at most n pin operations to close.
def F_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_F_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_F_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_F_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site budget conservation: pinned + free = total sites.
def F_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_F_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_F_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_F_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site budget closure: closed iff all sites pinned.
def F_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_F_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_F_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_F_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Certificate issuance: shared-frame grounding that lands in the type-equivalent neighbourhood admits a GroundingCertificate.
def GC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GC_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Geodesic condition: a ComputationTrace is a geodesic iff its steps are AR_1-ordered and each step selects the constraint maximising J_k over free sites (DC_10).
def GD_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GD_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GD_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GD_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Geodesic entropy bound: ΔS_step(i) = ln 2 for every step i of a geodesic trace.
def GD_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GD_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GD_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GD_2_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Total geodesic cost: Cost_geodesic(T) = freeRank_initial × k_B T ln 2 = TH_4.
def GD_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GD_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GD_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GD_3_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Geodesic uniqueness up to step-order equivalence: all geodesics for the same ConstrainedType share stepCount and constraint set.
def GD_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GD_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GD_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GD_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Subgeodesic detectability: a trace is sub-geodesic iff ∃ step i where J_k(step_i) < max_{free sites} J_k(state_i).
def GD_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GD_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GD_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GD_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Geodesic predicate decomposition: a trace is geodesic iff it is both AR_1-ordered and DC_10-selected.
def GD_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GD_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GD_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GD_6_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- σ = lower adjoint evaluated at current type. The saturation metric is the lower adjoint of the Galois connection. Derives from SC_2.
def GL_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GL_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GL_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GL_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- r = complement of upper adjoint image. The residual freedom is what the type closure does not reach.
def GL_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GL_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GL_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GL_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- CompleteType = fixpoint of Galois connection, σ=1, r=0. Derives from IT_7d.
def GL_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GL_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GL_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GL_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Type refinement = ascending in type lattice = descending in site freedom. The Galois connection reverses order.
def GL_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GL_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GL_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GL_4_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- GluingObstruction feedback: given a GluingObstruction class in H^1(N(C)), the killing RefinementSuggestion adds a constraint whose pinned sites contain the intersection of the cycle-generating pair.
def GO_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GO_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GO_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GO_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Binding monotonicity: freeRank(B_{i+1}) ≤ freeRank(B_i) for all i in a Session.
def GR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- ExecutionPolicy confluence: different execution policies on the same pending query set produce the same final resolved state (Church-Rosser for session resolution).
def GR_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_10_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Binding soundness: a Binding b is sound iff b.datum resolves under b.constraint in O(1).
def GR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Session convergence: a Session S converges iff there exists i such that freeRank(B_i) = 0.
def GR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Context reset isolation: bindings in C_fresh are independent of bindings in C_prior after a SessionBoundary.
def GR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Contradiction detection: ContradictionBoundary fires iff there exist bindings b, b' with the same address, different datum, under the same constraint.
def GR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Grounding re-entry free rank: for a session at full grounding, a new query q has freeRank equal to the number of q's site coordinates not already bound.
def GR_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Grounding degree degradation: after re-entry with query q, the grounding degree becomes min(current sigma, 1 - freeRank(q)/n).
def GR_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Session composition validity: compose(S_A, S_B) is valid at Q_k iff all pinned-site intersections agree at every tower level Q_0 through Q_k.
def GR_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_8_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (false)
  validityKind := some (.parametricLower)
  validKMin := some ((0 : Nat))
  validKMax := none
}

-- ContextLease disjointness: two distinct leases on the same SharedContext have non-overlapping site sets.
def GR_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GR_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GR_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GR_9_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Context temperature: T_ctx(C) = freeRank(C) × ln 2 / n.
def GS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GS_1_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Grounding degree: σ(C) = (n − freeRank(C)) / n.
def GS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GS_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Grounding monotonicity: σ(B_{i+1}) ≥ σ(B_i) for all i in a Session.
def GS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GS_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Ground state equivalence: σ(C) = 1 ↔ freeRank(C) = 0 ↔ S(C) = 0 ↔ T_ctx(C) = 0.
def GS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GS_4_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- O(1) resolution guarantee: freeRank(C) = 0 ∧ q.address ∈ bindings(C) → stepCount(q, C) = 0.
def GS_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GS_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GS_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GS_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Pre-reduction of effective budget: effectiveBudget(q, C) = max(0, siteBudget(q.type) − |pinnedSites(C) ∩ q.siteSet|).
def GS_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GS_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GS_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GS_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Thermodynamic cooling cost: Cost_saturation(C) = n × k_B T × ln 2.
def GS_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_GS_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_GS_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_GS_7_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint nerve: N(C) is the simplicial complex on constraints.
def HA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HA_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Stall iff non-trivial homology: resolution stalls ⟺ H_k(N(C)) ≠ 0 for some k > 0.
def HA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HA_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Betti-entropy theorem: S_residual ≥ Σ_k β_k × ln 2.
def HA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HA_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Additive holonomy is trivial (abelian group).
def HG_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HG_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HG_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HG_1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dihedral generator holonomy group is D_{2^n}.
def HG_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HG_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HG_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HG_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Unit multiplication holonomy equals the unit group.
def HG_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HG_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HG_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HG_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Full holonomy group is the affine group.
def HG_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HG_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HG_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HG_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Holonomy group decomposition into dihedral and non-trivial units.
def HG_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HG_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HG_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HG_5_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- A ComputationDatum’s ring value is the content hash of its certificate.
def HO_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HO_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HO_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HO_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Application preserves certification.
def HO_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HO_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HO_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HO_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Composition certification requires both components certified and type-compatible.
def HO_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HO_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HO_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HO_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Fully saturated partial application equals direct application.
def HO_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HO_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HO_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HO_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Pipeline composition: nerve construction + Kan promotion = ψ_7 ∘ ψ_1.
def HP_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HP_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HP_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HP_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Homotopy extraction agrees with homology on k-skeleton.
def HP_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HP_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HP_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HP_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- k-invariant computation detects QLS_4 convergence.
def HP_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HP_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HP_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HP_3_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Complexity bound for homotopy type computation.
def HP_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HP_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HP_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HP_4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- KanComplex(N(C)) — the constraint nerve satisfies the Kan extension condition for all horns of dimension ≤ d where d is the maximum simplex dimension of N(C).
def HT_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Path components of nerve recover β₀: π₀(N(C)) ≅ Z^{β₀} counts the connected components of the constraint configuration.
def HT_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- MN_6 monodromy is abelianisation of full π₁: the fundamental group π₁(N(C)) surjects onto the HolonomyGroup D_{2^n} via abelianisation.
def HT_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Higher homotopy groups vanish above nerve dimension: π_k(N(C)) = 0 for all k > dim(N(C)), because the nerve is a finite CW-complex.
def HT_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_4_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- 1-truncation determines flat/twisted classification: τ_{≤1}(N(C)) captures the holonomy action that distinguishes FlatType from TwistedType.
def HT_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_5_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Trivial k-invariants beyond depth d imply spectral collapse: if κ_k is trivial for all k > d then the spectral sequence collapses at E_{d+2}.
def HT_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_6_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Non-trivial Whitehead product implies lift obstruction: [α, β] ≠ 0 in π_{p+q−1} implies a non-trivial LiftObstruction that Betti numbers alone cannot detect.
def HT_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_7_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Hurewicz isomorphism for first non-vanishing group: π_k(N(C)) ⊗ Z ≅ H_k(N(C); Z) for the smallest k with π_k ≠ 0, linking homotopy invariants to homology.
def HT_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_HT_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_HT_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_HT_8_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inhabitance soundness (bidirectional). For any type:ConstrainedType T, cert:InhabitanceCertificate(T) .verified is true iff the carrier of T is non-empty. Discharge: the verdict reified by cert:InhabitanceCertificate agrees with the denotational carrier-non-emptiness predicate, by direct unfolding of cert:verified against the predicate semantics.
def IH_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IH_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IH_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IH_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inhabitance cost (2-SAT restriction). For instances T satisfying predicate:Is2SatShape, the cost of resolver:TwoSatDecider on T is inherited from the classical 2-SAT decision procedure: O(n+m) where n is the variable count and m the clause count. Unrestricted IH_2 would be equivalent to P = NP and is not adopted.
def IH_2a : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IH_2a_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IH_2a_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IH_2a_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inhabitance cost (Horn-SAT restriction). For instances T satisfying predicate:IsHornShape, the cost of resolver:HornSatDecider on T is inherited from the classical Horn-SAT decision procedure via unit propagation: O(n+m).
def IH_2b : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IH_2b_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IH_2b_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IH_2b_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Carrier preservation under basis reduction. The constraint-nerve reduction performed during inhabitance search preserves the carrier of the input ConstrainedType. Discharged against resolver:JacobianGuidedResolver and resolver:guidingJacobian vocabulary.
def IH_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IH_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IH_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IH_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- d_Δ as interaction cost between entities.
def IN_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Disjoint leases imply commutator = 0.
def IN_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_2_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Shared sites imply commutator > 0.
def IN_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_3_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SR_8 implies negotiation converges (commutator decreases monotonically).
def IN_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_4_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Convergent negotiation selects U(1) ⊂ SU(2).
def IN_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_5_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Outcome space of pairwise negotiation is S².
def IN_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_6_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Mutual modeling selects H ⊂ O.
def IN_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_7_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Interaction nerve β_k bounds coupling complexity at dimension k.
def IN_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_8_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- β_2(nerve) × max_disagreement bounds associator norm.
def IN_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IN_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IN_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IN_9_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Ingested data conforms to the source’s declared type.
def IO_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IO_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IO_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IO_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Emitted data conforms to the sink’s declared type.
def IO_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IO_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IO_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IO_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every ingestion through a source produces a valid ring datum via grounding.
def IO_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IO_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IO_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IO_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every emission through a sink produces a valid surface symbol via projection.
def IO_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IO_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IO_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IO_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every boundary effect touches at least one site.
def IO_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IO_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IO_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IO_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Resolution monotonicity: pinned count never decreases.
def IR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IR_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Resolution convergence bound: at most n iterations.
def IR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IR_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Convergence rate definition.
def IR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IR_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Resolution termination: loop terminates if constraint set spans all sites.
def IR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IR_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Euler-Poincaré formula for constraint nerve.
def IT_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IT_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IT_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IT_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Spectral Euler characteristic.
def IT_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IT_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IT_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IT_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Spectral gap bounds convergence rate from below.
def IT_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IT_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IT_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IT_6_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- UOR index theorem (topological form): total curvature minus Euler characteristic equals residual entropy in bits.
def IT_7a : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IT_7a_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IT_7a_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IT_7a_forAll)
  verificationDomain := #[.indexTheoretic, .analytical, .topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- UOR index theorem (entropy-topology duality).
def IT_7b : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IT_7b_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IT_7b_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IT_7b_forAll)
  verificationDomain := #[.indexTheoretic, .analytical, .topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- UOR index theorem (spectral cost bound): resolution cost ≥ n - χ(N(C)).
def IT_7c : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IT_7c_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IT_7c_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IT_7c_forAll)
  verificationDomain := #[.indexTheoretic, .analytical, .topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- UOR index theorem (completeness criterion): resolution is complete iff χ(N(C)) = n and all Betti numbers vanish.
def IT_7d : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_IT_7d_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_IT_7d_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_IT_7d_forAll)
  verificationDomain := #[.indexTheoretic, .analytical, .topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- In a linear trace, every site is targeted exactly once. Total effect count equals site budget.
def LN_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LN_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LN_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LN_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- After a LinearEffect, the target site is pinned.
def LN_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LN_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LN_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LN_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- A consumed LinearSite cannot be targeted again.
def LN_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LN_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LN_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LN_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease allocation decrements the linear budget by the lease cardinality.
def LN_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LN_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LN_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LN_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease expiry returns claimed sites to the budget.
def LN_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LN_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LN_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LN_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every geodesic trace is a linear trace.
def LN_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LN_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LN_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LN_6_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Suspended lease preserves pinned state.
def LS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LS_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease expiry triggers resource release.
def LS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LS_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Checkpoint restore is idempotent.
def LS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LS_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Active → Suspended → Active round-trip preserves state.
def LS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_LS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_LS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_LS_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease partition conserves total budget: the sum of freeRank over all leases equals the SharedContext freeRank.
def MC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Per-lease binding monotonicity: within a leased sub-domain, freeRank decreases monotonically (SR_1 restricted to lease).
def MC_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- General composition freeRank via inclusion-exclusion.
def MC_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Disjoint-lease composition is additive: the intersection term vanishes when leases are site-disjoint (SR_9).
def MC_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Policy-invariant final binding set: different execution policies produce identical SiteBinding records.
def MC_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Full lease coverage implies composed saturation: k sessions on disjoint covering leases, each locally converged, produce a GroundedContext via composition.
def MC_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Distributed O(1) resolution: a query against a composed GroundedContext resolves in zero steps.
def MC_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_7_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Parallelism bound: per-session resolution work is bounded by lease size, not by total site count n.
def MC_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MC_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MC_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MC_8_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Moduli space dimension equals basis size of any contained type.
def MD_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The site of a ModuliTowerMap at T is empty iff T is a TwistedType at every level.
def MD_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_10_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Zeroth deformation cohomology = automorphism group intersected with dihedral group.
def MD_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- First deformation cohomology = tangent space to the moduli space at T.
def MD_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Second deformation cohomology = LiftObstruction space.
def MD_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_4_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- FlatType stratum has codimension zero in the moduli space.
def MD_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_5_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- TwistedType stratum has codimension at least 1.
def MD_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_6_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- VersalDeformation existence is guaranteed when the obstruction space H² vanishes.
def MD_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- A deformation family preserves completeness iff H²(Def(T_t)) = 0 along the entire path.
def MD_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_8_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The site of a ModuliTowerMap at T has dimension 1 when the obstruction is trivial.
def MD_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MD_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MD_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MD_9_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Holonomy group containment: HolonomyGroup(T) ≤ D_{2^n} for all ConstrainedTypes T over R_n. The holonomy group is always a subgroup of the full dihedral group.
def MN_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Additive flatness (extends OB_H1): if all constraints in T are additive (ResidueConstraint or DepthConstraint type), then HolonomyGroup(T) = {id} — T is a FlatType.
def MN_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dihedral generation: if T contains both a neg-related and a bnot-related constraint in a common closed path, then HolonomyGroup(T) = D_{2^n} — T has full dihedral holonomy.
def MN_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Holonomy-Betti implication: HolonomyGroup(T) ≠ {id} ⟹ β₁(N(C(T))) ≥ 1. Non-trivial monodromy requires a topological loop. (Converse is false: β₁ ≥ 1 does not imply non-trivial holonomy.)
def MN_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_4_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- CompleteType holonomy: a CompleteType (IT_7d: χ = n, all β = 0) has trivial holonomy. IT_7d implies FlatType because IT_7d requires β₁ = 0, which by MN_4 implies trivial monodromy.
def MN_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_5_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Monodromy composition: if p₁ and p₂ are closed constraint paths, then monodromy(p₁ · p₂) = monodromy(p₁) · monodromy(p₂) in D_{2^n} (group homomorphism from loops to dihedral elements).
def MN_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_6_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- TwistedType obstruction class: the monodromy of a TwistedType contributes a non-zero class to H²(N(C(T')); ℤ/2ℤ) where T' is any WittLift of T. TwistedTypes always have non-trivial lift obstructions.
def MN_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_7_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Holonomy classification covering: every ConstrainedType with a computed holonomy group is either flat or twisted, not both.
def MN_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MN_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MN_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MN_8_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Unit law: I ⊗ A ≅ A ≅ A ⊗ I.
def MO_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MO_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MO_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MO_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Associativity: (A⊗B)⊗C ≅ A⊗(B⊗C).
def MO_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MO_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MO_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MO_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Certificate composition: cert(A⊗B) contains cert(A) and cert(B).
def MO_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MO_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MO_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MO_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- σ(A⊗B) ≥ max(σ(A), σ(B)): sequential composition does not lose saturation.
def MO_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MO_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MO_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MO_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- r(A⊗B) ≤ min(r(A), r(B)): residual can only shrink under sequential composition.
def MO_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MO_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MO_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MO_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ModuliResolver boundary agrees with MorphospaceBoundary.
def MR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MR_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- StratificationRecord covers every CompleteType in exactly one stratum.
def MR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MR_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ModuliResolver complexity bound.
def MR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MR_3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Achievable signatures correspond to membership in some HolonomyStratum.
def MR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MR_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Connectivity lower bound: β₀(N(C)) ≥ 1 for all non-empty C.
def MS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MS_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Euler capacity ceiling: χ(N(C)) ≤ n for all C at quantum level n.
def MS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MS_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Betti monotonicity under addition: χ(N(C + c)) ≥ χ(N(C)) for any constraint c added to C.
def MS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MS_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Level-relative achievability: a signature achievable at quantum level n remains achievable at level n+1.
def MS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MS_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Empirical completeness convergence: verified SynthesisSignature individuals converge to the exact morphospace boundary.
def MS_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_MS_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_MS_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_MS_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- nerve(C₁ ∪ C₂) = nerve(C₁) ∪ nerve(C₂) for disjoint constraint domains.
def NV_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_NV_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_NV_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_NV_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Mayer–Vietoris: β_k(C₁ ∪ C₂) computable from parts and intersection.
def NV_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_NV_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_NV_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_NV_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Single constraint addition: Δβ_k ∈ {−1, 0, +1} per dimension.
def NV_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_NV_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_NV_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_NV_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Constraint accumulation monotonicity: β_k non-increasing under SR_1. Derives from SR_1.
def NV_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_NV_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_NV_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_NV_4_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Ostrowski product formula at p=2: |2|_2 · |2|_∞ = 1. The 2-adic and Archimedean absolute values of 2 are multiplicative inverses.
def OA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OA_1_forAll)
  verificationDomain := #[.arithmeticValuation]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Crossing cost equals ln 2: the Archimedean image of one unit of 2-adic valuation, under the product formula, is ln 2 nats.
def OA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OA_2_forAll)
  verificationDomain := #[.arithmeticValuation]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- QM_1 grounding: the Landauer cost β* = ln 2 is the crossing cost from OA_2, derived from the prime p=2 that structures the Witt tower.
def OA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OA_3_forAll)
  verificationDomain := #[.arithmeticValuation]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Born rule bridge (conditional on amplitude rationality): P(outcome k) = |α_k|_∞², where |·|_∞ is the Archimedean image of the 2-adic amplitude via the product formula.
def OA_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OA_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OA_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OA_4_forAll)
  verificationDomain := #[.arithmeticValuation]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Entropy per delta-level equals the crossing cost: each application of the delta-operator (division by 2) costs ln 2 nats in the Archimedean completion, which is the per-bit Landauer cost. v0.2.2 Phase C.4: grounds MultiplicationCertificate.
def OA_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OA_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OA_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OA_5_forAll)
  verificationDomain := #[.arithmeticValuation]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Negation-complement commutator is constant 2.
def OB_C1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_C1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_C1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_C1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Negation-translation commutator.
def OB_C2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_C2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_C2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_C2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Complement-XOR commutator is trivial.
def OB_C3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_C3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_C3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_C3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Additive paths have trivial monodromy (abelian ⇒ path-independent).
def OB_H1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_H1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_H1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_H1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dihedral generator paths have monodromy in D_{2^n}.
def OB_H2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_H2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_H2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_H2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Successor-only closed path winding number.
def OB_H3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_H3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_H3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_H3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Ring metric triangle inequality.
def OB_M1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_M1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_M1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_M1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Hamming metric triangle inequality.
def OB_M2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_M2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_M2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_M2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Incompatibility metric is the absolute difference of ring and Hamming metrics.
def OB_M3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_M3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_M3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_M3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Negation preserves ring metric.
def OB_M4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_M4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_M4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_M4_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Bitwise complement preserves Hamming metric.
def OB_M5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_M5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_M5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_M5_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Successor preserves ring metric but may change Hamming metric.
def OB_M6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_M6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_M6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_M6_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Path length is additive under concatenation.
def OB_P1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_P1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_P1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_P1_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Total variation is subadditive under concatenation.
def OB_P2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_P2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_P2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_P2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Reduction length is additive under sequential composition.
def OB_P3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OB_P3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OB_P3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OB_P3_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Adjacent stages with compatible guards may fuse.
def OE_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OE_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OE_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OE_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Independent effects commute.
def OE_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OE_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OE_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OE_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Disjoint leases parallelize.
def OE_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OE_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OE_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OE_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Stage fusion preserves semantics.
def OE_4a : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OE_4a_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OE_4a_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OE_4a_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Effect commutation preserves outcome.
def OE_4b : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OE_4b_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OE_4b_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OE_4b_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease parallelism preserves coverage.
def OE_4c : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OE_4c_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OE_4c_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OE_4c_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Benefit of a constraint is the number of new pins it provides.
def OO_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OO_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OO_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OO_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint cost is step or popcount depending on type.
def OO_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OO_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OO_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OO_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Greedy selection maximizes benefit-to-cost ratio.
def OO_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OO_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OO_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OO_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Greedy approximation achieves (1 − 1/e) ≈ 63% of optimal.
def OO_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OO_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OO_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OO_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Tiebreaker: prefer vertical (residue) before horizontal (carry).
def OO_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OO_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OO_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OO_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Site additivity: siteCount(F(G)) = F.sites + Σ_i G_i.sites.
def OP_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OP_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OP_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OP_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Grounding distributivity: grounding(F(G(x))) = F.ground(G.ground(x)).
def OP_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OP_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OP_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OP_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- d_Δ decomposition: d_Δ(F(G)) decomposes into outer + inner d_Δ.
def OP_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OP_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OP_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OP_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Table(Tuple(fields)): standard tabular data structure decomposition.
def OP_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OP_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OP_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OP_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Tree(leaves): standard hierarchical data structure (AST, XML, JSON).
def OP_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_OP_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_OP_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_OP_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Disjoint parallel computations commute: A ⊗ B = B ⊗ A when site targets are disjoint.
def PAR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PAR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PAR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PAR_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Parallel free-count deltas are additive.
def PAR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PAR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PAR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PAR_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Partitioning is exhaustive: component cardinalities sum to total site budget.
def PAR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PAR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PAR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PAR_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- All interleavings of disjoint parallel computations yield the same final context.
def PAR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PAR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PAR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PAR_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Parallel certificate is the conjunction of component certificates plus disjointness.
def PAR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PAR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PAR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PAR_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Accumulation permutation invariance: accumulating bindings in any order yields the same saturated context (derives from SR_10).
def PA_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PA_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PA_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PA_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Accumulation monotonicity: α(b,C) ⊇ C (the context only grows, never loses bindings). Derives from SR_1.
def PA_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PA_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PA_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PA_2_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Accumulation soundness: α(b,C) preserves all previously satisfied constraints. Derives from SR_2.
def PA_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PA_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PA_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PA_3_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Accumulation base preservation: α does not modify previously pinned sites.
def PA_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PA_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PA_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PA_4_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Accumulation identity: α(∅, C) = C.
def PA_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PA_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PA_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PA_5_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Phase space definition.
def PD_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PD_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PD_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PD_1_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Phase boundaries occur where g divides 2^n − 1 or g is a power of 2.
def PD_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PD_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PD_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PD_2_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Parity boundary divides R_n into equal halves.
def PD_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PD_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PD_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PD_3_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Resonance lines in the phase diagram.
def PD_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PD_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PD_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PD_4_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Phase count at level n is at most 2^n (typically O(n)).
def PD_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PD_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PD_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PD_5_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Stage 0 initializes state vector to 1.
def PE_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PE_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PE_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PE_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Stage 1 dispatches resolver (δ selects).
def PE_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PE_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PE_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PE_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Stage 2 produces valid ring address (G grounds).
def PE_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PE_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PE_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PE_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Stage 3 resolves constraints (Π terminates).
def PE_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PE_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PE_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PE_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Stage 4 accumulates without contradiction (α consistent).
def PE_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PE_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PE_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PE_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Stage 5 extracts coherent output (P projects).
def PE_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PE_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PE_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PE_6_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Full pipeline is the composition PE_6 ∘ … ∘ PE_1.
def PE_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PE_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PE_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PE_7_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inference idempotence: ι(ι(s,C),C) = ι(s,C) on GroundedContext.
def PI_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PI_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PI_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PI_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inference soundness: ι(s,C) resolves to a type consistent with C.
def PI_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PI_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PI_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PI_2_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inference composition: ι = P ∘ Π ∘ G (references phi_4).
def PI_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PI_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PI_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PI_3_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inference complexity: O(n) worst case, O(1) on CompleteType.
def PI_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PI_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PI_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PI_4_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Inference coherence: roundTrip(P(Π(G(s)))) = s.
def PI_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PI_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PI_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PI_5_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Composition validity: κ(S₁,S₂) is a valid session if S₁,S₂ have disjoint leases (derives from SR_8).
def PK_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PK_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PK_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PK_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Distributed resolution: resolving in κ(S₁,S₂) equals resolving in S₁ or S₂ independently (derives from MC_7).
def PK_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PK_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PK_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PK_2_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease disjointness: partitioned leases have pairwise disjoint site sets (derives from SR_9).
def PL_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PL_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PL_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PL_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease conservation: union of all leases equals the original shared context (derives from MC_1).
def PL_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PL_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PL_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PL_2_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease coverage: every site in the shared context appears in exactly one lease (derives from MC_6).
def PL_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PL_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PL_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PL_3_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Phase rotation Ω^k at stage k.
def PM_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PM_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PM_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PM_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Phase gate checks Ω^k at boundary.
def PM_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PM_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PM_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PM_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Gate failure triggers complex conjugate rollback.
def PM_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PM_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PM_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PM_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Rollback is involutory: (z̄)̄ = z.
def PM_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PM_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PM_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PM_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Epoch boundary preserves saturation.
def PM_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PM_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PM_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PM_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Service window provides base context.
def PM_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PM_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PM_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PM_6_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Machine is deterministic given initial state.
def PM_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PM_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PM_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PM_7_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Pipeline unification: κ(λₖ(α*(ι(s,·))),C) = resolve(s,C). The full composed pipeline equals the top-level resolution function.
def PP_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PP_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PP_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PP_1_forAll)
  verificationDomain := #[.composedAlgebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every predicate is total: evaluation terminates for all inputs.
def PR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PR_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every predicate is pure: evaluation does not modify state.
def PR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PR_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Exhaustive + mutually exclusive dispatch is deterministic.
def PR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PR_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Match evaluation is deterministic given exhaustive, ordered arms.
def PR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PR_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Stage transition requires typed guard satisfaction.
def PR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PR_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Product type site additivity: siteBudget(A × B) = siteBudget(A) + siteBudget(B).
def PT_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PT_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PT_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PT_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Product type partition product: partition(A × B) = partition(A) ⊗ partition(B). v0.2.2 T1.3: grounds cert:PartitionCertificate — every PartitionCertificate attests the PartitionComponent classification (Irreducible / Reducible / Units / Exterior) derived from this partition-map identity.
def PT_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PT_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PT_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PT_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Product type partition tensor: the partition of a product type is the tensor product of the component partitions.
def PT_2a : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PT_2a_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PT_2a_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PT_2a_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Sum type partition coproduct: the partition of a sum type is the coproduct of the variant partitions.
def PT_2b : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PT_2b_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PT_2b_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PT_2b_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Product type Euler additivity: χ(N(C(A × B))) = χ(N(C(A))) + χ(N(C(B))).
def PT_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PT_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PT_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PT_3_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Product type entropy additivity: S(A × B) = S(A) + S(B).
def PT_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PT_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PT_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PT_4_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Zero exponent identity: pow(a, 0) = 1.
def PW_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PW_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PW_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PW_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Unit exponent identity: pow(a, 1) = a.
def PW_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PW_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PW_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PW_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Additive exponent decomposition: pow(a, add(b, c)) = mul(pow(a, b), pow(a, c)).
def PW_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_PW_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_PW_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_PW_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- neg(bnot(x)) = succ(x) holds in Z/(2ⁿ)Z for all n ≥ 1. Universal form of the Critical Identity across all quantum levels.
def QL_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The dihedral group D_{2ⁿ} generated by neg and bnot has order 2ⁿ⁺¹ for all n ≥ 1.
def QL_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The reduction distribution P(j) = 2^{-j} is the Boltzmann distribution at β* = ln 2 for all n ≥ 1.
def QL_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_3_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The site budget of a PrimitiveType at quantum level n is exactly n (one site per bit).
def QL_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Resolution complexity for a CompleteType is O(1) for all n ≥ 1.
def QL_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Content addressing is a bijection for all n ≥ 1 (AD_1/AD_2 universality).
def QL_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The ψ-pipeline produces a valid ChainComplex for any ConstrainedType at any quantum level n ≥ 1.
def QL_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_7_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Witt level chain inverse: wittLevelPredecessor is the left inverse of nextWittLevel.
def QL_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QL_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QL_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QL_8_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Von Neumann–Landauer bridge: S_vonNeumann(ψ) = Cost_Landauer(collapse(ψ)).
def QM_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QM_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QM_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QM_1_forAll)
  verificationDomain := #[.quantumThermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Measurement as site topology change: projective collapse on a SuperposedSiteState is topologically equivalent to applying a ResidueConstraint that pins the collapsed site.
def QM_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QM_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QM_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QM_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Superposition entropy bound: 0 ≤ S_vN(ψ) ≤ ln 2 for any single-site SuperposedSiteState.
def QM_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QM_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QM_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QM_3_forAll)
  verificationDomain := #[.quantumThermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Collapse idempotence: collapse(collapse(ψ)) = collapse(ψ). Measurement on an already-collapsed state is a no-op.
def QM_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QM_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QM_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QM_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Amplitude normalization (Born rule): the sum of squared amplitudes equals 1 for any well-formed SuperposedSiteState.
def QM_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QM_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QM_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QM_5_forAll)
  verificationDomain := #[.quantumThermodynamic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Amplitude index set characterization: the amplitude index set of a SuperposedSiteState over ConstrainedType T at Q_n is the set of monotone pinning trajectories consistent with T's constraints.
def QM_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_QM_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_QM_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_QM_6_forAll)
  verificationDomain := #[.superpositionDomain]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Reversible pinning stores prior state in ancilla site.
def RC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RC_1_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Reversible pinning has zero total entropy change.
def RC_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RC_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RC_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RC_2_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Deferred Landauer cost at ancilla erasure.
def RC_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RC_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RC_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RC_3_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Reversible total cost equals irreversible total cost (redistributed).
def RC_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RC_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RC_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RC_4_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Quantum UOR: superposed sites, cost proportional to winning path.
def RC_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RC_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RC_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RC_5_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Amplitude renormalization: a SuperposedSiteState can always be normalized to satisfy QM_5.
def RC_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RC_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RC_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RC_6_forAll)
  verificationDomain := #[.superpositionDomain]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Resolution determinism: same type and constraint sequence yield unique partition.
def RD_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RD_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RD_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RD_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Order independence: complete constraint sets yield the same partition regardless of order.
def RD_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RD_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RD_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RD_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Resolution strategy equivalence: dihedral, canonical-form, and evaluation resolvers all compute the same partition.
def RE_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RE_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RE_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RE_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- The working set is determined by the constraint nerve and the stage’s site targets.
def RG_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RG_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RG_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RG_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- All addresses within a region are within the region’s diameter under the chosen metric.
def RG_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RG_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RG_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RG_2_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Total working set size is bounded by the addressable space at the quantum level.
def RG_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RG_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RG_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RG_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The resolver at stage k accesses only addresses within its working set.
def RG_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_RG_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_RG_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_RG_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Additive associativity: add(x, add(y, z)) = add(add(x, y), z).
def R_A1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_A1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_A1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_A1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Additive identity: add(x, 0) = x.
def R_A2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_A2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_A2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_A2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Additive inverse: add(x, neg(x)) = 0.
def R_A3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_A3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_A3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_A3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Additive commutativity: add(x, y) = add(y, x).
def R_A4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_A4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_A4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_A4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Subtraction definition: sub(x, y) = add(x, neg(y)).
def R_A5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_A5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_A5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_A5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Negation involution: neg(neg(x)) = x.
def R_A6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_A6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_A6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_A6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Multiplicative associativity: mul(x, mul(y, z)) = mul(mul(x, y), z).
def R_M1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_M1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_M1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_M1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Multiplicative identity: mul(x, 1) = x.
def R_M2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_M2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_M2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_M2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Multiplicative commutativity: mul(x, y) = mul(y, x).
def R_M3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_M3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_M3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_M3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Distributivity: mul(x, add(y, z)) = add(mul(x, y), mul(x, z)).
def R_M4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_M4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_M4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_M4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Annihilation: mul(x, 0) = 0.
def R_M5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_R_M5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_R_M5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_R_M5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Subtyping is constraint superset: more constraints = more specific.
def SB_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SB_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SB_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SB_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Subtype has fewer valid resolutions.
def SB_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SB_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SB_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SB_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The constraint nerve of the supertype is a sub-complex of the subtype’s nerve.
def SB_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SB_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SB_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SB_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Covariance preserves inclusion.
def SB_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SB_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SB_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SB_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Contravariance reverses inclusion.
def SB_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SB_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SB_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SB_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lattice depth equals site budget.
def SB_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SB_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SB_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SB_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ScalarType grounding: quantize(value, range, bits) produces ring element where d_R reflects value proximity.
def SD_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SymbolType grounding: argmin_{encoding} Σ d_Δ over observed pairs (CY_5).
def SD_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SequenceType = free monoid on element type with backbone constraint.
def SD_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- TupleType site count = Σ field site counts, site ordering follows CY_6.
def SD_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- GraphType constraint nerve = graph nerve, β_k equality.
def SD_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SetType d_Δ invariant under element permutation, D_{2n} symmetry.
def SD_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- TreeType β_1=0, β_0=1 topological constraints.
def SD_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- TableType = SequenceType(TupleType(S)), functorial decomposition.
def SD_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SD_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SD_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SD_8_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Evaluation resolver directly computes the set-theoretic partition.
def SE_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SE_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SE_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SE_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dihedral factorization resolver yields the same partition via orbit decomposition.
def SE_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SE_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SE_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SE_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Canonical form resolver yields the same partition via confluent rewrite.
def SE_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SE_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SE_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SE_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- All three strategies compute the same set-theoretic partition.
def SE_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SE_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SE_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SE_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Optimal resolution level for a factor: n ≡ 0 (mod ord_g(2)).
def SF_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SF_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SF_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SF_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint ordering by step cost: smaller step_g first.
def SF_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SF_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SF_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SF_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Classical embedding: superposition resolution of a classical (non-superposed) datum reduces to classical resolution.
def SP_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SP_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SP_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SP_1_forAll)
  verificationDomain := #[.superpositionDomain]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Collapse–resolve commutativity: collapsing then resolving classically equals resolving in superposition then collapsing.
def SP_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SP_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SP_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SP_2_forAll)
  verificationDomain := #[.quantumThermodynamic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Amplitude preservation: the SuperpositionResolver preserves the normalized amplitude vector during ψ-pipeline traversal.
def SP_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SP_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SP_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SP_3_forAll)
  verificationDomain := #[.superpositionDomain]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Born rule outcome probability: the probability of collapsing to site k equals the squared amplitude of that site.
def SP_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SP_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SP_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SP_4_forAll)
  verificationDomain := #[.quantumThermodynamic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every epoch terminates: the reduction within each epoch reaches convergence angle π.
def STR_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_STR_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_STR_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_STR_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Grounding preservation across epoch boundaries.
def STR_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_STR_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_STR_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_STR_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Every finite prefix computes in finite time.
def STR_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_STR_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_STR_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_STR_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The first epoch starts from the unfold seed context.
def STR_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_STR_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_STR_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_STR_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Each subsequent epoch starts from the previous boundary’s continuation context.
def STR_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_STR_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_STR_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_STR_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lease expiry at an epoch boundary returns claimed sites to the next epoch’s linear budget.
def STR_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_STR_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_STR_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_STR_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Sum type site budget: siteBudget(A + B) = max(siteBudget(A), siteBudget(B)).
def ST_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- SumType Betti corollary for PartitionCoproduct constructions: combining ST_8's disjointness guarantee with the universal ST_4 yields Betti additivity at every dimension for any A + B assembled via the PartitionCoproduct construction.
def ST_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_10_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Sum type entropy: S(A + B) = ln 2 + max(S(A), S(B)).
def ST_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_2_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- SumType Euler characteristic additivity: for a SumType with topologically disjoint component nerves, the Euler characteristic is additive.
def ST_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_3_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SumType Betti number additivity: for disjoint component nerves, all Betti numbers are additive.
def ST_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_4_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SumType completeness transfer: a SumType A+B is CompleteType iff both A and B are CompleteType and they have equal quantum levels.
def ST_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_5_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- SumType tag site unique existence: every PartitionCoproduct A + B has exactly one tag site, logically distinct from every data site of A and B, carrying the ln 2 entropy quantum of ST_2.
def ST_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- SumType variant tagging: the constraints of a PartitionCoproduct decompose into the operands' constraints plus one tag-pinning constraint per variant (tag = 0 for the left variant, tag = 1 for the right).
def ST_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- SumType variant nerve disjointness bridge: a PartitionCoproduct constructed via ST_6 + ST_7 + the foundation layout convention produces topologically disjoint component nerves. Bridges the existing conditional ST_3 and ST_4 to PartitionCoproduct constructions concretely.
def ST_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_8_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- SumType Euler corollary for PartitionCoproduct constructions: combining ST_8's disjointness guarantee with the universal ST_3 yields Euler additivity for any A + B assembled via the PartitionCoproduct construction.
def ST_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_ST_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_ST_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_ST_9_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Service window bounds context memory.
def SW_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SW_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SW_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SW_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Window slide preserves saturation invariant.
def SW_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SW_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SW_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SW_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Evicted epochs release lease resources.
def SW_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SW_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SW_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SW_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Window size ≥ 1 (non-empty).
def SW_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_SW_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_SW_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_SW_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Entropy of a site budget state.
def TH_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_1_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Type resolution is measurement; cost ≥ entropy removed.
def TH_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_10_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Maximum entropy: unconstrained state has S = n × ln 2.
def TH_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_2_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Zero entropy: fully resolved state has S = 0.
def TH_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_3_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Landauer bound on total resolution cost.
def TH_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_4_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Critical inverse temperature for UOR site system.
def TH_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_5_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraint application removes entropy; convergence rate equals cooling rate.
def TH_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_6_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- CatastropheThreshold is the temperature of a partition phase transition.
def TH_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_7_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Step formula as free-energy cost of a constraint boundary.
def TH_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_8_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Computational hardness maps to type incompleteness (high temperature).
def TH_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TH_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TH_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TH_9_forAll)
  verificationDomain := #[.thermodynamic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- AllOrNothing transaction rolls back on any failure.
def TJ_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TJ_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TJ_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TJ_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- BestEffort transaction commits partial results.
def TJ_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TJ_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TJ_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TJ_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Transaction atomicity within a single epoch.
def TJ_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TJ_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TJ_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TJ_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Nerve realisability: for any target (χ*, β₀* = 1, β_k* = 0 for k ≥ 1) with χ* ≤ n, there exists a ConstrainedType T over R_n whose constraint nerve realises the target.
def TS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- ForbiddenSignature membership criterion: a topological signature is a ForbiddenSignature iff no ConstrainedType with at most n constraints realises it at quantum level Q_n.
def TS_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_10_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Minimal basis bound: for the IT_7d target (χ* = n, all β* = 0), the MinimalConstraintBasis has size exactly n (one constraint per site position). No redundant constraints exist in the minimal basis.
def TS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Synthesis monotonicity: adding a constraint to a synthesis candidate never decreases the Euler characteristic of the resulting nerve (χ is monotone non-decreasing under constraint addition).
def TS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Synthesis convergence: the TypeSynthesisResolver terminates for any realisable target in at most n constraint additions (for n-site types).
def TS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Synthesis–certification duality: a SynthesizedType T achieves the IT_7d target if and only if the CompletenessResolver certifies T as a CompleteType. The forward ψ-pipeline and the inverse TypeSynthesisResolver are dual computations.
def TS_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Jacobian-guided synthesis efficiency: using the Jacobian (DC_10) to select the next constraint reduces the expected number of synthesis steps from O(n²) (uninformed) to O(n log n) (Jacobian-guided).
def TS_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_6_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Unreachable signatures: a Betti profile with β₀ = 0 is unreachable by any non-empty ConstrainedType — the nerve of a non-empty constraint set is always connected (β₀ ≥ 1).
def TS_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_7_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Betti-1 minimum constraint count: the minimum number of constraints needed to achieve first Betti number beta_1 = k in the constraint nerve is 2k + 1.
def TS_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_8_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- TypeSynthesisResolver termination: the resolver terminates in at most 2^n steps for any target signature at quantum level Q_n, returning either a ConstrainedType or a ForbiddenSignature certificate.
def TS_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_TS_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_TS_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_TS_9_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Dihedral group acts on constraints by transforming them.
def T_A1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_A1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_A1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_A1_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dihedral group action on partitions permutes components.
def T_A2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_A2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_A2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_A2_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Dihedral orbits determine irreducibility boundaries.
def T_A3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_A3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_A3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_A3_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Fixed points of negation are {0, 2^{n−1}}; bnot has no fixed points.
def T_A4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_A4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_A4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_A4_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Category left identity: compose(id, f) = f.
def T_C1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_C1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_C1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_C1_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Category right identity: compose(f, id) = f.
def T_C2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_C2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_C2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_C2_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Category associativity of transform composition.
def T_C3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_C3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_C3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_C3_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Composability condition: f composesWith g iff target(f) = source(g).
def T_C4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_C4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_C4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_C4_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding injectivity.
def T_E1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_E1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_E1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_E1_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding is a ring homomorphism.
def T_E2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_E2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_E2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_E2_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding transitivity: composition of embeddings is an embedding.
def T_E3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_E3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_E3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_E3_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Embedding address coherence: glyph ∘ ι ∘ addresses is well-defined.
def T_E4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_E4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_E4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_E4_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Negation is a ring-metric isometry.
def T_I1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_I1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_I1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_I1_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Bitwise complement is a Hamming-metric isometry.
def T_I2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_I2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_I2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_I2_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Successor as composed isometries: succ = neg ∘ bnot preserves d_R but not d_H.
def T_I3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_I3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_I3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_I3_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Ring isometries form a group under composition.
def T_I4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_I4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_I4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_I4_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- CurvatureObservable measures failure of ring isometry to be Hamming isometry.
def T_I5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_T_I5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_T_I5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_T_I5_forAll)
  verificationDomain := #[.geometric]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Unit group decomposition: R_n× ≅ Z/2 × Z/2^[n-2] for n ≥ 3.
def U_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_U_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_U_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_U_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Unit group special cases: R_1× ≅ [1]; R_2× ≅ Z/2.
def U_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_U_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_U_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_U_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Multiplicative order: ord(u) = lcm(ord((-1)^a), ord(3^b)).
def U_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_U_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_U_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_U_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Resonance period: ord_g(2) divides φ(g).
def U_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_U_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_U_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_U_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Step formula derivation: step_g = 2 * ((g - (2^n mod g)) mod g) + 1.
def U_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_U_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_U_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_U_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Witt coordinate identification: the bit coordinates (x_0, …, x_[n−1]) of x ∈ Z/(2ⁿ)Z are exactly its Witt coordinates under the canonical isomorphism W_n(F_2) ≅ Z/(2ⁿ)Z.
def WC_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The δ-ring Frobenius lift on W_n(F_2) is the identity map because F_2 is a perfect field of characteristic 2 (a² = a for a ∈ F_2).
def WC_10 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_10_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_10_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_10_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The Verschiebung on W_n(F_2) is multiplication by 2: V(x) = 2x = add(x,x). This is a coordinate shift with zero Witt defect.
def WC_11 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_11_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_11_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_11_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The δ-operator on W_n(F_2) is the squaring defect divided by 2: δ(x) = (x − mul(x,x)) / 2. Expressible entirely in existing op/ primitives (sub, mul, arithmetic right shift).
def WC_12 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_12_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_12_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_12_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Witt sum correction equals carry: the k-th Witt addition polynomial correction term S_k − x_k − y_k (mod 2) is exactly the carry c_k(x,y).
def WC_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Carry recurrence is the Witt polynomial recurrence: CA_2 implements the ghost equation for S_[k+1] at p=2.
def WC_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- The δ-correction at level k equals the single-level carry c_[k+1](x,y). Each application of δ divides by 2, consuming one unit of 2-adic valuation.
def WC_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- LiftObstruction is equivalent to δ-nonvanishing: a nontrivial LiftObstruction at Q_[k+1] means δ_k ≠ 0 for some element pair.
def WC_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_5_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Metric discrepancy equals Witt defect: d_Δ(x,y) > 0 iff the ghost map correction (carry) is nonzero.
def WC_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_6_forAll)
  verificationDomain := #[.analytical]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- D_1 is the Witt truncation order relation: succ^[2ⁿ](x) = x is the group relation r^[2ⁿ] = 1 in the Witt-Burnside ring of D_[2∞].
def WC_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- D_3 is the Witt-Burnside conjugation relation: neg(succ(neg(x))) = pred(x) is srs = r⁻¹ in the pro-dihedral group.
def WC_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_8_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- D_4 is a Witt-Burnside reflection composition: bnot(neg(x)) = pred(x) is the product of two reflections yielding inverse rotation.
def WC_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WC_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WC_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WC_9_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Lift unobstructedness criterion: WittLift T' is a CompleteType iff the spectral sequence E_r^{p,q} collapses at E_2 (d_2 = 0 and all higher differentials zero).
def WLS_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WLS_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WLS_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WLS_1_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Obstruction localisation: a non-trivial LiftObstruction is localised to a specific site at bit position n+1. The obstruction class lives in H²(N(C(T))) and is killed by adding one constraint involving the new site.
def WLS_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WLS_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WLS_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WLS_2_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Monotone lifting for trivially obstructed types: if T is a CompleteType at Q_n and its constraint set is closed under the Q_{n+1} extension map, then T' is a CompleteType at Q_{n+1} with basisSize(T') = basisSize(T) + 1.
def WLS_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WLS_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WLS_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WLS_3_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Spectral sequence convergence bound: for constraint configurations of homological depth d (H_k(N(C(T))) = 0 for k > d), the spectral sequence converges by page E_{d+2}.
def WLS_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WLS_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WLS_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WLS_4_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Universal identity preservation: every op:universallyValid identity holds in ℤ/(2^{n+1})ℤ with the lifted constraint set. The lift does not invalidate any certified universal identity.
def WLS_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WLS_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WLS_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WLS_5_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ψ-pipeline universality for quantum lifts: the ψ-pipeline produces a valid ChainComplex for any WittLift T' — the chain complex of T' restricts to the chain complex of T on the base nerve, and the extension is well-formed by construction.
def WLS_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WLS_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WLS_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WLS_6_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- LiftChain(Q_j, Q_k) is valid CompleteType tower iff every chainStep WittLift has trivial or resolved LiftObstruction.
def WT_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_1_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Obstruction count bound: the number of non-trivial LiftObstructions in a chain is at most the chain length.
def WT_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_2_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Resolved basis size formula: the basis size at Q_k equals basisSize(Q_j) + chainLength + obstructionResolutionCost.
def WT_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_3_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Flat tower characterization: isFlat(chain) iff obstructionCount = 0 iff HolonomyGroup trivial at every step.
def WT_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_4_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- LiftChainCertificate existence: a CompleteType at Q_k satisfies IT_7d with a witness chain.
def WT_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_5_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Single-step reduction: QT_3 with chainLength=1 and cost=0 reduces to QLS_3.
def WT_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_6_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Flat chain basis size: for flat chains, resolvedBasisSize(Q_k) = basisSize(Q_j) + (k - j).
def WT_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_7_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ObstructionChain length bound: the length of the ObstructionChain from Q_j to Q_k is at most (k-j) times C(basisSize(Q_j), 3).
def WT_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_8_forAll)
  verificationDomain := #[.indexTheoretic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- TowerCompletenessResolver termination: the resolver terminates for any finite LiftChain within the QT_8 bound, producing a CompleteType certificate or a bounded ObstructionChain.
def WT_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_WT_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_WT_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_WT_9_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Neg via subtraction: neg(x) = sub(0, x).
def X_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_X_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_X_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_X_1_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Complement via XOR: bnot(x) = xor(x, 2^n - 1).
def X_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_X_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_X_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_X_2_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Succ via addition: succ(x) = add(x, 1).
def X_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_X_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_X_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_X_3_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Pred via subtraction: pred(x) = sub(x, 1).
def X_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_X_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_X_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_X_4_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Neg-bnot bridge: neg(x) = add(bnot(x), 1).
def X_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_X_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_X_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_X_5_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Complement predecessor: bnot(x) = pred(neg(x)).
def X_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_X_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_X_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_X_6_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- XOR-add bridge: xor(x, y) = add(x, y) - 2 * and(x, y) (in Z before mod).
def X_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_X_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_X_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_X_7_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- α(b, C) = C': accumulates a binding into a resolution context, pinning a site. Non-commutative, associative at convergence (SR_10).
def accumulate : UOR.Kernel.Op.AccumulationOperation UOR.Prims.Standard := {
  accumulationBase := none
  accumulationBinding := none
  composedOfOps := #[]
  operatorDomainType := none
  operatorRangeType := none
  operatorComplexity := none
  operatorIdempotent := none
  composedOperatorCount := none
  isInvolutory := none
  convergenceGuarantee := none
  compositionResult := none
  compositionComponents := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := none
  outputClass := none
  hasWitness := #[]
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.siteBinding)
  inverse := none
  composedOf := none
  isRingOp := none
}

-- Bitwise and: and(x, y) = x ∧ y. Commutative, associative.
def and : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (true)
  associative := some (true)
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.hypercubeProjection)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- Hypercube reflection: bnot(x) = (2^n - 1) ⊕ x (bitwise complement). The second generator of D_{2^n}. bnot(bnot(x)) = x.
def bnot : UOR.Kernel.Op.Involution UOR.Prims.Standard := {
  arity := some ((1 : Nat))
  hasGeometricCharacter := some (.hypercubeReflection)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- κ(S₁, S₂) = S₁ ∪ S₂: composes two sessions with disjoint leases into one. Commutative, associative (SR_8).
def compose_op : UOR.Kernel.Op.SessionCompositionOperation UOR.Prims.Standard := {
  compositionLeftSession := none
  compositionRightSession := none
  composedOfOps := #[]
  operatorDomainType := none
  operatorRangeType := none
  operatorComplexity := none
  operatorIdempotent := none
  composedOperatorCount := none
  isInvolutory := none
  convergenceGuarantee := none
  compositionResult := none
  compositionComponents := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := none
  outputClass := none
  hasWitness := #[]
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.sessionMerge)
  inverse := none
  composedOf := none
  isRingOp := none
}

-- Byte-sequence concatenation: concat(x, y) = x ⧺ y. The substrate's byte-packing primitive — admits header serialization and other byte-array construction patterns. Result length is len(x) + len(y), carried by the source-polymorphic TermValue (inline up to the application's carrier width, else borrowed or streamed — no fixed ceiling).
def concat : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := none
  associative := some (true)
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.hypercubeJoin)
  inverse := none
  composedOf := none
  isRingOp := some (false)
}

-- The foundational theorem of the UOR kernel: neg(bnot(x)) = succ(x) for all x in R_n. This identity links the two involutions (neg and bnot) to the successor operation, making succ derivable from neg and bnot.
def criticalIdentity : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_criticalIdentity_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_criticalIdentity_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_criticalIdentity_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- δ(q, R) = R(q): dispatches a query to the matching resolver in the registry. Non-commutative, non-associative.
def dispatch : UOR.Kernel.Op.DispatchOperation UOR.Prims.Standard := {
  dispatchSource := none
  dispatchTarget := none
  composedOfOps := #[]
  operatorDomainType := none
  operatorRangeType := none
  operatorComplexity := none
  operatorIdempotent := none
  composedOperatorCount := none
  isInvolutory := none
  convergenceGuarantee := none
  compositionResult := none
  compositionComponents := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := none
  outputClass := none
  hasWitness := #[]
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.constraintSelection)
  inverse := none
  composedOf := none
  isRingOp := none
}

-- Euclidean quotient: div(a, b) = q where a = q·b + r, 0 ⇐ r < b. Total on the ring for b > 0; b = 0 emits a ShapeViolation. Operands read as unsigned big-endian integers at the operand width.
def div : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (false)
  associative := some (false)
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.quotient)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- Byte-level greater-than-or-equal: ge(x, y) = 1 if x ≥ y else 0. Operands compared as big-endian unsigned byte sequences.
def ge : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := none
  associative := none
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.constraintSelection)
  inverse := none
  composedOf := none
  isRingOp := some (false)
}

-- Byte-level greater-than: gt(x, y) = 1 if x > y else 0. Operands compared as big-endian unsigned byte sequences.
def gt : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := none
  associative := none
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.constraintSelection)
  inverse := none
  composedOf := none
  isRingOp := some (false)
}

-- ι(s, C) = P(Π(G(s, C))): the φ-pipeline composed into a single inference step. Non-commutative, non-associative.
def infer : UOR.Kernel.Op.InferenceOperation UOR.Prims.Standard := {
  inferenceSource := none
  inferenceTarget := none
  inferencePipeline := none
  composedOfOps := #[]
  operatorDomainType := none
  operatorRangeType := none
  operatorComplexity := none
  operatorIdempotent := none
  composedOperatorCount := none
  isInvolutory := none
  convergenceGuarantee := none
  compositionResult := none
  compositionComponents := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := none
  outputClass := none
  hasWitness := #[]
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.resolutionTraversal)
  inverse := none
  composedOf := none
  isRingOp := none
}

-- Carry-carry joint satisfiability: two CarryConstraints are jointly satisfiable iff their bit-patterns have no conflicting positions.
def jsat_CC : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_jsat_CC_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_jsat_CC_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_jsat_CC_forAll)
  verificationDomain := #[.enumerative]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Carry-residue joint satisfiability: a CarryConstraint and ResidueConstraint are jointly satisfiable iff the carry-pinned sites are compatible with the residue class.
def jsat_CR : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_jsat_CR_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_jsat_CR_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_jsat_CR_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- CRT joint satisfiability: two ResidueConstraints are jointly satisfiable iff the gcd of their moduli divides the difference of their residues.
def jsat_RR : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_jsat_RR_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_jsat_RR_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_jsat_RR_forAll)
  verificationDomain := #[.algebraic]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Byte-level less-than-or-equal: le(x, y) = 1 if x ≤ y else 0. Operands compared as big-endian unsigned byte sequences. The catamorphism fold-rule emits Literal(1) on true, Literal(0) on false.
def le : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := none
  associative := none
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.constraintSelection)
  inverse := none
  composedOf := none
  isRingOp := some (false)
}

-- Byte-level less-than: lt(x, y) = 1 if x < y else 0. Operands compared as big-endian unsigned byte sequences.
def lt : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := none
  associative := none
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.constraintSelection)
  inverse := none
  composedOf := none
  isRingOp := some (false)
}

-- Euclidean remainder: mod(a, b) = r where a = q·b + r, 0 ⇐ r < b. Total on the ring for b > 0; b = 0 emits a ShapeViolation. Operands read as unsigned big-endian integers at the operand width.
def mod : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (false)
  associative := some (false)
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.remainder)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- Ring multiplication: mul(x, y) = (x × y) mod 2^n. Commutative, associative; identity element is 1.
def mul : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (true)
  associative := some (true)
  identity := some ((1 : Int))
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.scaling)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- Ring reflection: neg(x) = (-x) mod 2^n. One of the two generators of the dihedral group D_{2^n}. neg(neg(x)) = x (involution property).
def neg : UOR.Kernel.Op.Involution UOR.Prims.Standard := {
  arity := some ((1 : Nat))
  hasGeometricCharacter := some (.ringReflection)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- The dihedral group of order 2^(n+1), generated by neg (ring reflection) and bnot (hypercube reflection). Every element of this group acts as an isometry on the type space 𝒯_n.
def D2n : UOR.Kernel.Op.DihedralGroup UOR.Prims.Standard := {
  generatedBy := #[UOR.Kernel.Op.neg.toUnaryOp.toOperation, UOR.Kernel.Op.bnot.toUnaryOp.toOperation]
  order := none
}

-- Bitwise or: or(x, y) = x ∨ y. Commutative, associative.
def or : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (true)
  associative := some (true)
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.hypercubeJoin)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- λ(S, k) = (L₁, …, Lₖ): partitions a shared context into k disjoint leases. Non-commutative, non-associative.
def partition_op : UOR.Kernel.Op.LeasePartitionOperation UOR.Prims.Standard := {
  leaseSource := none
  leaseFactor := none
  leasePartitionCount := none
  composedOfOps := #[]
  operatorDomainType := none
  operatorRangeType := none
  operatorComplexity := none
  operatorIdempotent := none
  composedOperatorCount := none
  isInvolutory := none
  convergenceGuarantee := none
  compositionResult := none
  compositionComponents := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := none
  outputClass := none
  hasWitness := #[]
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.sitePartition)
  inverse := none
  composedOf := none
  isRingOp := none
}

-- Ring → Constraints map: negation transforms a residue constraint.
def phi_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_phi_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_phi_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_phi_1_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Constraints → Sites map: composition maps to union of site pinnings.
def phi_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_phi_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_phi_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_phi_2_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Sites → Partition map: a closed site state determines a unique 4-component partition.
def phi_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_phi_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_phi_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_phi_3_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Resolution pipeline: φ₄ = φ₃ ∘ φ₂ ∘ φ₁ is the composite resolution map.
def phi_4 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_phi_4_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_phi_4_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_phi_4_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Operations → Observables map: negation preserves d_R, may change d_H.
def phi_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_phi_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_phi_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_phi_5_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Observables → Refinement map: observables on a state yield a refinement suggestion.
def phi_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_phi_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_phi_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_phi_6_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Modular exponentiation: pow(base, exp) = base^exp mod 2^n. Fold-rule: square-and-multiply over exp bits. pow(_, 0) = 1; pow(0, b > 0) = 0.
def pow : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (false)
  associative := some (false)
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.iteratedScaling)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- Predecessor: pred(x) = bnot(neg(x)) = (x - 1) mod 2^n. The inverse of succ. pred is the composition bnot ∘ neg.
def pred : UOR.Kernel.Op.UnaryOp UOR.Prims.Standard := {
  arity := some ((1 : Nat))
  hasGeometricCharacter := some (.rotationInverse)
  inverse := none
  composedOf := some ("https://uor.foundation/op/bnot, https://uor.foundation/op/neg" : String)
  isRingOp := some (true)
}

-- Successor: succ(x) = neg(bnot(x)) = (x + 1) mod 2^n. The critical identity: succ is the composition neg ∘ bnot.
def succ : UOR.Kernel.Op.UnaryOp UOR.Prims.Standard := {
  arity := some ((1 : Nat))
  hasGeometricCharacter := some (.rotation)
  inverse := none
  composedOf := some ("https://uor.foundation/op/neg, https://uor.foundation/op/bnot" : String)
  isRingOp := some (true)
}

-- ψ_1: Constraints → SimplicialComplex (nerve construction). Maps a set of constraints to its nerve simplicial complex.
def psi_1 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_1_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_1_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_1_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- ψ_2: SimplicialComplex → ChainComplex (chain functor). Maps a simplicial complex to its chain complex.
def psi_2 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_2_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_2_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_2_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- ψ_3: ChainComplex → HomologyGroups (homology functor). Computes homology groups from a chain complex.
def psi_3 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_3_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_3_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_3_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- ψ_5: ChainComplex → CochainComplex (dualization functor). Dualizes a chain complex to obtain a cochain complex.
def psi_5 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_5_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_5_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_5_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- ψ_6: CochainComplex → CohomologyGroups (cohomology functor). Computes cohomology groups from a cochain complex.
def psi_6 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_6_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_6_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_6_forAll)
  verificationDomain := #[.topological]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- ψ_7: KanComplex → PostnikovTower — compute the Postnikov truncations τ_{≤k} for k = 0, 1, …, dim(N(C)).
def psi_7 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_7_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_7_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_7_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ψ_8: PostnikovTower → HomotopyGroups — extract the homotopy groups π_k from each truncation stage.
def psi_8 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_8_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_8_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_8_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- ψ_9: HomotopyGroups → KInvariants — compute the k-invariants κ_k classifying the Postnikov tower.
def psi_9 : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_psi_9_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_psi_9_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_psi_9_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := some (true)
  validityKind := some (.universal)
  validKMin := none
  validKMax := none
}

-- Ring subtraction: sub(x, y) = (x - y) mod 2^n. Not commutative, not associative.
def sub : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (false)
  associative := some (false)
  identity := none
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.translation)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

-- Ring addition: add(x, y) = (x + y) mod 2^n. Commutative, associative; identity element is 0.
def add : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (true)
  associative := some (true)
  identity := some ((0 : Int))
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.translation)
  inverse := some (UOR.Kernel.Op.sub.toOperation)
  composedOf := none
  isRingOp := some (true)
}

-- The Surface Symmetry Theorem: the composite P∘Π∘G is a well-typed morphism iff G and P share the same state:Frame F. When the shared-frame condition holds, the output lands in the type-equivalent neighbourhood of the source symbol.
def surfaceSymmetry : UOR.Kernel.Op.Identity UOR.Prims.Standard := {
  lhs := some (UOR.Kernel.Schema.term_surfaceSymmetry_lhs.toTermExpression)
  rhs := some (UOR.Kernel.Schema.term_surfaceSymmetry_rhs.toTermExpression)
  forAll := some (UOR.Kernel.Schema.term_surfaceSymmetry_forAll)
  verificationDomain := #[.pipeline]
  verifiedAtLevel := #[]
  universallyValid := none
  validityKind := none
  validKMin := none
  validKMax := none
}

-- Bitwise exclusive or: xor(x, y) = x ⊕ y. Commutative, associative; identity element is 0.
def xor : UOR.Kernel.Op.BinaryOp UOR.Prims.Standard := {
  commutative := some (true)
  associative := some (true)
  identity := some ((0 : Int))
  arity := some ((2 : Nat))
  hasGeometricCharacter := some (.hypercubeTranslation)
  inverse := none
  composedOf := none
  isRingOp := some (true)
}

end UOR.Kernel.Op
