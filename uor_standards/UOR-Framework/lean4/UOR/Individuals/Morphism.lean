-- @generated by uor-lean from uor-ontology — do not edit manually
--
-- UOR Foundation — Morphism individual constants.

import UOR.Structures
import UOR.Individuals.Op

open UOR.Primitives

namespace UOR.User.Morphism

-- A GroundingMap that maps host-supplied byte sequences to ring datums via a fixed bit-pattern interpretation (e.g., little-endian unsigned integer reading). Total and invertible on its declared bit-width; preserves no algebraic structure beyond bit identity.
def BinaryGroundingMap : UOR.User.Morphism.GroundingMap UOR.Prims.Standard := {
  groundingDerivation := none
  symbolConstraints := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/schema/HostStringLiteral" : String))
  outputClass := some (("https://uor.foundation/u/Element" : String))
  hasWitness := #[]
}

-- Projects partitions to raw byte symbol sequences. Dual of BinaryGroundingMap.
def BinaryProjectionMap : UOR.User.Morphism.ProjectionMap UOR.Prims.Standard := {
  projectionFrame := none
  projectionOrder := none
  roundTripCoherence := none
  outputElementClass := some (("https://uor.foundation/schema/Literal" : String))
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/partition/Partition" : String))
  outputClass := some (("https://uor.foundation/morphism/SymbolSequence" : String))
  hasWitness := #[]
}

-- A GroundingMap that maps fixed-size external byte strings to ring datums via a one-way digest function (e.g., SHA-256). Total but not invertible: every input grounds, but distinct inputs may collide; the inverse is computationally unbounded. Preserves no algebraic structure.
def DigestGroundingMap : UOR.User.Morphism.GroundingMap UOR.Prims.Standard := {
  groundingDerivation := none
  symbolConstraints := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/schema/HostStringLiteral" : String))
  outputClass := some (("https://uor.foundation/u/Element" : String))
  hasWitness := #[]
}

-- Projects partitions to fixed-size digest symbol sequences. Dual of DigestGroundingMap.
def DigestProjectionMap : UOR.User.Morphism.ProjectionMap UOR.Prims.Standard := {
  projectionFrame := none
  projectionOrder := none
  roundTripCoherence := none
  outputElementClass := some (("https://uor.foundation/schema/Literal" : String))
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/partition/Partition" : String))
  outputClass := some (("https://uor.foundation/morphism/SymbolSequence" : String))
  hasWitness := #[]
}

-- Grounds integer surface symbols to ring addresses.
def IntegerGroundingMap : UOR.User.Morphism.GroundingMap UOR.Prims.Standard := {
  groundingDerivation := none
  symbolConstraints := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/schema/Literal" : String))
  outputClass := some (("https://uor.foundation/u/Element" : String))
  hasWitness := #[]
}

-- Projects partitions to integer symbol sequences.
def IntegerProjectionMap : UOR.User.Morphism.ProjectionMap UOR.Prims.Standard := {
  projectionFrame := none
  projectionOrder := none
  roundTripCoherence := none
  outputElementClass := some (("https://uor.foundation/schema/Literal" : String))
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/partition/Partition" : String))
  outputClass := some (("https://uor.foundation/morphism/SymbolSequence" : String))
  hasWitness := #[]
}

-- Grounds JSON host strings to ring addresses.
def JsonGroundingMap : UOR.User.Morphism.GroundingMap UOR.Prims.Standard := {
  groundingDerivation := none
  symbolConstraints := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/schema/HostStringLiteral" : String))
  outputClass := some (("https://uor.foundation/u/Element" : String))
  hasWitness := #[]
}

-- Projects partitions to JSON symbol sequences.
def JsonProjectionMap : UOR.User.Morphism.ProjectionMap UOR.Prims.Standard := {
  projectionFrame := none
  projectionOrder := none
  roundTripCoherence := none
  outputElementClass := some (("https://uor.foundation/schema/HostStringLiteral" : String))
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/partition/Partition" : String))
  outputClass := some (("https://uor.foundation/morphism/SymbolSequence" : String))
  hasWitness := #[]
}

-- Grounds UTF-8 host strings to ring addresses.
def Utf8GroundingMap : UOR.User.Morphism.GroundingMap UOR.Prims.Standard := {
  groundingDerivation := none
  symbolConstraints := #[]
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/schema/HostStringLiteral" : String))
  outputClass := some (("https://uor.foundation/u/Element" : String))
  hasWitness := #[]
}

-- Projects partitions to UTF-8 symbol sequences.
def Utf8ProjectionMap : UOR.User.Morphism.ProjectionMap UOR.Prims.Standard := {
  projectionFrame := none
  projectionOrder := none
  roundTripCoherence := none
  outputElementClass := some (("https://uor.foundation/schema/HostStringLiteral" : String))
  source := none
  target := none
  preserves := #[]
  trace := none
  composesWith := #[]
  preservedInvariant := none
  inputClass := some (("https://uor.foundation/partition/Partition" : String))
  outputClass := some (("https://uor.foundation/morphism/SymbolSequence" : String))
  hasWitness := #[]
}

-- The critical composition law: neg ∘ bnot = succ. This is the operational form of the critical identity theorem. The composition of the two involutions (neg, bnot) yields the successor operation. Non-associative and non-commutative.
def criticalComposition : UOR.User.Morphism.CompositionLaw UOR.Prims.Standard := {
  isAssociative := some (false)
  isCommutative := some (false)
  lawComponents := #[UOR.Kernel.Op.neg.toUnaryOp.toOperation, UOR.Kernel.Op.bnot.toUnaryOp.toOperation]
  lawResult := some (UOR.Kernel.Op.succ.toOperation)
}

end UOR.User.Morphism
