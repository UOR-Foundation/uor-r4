# Kimi Code workspace for UOR-R4

Open `/Users/casey.allard/uor-r4-kimi` as the Kimi Desktop project. The repository-root `AGENTS.md` is primary; `.kimi-code/AGENTS.md` gives the lead workflow, `.kimi-code/TEAM.md` defines the team and handoff, `.kimi-code/RESEARCH-PROTOCOL.md` defines the Socratic evidence and negative-result scope, and `.kimi-code/TOOLS-AND-STORAGE.md` routes tools, GitHub and safe cleanup. DeepSeek Flash is the economical default main/subagent model; select K3 Max for the `uor-architect-reviewer` at consequential gates. Kimi discovers the specialist roles in `.kimi-code/agents/`. Their contexts are isolated, so every delegation needs an explicit source/artifact/task packet. Review roles have constrained tools; the implementer works only on assigned files.

The live research record is `docs/integration/current-state.md`; the stable roadmap is `docs/integration/project-track.md`. Historical retrieval is available through the local `uor-memory-bridge` skill and read-only `uor_knowledge` MCP, and the local handoff index is `~/.kimi-code/UOR-R4-HANDOFF.md`. The browser extension supplies web research in the signed-in Chrome session; `gh` supplies authenticated GitHub access. The user's Kimi installation also exposes the portable SymPy, Mathbox proof/literature/computation-audit and project research skills. Use `/Users/casey.allard/.kimi-code/venvs/math/bin/python` for exact SymPy checks. GitNexus and Lean MCP are configured locally but disabled until their index or proof tools are relevant. These are separate capabilities and should be verified at the start of a long run. Provider credentials, local MCP config and session traces stay in `~/.kimi-code/`, never in this repository.

In a fresh Kimi Desktop session, type **`Proceed UOR-R4`** to inject the local startup brief automatically. The run lead's normal cycle is: refresh live state; choose a falsifiable milestone; ask independent specialists when their questions are live; delegate owned implementation; inspect the result personally; run actual loaded behavior and relevant controls; invoke K3 for consequential architecture decisions; update current state, roadmap/README if needed, and owning issues; deliver by protected PR; record a concise next handoff and recommend the following milestone. The next milestone starts only after the owner says `Proceed UOR-R4` again. K3 is a reviewer, not an independent proof authority. Use no arbitrary short run timers; respect actual machine and authorized cost boundaries.
