# INC-0136: Measure-Consistent `H^4` / Hopf Route Return

## Status
Queued next.

## Trigger
A 2026-03-12 audit of the root theory corpus re-established that the project
goal is not just to refine translated retrieval operating points.

The active thesis remains:
- fixed geometry should force useful routing
- the routing object is asymmetric `H^4 x H^4`
- later sparse-event and hardware claims only matter if the earlier geometric
  route law is mathematically sound

Recent translated sparse-event work (`INC-0128` through `INC-0134`) produced
real supporting evidence:
- lower-bank sparse-event translated defaults are now explicit
- upper-bank routed quality-near systems behavior is real
- translated retrieval remains a useful downstream harness

But that line did not resolve the earlier open gate:
- `RR-061` Derive a measure-consistent `H^4` / Hopf route law

## Kill-List Stage
Primary:
1. measure-consistent shell routing

Secondary:
2. Hopf angular correctness

## Mathematical Object Under Test
- routing manifold on the first `H^4`
- shell law on hyperbolic geodesic radius
- Hopf base angular allocator on `(chi, delta)`
- preservation of global Poincare-ball alignment while routing in chart space

## Success Condition
- a bounded geometry-return screen materially improves shell-mass and/or
  Hopf-angular diagnostics, preserves or improves geodesic neighborhood
  overlap, and stays within the current routed quality/runtime band without
  collapsing route health

## Falsification Condition
- if the geometry-return screen cannot improve measure consistency without
  breaking route health, neighborhood preservation, or the routed
  quality/runtime band, keep `RR-061` open with a narrower next correction and
  do not return to lower-bank frontier work as the main queue

This is not a translated retrieval packet branch.

## Hypothesis
The current routed product branch is mechanism-live, but the underlying
coarse route law is still mathematically under-closed.

The next real correction should therefore target the route law directly:
- shells should be anchored to a measure-consistent hyperbolic radial law
- coarse angular mass should live on the Hopf base, not on ad hoc compressed
  sector allocations
- the correction should improve geometry diagnostics without destroying the
  current routed task band

## Minimal Scope
1. Re-open `RR-061` explicitly from the current product/translated evidence
   rather than from stale pre-phase assumptions.
2. Build one bounded screen around a geometry-return route law that combines:
   - geodesic-radius-aware shell handling
   - Hopf-base angular allocation
   - no new translated sparse-event tuning
3. Compare against the current geometry references:
   - `HOPF_K25_BASE_PHI`
   - `HOPF_BASE_K25_PHI`
   - `HOPF_PHI2_BAND_PHI`
4. Evaluate geometry-first diagnostics before any downstream packet work:
   - shell occupancy versus expected `H^4` mass
   - Hopf angular occupancy / concentration
   - geodesic neighborhood preservation
   - route health / shell collapse
5. Only if the geometry-return screen is promising, decide whether it should
   replace or fork the current product route law.

## Acceptance
- one explicit geometry-return screen artifact exists
- the branch materially clarifies whether the route law is becoming more
  measure-consistent or is still mathematically misallocated
- route-health and neighborhood diagnostics remain interpretable
- the branch ends with one of:
  - promote a corrected geometry route law
  - keep `RR-061` open with a narrower next correction
  - kill the attempted return mechanism

## Scope Guardrails
- Do not reopen lower-bank translated frontier naming or contract refresh.
- Do not spend the branch on dual-anchor packet comparison.
- Keep translated sparse-event results frozen as downstream evidence, not as
  the main proof surface.
- Do not claim hardware progress from this branch directly.

## Why This Branch Exists
This branch exists because the root project goal is:

`geometry -> routing -> phase / operator structure -> sparse event compute -> hardware savings`

The current repo has meaningful late-stage evidence, but the main queue had
drifted too far into the later translated sparse-event lane. `INC-0136`
restores the earlier unresolved geometry gate as the next active step.
