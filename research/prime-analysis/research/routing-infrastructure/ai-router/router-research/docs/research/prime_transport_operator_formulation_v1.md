# Prime Transport Operator Formulation V1

## Purpose

This step replaces candidate-generation-first motion with an explicit sparse
lawful operator over structured torus-fiber state.

The operator is the source of truth for allowed motion.
There is no external candidate pool.
There is no filtering stage.

## Structured State

`H_v1` acts on native structured states carrying:

- `b`
- `radial_class = r`
- `fiber_class = phi`
- `spin_class = spin_h`
- `composite_compat_class`
- persistent composite state:
  - `query_semiprime`
  - `binding_semiprime`

In `v1`, the explicit bounded spin identity is:

- `spin_h = (a, a, a, a)`

with fixed horizon `h = 4`, where `a = admissible_transition`.

This is mechanically justified in this operator because every nonzero operator
entry preserves:

- admissibility bit
- composite state
- class tuple

so the forward admissibility word remains constant along the operator support.

## Operator Definition

`H_v1` is a sparse rule-defined operator with exactly two nonzero components per
state:

1. `I` — identity / hold
2. `T_b` — torus-base advance

### Identity component

- `I(s) = s`

### Torus-base advance component

- `T_b(s)` changes only the torus base phase:
  - `b -> (b + 1) mod 5`

and preserves:

- `r`
- `phi`
- `spin_h`
- `composite_compat_class`
- `query_semiprime`
- `binding_semiprime`

So `T_b` is a non-trivial lawful transition component.

## Lawfulness

The operator contains only transitions whose source and target match exactly on
the frozen class tuple:

- `(radial_class, fiber_class, spin_class, composite_compat_class)`

No illegal transition exists in the operator support.

## Torus Periodicity

`H_v1` respects torus periodicity explicitly through:

- `b -> (b + 1) mod 5`

So repeated non-hold application cycles through the base torus exactly.

## Measurements

The CSV is:

- [prime_transport_operator_formulation_v1.csv](/Users/adminamn/AI-Research/ai-router/router-research/results/prime_transport_recursive_system/prime_transport_operator_formulation_v1.csv)

It reports:

- operator sparsity
- lawful transition fraction
- class distribution
- hold vs non-hold fractions

Observed bounded-surface measurements:

- reachable state count:
  - `5`
- nonzero transition count:
  - `10`
- lawful transition fraction:
  - `10 / 10 = 100.00%`
- illegal transition fraction:
  - `0 / 10 = 0.00%`
- nonzero transitions per state:
  - `2` from each reachable state
- hold fraction:
  - `5 / 10 = 50.00%`
- non-hold torus-base-advance fraction:
  - `5 / 10 = 50.00%`
- class distribution:
  - `class_r0_phi0_spin1111_compat100_100: 10 / 10 = 100.00%`

## Required Honesty Section

### Is transport now generated by operator structure rather than candidate filtering?

yes

### Do any illegal transitions exist in the operator?

no

## Single Next Step

Proceed to learned weighting over the existing lawful operator entries only,
without introducing arbitrary candidates outside the operator support.
