# Open Questions

## Q0: How much more `chart_opt` can be removed from the static routed frontier without losing the new win over `R0`?
- Priority: highest
- Why it matters: `INC-0043` solved the large-subset training-route bottleneck, so chart optimization is now the dominant remaining routed cost term.
- Current owner: Systems and Performance Team

## Q0d: Do large hierarchical embeddings remain stable as the problem scale increases?
- Priority: highest
- Why it matters: the imported external guidance makes embedding-stability failure the first real falsification gate; if curved embedding quality degrades with scale, the whole geometric architecture needs revision before more routing detail work.
- Current owner: Geometry Theory Team

## Q0e: Does routing overhead stay below sparsity gain under realistic hardware-cost accounting?
- Priority: highest
- Why it matters: the imported external guidance makes hardware-cost simulation a terminal gate, not a nice-to-have. Sparse routing only matters if routing overhead stays below the compute it saves.
- Current owner: Systems and Performance Team

## Q0f: Can the project demonstrate measurable spectral structure rather than only geometric sparsity?
- Priority: highest
- Why it matters: the imported guidance elevates spectral emergence from a side theory to a central architectural claim. If no stable spectral structure appears, the project should narrow its claims to routing only.
- Current owner: Geometry Theory Team

## Q0g: Can sparse event-driven routing remain trainable end-to-end once event thresholds become real rather than proxy abstractions?
- Priority: highest
- Why it matters: the imported guidance treats event-driven computation as an end-state mechanism. That means threshold stability and gradient viability need their own kill tests.
- Current read: `INC-0125` hardened the proxy answer positively on the fixed
  product route law, `INC-0126` explained the old downstream failure, and
  `INC-0127` showed the old translated reopen was audit-only on the present
  harness. `INC-0128` then made translated sparse-event behavior genuinely
  downstream-live via train-gate pruning, and `INC-0129` showed that the same
  coupling has no viable threshold window: tiny pruning preserves quality but
  worsens runtime, while useful pruning collapses translated top-1. `INC-0130`
  and `INC-0131` then showed that softer score bias is the right reopen:
  uncoupled near-hard is now the lower-bank systems reference, `SBI030` is the
  balanced quality lift, and `SBI080` is the quality-first comparator. True
  hard firing still remains open. `INC-0132` then resolved the lower-bank
  default explicitly in favor of uncoupled near-hard and demoted the old
  bounded-backfill point as stale. The next translated sparse-event question
  is how to refresh the broader and downstream contract layers around that new
  lower-bank default.
- Current owner: Systems and Performance Team

## Q0h: Can the routing law be made measure-consistent with `H^4` and Hopf angular measure without requiring repair heuristics?
- Priority: highest
- Why it matters: the broader top-level theory corpus repeatedly points to measure mismatch as the likely reason pressure terms, widening, and local rescue rules keep reappearing.
- Current owner: Geometry Theory Team

## Q0i: Which current routed family is closest to the intended `H^4` / Hopf measure law?
- Priority: highest
- Why it matters: before the next routing-law rewrite, the project needs direct evidence on whether pure Hopf, widened Hopf, or the dense control is actually closest to the desired shell and angular mass behavior.
- Current owner: Geometry Theory Team

## Q0j: Can a measure-consistent route law preserve the current routed task win while reducing shell-mass and Hopf angular error?
- Priority: highest
- Why it matters: `INC-0060` showed that routing works well enough to keep, but the shell and angular laws are still far from the intended `H^4` / Hopf measure behavior.
- Current owner: Geometry Theory Team

## Q0k: Is transported phase actually necessary once coarse routing is restricted to the Hopf base?
- Priority: highest
- Why it matters: `INC-0062` created the right no-fiber-phase control, and the
  corrected `INC-0063` rerun showed that standalone transport is mechanism-live
  once `alpha` bins are active. The next question is whether that transported
  phase is merely live or actually necessary/useful relative to the control and
  raw Hopf phase routing.
- Current owner: Geometry Theory Team

## Q0l: Can the second `H^4` discrete complex-value field induce non-inert phase jumps?
- Priority: highest
- Why it matters: the user’s intended two-lever mechanism is now explicit:
  - minima routing on the first `H^4`
  - phase jumping through the coupled complex field on the second `H^4`
  The corrected `INC-0064` rerun already shows that same-chart coupled-field
  transport moves addresses and produces nonzero field shifts. The next open
  question is whether the explicit product branch improves on that surrogate.
- Current owner: Geometry Theory Team

## Q0a: Does `train_route_mode=final_static` stay valid as sample count increases further?
- Priority: highest
- Why it matters: the current operational win is real on the larger-subset proxy harness, but it still needs scale robustness before it can be treated as stable doctrine.
- Current owner: LM Transfer Team

## Q0b: Which integration path is the right first translation target for the cheap routed frontier?
- Priority: highest
- Why it matters: `INC-0047` held near the proxy data ceiling, so the next real question is how to express the same routing law in a more model-like system without losing the advantage.
- Current owner: Systems and Performance Team

## Q0c: Can any future geometry family produce a stable amortized translated-retrieval crossover?
- Priority: highest
- Why it matters: `INC-0052` killed the first apparent `Q24/Q32` crossover on confirm, so future translated wins must be materially stronger before they are worth packaging.
- Current owner: Systems and Performance Team

## Q1: Why does `phase4_dims=0,2,4,6` outperform the other 4D projections?
- Priority: highest
- Why it matters: this determines whether the win is a seed artifact or a real geometric clue.
- Current owner: Geometry Theory Team

## Q2: Why does the `PHI_PHI_PHI` family work as well as it does?
- Priority: highest
- Why it matters: the route now has a coherent family lead; the project needs a mechanism, not just a winning config.
- Current owner: Geometry Theory Team

## Q2a: Why does `K25` beat `K20` on the 4-seed confirm after losing the 2-seed screen?
- Priority: highest
- Why it matters: this is the live clue about minimum coarse angular capacity versus screen noise.
- Current owner: Geometry Theory Team

## Q2b: Which roles should `pi`, `phi`, and `log(phi)` play in the route?
- Priority: highest
- Why it matters: the current evidence now supports a separation:
  - `pi` for angular geometry
  - hyperbolic / exponential law for continuous radial expansion
  - `phi` for discrete branch structure
  - `log(phi)` for additive convergence pressure
- Current owner: Geometry Theory Team

## Q2c: Should shells be phase-coupled or phase-shifted instead of remaining mostly radial objects?
- Priority: highest
- Why it matters: `INC-0024` showed that shell phase coupling is real, but the current continuous bias still does not beat `R0`.
- Current owner: Geometry Theory Team

## Q2d: Should shell-phase coupling be continuous, sparse, or quantized?
- Priority: highest
- Why it matters: the continuous `C035` law improved the routed family over `PHI_PHI_PHI v1`, but this now looks like a secondary local-correction question rather than the main missing geometry.
- Current owner: Geometry Theory Team

## Q2e: Is the route missing the true `H^4` shell-sector scaling law?
- Priority: highest
- Why it matters: the deep math review suggests the route still treats shell and sector capacity too separately, while true `H^4` geodesic polar geometry couples them through hyperbolic growth.
- Current owner: Geometry Theory Team

## Q2f: Should the 4D phase object be routed with Hopf-style angular coordinates on `S^3`?
- Priority: highest
- Why it matters: current routing uses `theta1`, `theta2`, and a balance heuristic, but may be missing the proper `chi` latitude coordinate and corresponding angular measure.
- Current owner: Geometry Theory Team

## Q2g: Why does pure Hopf win quality even when it uses only about `4` effective sectors?
- Priority: highest
- Why it matters: this is now the live clue about what part of the geometry is right and what part is still under-allocated.
- Current owner: Geometry Theory Team

## Q2h: Can a Fibonacci lattice widen Hopf capacity without destroying the quality signal?
- Priority: highest
- Why it matters: `INC-0028` showed that explicit low-cardinality `chi` alone is not enough, so the next discrete geometry branch must preserve the Hopf signal while widening capacity.
- Current owner: Geometry Theory Team

## Q2i: Why did the first Fibonacci allocator collapse back to the same effective `3x3` Hopf pair?
- Priority: highest
- Why it matters: this distinguishes “bad idea” from “bad first allocator law”.
- Current owner: Geometry Theory Team

## Q2j: Why does recurrence-constrained `phi^2` widening open Hopf cleanly but over-concentrate `chi`?
- Priority: highest
- Why it matters: `INC-0031` proved the widening mechanism is real, but it still collapses most traffic into one `chi` bin and pays a large runtime cost.
- Current owner: Geometry Theory Team

## Q2k: Which local gate should decide when the route is allowed to pay for a `phi^2` rung jump?
- Priority: highest
- Why it matters: the next branch should be sparse or gated, not global. The gating variable could be `chi` pressure, Hopf shell-capacity threshold, or local route concentration.
- Current owner: Geometry Theory Team

## Q2l: Can a small set of shared `phi^2` rung states preserve the geometry signal better than per-point gating?
- Priority: highest
- Why it matters: `INC-0032` showed that threshold gating is still too expensive and still over-concentrates `chi`. The next viable discrete branch is a banded lattice, not another threshold tweak.
- Current owner: Geometry Theory Team

## Q2m: Is the main live error source now loss of Poincare-ball global alignment after chart flattening?
- Priority: highest
- Why it matters: `INC-0034` showed that another local widening law is probably not enough. The next branch needs to test whether the route should preserve more of the original global `B^4` / `H^4` geometry directly.
- Current owner: Geometry Theory Team

## Q2n: Should shell assignment be anchored to original Poincare-ball geodesic radius instead of chart-distorted radius?
- Priority: highest
- Why it matters: `INC-0035` Slice A shows the alignment metric is live, and the next low-rank route-law test is whether shells should stay attached to original-ball geodesic structure while angular routing remains Hopf-aware.
- Current owner: Geometry Theory Team

## Q2o: Do shells and sectors need to share the same near-isometric route coordinate?
- Priority: highest
- Why it matters: `INC-0035` Slice B showed that shell-only anchoring improves MSE but degrades shell health and Poincare alignment, which points to a coordinate mismatch rather than a purely radial mistake.
- Current owner: Geometry Theory Team

## Q2p: How much widening should survive on top of a near-isometric route coordinate?
- Priority: highest
- Why it matters: `INC-0036` proved pure isometry preserves geometry exactly, but it over-compresses the route. The next live question is how to reintroduce capacity without falling back into chart-scale distortion.
- Current owner: Geometry Theory Team

## Q2q: Is the missing geometry actually dynamic state, not another static shell law?
- Priority: highest
- Why it matters: current routing still treats time mostly as a scalar control `tau`. A stronger formulation may need the route to evolve on an 8D state space: 4 geometric coordinates plus 4 flow / momentum coordinates.
- Current owner: Geometry Theory Team

## Q2r: Should the next deep geometry branch be `H^4` position plus tangent-flow state rather than plain `R^8`?
- Priority: highest
- Why it matters: if time truly evolves with the geometry, the clean formulation is not “switch to Euclidean `R^8`.” It is closer to a tangent-bundle or geodesic-flow picture:
  - position in `H^4`
  - flow / momentum in `T_xH^4`
  - local 8D state without abandoning hyperbolic global alignment
- Current owner: Geometry Theory Team

## Q2s: Does the route actually require `H^4 x H^4`, not just `H^4` plus tangent flow?
- Priority: highest
- Why it matters: this is stronger than the tangent-flow hypothesis. It says the dynamic half may need its own hyperbolic geometry rather than being only an auxiliary Euclidean or tangent-space variable set.
- Current owner: Geometry Theory Team

## Q2t: If `H^4 x H^4` is right, what should the second `H^4` represent?
- Priority: highest
- Why it matters: the second factor must have a job that is mathematically and operationally distinct from the first:
  - flow / momentum field
  - memory-pressure field
  - imaginary / retrieval transport field
  - convergence/divergence control field
- Current owner: Geometry Theory Team

## Q2u: How should tangent flow alter shell/sector allocation without breaking the current Hopf lead?
- Priority: highest
- Why it matters: `INC-0050` Slice A confirmed that the tangent surrogate wins the main dynamic proxy objective, so the next practical question is how to inject that signal into a real route law.
- Current owner: Geometry Theory Team

## Q2v: Why does product `H^4 x H^4` help top-1 more than proxy MSE?
- Priority: highest
- Why it matters: `INC-0050` Slice A showed that the product surrogate is not dead; it appears more retrieval/discrete-decision oriented than regression oriented, and that distinction should guide where it is tested next.
- Current owner: Geometry Theory Team

## Q2w: Should the second `H^4` store route keys in a discrete complex / imaginary field?
- Priority: highest
- Why it matters: `INC-0054` showed that same-bucket Hopf locality is real, but the product branch still looks more retrieval-like than regression-like. A discrete complex storage law for route keys may fit the second hyperbolic factor better than another continuous surrogate.
- Current owner: Geometry Theory Team

## Q2x: Why does same-bucket Hopf pruning preserve locality but lose MSE unless dynamic state compensates?
- Priority: highest
- Why it matters: `INC-0054` cut candidate fraction to about `0.34` with zero fallback, but the global dynamic baseline still won MSE. The project needs to know whether the loss comes from bucket granularity, metric mismatch, or missing second-field structure.
- Current owner: Geometry Theory Team

## Q2y: What is the right discrete complex key law for the second `H^4`?
- Priority: highest
- Why it matters: `INC-0055` showed that discrete complex route-key storage is evidence-positive, but the current `13`-root law still pays a bounded quality cost. The project needs a mathematically cleaner key law than “just pick a root count.”
- Current owner: Geometry Theory Team

## Q2z: Should the second-field complex key be a pure address field or a mixed address-plus-ordering field?
- Priority: highest
- Why it matters: `INC-0055` improved candidate pruning and runtime, but the product baseline still won quality. The next branch has to decide whether the complex field should only choose candidates or also modify within-bucket ordering.
- Current owner: Geometry Theory Team

## Q2ac: Is measurable spectral structure actually emerging from the routing manifold, or are we only imposing geometric sparsity by hand?
- Priority: highest
- Why it matters: the imported hypothesis notes make spectral structure a central architectural claim. If no spectral modes or stable operator structure show up, the theory needs to be narrowed sharply.
- Current owner: Geometry Theory Team

## Q2ad: Does phase transport provide a necessary computational signal or just an optional geometric decoration?
- Priority: highest
- Why it matters: the imported notes make phase transport a core mechanism candidate. The repo already carries phase/Hopf clues, but they still need a cleaner necessity test instead of incremental tuning.
- Current owner: Geometry Theory Team

## Q2ae: Can sparse event-driven computation remain trainable end-to-end once the routing law becomes more geometric?
- Priority: highest
- Why it matters: the imported notes frame sparse event firing as a core end-state, not just a retrieval optimization. That means trainability under thresholded or event-driven routing becomes a first-class kill test.
- Current read: the project now has a positive soft-threshold proxy point and
  a hardened near-hard proxy point (`INC-0125`), but `INC-0127` shows the
  current translated sparse-event surface is not route-coupled at all. The
  project still does not have a true hard-firing result or a real translated
  sparse-event downstream win.
- Current owner: Systems and Performance Team

## Q2af: What specific role should the second factor in `H^4 x H^4` play in the imported coupled-field picture?
- Priority: highest
- Why it matters: the imported theory sketch explicitly allows coupled fields, correction fields, and retrieval fields. The live product branch needs a narrower mathematical assignment for the second `H^4` than “auxiliary geometry.”
- Current owner: Geometry Theory Team

## Q2ag: Which branches are actually testing phase transport necessity rather than just using phase-shaped coordinates?
- Priority: highest
- Why it matters: the imported guidance distinguishes phase transport from generic angular parameterization. The project needs a necessity test, not just more phase-based routing variants.
- Current owner: Geometry Theory Team

## Q2ah: Can spectral modes be measured on the current routed manifolds before more architectural complexity is added?
- Priority: highest
- Why it matters: multiple project-base files argue that oscillatory or modal behavior may emerge from geometry itself. The repo still lacks a direct operator/spectrum measurement branch.
- Current owner: Geometry Theory Team

## Q2ai: Is the correct second-factor role in `H^4 x H^4` a transport field, retrieval field, correction field, or some mixture?
- Priority: highest
- Why it matters: the top-level corpus repeatedly distinguishes routing geometry from transport or key fields. The second factor cannot stay semantically vague if the product branch is going to mature.
- Current owner: Geometry Theory Team

## Q2aj: Can the second `H^4` be formalized as a discrete complex-value scaling field rather than a generic auxiliary latent?
- Priority: highest
- Why it matters: current user guidance narrows the intended product mechanism. If the second factor is really a structured complex field, the branch should stop pretending the role is still open-ended.
- Current owner: Geometry Theory Team

## Q2ak: Can phase shifts be derived from the geometry/connection law itself rather than introduced as an external scoring term?
- Priority: highest
- Why it matters: this is now the core theoretical bar for the product branch. If geometry does not force the phase behavior, the architecture remains heuristic rather than structural.
- Current owner: Geometry Theory Team

## Q2am: What is the correct coupling law by which the discrete complex-value `H^4` scales with the routing `H^4`?
- Priority: highest
- Why it matters: the current user guidance is no longer “have a second field somewhere.” It is specifically that the second factor should scale with the first, and both should move through phase shifts. That coupling law is now the central missing definition.
- Current owner: Geometry Theory Team

## Q2al: Does the routed manifold support measurable geometry-induced spectral structure before any explicit oscillatory mechanism is added?
- Priority: highest
- Why it matters: the current thesis is moving beyond “better routing” toward “geometry induces spectral fields.” This needs a direct test before stronger claims are made.
- Current owner: Geometry Theory Team

## Q3: Why does the radial optimum move from `3.0` (fixed) to `3.2` (`phi_ladder`) to `3.6` (`PHI_PHI_PHI`)?
- Priority: highest
- Why it matters: the shell-width term is clearly part of the routing law, and the optimum moves as the family becomes more self-consistent.
- Current owner: Geometry Theory Team

## Q3a: What is the shell-control phase boundary across controller law, shell metric, and `delta_r`?
- Priority: highest
- Why it matters: the project still needs the boundary analysis, but only after confirming whether the larger `H^4` scaling law is the real missing piece.
- Current owner: Geometry Theory Team

## Q4: Can hybrid local complex zoom turn its routed-quality gain into a runtime-competitive branch?
- Priority: high
- Why it matters: `INC-0020` rescued the hybrid branch into a healthy routed-quality result, but it still loses runtime to the best coarse-shell branch.
- Current owner: Geometry Theory Team

## Q4a: Why does `local_min_k=2` work even when local drive stays weak?
- Priority: high
- Why it matters: the rescued hybrid branch behaves like a stable two-way imaginary-field split with only occasional extra local opening, and that should be explained mechanistically rather than treated as a lucky setting.
- Current owner: Geometry Theory Team

## Q5: Is `R5B` stable under larger synthetic workloads and nearby hyperparameter shifts?
- Priority: high
- Why it matters: synthetic lead status is still useful if the transfer branch later regresses or fragments.
- Current owner: Experimental Validation Team

## Q6: Which hardware proxy metrics should govern promotion?
- Priority: medium
- Why it matters: shell count, shell concentration, unseen-rate, runtime, and bounded MSE loss define a Pareto frontier rather than a single scalar winner.
- Current owner: Skeptical Review Team

## Q6a: How much of the wall-clock variation is route signal vs batch-order / host-load noise?
- Priority: medium
- Why it matters: the routed family improved again in `INC-0024`, but the strict runtime margin vs `R0` is still small enough that systems noise remains relevant.
- Current owner: Systems and Performance Team

## Q7: Where is the cleanest integration point into modern model stacks?
- Priority: medium
- Why it matters: research value increases sharply if the route can plug into token routing, retrieval, or memory systems without rewriting everything.
- Current owner: Lead Researcher

## Q2aa: Can a hierarchical coarse-Hopf backfill recover top-1 on top of the translated complex key without giving back most of the pruning gain?
- Priority: highest
- Why it matters: `INC-0056` showed that discrete complex / imaginary route keys survive translation and improve candidate fraction, online cost, amortized cost, and proxy MSE, but they still pay a small top-1 penalty.
- Current owner: Geometry Theory Team

## Q2ab: What is the cheapest selective backfill policy that recovers top-1 on top of the translated complex key?
- Priority: highest
- Why it matters: `INC-0057` showed that broad fixed-size coarse backfill is materially expensive even after implementation cleanup, so recall repair likely needs a gated or cached policy rather than simple coarse augmentation.
- Current owner: Systems and Performance Team
