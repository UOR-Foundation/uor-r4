#!/usr/bin/env python3
"""Stub entrypoint.

NOTE:
Your local working version contains additional flags:
- --sector_mode
- --phase_dims
- --time_pressure_lambda
and prints the run summary blocks.

Copy your local `hyperbolic_router_so8.py` into this repo before starting PR-1,
or have Codex port those flags into the version here.

PR-1 requirement:
Emit a final JSON line:
  __JSON_SUMMARY__ { ... }

"""

def main():
    raise SystemExit("Replace this stub with your current working hyperbolic_router_so8.py (see README).")

if __name__ == "__main__":
    main()
