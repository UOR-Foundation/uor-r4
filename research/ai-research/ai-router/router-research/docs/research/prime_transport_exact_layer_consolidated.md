# Prime Transport Exact-Layer Consolidated Note

## 1. Exact Static Structure

The primary exact object is the recursive admissibility system on the transport
orbit

- `n(j) = 5 + 6j`

with odometer update

- `j -> j + 1`

At finite depth, the exact state factors as

- `j -> (b, phi, r)`

where:

- `b` is base phase
- `phi` is refinement fiber phase
- `r` is wheel depth

This phase-fiber-scale chart is exact. It is not a downstream approximation.

## 2. Predictive Compression

For horizon `H`, let

- `spin_H(j)`

be the length-`H` future admissibility word along the exact orbit.

The compressed predictive state is

- `(b, spin_H)`

This remains the stronger exact predictive state currently in hand. It belongs
to the exact recursive-system layer, even though it is already a compression of
future behavior.

## 3. Threshold Mechanism

The current exact-layer threshold picture is:

- internal split is immediate
- visible split is delayed

So newly added refinement layers become internally active before they become
visibly distinct in predictive state counts.

The best current exact-layer law class for visible threshold is:

- `density + first-splitting event`

This is a mechanism class, not a closed formula.

## 4. Static Versus Dynamical Distinction

The exact chart `j -> (b, phi, r)` is informative but not fully predictive.

Current bounded evidence shows:

- phase-fiber-scale carries real first-splitting signal
- first splitters concentrate in specific phase-fiber sectors
- but phase-fiber-scale does not yet replace spin as the stronger exact
  predictor of visible first splitting

So the first visible splitting mechanism currently appears:

- partially geometric
- partially genuinely dynamical

The geometric part is the nonuniform placement of first splitters in the exact
phase-fiber chart.

The dynamical part is the extra predictive structure carried by finite-horizon
spin beyond that static chart.

## 5. Residual Beyond Phase-Fiber-Scale

The residual should be understood as a compressive predictive question, not as
a raw-information question, because `(b, phi, r)` already coordinatizes the
exact orbit.

The best current small exact residual candidate is:

- `prev_next_gap_pair`

The next best candidate is:

- `next_return_gap`

These return-memory labels explain more of the predictive partition than short
local suffix memory or a local transition triple.

But the current bounded evidence is still conservative:

- full spin remains stronger, especially on deeper lifts
- no small exact residual object has yet earned replacement status for full
  spin

So the present exact-layer reading is:

- phase-fiber-scale is exact and informative
- spin is the stronger predictive compression
- the best current residual candidate class is return-memory
- the residual is only partially compressed so far

## 6. Open Question

The next sharp exact-layer question is:

can the residual predictive structure beyond static phase-fiber-scale be
compressed further into a small exact return-grammar object, or is full
finite-horizon spin genuinely irreducible for the visible-threshold mechanism?
