# Mechanism-First Research Plan

## Canonical Object
- `R ∈ H^4`
  - base routing geometry in hyperbolic polar / Hopf form
- `F ∈ H^4`
  - asymmetric coupled field
  - discrete complex-value field
- Coupling law:
  - `K(x) = K_base(R(x)) ⊕ K_field(F(x))`
  - plus geometry-induced phase updates

## Kill Ladder
1. embedding stability
2. measure-consistent shell routing
3. Hopf angular correctness
4. phase transport necessity
5. spectral emergence measurement
6. sparse event-driven trainability
7. hardware-efficiency confirmation

## Active Branch Sequence
1. `RR-059`
  - lock the coupled `H^4 x H^4` contract and notation guardrails
2. `RR-060`
  - add `H^4` / Hopf measure diagnostics and gate the current frontier with geometry-specific evidence
3. next
  - derive a measure-consistent routing law
4. next
  - run strict phase-necessity ablations
  - if standalone base-only transport is inert, continue phase work only through a coupled `H^4 x H^4` complex-field law
5. next
  - add operator/spectrum measurement
6. next
  - return to the coupled-field `H^4 x H^4` route law with the earlier gates live
7. later
  - sparse event-driven prototype

## Nonnegotiable Constraints
- Poincare-ball global alignment remains foundational.
- Hopf angular coordinates remain the default angular model.
- The second `H^4` is not duplicate routing capacity.
- Moonshot hardware files remain backlog context until the kill ladder clears.
- Gated-intelligence / synapse-style branches remain deferred until geometry routing is verified strongly enough to survive the measure and phase gates.
