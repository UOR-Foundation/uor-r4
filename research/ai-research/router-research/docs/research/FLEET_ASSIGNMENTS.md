# Fleet Assignments

## Lead Researcher
- Mission: own direction, not execution volume.
- Reads: `PROJECT_CONTEXT`, `DECISIONS`, `CURRENT_DIRECTION`, `OPEN_QUESTIONS`, `ROUTE_MATRIX`, latest gate notes.
- Current assignment:
  - maintain the cheap routed recommendation with an explicit transfer-health gate
  - choose the first translation target based on engineering leverage, not stale geometry assumptions
  - escalate to the user only when branch selection is materially ambiguous

## Geometry Theory Team
- Mission: explain the win and propose new geometry that is not just random flag search.
- Current assignment:
  - explain why `0,2,4,6` works better than neighboring phase choices
  - explain why the radial optimum moves from `3.0` to `3.2` to `3.6`
  - explain why `K25` beats `K20` on the 4-seed confirm after the opposite 2-seed screen result
  - maintain the strict role split:
    - `pi` for angular geometry
    - hyperbolic / exponential law for continuous radial growth
    - `phi` for discrete branch structure
    - `log(phi)` for additive convergence pressure
  - treat the new deep-math review as active direction:
    - derive `H^4` shell-sector growth from geodesic polar geometry
    - formalize Hopf-style angular allocation with `chi`
    - determine whether current `phi^balance` is just a surrogate for missing `S^3` measure
    - explain why pure Hopf wins quality with only `4` effective sectors
    - define the tri-axial phi/Fibonacci lattice so it widens capacity without erasing the Hopf quality signal
    - explain why the first Fibonacci allocator collapsed back to the same Hopf pair and how rung forcing should correct it
    - explain why `INC-0031` widens Hopf but still over-concentrates `chi`
    - explain why `INC-0032` threshold gating reduced cost only marginally
    - define a small shared-state lattice for `phi^2` widening instead of another per-point gate
    - use `INC-0034` as negative evidence that local widening overlays have likely plateaued
    - treat Poincare-ball global alignment as the next primary geometry question
    - use `INC-0035` Slice A as evidence that the alignment metric is live
    - explain why `HOPF_PHI2_BAND` preserved alignment best on the fast diagnostic screen while still over-concentrating `chi`
    - use `INC-0035` Slice B as negative evidence for shell-only repair
    - use `INC-0036` as evidence that pure isometry recovers geometry but over-compresses capacity
    - derive the next isometric-band route so widening can be reintroduced without reverting to chart-scale distortion
  - keep sparse / quantized shell-phase coupling as a fallback correction, not the primary next branch
  - explain why `local_min_k=2` rescues `phase4d_complex_local` even though local drive stays weak
  - derive a cheaper local law before any larger hybrid branch
  - `INC-0050` Slice A is now complete:
    - treat tangent surrogate `H^4 + T_xH^4` as the primary dynamic branch
    - keep product `H^4 x H^4` as a distinct secondary branch with likely retrieval/top-1 emphasis
  - next assignment:
    - use `INC-0054` as negative evidence:
      - same-bucket Hopf locality is real
      - tangent flow does not yet justify a promoted bucketed route law
    - formalize the intended `H^4 x H^4` object as hyperbolic polar on both factors, not Euclidean 8D polar
    - use `INC-0055` as positive evidence:
      - the second `H^4` can support a useful discrete complex / imaginary key field
    - design `INC-0056` around translation/integration of that key field
    - explain whether the second-field complex key should be a pure address law or also affect ordering

## Experimental Validation Team
- Mission: test lead candidates under robust synthetic conditions.
- Current assignment:
  - larger-`N` robustness check completed for `R5B`
  - keep synthetic support on `R5B` while the transfer branch shifts around the adaptive `phi` lead
  - keep `R0` as the standing control

## LM Transfer Team
- Mission: prove or falsify transfer into language-like workloads.
- Current assignment:
  - `INC-0042` completed; large-subset EMA cost was traced to per-step training rerouting
  - `INC-0043` completed; static training-route reuse restored a routed quality/runtime win over equal-schedule `R0`
  - `INC-0044` completed; cheap chart pressure produced a healthier cheap-schedule routed lead
  - `INC-0045` completed; cheap static routed branches now beat cheap `R0` on both quality and runtime
  - `INC-0046` completed; that win held through the next larger-subset step
  - `INC-0047` completed; that win held near the proxy data ceiling
  - `INC-0048` completed; translation path is live
  - `INC-0049` completed; routed online retrieval is faster than dense exact retrieval
  - `INC-0051` completed; plain Hopf crosses dense on amortized cost at `Q24`
  - `INC-0052` completed; the `Q24/Q32` crossover did not survive 4 seeds
  - next assignment: hold translated retrieval as an evaluation harness, not as the active systems lead
  - keep geometry fixed while the current systems frontier is live
  - the translated systems branch has now failed operational confirm
  - hand the lead back to the deep geometry branch
  - report quality, runtime, and route-health deltas
  - next assignment:
    - treat `INC-0056` as positive translated evidence for discrete complex / imaginary route-key storage
    - move next to `INC-0057` hierarchical complex backfill
    - preserve the translated harness as the main evaluation path for retrieval-addressing branches

## Skeptical Review Team
- Mission: attack weak conclusions before they harden into doctrine.
- Current assignment:
  - do not let `HOPF_K25_BASE` be described as a runtime winner; `INC-0030` is slower than `R0`
  - treat `HOPF_K25_BASE` as the routed-quality lead, not a finished system answer
  - challenge whether the phi/Fibonacci lattice really preserves the Hopf quality signal instead of merely re-tuning capacity
  - treat mean-only gate passes as insufficient when any seed breaches shell concentration or unseen-route exposure

## Systems and Performance Team
- Mission: keep experiments cheap enough that research direction can change quickly.
- Current assignment:
  - preserve parser/summarizer/sweep reliability
  - maintain caching and timing fidelity for fair route comparison
  - keep `seed_major` available for control batches
  - treat integration translation as the primary systems question after `INC-0047`
  - identify the smallest harness that can test the routed win in a more model-like path before reopening chart-structure work
  - translated retrieval amortization is now killed as a promotion branch
  - keep the harness available for future evaluation of new geometry families
  - keep timing decomposition live so chart, routing, and update costs cannot drift back into one opaque number
  - dynamic-state support is now active:
    - maintain `tasks/dynamic_h4_state_eval.py`
    - preserve ordered-window evaluation behavior in this branch
    - preserve `candidate_mode=global_knn|static_bucket_knn` and the bucket-locality metrics added in `INC-0054`
    - preserve `route_key_mode=hopf_bucket|hopf_plus_complex` and the secondary-key metrics added in `INC-0055`
    - preserve the translated retrieval support for `route_key_mode=hopf_bucket|hopf_plus_complex` added in `INC-0056`
