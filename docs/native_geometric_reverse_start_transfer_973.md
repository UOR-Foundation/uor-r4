# Reverse phrase-start transfer — bounded positive, #973

**Retain `57cee4c4`**, the combined historical-reader and reverse-start refinement. Pre-selection validation and the separately frozen fresh gate pass; intended repairs to old spans are recorded separately from exact state preservation. This is a bounded complete-value memory result. General prose, temporal language, reasoning/program synthesis, alpha/frontier capability and complete-path energy advantage remain unqualified.

## Actual bottleneck and causal change

The previous candidate `f76e7452` selected the correct previous record in 64/64 fresh cases but emitted complete values in only 44/64. Actual writer traces now establish the cause: in `Record: eoqn rgoo holds eddwg`, the admitted full span `eoqn rgoo` scored 3 at H4 state `[51, 18]`, while singleton `rgoo` scored 4 at `[112, 51]`. The original record had no span. The authored `moss dale` contrast produced the same states/scores. `notes say moss dale` already selected the full span, so singleton and introductory-context controls were necessary.

Only two existing code rows change: shape key `(0, 1, 0)` changes from `[119, 71]` to `[66, 71]`, and `(0, 2, 0)` changes from `[119, 119]` to `[86, 119]`. The 100-key vocabulary, dictionary, feature law, landmarks, biases, ranks, admission and longest-first tie order remain unchanged. The full `eoqn rgoo` candidate now scores 6 at `[105, 18]`, versus singleton 4 at `[58, 51]`. Runtime still uses the same bounded ordered signed-H4 transport and scoring. Prime context, fixed zeta/geometry identities, exact endpoints, owner/version links and inherited arithmetic/emission operators are preserved. No new semantic metric or measured advantage for the entire geometric architecture is claimed.

The optional outer `relation_start_refinement` witness saves the previous router, complete parent CID, fitting configuration and document receipts. Validation restores and validates the full parent before checking allowed active-code changes and the new identity. `without_relation_start_refinement` reconstructs `f76e7452`; `RelationStartRefinementDisabled` uses its saved router under the unchanged serving law. Old artifacts omit the new optional field byte-exactly. Source hashes are bound in the selection receipt. The runtime delta is router selection for this control; fitting and trace allocation remain offline. Serving introduces no matrix products, transformer, expert network, provider, phrase parser, answer template or spelling exception.

## Construction and generated behavior

Rust preparation combines 1,264 inherited prompts with 36 new cases balancing three owners, one/two/three-word lowercase values and four introductory contexts. Exact record/owner/endpoint/start labels are resolved from actual admitted writer events; expected answers remain separate evaluation data. Empty overrides preserve actual parent reverse starts. The 245 observed commits deduplicate to 14 constraints. The existing discrete learner satisfies 14/14, up from 12/14, with seed 973, configured for up to four passes and 32 candidate roots per coordinate; it stops before its 120-second limit. It preserves all 100 keys and changes two root coordinates.

The 36 new answers pass 36/36 versus parent 30/36. All 53 inherited historical targets and exact predecessor selections remain correct. All 1,264 inherited output/EOS/first-decision/full-relation-state comparisons remain exact, including those 53 historical targets. Disabling the refinement restores parent behavior across all 89 targets. The second construction replay enforces 512 tokens/4,096 bytes on new cases; inherited population windows remain 8,192 tokens/65,536 bytes.

Replaying the previous opened negative panel yields 64/64 complete historical answers and 64/64 exact selections. All 28 current/absent-owner outputs, EOS and first decisions remain exact. Sixteen of those 28 cases gain the same intended old-span repair; each is checked against prompt bytes and historical target labels, with every other initial/final state field unchanged. Raw complete-state equality remains 36/92 and is not relabelled as total equality. The original wrapper failure and the comparator's omitted-versus-null failure are preserved; no model refit followed either. This diagnostic replay is not the final fresh draw.

## Separate fresh gate and preserved controls

After artifact/source selection was recorded, Rust drew 216 cases with fresh complete owner/value spellings. The authored forms vary one/two/three-word old values, four prefixes, eviction, and current/before/previous questions. Candidate answers pass 216/216 versus parent 192/216; all 216 old spans are complete, all 144 historical selections preserve the exact previous endpoint, and all 216 disabled comparisons match the parent. No refit or redraw follows selection. Fresh spellings in these forms do not establish unseen syntax or general language.

All 14 prior behavior/control reports have zero checked differences. Twelve retained multi-turn histories preserve outputs and applicable record/source identities; the 24-case word/Rust panel remains preserved. All 72 query-owner answers, EOS results and source decisions remain exact. In those same cases, 72 first reverse facts gain complete old spans verified against the original frozen labels and source bytes. All other state remains exact; raw full-state equality remains 0/72. Copy→Add and action emission each pass 24/24 ordinary and 24/24 stress cases. Actual generated Rust equalities and identifier fragments compile and execute.

The scoped native API passes 394/394 checks: 106 checks on inherited historical cases and 288 on fresh historical cases, covering actual answers, checkpoint import and independent sums.

All 33 focused tests pass. The actual-artifact test exercises eight historical→current→arithmetic sequences at 1,880 input and 264 output checkpoint positions, with eight disabled-control causal-state comparisons. The other focused checks cover exact endpoints and versions, byte roundtrip, full-parent restoration, malformed-witness rejection and the integer-kernel source guard. No allocation or energy measurement is inferred from these checks. Formatting and claim wording are checked separately before protected delivery.

## Preservation, resources and next step

Artifact: `.uor-models/native-typed-value-2026-09-05/reverse-start-transfer/model.json`, CID `blake3:57cee4c41e776a6d99c8617cfa89972eae668e428ae46f313515b98606a3aec4`, 14,856,195 bytes, SHA256 `44a975349d3fc1d0e5326d6ecae79fcc3b72eb869093800d91e387bb2f7015f9`. Earlier retained `4d81c3b0`, unretained `f76e7452`, rejected `a3c3093c` and all previous artifacts/results remain preserved. Evidence root: `/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-reverse-start-transfer`. The [machine receipt](evidence/native_geometric_reverse_start_transfer_973.json) owns hashes, command logs, controls, selection and resource accounting.

At the immutable evidence snapshot, this cycle used 1,281.365 model seconds and 338.895 engineering-command seconds. Cumulative model use is 35,456.085 / 38,250.000 seconds, preserving the 196.620-second prior reservation. Before use, standing authorization supplied a 2,100-second model increment and 512 MiB storage increment. Full projection: 3,600 model seconds, 1,800 engineering-command seconds, one model process/two threads, 4 GiB model/6 GiB build RAM, 1 GiB new storage, 512 new-input tokens/4,096 bytes and 96 output tokens; inherited windows remain 8,192 tokens/65,536 bytes and five turns with 1,056 padding tokens per turn. Accounted storage is 39,059,120,128 / 40,231,923,712 bytes, leaving 1,038,585,856 bytes after the 128 MiB stop margin. Final documentation/Git closure charges append to the mutable local receipt. No unique material was deleted and no paid external compute was used.

**Next under #973: version-aware owner/value compositional emission from an explicitly selected historical record.** The existing field compositor's initial anchor and record validator require current-directory membership, so they cannot yet compose an owner-first historical answer. Inspect actual generated behavior, then bind the shared compositor to the selected version without weakening current-record safeguards; learn appropriate lexical continuation. Initial-versus-immediate-previous selection also remains unfinished. Keep current answers, conflicts, abstention, exact identities and arithmetic/Rust controls protected. #973 remains open.
