# Learned styled occurrence roles — #973

**PASS_LEARNED_STYLED_ROLES.** Candidate0175ad86 restores all36 previously failing styled paths and preserves learned query participation, all current successful development paths and the older retained controls. Normal model15baec48 remains unpromoted. References #973.

| Actual complete-path panel | Retained8055 parent | New candidate |
| --- | ---: | ---: |
| Styled source/query roles | 60/96 | 96/96 |
| Optional query participation | 60/60 | 60/60 |
| Predicate anchors | 48/48 | 48/48 |
| Simultaneous prefix/name identity | 96/96 | 96/96 |
| Combined authored development | 264/300 | 300/300 |

All2,976 direct training byte/EOS responses pass. These are familiar authored grammar and open development results, not final independent qualification or general prose.

## Learning and implemented boundary

The former source learner could confuse a known endpoint with grammatical context because it was outside the requested output. The paired fitter joins reciprocal questions on exactly the same raw source, requiring two distinct questions with uniquely supported disjoint output-compatible spans before complementary context credit. Duplicate questions and record rotations cannot manufacture that support. Labels agree across compatible spans; ambiguity remains uncredited. The2976 training rows comprise1632 reciprocal forms of816 historical rows and1344 styled rows. The fitter receives only records, questions and final answers; expected source/path/span metadata is empty. Training/development raw record-question overlap is zero.

Before fitting, canonical center identity and immediate left/right anchors distinguish the motivating name/auxiliary occurrences in today will will call ... and ... amber will tomorrow. This finite representation check does not establish semantic distance. Paired credit provides708 supported source bundles out of786,3336 compatible spans and12 initial candidate patterns;216 multi-source rows and78 unsupported bundles do not provide complementary role labels. Explicit ambiguous occurrence credit remains blocked.

Incomplete evidence exposed two different failure modes. The first candidate passed300/300 development but overgeneralized a Ruby-neighbor rule, losing52 older paths. It finished2856/2976 training; those120 failures also include query-role ambiguity. The second candidate restricted unsupported keys, restoring all6688 older paths but suppressing12 valid styled paths and leaving72 training failures. Both candidates and their exact source/data/results remain sealed at their original paths.

The successful correction selects among the12 ambiguity-consistent source patterns using actual generation through the frozen outer participation wrapper and frozen prior query-role table. Each proposal removes one content rule; it is accepted only for strict final byte/EOS improvement with zero previously correct training responses lost. The deterministic largest safe gain wins. Across23 proposals and71424 actual forwards, one removal improves2568→2616 correct direct outputs with no losses. It removes the learned rule for will following ruby with any right neighbor; no lexical exception selects it. The final source table has11 patterns. Limits are32 initial rules,8192 rows and32 greedy passes. This training criterion protects final answers; the subsequent retained gate separately protects complete paths.

The initial query-role pass supplies eight content patterns and independently supported context patterns. A frozen second pass revisits72 unresolved rows using existing injective witnesses, source-role agreement, inherited correspondence predicates, coverage and output-compatible spans. All72 have one surviving witness, no conflicting or unsupported credit, yielding a ninth query pattern. Actual generation then passes2976/2976. Restoring only the initial query table returns2904/2976, demonstrating the contribution of refinement with the corrected source table retained.

The old query table is remapped into the new source-anchor inventory using whole canonical Word equality, preserving unknown64, edge65 and wildcard66 markers. Context center IDs stay in the unchanged inherited inventory. Optional-participation and replacement-eligibility tables remain byte-identical to8055a53c; there is no participation fit. A report-only raw parser authors/assesses examples and never supplies serving decisions.

No serving algorithm changes: learned source/query role tables execute through the existing geometric occurrence matching, ordered correspondence, exact span transport and shared Read/Emit/Stop loop. Serving adds no matrix product, transformer, provider or expert gate. Finite neighbor anchors and authored grammar remain limitations; equality with ExactIdentity does not show new metric advantage.

## Qualification and retained controls

All300 actual development structures pass source/span/byte bounds, query update, compatible source set, state continuity and final byte/EOS or typed unresolved checks. The168 successful204-panel parent cases and96 collision cases remain exact. ExactIdentity and reload each equal all300 Full structures. OriginalRolesRestored reproduces all300 sealed8055 structures, including its failures. ReadDisabled/UpdateDisabled yield zero correct answered dependent responses; UpdateDisabled preserves40 typed unresolved coverage results because the completion check terminates before the read is committed.

All300 successful structures from rejected attempt1 and288 successful structures from rejected attempt2 are retained row by row. All200 most recent sealed Full structures replay exactly. The wider6688 panel compares actual candidate and actual retained8055 parent outputs on earlier raw cases, all equal, including512 typed unresolved results. The previous sealed receipts establish8055's earlier retention; this is not a fresh comparison against every original historical control response. Historical adapter-control campaigns were NOT_RERUN. Final independent holdout remains NOT_RUN; no normal-model promotion or general grammar/prose claim.

## Execution, lineage and resources

Three sealed attempts contain seven coordinate fitting stages total: two source/query stages in each rejected attempt, then paired-source induction, actual-output source selection and query fitting/refinement in the successful attempt. Query initial/refinement subpasses are reported explicitly. Three successful optimized offline builds pass43,45 and46 focused tests respectively. A fourth build failed on a Rust borrowing error in report table sorting after26374ms; it ran no model and is charged. The corrected fitted source and binary are frozen separately. Final formatting joins only two use-import lines in the test report; an exact before/after source receipt preserves this distinction. Runtime source is byte-identical to the fitted source. No behavioral rerun was needed for that whitespace-only change. No runtime parameter is fitted from development labels.

Model/build/preparation/fit/evaluation/retry charge is1311773/1600000ms; parent13825351/14310000; shared133575924/134150000. Peak sampled process-tree RSS3417669632bytes against6GiB. Initial pre-use authorization receipt adds600000ms and256MiB each to parent/shared limits. A later complete correction projection extends the step allowance400000ms to1600000ms without raising those cumulative limits. All charges remain cumulative. No paid compute or destructive cleanup;128MiB storage margin retained. Latest local resource-final.json includes subsequent documentation/delivery storage.

All72 previous sealed roots/1701 files and20 predecessor source/binary versions verify unchanged; the three new roots add51 sealed files, bringing totals to75 roots/1752 files and23 source/binary versions. Both original dirty checkouts remain preserved. Candidate SHA256: 0175ad86850a77017fdc41b7b4da9c22fe41bc8bcbf348e1352967dd29e43a94. Retained parent SHA256: 8055a53c7801f1e6e11f808a6c15ddb090a5befe8d9d587b4ca65c74b7698793. Frozen final binary SHA256: dfede872611c561917d8a7232f780be6aa71b8d961fe620a7da9d84f6ac95035.

[Machine evidence](evidence/native_geometric_styled_role_973.json), [role learner](../crates/uor-r4-core/src/native_geometric/dependent_language/occurrence_role.rs), [Rust qualification report](../crates/uor-r4-core/src/native_geometric/dependent_language/styled_role_report.rs). Local evidence and exact restart: .uor-handoff/2026-09-12-codex-v7/shared-core-first-step/styled-role-1/. Source-selection.json retains every proposal, query-fit.json retains credit, and responses.json contains actual complete paths and controls. Provisional unsupported-key restrictions are a preserved negative intermediate table; the successful selection starts from the candidate table before that restriction.

## Next action

Evaluate the unchanged 0175ad86 artifact on lexical-neighbor transfer before another fit. Freeze unfamiliar name components around learned role words, repeated name/auxiliary occurrences and both endpoint directions, with changed-active/unchanged-inactive sources and valid, missing-compatible and conflicting continuations. Keep grammar, context size and depth fixed. Check actual first payload, whole rewritten query, source/span/state paths and final byte/EOS or typed unresolved results; compare failed observation keys to successful training keys, particularly unknown-neighbor marker64. Preserve all current successes. Only a demonstrated collision or missing distinction should justify changing representation; do not add name exceptions, a depth ladder or another broad research census.
