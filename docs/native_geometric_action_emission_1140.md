# Shared action-conditioned emission — #1140

## Decision and observed mechanism

**Retain `5ed24f4e` at bounded formatted Add/Copy scope.** It extends retained `9ab64902` with formatted Add→Copy of the latest result or original total, using the existing shared signed-H4 routers and exact committed records. The [evidence receipt](evidence/native_geometric_action_emission_1140.json) binds acceptance, artifact/source identities, actual outputs, controls and cumulative resources. All parent, partial and rejected artifacts remain preserved. The retained artifact is `.uor-models/native-typed-value-2026-09-05/action-emission/model.json`, CID `blake3:5ed24f4e7487b6f9cb7fb762fc8bcc9092152f40cb756d864a82880d08ca867d`, SHA256 `1979d9a13c44f36c2dccf0833a0f16900833339bdc2b50af30b710a9eddfb134`, 12,895,470 bytes.

With actual history `13 + 4 → 17` and an extra three, the authored sentence request now targets:

| Continuation | Complete generated response |
|---|---|
| Stop after Add | `20 is 3 plus 17.\n` |
| Copy the latest result | `20 is 3 plus 17.\n20 is 20.\n` |
| Copy the original total | `20 is 3 plus 17.\n17 is 17.\n` |
| Again | `20 is 3 plus 17.\n23 is 3 plus 20.\n` |

The corresponding Rust forms are equality expressions, such as `20 == 3 + 17\n17 == 17\n`. The construction population crosses those four operation forms with plain, sentence and Rust output in three numeric groups (36 cases). Open and reversed-order/equal-value stress each contain two other groups (24 cases each). These are finite authored forms; changed numbers, names and order do not qualify arbitrary wording or general language.

Exact identities matter independently of strings. Original result record 2, extra literal 3, first Add 4 and continuation 5 are labels for the authored history, never serving constants. First Add uses ordered `[3,2]`; Copy-latest uses `[4,4]`; Copy-original uses `[2,2]`; Again uses `[3,4]`. Zero-extra stress gives equal numeric results but still checks those different identities. Every correct Full case also generates a following independent sum and verifies fresh literal occurrence IDs.

## Actual failures and staged correction

The parent diagnostic preserved eight of twelve responses. Appending a formatting instruction to either Copy request disturbed the initial derived-role decision: the first operation copied 17 or 13 instead of adding to produce 20. Subsequent formatting repeated an Add-shaped suffix or emitted malformed text. The exact actual outputs and IDs remain in the diagnostic report.

First, [action_binding.rs](../crates/uor-r4-core/src/native_geometric/action_binding.rs) refines only the existing shared derived-role router, reusing the mixed initial-role learner. The staged artifact `a6faba85` fits all 43 distinct role frames and repairs all 36 initial decisions, but only 24/36 complete responses pass: all twelve formatted Copy responses stop after the first Add. Its 120-second fit timer was reached after reaching zero hinge; this stopped flag remains in the report.

Second, [action_emission.rs](../crates/uor-r4-core/src/native_geometric/action_emission.rs) refines the shared continuation router and learns new action-conditioned lexical codes. The first joint seed, 1140, fits all 65 lexical frames but only 99/101 operation frames. Candidate `61e24998` passes 30/36 responses, with all six formatted Copy-original cases stopping early. It is rejected and preserved, together with its source snapshot.

Before a second deterministic seed, the trainer gained a diagnostic for complete ordered feature alternatives with no jointly acceptable target slot, plus exact post-fit scoring of missed operation frames. It finds no contradictory frames; this does not prove separability or convergence. Seed 1141, from the same frozen binding parent and same data, fits all 101 operation and 65 lexical frames with zero hinge and passes 36/36 complete responses. Neither joint stage hits its timer. This supports an optimization failure in the first joint attempt. No serving law or population was changed between those two fits.

## Shared geometry, training and serving boundary

The lexical feature key now includes the committed anchor action. Add keeps the legacy zero tag and exact previous feature keys; Copy, Sub and Mul have distinct tags above existing fields. Only Add and Copy are trained and behaviorally qualified here. Without the optional action witness, older artifacts use the exact prior feature law.

The learner extends only Copy-tagged lexical feature roots. The 375 inherited codes, query dictionary, output tokens, landmarks, biases, ranks and scoring mode remain unchanged; 285 added codes bring the total to 660. Selection still uses the shared signed-H4 composition and angular ranking path. The change introduces no output-family dispatcher, serving sentence template, target answer field, transformer, provider response or mathematical matrix product. Exact operand reads use the committed anchor and retain record identity separately from text position.

The operation router extends its existing query dictionary by exact word identity to 31 entries and remaps prior prime-coded features through the same word identity. It grows from 286 to 323 codes. Prime identity is not a semantic metric. The role router grows from 679 to 689 codes. Fixed geometry, zeta identities, orientation and all unrelated model fields remain structurally identical to parent `9ab64902`; this experiment does not separately establish the usefulness of every geometric subsystem.

Training is explicit offline supervision. New lexical suffixes, exact Copy read targets and continuation/Stop choices are teacher-forced during frame construction. The typed executor computes values from actual records, and preparation checks generated numeric prefixes, committed action and ordered operand IDs before applying suffix labels. Complete free generation is evaluated separately. Historical turns are freely generated and compared with their saved responses before their selected preferences become preservation frames. For each preservation query, construction captures the staged parent’s freely generated preferences without comparing that query against a saved full answer; the separate final preservation evaluation performs the saved-answer comparison. No teacher text supplies serving responses.

Binding uses 36 new examples, 459 preservation contexts, 745 prompts and 472 generated history boundaries. The joint fit retains 351 presented lexical frames plus 1,109 operation frames (including 98 generic rejection companions), below the 4,096 frame limit. It validates target availability while skipping 4,824 unchanged legacy/Add lexical frames because those parameters are frozen. Unique fit populations are 65 lexical and 101 operation frames. Configuration is angular, 768 feature maximum, eight passes, 120 proposals, 120-second fit ceilings and seed 1141 for the accepted joint candidate; binding uses seed 1140.

The new outer witness stores all three complete parent components: lexical emitter, derived-role router and operation block including dictionary. Native validation restores and validates parent `9ab64902` before validating the new identity. Older artifacts omit the field; loading this artifact requires the updated runtime. Checkpoints bind the committed anchor action and exact read cursor.

## Controls and validation

On the open 24-case panel, restoring the old binding, restoring the old continuation block, restoring the old emitter, or erasing only the action tag each leaves 16 correct responses and loses all eight formatted Copy responses. The context control retains the new learned router, exact records, anchor and cursor while removing the action distinction. Disabling exact record reads or the learned emission transform leaves eight plain responses correct and loses all sixteen formatted responses. Those are measured dependencies at the authored scope, not broad language or isolated H4-versus-other-geometry superiority claims.

All 663 main retained answers, earlier preservation panels, 254 transition replay cases and 72 saved lexical outputs pass. Current-query 48/48 and harder histories 16/16 pass; old mixed open/stress each pass 8/8 and old composed open/stress each pass 28/28. All previously correct rows retain their exact compared output and identity fields. Twelve of fourteen historical result/control reports match exactly. Eight already-incorrect current-query-removal rows and ten already-incorrect instruction-disabled rows change their wrong outputs or identities; their correctness totals remain unchanged. Those eighteen changed negatives are preserved individually and are not called exact preservation or newly correct behavior.

Native API passes 91/91 checks for actual narrow output, parity, checkpoint continuity and artifact tampering. Core evaluation establishes exact record/read identities separately. Twenty-one unique focused tests, the kernel source guard and four actual-artifact checkpoint/allocation checks pass. Measured ingestion/begin/predict/observe performs zero allocations; load, encoding, session setup, BOS/end-response, checkpoint/JSON/report and API allocations are outside that measured scope.

After recorded selection, 24/24 fresh signed/multidigit numeric and changed-name cases pass, with no subsequent fit. Forty-two actual generated Rust equality lines from open/stress/fresh reports are embedded unchanged in assert harnesses, compiled and executed. These are numeric equality expressions, not synthesized programs. The first three action tests plus earlier lexical/mixed/operation tests ran before the final offline diagnostic addition; the final rebuilt library then passed all four action tests. Serving examples, API and allocation binaries were reused because later changes affected offline conflict diagnostics and fit-seed selection only. Formatting and claim wording pass. Queue compatibility acknowledgements execute no tests.

This cycle used **1445.811 model seconds** and **653.576 engineering-command seconds**, including the stopped binding fit, rejected joint candidate, failed driver build and all validation. Cumulative model use is **14153.352 / 15450.000 seconds**. The 2,100-second extension was recorded before use under standing authorization; the interface projection later moved 360 seconds from the existing retry allowance without raising the cycle or cumulative limits. Preserve the 196.620-second prior reservation, leaving 1100.028 seconds after it. Conservative storage is **32,767,975,424 / 34,863,214,592 bytes**, including the retained copy, positive storage high-water charges and 16 MiB final metadata reserve, with the 134,217,728-byte stop margin preserved. Limits remain one model process, two threads, 512 context tokens, 96 output tokens, 4 GiB model RAM, 6 GiB build RAM and 1.5 GiB new storage. Sampled peak model/build process-tree RSS is 2,195,947,520 / 2,693,267,456 bytes. Small documentation/receipt/Git commands are outside the engineering-command stopwatch. No unique material was deleted and no paid external compute was used.

## Limits and next source-directed step

This is a bounded formatted Add→Copy and preserved Add→Add result. Arbitrary wording, Copy→Add, three-operation learned stopping, general prose, full Rust synthesis, alpha and complete-path energy advantage remain unqualified. Sub/Mul tags reserve distinct representation but do not qualify those operators. New-artifact browser/WASM/HTTP remains NOT_RUN; native API results have their own narrower scope.

The next concrete step is to test the opposite operation order, Copy→Add, with a changed dependency on the copied record. Diagnose the actual first-selection and continuation failures before refining the shared routers; preserve all current formatted outputs, exact equal-value identities and following independent turns. This tests whether the same learned state and emission mechanism composes under a new order. Three-operation stopping and broader lexical/conversational learning follow only from observed gaps; repeating finite panels is not the broad-language programme. #1140 remains open, with broader native language learning owned by #973.
