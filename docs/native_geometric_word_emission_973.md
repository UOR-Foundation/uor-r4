# Typed word/span context for shared lexical emission — #973

## Retained result and measured scope

Retained `91ede422` combines learned sentence emission with learned Base/defer
selection for inherited continuations. It passes 132/132 construction answers,
28/28 open answers and 663/663 retained construction answers. The matched
12-case history comparison preserves every record field, including causal
pose/phase state. The API check passes all 134 checks. The corrected allocation test and all other focused checks pass. Post-selection fresh transfer passes 24/24: twenty fixed-form fact/name/value cases and four new Rust identifier returns.

The candidate is
`blake3:91ede422c51a2e80aa0dc4f579a005d94a60e8023a0960d7d891f98cc7e7db9b`,
produced from the preserving checkpoint `50734c5c`. Its frozen parent remains
`blake3:2dc63b2c7b7073155fd3c5a8c743eaaace66f409483d86dd390966d4f9fc03f6`.
The candidate supersedes parent2dc at this bounded scope; the parent and all negative candidates remain preserved.

The measured new output forms are value-first:
` VALUE is the place.\n` and ` VALUE is the stop.\n`. They are declared offline
labels over an actual copied value. They do not establish arbitrary prose,
owner-first sentence composition, general reasoning, Rust synthesis, alpha,
frontier capability or complete-path energy savings.

## Original diagnostic remains negative

Parent `2dc63b2c` selects and copies the correct word/span in all 18 open variants
of six previously passing nonnumeric facts. Six plain answers pass. All twelve
authored **owner-first** sentence targets fail: inserting `Explain in a sentence.`
before or after the original question still produces the correct bare answer.
This includes explicit revised values and multiword spans. Numeric completion
never activates.

That diagnostic is frozen. The successor's narrower value-first labels do not
regrade or solve its twelve owner-first failures. The causal implementation seam
is a completed, valid word/span read whose payload is already correct, followed
by insufficient lexical continuation.

## Implemented mechanism and invariants

The adapter exposes a completed exact word/span read to the existing shared
lexical `SourceRouting` selector. Ordered signed-H4 transforms and geometric
ranking choose between Base and admitted bytes/EOS. Word contexts use a separate
typed feature namespace and candidate-admission metadata. Exact payload bytes
still come from the committed occurrence/span; geometric codes do not reconstruct
them. Prime identities name words and are not semantic distances.

The outer `word_emission` witness stores the previous lexical block and
reconstructs the complete original parent. The 660 inherited lexical roots,
landmarks, biases, ranks, numeric dictionary and numeric candidate set remain
fixed. The adapter uses existing source endpoint, relation-version, dependency,
span and observation checks. It creates no numeric anchor or derived record to
activate completion. Final serving adds no matrix product, transformer,
provider, phrase template, language parser or manual instruction gate.

The selected candidate uses the smaller, unqualified query feature law. Its
five-word query dictionary was selected offline by exact document-presence
association with the balanced suffix-label classes. Construction names, values
and source-form words have class-independent counts and are excluded. The rule
is implemented in Rust; there is no hardcoded list of instruction words.
Prefix-qualified query keys remain an optional, identity-bound negative
experiment and are disabled in `91ede422`.

The word-response window remains 32 total entry steps, including EOS. The
complete ` Quiet River Bank is the place.\n` plus EOS requires 33 steps. Its two
instruction placements score 0/2 in the explicit overflow panel, with the
payload unshortened. Both emit the complete sentence, but the entry cap forces
fallback before EOS. One continues with garbage to the 96-token harness limit
without terminating; the other adds wrong trailing text before EOS. This is not
graceful truncation. A longer response lifetime and larger checkpoint evidence
window are separate work.

## Development candidates and preserved failures

| Candidate | Executed result | Interpretation and disposition |
| --- | --- | --- |
| `e0994c5b` | 26/30 construction answers; 386/390 token frames | Full-query vocabulary learned construction-specific identities. Rejected and preserved. |
| `826fc1f2` | Exact-data resume remains 26/30 construction; 9/28 open, including one plain-answer regression and three nonterminations | Correct source payloads do not establish a correct lexical continuation. Rejected and preserved. |
| `f8d5ba38` | Balanced vocabulary: 32/34 unique frames, but only 12/30 complete answers | Removing irrelevant identities exposes two remaining choice errors. Rejected and preserved. |
| `ba2398a2` | Prefix-qualified query features: 32/34 frames and 12/30 answers; query roots remain at identity | A larger serving feature interaction did not resolve the single-coordinate fitting plateau. Rejected and preserved. |
| `9e8d9cfb` | Pair updates: 34/34 frames, 30/30 construction and 28/28 open; broader retained construction falls to 563/663 | Sentence emission overrides 100 retained Rust continuations and two earlier incidental `A ledger...` answers. Rejected and preserved. |
| `50734c5c` | Base/defer refinement restores the 100 retained construction failures; 130/132 refinement answers | The two incidental ledger requests still receive unwanted formatting. Rejected and preserved. |
| `91ede422` | Same-data pair resume: 43/43 unique frames, zero hinge, 132/132 actual answers | Retained after full preservation, API/allocation and post-selection fresh checks. |

The first pair fit resumes `f8d5ba38` without changing data, vocabulary, features
or serving geometry. It changes four new codes, leaves the 660 inherited codes
fixed and reaches zero hinge after 57,598 joint proposals. This supports the
bounded optimization diagnosis: complementary parameter moves can improve the
exact objective where the observed single-coordinate runs stalled. It is not a
proof that every possible single-coordinate route is impossible.

For rejected `9e8d9cfb`, disabling the word emitter, all word context or its
learned geometric transforms reduces open correctness to 6/28. Disabling prefix
qualification has no effect because that optional law is off. These are
artifact-specific causal controls; they do not establish a global geometric
predictive advantage or substitute for final-candidate checks.

The broad failure of `9e8d9cfb` is continuation takeover. Rust word returns receive
prose punctuation instead of their inherited newline/closing sequence. Source
payloads are present and correctly selected; numeric Copy/Add panels remain
correct. The narrow 30/30 and 28/28 panels were insufficient for acceptance.

## Learned preservation and current evidence

The preserving fit contains 132 contexts: the 102 observed regressions, the six
plain construction rows and the 24 formatted construction rows. The 108 inherited
rows teach Base/defer. Their prefixes and suffixes are freely generated by the
reconstructed original parent, checked for exact decoded bytes and EOS, and
observed through the existing state lifecycle. Rust suffix bytes are not added
to the sentence alphabet. The 24 formatted rows retain explicit byte supervision.
There is no manual injection of the inherited continuation tokens.

Existing query vocabulary, admitted tokens and feature-law flags stay fixed.
Additional required word-context codes can be fitted while all original 660
roots remain frozen. Exact-data resumes reuse the same admission and training
receipts. Contradictory feature/target frames fail before learning.

The `91ede422` fit retains 696 presented frames, including 324 inheritance frames,
which deduplicate to 43 unique frames. Its lexical router contains 1,125 codes.
The final pair phase changes two new codes in 117,430 proposals, with zero frozen
code changes. Free generation is checked separately from these fitting metrics.

| Check | Current executed result |
| --- | --- |
| Preserving construction | 132/132 complete answers |
| Open value-first sentence panel | 28/28 complete answers |
| Retained construction | 663/663 |
| Matched version/conflict history | 12/12 outputs and full record-field comparisons, including pose/phase |
| Earlier semantic reports | All 14 match exactly at their declared comparison scope, including negative controls; timings and scores are excluded |
| Transition replays | 254/254 |
| Prior lexical outputs | 72/72 |
| Copy/Add and prior action open/stress panels | Each 24/24 |
| Native API integrity and narrow behavior | 134 checks, PASS |
| Absent-owner controls | 6/10; all six prior Unknown answers and all ten selected-source identities preserved; four wrong-source negatives remain |
| Over-limit sentence panel | 0/2; fallback produces wrong trailing output, including one nontermination |
| Focused tests and allocation | 26 unique tests pass, including six actual-artifact allocation/checkpoint checks and the kernel source guard |
| Post-selection fixed-form transfer | 24/24: twenty fact/name/value cases and four fresh Rust identifier returns |

The fresh draw follows `selection.json`; no fitting follows selection. All four Rust identifiers are absent from the preserving training data. Actual generated identifier-return fragments compile and pass eight assertions; 24 retained Copy/Add equality lines also compile and execute. Names/values transfer within fixed prompt/output forms does not qualify arbitrary Rust synthesis.

The allocation assertion initially expected a completed-copy wrong non-EOS
observation to increment the word-copy mismatch counter. The existing runtime
records that mismatch in the response-entry counter. The test was corrected at
that ownership boundary; no runtime or artifact change was made. The failed run
remains preserved; the corrected actual-artifact rerun passes with no false commit and exact completed-source preservation.

The history comparison needs its exact scope. The original checker result of
4/12 remains preserved. Correcting an inappropriate word-Stop requirement for
source-free NoRead gives 8/12 against the original unmodified-history criteria.
Four instruction-suffix requests create an extra relation compared with that
unmodified query. The parent also creates those extra relations under the same
modified requests. A matched comparison does not erase this write-boundary
negative.

For rejected `9e8d9cfb`, the matched history outputs and source identities could
agree while record pose/phase fields differed: candidate byte tokens 48/12 and
parent punctuation tokens 506/258 decode alike but update causal state
differently. That limitation belongs to the rejected candidate. In
`history-comparison-final.json`, `91ede422` preserves **all** record fields,
including pose/phase, across all 12 matched cases. Base/defer retains the original
punctuation token identities. The matched cases preserve current version 3 over
the byte-identical older version 1, and conflict abstention at current version 5.
Each artifact's checkpoint replay is independently exact.

## Remaining source/role failures and next step

The absent-owner panel contains four preexisting stop-request failures: the
parent selects a Lodov/Talven fact belonging to a different owner instead of
Unknown. The final candidate also scores 6/10: all six prior Unknown answers
and all ten selected-source identities are preserved. The four wrong selected
facts receive formatting, so those negative responses are not byte-identical
to the parent. Formatting does not repair the source error and is not a
successful abstention. Preserve both the correct Unknown cases and these four
negative source selections.

Together with the four history queries that write an extra relation when a
formatting instruction follows the question, these results identify the next
bounded diagnostic: distinguish instruction words from fact/read/write roles
in the shared nonnumeric state path. Compare matched requests and histories,
inspect the actual selected occurrence and write proposal, and intervene at the
observed role-binding boundary. Do not add another output template or infer that
arbitrary prose is solved. Execution of that successor remains separate from
finishing this candidate's checks and protected delivery.

## Resources and evidence

Local evidence root:
`/Users/casey.allard/Documents/Codex/2026-09-08/uor-r4-word-sentence`.
Primary candidate evidence is in `fit-preserving-pairs/fit.json`,
`fit-preserving-pairs/full.json`, `open-final/full.json`,
`history-comparison-final.json`, `preserved-comparison-v2.json`,
`validation-preserving/` and `api-preserving.json`. Earlier candidate directories,
checker failures, source freezes and all negative outputs remain preserved.

The latest projection allows 60 minutes of cycle model work and 30 minutes of
engineering commands. Standing authorization first increased the cumulative
model limit from 17,250,000 to 19,650,000 ms, then by another 900,000 ms to
**20,550,000 ms** before the additional preservation/delivery work. Prior charges
and the 196,620 ms unverified reservation remain counted; a new fit or this record
does not reset them.

Execution remains bounded to one model process and two threads, 512 retained
context tokens, 96 output tokens for the evaluation harness, 4,096 input bytes
per bounded request, 256 construction/open cases, 4,096 construction frames,
4 GiB model RAM and 6 GiB build RAM. The adapter's 32-step response-entry limit
is narrower than the harness output bound. The word-fit allowance is 4,096
features, while other fit APIs retain their previous 768-feature input limit.

The cycle new-storage allowance remains 1.5 GiB. The shared storage ceiling is
37,010,698,240 bytes, with the 134,217,728-byte stop margin preserved. No unique
material is deleted and no paid/external compute is used.

This cycle used **2663.365 model seconds** and **1255.643 engineering-command seconds**, including rejected fits, failed assertions, preparation and reruns. Cumulative model use is **19111.726 / 20550.000 seconds**. Both necessary extensions were recorded before use; the preserved 196.620-second reservation leaves 1241.654 seconds. Conservative storage is **35,638,030,336 / 37,010,698,240 bytes**, including the retained copy and 16 MiB metadata reserve, with 1,238,450,176 bytes remaining after the 128 MiB stop margin. Cycle storage allowance remains 1.5 GiB. One model process, two threads, 512 context tokens, 96 output tokens, 4 GiB model RAM and 6 GiB build RAM remain configured. Sampled model/build process-tree peaks are 2,472,624,128 / 2,598,682,624 bytes. No unique deletion or paid external compute occurred. Small metadata/document/Git commands are outside the engineering stopwatch.

Retained artifact: `/Users/casey.allard/uor-r4/.uor-models/native-typed-value-2026-09-05/word-sentence/model.json`, SHA256 `a98846a05dbc88d6419e1320b289ef07d5b3136677b2542a7ffc5abded479cc3`, 13,233,012 bytes. The [machine-readable evidence](evidence/native_geometric_word_emission_973.json) binds source, data, every candidate, command logs, toolchain, generated outputs, resource projections and limitations. Delivery uses a protected PR; #973 remains open.
