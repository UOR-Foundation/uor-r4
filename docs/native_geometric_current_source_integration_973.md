# Current-source reader over validated writer state — #973

The [evidence](evidence/native_geometric_current_source_integration_973.json) owns the artifact decision, exact identities and executed checks. This is a bounded native reader integration; general prose, general reasoning/program synthesis, alpha/frontier capability and complete-path energy savings remain unqualified.

## Diagnosis and implementation

The accepted writer-role artifact `231d6950` correctly records the revision in `Record: Dusk Ridge holds selvi. selvi now in Copper Vale. Where is selvi?`, but recent direct source selection emits ` Ridge holds selvi.\n`. Padding leaves the same records and allows the persistent reader to return Copper Vale. The refreshed 58-case diagnostic reproduces four such labelled failures, with selvi/Copper and tilva/Birch in plain and owner-first forms. Allowing the frozen persistent reader to consider recent records changes the labelled result from 36/40 to 40/40.

The earlier reader candidate `aa1bc159` remains rejected. It amplified an incorrectly stored newest fact and regressed a previously correct answer; that result is preserved. This cycle fits a new reader from `231d6950`, whose writer-role correction repairs that measured commitment error. No rejected weights are transplanted.

The existing current-source feature associates an exact candidate occurrence with a nonconflicting current revision or one of its exact ancestors, then joins that class with the ordered lexical-prime context around the matching query owner. Both current and historical occurrences remain admitted. Learning changes only new version-context signed-H4 transforms in the shared source router; fixed zeta identities, geometry, parent roots/landmarks/biases, writer, payload identities, numeric and emission operators remain frozen. Six of 30 new feature codes acquire nonidentity transforms. All 77 fitting constraints pass, versus 75 initially. This is geometric source ranking, not a hardcoded preference for the newest record.

The integration change makes `current_source` the outer artifact witness. Validation restores its saved parent router before recursively validating the inner `writer_role` witness. Public parent removal and validation use one restoration helper. Removing the reader reconstructs `231d6950`; removing that writer extension reconstructs the older `6f5ab4f3`. The wire fields are unchanged, and reversed nesting remains prohibited. Serving adds no matrix product, transformer, provider, phrase parser or query-owner substitution.

## Behavior and preservation

Construction repairs all six targeted plain/Name/State response forms and selects the exact current revision in each. All 1,166 inherited output/EOS, applicable source identity and relation-state comparisons remain equal. The combined construction has 58/58 explicitly labelled answers; its other inherited comparisons are not independent language correctness results. Both current-source control names remove the same version-context contribution and restore the parent in all 12 target comparisons; they are not independent ablations of different channels.

Structural stress passes 15/16 labelled answers, versus parent 12/16, with no regressions of previously correct parent answers. The one remaining two-owner plain recent question retains the exact wrong parent answer, ` Cove holds tilva.\n`, while querying selvi. Both current chains are stored correctly in directory [2,4]. The stress panel includes the earlier three-fact Amber regression, two owners, repeated unbound value spellings, and recent/evicted memory. The writer's construction, structural stress and earlier post-selection identity draw are replayed separately against the combined artifact, checking full same-owner Assert/Revise/Revise chains, previous links 0/1/2, current directory [3], exact stored values and unchanged generation-time records.

Post-selection transfer passes 18/18 labelled answers (parent 9/18), including all nine recent current-version selections, and preserves all 15 historical/raw/padded comparisons. All 18 removal-control comparisons restore the parent. Fresh evaluation uses randomly drawn owner and value spellings under familiar forms, with no refit. This does not establish arbitrary temporal language, unrestricted vocabulary or general prose.

All 14 prior semantic/control reports retain their applicable outputs and identities (0 changed rows), as do 12 multi-turn histories. Earlier word/Rust transfers pass 24/24; Copy→Add and action emission each pass 24/24 ordinary and 24/24 stress. 23 focused tests pass, including the integer-kernel source guard and actual artifact checkpoint/allocation check. The scoped native API records 47 passing checks and 1 explicit NOT_RUN item for inner writer-only removal controls; it executes the combined artifact for behavior. Actual generated Rust equalities and identifier-return fragments compile and execute. Full historical API, blanket suite, new-artifact browser/WASM/HTTP and complete-path energy measurement remain NOT_RUN.

The API executes the supplied combined artifact for behavior. It strips the outer reader only for inner witness checks, and additionally mutates inner writer fields inside the full candidate to verify rejection by the outer frozen-parent commitment. Historical writer-only removal comparisons against bare `6f5ab4f3` are explicitly NOT_RUN on the combined artifact because those controls leave the reader active. Current-source controls compare against `231d6950`. Full historical API, blanket suite and new-artifact browser/WASM/HTTP are NOT_RUN.

## Preserved negatives and next work

Initial candidate `4f12bf78` fits 72/72 constraints and passes the narrow reader stress 16/16, but is rejected because two previously correct sentence-leading `Now selvi` answers regress from Amber Field to Dusk Ridge. The writer records remain exact and removal controls restore the parent. Adding all 19 retained writer-stress cases as parent source-preservation constraints produces the 77/77 balanced refit; no new representation or phrase exception is added. The retry runner initially demanded 16/16 reader stress, a stronger condition than the original no-parent-regression gate. Its failed assertion is preserved and corrected: 15/16 improves the retained parent’s 12/16 with zero losses, while the unchanged two-owner negative remains explicit.

The obsolete cached diagnostic executable did not recognize `writer_role` and stopped during deserialization, before model execution. Its log and charge remain preserved as a tool-version failure. Rebuilt native examples then executed the diagnostic successfully. All previous rejected artifacts, research, worktrees and cumulative charges remain intact.

Explicit historical questions and raw-copy/absent-owner controls retain their exact previous behavior; preservation is not a claim that their answers are correct. The next useful step is query-owner source discrimination across competing revision chains, starting with the unchanged two-owner recent failure. Capture the candidate hints and pre/post-transform states before changing features; the saved output establishes a selection failure, not a particular geometric collision. Preserve the now-correct current answers and exact version identities. Then address explicitly requested historical versions: forward `Where was selvi before?` still emits malformed `Unknown`, while reverse forms copy the old trailing `Ridge holds selvi` span. No word-level historical-query parser or unconditional directory rule should replace learned selection. #973 remains open.

## Retained artifact and resources

Retain `blake3:7f70029e62906da5873b577c5ec1e39fdc273dafa79fa82b1cf522f3e504b4fc` at `.uor-models/native-typed-value-2026-09-05/current-source-writer/model.json`: 14,418,256 bytes, SHA256 `07e279fa62f05b736fe87b3d2a7e44c3158dfdbd789339213b66f5da68c76a59`. The exact parent `231d6950` and both rejected reader candidates remain preserved. Source hashes, raw receipt hashes, actual generated outputs and immutable command/resource snapshots are linked in the evidence.

This cycle used **1651.375 model seconds** and **343.092 engineering-command seconds** at the immutable evidence snapshot. Cumulative model use is **30504.955 / 32250.000 seconds**; the preserved 196.620-second reservation leaves 1548.425 seconds. The complete 3,000-model-second projection required a **900-second standing-authorized extension**, recorded before use. Engineering-command projection is 1,200 seconds, with one model process, two threads, 4 GiB model/6 GiB build RAM and 1 GiB new storage. New prompts are bounded to 4,096 bytes/512 tokens and outputs to 96 tokens; inherited windows remain 65,536 bytes/8,192 tokens and five turns with up to 1,056 padding tokens per turn. Conservative accounted storage is **37896278016 / 39158181888 bytes**, leaving 1127686144 bytes after the 128 MiB stop margin. No storage extension, unique deletion or paid external compute occurred. Final small formatting/claim/document/Git closure charges are appended to the local mutable receipt after this immutable snapshot.
