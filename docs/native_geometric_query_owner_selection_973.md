# Query-owner selection across competing revisions — #973

The [evidence](evidence/native_geometric_query_owner_selection_973.json) owns the artifact decision, exact identities and executed checks. This result concerns bounded owner/source selection under familiar forms. General prose, general reasoning/program synthesis, alpha/frontier capability and complete-path energy advantage remain unqualified.

## Observed failure and intervention

Retained reader `7f70029e` correctly stores both revision chains in `Record: Dusk Ridge holds selvi. selvi now in Copper Vale. Silver Cove holds tilva. tilva now in Birch Grove. Where is selvi?  Answer:`, yet emits ` Cove holds tilva.\n`. Swapping the fact order exposes the symmetric wrong-owner answer. The 24-case open panel varies both owners, both fact orders, plain/Name/State requests and recent/evicted context: the baseline answers 22/24 correctly, while the frozen persistent reader allowed to consider recent values answers 24/24.

The expanded offline trace compares the same candidate and current router before and after current-source hints. In both failures the correct candidate starts at H4 state `[9,119]`, with action score 12, and the learned current-version transform moves it to `[35,119]`, scoring 13. The wrong old occurrence has no matching-owner hint and already occupies `[35,119]`, scoring 13. Existing strict greater-than selection retains the earlier wrong candidate on the tie. The feature distinction is present; this trace does not establish that representation expansion is needed. Scores are geometric ranks, not probabilities.

The intervention refits the existing kind30 current-source transforms from the same frozen writer parent `231d6950`. Its current-source witness is a sibling of `7f70029e`, with direct preservation comparisons against that retained reader. No rejected weights are transplanted. All 80 fitting constraints pass. Three existing code rows differ from `7f70029e`; the full source vocabulary remains 286 codes, including the same 30 version-context codes. Parent geometry, fixed zeta identities, ordered prime context, exact occurrence/version payloads, writer, numeric and emission operators remain frozen. No serving feature, candidate-admission rule, source-order tie-break, matrix product, transformer, provider or phrase parser is added.

The candidate trace moves the correct Copper source from the same inherited `[9,119]` state to `[38,119]`, scoring 14. The wrong Cove occurrence remains `[35,119]`, scoring 13. The exact current occurrence therefore wins by one rank point under the unchanged selection rule. The source trace and generated answer provide separate checks of the ranking and its actual use.

## Preparation and preserved unsuccessful work

Rust constructs the contrasts and binds fitting targets to the baseline's recorded `direct_source_dispatch`, checking exact case identity and prompt against the diagnostic receipt. Direct-reader cases use a unique frozen learned current-revision source/action target. Cases that bypass this reader preserve their existing route. Expected answer bytes remain evaluation metadata and never enter fitting or serving. The complete construction has 1,196 documents, below the existing 1,200 limit; it retains all 1,172 earlier cases.

The first fit attempt stops in preparation because a recent first-chain owner-first question already dispatches through persistent memory. It produces no candidate. A surface-form-only correction then mistakenly marks second-chain owner-first direct reads as preservation of the older writer parent's answer. Candidate `39575f74` satisfies 79/80 constraints under the incorrect preservation labels but actually answers all 82 labelled cases correctly; its four reported inheritance mismatches are improvements over `231d6950` already required by accepted `7f70029e`. It remains unselected with its exact preparation scope, not classified as an observed model regression. Binding labels to the captured dispatch corrects the mismatch, and the refit passes 80/80. All input versions, logs, candidate bytes and charges remain preserved. No optimizer or representation change was justified by that preparation error.

## Behavioral boundaries and next work

Construction passes 82/82 labelled answers, including all 24 competing-owner contrasts, and all 1,172 direct comparisons against `7f70029e` preserve output/EOS, applicable first source/field identities and initial/final records. These inherited comparisons are not 1,172 independent language correctness results. Existing current-source removal controls restore the frozen writer parent `231d6950`; their two names remove the same contribution and do not constitute independent channel ablations. Baseline `7f70029e` is separately replayed for preservation and fresh transfer.

The API's opt-in `query-owner` scope runs actual candidate/native API generation for both owners, both fact orders and plain/owner-first answers, checks all four records and current directory `[2,4]`, and follows checkpoint import with an independent arithmetic turn. The additional allocation/checkpoint test checks exact queried current endpoints across all four plain cases. Expected values belong only to assertions. Full historical API, blanket suite and new-artifact browser/WASM/HTTP remain outside this cycle's qualification.

Explicit historical-version questions remain the next concrete language task after current-source acceptance. Preserve exact previous links and current answers while distinguishing a requested old version from a current one. Existing historical questions may copy a wrong trailing span or malformed Unknown; unchanged output does not qualify historical understanding. Arbitrary wording, arbitrary numbers of entities, general instruction following and sustained prose remain unfinished. #973 stays open.

## Retained result and resources

Retain `blake3:4d81c3b003499648e47248fb8d5d4a50622f6cc0fbad83f0baad2f6cf25b3fac` at `.uor-models/native-typed-value-2026-09-05/query-owner/model.json`, 14,421,414 bytes, SHA256 `f688f842186c64a96d5a12ff9d7a70b6ce133512bf11a550c6eb9453d1b3897c`.

The 24 competing-owner contrasts pass 24/24, versus retained baseline 22/24. Complete construction passes 82/82 labelled answers and 1,182/1,182 inherited comparisons against frozen writer parent `231d6950`; all 1,172 actual output/EOS/source/record comparisons against retained reader `7f70029e` remain exact. The earlier reader stress now passes 16/16 versus 15/16, preserving every previously correct answer. Writer construction/stress/prior-fresh panels and the earlier reader fresh draw pass unchanged. New post-selection transfer passes **72/72** labelled answers versus baseline **66/72**, all 36 current selections and all 36 inherited comparisons, without refit. All previously correct fresh baseline answers retain their exact output and applicable identities. These are familiar forms with freshly drawn whole owner/value spellings, not general language qualification.

All 14 prior semantic/control reports retain exact applicable outputs and identities, as do 12 multi-turn histories. Word/Rust transfer passes 24/24; Copy→Add and action each pass 24/24 ordinary and 24/24 stress. Twenty-five focused tests pass, including the trace invariant, integer-kernel source guard and both actual current-source/query-owner allocation/checkpoint checks. The scoped native API records 63 PASS and 1 explicit NOT_RUN item for inner writer-only removal controls. Actual generated Rust equalities and identifier-return fragments compile and execute. Both current-source control names remove the same contribution and compare against `231d6950`; they are not independent ablations. `7f70029e` is separately compared. Full historical API, blanket suite, new browser/WASM/HTTP and complete-path energy measurement remain NOT_RUN.

At the immutable evidence snapshot, this cycle used **1726.541 model seconds** and **321.781 engineering-command seconds**; cumulative model use is **32231.496 / 34050.000 seconds**. The necessary **1,800-second standing-authorized increment** was recorded before use; the prior 196.620-second reservation leaves 1621.884 seconds. Complete projection: 3,000 model seconds, 1,200 engineering-command seconds, one model process, two threads, 4 GiB model/6 GiB build RAM, 1 GiB new storage, 512 new-input tokens/4,096 bytes and 96 output tokens. Inherited windows remain 8,192 tokens/65,536 bytes and five turns with up to 1,056 padding tokens per turn. Accounted storage is **38246420480 / 39158181888 bytes**, with 777543680 bytes remaining after the 128 MiB stop margin. No storage extension, unique deletion or paid external compute occurred. Final small formatting/claim/document/Git closure charges append to the local mutable receipt after this snapshot.
