# Learned unknown-neighbor roles — #973

**PASS_LEARNED_UNKNOWN_NEIGHBORS.** Candidate 60d19679 reaches 492/492 open-development paths, recovering 24 failures from 0175ad86 with no lost success. All 2,976 training answers,300 latest sealed outputs,200 earlier sealed outputs and6,688 candidate/current8055 comparisons remain exact, including 512 typed unresolved cases. Both rejected candidates' 492 successful outputs also remain exact. This is bounded native geometric-model development; the normal 15baec48 model remains unpromoted.

## Change and causal result

The role learner previously collapsed a question-only word and an unseen name into the same neighbor observation. It now retains canonical training words from records and questions, plus the enclosing artifact's already-trained query vocabulary. The role inventory grows 29→30→36 within the 64-anchor bound. Inherited table and credit neighbor indices are remapped by exact canonical Word equality; center indices belong to a separate unchanged context-word namespace.

The already-trained outer inventory comes from 192 direct training examples plus 96 replacement templates, yielding 384 training queries and 15 distinct words. It adds me/person/please/them/they/which to the role inventory. These are inherited identities, not new role labels or inserted evaluation vocabulary. The outer participation anchors, optional requirements and replacement parameters remain unchanged.

One known-observation witness-credit extraction uses the existing final byte/EOS-compatible spans and source roles. Two predetermined observations retain all neighbors or mask all eligible training-answer neighbors; eligibility excludes inherited context identities. Both positive and contrary credits and strict ambiguous blockers are projected before induction. Masking changes neither raw words, center identity nor geometric matching. Augmented counts are not independent examples.

The final rule policy forbids the explicit UNKNOWN/ANY and ANY/UNKNOWN combinations before maximal-generalization pruning. Specific alternatives therefore survive. This preserves the opposite observed category, which may itself be UNKNOWN or EDGE; it does not require a known word on the other side. UNKNOWN/UNKNOWN remains possible, so global separability is not established. Historical source/query induction keeps its old policy. Source-role behavior matches on 939 unique training/development records; no source-role or participation fit is performed.

| Configuration | New panel | Styled valid | Styled conflicting |
| --- | ---: | ---: | ---: |
| Retained 0175 / original roles restored |468/492|84/96|84/96|
| Expanded anchors only |468/492|84/96|84/96|
| Known-observation refit without projection |468/492|84/96|84/96|
| Final projected-credit candidate |492/492|96/96|96/96|

Coverage 60/60, predicate 48/48, prefix/name 96/96 and styled missing 96/96 pass in the final candidate. Valid, missing and conflicting siblings now respectively answer, return NoCompatibleCandidate and return Ambiguous. ExactIdentity and reload match 492/492 full structures; restoring original roles matches 492/492 parent structures. ReadDisabled and UpdateDisabled produce zero correct answered dependent outputs. Some unresolved cases stop during lookahead before a committed read; their disabled-control success does not independently establish relation understanding.

## Bounded corrections and preservation

- Attempt 1 stopped before model load or fit: the report conflated all 17 development tokens absent from direct role training with 13 declared test names. The corrected check uses the predecessor's sealed 13-name inventory (12 used names plus unused bexil), separately recording existing question/predicate tokens. Model acceptance and data were unchanged.
- Attempt 2 / d0860ccc passed 492/492 and 2,976 training but only 6,368/6688 retained comparisons. The added will/UNKNOWN/ANY rule overgeneralized and 320 earlier scheduling answers became Exhausted. This is a rejected candidate, not a near-pass.
- Attempt 3 / d2c119f9 constrained unknown-neighbor wildcarding using identical frozen credits. It retained 492/492 and restored 240 of 320 losses, leaving 80 unknown/unknown collisions and 6,608/6688 retained.
- Attempt 4 / 60d19679 also inherits the existing trained query vocabulary and remaps the same credits. It restores 320/320 divergent controls, passes 492/492, and returns 6,688/6688 retained. It preserves 492/492 full outputs against each rejected candidate as well as 468/468 original successes.

Canonical normalization makes all 150 pooled credit rows, the entire witnessed receipt (initial/refinement credits, patterns, blockers, tables and statistics), known query table and eligible words identical across credit migration. Counts of 1,584 content and 10,720 context credits are augmented observations; blockers remain 0 for this corpus. The final query table has 13 rules, versus 9 without projection. No new witness extraction or answer label was added during the two induction corrections. One short report JSON-macro compile correction is separately charged and its source retained.

## Scope and limits

The 492 cases are open development using an authored grammar: four records, two clauses, at most 16 words/128 bytes per record, 256 prompt bytes, three-word/50-byte payload capacity and 96 runtime steps. These inputs use one-/two-word novel styled endpoints; novel three-word endpoint transfer is not yet qualified. The 2,976 training rows include one-to-three-word authored endpoints. All 13 declared test names remain absent from training and every nested candidate word inventory. No semantic distance is inferred from prime/hash identity. Geometric matching, source-role operators, contextual read/update and byte/EOS execution remain the existing native path; there is no new transformer, provider or serving matrix product.

Final independent held-out evaluation is NOT_RUN. Historical adapter-control campaigns are NOT_RERUN. The 6,688 comparison is actual candidate versus actual 8055 on retained raw inputs; earlier sealed receipts establish that parent's lineage, rather than a fresh replay of every historical adapter control. Neither broad prose, arbitrary discourse reference, coding capability nor full-path energy savings follows from this panel.

## Artifacts, source, resources and delivery

Evidence directory: /Users/casey.allard/uor-r4-worktrees/initial-previous-intent/.uor-handoff/2026-09-12-codex-v7/shared-core-first-step/unknown-neighbor-1. The four sealed attempts contain 64 files. All 76 prior roots/1,764 files and 24 prior source/binary versions are unchanged; totals are 80 roots/1,828 files and 28 source/binary versions. Preserve both original dirty checkouts and all negative artifacts.

Final artifact: /Users/casey.allard/uor-r4-worktrees/initial-previous-intent/.uor-handoff/2026-09-12-codex-v7/shared-core-first-step/unknown-neighbor-1/attempt-4/candidate.json, SHA256 60d19679347ff349c0809353e3cca48925314a880b59c277bc9977ebd816ad73. Original retained experimental 0175, comparison 8055 and normal 15baec48 remain at their original paths. The executed source is source-freeze-4.json with unknown-neighbor-tests-4; four successful optimized builds pass 50, 50, 51 and 51 focused tests. Source-freeze-4's files match the delivered Rust source. The report-only failed-build source is build-error-3-report.rs.

Actual charged model/build/evaluation work: 959381/1200000ms, including every failed build/report and retry. Parent cumulative 14998172/15510000ms; shared 134748745/135350000ms with the prior 196620 ms reservation retained. Necessary local time extensions of 600000 then 300000 ms and one 512 MiB storage extension were recorded before use. No second storage extension was needed. Peak sampled process-tree RSS 3478437888 bytes, within 6 GiB. Resource-final.json owns updated storage/accounting snapshots and the 128 MiB stop margin. External spend 0; destructive cleanup false.

Machine evidence: [receipt](evidence/native_geometric_unknown_neighbor_973.json). Read current-state for live delivery and next action. #973 remains open; this change references its broader acceptance and does not close it. Protected PR delivery is required; queue compatibility acknowledgements are not local test results.

## Next action

Freeze acceptance and evaluate the unchanged 60d19679 artifact on one independent bounded fresh panel crossing one-, two-, and three-word names with already-supported query styles and valid, missing-compatible and conflicting continuations. Keep the four-record/two-clause window and fitting disabled; require actual query/payload/source/span/byte-EOS or typed-unresolved correctness, matched active/inactive/read/update controls and preservation of existing outputs. Draw only after acceptance is frozen, record data lineage and cumulative resources, and keep exposed development cases separate. This qualifies only the stated finite transfer scope; do not call it global role separability or general prose. Diagnose any fresh failure before another learning change.
