# Coupled state/emitter coordinate profile — #973

**Actual gate: FAIL_COUPLED_PROFILE_DEVELOPMENT_GATE. No promotion; retain 15baec48.** All 239 conditional fits completed. The unchanged state/decoder combination remains the unique numerical minimum within the declared tolerance; no profile setting improves construction NLL or exceeds baseline accuracy.

The preceding [fixed-decoder pair experiment](native_geometric_tree_state_pair_973.md) found no improving setting for indices 1176/1930. This follow-up permits the decoder to adapt to each changed trajectory, while retaining the same pair, inherited caps, signed H4 geometry, construction documents and head configuration. It is a restricted coordinate profile with a greedy head learner, not an exhaustive joint state/head optimum.

The first fit reproduces exact **8f8e5c34** artifact bytes and CID before alternatives. The remaining settings are the 119 alternative roots for index 1176 with index 1930 fixed 119, and the 119 alternatives for index 1930 with 1176 fixed 119. Each starts from the same cap parent 0f82 and recomputes all 672 causal construction byte/EOS positions. Every head uses maximum depth 3, minimum leaf 16 and a 60-second per-fit ceiling within the 120-second complete attempt. No opened or disabled-control labels select a setting. Global-minimum NLL selection prefers the unchanged parent within 1e-10, otherwise lexicographic roots.

Exactly **239 fits**, with no extra selected-head refit, finish in **9,325 ms internally** (supervised command 9,617 ms). All 239 head payloads and fit receipts are preserved; the selected disk payload is hash-verified and reloaded. Head payloads total 2,588,617 bytes, with a maximum 11,715 bytes each, below the 128 KiB per-head and 64 MiB complete-attempt limits. The seal manifest is included in the final size check.

| State roots 1176 / 1930 | Fixed-head construction NLL | Conditionally refitted head NLL | Refitted correct / 672 |
| --- | ---: | ---: | ---: |
| **119 / 119, unchanged** | **2.839534** | **2.839534** | **166** |
| 119 / 48 | 3.063310 | 2.843190 | 108 |
| 119 / 1 | 3.112568 | 2.849395 | 123 |
| 119 / 101 | 3.087278 | 2.853128 | 134 |

Comparing saved profile rows with the preceding fixed-head table shows that head refitting lowers NLL for **all 238 changed settings**, but none beats the unchanged baseline. Thus decoder adaptation reduces the damage from these state changes; it does not yield a useful update in the tested family. The closest alternative's 108 correct versus 166 illustrates why a small likelihood gap is not sufficient. All reported minima are finite f64/libm numerical results, not exact-real certificates or a limit on the entire native architecture.

Witness **blake3:9661d4825e54c0a21469c23dae0386d14a59edf0beb82237a0c9fb0661229b95** binds the cap parent, selected parent, runtime/driver/design/corpus, state overrides, exact fitted angular head and the modified cap's numerical-content digest. Intermediate identities are explicitly test-only content identities, not standard trained-model provenance. No standard changed model is exported. The selected baseline changes zero root parameters and retains the original head. Every cap, non-pair parameter, geometry and production source file remains unchanged.

| Panel | Witness correct | Witness NLL | Lost correct vs 8f8 / 1198 / 0f82 / ade9 |
| --- | ---: | ---: | --- |
| Construction Full | 166/672 | 2.839534 | 0 / 74 / 47 / 43 |
| Construction ContextDisabled | 126/672 | 3.356711 | 0 / 38 / 34 / 30 |
| Construction StateDisabled | 69/672 | 3.943612 | 0 / 33 / 48 / 51 |
| Opened Full | 52/381 | 3.653604 | 0 / 20 / 20 / 13 |
| Opened ContextDisabled | 49/381 | 3.737585 | 0 / 21 / 15 / 14 |
| Opened StateDisabled | 49/381 | 3.790113 | 0 / 13 / 31 / 29 |

Construction optimization fails because no strict improvement exists. Preservation totals 0/199/195/180 against 8f8/1198/0f82/ade9 are inherited decoder differences, not new regressions. The opened gate remains failed because 52/381 is below the previous tree's 57. Every root/source trace is unchanged, and prediction Hamming against 8f8 is zero. Counts against other models remain separate; aligned categorical Hamming is not token/hash semantic distance.

All **12,636 prior rows and 48 prior outputs** replay exactly. The attempt preserves 60 final prior/witness prompt/control outputs plus four immediate Full outputs. All four witness Full continuations equal 8f8's incoherent 96-byte outputs without EOS. The fact and Rust prompts yield no useful answer or program. No language, reasoning, Rust, fresh generalization or retained-session qualification is established.

**Next action (NOT_RUN):** One bounded complete-construction sensitivity scan of currently addressed TRANSITION, QUERY, KEY and READ entries under fixed 8f8e5c34. Record actual baseline parameter-address coverage and verify traced versus untraced outputs, then evaluate each legal alternative root for each active entry with full causal sequences and all other fields frozen. The declared table domain is at most 1,200 entries × 119 alternatives; freeze the actual call count, time/RAM/storage and selection rules before execution. Reuse the valid sealed single-coordinate results for 1176/1930 rather than repeating them. Select at most one construction-loss improvement and run immediate generation plus all prior/control comparisons; preserve source-bound witnesses and a no-improvement result if none exists. This tests where the current learner has an actionable update beyond the two historical entries. No head refit, fresh draw, new geometry, automatic joint campaign or JEPA training is included. JEPA remains a documented candidate auxiliary objective, requiring a separate causal, collapse-controlled design.

The next scan addresses a limitation of the current result: two historically chosen entries cannot characterize the rest of the state/read learner, including its query/key selection tables. A currently unaddressed entry cannot affect a single-entry baseline intervention until it is reached; that pruning argument does not apply to multi-entry changes. Actual coverage and complete causal scoring must be recorded, rather than reusing a historical hot list or occurrence-wise credit as tied-trajectory evidence.

The owner also raised JEPA as a potential prediction objective. [The source-linked JEPA assessment](native_geometric_jepa_research_973.md) records it as a plausible auxiliary training signal for native state learning, with explicit view separation, exact-identity and anti-collapse controls. It is **NOT_IMPLEMENTED and NOT_RUN** here and did not affect the frozen profile. Existing SpiralCore/FBS research and the knowledge map remain available; no donor runtime or Python model dependency was imported.

Executed validation: release compilation and a focused 239-setting coverage/witness-lineage/forbidden-field/global-tie test pass, as does the single actual experiment's execution. The model gate fails. A 512-step census observes at most 23 prediction angular comparisons, within bound 27. Allocation measurement is NOT_RUN this step. All 239 heads/receipts, the exact 488-file pre-seal set and complete 489-file sealed attempt verify. Fifteen earlier sealed roots / 168 files, the 22 parked dirty paths and retained model verify unchanged. Production training/runtime/artifact source is unchanged. No blanket suite or complete-path energy measurement ran.

[Tracked evidence](evidence/native_geometric_coupled_profile_973.json) binds source/compiler/binary, frozen design, all fitted heads, numerical profile, actual behavior, independent reviews, JEPA-source provenance and resource receipts. Local originals remain in shared-core-first-step/coupled-profile-1 under the established handoff. Sealed roots and original artifact paths remain in place.

Model/build/test charge 111,115/360,000 ms; parent cycle 2,372,266/2,800,000 ms; shared cumulative ledger 122,122,839/132,950,000 ms. The 200,000 ms local-cycle increase was recorded before use under standing owner authorization; the shared ceiling remains unchanged. Added storage 33,370,112 bytes within 128 MiB, parent growth 1,552,793,600 bytes within 2 GiB with the 128 MiB stop margin. Peak sampled RSS 2,335,850,496 bytes. No paid compute or cleanup. Final engineering and delivery receipts append locally.
