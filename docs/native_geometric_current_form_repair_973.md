# Current-request form repair after the fourth independent review — #973

Status: **retained bounded positive; `4568679d` remains the preceding accepted artifact**. The [evidence snapshot](evidence/native_geometric_current_form_repair_973.json) binds source, binary, data, artifact, acceptance, commands and every disposition. This is a bounded UOR-R4 Geometric Language Model development result on authored forms with fresh spellings; general prose, temporal language, HTTP/browser/WASM and complete-path energy remain unqualified.

## What the fourth review found

The [fourth independent review](../../Documents/Codex/2026-09-10/uor-r4-claude-minimal-review/README.md) (local root `/Users/casey.allard/Documents/Codex/2026-09-10/uor-r4-claude-minimal-review`) confirmed the minimal-chain previous-record repair of PR #1226, the rejection of `ec79c303` and the checkpoint-test correction, and reproduced 213/221 on the earlier review, 96/96 on new repeated-record requests with 64/64 exact historical identities, and eight improvements on explicit owner-first current requests. It found one qualification defect: the 36 `same-only-explain` rows of the previous construction carried no typed label, so equality to the training parent `f0901dad` was counted as preservation although four reverse-form parent answers are malformed. On those four fresh rows (`same-only-explain/{0,1}/1/2/{single,turn}`) and on four new review rows (`independent-style/{pemru,suvni}/reverse/true/both/{false,true}`), `4568679d` lost properties that `fc479376` had. The previous record described the four rows as malformed on both artifacts; that conflated the scoping probes with the exact changed rows. The corrected classification is appended to the [minimal repeated-head record](native_geometric_minimal_head_973.md#qualification-correction-after-the-fourth-independent-review).

## Diagnosis: the first divergent decision

Example: `Record: amber quay holds pemru. Record: amber quay holds pemru. Where is pemru? Explain in a sentence. Name the owner first. Answer:` (required ` pemru is in amber quay.` from live head record 2, with EOS).

1. **Writer binding** is intact on every artifact: both records are committed with owner `pemru`, span `amber quay`, record 2 the live head (`final_relations` identical across candidates).
2. **Current selection** is where the artifacts diverge. The handoff trace on `4568679d` shows `candidate: None` for every request that carries both instructions, in every layout (`handoff-trace-1/context-trace.json`), while the same records with the owner-first instruction alone or the explanatory instruction alone offer record 2 through the committed relation source 33. The frozen persistent relation reader (`relation::read_choice_with_recent`) scores each record from `read_features`, whose owner scan covers only the eight most recent request words. The request `Where is pemru? Explain in a sentence. Name the owner first. Answer:` has twelve words and the owner sits at reverse index 9, so no owner feature is produced, no record scores above zero, and no relation candidate exists. This is the first decision that differs from the eight-word requests.
3. **Exact source and span.** Without a relation candidate the parent dispatch falls back to a copy of a query occurrence (word index 14, `quay` in the second record) and the frozen extent operator extends it forward through `holds pemru` (span words 2). In the forward layouts the same fallback happens to reproduce the record span (`amber quay`, one extension), which is why forward both-instruction requests already read correctly, from the occurrence rather than the committed record.
4. **Field composition** then cannot anchor: `field_composition::initial_anchor` requires the copy candidate's span to equal the record's stored span, so the owner-first composer never fires on the reverse layout and the copy path emits the mis-spanned words followed by learned continuation text without a terminating decision.
5. `fc479376` answered these prompts through its historical selector instead (anchor on record 1, the root, with a historical proof to head 2, past tense); `4568679d` correctly defers there and thereby exposes the broken frozen current path.

## The change

The smallest causal change is at the divergent decision: the frozen persistent relation reader's own owner scan. A **reader-request-window contract** (`reader_request_window` on the historical-version witness, absent in legacy artifacts) lets `relation::read_choice_with_recent` scan the request words after the latest committed fact, up to the sixteen-word view, for the named owner instead of the eight most recent words of any kind. The scan is bounded by `relation::committed_cutoff`, the byte end of the last committed owner, value or span terminal, so an owner spelled inside an earlier record is never taken for the requested owner. The change is versioned and ablatable: `Control::HistoricalVersionIntentReaderWindowDisabled` withholds it, and removing the witness (`HistoricalVersionIntentDisabled`) reproduces the frozen parent exactly, which the actual-artifact test asserts on every new sequence.

What is deliberately unchanged: `relation::read_features`, the inherited eight-word feature base of every later learner (dependent reads, the current-query handoff, historical read, historical version intent), keeps its exact frames through a new `read_features_window(…, 8, None)` wrapper; the role reader's eight-word context, the word-copy hints, the extent operator, field composition, the emitters, all learned codes, dictionaries and receipts of `4568679d` are untouched. The retained artifact is therefore a **promotion** of `4568679d` (`native_historical_version promote-window`): the same learned witness with the contract flag set and the identity it binds, no refit. No prompt keyword, owner/value list, response template, source-index answer, oracle call or evaluation-ID lookup enters serving; the reader still decides from its learned owner-hint features, and the field composer still decides owner-first composition from its own learned request-word codes, as the forward layouts already showed.

Why this is the operator repair the review asked for rather than a historical-selector patch: the historical witness is right to defer on a current request; the defect was that the shared reader below it could not see the owner. With the owner visible, the existing dispatch order (historical, handoff, dependency, persistent read) selects the committed live head through source 33 and the owner-first composer anchors on the stored span `amber quay` (span words 0), which the earlier occurrence copy could never satisfy on the reverse layout.

## Typed current labels and coverage

The 36 unlabeled `same-only-explain` rows are replaced by a **`current-form` family of 240 typed current targets**: two owners × three layouts (`o in v.`, `Record: o in v.`, `Record: v holds o.`) × with/without the repeated fact × with/without a competing owner's record × five request forms (plain, owner-first, explanatory, explanatory then owner-first, owner-first then explanatory) × a single prompt or an actual second turn whose first turn (`Where is o? Answer:`) is answered by the evaluated model itself. Every row carries the live head record (`current_record`), the complete value, the primary text and a frozen accepted list from the typed oracle: `answer_oracle::accepted(Current, plain, …)` for plain and owner-first requests, and the new `answer_oracle::accepted_explanatory(owner_first, …)` for the explanatory forms, which accepts only the present-tense owner sentence under an explicit owner-first instruction and the inherited `VALUE is the place.` form or the owner sentence without it (the reviewer's corrected reading of the 24 explain-only controls; the bare value is not an explanation). Unit tests pin both lists. A current target is **selected** only when the first decision reads the committed live-head record at its own source and value endpoint (field anchor without any historical proof, or a dependency-free copy); a copy of a matching query occurrence is text-correct but not selected, and is reported as such. The typed intent partition, the API case export, the disjoint counts and the fresh draw all include these rows; rows the frozen path cannot answer stay in the denominator as named negatives. The training documents for the top witness keep `inherit` (defer) labels for these rows, because the correctness of a current answer belongs to the inner path, not to the historical selector; no current request is disguised as an initial or previous target.

## Results

**Retained candidate `f3620cb7`** (`promote-window-2`): `4568679d` under the reader-request-window contract, parent `f0901dad`, no refit. All gates below ran on this exact artifact and the frozen source (`selection.json`).

**Fourth review's 414 prompts, row by row.** Against `4568679d`, 24 rows differ, all both-instruction current requests; on every one of them every classified property improves or is unchanged and none degrades: acceptance 24 improved, declared form 16 improved, complete value 24, expected record 24, exact head source 24, EOS 9 improved (15 already terminated), relation state intact on all (`classify-review4-w2-vs-minimal.json`). Against `fc479376`, 44 rows differ: the same 24 plus the 20 previous-record repairs of PR #1226; no property degrades. Scored: **381/413** for the candidate against 357 (`4568679d`) and 337 (`fc479376`), with no row moving from correct to wrong against either; by group, prior review 113/113, repeated records 96/96, explicit owner-first emission **72/72** (was 48 and 40), head probes 100/108 (the eight inherited bare-value rows on all three artifacts).

**Construction-3** (5,090 cases; 1,704 targets = 930 initial + 234 previous + 360 current (120 follow-up, 240 typed current-form) + 180 abstain; 3,386 inherited rows): **1,700/1,704** exact and selected, against 1,644/1,704 for `4568679d` on the same rows under the same judge. Every initial, previous, follow-up and abstention target is exact on both; the difference is entirely the typed current family, **236/240** against 180/240, and the 56 changed rows are all both-instruction requests (plain and forward layouts in the second turn, every reverse layout single and second turn), each improving acceptance, complete value, expected record and exact head source with no degraded property (`classify-construction-3-w2-vs-4568679d.json`). Reads through the committed relation source itself: 112 of the 236 selected (56 of 180 before); the rest read the head's own occurrence through the recent capture. Preservation 3,386/3,386; complete removal reproduces the parent on all 1,704 targets; withholding the reader window returns the parent on 558 targets (every current row whose answer the window changes, and the parent-equal rest); the head-hop and interior-link ablations, the abstention ablation, the deep-admission control and the transform control keep their earlier pattern; the four remaining current rows are the second-turn plain requests after a repeated fact, text-correct but read from another occurrence on both artifacts.

**Stop gates.** Matched histories 12/12 with exact source identities (`dev-history-w2`); handoff-fresh 764/764 (`dev-handoff-fresh-w2`); the early actual-checkpoint identity test passed on the candidate with 32 sequences (8 new reader-window sequences: single and actual second-turn owner sentences from the live head on reverse, reverse-once, reverse-with-competitor and forward layouts in both instruction orders, parent equivalence under witness removal and under the withheld window, the explanatory-only form unchanged), 104 disabled-parent turns; the allocation census reports zero allocations and zero bytes including the two new current rows.

**Earlier populations, row by row against `4568679d`:** identical on the third review (222), the first (51) and second (114) reviews, and on every earlier construction and draw (4,601 + 2,184, 4,285 + 1,868, 3,669 + 1,252 + 890 + 794, 3,789 + 1,372); the previous cycle's construction (4,891) and draw (2,474) differ only on the four reverse-form `same-only-explain` rows, which now carry the owner sentence with EOS. The third review's table is unchanged (213/221, no correct-to-wrong).

**Independent draw** (`fresh-1`, frozen before the draw in `selection.json`; owners `xtpxf`, `vtzkh`, `ccgzz`; 2,678 cases, 1,704 targets, 974 inherited rows): **1,700/1,704** exact and selected (930/930 initial, 234/234 previous, 356/360 current, 180/180 abstain; exact authored text alone 1,548), preservation 974/974, complete removal reproduces the parent on all 1,704, the four remaining rows the same second-turn plain requests after a repeated fact (text-correct, other occurrence). Row by row against `4568679d` the draw differs on 56 rows, all both-instruction current requests, with acceptance, complete value, expected record and exact head source improved on every one, EOS improved on 11 (45 already terminated) and no degraded property (`classify-fresh-1-vs-4568679d.json`); against `fc479376` it differs on those 72 current-form rows (the 16 further ones are the past-tense sentences `fc479376` gave on repeated forward chains) plus the 46 previous-record repairs of PR #1226. Current reads through the committed relation source: 112 of the 236 selected.

**Retained behavior on the candidate:** 3,408 construction-derived and 3,408 fresh-derived API inputs pass through the native API driver (1,704 primary tasks and 1,704 independent checkpoint sums each, typed current targets included); 72/72 query-owner, 216/216 historical-transfer and 764/764 handoff-fresh comparisons exact; all fourteen older behavior/control reports with zero checked differences; 12/12 matched multi-turn histories with the retained source identities; 24/24 word/Rust cases; Copy/Add and action panels 24/24 ordinary and 24/24 stress; actual generated Rust compiles and executes; the focused actual-checkpoint identity test repeats the early result (32 sequences, 8 reader-window sequences, 104 disabled-parent turns) and the allocation census reports zero allocations and zero bytes on every measured turn including the two new current rows.

## Remaining failures, recorded and not credited

- **Eight owner-first bare-value rows on head chains** (`new/{nelqi,tavru}/{revised-head,interior-and-head,return-to-original,four-values-head}/current/True`, `What is the current location of {o}? Name the owner first.` on chains whose live head is a same-value reassertion): still the correct bare value without the owner on `fc479376`, `4568679d` and the candidate. The owner sits within the eight-word window, so this is a different seam (owner-first composition on a reassertion head, not reader visibility). Recorded as failures under the typed oracle, not credited, left for the emission cycle.
- **Four second-turn plain requests after a repeated fact** (`current-form/{0,1}/{plain,record}/repeat/alone/plain/turn`): the text is the correct bare value with EOS, but the read copies a different occurrence of the same spelling (not the live head record's own occurrence), identically on `4568679d` and the candidate. Recorded as text-correct, not selected; an inherited recent-capture choice, unchanged by this cycle.
- **Query-occurrence reads on forward layouts.** On plain and forward `Record:` layouts the owner-first and explanatory current requests read the head's value through the recent capture of the same occurrence rather than the committed relation source (`current_relation_source_exact_and_selected` 112 of 236 selected). The occurrence is the record's own and the answers are exact, so these rows are selected; the routing kind is reported separately and not claimed as a change.
- The reviewer's 24 explain-only controls keep the inherited `VALUE is the place.` form on every artifact; under the corrected acceptance they are compatibility controls, not failures.
- Scope unchanged: bounded authored forms with fresh spellings; the four reviews' prompt sets are development evidence and only the fresh draw is held out; no HTTP/browser/WASM, general prose, temporal-language or energy claim.

## Rejected attempts

- **`52fc916b` (W1, `promote-window-1`)**: the same promotion with an *unscoped* sixteen-word owner scan (`reader_window`). It repaired the same 24 review rows with no degraded property and kept the 12 matched histories with exact sources, but the handoff-fresh panel returned 749/764: on 12 rows naming an absent owner the reader matched a competitor's owner spelled inside an earlier record and answered that record's value where the parent abstained, and on 3 rows naming a present owner on reverse-form chains it answered the other owner's record. The stop gate stopped the chain there; no before-measurement, construction evaluation or comparison ran on it. Diagnosis and the fifteen rows are in `rejected-w1.json`; the contract was re-scoped to request words after the latest committed fact and renamed so the two candidates never share an identity.
- No refit was attempted: the promotion alone met every gate, so the learned witness of `4568679d` is retained unchanged.

## Resources and preservation

Model time charged this cycle is **6,290,011 ms** over 82 wrapped model commands (three handoff traces, two promotions, five review-case comparisons, construction preparation, two development history and handoff panels each, two before-measurements and two candidate evaluations of the construction, three early artifact runs and one allocation census, unit tests, seventeen population comparisons, the fresh draw with its evaluation and two comparisons, API, retained, older, word/Rust and focused runs), within the projected 8,000,000 ms; the shared ledger stands at **80,417,431 / 82,950,000 ms** after the 8,000,000 ms extension recorded in `projection.json` and `ledger-extension.json` before use, leaving 2,335,949 ms above the preserved 196,620 ms reservation; the review's own 76,804 ms remain charged. Engineering time is 700,807 ms over 23 commands (formatting, builds, test-binary builds, cargo check, claim wording); the closing claim-wording check after this record was completed is appended to `commands.jsonl` and is not in that total. Storage high water is 5,583,519,744 bytes of evidence, 6,180,864 of build output and 49,152 of worktree growth; the retained copy allocates 17,915,904 bytes; cycle growth 5,607,665,664 bytes stays within the projected 8 GiB, and the accounted total 69,124,304,896 bytes (from the review closure's 63,499,862,016) sits under the 73,517,920,256-byte limit (extended 7 GiB by the projection) with 4,259,397,632 bytes of headroom above the 128 MiB stop margin. Peak sampled process-tree RSS is 3,314,515,968 bytes (model) and 3,389,849,600 bytes (engineering). Four commands exited nonzero and are charged: two builds with compile errors (a missed call site, then two test compile errors) and two early artifact runs whose test expectations were wrong (the helper demanded that the frozen parent terminate on the very prompt it fails; the forward layout's parent already emits the owner sentence), each corrected in the test, never in an expected answer. No command was stopped, nothing unique was deleted, no paid external compute was used, and `.serena`, the untracked scratch diagnostic, every worktree, model, research directory, receipt and Downloads item are preserved.

The [immutable evidence snapshot](evidence/native_geometric_current_form_repair_973.json) binds commands, source/binary/artifact/data hashes, controls, classifications and all dispositions. The retained artifact is `.uor-models/native-typed-value-2026-09-05/current-form-repair/model.json`, CID `blake3:f3620cb7f15a6a1e8a369bd268dcd6651aba5840d2630517edadd23a6245b13c`, SHA256 `296ec483017b0e0b63cd63e5cb7ab44cb114230343888f49e81251f886b1d6a4`, 17,421,535 bytes. `4568679d`, `fc479376`, `90ebd843`, `e44a9ae4`, `e3a906c8`, parent `f0901dad`, rejected `ec79c303` and `52fc916b` and every earlier positive/negative artifact are preserved. Queue status names are compatibility acknowledgements, not test results. General prose, general reasoning/program synthesis, alpha/frontier capability and complete-path energy savings remain unqualified. #973 stays open; the matched writer-context obligation in #1139 remains separate and open.

## Next

Intermediate-version intent with exact depth labels is the next capability under #973, now that the current path answers its declared forms: define head-relative record depth against root-relative ordinal before any label, keep previous as one record hop (not previous distinct value), start with a bounded concrete depth request with exact candidate/anchor/restore proof, and never let an unknown or evicted target become the nearest surviving record. The eight owner-first bare-value rows on head chains are a separate emission cycle; they are not mixed into that acceptance.
