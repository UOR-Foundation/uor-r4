# Contextual writer refinement — #1139

## Implementation and pre-use projection — 2026-09-07

The retained contextual phrase-start model is `50dc0d23`, delivered through
PR #1175 at `f9525e3e`. Its supplied phrase panel is 50/52: both `quiet river`
examples have no writer record before start selection. Source and stored weights
identify a three-point action-1 loss when `quiet` moves from masked endpoint to
preceding context. This is an upstream writer hypothesis, not a demonstrated
repair. See the [preceding result](native_geometric_contextual_start_1139.md).

The implementation continues the existing writer's sparse rows from their actual
weights with a frozen 23-word prime dictionary, signed-H4 role geometry, exact
owner/endpoint candidate support and feature law. All 2,505 old row keys remain;
new observed keys start at zero. The original 200 labeled writer construction
examples anchor the fit alongside 24 new assertion/nonassertion contrasts with
known contextual words inside multiword values. Labels remain offline. The
learner retains its best complete construction score including epoch zero; a
partial or worse epoch cannot replace the warm parent. It uses the existing
margin-eight updates and first-maximum NoWrite tie order, within ten seconds.

An outer refinement witness preserves the exact previous writer and cache and
reconstructs the full `50dc0d23` parent before validating existing model layers.
The active old cache is removed and inherited-cache reuse disabled. The existing
NoWrite compiler rebuilds an exact sparse cache bound to the refined uncached
artifact. Serving still executes one writer and the same bounded integer/table
path; the prior writer is lineage evidence only. No new grammar, special-case
`quiet` rule, phrase parser, matrix product or transformer is introduced.

Before execution, `writer-refinement-projection.json` records 180 seconds of
expected model work, capped at 360 seconds: 45 preparation/fit, 65 cache/control/
preservation/fresh, 55 artifact/CLI and 15 correction reserve. Engineering
commands project 800 seconds, capped at 1,500: 30 preparation, 380 release build,
270 focused test build/checks and 120 correction reserve. New storage is capped
at 256 MiB: 48 model artifacts, 16 reports, 128 compiler and 64 transients/reserve.
One model process, one build job and a 4 GiB model RSS cap apply. Context is 512
tokens, retained relations and span words each 16, payloads 28 bytes, responses
32 tokens, fitting at most 256 documents/1 MiB/8,192 frames/16,384 rows. The
configured fit uses 32 epochs with a 64-epoch hard bound and ten-second learner
ceiling. Final artifacts and selection-before-fresh decisions are checkpointed;
actual-artifact tests compare restored predictions at every response step.

Standing authorization supplies a necessary 300-second cumulative extension
from 4,950 to 5,250 seconds while preserving 4,870.743 seconds already charged.
The 128 MiB storage extension raises the broad ceiling from 8,069,840,896 to
8,204,058,624 bytes and the tighter inherited growth allowance from
2,315,255,808 to 2,449,473,536 bytes. All prior charges, unique artifacts and the
128 MiB stop margin remain preserved. No deletion or paid external compute is
used. Read the resource projection and shared ledger for exact pre-use samples.

Selection requires exact complete bytes/EOS and owner/endpoint/action writes
on all 224 construction cases, twelve open cases and the exact prior 76 phrase
cases, plus accepted earlier preservation. Eight newly authored nonasserting
construction cases explicitly require zero writes. Cached and uncached results
must match; new fresh twelve run only after saved selection. Previous phrase
fresh twelve are now exposed preservation; the older shape candidate's separate
fresh eight remain unopened. This is a bounded writer decision, not general
language, causal composition or an energy claim.

## First execution and boundary correction

Coefficient-only candidate `990ccbce` fits all 213 distinct writer frames
(207 initially), retaining 2,505 old rows and adding 60. All 224 construction
writer labels, twelve open answers/writes and 76 prior phrase answers/writes
pass. The two `quiet river` answers are restored. Cached and uncached outputs
and writes match, and reconstructing the complete parent yields identical bytes.
Selection nevertheless fails: construction answers are 183/224, and eight prior
object-span answers regress. Fresh evaluation remains `NOT_RUN_SELECTION_FAILED`.

A matched diagnostic establishes that all 200 original anchor outputs and writes
are unchanged from `50dc0d23`. Both reach 159/200 against the historical answer
labels: forty conflict/retraction cases and one question-only case fail. These
are newly measured preexisting reader limitations; they are not silently removed
from the results. All 200 original writer labels remain exact.

The eight actual regressions emit `carries MATERIAL` instead of `MATERIAL holds`.
The parent already creates a false cross-boundary `User`→`MATERIAL` record,
followed by a conflicting `User`→`Assistant` record. The candidate removes the
second false write, exposing the first to the retained reader. Its source has
an exact candidate-feature alias between `holds.\nUser:` and a valid `holds OWNER`
assertion: dictionary addresses and masked roles omit separator/byte adjacency.
All other selected preservation passes, including 663/663 retained answers.

The correction adds an opt-in learned boundary feature and corresponding exact
NoWrite cache guards, with eight new offline NoWrite object contrasts. It does
not hard-code sentence admission. The original 200 prompts and labels remain
unchanged; the corrected gate requires their exact parent outputs and writer
labels, while reporting their historical answer failures. New construction,
open cases, all prior phrases and all preservation must pass before fresh runs.
This gate correction follows measured parent equality; the failed first
selection and its original criterion remain preserved.

`writer-refinement-correction-projection.json` records the complete remaining
requirement before use: 186 model seconds, 650 engineering seconds and 128 MiB
additional growth. Existing cycle/cumulative/storage limits suffice, so the
increment is zero. Prior charges and the 128 MiB stop margin remain intact.
Before corrected selection, four new short object cases are added to the twelve
unchanged reserved fresh phrases: two new owners/materials crossed with
dot-newline/dot-space boundaries. Those four require zero writes and exact copied
answers while their raw source remains available; the original twelve retain
their source-eviction condition. This remains within the same complete resource
projection. No object-value persistence claim follows from the short cases.

## Boundary-aware result — selected `79710468`

The corrected run fits 236/236 distinct writer frames from 226 initially in one
complete epoch (16 ms learning, 28 ms preparation). It retains all 2,505 old row
keys and adds 126, for 2,631 rows. The 23-word dictionary, signed-H4 role geometry,
candidate support and all other parent model parameters remain fixed. The two
new learned features describe only the gaps at the endpoint edges of the
owner/value interval, with role direction; they do not describe every interior
separator or parse arbitrary punctuation.

The selected cached artifact is
`blake3:79710468e3b75905550c15fbea62079e4267df774dbf4bc90ea45114826e4bfd`,
11,881,164 bytes, SHA-256
`d65434912f8b47317ea38ed0c76b6485a7e20c41a20ffab0e2b082621b8be82b`.
Its uncached parent is `069b01a9`; the full retained parent is `50dc0d23`.
The exact cache certifies 247 observed NoWrite signatures, each including eight
boundary descriptors. Periodic proposals are disabled for this law. Cached and
uncached generated bytes, EOS and exact writes agree across all 320 construction,
open and prior-phrase cases. Restoring the parent reconstructs identical bytes.

| Check | Parent | Boundary-aware candidate |
|---|---:|---:|
| New construction complete answers | 24/32 | 32/32 |
| New construction exact writer labels | 16/32 | 32/32 |
| All construction exact writer labels | — | 232/232 |
| Original anchor outputs and writes preserved | reference | 200/200 |
| Original anchor historical response targets | 159/200 | 159/200 |
| Open complete answers and writes | 8/12 answers | 12/12 |
| Prior phrase complete answers and writes | 74/76 answers | 76/76 |
| Fresh evicted phrases after selection | NOT_RUN | 12/12 |
| Fresh short boundary objects after selection | NOT_RUN | 4/4 |

All sixteen fresh cases have exact writer labels and no writes during emission;
the eight fresh nonasserting cases have zero false-positive writes. The two old
`quiet river` cases now return the complete intended value. All earlier span
panels recover 14/14, 6/6 and 9/9, and all other selected preservation passes,
including 663/663 retained answers, 28/28 long-window and 48/48 dependent reads,
forward/reverse sessions, version counts, isolation and the eight prior start
challenges. The old eight span cases are output-preservation evidence; exact
zero-write evidence belongs to the new labeled construction and fresh cases.

Selection is saved before the fresh source and results. The first coefficient-
only candidate and its failed selection remain preserved. The 41 preexisting
reader failures remain visible as 191/232 total historical construction-response
matches; this result qualifies contextual writing and preserves the retained
reader, rather than establishing broad conflict/retraction correctness.
The measured result combines boundary features with the new construction
contrasts; no isolated same-artifact boundary-feature ablation was run.

The next implementation is a bounded shared causal transition: the second Add
must consume the first committed result and retain its actual write ID as an
operand dependency. Shared state transitions and compositional emission remain
unfinished. General prose, arbitrary separators, retained object values beyond
the raw source window, frontier capability and energy advantage are unqualified.

## Verification and cumulative resources

Pinned offline release builds of the native probe, root CLI and focused tests
pass. Four warm-refinement tests, eight relation/cache tests and the integer-
kernel source guard pass. The actual artifact passes invalid parent/dictionary/
stale-cache rejection, checkpoints at every response step, exact assertion and
nonassertion writes, short/evicted reads and zero-allocation ingestion/emission.
The root CLI emits ` quiet river.\n` with EOS for
`the report says quiet river holds selra. Where is selra? Answer:`. The same CLI
loads the preserved coefficient-only `990ccbce` artifact and preserves that
opened output, verifying the absent-boundary-flag compatibility path.

Actual-artifact load is 15.839771166 seconds. Encoding is measured separately;
short ingestion plus response-start is 0.018125–0.240125 ms, and the evicted input
takes 3.955 ms. Across 52 predict/observe samples, median is 0.000541 ms and
maximum 0.249750 ms, excluding loading, encoding, ingestion and checkpoints.
The evicted ingestion sample is slower than the earlier phrase-start artifact's
sample; no matched throughput or energy comparison was performed. This delivery
prioritizes the bounded behavior repair and does not claim a speed advantage.

The complete cycle charges 292.555 model seconds, including both fits, parent
comparisons, the diagnostic, controls, preservation, fresh evaluation, focused
tests, actual-artifact work and both CLI runs. Cumulative use is
5,163.298/5,250 seconds; 86.702 seconds remain. The initial necessary extension
was 300 seconds; the correction used the existing allowance. The broad storage
ceiling remains 8,204,058,624 bytes, with all inherited charges and the 128 MiB
stop margin preserved. Model-process sampled peak RSS is 1,948,729,344 bytes,
below the 4 GiB limit. Command-level engineering charges and conservative storage
samples are retained in the evidence; samples are not exact transient peaks.
No unique material was deleted and no paid external compute was used.

Formatting, architecture-policy, claim-wording and diff checks are recorded
alongside the source hashes. Protected PR and merge-group CI statuses are
transport acknowledgements only; local executions above supply the actual
verification. Broader release suites and Studio/WASM/HTTP execution remain
`NOT_RUN` for this artifact.
