# Independent delivery review — #1094 retained assembly

2026-09-03. **`ACCEPTED_FOR_PROTECTED_DELIVERY`.** The independent
`/root/retained_admission_audit` reviewer accepts the bounded implementation,
metadata assembly, conditional release and accompanying evidence/roadmap update
for the protected PR path. This delivery does not complete the scientific
comparison issue and does not execute its release.

## Exact reviewed state

The reviewed committed head is
`2974e4a6509f7ee83a7db708099ba301a7ed5f4f`, against base
`6008e3527cb119d12af4abd99fe86a3d4ebe5a53`. The separately reviewed working-tree
ledger, `docs/integration/claim-ledger.json`, is **55,448 bytes**, SHA256
**`c3ec1582e3979e8402d63677e27a107ecc71df4245b9558839e60274b0f79eeb`**.
This review file is the reviewer's only change in the final delivery pass.

The source and two named check files are unchanged from the independently
accepted source commit `07ec3f0d39d08ac5bf9c2ba7a6b864229e007867`; a scoped Git
diff confirms that identity. No test, source-closure traversal, runtime check,
model operation or withheld access was repeated during this pass. The earlier
[implementation and exact-envelope review](r4_retained_assembly_1094_review.md)
and [direct named-check transcript](r4_retained_assembly_1094_checks.txt)
remain the source/admission evidence.

## Preservation and evidence checks

The reviewer used an independent standard-library comparison against Git
objects and the final diff to establish:

- All **10 previous claims** retain every substantive field, including
  statements, classifications, observations, assumptions, counterevidence,
  limits, evidence references and proof-artifact lists. Only next actions
  change. All **40 previous source records** remain exactly unchanged.
- The ledger adds **one explicitly scoped metadata/synthetic-behavior claim**
  and **nine source records**, giving 11 claims and 49 sources. Each new
  record's URL, revision and SHA256 matches its pinned Git object and current
  file bytes. Every claim's evidence references resolve.
- The new claim identifies observed metadata construction, independent
  conditional admission and 18 synthetic checks. It explicitly reports no
  model load/forward, fit, runtime/asset verification, withheld comparison or
  replay. No mathematical proof or raw-text model-output result is added.
- The original #1094 measurement record has only an appended checkpoint. The
  original preparation contract, machine-readable contract and independent
  contract/budget/source reviews remain byte-identical to the base revision.
- All **five public receipt copies** match their named originals byte for
  byte, with matching lengths and SHA256 values. The original output contains
  exactly those five unique regular files and no admission, execution, replay,
  stop or completion marker. It totals **185,873 bytes**; the original
  historical ledger remains **3,465,401 bytes**, giving **3,651,274 bytes** at
  this release before later campaign receipts.
- The nine current pointers—AGENTS, README, ROADMAP, RESEARCH, intelligence
  completion plan, geometric programme, integration current-state, integration
  plan and CONTINUE—agree that implementation/metadata assembly and independent
  conditional release are complete while comparison and replay remain
  `NOT_RUN`. They preserve #1079's weak control, #1082's descriptive limits,
  #1094/#973 open and #954 blocked. The immutable envelope's embedded
  `NOT_ADMITTED` is distinguished from the later separate release receipt.

The independently checked new source records are `retained_record_1094`,
`retained_source_audit_1094`, `retained_review_1094`, `retained_checks_1094`,
`retained_envelope_1094`, `retained_release_1094`, `retained_index_1094`,
`retained_coordinator_1094` and `retained_assembler_1094`. Diff whitespace also
passed. No blocking delivery finding remains.

## Delivery and closure boundary

Use **References #1094**, not an automatic issue-closing directive. The
protected queue acknowledges delivery transport; it supplies no additional
model or proof evidence. Preserve the bound coordinator/worker worktrees,
runtime, original receipts and sealed population. The reviewed storage record
identifies retained originals, public copies and regenerable index material;
this review authorizes no deletion or permission change.

The one concrete next action is the separately activated frozen comparison and
fresh-process replay through the exact reviewed `run-retained` envelope and
release. Fresh source/runtime checks remain mandatory inside the unchanged
budgets, retaining the 120-second historical policy debit and all prior bytes.
Any admitted-attempt marker consumes the envelope; a stop overrides completion
and permits no automatic retry. Delivery of this implementation/preparation
handoff leaves #1094's scientific DoD and #973 open, with #954 blocked.
