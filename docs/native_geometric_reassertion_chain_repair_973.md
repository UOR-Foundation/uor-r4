# Reassertion-versus-truncation repair of the version-intent chain contract — #973

This corrective cycle answers the independent review of [PR #1221](native_geometric_historical_version_973.md) and [PR #1222](native_geometric_truncated_history_abstention_973.md). The review reproduced four correct-to-wrong answers introduced by `e44a9ae4`: a resident, nonconflicting same-value reassertion (`selvi in Dusk Ridge. selvi in Dusk Ridge. selvi now in Copper Vale.`) was mistaken for a truncated history, so an initial-version request answered `Unknown.` where `e3a906c8` and `f0901dad` answered `Dusk Ridge`. It also found that fourteen rejected-candidate report directories from the first cycle had been overwritten and that the cited fallback logs could not recover them. This record repairs the chain contract, requalifies the version-intent witness, corrects the recoverability claim by [erratum](evidence/native_geometric_historical_version_973_erratum.json), and adds executable safeguards. The retained successor is **`90ebd843`**; `e3a906c8` remains the prior accepted comparison baseline and `e44a9ae4` is preserved as evidence of both the real truncation improvement and the discovered regression.

## The mistake and the contract

The writer stores a same-value assertion over a live head as a nonconflicting record with `action` one and a nonzero `previous` link; a differing assertion is marked a conflict. The version-intent scan followed only explicit revisions (`action` two), and `e44a9ae4` treated any failure to reach a genuine root as proof that the original was absent. That confused traversal rejection with physical absence: all three records of the reassertion chain were resident and the live directory named the head.

The repaired contract, versioned in the witness as `reassertion_links`, has two parts. A validated descending link is either an explicit revision or a resident same-owner, same-value, nonconflicting reassertion of a strictly older record; the value equality is re-proven byte for byte against the resident predecessor rather than trusted from the conflict flag. A chain is truncated only when the record where the walk stopped names a strictly older predecessor whose ring slot now holds a different identity; a conflicting, foreign-owner, malformed or otherwise rejected link is not evidence of eviction and yields no abstention candidate. Depth counts validated record links, not distinct values, so a reassertion adds one link; the class features stay chain positions. The frozen immediate-previous reader is unchanged: the previous version of a revised head is still the record it names, which after a reassertion is the reassertion itself.

The same contract is carried through selection, the field-anchor proof, emission and checkpoint restore: an ancestor anchor records `reassertion_links` and restores only under exactly that contract, while a legacy anchor claiming a revision-only proof for a path with a reassertion link is rejected, as is a reassertion whose value no longer matches. Legacy artifacts have neither flag on the wire and keep their exact behavior; `e3a906c8` and `e44a9ae4` are unchanged files with unchanged outputs under this source. A `HistoricalVersionIntentReassertionDisabled` control withholds the reassertion links and the eviction proof, exposing the legacy contract for causal comparison. A head that is itself a same-value reassertion still has no historical read, as under the frozen parent; this is a stated limit, not a claim.

## Repair targets, development evidence and the candidate

Repair targets were established as failing checks before the repair was measured. The review's 51 prompts (50 with independent semantic criteria) are open development evidence. The version-intent construction was extended with families whose expectations are exact record identities derived from the actual retained records, not from any model's output: same-value reassertions at the root, at the root of a longer chain, in the middle, repeated three times, interleaved with another owner's record, and beside an equal value owned by someone else (144 initial targets across four phrasings and three styles); equal-valued records under distinct owners (24 targets); a four-record reassertion chain followed by twelve, thirteen or fourteen unrelated facts, where twelve leave the root resident (12 root targets) and thirteen or fourteen evict it (24 abstention targets: the surviving same-value reassertion is not the root, so by exact-record identity the initial version is proven absent); the three-version chain with thirteen trailing facts, whose root is still resident (24 root targets); and heads that are themselves reassertions (inherit rows). The construction holds 4,131 documents, 822 targets including 132 abstentions, and 3,309 inherited rows.

Before the repair, `e44a9ae4` on this construction passes 662/822 targets: every reassertion-link initial target fails (0/144, all answering `Unknown.`), the twelve-filler reassertion roots fail (0/12), and four boundary abstentions answer the surviving reassertion's value; every other family, including the review's real truncation improvement, passes. The flag-only diagnostic candidate `6e8901a7` (the `e44a9ae4` witness under the versioned contract, no parameter changed) passes 818/822 and answers all 50 scored review prompts correctly (`e3a906c8` 42, `e44a9ae4` 42, `f0901dad` 37), repairing the four confirmed regressions and the four inherited middle-reassertion failures without any refit; its four remaining failures are thirteen-filler reassertion abstentions where the learned selector, never having seen a truncated chain beside a surviving depth-two reassertion, prefers that reassertion. That is why a refit was required by the mechanism, not by aggregate accuracy.

The first refit from frozen parent `f0901dad` over that construction (`d9712bfe`; 443 frames, 507 codes) passed 822/822 construction targets, the development panels, every row-by-row comparison, an independent draw (822/822 targets, 892/892 preservation), the API, the three retained populations and the fourteen older reports, and was then **rejected by the focused actual-artifact test**: in a multi-turn session, after an initial-version turn on a four-version chain with the `State the owner first.` instruction, its previous-request turn anchored record 2 (`tilva was in Birch Grove.`) instead of the immediate previous record 3 (`Pine Hollow`), and its current-request turn anchored record 2 as well, while `e3a906c8`, `e44a9ae4` and the flag-only candidate answer both turns correctly. The cause was structural rather than a bad draw: a follow-up request view carries the prior response's words, and no such view existed in the construction, so a fresh fit's deferral on follow-ups was untrained and fragile. The candidate is preserved with its evidence (`selection-1-rejected.json`), and the design was explicitly reopened.

The reopened construction adds **follow-up documents**: prior turns replayed in the same session and answered by the model itself (in training, by the frozen parent; in evaluation, by the candidate), after which a previous or current request must keep the frozen parent's immediate-previous or current record, in owner-first and plain styles and after one or two earlier turns, on three- and four-version chains, competing chains, root and middle reassertion chains, an abstained evicted-root chain and an abstained evicted reassertion chain. The training-document format gains an optional `history` list (absent for single turns) and the fit replays it before the labeled prompt; the evaluator, comparison mode and the native API check replay it the same way, and a follow-up target is judged on the exact record identity through the one-link proof or the live head. Before the repair, `e44a9ae4` on this construction passes 806/976 targets: the 144 reassertion-link and 12 reassertion-root failures above, four boundary abstentions, and ten follow-ups where, after an abstained initial turn and a previous turn, the current request also answers `Unknown.` (144/154 follow-ups). The second refit from frozen parent `f0901dad` satisfies all 454 distinct frames with 507 feature codes over a 128-word per-view dictionary. The candidate **`90ebd843`** passes **976/976 construction targets**: 690 exact root answers and selections at depths one to three, **132/132 abstentions** through actual no-read decisions and **154/154 follow-ups** keeping the exact immediate-previous or current record after one or two earlier turns, while preserving **3,309/3,309** inherited rows; exact authored text alone is 864/976. Complete removal reproduces the parent on every target. Withholding only the reassertion links and eviction proof leaves exactly the 820 targets that need neither, so the repaired behavior is carried by the versioned contract; withholding the abstention candidate returns the parent's answer on 116 of 132 abstention rows; immediate-previous-only admission answers none of the 498 deeper root targets; the isolated transform control leaves the parent's 302; removing the committed request scope preserves only 3,050/3,309 and 965/976.

## Independent draw, comparisons against `e3a906c8` and `e44a9ae4`, and retained behavior

After the source, binary, data, artifact, selection decision and acceptance criteria were frozen a second time (`selection.json`; the first freeze is preserved as `selection-1-rejected.json`), a genuinely new draw with fresh owner, value and filler spellings (owners `adgmi`, `xdnka`, `lqtkt`; 1,868 cases) passes **976/976 targets (690 exact root answers and selections, 132/132 abstentions through actual no-read decisions, 154/154 follow-ups) and 892/892 preservation cases, with complete removal reproducing the parent on all 976**; exact authored text alone is 864/976, withholding the reassertion links leaves exactly the 820 targets that need neither link nor eviction proof, withholding the abstention candidate returns the parent on 116 abstention rows, and removing the committed scope preserves only 738/892. Row by row on the draw, the candidate differs from `e44a9ae4` on 200 rows and from `e3a906c8` on 330 rows, every one a declared reassertion-link target or follow-up, reassertion-boundary root, abstention or follow-up, or evicted-root/evicted-pair abstention or follow-up; no undeclared row changes. All **3,904 native API checks** pass: 1,952 construction and 1,952 fresh completion/checkpoint-independent-sum checks through the actual Rust native API, including every abstention and every follow-up replayed through the same API session, with the plain-style owner-sentence alternate declared in the case files before the draw. All 72 query-owner, 216 historical-transfer and 764 handoff-fresh comparisons are exact. All fourteen older behavior/control reports have zero checked differences, including their negative interventions. Twelve multi-turn histories replay with identical outputs, records, versions and sources, 24 word/Rust cases are preserved, Copy/Add and action each pass 24/24 ordinary and 24/24 stress cases on the new artifact, and actual generated Rust is compiled and executed. Focused verification passes **35 checks** on `90ebd843`: eight chain-traversal tests (reassertion link, eviction proof), three version-intent format tests (contract flag, follow-up documents), nineteen field-composition, historical-field, handoff, query-context and role-read tests, two report-directory collision and manifest tests, the native integer-kernel source guard, the actual-artifact test and the allocation census. The artifact test executes sixteen conversations over 56 turns, checking 6,074 input and 1,298 output checkpoint positions and 44 active anchors: two evicted-root abstentions, two reassertion-root and two reassertion-middle sequences whose anchors restore only under the versioned contract and reject a legacy claim or a changed reassertion value, two evicted-reassertion abstentions with the reassertion resident, parent equivalence under complete removal, and no root anchor when the reassertion links are withheld. The allocation census records zero allocations and zero bytes across 1,054 input and 140 output positions including reassertion-path and evicted-root cases; it is a census of observe/predict, not an energy measurement. The same artifact test rejected `d9712bfe`; the first focused chain of this cycle failed to spawn its unit tests (five zero-length rows, charged) and was rerun. This is not HTTP/browser serving. Full-suite, new-artifact HTTP/browser/WASM and complete-path energy checks are NOT_RUN.

## Evidence-preservation safeguard and erratum

Every driver used in this cycle now claims its report directory through `uor_r4_core::report_output::claim`, which creates the leaf exclusively before any model work and fails byte-identical on an existing report, and seals completed attempts with a BLAKE3 manifest through `report_output::seal`; the version-intent driver gains `seal DIR` for the panel scripts and the API check creates its report file exclusively. Two focused tests cover the collision (an existing report and its sentinel stay byte-identical, the claim fails, a neighbouring attempt path completes) and the manifest. The first cycle's evidence entry claiming that the rejected `d6144304` reports survive in the `preserve-*-2` logs is corrected by the [erratum record](evidence/native_geometric_historical_version_973_erratum.json): those logs hold at most a one-line count; the fourteen older reports and the word/Rust report were overwritten in place by the retained candidate's run; what survives of the rejected candidate is listed with hashes; no backup was found in a read-only search; nothing was reconstructed. The original snapshot bytes are unchanged. The same record corrects two narrative counts: twelve fit/refit invocations rather than seven, and 76 owner-sentence alternatives on the final 474-target draw rather than 64.

## Limitations and next implementation

Abstention after a reassertion chain loses its root is a decision by exact-record identity: the surviving reassertion carries the same value as the evicted original, yet it is not the initial record, so the model answers `Unknown.` rather than recovering the value; this is the authored expectation, frozen before measurement, and a design choice that a later cycle may revisit explicitly. A head that is itself a same-value reassertion has no historical read under the frozen parent contract and preserves the parent's answer. Plain-style initial requests under two phrasings still receive the inherited owner sentence for the same exact record; exact authored text alone is reported separately. Depth counts validated record links, so a request for an intermediate version is neither labeled nor claimed. The review prompts became open development evidence in this cycle; the independent draw is the only held-out gate. These are bounded authored forms with fresh spellings, not temporal language understanding.

Next under #973, once this repair is qualified: intermediate-version intent with exact depth labels, so a request for a specific earlier version selects that exact record or abstains when the chain no longer holds it, while preserving initial, previous and current behavior, reassertions, competing owners, dependency abstention and causal follow-ups.

## Resources and preservation

At the immutable evidence snapshot this cycle used 8,831.255 model seconds and 1,404.460 engineering-command seconds, including the review-prompt and prior-population comparisons run twice (once for the rejected refit), one fit refused at the document bound, one rejected refit with its complete qualification, one comparison stopped by the storage monitor, five failed unit-test spawns, one failed word/Rust panel step (a duplicate exclusive claim, corrected and recorded), one construction API case list built from the wrong construction (rerun on the right one) and four multi-turn probes, all charged and preserved. Cumulative model use is 61,371.301 / 62,950.000 seconds, with 196.620 seconds reserved. The initial projection allowed 3,600 model seconds, 1,800 engineering seconds, one process/two threads, 4 GiB model/6 GiB build RAM and 1.5 GiB new storage, with a 3,000-second ledger extension and a 1 GiB storage extension recorded before use. Four standing-authorized revisions, each recorded before use, raised the cycle to 10,400 model and 2,700 engineering seconds and 7 GiB new storage, with further ledger increments of 1,000 and 4,500 seconds and storage-limit increments of 2.5 and 2.5 GiB. The review's 184.344 seconds were already in the ledger and were not recharged. Cycle growth is 6,632,177,664 bytes; accounted storage is 52,884,475,904 / 54,190,567,424 bytes, leaving 1,171,873,792 bytes after the 128 MiB margin. Peak sampled RSS was 3,142,303,744 bytes for model commands and 3,319,185,408 bytes for builds. No unique deletion or paid external compute occurred. Final documentation and protected-delivery charges append to the mutable receipt without resetting prior charges.

The [immutable evidence snapshot](evidence/native_geometric_reassertion_chain_repair_973.json) binds commands, source/artifact/data hashes, controls and all dispositions. The retained artifact is `.uor-models/native-typed-value-2026-09-05/reassertion-chain-repair/model.json`, CID `blake3:90ebd8434ce14a74763cf2e31f9c14128e7a9b977258e90833ddc1a2e76382a6`, SHA256 `f17a46e6aea16134f866c68167e2008efcae8b18bf021c39662cfaec61d50698`, 17,326,536 bytes. `e3a906c8`, `e44a9ae4`, parent `f0901dad`, the flag-only diagnostic candidate and every earlier positive/negative artifact are preserved. Queue status names are compatibility acknowledgements, not test results. General prose, general reasoning/program synthesis, alpha/frontier capability and complete-path energy savings remain unqualified. #973 stays open; the matched writer-context obligation in #1139 remains separate and open.
