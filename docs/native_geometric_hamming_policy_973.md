# Deterministic shared Hamming policy and complete byte path — #973

**PASS_HAMMING_POLICY_TRAJECTORY_CONFORMANCE. Language: UNQUALIFIED_INITIALIZED_NO_FIT.** The initialized policy produces0/24 correct complete answers and1/460 correct teacher-observed response symbols, mean CE10.9981724105. No learning fit, optimizer update or model promotion occurred. Retain15baec48. [Source-bound evidence](evidence/native_geometric_hamming_policy_973.json) binds the executed sources, binary, raw traces, artifacts and cumulative resources.

## Implemented path

The new Rust module hamming_policy replaces the authored refinement adapter with a source-bound parameterized policy and connects it to actual session prediction/observation. One fixed seeded LUT4 circuit (4096→1024→256→1796;3076gates,6152parameter bytes) is shared across Query,Extent,Delta,Control,Emit,Observe,Key. The wide output interface avoids the predecessor's nine-bit context/head restriction. Structural coverage does not establish effective capacity or learning.

Two deterministic Hamming refinement hops read the actual committed256-occurrence/eight-result store. The frame carries all four initial/working/selected start-context roots, two complete64-byte payloads with validity and keys, observed byte, phase/round, position, fixed-zeta bins and active-lease metadata. Exact references remain in typed memory and receipts; prime/hash ID bits do not supply semantic distance. Four120-bit outputs decode to actual H4 roots by nearest XOR/popcount signature and canonical-index tie resolution. Group-table composition changes the state before the next query. Separate four-bit extent/action/byte-EOS scores use shift/OR and legal argmax. No serving matrix product, weighted-bank contraction, transformer or provider is introduced. Offline CE normalizes the same hard integer scores.

Control prepares exact typed operands and prospective actions; Emit receives refined state, full selected spans and active lease. Observation processes the actual byte after prediction. Causal prefix refinement is retained on a teacher mismatch, while speculative action/lease acknowledgment still requires the offered symbol. Observe/Key updates roots and publishes the byte or completed typed result. Input's current byte is known before its own two reads, but is committed afterward. Repeated predict is cached; artifact/parameter/geometry/intervention bindings prevent a different pending model.

This version uses bounded snapshot imports and transactional clones. It is not allocation-free or optimized serving, and no complete-path energy advantage is measured. Full paired-H4/fiber transport, hierarchical routing, durable learned page writing, normal retained-artifact dispatch and a scalable learner remain unfinished. Canonical geometry construction binds the existing prime/token/zeta/exact-H4 authorities; this does not establish their predictive contribution.

## Executed checks and actual effects

Eleven focused release tests pass, plus the separately invoked/sealed report. They cover strict source-bound artifact decoding and corruption, full-root/full-payload fields, structural circuit reach, tied parameter counts, parameter/geometry/intervention binding, repeated/stale offers, mismatch/EOS/turn behavior, complete replay, loss arithmetic and typed result publication. The authored1+1 fixture emits2 through actual Session: correct acknowledgment publishes a canonical result with operand references and final keys/roots, and the next scan admits it. Mismatching3 publishes nothing. Admission is tested; equal-key ordering still selects the older occurrence, so this is not learned computed-result selection.

Independent review found and corrected the PayloadMasked active-byte bypass before the separate report. Both full builds are charged and the earlier source is preserved. Final masking covers both selected span arrays and the active byte; observed prior bytes, geometry, span length and validity remain available.

Two predeclared interventions complement all truth cells of hidden gate0 and geometric-output gates1280..1296exclusive, on train/memory-a/0 and train/add-3-5/0. Each parent, changed and loaded-artifact trajectory restarts from empty memory and re-ingests the whole document. Changed selected references/payloads count81,105,80,108; changed teacher-predicted symbols7,28,7,29. Later roots and queries change as well. This demonstrates real whole-trajectory consequences rather than a frozen-parent-pointer calculation.

Exact response-loss differences are+4.8458394943,+24.7510504658,−34.7147917370,−15.0594151699; an independently arranged logsumexp matches within1e-9. Reused parameter visit counts are350,482,5600,7712. No variant is selected as a candidate. This is an exact discrete finite contrast, not a derivative, unbiased gradient estimator, optimizer or proof of learnability.

## Generated behavior and initialization limitation

All16 reused construction and8 development records generate under Full,ReadDisabled,UpdateDisabled,FirstRootOnly,PayloadMasked with64 output-symbol cap:120 saved complete attempts. Every control has0/24 exact answers. Full produces no EOS within64symbols and no correct program; program execution is NOT_RUN. These are authored open development data, not independent final holdout or a comparison qualifying regression against retained15baec48.

Saved-score inspection, with zero new model calls, finds ties at the maximum in1536/1536 Full steps (mean16.2526 tied symbols). EOS reaches the maximum64times but loses the canonical lower-index tie. This explains part of the initialized low-byte/output behavior; it does not establish that learning can overcome it. The first learning update must change actual content/EOS winning margins, not merely a surrogate loss or source-selection count.

Initialized artifact ID d6b057999fb2605d007813f1db611307c8ea5f4f6472d1b77058f9385feec932; parameter digest 810581c6f60a2e0e014c34a07d3a6cde7158f835ee18aa0a2fb2e028811ce73b. The artifact schema explicitly says INITIALIZED_OR_CONFORMANCE_INTERVENTION_NO_OPTIMIZER. Actual code/data/operator/binary bindings and all raw tokens are in the evidence record. Separate saved parameter interventions are conformance material only.

## Resources and preservation

Complete model/build/test/report charge239769/360000ms, including both builds118354+118301ms and report3114ms. The report's internal2.829602s covers the finite contrasts and all generated controls; it is not per-token serving latency. Peak sampled build RSS2628534272bytes; report RSS22331392bytes. Shared cumulative123730724/132950000ms; parent3980151/4110000ms. Necessary parent+240000ms and+64MiB storage extensions were recorded before model execution under standing local authorization. Step storage growth37335040bytes before delivery, within128MiB;128MiB stop margin retained. Final handoff receipt records delivery growth and engineering time.

Both original dirty checkouts, retained model, all earlier source and29prior sealed roots/876files verify unchanged. Source freeze binds54files plus executed binary. No V3–V7 replay, old fit, paid compute or destructive cleanup. #973 remains active/open; #964/#820 acceptance is unchanged. Queue acknowledgments are not the executed Rust tests.

## Next action

First establish one learned contextual read producing a correct single byte followed by EOS on a deliberately small construction task. Vary the source value while keeping the query fixed, and compare the same artifact with ReadDisabled; demonstrate learning from parameters, not an authored routing/output fixture. Freeze that minimal acceptance and one finite dose before fitting. The existing24 older examples are observational baselines only, not pass/fail requirements for this first learning step. Do not require accumulated memory-repair, reasoning, multi-step or retained-model parity now. After the one-read task is learned, extend to genuinely dependent reads with update/restricted-context interventions; only later run the broader accumulated panels and retained regression controls when considering model replacement. Implement/select the update using the current complete deterministic trajectory reference, sharing parameters across repeated calls and recomputing downstream queries/observations/publications; declare any surrogate objective and bias. Address actual content/EOS winning margins (initialized Full had1536/1536maximum ties and EOS lost64ties) without answer overrides or silently changed tie rules. Do not resume the old per-invocation stochastic recipe or exhaustive coordinate harvesting. Preserve current initialized and negative artifacts; no promotion follows construction learning. Record the complete local preparation/build/fit/evaluation/correction/storage projection before execution under standing authorization; no paid compute or cleanup.
