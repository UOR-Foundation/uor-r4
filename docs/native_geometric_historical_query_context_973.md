# Contextual historical selection — #973

Balanced candidate `84f82191` passes all 48 construction targets and all 64 separately drawn fresh historical owner/value targets, while preserving 171/171 construction and 227/227 fresh inherited cases. Broader retained behavior and the actual-artifact checks also pass. The candidate is now **retained**, replacing `8cacb2fe` for this bounded native path. All 330 native API checks pass. The parent and both negative candidates remain preserved.

This cycle exposes the historical selector to the sixteen query words already captured by the model and allows a bounded refinement of its shared signed-H4 code roots. Exact owner matching remains limited to eight words. Query-local input boundaries, current-directory admission, immediate-previous links and the historical owner/value compositor are unchanged. The new serving path adds no phrase parser, response template, transformer, provider, expert network or matrix-product operation.

## Observed failure and exposure-only result

The retained `8cacb2fe` answers `Where was selvi before? Name the owner first.` with the correct historical sentence. Appending the same instruction to `What was the previous location of selvi?` instead produces ` Unknown.\n`.

Actual Rust traces show `was` at captured reverse index 10 with prime 421, outside the eight-word ordered historical features. The historical-copy score is 4 against Base 5; the short query scores 9 against 5. Both records and the queried owner are present. After historical deferral, the dependent selector chooses a missing link and the existing NoRead response.

The sixteen-word exposure-only artifact `0fe078da` changes none of the 18 diagnostic outputs, stops, first decisions, relation states or historical scores. It remains a preserved negative intervention. Merely exposing additional words did not repair the measured failure.

## Rejected first fit: owner-position shortcut

Candidate `7c33071d` changes an inherited global `was` unigram code and adds a nonidentity ordered pair involving `holds`. Its 1,759 deduplicated examples produce 134 distinct competitive frames, all satisfied after three accepted coordinate updates. The candidate passes the original single-owner construction: 32/32 exact historical sentences with their old/current record proofs, and 115/115 untargeted comparisons. Those positive results remain evidence for that candidate and panel.

The subsequent [owner diagnostic](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/owner-diagnostic.json) exposes a shortcut: queries for either of two owners, and for absent owner `merli`, copy the first directory owner's old value. Reversing the fact order reverses the answers. Only **4/8 present-owner requests** are correct, and **all four absent-owner requests incorrectly copy a value**. The [rejection receipt](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/fit-rejection.json) records `REJECTED_OWNER_POSITION_SHORTCUT`; `7c33071d` is not promoted.

Source and artifact inspection explain the risk. Global ordered-query features apply to every historical candidate. The exact queried-owner features were present in the representation but retained identity roots for the long Name/State forms, so they did not distinguish that owner's candidate from the others. The original single-owner fitting constraints did not require that distinction. Earlier passing preservation and interface checks on `7c33071d` remain scoped evidence for the rejected artifact; they do not validate its successor.

## Balanced refit using the same feature law

The successor starts again from `8cacb2fe`, with 72 additional contrasts covering both owners, reversed order, recent and evicted facts, absent owners, current requests and short historical requests. It changes no feature or candidate-admission law.

The [balanced fit receipt](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-fit/fit.json) records 1,831 documents, 149 distinct competitive frames, 255 feature codes and five accepted coordinate updates. Frame correctness improves from 143/149 to 149/149 with zero final hinge loss. Expected response bytes are absent from the fitting examples: labels supply exact current-record identities, explicit Base decisions, or preservation of the parent's actual historical choice.

The complete successor identity is `blake3:84f8219164c08fe2ff10de0794af9658075b2ad5ad7723e1aa2b12d40a7a1fe5`. The [root-change receipt](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-root-changes.json) records two final nonidentity changes relative to the parent and identity-initialized new keys:

| Feature | Context | Initial roots | Final roots |
|---|---|---|---|
| `kind=3, a=313, b=0` | Exact owner match with neighboring `of` and an unknown-prime word | `[119,119]` | `[112,119]` |
| `kind=6, a=229, b=0` | Inherited global `is` unigram | `[119,119]` | `[100,119]` |

The first is a newly admitted owner-context key; the second is inherited. The rejected candidate's learned `was` and `holds` changes are absent. All 185 inherited keys survive in the 255-code vocabulary. Dictionary, query scope, landmarks, biases, ranks, original training receipts and the complete inner model remain frozen.

## Construction results and control limits

The [balanced construction result](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-construction-result/result.json) records:

- **48/48 exact target sentences**, with EOS, unchanged relation state and the expected historical/current record proofs.
- **171/171 inherited comparisons preserved**, covering output, EOS, first decisions apart from diagnostic scores, and initial/final relation state.
- **48/48 full-removal comparisons equal to the parent**.
- **48/48 target sentences with the window-only control**, which keeps the learned router but restores eight-word exposure.
- **0/48 target sentences** under each of full context removal, historical-read removal, global learned-transform removal and field-read removal.

The balanced candidate's target answers therefore do **not** establish a contribution from the wider window. Its learned owner-context change is already visible within eight words. The matched [window-control panel](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-window-result/result.json) identifies a different contribution: the active router with eight-word exposure preserves only **155/171 inherited cases**, while Full preserves **171/171**. All 16 differences are long current requests of the form `What is the current location of OWNER? Name the owner first.` Full and the parent return `Unknown.`, an existing negative. The eight-word control instead emits a previous-version owner sentence.

Under this fitted router, sixteen-word exposure is therefore needed on this panel to preserve historical deferral for current requests; it is not needed for the 48 historical target answers. This does not make `Unknown.` a correct current answer. The global transform control affects the entire learned path and is not an isolated historical-root intervention.

## Fresh transfer and retained behavior

The [fresh summary](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/fresh-summary.json) records **64/64 exact target sentences and historical/current proofs**, compared with **0/64** target sentences for the parent, plus **227/227 inherited comparisons preserved**. All 64 full-removal responses equal the parent. The window-only control still produces 64/64 target sentences; full context removal, historical-read removal, global learned-transform removal and field-read removal each produce 0/64. All eight fresh absent-owner cases return the expected `Unknown.`.

The fresh receipt verifies that selection preceded the draw, all three new owner/value tuples were absent from training, the source and model hashes stayed unchanged, and no refit or redraw occurred. Target generation checks 1,664 output checkpoint positions. This establishes transfer to these fresh identities within the authored forms and tested owner/history bounds.

The balanced candidate also preserves the broader retained behavior:

- [Fourteen prior reports](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-validation/preserved-comparison-v2.json) have zero differences in actual output, EOS, correctness and applicable action/value/record/read/dependency identities. Diagnostic scores and timing are excluded; previously incorrect interventions remain negative results.
- The 1,300-case retained construction, 72-case query-owner panel and 216-case reverse-start fresh panel match their parent comparisons exactly.
- All [12 multi-turn histories](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-validation/history-comparison.json) and 24 word/Rust cases are preserved. Copy/Add and action panels each pass 24/24 ordinary and 24/24 stress cases. Generated Rust is compiled and executed.
- Replaying the two prior historical-field panels repairs their 16 and 24 long previous-location owner-first cases, while preserving the remaining 99 and 147 cases respectively. These are the 40 declared repair cases, not unnoticed changes to inherited targets.

These are executed checks on `84f82191`; the earlier checks on rejected `7c33071d` remain separate evidence.

## Causal checkpoints and native interfaces

Focused verification totals **44 passing checks**: 42 library tests, one integer-kernel source guard and one actual-artifact test. The [actual balanced-artifact test](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/actual-balanced-artifact.log) exercises 22 sequences, including 16 with two owners. It checks 6,461 input checkpoint positions, 1,308 output checkpoint positions, 66 expected malformed-restore rejections and 66 disabled-control parent-turn comparisons. Each sequence checks historical/current fields and an independent sum. This is causal and interface validation, with no allocation or energy measurement.

The [construction native API receipt](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-validation/api.json) passes 96 checks, and the [retained historical API receipt](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-validation/api-retained.json) passes 106. The [fresh native API receipt](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/api-fresh.json) passes another 128 checks, for **330/330 native API checks** in total. These exercise the actual native API, checkpoint import and independent sums; they do not qualify HTTP/browser serving or general language behavior.

The retained artifact is [historical-query-context/model.json](/Users/casey.allard/uor-r4/.uor-models/native-typed-value-2026-09-05/historical-query-context/model.json), with identity `blake3:84f8219164c08fe2ff10de0794af9658075b2ad5ad7723e1aa2b12d40a7a1fe5`, SHA-256 `ba5e72409642c6710a8d694b48e63b311c2e63cf6d90b7ae2ed3bd8d2291f3b0`, and 15,731,317 bytes.

## Artifact and runtime safeguards

The outer witness in [historical_query_context.rs](../crates/uor-r4-core/src/native_geometric/historical_query_context.rs) binds the full `8cacb2fe` parent and stores the active reader separately. Removing the witness restores the parent's identity and bytes. Empty training receipts are valid only for an exposure-only artifact with exact inherited parameters; fitted artifacts bind their actual examples and configuration.

[historical_read.rs](../crates/uor-r4-core/src/native_geometric/historical_read.rs) selects the active reader with sixteen-word exposure under Full. `HistoricalQueryContextDisabled` uses the inherited reader and eight-word exposure. `HistoricalQueryWindowDisabled` keeps the active reader and exposes eight words. The ordered feature buffer remains 96 entries, with a maximum of 65 used; capture, record and checkpoint-state capacities do not grow. Historical-field anchor replay uses the same controlled historical selection.

Forty prior long previous-location owner-first negatives from the two historical-field panels are declared repair targets and excluded from inherited-output constraints. Preparation records each exclusion and its original source. Other imported cases constrain the actual parent's historical selection. Original reports, positive and negative artifacts, full worktrees and owner material remain preserved.

These results concern bounded authored forms and exact addressed-memory operations. General prose, arbitrary wording and entity counts, general reasoning/program synthesis, alpha/frontier capability and complete-path energy savings remain unqualified. Selection of an initial version rather than the immediate previous version remains unfinished. The [actual current-request diagnostic](/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context/balanced-current-diagnostic.json) identifies the next blocked step. All six long current requests return `Unknown.` after historical deferral. The dependent selector chooses `[16,0,[2,0]]`: it selects record 2 for a dependency operation, finds no second link and emits NoRead. For Name/State, its H4 state is `[119,81]`; copy action 1 scores 10 against Base 9. The matched short current request has state `[119,119]` and action scores `[9,8,8]`, so dependent selection defers and the current answer succeeds.

The long form's only mapped nonidentity dependent feature is `kind=4, a=0, b=419`, the owner-neighbor `of` feature. Current and previous requests do contain distinct dependent unigram features: `is` has prime 313 with identity roots, while `was` has prime 541 and no mapped code. These are unused distinctions, not identical feature representations; dependent primes differ from the historical reader's dictionary.

The smallest next learned intervention to investigate is contextual dependent Base/defer with owner, missing-link and genuine dependency controls. Its success alone would not establish a complete answer repair: the diagnostic current reader including recent values selects record 2's source `[33,2]` in all six cases, but the default persistent reader defers because that value remains recent, and counterfactual direct routing selects NoRead in five of six cases. Preserve those dispatch and ranking observations when testing the next change, before extending selection to richer initial-versus-previous histories.

## Resource projection

The pre-use projection reserves 3,600 model seconds, 1,800 engineering-command seconds, one model process with two threads, 4 GiB model RAM, 6 GiB build RAM and 1 GiB new storage. Input bounds are 512 tokens / 4,096 bytes for new cases and 8,192 tokens / 65,536 bytes for inherited cases, with 96 output tokens. Inherited five-turn histories and 1,056 padding tokens per turn remain available.

Standing-authorized increments are 1,200 model seconds and 256 MiB storage, preserving all prior charges, the 196.620-second reservation and the 128 MiB stop margin. These are projected allowances, not final measured cycle usage. No unique deletion or paid external compute is authorized by this projection.

Local evidence: `/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-query-context`.

The [immutable evidence snapshot](evidence/native_geometric_historical_query_context_973.json) records 3,001.982 model seconds and 770.737 engineering-command seconds for the complete cycle, including rejected candidates, the corrected initial build and a missing-input API attempt. The latter produced no API behavior result; only its corrected rerun is counted as passing. Cumulative model use is 39,707.311 / 40,650.000 seconds, with 196.620 seconds reserved. Accounted storage is 39,912,017,920 / 40,768,794,624 bytes, leaving 722,558,976 bytes after the 128 MiB margin. Sampled peak process-tree RSS is 1,929,494,528 bytes for model commands and 4,130,078,720 bytes for engineering commands; these are samples, not exact transient maxima.

Final documentation, checks and Git delivery append to the mutable command/resource receipts without resetting cumulative charges. Queue status names are compatibility acknowledgements, not test results. No unique material was deleted and no paid external compute was used. The historical writer-context matched-control obligation under #1139 remains separate and open.
