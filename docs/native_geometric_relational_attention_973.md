# Learned typed geometric attention — #973

**PASS_TYPED_ONE_READ_ATTENTION_LEARNING.** A Rust-trained finite compatibility reader and byte/EOS codec generate all64 development answers correctly, choose all64 correct sources, and answer both members of all32 changed-source pairs. The same fitted codec with uniform initial routing generates16/64 correctly; ReadDisabled and QueryReversed each generate0/64 original answers. This is the first learned compatibility/decoder boundary of research hypothesis H1. It does not complete the jointly trained recurrent model or qualify prose. Retain15baec48; no candidate promotion.

## What executes and what is supplied

The public [relational_attention module](../crates/uor-r4-core/src/native_geometric/relational_attention/mod.rs) receives a two-byte query and four typed records, each with a two-byte ordered key and one-byte payload. Record boundaries and key/value bindings are supplied. The answer, target position, example identity and family are absent from the runtime interface. The test does not learn tokenization, language parsing, query construction, durable-memory admission or dependent state updates.

The fixed canonical encoder forms four roots: the two byte leaves and their products in both orders. Each query/candidate packet exposes all16 directed relative roots, all16 existing canonical-landmark Hamming distances and four ordered modular phase differences:288 Boolean inputs. Ordered prime addresses remain identity metadata, not semantic hash distances. These four roots are not a claimed paired-H4/icosian lift; retained fiber and complete E8 integration remain unfinished.

The shared scorer has288 trainable unary Boolean predicates followed by a fixed balanced conjunction,575 LUT2 nodes in total. Every input can reach its decision; a focused dependency test exercises every input. Its output is one learned compatibility bit, not graded softmax weights. All four candidates are admitted; earliest maximum wins, including ties. Learned table inspection finds30 changed relative-root predicates,24 Hamming predicates and64 phase predicates. Parameter changes alone do not establish each feature group's causal necessity.

Nine learned LUT2 decoder nodes predict eight byte bits plus EOS using selected payload bits and whether a first symbol was observed. Codec curriculum learns this mapping over the training-answer repertoire, followed by routing updates against whether an actual forced-read generated byte and EOS match the answer. No authored target index trains the scorer. Query/key encoding, conjunctive topology and typed record bindings are fixed; this is curriculum learning, not joint query/state learning.

Exported prediction uses Boolean operations, finite geometric/parameter table reads, shifts, popcount, addition and comparisons. It executes no matrix contraction, transformer or source-model/provider response. Bounded vectors and validation still allocate; optimized latency, zero allocation and energy savings are not established. The JSON artifact binds geometry, training data, implementation digest, topology, hard tables and training provenance. Hard export/reload reproduces all8256 Full outputs exactly. Optimizer state is not an exact-resume checkpoint.

## Environment, learning and preserved corrections

Before fitting, the environment checked complete candidate admission, no full-packet target/distractor collisions, ordered-query discrimination, disjoint split families, balanced answers/target positions, changed-source pairs and absence of answer metadata from prediction. A query-slot shortcut was corrected before the first fit. The64 development rows remained byte-for-byte identical across all four attempts; they are open development data, not a final untouched holdout.

The gate requires at least61/64 exact answers and correct selections,31/32 complete changed-source pairs, at least32 additional exact answers over the same learned codec with uniform routing, zero original-answer successes under ReadDisabled and QueryReversed, and completion of the configured finite training dose. Old24-panel and V3–V7 parity are not first-learning entry conditions. Acceptance thresholds and development rows were not relaxed after failure.

| Preserved attempt | Training rows | Development exact | Complete pairs | Outcome and demonstrated cause |
| --- | ---: | ---: | ---: | --- |
| 1 | 128 | 14/64 | 6/32 | FAIL. Soft loss fell without changing any hard scorer table; codec coverage also remained incomplete. |
| 2 | 128 | 16/64 | 8/32 | FAIL. Hard routing learned training perfectly, but rejected unseen positive feature values. |
| 3 | 2048 | 42/64 | 21/32 | FAIL. Broader exposure improved transfer; two remaining rejecting bits had unseen positive values. |
| 4 | 8192 | 64/64 | 32/32 | PASS at this typed one-read scope. Byte coverage corrected the demonstrated exposure gap. |

The routing correction uses the actual exported hard compatibility prediction in a balanced squared-error objective. Its backward chart is deliberately biased: the active unary cell receives a hard residual scaled by1/p through a log-multilinear conjunction, rather than multiplying288 attenuating gradients. It is not a derivative of hard argmax. Adam updates offline floating-point parameters; export uses the unchanged0.5 threshold. Final configuration:256 codec epochs,800 routing epochs, learning rate0.01 and600second fit ceiling. Hard training exactness selects among epoch1 and fixed25-epoch checkpoints, earliest tie. Attempt4 selected epoch150, completed the full configured dose, and skipped zero no-useful-read updates.

Final training uses252 byte symbols, excluding the four development key symbols EFGH, with four cyclic partner offsets and1008 ordered query keys. Each key occupies all four target positions; answer/slot counts remain balanced. These are structural byte-level combinations, not prose examples. Exposure was broadened after training-only feature-coverage diagnostics explained development rejection. Consequently this result supports open-development transfer, not an independent final qualification.

## Actual generated behavior and controls

| Final artifact/control | Training exact | Development exact | Interpretation |
| --- | ---: | ---: | --- |
| Full | 8192/8192 | 64/64 | Correct byte followed by EOS; source selection also perfect. |
| Same codec, uniform routing | 2048/8192 | 16/64 | Learned routing adds48 development successes. |
| ReadDisabled | 0/8192 | 0/64 | Disable selected payload access; key scoring still executes. There is no ingestion recurrence here. |
| QueryReversed | 0/8192 | 0/64 | Changed ordered query changes the answer relative to the original target. |
| PhasesDisabled | 8122/8192 | 64/64 | Phase removal loses70 training answers, but none on this development panel. No unique zeta advantage established. |
| DiagonalOnly | 8192/8192 | 64/64 | Cross-lane packets are unnecessary on this simple task; no all-pairs advantage established. |

For example, query EG reads h from record EG and emits h then EOS. Changing that record to d while moving h to a distractor changes the output to d then EOS, with the query and keys unchanged. All32 paired families pass this intervention. Final development retains every success of each earlier attempt, adding50,48 and22 respectively.

Candidate SHA256: 9901cc50d36d32b4c41f24db2c5f3af41b7cb1f2eaa77ff6e7dcde339e66a582. Retained normal model15baec48 is unchanged. Final held-out evaluation: **NOT_RUN**. General conversation, prose, reasoning, coding, recurrent language learning and model replacement: **not established by this gate**.

## Verification, preservation and resources

Nine focused relational tests pass, with the explicit fit entry point ignored during the unit run and executed separately. Eleven retained Hamming-policy tests pass, with its prior report entry point ignored. Checks cover arithmetic gradients, Boolean export, malformed artifacts, complete feature reachability, relative inverses, causal/data balance and leakage. Each of four separate report roots was exclusively claimed, sealed and Rust-verified on completion. Postflight verifies file sets/sizes and records SHA256 identities:44 new report files and1006 files across30 prior sealed roots. Original dirty checkout statuses/HEADs, retained model, prior source receipts and binaries are preserved. No historical fit/report rerun or cleanup occurred.

Before execution, the complete step reserved3600000ms model/build work,300000ms engineering commands,3hours wall time,6GiB RAM,768MiB new storage and a128MiB storage stop margin, with two build threads and one model process. Standing owner authorization expanded the parent model ceiling by3600000ms and storage by1GiB; no external spending. Actual model/build/tests/fit/diagnosis charge is762866ms, including232654ms for a stopped overbroad initial compile and all corrections. Shared ledger is124493590/132950000ms; parent model charge4743017/7710000ms. The final fit/report took44.960seconds. Final storage, engineering and delivery receipts live in the local handoff below. Successful CI compatibility acknowledgements are not test execution.

[Machine-readable evidence](evidence/native_geometric_relational_attention_973.json) records exact reports, frozen sources, binaries, individual comparisons and preservation. Local complete evidence remains at /Users/casey.allard/uor-r4-worktrees/initial-previous-intent/.uor-handoff/2026-09-12-codex-v7/shared-core-first-step/relational-attention-1/; resource projection, per-attempt projections, command receipts, verification.json and resource-final.json share that directory. Original sealed report roots are never rewritten.

## Next responsibility and harmonic research

Continue H1 by making the first selected value determine a later learned query/state update, then a second actual read and byte/EOS response. Recompute downstream reads after changed early selections. Freeze a small dependent-read development task and update-disabled/restricted-context interventions before fitting; retain this first-learning artifact as a control. A hardcoded pointer-following fixture would not establish learned query construction. General prose and accumulated replacement controls come afterward. No broad research restart or V3–V7 replay is needed to begin that implementation.

The owner's spherical-harmonic recursion suggestion is recorded as a later research candidate. Three-term polynomial recurrences and trigonometric identities can support finite angular representations; spherical harmonics provide a concrete angular basis ([NIST recurrence reference](https://dlmf.nist.gov/18.9), [NIST spherical harmonics](https://dlmf.nist.gov/14.30)). A future mechanism must specify retained orientation/fiber, exact versus quantized identities, useful learned interactions and legal serving operations. Neither mathematical recurrence nor a harmonic coefficient contraction is automatically the model's learned recurrent attention. No harmonic layer was added in this attempt.
