# Current-version source refinement — rejected candidate

September 9, 2026. References #973. **Retain owner/revision artifact `6f5ab4f3`; do not promote `aa1bc159`.** The [machine-readable evidence](evidence/native_geometric_current_version_read_973.json) binds source, data, artifacts, actual outputs and resource receipts. This is a bounded implementation and negative experimental result, not a model capability upgrade.

## Problem and implementation

The retained model correctly records the reverse-first fact `Dusk Ridge holds selvi` and the subsequent revision `selvi now in Copper Vale`, but its recent source router can select historical Ridge. After recent-window eviction, the same stored records produce Copper Vale through the persistent reader.

A diagnostic control allowing all recent persistent reads improves some answers but regresses a correct forward answer and changes historical/raw queries. It is retained explicitly as `CurrentRelationReadAll`, not the default. No global persistent-first dispatch was promoted.

The optional `current_source` artifact component adds exact current-versus-ancestor occurrence metadata joined to the owner's neighboring lexical-prime addresses. Both current and historical candidates remain admitted. Only 28 new signed-H4 source transform entries are fitted; two finish with nonidentity roots. All old source parameters, dictionaries, geometry, writer, typed field composition, emission and numeric operators remain frozen. The feature extraction follows at most 16 directory entries and 16 strictly decreasing previous links, then at most eight query positions. Ambiguous, conflict and assertion-only associations receive no hint. Exact occurrence equality is separate from spelling equality.

The shared geometric router still makes the source/action choice using integer/table operations; this adds no serving matrix products, transformer backbone, provider response, phrase parser, global word exclusion or query-owner substitution. Prime context addresses index lexical identities; they are not asserted to be semantic distances. Existing fixed zeta/H4/orientation and paired geometry remain artifact-bound and unchanged. Their comparative semantic or energy advantage is not measured here.

Offline Rust preparation uses exact parent source/action preservation or a unique positive current-revision choice from the frozen learned persistent reader. Expected response strings exist only in the evaluation driver. All 1,117 documents are receipted; deduplication uses complete ordered feature/action/label frames. Fitting reaches 70/70 distinct decisions, from 68/70. Fitted decisions are not acceptance evidence on their own.

## Actual behavior and rejection

The six targeted generated responses pass 6/6, and 1,111/1,111 inherited output/EOS, first-source/field decision and stored-record comparisons remain exact. The two new controls restore the parent on all 12 target comparisons. The original construction report's `current_commits` field counted pending selections, not independently observed commits; the driver now calls it `current_selections`, and the correction is recorded without rewriting the original receipt. Three known forward writer negatives remain unchanged.

Structural stress then exposes a regression:

`Record: Dusk Ridge holds selvi. selvi now in Copper Vale. selvi now in Amber Field. Where is selvi? Answer:`

| Model | Actual complete output |
|---|---|
| Retained parent | ` Amber Field.\n` |
| Rejected candidate | ` Copper Vale.\n` |

Both models have identical stored records: record 1 asserts selvi at Dusk Ridge; record 2 revises selvi to Copper Vale with previous=1; record 3 incorrectly asserts **now** at Amber Field with previous=0. The parent's recent source choice happens to return the requested newest location despite this write error. The candidate reinforces the directory's stale current selvi value. Thus correct directory membership is not sufficient evidence that a write represented the latest statement correctly. Padding makes both models return the stale value.

The stress panel scores 12/16 candidate versus 9/16 parent, including four improvements and **one lost previously correct response**. The higher aggregate score does not justify admission. Two-owner and same-spelling unbound-occurrence contrasts are retained in the report; writer records are unchanged throughout. No fresh identities were drawn because no candidate was selected.

## Validation and limits

The changed release core/example path and API checker compile offline. Five focused current-source tests, the integer-kernel source guard and one actual allocation/checkpoint test pass (7 executed tests total). The actual artifact allocation check covers input/output checkpoints, precise source endpoints, stored revision identity, recent-window eviction, independent sum followup, parent controls and zero-allocation inference. The focused native API runner passes 27 checks covering base identity/load-save, generated API/direct parity, checkpoint identity isolation, chunked input and the current-source extension's actual outputs, exact records, controls and malformed-artifact rejection. Its report explicitly identifies this scope. The full historical recovery runner and prepared broader replay campaign are **NOT_RUN** this cycle; no prior test counts are relabelled as new executions.

The owner requested conclusion after an assistant capacity message. No further fit or implementation cycle was started. General prose, arbitrary instructions, general reasoning/coding, alpha/frontier capability and complete-path energy advantage remain unqualified; new-artifact browser/WASM/HTTP is NOT_RUN. The retained parent is unchanged. Optional experimental code is not a recommendation to serve the rejected artifact.

## Preservation, resources and next step

Retained parent: `/Users/casey.allard/uor-r4/.uor-models/native-typed-value-2026-09-05/owner-revision/model.json`, SHA256 `416fda343c34c68c3aa73a6ca82ae672c93f7a5383f9872b9ead2534fd22cab5`, 13966704 bytes. Rejected candidate: `/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-current-version-read/fit/model.json`, SHA256 `72965f76d118cfe73ceef31eac850ec3b9d52ec7e929c0f68da5632007a4c022`, 14239933 bytes. Preserve both, the failed overlong diagnostic, all earlier rejected artifacts and every prior charge. No unique deletion or paid external compute occurred.

This cycle charged 461.293 model seconds and 460.572 engineering-command seconds. Cumulative model usage is 27800.220 / 31350.000 seconds, with the previous 196.620-second reservation preserved. The necessary +3,000-second cumulative extension and full 3,600-second cycle projection were recorded before use. Limits remain one model process, two threads, 4 GiB model RAM, 6 GiB build RAM, 512 configured context tokens and 96 output tokens; inherited long histories permit 8,192 input tokens. New storage was projected at 1 GiB without extending the 39158181888-byte storage cap; accounted usage at the immutable evidence snapshot is 37265051648 bytes, leaving 1758912512 bytes after the 128 MiB margin. Small metadata/document/Git commands are outside the engineering stopwatch. The immutable `resources-at-evidence.json` is hash-bound here; mutable `resources.json` records later delivery metadata growth.

Static inspection supplies a narrower diagnostic lead: Amber is already dictionary prime 2, but its corresponding wrong-`now` Assert correction is absent; otherwise matching rows for unknown prime 0 and Copper prime 31 have weight -3. The hash-bound `writer-gap-inspection.json` records exact integer keys and JSON pointers. This is a parameter-coverage difference, not measured proof of scorer causality. Capture actual competing scores at the completed Amber boundary and compare the same prefix with Copper and an unknown spelling before choosing the correction. Do not insert a word-specific serving exception.

**Next: repair the newest owner/revision write in consecutive facts**, including changed complete spellings, reversed initial facts, literal-now owners, conflicts and retained dependency revisions. Inspect the actual competing writer features and their collision before fitting. Preserve the parent's correct recent answer as well as the intended revision chain. Revisit current-version source learning only after the writer supplies a correct latest relation; do not make the directory authoritative through a blanket precedence rule. #973 remains open.
