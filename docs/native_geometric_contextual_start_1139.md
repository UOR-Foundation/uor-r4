# Contextual lexical phrase-start binding — #1139

## Implementation and projection — 2026-09-07

The retained reverse-span artifact `321e990f` admits correct phrase values but
can include introductory words. The preserved shape-only `bb6b8ba4` candidate
reaches 8/8 construction and 6/8 open answers and remains unpromoted; its fresh
panel remains unopened. The [direction review](integration/model-direction-2026-09.md)
identifies a stronger limitation than an inadequate fit: all-lowercase admitted
starts can share all seven existing shape features.

This continuation exposes bounded exact lexical/prime and writer-role context
to the existing signed-H4 start learner. It reuses the construction-prompt-only
registry and exact predecessor carrier from the
[contextual span mechanism](native_geometric_span_context_1139.md). Prime
identities address learned codes; their magnitudes are not semantic distances.
The writer endpoint, admitted candidates, exact payload transport, immutable
versions and complete parent parameters stay fixed. A separate optional registry
binds the new feature law while older artifacts retain their shape-only law.
The matched context-disabled model retains the registry, feature keys, lookup
work, fitted shape codes, configuration and scoring parameters. It replaces only
lexical/role code roots with the geometric identity. This isolates the learned
contextual contribution without changing the candidate or feature population.

The complete local projection is recorded before execution in
`.uor-models/native-typed-value-2026-09-05/contextual-start-projection.json`.
Expected model work is 110 seconds, capped at 180 seconds including preparation,
fit, comparisons, controls, preservation, fresh-after-selection, artifact/CLI
checks and a correction reserve. Expected engineering command work is 750
seconds, capped at 1,200 seconds. New storage is capped at 256 MiB: 32 MiB for
models/control artifacts, 208 MiB compiler growth/transients and 16 MiB reports.
One model process, one build job and a 4 GiB model RSS cap apply. Context is
512 tokens, relation records and span words are each bounded at 16, payloads at
28 bytes, and responses at 32 tokens. The contextual fit permits 64 documents,
256 feature codes, four passes, twelve proposals and five seconds in the learner.

Standing owner authorization supplies a necessary 180-second cumulative model
extension, from 4,410 to 4,590 seconds, preserving 4,387.536 seconds charged.
A 320 MiB storage extension raises the inherited broad ceiling from
7,734,296,576 to 8,069,840,896 bytes and the tighter growth allowance by the same
increment. All inherited charges and the 128 MiB stop margin remain enforced.
The initial refreshed conservative storage sample is 7,529,529,344 bytes.
No unique material is deleted and no external paid compute is used.

Construction and open development compare complete generated bytes and EOS.
Prior generated-answer and state preservation use the existing Rust driver.
Fresh execution follows a saved selection decision only. Opened errors are
development feedback; neither perfect fitting nor a protected merge establishes
general phrase segmentation, sustained prose, reasoning or laptop efficiency.
Wider separator coverage remains a separate representation limitation.

## Preparation stop and explicit eligibility correction

The first combined command used 28.722 model seconds and stopped before fitting:
`no completed reverse write in context-start-fit/2/0`. The parent generated
` river holds selra.\n` for both `the report says quiet river holds selra` and
the bare `quiet river holds selra`. Both have zero relation writes. The singleton
`quiet` cases do write. The original 32-case construction report and error remain
in `contextual-start-angular/`; no candidate was produced by that attempt.

The corrected data preparation admits the 30 supplied examples with existing
reverse writes. These two unsupported multiword examples remain separately
reported upstream writer diagnostics and remain in the full 32-example response
comparison. They are excluded by the already observed absence of the required
writer event, not by the successor's generated correctness. This restricts the
start-selector decision to supported writes; it does not repair or qualify the
upstream writer. No admission, endpoint or payload rule is changed. All elapsed
work remains charged against the same cycle and cumulative limits. The next
attempt uses a new output directory and preserves the stopped attempt intact.

## First candidate — lexical memorization and transfer negative

Candidate `cc766ed9358c48d6acbc1165daa6f3a1ba80913a7ac80fec6b392883de4efb6c`
fits 30/30 frames with zero hinge. Complete supported construction improves
7/30 to 30/30, and all supplied construction is 30/32. Open development is
7/12 versus parent 3/12 and contextual-root-disabled 6/12. The disabled arm
scores 22/30 supported construction. The two missing-write cases remain failed.

All 663 retained construction answers and accepted forward/reverse, long-window,
dependent, source-span and other selected preservation pass. The driver's false
`preservation` aggregate comes exclusively from 5/8 on the earlier unqualified
phrase-start open challenge. It does not mean accepted model behavior regressed.
Selection fails; fresh cases remain `NOT_RUN_SELECTION_FAILED`. The artifact,
all generated bytes and the complete source/fit/control reports are preserved
under `contextual-start-angular-v2/`.

The registry contains 38 words and the learner uses 238 codes. Only unary prime
features and five shape rows become nonidentity; all ordered-pair and writer-role
codes remain identity. Repeated forms of one owner/value world make payloads
such as `fine`, `water`, `meadow` and `river` recurrent. The learner can fit those
identities without learning boundary transfer to new payloads such as `soft clay`.
The gain therefore does not establish learned compositional role binding.

The next correction requires prompt-word recurrence across distinct exact owners
selected by the unchanged parent writer. Repeated forms of one owner no longer
admit its payload vocabulary. It changes offline registry preparation, keeps
the runtime feature law fixed, and makes unknown-payload behavior part of fitting.
No opened evaluation case enters construction. Before this correction, the
recorded complete projection adds 120 seconds to the cumulative ceiling
(4,590 to 4,710), preserves 4,507.795 seconds charged, raises this cycle's model
cap from 180 to 300 seconds and engineering cap from 1,200 to 1,800 seconds.
It projects 150 further model seconds and 750 engineering seconds. The original
256 MiB growth cap and all machine/stop-margin limits remain unchanged.

One inference diagnostic in v2 incorrectly expected a latest-value answer after
two conflicting ordinary assertions. All three arms correctly emitted
` Unknown.\n` under the existing conflict contract. Its frozen output remains
unchanged; the driver now labels the case `conflicting-assertions` with the
correct oracle. It was outside selection and is not a model failure.

## Cross-owner registry — narrower transfer negative

Candidate `0e1914c4e3e8fa8f9049302a474267e40b80d07618f292514d8e982569139c81`
uses nine recurring prompt words across distinct exact parent-selected owners,
89 codes and the same 30 supported frames. It reaches 30/30 supported, 30/32
supplied and 10/12 open; parent open is 3/12 and identity-context control 6/12.
The control reaches 21/30 construction. All accepted preservation passes; the
earlier unqualified start challenge is 6/8. The remaining open errors include
`report notes notes` and `tells Silver` in singleton values. Contextual pair and
writer-role roots remain identity. Selection fails and fresh remains unopened.
The 92.172-second run and its exact source are preserved separately in
`contextual-start-cross-owner/` and `contextual-start-source-v3/`.

## Crossed construction — open fit, earlier challenge negative

Twelve newly authored construction examples cross `log tells`, `report notes`
and `A ledger says` with singleton/multiword and introduced/bare values under
new owners. These are explicitly informed by open development feedback. The
original 32 supplied examples, the open 12 and the unexecuted fresh 12 remain
unchanged. The runtime and feature law do not change. The pre-use projection
adds 120 seconds to the cumulative ceiling (4,710 to 4,830), preserving
4,599.967 seconds charged, and raises this cycle's model cap to 420 seconds.
It projects 160 further model seconds and 90 engineering seconds; storage and
all other bounds remain fixed.

Candidate `af997b1737b3c753cda43176f588995864573fd773804354b12da4f2e32edd5c`
reaches 42/42 supported construction, 42/44 supplied, 30/32 original and 12/12
open. Parent supported is 10/42 and open 3/12; identity-context control is
28/42 and 6/12. It learns four ordered predecessor/first pair codes in addition
to unary/context and shape codes; this is not evidence for the unused writer-role
features. All accepted preservation passes, but the earlier unqualified start
challenge is 6/8: `Report notes Cobalt Field` and `Report notes Cobalt` retain
`notes`. The candidate therefore remains unpromoted and fresh remains unopened.
The 90.584-second run, exact generated bytes and source-v4 remain preserved.

The next correction adds eight construction examples with `Record notes` and
`Memo notes` under two new owners and values. Each introductory word occurs
under one owner, so it remains unknown in the cross-owner registry. This tests
transfer through the existing title shape and exact `notes` context, without
adding the failing `Report` prompts to construction. The complete 160-second
model and 60-second engineering projection was recorded before use. It adds
120 seconds to the cumulative ceiling (4,830 to 4,950), retaining 4,690.551
seconds charged, and raises this cycle's model cap from 420 to 540 seconds.
The 1,800-second engineering cap, 256 MiB growth cap, broad storage ceiling,
4 GiB RSS cap, one process/build job and 128 MiB stop margin remain unchanged.

## Selected result — bounded contextual phrase binding

Retain `50dc0d23175183272e557a47f6ad8cb5f538f30b5185f1b372c0c3bc010b8918`
at the scope below. The final candidate has ten registry words and 100 feature
codes. It reaches 50/50 supported construction versus parent 12/50 and the
contextual-root-disabled control 38/50. Complete supplied construction is 50/52;
the original subset remains 30/32 because the two upstream no-write cases are
unchanged. Open development reaches 12/12 versus parent 3/12 and control 7/12.
The earlier phrase-start challenge passes 8/8. Only after the saved selection
decision, all twelve fresh examples pass complete bytes and EOS, using new
owners/payloads and 96 padding sentences to evict the source before the query.
The older shape candidate's separate eight fresh cases remain unopened.

All 663 retained construction answers, earlier forward/reverse turns,
28/28 long-window and 48/48 dependent reads, 14/14, 6/6 and 9/9 source-span
panels and other selected preservation pass. Exact parent parameters and
artifact roundtrip equality pass. Four ordered predecessor/first pair codes
become nonidentity, alongside five unary lexical and nine shape codes. The
first/next pair and writer-role code roots remain identity. Removing all
contextual roots establishes a contextual contribution; it does not separately
establish that each pair or role feature improves prediction. The 41 ms learner
performs 10,783 proposals, reaches 50/50 fitted frames with hinge 1 and does not
hit its five-second limit. That residual margin does not change the observed
complete-answer result. Total guarded fit/comparison work is 90.258 seconds.

This is transfer of familiar cue/role wording to new exact values and owners,
including eviction. Open data informed construction design and is not fresh
held-out evidence. Broad phrase parsing, unknown cue generalization, arbitrary
separators, sustained prose, general reasoning/coding and energy advantage remain
unqualified. Retain all earlier negative candidates and source snapshots.

The remaining writer error is upstream: at `quiet river holds selra`, the
writer compares a reverse endpoint `river` with `quiet` as unmasked preceding
context. The stored action-1 feature `(holds-prime=37, predecessor=0)` contributes
+3 for singleton `quiet`; `(37, quiet-prime=73)` is absent and contributes 0 for
multiword `quiet river`. Other changed action-1 lexical coefficients are zero.
The preserved parent trace shows full scoring and no record writes, not skipped
admission-cache work. This source/weight-derived diagnosis has not been verified
by an intervention. A bounded, parent-preserving writer refinement with new
cross-owner payload/context contrasts is the next direct repair; rebuild the
writer NoWrite admission cache if its operator changes. Do not special-case
`quiet`, erase all context or refit phrase starts for missing writes. Reusable
shared state transitions and causal compositional emission remain the following
architectural work under #1140; #1139 and #973 remain open.

## Actual artifact, interface and resource checks

The pinned Rust release example, root `r4` CLI, core tests and allocation-test
binary compiled offline with one build job. Five relation-start tests cover
shape aliases and construction-owner registry independence; five relation-span
tests preserve state/version/endpoint contracts. The integer-kernel source guard
and native architecture-policy check pass. No full workspace, Clippy, WASM,
HTTP, Studio, fuzz, no_std or energy campaign is claimed.

The final artifact passes invalid-root/parent/registry rejection, control
roundtrip, five short/evicted/singleton outputs, per-step checkpoint equality and
zero allocations through both ingestion and emission. Loading is 14.651512584 s.
Five encoding samples are 0.006666–0.043833 ms; ingestion plus begin-response is
0.180833–0.486375 ms. Across 67 uncached predict/observe samples, median is
541 ns and maximum 175,417 ns. The warm samples exclude model loading, encoding,
ingestion and checkpoint host work. Whole-generation per-case timings and work
counters are also retained. These small samples are not energy measurements or
broad performance guarantees. Root CLI reads the same artifact and emits
` Cobalt Field.\n` with `end_of_document` for
`Report notes Cobalt Field holds vorin. Where is vorin? Answer:`.

The guarded actual-artifact test costs 60.148 model seconds and CLI invocation
15.078 seconds, including loading/validation. This exceeds the final 35-second
artifact/CLI estimate but remains inside the previously recorded full-cycle
and cumulative ceilings; no extension is applied retroactively. Total model
work for this continuation is 468.499 seconds against its 540-second cap.
Cumulative use is 4,856.035/4,950 seconds, leaving 93.965 seconds. All prior
charges and all pre-use extension projections remain in the shared ledger and
receipts. The final model is 11,645,070 bytes. Peak sampled model RSS is
1,529,954,304 bytes, below 4 GiB. The final CLI storage sample is
7,629,463,552 bytes against the broad 8,069,840,896-byte ceiling; the tighter
inherited allowance leaves 252,743,680 bytes before its stop. The 128 MiB broad
stop margin and all unique material remain preserved. These are periodic
conservative samples, not exact transient peaks or unique APFS extent totals.

The [evidence](evidence/native_geometric_contextual_start_1139.json) binds exact
source snapshots, artifacts, complete comparison reports, checked commands and
resource totals. Protected queue acknowledgements are transport only; actual
local checks above supply the verification. This is a partial #1139 delivery,
not closure of broader contextual language or shared-transition responsibilities.

The final backward-compatibility check loads preserved shape-only `bb6b8ba4`
through the changed root CLI and reproduces ` Amber Meadow.\n` with EOS on its
already opened construction prompt. Its 14.708 seconds remain charged within
the existing projection/cap. Final model use is therefore **483.207 seconds**
for this cycle and **4,870.743/4,950 seconds cumulative**, leaving 79.257 seconds.
Engineering commands total **1,280.628 seconds** against the 1,800-second cap;
this is command time, not total conversational wall time. Final formatting,
claim wording, architecture-policy and diff-whitespace checks pass. This
compatibility check does not promote the old failed artifact or open its fresh
panel. The last guarded storage sample is 7,629,496,320 bytes, leaving
252,710,912 bytes before the tighter inherited stop; later metadata snapshots
are recorded separately in the evidence.
