# Immediate previous-version selection — partial result, #973

**Retain parent `4d81c3b0`; do not promote candidate `f76e7452`.** The post-selection fresh gate failed. This cycle adds an optional learned previous-version read to the native geometric model. The shared signed-H4 selector chooses between Base and structurally valid previous-record references. Expected answer bytes are evaluation data, never fitting or serving inputs. General temporal language, historical owner-first composition, sustained prose, general reasoning/program synthesis, alpha/frontier capability and complete-path energy advantage remain unqualified.

## Observed bottleneck

Retained `4d81c3b0` stores the exact history but does not offer old records to its persistent reader, which enumerates the current directory. Its recent source selector can select an old terminal anchor without reconstructing the stored span: `Where was selvi before?` emits ` Ridge holds selvi.\n` after Dusk Ridge → Copper Vale. `What was the previous location of selvi?` enters an unrelated dependent-read path and emits ` Unknown.\n`. Merely admitting all stored records would give same-owner versions identical old reader features. The existing retained-span and source-version machinery can already emit a specifically selected old record.

## Implementation

The new `historical_read` artifact block binds a shared `SourceRouting` instance to the complete frozen parent and clones the inherited lexical-prime dictionary exactly. Ordered query prime and pair features join exact owner/query relation features. Offline fitting learns signed-H4 code transports, landmarks and biases through the existing discrete learner. Runtime ranks Base against a copy of an immediate predecessor; Base defers to the retained dependent/persistent/recent dispatch; scope 2 clips the dependent reader's contextual feature view at the current input boundary. This is selection of an exact typed read operation, with no expert network, serving phrase parser, provider, transformer or matrix-product operation.

Historical candidates require a live current Revise record, a descending live previous link, exact owner agreement and nonconflicting records. Admission does not depend on question spelling. Both the retained occurrence endpoints and record identity survive through the existing space-prefixed Prepare action, retained-span byte reader and source commitment. The operation follows one link; it does not mean oldest version or an arbitrary temporal offset. The writer, geometry, fixed zeta identities, existing source-reader parameters, shared arithmetic and emission operators remain frozen. The versioned query view below intentionally changes which contextual features the dependent selector can inspect after an input boundary.

Outer artifact validation reconstructs and validates the entire `4d81c3b0` parent before checking the new block. It validates the inherited dictionary, H4 root/landmark domain, angular ranks, feature namespaces, configuration and training-document receipts. Selection/evidence metadata separately bind Rust source hashes. `without_historical_read` reconstructs the parent; `HistoricalReadDisabled` removes both the new selection and the scope-2 dependent query view, restoring the parent path.

## Construction and measured development

The 1,264-document construction includes 53 historical targets and 1,211 Base preservation cases. Five earlier unqualified historical cases are explicitly retargeted; raw Copy, Repeat-first and absent-owner cases remain preservation cases. Forty-eight new targets vary two owners, fact direction, eviction, two revisions, competing chains and the two authored question forms. Training examples contain only ID, prompt and an optional current-record ID whose exact previous link supplies the target. Expected output strings are serialized separately for evaluation.

Candidate `a3c3093c` satisfies 86/86 distinct fitting frames versus 77 initially, using 185 codes. Actual development generation answers 53/53 targets versus parent 0/53 and selects every exact immediate previous record. All 1,211 untargeted output/EOS/applicable first-decision/record comparisons remain exact. Disabling the new read restores parent behavior on all 53 target cases. These preserved rows are not 1,211 independent correctness qualifications. Selected causal checkpoint fields are replayed by the evaluation driver; the separate artifact test checks complete checkpoint state excluding work counters.

With Dusk Ridge → Copper Vale → Amber Field, both authored historical forms now emit ` Copper Vale.\n`. The one-revision counterpart emits ` Dusk Ridge.\n`. Current records are unchanged during generation.

## Rejected first candidate and query-boundary correction

The single-prompt results above did not qualify multi-turn use. The actual checkpoint test rejected `a3c3093c`: after correctly returning Dusk Ridge for a historical question, a bare current-location follow-up returned Dusk Ridge instead of Copper Vale. Diagnostic replay covered eight sequences. Following the before form, disabling the new historical read recovered the current answer; following the previous-location form, both full and disabled execution returned Unknown through the inherited dependent reader. The prior query's words remained in the recent lexical window across the caller-supplied input boundary.

Candidate `f76e7452` adds artifact-bound `query_scope: 2`. The historical and dependent selectors use a bounded local feature view containing words at or after the existing input-turn boundary. Their source windows, source endpoints, committed records and stored spans remain intact. The inherited dependent parameters are unchanged. Scope 1 is the omitted legacy default, so the rejected artifact keeps its original bytes and failure. `HistoricalReadDisabled` removes both new effects and reconstructs the parent's feature law.

Refitting the unchanged construction data produces the same 86/86 frame result and the same learned router weights; the artifact identity changes with the query-scope law. The corrected candidate answers all eight historical-to-current follow-ups correctly, including reversed facts, evicted source words, competing owners and two revisions. The separate checkpoint test still checks real current answers and subsequent arithmetic. These development sequences are not held-out transfer evidence.

## Fresh transfer and retention decision

Construction passes 53/53 historical answers versus parent 0/53, with all exact previous-record selections and 1,211/1,211 untargeted comparisons preserved. Post-selection fresh transfer selects the exact previous record in 64/64 cases, but only 44/64 answers contain the complete value; the parent answers 0/64 correctly. All 28 untargeted fresh comparisons are preserved, and disabling the new operation restores parent behavior on all 53 construction and 64 fresh targets. The predeclared fresh gate failed; it was not relaxed.

All 20 failures occur in unfamiliar lowercase reversed values. For `Record: eoqn rgoo holds eddwg. eddwg now in bbew ezye.`, the initial old record stores only `rgoo` with no span, while the revision stores the complete `bbew ezye`. Historical output ` rgoo.\n` accurately copies the selected record. All 20 initial relation states equal the parent's. Selection and complete value retention are separate measurements.

The source path is `relation_span::reverse_candidates` → `relation_start::select` → record commitment. The reverse-start context dictionary omits the unfamiliar value words and Record, so their context prime is zero. Shape/neighbor/singleton features still distinguish full and singleton candidates; a full feature collision has not been shown. The unknown reverse and successful forward edges share the same space-only edge feature law. This points toward start ranking, but admission and score traces remain NOT_RUN. No dictionary memorization, answer reconstruction patch, refit or second fresh draw was used after this result.

The candidate is preserved at `/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-historical-selection/retry/fit/model.json`, 14634216 bytes, SHA256 `0a4a5c0decdb4cd2d9a127146994773fa661cd83ecdc828ccf19f141f715de64`. The retained parent stays at `.uor-models/native-typed-value-2026-09-05/query-owner/model.json` with its unchanged identity. The [evidence](evidence/native_geometric_historical_selection_973.json) binds the candidate, parent, source hashes, positive and negative results and resource receipts.

## Executed checks and resources

All 14 earlier behavior/control reports have zero checked differences. The 12 retained multi-turn histories preserve outputs and applicable record/source identities; the earlier 24-case word/Rust panel and all 72 query-owner cases are preserved. Copy→Add and action emission each pass 24/24 ordinary and 24/24 stress cases. Actual generated Rust outputs compile and execute. The scoped native API passes 106/106 historical-answer and checkpoint/independent-sum checks. All 31 focused tests pass, including eight historical→current→arithmetic sequences with 1,636 input and 274 output checkpoint positions, and byte-exact replay of the rejected legacy artifact's negative behavior. Formatting and the integer-kernel source guard pass. These checks do not override the failed fresh text gate.

Full older nested API scope, new HTTP/browser/WASM integration, allocation and complete-path energy measurements remain NOT_RUN. GitHub's five compatibility statuses acknowledge queue compatibility and execute no formatting, Clippy or tests.

The immutable evidence snapshot records 1943.224 model seconds and 696.050 engineering-command seconds for this cycle, including failed attempts. Cumulative model use is 34174.720 / 36150.000 seconds, with the prior 196.620-second reservation preserved. Before use, the standing authorization supplied a 2,100-second model increment and a 512 MiB storage increment. The complete projection was 3,600 model seconds, 1,800 engineering-command seconds, one model process/two threads, 4 GiB model/6 GiB build RAM and 1 GiB new storage. New inputs use 512 tokens/4,096 bytes and 96 output tokens; inherited population bounds remain 8,192 tokens/65,536 bytes with five-turn histories and 1,056 padding tokens per turn. The retry build projection increased from 400 to 650 seconds within the overall unchanged allowance after observing API dependency rebuilding. Accounted storage is 38741913600 / 39695052800 bytes, leaving 818921472 bytes after the 128 MiB stop margin. No new model was promoted, no unique material was deleted and no paid external compute was used. Final documentation/Git closure charges append to the mutable local resource receipt.

## Next action

Next under #973: inspect the admitted reverse phrase starts and their geometric scores, then improve the existing reverse-start training for unfamiliar lowercase multiword values if the full candidate is admitted. Balance singleton and introductory-context controls, preserve exact endpoints and all retained behavior, and use a new separately frozen transfer draw. Complete stored-value recovery takes priority over initial-versus-previous selection or historical owner-first emission.

The operation follows one previous link; it does not select the oldest version or an arbitrary temporal offset. Earlier Repeat-first failures remain unqualified. General temporal language, historical owner-first composition and sustained prose remain unfinished.
