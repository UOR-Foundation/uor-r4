# Shared Copy→Add ordering and exact copied-record dependencies — #1140

## Scope and observed parent failure

This cycle addresses the opposite order from retained `5ed24f4e`: first Copy the original total or the extra literal, then optionally Add the other value to that newly committed Copy. It uses the existing shared geometric role and continuation routers. The lexical emitter, fixed geometry and proposal admission remain unchanged. The [evidence receipt](evidence/native_geometric_copy_add_1140.json) binds the decision, source, artifact, actual outputs and resources.

Actual history computes `13 + 4 → 17`, then the next query introduces three extra coins. The authored targets are:

| Request | Plain output | Exact ordered writes |
|---|---|---|
| Copy original only | `17.\n` | Copy write 4 from `[2,2]` |
| Copy original, Add extra to copied result | `17.\n20.\n` | Copy `[2,2]`, then Add write 5 from `[3,4]` |
| Copy extra only | `3.\n` | Copy write 4 from `[3,3]` |
| Copy extra, Add original to copied result | `3.\n20.\n` | Copy `[3,3]`, then Add write 5 from `[2,4]` |

Sentence and Rust-equality instructions use the same fixed emitter. For example, Copy-extra→Add-original targets `3 is 3.\n20 is 17 plus 3.\n` or `3 == 3\n20 == 17 + 3\n`. IDs above label the authored history only; serving receives no target IDs or answer text.

The retained parent passes 0/12 complete diagnostic responses. Most requests begin with Add 20 instead of Copy. Copy-original→Add-extra chooses Copy 17 and formats it correctly, then stops. All three output styles show the same selection/continuation pattern. The actual 16-word query window retains both Copy and Add in these requests, with distinct original/extra positions. No context-width increase is justified by this diagnostic.

The preliminary diagnostic was compiled while scaffolding work was coordinated, leaving its exact concurrent source binding uncertain. A second run on the frozen source reproduces all twelve actual outputs and write/read identities exactly. Both runs and charges remain preserved. One diagnostic compile failed on a mistyped control enum and was corrected before execution; this is build evidence, not a model failure.

## Native implementation and supervised boundary

[shared_operator_refinement.rs](../crates/uor-r4-core/src/native_geometric/shared_operator_refinement.rs) refines existing components, with a nonexecuting outer provenance witness. Its initial stage reuses `fit_mixed_initial` to learn the exact first Copy and operands while preserving old role preferences. The checkpoint is saved before continuation fitting.

The continuation stage expands the existing query dictionary through exact word identity where needed, preserving prime-feature roots through the established remapping helper. It freely generates the complete first response with subsequent transitions disabled, checks exactly one committed derivation against the first target, then collects the requested next-operation and Stop frames. The labelled next operation executes on actual records, and the fixed lexical emitter generates its suffix. Operator and record labels are explicit offline supervision; no lexical labels are teacher-forced by this trainer. Final complete-response evaluation separately uses model-selected operations.

The existing proposal law already admits both requested Add pairs. It distinguishes the new Copy ID from its source using latest-record flags and ranks, while excluding inappropriate self-alias additions. This cycle changes neither that admission law nor the serving feature representation. Prime values remain exact word identities, not semantic distances. Shared signed-H4 composition and ranking remain the learned execution path; no parser/template, new answer-family head, serving matrix product, transformer or provider response is introduced.

The witness stores the full previous role router and operation block, including its dictionary, plus the exact parent CID. Validation removes the witness, restores both blocks and recursively validates the entire parent. The prior action-emission witness and lexical component remain exact. `continuation_fitted` records that the training stage ran; it is not a quality flag. Staged artifacts report the new API behavior as NOT_APPLICABLE; fitted candidates must execute the actual checks. The matched binding and transition controls restore the corresponding complete parent components.

Preservation preparation runs in Rust and verifies the 459 inherited typed contexts plus 36 accepted action-emission construction cases and their actual reports, yielding 495 contexts. Those 36 cases include their earlier histories; they are not 36 extra post-action independent-turn training contexts. The constructor verifies source follow-ups, and new evaluation independently checks following calculations with fresh literal occurrence IDs. Historical turns are freely generated and compared against saved text during training collection. Preservation-query preferences are captured from the exact reconstructed parent; final saved-answer comparison is a separate evaluation.

The first joint candidate `9365f229` passes the 36 construction, 24 open and 24 reversed/equal-value cases, but is rejected: eight previously correct two-history “Repeat the total” queries choose Add instead of Copy. Its 663/663 retained-construction score does not outweigh those regressions. Rust preparation therefore appends all 32 qualified original-parent chain contexts, including both histories and refresh turns, bringing preservation to 527 contexts. The historical chain reports qualified commutative Add values; preparation accepts only the exact labelled pair or its Add reversal, while Copy remains strict. The first stricter preparation attempt rejected those already-qualified reversals and is preserved. Converted contexts contain no operand targets: fitting captures the original parent’s actual ordered record IDs. The retry starts from the original parent with the same seed and unchanged serving law. All stopped and rejected results remain preserved.

The offline context bound is 640, while retained continuation frames remain capped at 4096. Rejection companions share that cap atomically. They train Stop above substitute proposals when the labelled operation is unavailable; this is explicitly trained candidate-availability robustness. Complete ordered feature alternatives are checked for contradictory labels, and missed fitted frames retain exact scores and features. Absence of a conflict does not prove convergence or separability.

## Evaluation and decision

**Retain `2dc63b2c`**, CID `blake3:2dc63b2c7b7073155fd3c5a8c743eaaace66f409483d86dd390966d4f9fc03f6`, SHA256 `bec859344be857eced857adf6ee360bccbacc81d91282de7eed31bb4aa0e5233`, 13,132,214 bytes.

Construction passes 36/36, open 24/24, reversed/equal-value stress 24/24 and post-selection numeric/name transfers 24/24. Copied-result exclusion preserves the first Copy and learned Stop in every case (ordinary-target correctness is reported separately). Restoring parent binding reduces open correctness to 6/24, restoring parent continuation to 12/24, and disabling action context, exact record reads or learned emission geometry to 8/24 each. All 663 retained answers and earlier panels, including both 16-case history-chain populations, 254 transition replays and 72 exact lexical outputs pass. Thirteen of fourteen historical reports and all fourteen prior action-emission reports match exactly. Eight already-incorrect historical control rows change wrong outputs or identities, with every previously correct row and correctness count preserved. Native API passes 117 checks; 22 unique focused tests, the kernel source guard and five actual artifact allocation/checkpoint checks pass. All 36 actual generated Rust equality lines from open/stress/fresh panels compile and execute.

The final artifact changes only the existing role/continuation blocks plus its nonexecuting provenance witness and identity. The entire lexical component, prior action witness and fixed geometry equal parent 5ed24f4e. Initial and continuation decisions are learned offline from exact labels and actual parent preferences; Copy/Add arithmetic and addressed record reads are deterministic infrastructure. Combined with retained Add→Add and Add→Copy, the result satisfies #1140's declared bounded two-operation, complete-emission, causal-control and executed-Rust acceptance. This completes that milestone at its stated scope, not the broader language-model objective.

The evaluator checks actual history and complete response text, EOS, pending and committed operation/value/operand identities, exact lexical read anchors, per-token checkpoint consistency and following independent calculations using fresh literal records. Construction uses three numeric groups under four operation forms and three output styles (36 cases); open and reversed/equal-value stress each use two groups (24 cases). Equal-value stress uses an original total and extra of 67 while retaining their different identities. Fresh values and names are chosen only after recorded selection. New retry reports use compact JSON serialization with every original field/checkpoint/byte value retained; earlier reports are preserved unchanged.

The copied-result exclusion control retains the actual Copy record but removes it from subsequent operation proposals. Its intervention target is exactly the first Copy followed by Stop, with its exact output, writes and reads preserved. Ordinary-target correctness and intervention correctness are reported separately. Falling back to the original record with the same number is a failure of this control.

This cycle used **2295.009 model seconds** and **397.988 engineering-command seconds**, including rejected candidates, failed preparation, interrupted stress work and all reruns. Cumulative model use is **16448.361 / 17250.000 seconds**. The 1,800-second extension was recorded before use; the later five-minute cycle extension stays within that unchanged cumulative limit. Preserve the 196.620-second reservation, leaving 605.019 seconds. Conservative storage is **35,169,001,472 / 37,010,698,240 bytes**, with the 128 MiB stop margin and 16 MiB metadata reserve preserved. An interrupted stress control exposed underestimated full-checkpoint report storage; a 2 GiB extension was recorded before further use, bringing this cycle's storage ceiling to 3.5 GiB. Compact JSON for subsequent reports preserves all fields. Limits remain one model process, two threads, 512 context tokens, 96 output tokens, 4 GiB model RAM and 6 GiB build RAM. Sampled model/build process-tree peaks are 2,512,551,936 / 2,416,443,392 bytes. No unique deletion or paid external compute occurred. Small metadata/document/Git commands are outside the engineering stopwatch.

## Capability limits and next action

**Next: move to #973's linguistic learner.** Diagnose currently passing nonnumeric fact answers under complete-sentence requests, tracing exact source/span/version identity and shared lexical activation. Source inspection shows that completion currently anchors only to a committed numeral, while word/span answers use a separate copy path. If the correct source is retained but cannot enter shared emission, add a bounded typed word/span anchor and exact-read path through the existing geometric selector. If wording first disrupts source selection, repair that observed boundary. Preserve the numeric emitter and abstention controls; do not add a parallel decoder or sentence template. Arbitrary wording and sustained prose remain unqualified.

The evaluated forms do not qualify arbitrary wording, general prose, generalized reasoning, Rust program synthesis, alpha or complete-path energy savings. The runtime continues to require the updated artifact loader. New-artifact browser/WASM/HTTP remains NOT_RUN; native API results are scoped separately. #1140 is complete at its declared bounded acceptance; #973 and broader capability issues remain open. All earlier artifacts, stopped or rejected candidates, source snapshots, imported research and cumulative charges are preserved.
