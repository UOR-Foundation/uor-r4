# Unchanged lexical-neighbor transfer — #973

**FAIL_UNCHANGED_NEIGHBOR_TRANSFER: 468/492 complete new cases.** Frozen artifact 0175ad86 retains all prior qualified paths, but fails 24 unfamiliar-name continuations. Correct first payloads and rewritten queries localize those failures to second-read compatibility. The report observes one identical query-role key for a name occurrence and an auxiliary occurrence. No model parameter or serving algorithm changes in this step. References #973.

| Novel-name panel | Complete cases |
| --- | ---: |
| Optional query participation | 60/60 |
| Styled valid continuations | 84/96 |
| Predicate anchors | 48/48 |
| Same identity as prefix and required name | 96/96 |
| Styled missing-compatible continuations | 96/96 |
| Styled conflicting continuations | 84/96 |
| Total | 468/492 |

## Environment and controls

The report replaces ten ordinary names bijectively in the previous 300 authored cases, preserving role/prefix/auxiliary/predicate words, punctuation, direction, rotation and active/inactive changes. Three additional names construct missing/conflicting controls. All 13 tokens are absent from the retained 2,976-row role-training data and every nested serialized canonical-word inventory in the bound artifact. Expected queries, source identities, word spans, byte bounds, answers and typed outcomes are independently rebuilt from raw text. All 492 raw inputs are unique and disjoint from those training inputs.

For each of the 96 styled cases, two additional records configurations provide a missing-compatible or a different-answer conflicting second hop. The raw oracle requires an unchanged unique first selection and unchanged expanded queries, with exactly zero or two second-hop compatible sources. Preflight review corrected the conflict insertion to preserve the separate inactive-record intervention; this correction occurred before execution. The panel has 260 answered and 232 typed-unresolved expectations. Context remains four records, 16 words/128 bytes per record, 256 prompt bytes, two clauses, and a maximum three-word/50-byte payload with 96 runtime steps.

Acceptance and data are frozen before artifact loading; novelty is verified before generation. Authored parsing, expected role labels and source/span metadata are report-only and never enter inference. The unchanged artifact is decoded and reloaded; no fitting or training-data augmentation is performed. This is open development, not an independent final holdout.

## Actual failure boundary

All 24 failed cases concern the name form amber will → selka will in the object-direction question, across four rotations and baseline/active/inactive mutations. Twelve valid cases and twelve conflicting siblings fail. Every one has the correct initial phrase and whole rewritten query, but an empty continuation-compatible set. All return typed NoCompatibleCandidate, including the cases that should answer and those that should report Ambiguous.

For example, records include today navor will call selka will tomorrow. and today selka will did help dorva tomorrow. The question please tell me who will navor call? please tell me who did they help? correctly produces the first phrase selka will and the lookahead question please tell me who did selka will help?. The valid second record is then rejected. Adding a second matching relation with answer zelun also produces NoCompatibleCandidate rather than the required Ambiguous result.

The matched 12 missing cases return the same rejection and therefore pass. Their score does not establish that this affected relation is understood: the valid/missing/conflicting distinction fails. For unresolved rows, the report distinguishes actual initial-frame lookahead from committed reads; it does not claim that the intermediate read was committed when completion lookahead stopped execution.

In the original-versus-substituted 300 pairs, none of the 1,200 source-role vectors change, 12 of the 600 query-role vectors change, and none of the 600 required-participation masks change. The actual affected query key is:

| Authored occurrence | Required role | Observed query-role key |
| --- | --- | --- |
| will in who will help oscar? (retained training) | auxiliary/context | (13, 64, 15) |
| will in please tell me who did selka will help? (new name) | name/content | (13, 64, 15) |

Center 13 identifies will, left 64 is an unknown neighbor, and right 15 identifies help in this role inventory. The role-anchor builder currently reads training records only; the question-only word who is absent, as is the new name selka. Both collapse to the same neighbor marker. Exact canonical word identities remain available elsewhere in matching. The observed collision belongs to this finite role projection, not a demonstrated failure of canonical identity or Hamming geometry.

A deterministic decision using only that key cannot distinguish the two authored roles. All failures align with the changed query role and empty continuation routes, but a direct intervention changing only that role was NOT_RUN. Correcting the key alone has therefore not been shown sufficient to restore complete generation. Novel triple-name behavior was NOT_TESTED; the new styled endpoint forms contain one or two words. Frozen training queries are used only for the observation comparison.

## Retention and verification

All 300 latest sealed Full structures replay exactly. All 200 preceding sealed Full traces also replay exactly. The wider 6,688 panel compares actual frozen 0175 generation with actual retained 8055 generation on the earlier raw cases, all equal, including 512 typed unresolved outcomes. This retains the prior report's comparison scope; it is not a fresh comparison against every historical adapter-control response. Those broader adapter-control campaigns were NOT_RERUN.

ExactIdentity and reload each equal Full on all 492 new structures, including failures. ReadDisabled and UpdateDisabled have zero correct answered dependent results. UpdateDisabled retains the unresolved cases where completion ends before a read is committed; this does not override the 24 Full failures. All 75 previous sealed roots/1,752 files and 23 source/binary versions verify unchanged. The new report adds one root and 12 sealed files, giving 76 roots/1,764 files and 24 source/binary versions. Both original dirty checkouts and normal model 15baec48 are preserved.

One optimized offline build passes 48 focused tests, including two new environment tests. One frozen-artifact evaluation completes; zero fits, zero model retries and zero runtime parameter changes. Total charged build/preparation/evaluation/control work is 213440/600000 ms; parent 14038791/14610000; shared 133789364/134450000. Peak sampled process-tree RSS is 3478355968 bytes against 6 GiB. The pre-use projection records necessary parent/shared time extensions of 300,000 ms each under standing authorization, with storage limits unchanged. No paid compute or cleanup; the 128 MiB storage stop margin remains. The local final resource receipt supersedes the pre-delivery storage snapshot.

Artifact SHA256 remains 0175ad86850a77017fdc41b7b4da9c22fe41bc8bcbf348e1352967dd29e43a94. Frozen binary SHA256 is 34f91f123259092b802b10c381ca63aaf66071022b109329a1bb2a9bc5ec1240. Full fitted/runtime source remains unchanged; source edits are test-module registration, retained report-helper visibility, and the new Rust diagnostic. [Machine evidence](evidence/native_geometric_neighbor_transfer_973.json), [Rust report](../crates/uor-r4-core/src/native_geometric/dependent_language/neighbor_transfer_report.rs). Exact responses, observation pairs/collision witnesses, resources, preservation and restart are under .uor-handoff/2026-09-12-codex-v7/shared-core-first-step/neighbor-transfer-1/.

## Next action

Correct the role observation/learning interface using training data only. First include canonical words from both training records and questions in the role-anchor inventory, and verify that the measured opposite-role key becomes distinguishable. Then use predetermined training-only views that withhold eligible name anchors from the finite role observation while preserving full canonical word identity for geometric matching. Learn unknown-name behavior through existing final-output-compatible credit, preserving auxiliary negatives and withholding ambiguous credit. Keep the finite anchor bound and remap inherited table indices by exact canonical Word equality. Do not insert the exposed development names or hand-label the unseen name rule. Adding question-only anchors alone is not a demonstrated fix. Qualify actual generation on the now-open 492 cases and all retained controls; independent final evaluation remains separate.

Normal model 15baec48 remains unpromoted and the qualified experimental parent remains 0175ad86 at its original path. This diagnostic does not establish general grammar/prose or semantic metric advantage. #973 stays open.
