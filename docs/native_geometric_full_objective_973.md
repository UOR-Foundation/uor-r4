# Full-objective joint proposal comparison

September 12, 2026. References #973 and #964. **FAIL_FULL_OBJECTIVE_JOINT_METHOD_SMOKE; no promotion.** Retain `15baec48`. The [preceding tied-learning result](native_geometric_tied_learning_973.md) improved a 132-target subset but regressed full construction. This step changes selection to the complete original 672-target construction objective, preserving the same parent `ade9a1cf`, two parameter indices, four seeds and per-arm call budget.

Fixed-distribution joint sampling and greedy uniform coordinate search each execute 2,050 real full-objective evaluations per seed: one parent score, 2,048 proposals and final witness replay. Four seeds produce eight arms, followed by an exhaustive 14,400-pair reference with all other model parameters fixed. The opened 381-target development set is excluded from selection. The previous categorical learner is not retrained, and the parameter block is not expanded.

| Frozen condition | Result |
| --- | --- |
| Fixed sampling strictly beats coordinate in at least 3/4 seeds | 0/4; all ties |
| Fixed sampling strictly improves construction loss while retaining aggregate accuracy in at least 3/4 seeds | 0/4 |
| Witness eligibility: strict construction loss improvement and no parent-correct-to-wrong rows across both datasets and three controls | 0/8 eligible |

**Every arm retains the original `[119,119]` values at indices `[1176,1930]`.** The exhaustive table finds zero improving pairs. The parent is the unique measured minimum at NLL 3.375390554740109; the next setting `[92,119]` has NLL 3.433976746295292, a gap of 0.058586191555183. This establishes the conditional minimum for this exact block and frozen remainder under the executed likelihood calculation. It does not establish a global model optimum, a capacity limit, or failure of geometric language modeling.

The result resolves the sampler question for this block: neither method missed a lower-loss setting. The prior subset's read update `[119,76]` is inadmissible on the complete objective. Repeating or increasing the sampler budget here is not warranted.

All eight replayed witnesses reproduce the parent on every measured panel: Full construction 79/672 correct, NLL 3.375391; Full opened development 28/381, NLL 3.663040. ContextDisabled and StateDisabled panels also match exactly. Across 25,272 aligned row comparisons there are zero correct-to-wrong changes and zero categorical root-slot, source-identity or prediction Hamming differences. These are equality counts at aligned positions, not bit distances between arbitrary IDs or semantic metrics. All 32 Full witness continuations equal the parent's four incoherent continuations and reach 96 bytes without EOS. No useful prose or Rust generation is established.

The eight saved objects are explicitly **test-only parameter witnesses**, not newly trained standard model artifacts. Each binds its exact parent, unchanged implementation, driver, original frozen design bytes, corpus, mode, configuration, roots and search report. It is serialized/reloaded and replayed against its parent before actual generation. Changed in-memory copies are never presented under an inherited model CID or exported with fabricated training provenance. No witness changes any parameter in this result; identities still differ by configuration and mode.

Only a Rust test driver and its module declaration changed. Production training, serving and artifact source files are byte-identical to the previous checkpoint. One focused release test covers witness binding/replay, EOS inclusion, explicit lost/gained predictions, root/source mismatch counts and row alignment. The single actual experiment completed in 9.873 seconds internally with 16,400 search calls and 14,400 separate oracle calls, plus recorded replay/behavior checks. The first compile failed because the row diagnostic called a sibling module's private loss method; the corrected driver uses the existing equivalent test-only helper and checks row losses/accuracy against the production evaluator. Both build charges are preserved. Independent source and result review, formatting and claim checks are recorded. No new allocation census, full suite, fresh holdout or full-model campaign was run; prior allocation evidence retains its original scope.

Research consultation reused the local knowledge result, current Rust source and the [SpiralCore/FBS review](../research/spiralcore-v68/README.md). An older GitNexus snapshot returned historical dense trainers, which were not adopted. [Bergstra and Bengio's random-search paper](https://www.jmlr.org/papers/v13/bergstra12a.html) supports a baseline-design analogy in its hyperparameter setting; it is not evidence for geometric weight learning. The actual previous comparator result and objective mismatch justify this experiment.

**Next action:** one exact emission-block recalibration on the complete construction trajectories of final `ade9a1cf`, using existing `calibrate_emission_blocks`. The earlier exact calibration produced `4d83b1bb` before the subsequent 149 accepted joint proposals changed query/key/read and emission parameters. Its conditional optimum therefore does not establish optimality on the final state/target rows. Freeze one pass, full-objective and accuracy conditions, all six parent row controls, actual generation and resources before execution; keep recurrence and context routing fixed, and keep opened development outside fitting. A gain would establish missed conditional emission optimization only. No automatic subsequent joint-fit cycle, new mechanism, oracle-pair installation, fresh draw or promotion follows.

[Tracked evidence](evidence/native_geometric_full_objective_973.json) binds the exact source/compiler/binary, frozen design, all witness identities, exhaustive table, raw row comparisons and generation, preservation and cumulative resources. Local originals remain under `shared-core-first-step/full-objective-1/` in the established handoff.

Resource receipt: 120,471/360,000 ms model/build/test, including the failed compile; parent cycle 1,753,135/2,100,000 ms; cumulative ledger 121,503,708/132,950,000 ms. Added storage 25,948,160 bytes within 128 MiB; parent growth 1,406,492,672 bytes within 2 GiB, retaining the 128 MiB stop margin. Peak sampled RSS 2,246,492,160 bytes. No allowance extension, paid compute or cleanup. Final engineering/delivery receipts append locally.
