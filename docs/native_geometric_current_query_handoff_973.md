# Contextual current-record handoff — #973

This cycle repairs a measured dispatch boundary in the native geometric model: the frozen current reader offers the correct exact record, but long current-location requests enter an inherited dependency operation and emit `Unknown.`. Current selection must also preserve genuine two-hop reads and missing-link abstention. The retained successor is **`f0901dad`**, after construction, independent fresh, preservation, checkpoint and native API gates pass. Parent `84f82191` and all negative candidates remain preserved.

## Mechanism and boundaries

The optional outer witness binds the complete `84f82191` parent. The shared signed-H4 selector chooses between accepting the frozen current reader's exact, current, nonconflicting, representable copy candidate and deferring to the unchanged parent dispatch. Historical selection still runs first. This is bounded source/operator arbitration, not an expert network. Stored records, owner/version identities, copy/field emission, inner dictionaries, landmarks, biases, ranks and all parent parameters remain frozen.

Training labels contain prompts and exact current-record IDs or explicit defer decisions; they contain no response strings. The new dictionary uses the existing Rust word scanner and deterministic frequency/byte ordering over construction prompts, capped at 128 entries. Prime identities address lexical features; they are not semantic distances. Training learns fresh signed-H4 code roots over those identities. Final serving uses bounded geometric transport, integer scores and table lookup, with no matrix product, transformer, provider or response template.

The scoped successor additionally limits this selector's context to source words after the latest committed owner/value endpoint. It uses byte ordinals, includes stored noncurrent/conflicted records and complete span terminals, and retains the existing turn/query boundary. Both lexical addresses and owner-context features consume the same local view. The frozen candidate reader is unchanged. This rule depends on actual learned-writer commits; it is not a general linguistic boundary detector. Uncommitted prose after a fact and queries before later facts remain limitations.

## Preserved diagnostic and rejected candidates

On parent `84f82191`, the diagnostic offers the expected current record for all 204 original targets, but actual generation answers none correctly. The first fitting attempt fails before learning because its configured role-context mode differs from the frozen parent; its 71.711 seconds remain charged.

Inherited-dictionary candidate `540dfa71` passes 204/204 construction targets and preserves 2,240/2,240 inherited comparisons. Opening 32 genuine dependency contrasts reveals sixteen regressions: the `What` forms answer with the first-hop owner instead of the final value or `Unknown.`. Its original dictionary does not represent `current`, `location` or `previous`. The artifact, exact wrong outputs and source receipts remain preserved; it is rejected.

The finalized construction population contains 2,500 examples: all 2,444 original cases, all forty opened contrasts and sixteen newly authored uses of literal `current` in factual roles. Four opened direct-current cases are explicit repair targets; the other opened and role contrasts preserve the complete parent dispatch. There are 208 current targets and 2,292 preservation cases. No original row is removed.

With identical data and fitting configuration, inherited-dictionary control `fef57f97` satisfies 316/320 distinct competitive frames, answers 138/208 targets and preserves 2,292/2,292 cases. Lexical candidate `6816176b` satisfies 729/729 distinct frames, answers 208/208 targets and preserves all 2,292 cases. Its 128-word dictionary omits 147 lower-frequency words and 2,698 occurrences; it is bounded coverage, not a universal vocabulary. Erasing only its learned outer transforms reduces the target answers to 0/208. All 32 construction dependencies and sixteen literal-word role cases are semantically correct.

That construction success does not qualify transfer. After its source/artifact/data selection was frozen, the first 96-byte fresh draw produced 764 cases using 21 previously unseen owner/value words. Candidate `6816176b` answers 194/196 targets and preserves 564/568 inherited comparisons. Two plain direct-current requests still return `Unknown`; four uses of literal `current` in facts bypass dependency or missing-link abstention. For example, `kzamt in current. What is the location of kzamt?` changes the parent's `Unknown.` into `current.`. This candidate is **rejected**, and the complete fresh draw and failed rows remain unchanged.

The successor explicitly reopens development after that rejection. The first fresh population is open evidence for the successor, not its held-out evaluation. It starts again from `84f82191` and the unchanged 2,500 construction examples. No failed fresh response becomes a serving answer or training response template. A separate draw may occur only after selecting and freezing the successor.

## Successor validation and disposition

Scoped candidate `f0901dad` uses the same dictionary, training receipts and fit configuration as rejected `6816176b`. Scope changes the competitive representation to 179 distinct frames and 278 feature codes. All frames are satisfied after eight accepted updates; four final rows have nonidentity roots: owner-neighbor `of/Name` and `of/State`, and the `current` and `location` unigrams. Fitting configurations match, but feature populations and executed training costs differ; this is not a fixed-work comparison of the two dictionaries.

Construction generation passes **208/208 exact current answers and record selections**, with EOS and unchanged relation records, and preserves **2,292/2,292** inherited comparisons. The explicitly reopened first draw passes **196/196 targets and 568/568 preservation cases**, including every prior failure. Removing the complete handoff reproduces the parent on all target comparisons. Erasing only the new geometric transforms produces zero target answers.

After the scoped artifact/source/data selection was frozen, a separate 96-byte draw generated 764 cases. All 21 drawn owner/value words are absent from construction and the previous opened draw. The successor passes **196/196 exact target answers/selections and 568/568 inherited comparisons**. All 32 dependency, four absent-owner, four explicit direct-current and sixteen literal-word contrasts are semantically correct. No successor refit or redraw follows this selection. These are fresh spellings in authored forms, not unrestricted language qualification.

The matched scope-removal control keeps all 208 construction and 196 fresh targets correct, but preserves only **2,276/2,292 construction** and **552/568 fresh** cases. All sixteen failures in each population are literal-`current` roles: a factual word improperly triggers the first-hop copy. Full preserves all sixteen. The occurrence scope therefore has a measured role in protecting dependency and abstention behavior under this fitted router, rather than being credited merely for current-answer success. The isolated transform control scores **0/196 fresh targets**; complete handoff removal equals the parent on **196/196**.

Focused verification passes **12 checks**: three dictionary tests, three lexical-format tests, four committed-scope/ordinal tests, the native integer-kernel source guard and the actual-artifact test. The artifact test executes eight current→historical→arithmetic conversations with both owners/orders and Name/State styles. It checks 2,064 input and 476 output checkpoint positions, sixteen active anchors, 48 malformed-restore rejections and 24 disabled-control parent-turn comparisons. Parent identity/bytes and malformed model commitments are checked. Both rejected formats load byte-for-byte and reproduce their pinned negative outputs; identical work counters are not claimed.

All **808 native API checks** pass: 416 construction and 392 fresh completion/checkpoint-independent-sum checks. These exercise the actual Rust native API, not HTTP/browser serving. All 72 query-owner and 216 historical-transfer comparisons are exact. All fourteen older behavior/control reports have zero checked differences, including their negative interventions. Twelve multi-turn histories and 24 word/Rust cases are preserved. Copy/Add and action each pass 24/24 ordinary and 24/24 stress cases. Actual generated Rust is compiled and executed. Full-suite, HTTP/browser/WASM, allocation instrumentation and complete-path energy checks are NOT_RUN.

## Next implementation

The actual eighteen-case three-version diagnostic passes all six current and all six immediate-previous forms. All six initial-version requests incorrectly emit the immediate previous value: the chain Dusk Ridge → Copper Vale → Amber Field answers an initial-location question with Copper Vale. The reverse initial-value layout has the same failure. The historical trace gives `initial` prime zero and offers only current record 3's immediate predecessor, record 2; record 1 is not an offered historical candidate.

Next under #973, learn contextual initial-versus-previous intent and select the corresponding exact ancestor through a bounded version-chain operation. Extend the historical field proof to validate the selected ancestor path while preserving immediate-previous and current-record safeguards. Additional lexical identities alone cannot choose an ancestor that is never admitted. Preserve competing owners, full spans, consecutive revisions, dependency/missing-link behavior, causal follow-ups and arithmetic. This diagnostic identifies a next implementation, not evidence that it is already working.

## Resources and preservation

The complete pre-use projection allows 3,600 model seconds and 1,800 engineering-command seconds, one model process/two threads, 4 GiB model RAM, 6 GiB build RAM and 1 GiB new storage. Context bounds are 512 tokens / 4,096 bytes for normal cases, 8,192 tokens / 65,536 bytes for inherited cases, and 96 output tokens. Training is bounded to 4,096 documents and 8 MiB aggregate prompt bytes. Inherited five-turn histories and 1,056 padding tokens per turn remain available.

Standing-authorized increments recorded before work are 3,000 model seconds and 512 MiB storage, raising cumulative limits to 43,650.000 seconds and 41,305,665,536 bytes. The 196.620-second prior reservation and 128 MiB stop margin remain intact. The lexical and occurrence-scope refinements have separate complete projections within those limits, with zero additional allowance increments. All failures, fits, preparation, checks and retries remain charged. No unique material is deleted and no paid external compute is used.

Local evidence: `/Users/casey.allard/Documents/Codex/2026-09-09/uor-r4-current-query-handoff`. General prose, arbitrary instruction following, general reasoning/program synthesis, alpha/frontier capability and complete-path energy savings remain unqualified. #973 stays open. The matched writer-context contribution obligation in #1139 remains separate and open.

At the immutable evidence snapshot, this cycle used 2636.903 model seconds and 805.356 engineering-command seconds, including rejected candidates and the failed configuration attempt. Cumulative use is 42344.214 / 43650.000 seconds, with 196.620 seconds reserved. The complete projection allows 3,600 model seconds, 1,800 engineering-command seconds, one process/two threads, 4 GiB model/6 GiB build RAM and 1 GiB new storage. Standing-authorized increments of 3,000 seconds and 512 MiB were recorded before use; later refinements use the same allowances. Accounted storage is 40,862,650,368 / 41,305,665,536 bytes, leaving 308,797,440 bytes after the 128 MiB margin. No unique deletion or paid external compute occurred. Final documentation and protected-delivery charges append to the mutable receipt without resetting prior charges.

The [immutable evidence snapshot](evidence/native_geometric_current_query_handoff_973.json) binds commands, source/artifact/data hashes, controls and all dispositions. The retained artifact is `.uor-models/native-typed-value-2026-09-05/current-query-handoff/model.json`, CID `blake3:f0901dadf87f82cd283c8cae56c41a3048ca7b839c1810b3577e589a18af38e3`, SHA256 `9e126c2f7d8af95164e6b6ace81fea63feb3b9431f211120bd7e4ab881058435`, 16,375,930 bytes. Queue status names are compatibility acknowledgements, not test results.
