# Composed operator transitions and formatted output — #1140

## Behavior and scope

Retain `866cb92d` over preserved parent `ccbe7f2a` at the scope described below.

The candidate combines two Add operations with the existing learned sentence/Rust emitter. Actual history first computes `13 + 4` and emits `17.\n`. A subsequent request to add three extra coins, do it again and explain in a sentence produces:

```text
20 is 3 plus 17.
23 is 3 plus 20.
```

The corresponding Rust request produces:

```rust
20 == 3 + 17
23 == 3 + 20
```

These are two generated equality expressions, not a generated Rust program. The test harness wraps each unchanged expression in a separate assertion before compiling and executing it.

The first new result is committed as record 4 from ordered operands `[3, 2]`; the second is record 5 from `[3, 4]`. Formatted emission reads exactly `(write 4, record 3)`, `(4, 2)`, `(5, 3)`, `(5, 4)`. Both output text and final committed derivations are checked against independently authored values and identities. The original captured numeric and lexical query/source arrays remain unchanged through generation. EOS follows the requested one or two operations below the unchanged three-operation bound.

There are seven authored request placements, each with and without `Again`: plain, sentence/Rust prefix, sentence/Rust suffix, and ordinary sentence/Rust suffix phrases. Construction uses three numeric groups (42 cases); open evaluation changes numbers and names (28); stress reverses initial fact order (28). These finite forms establish bounded composition. Mixed operator sequences, arbitrary references and wording, general prose, broad reasoning/coding, alpha and complete-path energy advantage remain unqualified. #1140 remains open.

## Mechanism and preservation

The parent could generate each first formatted result and could generate plain Add/Add responses, but stopped after the first formatted segment in all eighteen combined construction cases. This isolated the next-operation decision at the learned completion boundary.

[composed_output_training.rs](../crates/uor-r4-core/src/native_geometric/composed_output_training.rs) warm-fits the existing shared signed-H4 Stop/Copy/Add router. It changes no serving feature law, dictionary, operation limit, tokenizer, literal/admission router, lexical emitter or fixed geometry. The final fit uses 42 new cases and 254 earlier preservation contexts, producing 331 decision frames and 43 distinct frames after deduplication. It adds 161 identity-initialized feature codes to the previous 109, then learns code roots, landmarks and biases with the existing discrete geometric learner. Configuration is 768 maximum features, eight passes, 24 proposals, angular mode, seed 1140 and a 120-second fitting ceiling. All 43 distinct frames fit without reaching that ceiling.

History and each first segment are freely generated by the parent and checked against their receipts. Offline next-action supervision executes the declared operator against actual records; it never observes expected response bytes. The subsequent segment is generated from that execution. Fit-frame correctness does not qualify a candidate; the separate driver freely generates complete candidate responses.

Serving uses the unchanged bounded signed-H4 composition/ranking and exact typed integer/table operations. The serving diff only selects the current or preserved operation router for the declared control. There is no prompt-specific answer branch, response template, provider, parser or new matrix product. Prime identities remain identities rather than semantic distances. This intervention does not isolate zeta or paired-H4 contributions.

[composed_output.rs](../crates/uor-r4-core/src/native_geometric/composed_output.rs) retains the previous router. Removing the outer witness and restoring that router must reconstruct the exact parent before current identity is validated. The optional field preserves old-artifact loading; the new artifact requires the updated runtime. The matched control restores just the old operation router.

Candidate `ae245428` passed new construction/open/stress and the initial 196-case replay, but failed ten of the 58 earlier computed-construction responses by continuing after their correct answers. It is rejected and preserved. Adding the complete already-open 58-case panel as Stop contexts produces candidate `9805e99d`. Its replay exposed six evaluator false negatives: correct zero-write abstentions were scored by an older helper that assumed one numeric write. The corrected replay uses explicit write-count labels from the preserved baseline and retains the legacy results. Training now skips six boundaries where serving would never offer an operation transition, producing candidate `866cb92d`. No response-string heuristic, serving exception or feature-law change accompanies these corrections. All three candidates, fit-source copies, reports and full resource charges remain available.

An earlier open run used the previous evaluator because it started before the strengthened checker build finished. Its 40.732 seconds and results are preserved as superseded checker evidence. It overlapped that build and supports no performance claim. Final evaluation is rerun after completed builds and checks actual committed records in addition to pending decisions, repeated predictions and per-byte checkpoint parity.

## Executed validation

| Check | Executed result |
|---|---|
| Parent construction / retained construction | 24/42 → 42/42; eighteen formatted continuations repaired |
| Open / reversed initial fact order | 28/28 / 28/28 with independent pending and committed write identities, exact reads, complete text and EOS |
| Previous operation router restored | 16/28; twelve formatted continuations lost, all other open answers preserved |
| Whole transition disabled / latest intermediate excluded | 14/28 each; fourteen single-operation answers retained, all fourteen second operations lost |
| Lexical emitter / record read / learned emission transform disabled | 4/28 each; four plain answers retained, formatted output lost |
| Earlier construction replay | 254/254, including six zero-write abstentions |
| Main retained population and all earlier reports | 663/663; every recorded preservation panel passes |
| Earlier exact lexical outputs | 72/72 saved responses, read identities and ordered operands preserved |
| Earlier instruction open / reversed-name-order / former fresh | 24/24 each; these are already-open preservation cases |
| Earlier operator / current-query / harder histories | 24/24 / 48/48 / 16/16; individual dependency and lexical/instruction controls match previous actual outputs and identities across fourteen reports |
| Actual native API | 50/50, including composed direct/API parity, checkpoint import, later independent sum, parent/current identity tampering and unknown witness fields |
| Focused runtime checks | Six read and six operator tests, integer-kernel source guard, formatting, actual composed and prior lexical checkpoint/allocation checks |
| Separate post-selection fresh panel | 28/28 with changed positive/signed values and names under the same seven forms; no refit follows |
| Actual generated Rust expressions | 54 compile and evaluate: eighteen open, eighteen reversed-context and eighteen fresh |

All preservation controls retain their individual declared results; aggregate improvement cannot override a failed prior response. Queue compatibility acknowledgements execute no tests. The separate fresh panel is evaluated after recorded selection and is not used for fitting. New-artifact browser/WASM/HTTP is NOT_RUN.

## Artifact, resources and next step

Retain `blake3:866cb92de4ad5c130da811d3f2fe8828aed1fe9b2b39b9c9b3c7ad3dbf7dee62` at `.uor-models/native-typed-value-2026-09-05/composed-output/model.json`, 12,365,057 bytes, SHA256 `3a951eb5cf88af0982bcafac3ac0a00ac1340dca3078413a95424fb749db38f0`. The artifact adds 59,458 bytes over parent `ccbe7f2a`. The [evidence receipt](evidence/native_geometric_composed_output_1140.json) binds source, all candidates, actual reports, controls, selection and resource accounting. Raw files remain at `/Users/casey.allard/Documents/Codex/2026-09-08/uor-r4-composed-output`; these local model artifacts are not included in a fresh clone.

The pre-use projection reserved 1,800 model seconds and 1,800 engineering-command seconds, one model process/two threads, 512 context tokens, 96 output tokens, 4 GiB model RSS, 6 GiB build RSS and 1 GiB new storage. A necessary 900-second cumulative extension was recorded before use. Unused fit allowance was reassigned to preservation before later checks without increasing the cycle total. This cycle used **920.669 model seconds** and **860.544 engineering-command seconds**. Every fit, failed/superseded replay, data preparation, API, runtime and fresh check is charged. Small documentation/receipt/Git commands remain outside the engineering-command stopwatch.

Cumulative model use is **9,967.755 / 11,250.000 seconds**, retaining the prior 196.620-second reservation and 1,085.625 seconds after it. Conservative storage is **32,033,710,080 / 34,863,214,592 bytes**, including the retained copy and an 8 MiB final metadata reserve. The 134,217,728-byte stop margin remains. Sampled peak process-tree RSS was 1,821,720,576 bytes for model commands and 3,782,328,320 for engineering commands. No unique material was deleted and no paid/external compute was used.

The actual composed allocation test checks two development responses and their real histories over 72 checkpoint positions, with two exact writes and ordered reads per composed response and zero allocations in ingestion/begin/predict/observe. The eight earlier lexical cases also retain zero allocations and 136 checkpoint positions. Composed cold loading took 20.470 seconds; scoped warm predict/observe median was 0.125 ms and maximum 0.222 ms. Encoding, session creation, checkpoints, JSON/reporting and cold loading are excluded from those warm numbers. No complete-path performance or energy advantage is established.

Next: learn mixed shared Copy/Add sequences and their operand references, including changed intermediate dependencies and stopping positions, while preserving this result. The current positive panel composes repeated Add with one reused current-query literal. It does not yet demonstrate general selection among operator sequences or unrestricted language construction. Broader language learning remains with #973.
