# Independent contract and source review — #1094

Status: `REVIEW_IN_PROGRESS`; withheld execution is not admitted by this draft.
Date: 2026-09-03. The reviewer did not author the adapter, curator or comparison
driver. Review is against the protected
[#1085 specification](integration/clause-segmentation-1085.md), unchanged SHA256
`85f928fec94fa0f6793cff4c35e1fc8c9cba691739d34db272465766c7c9dab1` at
`3e894820c520f3b7803a48c6a2eeeb5b7d7021c5`.

## Completed source inspection

The policy and adapter match the four literal fact forms and one query form,
fixed owner/object/location lexica, retained punctuation, right-padding ID 57,
five clauses and 13-token limit. The boolean recognizer exposes no captured
semantic fields. The raw-input path preserves maximal lowercase words and
half-open byte spans; strict request fields, byte limit, encoding/lexing,
boundaries, length and syntax are checked in the frozen order. The derived
input identity preserves ordered shapes, IDs and lengths. Artifact startup
precedence remains a wrapper obligation pending the driver review.

The independent curator reads the pinned #1073 construction records and #1077
construction/lexical records. It imports no adapter, Torch/model, historical
parser, renderer or decoder helper. Its source independently checks the
historical IDs, fact order, targets and distractor assignments; it constructs
text and span annotations from the declared templates. Fixed fact role
positions and the unused query-location sentinel agree with the original
source. These are evaluator references, not inference inputs.

The selection receipt freezes canonical group serialization before text
generation. The reviewer checked its 4/16 group counts, two/eight groups per
question family, and disjoint source/group IDs. Source inspection confirms
within-family SHA256 sorting, first-two/next-eight selection, all five original
variants, all four fact forms and all four frozen spacing profiles. The refusal
mutations preserve the specified sixteen families and precedence, and the
sixteen boundary controls remove one period in one preselected row per
fact-form/profile cell. Semantic worlds remain historically observed.

The policy, curator and selection digests were checked without opening any
generated withheld raw/reference file or executing the adapter, curator, model
or proof tools.

| Inspected object | SHA256 at this review checkpoint |
|---|---|
| `policy.json` | `91cce30a0b78c48130595369d3ea2a47c4de89cab5db1d4219d1874198cf52d0` |
| `adapter.py` | `21af8d2d43f9622a7a6e2d88c0c07e358b1bc8d519cc193b0574c8f6ced4fc27` |
| `curate.py` | `4d8012f8647cbbf57136999de6522af7b27bf994466744d0efac20e435a5defb` |
| Independent selection receipt | `892e3239773e8a14e72ee650dc12c98ee4e1a5b432b69365a60cef8b15c9b5fa` |

## Release conditions still under review

The worker and campaign driver must bind the actual startup artifact/source
closure, runtime, raw-only process access, exact full soft-output comparison,
observed forward counters, immutable before/after states, one evaluation/replay
and nonrenewable resource accounting. Every oracle/reference/population path
must be denied to the adapter worker by the actual process profile, including
the fresh-process replay; runtime/source allowlists must not include those
paths. A profile declaration alone is not evidence of effective isolation.

No material parser or curation blocker has been found in the inspected scope.
That statement is provisional while comparison code and preparation receipts
are incomplete. Final reviewed source hashes and release decision will be
appended here before withheld release. No empirical outcome, mathematical
proof, general language capability or revision of #1079/#1082 follows from this
source review.

## Driver review findings before withheld release

The initial worker source preserves the frozen reader/core, full soft role
vectors, tied full-vocabulary head and R4 execution. Its imports and load path
use exact model/frame assets rather than historical answer reports. It counts
attempted forwards, validates state identities, checks an OS-denied sentinel
and emits complete typed tensor bytes. These observations do not yet admit the
comparison: the following initial coordinator findings require resolution.

1. Map adapter results through `valid_indices` when a valid reference row is
   refused. Compressed predictions must not shift to subsequent row identities;
   a complete parser miss must remain `CLAUSE_ADAPTER_MISS`, with missing
   predictions included in denominators and actual forward counts retained.
2. Preserve typed worker failures and their partial counters/resources. The
   initial generic `ValueError` path could relabel `INCOMPLETE_RESOURCE` or
   `UNAVAILABLE_REFERENCE_REPLAY` as an input-provenance failure.
3. Commit the immutable execution-start receipt before the first withheld file
   read. Charge integrity, comparison and terminal-write work to the declared
   phase/cumulative budgets; do not create a new budget after a revealed failure.
4. Verify authoring file bytes against the independent population commitment
   before preflight. Compare pinned offsets and success identities as well as
   tensor/span fields, and count sealed corpus bytes from its receipt while the
   withheld directory remains unreadable.
5. Stream full tensor comparison at a batch boundary. The initial coordinator
   retained all full batches from both arms in memory; the frozen contract
   requires bounded comparison followed by retained digests, with no four full
   persistent copies. Recheck byte/RSS limits after terminal writes.
6. Align the population paths with the curator's `population.json`,
   `authoring/raw.jsonl`, `authoring/reference.jsonl`, `withheld/raw.jsonl` and
   `withheld/reference.jsonl`; boundary controls are already in the withheld
   pair. Read and validate both readiness and zero-forward terminal receipts.

These findings were sent to the driver author while withheld files remained
sealed. No adapter execution, model forward or withheld content inspection was
performed by this reviewer. Their disposition and the final source hashes are
pending.

## Source disposition before authoring preflight

Status at this checkpoint: `SOURCE_READY_FOR_AUTHORING_PREFLIGHT`. All six
initial coordinator findings above are resolved in the source identified below.
This status admits only the declared authoring/input and zero-forward runtime
preparation. It does not admit withheld release or make an empirical preservation
claim. Final comparison acceptance still requires independent inspection of the
actual preparation, bindings, sandbox and sealed population commitments.

The final coordinator maps compressed adapter outputs to original row IDs,
counts all fourteen used role pointers per valid row even when output is
missing, and retains a parser failure as an adapter miss. It compares full tensor
bytes with strict shapes, dtypes and success/refusal schemas. Oracle and adapter
fresh-process replay identities are separate, so oracle instability cannot be
presented as adapter preservation. The oracle diagnostic has its own schema;
only adapter success emits the frozen model-token schema.

The coordinator verifies independent authoring commitments before preflight,
binds the source closure and exact model assets, and records authoring failures
before runtime readiness can fail. The worker validates both initial readiness
and terminal state/counters; it checks the exact Python/Torch/CPU/thread/BLAS
mapping and pre-forward per-arm cap. The process profile permits only package
source, the resolved Python runtime trees, exact bindings and five exact model,
vocabulary and frame files under the home directory. Corpus, reference,
historical report and result paths remain denied. An actual denied-read sentinel
receipt is still required; reading the profile alone does not establish its
runtime behavior.

One oracle tensor stream is retained between sequential workers and consumed
one batch at a time. On interruption the stream and partial counters remain
evidence. A complete stream is removed only after the phase's deterministic
tensor receipts have been durably written. The full phase and cumulative clocks,
actual logical forwards, conservative combined RSS bound, sealed corpus bytes,
output bytes and isolation-probe bytes remain charged. Typed stops preserve the
original cause. `result.json` is a candidate terminal; `completion.json` must
bind it, and any `run-stopped.json` takes precedence. The package entry point
propagates the nonzero stopped return code.

The binding includes the four literal physical-hardware fields and rechecks
them before execution and replay. The unchanged retained reader, core and R4
implementation preserve full soft role mixing, the full tied vocabulary head,
and before/after model state identities. This is a source-level contract check;
there has been no model forward, fitting, proof execution or withheld inspection
by this reviewer.

| Final source candidate | SHA256 |
|---|---|
| `__init__.py` | `ce8dc726a27c61ec8134c4bdb2c4fbfe40db8ca6eab6ddb05a540b9f2a09ca4a` |
| `__main__.py` | `691f0fabe33fc8c87bb333595016d019667b1af357bdda8df48bef162c7bf8f3` |
| `adapter.py` | `21af8d2d43f9622a7a6e2d88c0c07e358b1bc8d519cc193b0574c8f6ced4fc27` |
| `policy.json` | `91cce30a0b78c48130595369d3ea2a47c4de89cab5db1d4219d1874198cf52d0` |
| `curate.py` | `4d8012f8647cbbf57136999de6522af7b27bf994466744d0efac20e435a5defb` |
| `contract.py` | `e341a5ff0a0498827ddcd6d4915ba86f4324d46d967b136a579d12b667a5a118` |
| `worker.py` | `ee6e7dd554e6b333932a37b743820c1674960c151a33f46e94037571b088b7f5` |
| `campaign.py` | `e0e6c8d1387800cf587e82928accb988a9e13fed31a9dbb73763104069d56023` |

No material source blocker remains within the frozen decision contract.
Preflight acceptance must bind these source bytes to the committed revision and
the actual preparation receipt before the curator releases withheld files.
