# Contextual writer-role transfer across value primes — #973

**Retain `231d6950`**, CID `blake3:231d6950b93db3be52d4ce1df7f20c5dabd0329c2183d228e39c8f271f603dfa`, SHA256 `7bb912761e09a34edf40731038f700ad90c1aad53188da0967910382fc280d46`, 14127792 bytes at `.uor-models/native-typed-value-2026-09-05/writer-role/model.json`. Decision and artifact identity are recorded in the [evidence](evidence/native_geometric_writer_role_transfer_973.json). This is a bounded native writer repair; general prose, arbitrary instructions, reasoning, program synthesis, alpha/frontier capability and complete-path energy advantage remain unqualified.

## Observed failure and causal change

The retained `6f5ab4f3` writer misbound the third fact in `Record: Dusk Ridge holds selvi. selvi now in Copper Vale. selvi now in Amber Field.` It stored `now → Amber` as a separate assertion, leaving selvi's directory entry at Copper. Actual owner-first generation emitted ` now is in Amber Field.\n`. The previous current-version reader candidate `aa1bc159` then amplified that stale directory entry; its rejection remains valid.

Six actual parent traces separate the value-prime coverage gap from the older reverse-fact reader failure. At the third Amber boundary, the wrong now/Assert proposal scores 3 and the correct selvi/Revise proposal scores 1. The retained lexical writer-choice block contributes zero to that Amber proposal, but contributes −3 in matching Copper and unknown-value contexts. In the single-revision reverse-fact case the correct writer proposal already scores 5; its old-source answer is a separate reader problem.

The optional `writer_role` block learns a nonpositive correction over the existing writer context. Its feature namespace, kind 17, retains the owner prime, both exterior primes, exact boundary descriptor and directed endpoint separation, while omitting only candidate value identity from this correction. The inherited kind-16 feature, exact bytes/occurrences and all parent model components remain intact. Omission uses a distinct namespace; it is not the unknown-value prime zero.

Offline Rust fitting enumerates the actual writer's ordered alternatives, including all inherited writer, lexical and writer-choice scores. Explicit completed-word endpoint/action labels correct the new targets; every other frame preserves the parent's winner. Full alternative vectors are deduplicated exactly, and incompatible labels are errors. All 4,207 distinct constraints fit, up from 4,201, with three updates in one completed epoch. The result has three learned nonpositive rows. No new H4 transform is fitted in this intervention: the retained prime/zeta/H4 geometry, shared routers and typed emission operators remain frozen.

Serving performs bounded scalar table scoring and existing integer geometric execution. It adds no mathematical matrix product, transformer, provider, phrase parser, owner substitution or global exclusion of the word `now`. Nonpositive coefficients preserve the parent's certified NoWrite cache. Removing the outer witness reconstructs `6f5ab4f3`; format validation binds the parent, training receipts, row domain and full artifact identity. Current-source refinement is deliberately not stacked in this version and requires an explicit future integration change.

## Generated behavior and preservation

The construction combines 1,057 inherited prompts with 36 new three-fact contexts crossing owners, Amber/Birch value primes, plain/owner-first forms and recent/evicted memory. All 36 new answers and complete revision chains pass, versus 6 parent answers. The current directory contains only record 3; records 1, 2 and 3 have the same exact owner, actions Assert/Revise/Revise and previous links 0/1/2. Generation leaves the records unchanged. All 1,057 inherited output/EOS, record and applicable first-decision identity comparisons remain equal, including all 136 older explicitly labelled answers. These comparisons are not 1,057 independent language correctness results.

Structural stress passes 16/16 labelled answers and records, versus 10 parent answers, and preserves three historical/raw/absent-owner controls. Literal `now` owners and sentence-leading `Now OWNER` keep their retained behavior. All 72 construction and 32 stress removal-control comparisons restore the parent. The two exposed control names remove the same new contribution; they are duplicate ablation interfaces, not independent evidence for separate context channels.

Replaying the earlier opened source panel improves 33/40 labelled answers to 36/40, preserves all 18 inherited comparisons and loses no previously correct answer. Four recent reverse-first-fact responses remain wrong: selvi/Copper and tilva/Birch, each in plain and owner-first forms, still emit the old Ridge/Cove span despite correct revision records. The historical query control's malformed `Unknown` continuation remains negative evidence.

Post-selection transfer passes 24/24 responses and full chains (parent 15/24), split into 12 known-prime and 12 unknown-value cases. All 48 removal-control comparisons restore the parent, and the artifact bytes remain unchanged after selection. Fourteen prior semantic/control reports and 12 complete multi-turn histories remain exact; 24 retained word/Rust transfers and all four 24-case Copy→Add/action panels pass. Seventeen focused tests pass, including actual zero-allocation observation/begin-response/predict work and restoration across 218 input and 57 output positions. The scoped native API passes 29/29 checks; generated Rust equalities and identifier-return fragments compile and execute. Measured model load is 28.343 seconds and the sampled warm predict/observe median is 147 microseconds, excluding loading, encoding, session creation and persistence. These are not complete-path energy measurements. Full historical API, blanket suite and new-artifact browser/WASM/HTTP are NOT_RUN. Exact resource charges are recorded in the evidence accompanying this report. Fresh transfer uses new whole prompts and owner spellings with both known nonzero value primes and unknown values under familiar forms; it must not be interpreted as unrestricted unseen-language generalization.

## Preservation and next implementation

Preserve the accepted parent, rejected reader, earlier `19fe175c` and aliased `fcd0ab1d` writer candidates, every earlier artifact, and all raw receipts. The old alias failure explains why the directed endpoint separation remains in the new shared key. More value-specific weights alone would not test transfer of role binding across known value primes.

Next, revisit query-conditioned current-versus-historical source selection on the validated writer state. Start from the four remaining recent reverse-fact failures and compare their exact records and source proposals before/after eviction. Integrate any reader refinement against this writer's parent witness explicitly, then preserve three-fact current versions, explicit historical references, conflicts, abstention and old numeric/word emission. A directory membership preference must not automatically override the requested occurrence. #973 remains open.
