# Continue UOR-R4 geometric model research

**PR #1328, independently reviewed:** the exported policy retains a small reader-held-out text probability gain: H4 **−0.019477 bits/token [−0.032861,−0.005507]**, eight documents /1,952 positions; all five declared document intervals reproduce. Text emitted correctness remains 406/1,952, equal to local; general prose remains degenerate. Full construction emitted correctness drops 362→29. **The contextual-representation diagnosis is withdrawn:** fitting used legacy zero gap thresholds and serving used new negative thresholds, leaving 24/32 gap-band entries untrained. The intended observation was not tested consistently. [Principal review](reader-utility-review-2026-09-21.md); [saved-data audit](../evidence/reader-utility-principal-review-2026-09-21.json).

**Delivered: the corrected reader-policy experiment (2026-09-21).** One immutable `PolicyConfig` now supplies the fit-time selector, the events, the export and serving, with an acyclic binding (config digest from declared semantics plus thresholds; artifact carries the fit-input digest; the delivery manifest carries artifact byte hashes) and a verified loader contract that the consumer calls before prediction. **The corrected fit is a clean negative, and the PR #1328 text finding does not reproduce.** On 8 **new** disjoint reader documents (1,952 positions) the corrected H4 policy is **+0.1193 bits/token [+0.0702,+0.1679]** and categorical +0.1533, where the frozen parent is +0.4532, while a **constant one-nat boost** on the same ungated source and contract gives **−0.0148**: the small held-out text benefit came from a generic small boost, not the fitted table. On the previously inspected documents the corrected policy gives +0.1469 versus the misindexed −0.0195. The corrected fit is materially different — 16 buckets earn their own action and the opcode histogram is [0,0,25,7], not [0,0,31,1]. **All four declared categories are false:** harm containment, useful transfer, relational preservation (present-query emitted correct **56 vs the parent's 77**) and absence (reads **19 vs 5**, loss **0.2969 vs 0.1379** bits/absent query). **Metrics repaired and verified:** the actual-action decomposition satisfies `ranking + gate + dose = actual − Lpool` with **0** actual-vs-emitted-logit mismatches and 0 served-source disagreements; admission regret now uses correct denominators and reproduces the review's independent reconstruction (**0.1946** candidate-only / **0.8316** complete-stream nats, empty pools with `Lpool = 0`); the four-condition intervention shows disabled invariance **140/140** and the original occurrence still admitted **140/140**; reload rejection of a drifted config, a swapped opcode table and a mismatched input identity passes with `reload_failures = 0`. The unresolved distinction is an **objective tradeoff** between expected action cost and emitted accuracy on a mixture whose text half has low copy reliability — not a representation limit and not insufficient support. **Next:** a prospectively declared **constrained/Pareto utility objective on the same features** (per-bucket and per-action costs are additive with fixed sources), before any new observation or representation. Reader incremental cost and physical energy remain UNAVAILABLE. No model promotion.

Read the complete review and next prompt, recover live authority/source/artifact/issue state and project full resources before work. Preserve the owner checkout and unique evidence. Ledger 191016749/194900000ms at review is historical after subsequent work; refresh the live JSON. Necessary local extensions are already authorized when projected and recorded before use, preserving cumulative charges and the 128 MiB stop margin. Use protected PR delivery, verify merge/content, update owning issues and compact revision-pinned knowledge; broad acceptance remains open.
