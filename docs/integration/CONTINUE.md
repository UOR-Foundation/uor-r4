# Continue UOR-R4 geometric model research

Read the [principal review after PR #1308](geometric-query-review-2026-09-20.md) and [complete next DeepSeek prompt](deepseek-geometric-query-step-2026-09-20.md), then refresh origin/main, canonical plan/current state, live issues and resources. The next task is **implementation plus one matched geometric query-read experiment**, not another head-projection campaign. It is currently NOT_RUN.

Corrected empirical E (`565cf98a…`, `head-projection-3/corrected/empirical.cpl2`) remains the numerical incumbent at 7.170816 bits/target. Executed Q0/QG score 7.416178/7.190872; QG improves over Q0 but both fail against E. The complete prescribed projection omitted one required optimized seed and remains NOT_RUN; Gram singularity was not measured. Correct those small reusable source/report defects with focused tests and preserve the actual negative receipts. Both superseded projection roots are sealed. No generated-language or energy qualification follows.

Freeze E; fit Q/S/L with identical action/reader/output counts and two reader accesses. Q combines older geometric state with a current-token query, S reads their separate contributions and L uses only local state. Verify an exported constructive witness and a learned-query fixture first, then use the fixed fit dose and exact older-prefix donor control. Save complete resumable states and final reloaded losses/generation. Selective writes/reset, exact memory and useful shared-model language/coding are later responsibilities.

Authoritative ledger snapshot 170238565 /170900000 ms leaves 661435 ms. The next prompt proposes 7200000 ms complete work and a standing-authorized +7200000-ms limit increment, **not yet applied**. Refresh and record before use, preserve prior charges and the 128 MiB margin. Use a full isolated worktree; preserve owner checkout, artifacts and research; deliver through protected PR and update #973/#820/#963/#964 and existing project items. D0-b and geometric priority remain unchanged.
