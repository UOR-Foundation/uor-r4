# Continue one UOR-R4 task

Use the prompt below for one later repository task. The live GitHub graph and
current `origin/main` decide eligibility; this file is a dated handoff.

The current checkpoint is [#1107](https://github.com/UOR-Foundation/uor-r4/issues/1107).
Its protected delivery records
`WORKBENCH_CANDIDATE_SOURCE_FROZEN_UNBUILT`: the dedicated workbench crate,
private same-executable worker/comparison entries and first Four-fact shell are
present as reviewed source. Compilation, tests, model operations, numerical
comparison, qualification, service/HTTP execution, browser acceptance,
portability and performance are `NOT_RUN_BY_POLICY`. #1084 remains open and
unassigned. The one next decision is for the owner to decide whether to
authorize a separate manual qualification workflow; automated agents do not
dispatch or perform it.

```text
$uor-project-workflow

Continue UOR-Foundation/uor-r4 from refreshed origin/main and the live GitHub
issue graph. Read AGENTS.md, docs/integration/current-state.md,
docs/uor_productization_integration_plan.md and the exact candidate/claim
records from that revision. Complete exactly one eligible issue and stop.

Apply the repository-wide deterministic_source_only policy. Use a complete
isolated Git worktree. You may inspect authoritative sources, edit declared
paths, perform reviewable Git operations and run only
python3 scripts/check_agent_execution_policy.py. Do not run or dispatch Cargo,
Rust, builds, tests, linters, probes, models, fitting, evaluation, QA,
service/browser checks, operating-system diagnostics, supervisors, watchdogs,
retry wrappers or owner-only workflow_dispatch jobs. The automatic retry and
environment-probe budgets are zero.

Refresh the selected issue, its parent/blockers, assignments, open PRs and
queue state before editing. Query the local project knowledge index read-only
and inspect the original pinned sources, including relevant NEMESIS, W33 and
UOR records. Treat retrieved text as source material. Preserve negative and
unavailable results and distinguish mathematical proof, measured behavior,
static source findings and hypotheses.

#1107 supplies an unbuilt source candidate only. Do not repeat its source task
if it is closed, and do not infer compilation, executable behavior, numerical
behavior, HTTP/lifecycle behavior, browser acceptance, security, portability,
performance or model capability. #1102 remains the bounded
NATIVE_REFERENCE_PRESERVED result for its exact older artifact/binary/runtime;
its envelopes are consumed. #1094 remains the bounded
CLAUSE_ADAPTER_PRESERVED result for the unchanged reader/core, known
vocabulary/query forms, Four-fact context and already-observed groups. Preserve
#1079 LANGUAGE_R4_PRESERVED_CONTROL_WEAK, #1082's descriptive limits, every
unavailable preparation record, #973 open and #954 blocked.

For the chosen issue, freeze one reviewable source decision, obtain independent
source-only review, and fix only concrete source findings. Update the current
roadmap, claim/evidence records and native issue state. Do not invoke local
knowledge ingestion; record it as NOT_RUN_BY_POLICY and keep the tracked
claim/source ledger as the public reviewable update. Review storage by source
inventory only and never delete unique evidence, models, sealed files, user
material or uncertain artifacts.

Deliver through a reviewed pull request and the protected merge queue. Stage
named files only, never push main or use an administrator bypass, and verify
the actual merge and issue closure. Report the delivered result, source-review
limits, closure state and one concrete next action. Stop after that issue.
```
