# Continue UOR-R4

Read [AGENTS.md](../../AGENTS.md), owner-adopted [DECISIONS](DECISIONS.md), [machine policy](agent-execution-policy.json), [canonical plan](project-track.md), [current state](current-state.md), and the [review after PR #1304](prefix-pilot-review-2026-09-20.md).

The [complete readout diagnostic prompt](deepseek-readout-diagnostic-step-2026-09-20.md) has been **executed**; the [receipt](../evidence/native_geometric_prefix_recovery_readout_2026-09-20.txt) records both halves. The three rejected prefix candidates were recovered from their saved `CPXS` checkpoints into executable `CPX2` artifacts with an exact, bijection-bound `2I` table (**14,400 products verified twice, 0 mismatches** against the floating table) and exact reloaded parity, without retraining; arm-aware generation and a full-vocabulary count-reference argmax are repaired and every control loss vector is now saved.

The paired fixed-feature output-head diagnostic is also executed: **empirical** conditional-target training reaches 7.170816 bits/target (**+0.387614** [0.360308, 0.415469] over the frozen parent) and **smoothed** count-family targets 7.188585 (+0.369845). The practical screen passes, but smoothed targets do **not** beat equally dosed empirical refitting on true-label development CE, so no benefit is attributed to the smoothed objective. The conditional floating head on the same features reaches 6.874144 with a teacher KL still of 3.2-4.5 bits against a 5.061160 full-fit reference.

The measured bottleneck is the **frozen local feature map**, not the output objective or the target estimator: refitting the head recovers most of the same-dose gap, smoothed targets change nothing, and even a convex floating head stays ~1.8 bits above the full-fit reference. The next bounded increment is a local **representation** increase (wider hidden feature or a learned local interaction) under the same hard integer serving contract and dose, measured against the same panel and references. Count matching is not a prerequisite.

Refresh origin/main, live issues/PRs and owner JSON before work. The recorded charge is 166,038,565 / 167,600,000 ms after the second recorded extension; the earlier 5,400,000 ms charge is marked unverified/conservative and was not refunded. Charge measured non-overlapping intervals once, not the whole reservation. Preserve original checkouts/artifacts and the 128 MiB margin. No deletion or paid compute. Follow protected delivery and update the owning issues.

The [older-prefix prompt](deepseek-ordered-prefix-step-2026-09-20.md) and [frozen replay prompt](deepseek-frozen-prior-step-2026-09-20.md) remain historical experiment specifications, not current scheduling authority.
