# Continue UOR-R4 geometric model research

**PR #1321, corrected:** H4 retains a constructed hard-loss gain of **-0.179540 bits/candidate position [-0.270206,-0.093226]** against exact recurrence; the old -2.4956 figure is bits/sequence. Natural text still regresses **+1.2908 bits/token** and `positive=false` remains. The categorical code map did not learn, the query-blind arm is a partial relation-channel lesion, generation used a different ring boundary, and the partner/absence fixture was defective. Unique-H4 advantage and solved ranking are not established. The [principal review](competitive-reader-review-2026-09-20.md) and [saved-data receipt](../evidence/competitive-reader-principal-review-2026-09-20.json) preserve the useful result with its limits.

**Delivered next: contextual utility.** The [contextual utility controller](contextual-utility-result-2026-09-20.md) is executed and unpromoted: contextual minus frozen global strength is **-0.257644 bits/candidate position [-0.327088,-0.182968]** (140 sequences, 2,000 draws), preregistered `positive=true`, with the arm reading *more* (0.532 vs 0.365) and harming natural text less (+0.2189 vs +0.2443 bits/token). On the repaired construction the **H4 relation term is not significantly better than exact recurrence** (-0.010151 [-0.104470,+0.083675]), and the repaired categorical comparator is the strongest static arm (10.3936 vs geometric 10.7185, contextual 10.4608). The target-using diagnostic attributes the residual headroom to ranking/admission (~12.8x the strength residual; correct source top-ranked 46.3 %, selected 25.2 %). **Next constructive task:** a prospective matched-cost comparison of the source term at higher resolution on a fresh construction draw, with the utility controller frozen on both sides. Do not widen the ring, sweep widths, re-run S attribution or start a scheduler.

The repair aligns generation/evaluation prefix state, uses shared mode-aware categorical/H4 refinement, validates RLR2 maps and vocabulary, preserves categorical continuation in RLRK v2, and corrects fixture truth, metric denominators, cutoff semantics and timing counts. [Validation](../evidence/competitive-reader-repair-validation-2026-09-20.json) distinguishes focused source/artifact checks from a full experiment, which remains NOT_RUN. Preserve `.uor-models/realtext-prior-2026-09-20/competitive-reader-1`, including its unlisted `sum.py`; the actual RLR2 is 4,340 bytes. Physical energy remains UNAVAILABLE.

Read the complete linked result and design note, then refresh origin/main, AGENTS.md, canonical plan/current state, live issues/project items, artifact identities, the shared ledger and free space. PR #1322 is merged as `89ee803b`. The contextual utility run preserves every old artifact and the old `positive=false` scope, and adds its own preregistered positive for the *strength* question only.

The five stages remain useful contextual access and utility; structural persistence; dependent reads/composition; broader language and executed Rust; qualified scale/delivery. Finite H4/Hopf/spin connections are active. S7/E8/harmonic memory is conditional on the limit it addresses. Cost profiling happens throughout.

Necessary local allowance extensions are owner-authorized when recorded before use. Preserve the 128 MiB stop margin and machine reserve; no paid/external compute or destructive unique-data cleanup. Deliver via protected PR and synchronize public claims and local knowledge snapshots.
