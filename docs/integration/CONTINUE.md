# Continue UOR-R4 geometric model research

Read the [principal review after PR #1310](query-read-review-2026-09-20.md) and [complete next DeepSeek prompt](deepseek-separable-attribution-step-2026-09-20.md), then refresh origin/main, current authority, live GitHub and resources. **The next task is frozen S attribution, currently NOT_RUN.** Do not start another training run.

E/Q/S/L dev CE is7.170816/7.141506/7.032228/7.101049. Q is a valid negative at the prescribed dose. S is the strongest numerical candidate, but its conditional older-prefix penalty−0.002298 [−0.014072,+0.008581] supplies no detected history benefit. Its local-query dependence is positive. Preserve the measured improvement while separating individual history, query and constant calibration.

The prompt evaluates exact identity corners, direct zero-row conditions and a fit-only length-conditioned mean-history comparator, with old-panel reproduction and a predetermined nonoverlapping position panel in the same existing dev documents. No refit, new corpus, width/precision or reset mechanism. It repairs zero CPX3 tokenizer metadata and redundant inference folds with unchanged numerical fields/output. Full CPQK production continuation is still unimplemented; the mini check only establishes narrower parameter-state parity. Source, timing and cycle claims are corrected in the review.

Freeze retained S at `query-read-2/artifacts/separable_older_query_read.cpx3`, SHA256 `9f8cda09bfb562221b5718a8330cc4a296215b8e94d3e0c2b8abe2ce31fae851`, above corrected E `565cf98a…`. Re-export metadata-only descendants into a new root; preserve every old report and artifact. Selective maintenance/reset and exact occurrence/version memory remain future steps connected to the shared geometric language-model roadmap.

Live resource snapshot 176138565/178100000 ms leaves 1961435 ms. Preserve the 5900000-ms mixed debit. The next complete projection is 3600000 ms, with proposed standing-authorized +2400000-ms limit increase to 180500000 ms, **not applied by this review**. Refresh and record before use, preserve the 128 MiB margin, owner checkout and research. Deliver through protected PR and update current issue bodies/project items; #973/#820/#963/#964 stay open.
