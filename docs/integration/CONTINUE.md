# Continue native geometric AI work

Read [AGENTS.md](../../AGENTS.md), the [project plan](project-track.md), the
[current implementation](current-state.md), and the
[architecture/source audit](architecture-2026-09/README.md). Reuse its supplements
for the mechanism being changed; do not repeat the whole scan before each build.
Refresh `origin/main` and relevant
live GitHub state. The current owner request determines scope; this template
cannot narrow an explicit whole-plan request to one historical issue.

```text
$uor-project-workflow

Continue UOR-Foundation/uor-r4 toward the native geometric AI plan from refreshed
origin/main and live GitHub. Read AGENTS.md, docs/integration/project-track.md
and docs/integration/current-state.md. Reuse the source audit at
docs/integration/architecture-2026-09/README.md and inspect the concrete source
before adopting a mechanism. Work in an isolated full worktree and
carry the authorized objective through its necessary implementation tasks.

Use Rust throughout preparation, training, artifact construction and inference.
Training may use matrix multiplication. Keep prime/ordered-n-let addresses,
fixed zeta phases, R4/S3/H4 causal state, exact Z[phi]/orientation, the typed
paired-H4/icosian bridge and UOR identity primary. Final inference must execute
learned geometric operators through bounded routing/state/integer-table lookup,
with no matrix products, including lookup/add contraction, and no transformer
backbone. Deterministic geometric address/page selection is allowed. Shared typed
operators are current; expert gates are a conditional later option requiring
measured capability and total M1 cost. Preserve existing Python/dense
references as evidence, with no new Python model dependency.

Follow the immediate build sequence and audit's selected direction. Refresh the
retained checkpoint from current-state.md: the September review retained
PR #1160's d59070c2, descended from PR #1159's e7c14c99. Source/NoRead selection,
writer repair, exact NoWrite reuse, dependent reads and computed-role selection
have completed bounded results. Do not repeat them as missing work. The observed
remaining cross-domain failure admits a numeric answer where a supported location
or identifier is required. Implement one learned typed decision over legal
metadata alternatives before payload execution. Keep NoOperation and NoRead
distinct, calibrate the common choice, preserve correct cases and exact causal
provenance, and measure complete generated answers. Then advance shared contextual
transitions/emission and broader #1139/#1140 language and composition.

Preserve all parents, negative results, research and user material. Refresh the
cumulative model/storage ledger and current build cache before execution. Project
the complete preparation, build, fit, control, evaluation, retries, checkpoint and
retained-output costs, with wall-time, CPU/thread, RAM, storage and stop margins.
The audit's preliminary envelope and historical receipts are not fresh budgets.
Use one model process by default; coordinate bounded independent work only when
it saves effort. Continue in the existing task without an unnecessary process
framework or external cost.

Evolve the actual native model under #973, using existing correct components.
Develop conversation/memory and coding/reasoning on that same path. Select
meaningful configurable context/training/evaluation windows and charge work
cumulatively against the machine's wall/RAM/storage/thread/checkpoint budget.
Diagnose, correct and resume within the remaining budget; old 15-minute,
one-retry and frozen-window rules do not govern new development.

Compile and exercise the changed path; run focused meaningful checks rather
than blanket suites or proof paperwork. Preserve all historical results at
their exact scope. Report actual behavior and resource use, including failures,
missing work and the next action. Compatibility-only statuses are not test
evidence; inspect the actual native checks that ran. Deliver
through protected pull requests. Qualify model behavior and a complete capability
API before integrating the same native artifact into the GitHub Pages Studio.
Keep current-state.md as the single current implementation pointer. Do not claim a model or alpha before measuring it.
```
