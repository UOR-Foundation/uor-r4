# Continue UOR-R4 geometric model research

The [principal review after PR #1306](readout-result-review-2026-09-20.md) preserves the empirical head's development improvement and withdraws the claim that feature rank/capacity is the measured dominant bottleneck. On the fixed 36-document /288-window /17342-target panel, parent/E/S/float score **7.558429 /7.170816 /7.188585 /6.874144 bits/target**. The independently reconstructed paired S-over-E gain is −0.017769 [−0.033419, −0.001768]; float-over-S is +0.314440 [0.296844, 0.331694]. These are nominal open-development comparisons, not useful-language qualification.

The new heads use 4096 windows; the 512-window count reference is not their same-dose comparator. E's gap to the 4096 reference is about 1.232764 bits. E/S KLs concern different teachers; the float's implied KL against the actual S teacher is 2.866958. S/F used λ=(0.8,0.7), not the prescribed consumed-reference (0.7,0.5). Gate-code mistakes do not change this run's decisions. E/S exports contain a placeholder tokenizer digest and require numerical-preserving metadata correction and complete reloaded evaluation. Both hard heads still enter short pair cycles.

**The [ternary head-projection prompt](deepseek-head-projection-step-2026-09-20.md) has been executed** ([receipt](../evidence/native_geometric_head_projection_2026-09-20.txt)). With the parent's embeddings, integer features, bias and `F` frozen and only the output head changed, the existing quantizer **Q0** scores 7.416178 bits/target and one activation-aware dyadic ternary calibration **QG** scores 7.190872, against the incumbent gradient-trained ternary head **E** at 7.170816 and the floating head **F** at 6.874144 on the same development panel. **Both conversions fail** the predeclared practical screen against E, while the method-attribution screen **passes** strongly (QG is +0.2253 bits/target better than Q0). QG reconstructs F's scores 12.7x better than E and still predicts worse, so lower reconstruction error does not imply lower CE. **E remains the stronger qualified numerical baseline**; the earlier capacity-diagnosis reading stays withdrawn and neither ternary infeasibility nor a need to widen is established.

**One next architectural step:** return to a **query-conditioned geometric state read/update** on the strengthened local baseline, with a separable-prefix residual and a matched local-only control at the same dose and panel. The falsifiable hypothesis is that conditioning the read on both the local query and the stored state changes the pairwise-logit effect in a way `Z(local,q) = Z_parent(local) + r(q)` cannot. Do not launch that fit here.

Read the complete prompt and review, refresh live resources/GitHub, preserve owner checkout and all artifacts, and use a full isolated worktree. D0-b and geometric priority remain unchanged.
