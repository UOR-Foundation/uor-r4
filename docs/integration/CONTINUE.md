# Continue UOR-R4 geometric model research

**Executed, all declared checks pass.** One learned state-conditioned lexical decoder serves through the retained scoped session (`OutputContract::StateLexicalV1`): it consumes the symbols it actually emitted and a content embedding of the selected evidence and consumed computation, says `it is`/`it was`/`at first it was` from the requested history, and reports `still`/`became` from the computation's actual effect. Learned **31/31** (fit documents 23/23, held-out source documents 8/8); retained copy contract 31/31; retained finite table on the same text **0/31**; recurrence ablation 0/31; context ablation 7/31; value-sensitive comparison 4/4 with `derived`/`history`/`prior_differs` held equal; ten in-process and ten cross-process restarts agree. [Result](state-conditioned-lexical-result-2026-09-22.md); [evidence](../evidence/state-lexical-2026-09-22.json).

**Retained at its original scope (PR #1349, principal review):** the earlier learned Copy/Insert/Stop interface remains an exposed four-word construction whose authored targets misused tense (`was` for the current value; `now` for computation). Its artifacts and the `RealizedV1` path are preserved. [Review](grounded-lexical-realization-review-2026-09-22.md) and [independent audit](../evidence/grounded-lexical-realization-principal-audit-2026-09-22.json) distinguish implementation, semantic validity, exposure and same-artifact scope.

**Next:** broaden the same decoder to declared source-separated prose/conversation with a larger learned vocabulary, and carry a computed result into executed Rust through the native path. Preserve exact memory/computation and versioned copying; reintroduce geometry only for a witnessed aliasing, interference or cost problem measured against an equal-information ordinary mechanism.

**Leadership and resources:** principal mathematical/ML/systems review governs the roadmap; DeepSeek has substantive design and diagnostic discretion. Record necessary local extensions prospectively, cumulative work and measured physical space. No arbitrary short timer or retry quota, no paid compute, and no deletion of unique artifacts. Learned behavior, exact infrastructure and supplied supervision remain separate.

Read the complete principal review and next brief, then recover live source, knowledge, PR status and resource receipts. Verify the protected principal repairs before depending on them. Treat the old realizer as flawed development supervision, preserve its artifacts, and complete the next coherent learned-language milestone through actual loaded behavior and protected delivery. The submitted12,600,000ms charge is already applied; do not charge it again. Use release cache, one compiler worker and CARGO_INCREMENTAL=0 unless a measured reason justifies another plan.
