# DeepSeek execution brief: learned relational access and content-dependent session control

September 21, 2026. Principal-directed successor to the reviewed PR #1340. You are an autonomous research contributor and implementation owner. Codex retains principal mathematics/ML/systems architecture and holistic review. Complete the integrated milestone below, including evidence, documentation and protected delivery; do not stop at compiled helpers or a proposed next experiment.

## Objective and authority

Build the **UOR-R4 Geometric Language Model**: a native, transformerless geometric state model with exact addressed memory and learned typed operators, toward useful conversation/memory and coding/reasoning on an M1-class laptop. The immediate objective is a learned relational query/control policy that completes a language-facing memory answer using newly supplied facts and a resumable owned state. Broad prose, frontier reasoning and complete-path energy savings remain unqualified.

Current owner instructions, AGENTS.md and the stable execution policy govern. D0-b permits bounded <=4-bit additive integer maps at final inference, executed through add/subtract/shift/table reads in the declared kernel; geometric routing and typed operators remain preferred. Offline Rust training may use floating point, gradients and matrix multiplication. No hidden transformer or runtime teacher/provider answer path. Preserve scoped legacy numerical contracts. Necessary local time/storage extensions are already owner-authorized: project the complete increment and record the new cumulative allowance before use. No paid/external compute or destructive deletion is authorized by that clause.

You may change the illustrative design, choose a stronger learning method, investigate an unexpected result and continue necessary successive implementation steps. Explain consequential deviations using the source, mathematics and development evidence. Keep causal boundaries, artifact integrity, held-out separation and honest claim scope; those are not optional thresholds. Practical quality screens may be revised prospectively with reasons, while preserving old results. Do not turn a near miss into an endless minor calibration campaign.

## Recover the actual reviewed state

Refresh origin/main and verify that PR #1340 has merged with principal corrections. Inspect its changed source and merge-tree contents; do not assume the original `17d5072c` is the delivered state. Use an isolated full worktree and preserve the owner's checkout and all unique artifacts.

Read completely:

- [Principal grounded-session review](grounded-session-review-2026-09-21.md), this brief and the corrected [result](grounded-session-result-2026-09-21.md).
- README -> [canonical plan](project-track.md) -> newest [current state](current-state.md) -> [direction](model-direction-2026-09.md) -> [project map](../PROJECT_MAP.md).
- [Mechanism synthesis](geometric-attention-mechanism-synthesis-2026-09-20.md), [structural/Hopf direction](structural-memory-hopf-direction-2026-09-20.md), and relevant [architecture source audit](architecture-2026-09/README.md).
- The original and corrected principal audit/check JSONs under `docs/evidence/grounded-session-*`, plus actual retained report manifests. Historical `next` paragraphs do not override the current plan.
- AGENTS.md, DECISIONS.md D0-b/D1/D2/D3, stable execution policy, live model-time JSON, storage receipts, and live issues #820/#973/#1139/#962/#963/#964.

Use the source-bound knowledge store to recover connections quickly: query `PR1340 principal`, `learned relational session`, `owned evidence`, `Hopf structural memory`, and the current brief title. Fetch relevant sources and verify against current files; old knowledge snapshots are evidence, not authority. Reuse the source audits and inspect relevant donor code rather than repeating the entire project survey. Record any unavailable tool/source honestly.

## What is settled and what is not

The regular Q8 factorizer derives mappings from observed development transitions. All 672 transitions are exact; actual program totals are 288/288 development, 124/128 length four, 64/64 reversal, matching the competent finite transition control. The newly fitted recurrence is 248/76/24 and has a different calibration/restart configuration from PR #1339. The 137-byte grounded artifact is not a whole-path or encoding-normalized compression win. Its 102 parameter estimate and the 1668-byte JSON comparator must not be mixed.

The repaired fixed chain is a component regression: exact-key retrieval, supplied record layout, two supplied primitive operations and host-prescribed scheduling. A first computed result changes a second query/occurrence and a derived answer. This is useful dependence; it does not learn what relation to seek or whether more computation is needed. The principal repairs use independently loaded objects, prefix-derived instructions, one-position source intervention, exact occurrence checks, saved snapshots and continuation of remaining work. An immutable captured payload remains valid after source eviction; a borrowed live reference requires its exact current version. Preserve these distinctions.

Do not restart quaternion fitting to improve a fixture whose observed transition graph is already solved. Do not add a fixed third hop and call it learned reasoning. The next gap is request/relation-sensitive access and content-sensitive action choice.

## Deliver one complete learned relational session

Choose a compact language-facing memory task with multiple plausible facts per entity/key and different relation or scope. For example, evidence may include supervisor, office, assignment, previous assignment and a terminal answer fact. A request must select the required relation, and a retrieved intermediate entity/value must influence the next query and whether to continue. Exact entity joins are legitimate; matching entity identity alone must not resolve the task.

Include cases with equal request length and similar local tails but different supplied evidence requiring different read/compute depths or an unresolved stop. A source change must sometimes change only the answer, sometimes change the next relation/occurrence or continuation decision. Some requests should emit after one read, others continue, others lack a required fact. Use coherent task semantics; avoid manufacturing a meaningless program whose hidden fixture index already determines its schedule.

The same artifact must handle new supplied worlds/record assignments, familiar relations in new compositions and at least a declared set of varied request renderings. Separate development fitting/model selection from a fresh final draw after the design is chosen. Hold out worlds/source assignments and relevant compositions, not merely values with the same programs in every split. Report authored grammar/layout, tokenizer vocabulary limits and any supervised intermediate actions openly. A bounded declared input format is acceptable for the first milestone; it is not broad learned language understanding. Include actual decoded complete answers, not only numeric state or action accuracy.

Trainable interfaces may be small finite tables, discrete coordinate searches, bounded integer shared maps or other justified native mechanisms. A useful starting hypothesis is

```text
q_next = T_theta(request observation, retained result, role/scope, control state)
score(candidate) = F_theta(inverse(q_next)*key_descriptor,
                          exact identity compatibility, type/version observations)
action = Policy_theta(request, retained operands/result, candidate/read status)
```

These equations describe responsibilities, not a mandated architecture. Keep exact occurrence identity and payload ownership outside lossy semantic descriptors. Allow learned geometric transport to form a query without requiring every world relation to be a group action. A supervisor relation may be many-to-one; typed reads and nonlinear updates handle that. Separate candidate admission from ranking; all comparator arms receive the same available evidence unless the experiment specifically tests admission.

Learn a content-dependent policy over the useful subset of `Read`, `Apply`, `Emit`, `Stop` and explicit unresolved outcomes. Do not give serving a gold hop count, target, fixture family, correctness bit, next-source pointer or supervised record. Gold intermediate actions may supervise offline training if declared. A real bounded execution cap must report execution-budget exhaustion, separately from observed program completion and learned successful Stop. Consider answer utility plus operation cost for policy learning; ensure cost pressure cannot win by always abstaining. You may stage interface/teacher-supervised control learning before joint refinement if this gives clearer and more reliable credit assignment.

## Session and source ownership

Use one causal incremental runtime from observed input to action, read, update and actual emission. Teacher forcing, autoregressive generation, interventions, timing and reload must call that path. Design the frame so the required request/control state, owned operands, computed state, pending action and output position survive serialization. Restoring a frame must continue remaining work without rebuilding the answer or silently rerunning the first read in a fresh host loop.

Inspect donors before duplicating them:

- `native_geometric/learner/grounded_session.rs`: constructive Q8 action and current owned payload/frame.
- `native_geometric/addressed_attention/objects.rs`: model/geometry/session-bound private leases, copied bytes and exact references; copied payloads remain authoritative after ring eviction.
- `native_geometric/dependent_language/scheduling.rs` and related operation/continuation modules: learned actions, feedback, cursor, ownership and intervention interfaces. Their authored byte/clause grammar is not a learned BPE adapter.
- Current competitive reader/relation/policy modules: exact candidate occurrence, relation encoding, hard causal features, NoRead and output calibration. Preserve previous fit/serve and loader repairs.

Bind a production-facing snapshot to the actual model/geometry/tokenizer and session namespace it needs. Decide explicitly which fields are immutable owned values, which are current references, and which are derived cache. Revalidate a borrowed reference when dereferenced; do not invalidate a safely captured immutable value merely because the ring advanced. Check same-value replacement, wrong session/model and pause after a first read or computation. Avoid importing a whole legacy framework when a small reusable boundary suffices.

## Test the claimed mechanism, then move forward

Before an expensive fit, demonstrate on a few instrumented development cases that the task requires relation discrimination, content-dependent continuation and current evidence. Establish an ordinary capable relational/controller comparator using the same observations, supervision, candidate pool, output choices and comparable capacity. A fixed two-hop schedule, exact-key-only reader, and arbitrary categorical relation encoding answer different questions; choose the controls needed for each claim rather than handicapping one baseline.

After fitting, evaluate actual independently loaded artifacts and save item-level events with source references, query/control observations, actions, states, emitted tokens and terminal reasons. Test meaningful causal lesions and interventions:

- Change exactly one first source, keep downstream records fixed, independently derive the expected later query/source/answer; record the changed byte/token positions.
- Change request relation or scope while holding entity and evidence constant; verify the selected fact changes as required.
- Remove a required record, add plausible distractors, disable reads and disable selected-context update; record correct unresolved behavior and loss of dependent success.
- Disable/freeze query transformation or continuation policy while preserving other components; distinguish a real learned use from a redundant feature.
- Save and restore mid-session, resume only pending work, interleave two independent sessions, and exercise exact-reference versus owned-copy behavior under ring overwrite.
- Compare loaded/unloaded output and control traces, actual artifact bytes and source/configuration hashes. A loader call whose return value is not used is not loaded serving.

Keep the tests proportional to causal risks; no blanket full suite or repeated sealed-panel campaign. Report per-world/program successes, paired differences where appropriate, source admission/selection, computation, continuation and emission failures separately. A confidence interval over repeated items from one world does not establish new-world generalization. For this development milestone, one successful complete learned case can justify continued engineering, but capability promotion requires the declared broader held-out panel and controls. Retain a useful geometric component even if the full system fails another criterion.

## Wider mathematics remains available

Use signed H4/Spin and retained Hopf fiber for ordered relational distinctions; structural role/scope state for repeated entities, corrections and multiple concurrent relations; finite harmonic or scalar coefficient fields for measured discrimination/sharing; exact prime/UOR identity for addressing. Orthogonal mathematical functions do not automatically assign learned roles or prevent finite precision/capacity interference. Geometry is a candidate inductive bias, not a proof of semantic relevance.

Paired-H4/icosian E8 or S7 can supersede a smaller state when you identify a concrete erased distinction and a plausible equal-cost advantage. Preserve the coupled companion, maps, orientation and inverse witness. Do not build a general S7 field merely to attach the latest mathematical name. Conversely, do not exclude a well-motivated mechanism just because a dated experiment omitted it. Present the mathematical connection, smallest informative implementation and falsification control before expanding it.

Review relevant primary literature and source only as needed to decide the mechanism. Starting points are QuatE for relational quaternion scoring, SpaceE for noninjective-relation limitations, PonderNet for learned computation allocation, and the May2026 geometric-memory paper's distinction between in-weights and in-context graphs, linked in the principal review. New compressed-memory papers can inform sparse access without importing dense kernels. Explain what is actually reused and which claims do not transfer.

## Resources, autonomy and delivery

Refresh free space and the authoritative cumulative JSON. Record complete preparation/build/fit/development/retry/final evaluation/checkpoint/delivery projection, CPU/threads, RAM, temporary and retained storage and physical reserve before costly work. Use the standing authorization to extend necessary local allowance prospectively; retain cumulative charges and the 128 MiB storage stop margin. No fixed 15-minute cap or one-retry quota. Diagnose and fix worthwhile failures, checkpoint cleanly when needed, and avoid spending unused allowance without a research reason. Prefer existing valid binaries/caches. Preserve all unique models, negative candidates, source, reports, other agents' work and Downloads; remove only verified disposable build/cache material when justified.

Compile and exercise changed Rust paths. Use focused arithmetic/causality/serialization/interface tests, formatting and claim wording. Report existing warnings/unrun checks accurately. Five compatibility CI names execute no tests; local evidence carries validation. Claim report roots exclusively before model load, seal complete attempts, never add derived inputs under sealed roots or reuse a failed root.

Deliver through a protected PR; never push main or bypass protection. Update claims where asserted: README/front doors if needed, canonical plan/current state/result/EVIDENCE, live owner issues and knowledge store. Use References, not Closes, for incomplete programme acceptance. Inspect merge source/tree equality if reporting merged delivery; preserve owner checkout. Record exact retained artifacts and report paths, test commands/results, budget, free space, remaining limits and one evidence-supported successor. Make the final response state what the learned model actually emitted, why you think the mechanism helped, what failed, which decisions you changed and why. A clean PR of helpers without the complete executed session is a partial result, not completion.
