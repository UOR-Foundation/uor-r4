# DeepSeek next step: learn a discriminating, consistently served geometric emitter

You are a research contributor to the UOR-R4 Geometric Language Model. Execute
this step through implementation, bounded fitting, actual loaded generation,
evidence-scoped documentation and protected GitHub delivery. Use your judgment
inside the objective; explain a better mechanism before adopting it. Do not
stop after compiling helpers, and do not run an open-ended parameter sweep.

## 1. Recover the current state and authority

Refresh `origin/main`; verify PR #1335 actually merged and compare the reviewed
files. If it remains open, inspect its latest head and principal correction
rather than assume this submitted handoff is delivered. Preserve the owner's
checkout, unique artifacts, sealed roots and negative candidates. Use an
isolated full worktree and an explicit `codex/` branch.

Read AGENTS.md, README, canonical plan, current-state, execution policy and
D0-b/D1/D2/D3, then these required documents completely:

- `docs/integration/contextual-emission-review-2026-09-21.md`
- This execution prompt.
- `docs/evidence/contextual-emission-principal-review-2026-09-21.json`

Use the mechanism synthesis, structural-memory/Hopf direction, project map,
prior read-conditioned review and targeted earlier mechanisms when needed.
Query the UOR knowledge store with provenance; do not treat a stale retrieved
“next” or result claim as current authority. The latest principal correction
supersedes the submitted capacity diagnosis. Inspect the actual source of any
borrowed operator. Source/evidence and live issues outrank a summary.

Rust training may use floating point, gradients and matrix multiplication.
Final serving targets bounded geometric table/routing/state operations and
<=4-bit additive maps; no floating-point/transcendental numerical kernel or
hidden transformer/provider. Ternary maps are mathematical linear maps even
when implemented by adds/subtracts. Name their actual parameter traffic and
storage. Geometric routing remains preferred, not automatically advantageous.

## 2. What the last run established

The paired task genuinely changes one older relevant value while holding the
query and recent suffix fixed. Saved aggregates report H4 35/180 development,
22/120 tune and 13/120 on the population originally called fresh; both members
are correct in 6/90 development pairs and 1/60 reused-final pairs. C120 gives
27/180, 16/120, 12/120 and 0/90, 1/60. Keep the actual uncopied-output samples.
These are narrow authored-task signals, not reliable transformation or prose.
All previous draws are now exposed development/regression data.

The principal audit found and repaired concrete defects; review the patch:

- Accepted ternary coefficients could be reverted while the improved score
  remained cached. More passes did not prove convergence.
- Raw fixed-point logits were exponentiated as nats; old enormous “bits” are
  not calibrated CE. Float fitting used shift zero while serving used ten.
- Training extraction and serving used different read entrypoints. Any read
  was reported as source success; correct occurrence/value were unmeasured.
- Exports were loaded after evaluation and discarded. The repaired runner
  now loads before evaluation and compares actual predictor outputs.
- The residual arithmetic is shared signed-add/subtract/shift code; this is
  still not a whole-path machine-instruction or energy qualification.

The original embeddings **are byte-identical** despite nominal seed names;
`seed | 1` makes them identical. Do not introduce a false initialization claim.

Most importantly, decoded H4 value codes are `[22,22,11,22,4,89,89,27]` for the
eight payloads: the learned map merges answers requiring different outputs.
C120 also aliases values. Conditional on correct paired sources, those merged
values produce the same updated state and cannot be separated by any wider
head. H4 has 14/90 development and 10/60 reused-final pairs of this type in the
source-reconstructed fixture. These are not measured reader-correctness counts.

A development-chosen constant output scores 20/120 on the reconstructed reused
panel, above H4's reported 13/120, while solving zero pairs. Compare both paired
context use and useful accuracy; beating only the zero-hit frozen local prior
is an insufficient standard for a useful transformation.

All four six-file seals verify. Attempt 4 matches all four submitted source
hashes and its executable. The aggregate reports omit per-position sufficient
statistics, so do not claim the audit numerically replayed their full scores.
Two artifacts per arm are necessary but not a self-contained model package:
mode, parents, group, tokenizer, scales, data and selection must also be bound.

## 3. Research objective and initial scope

**Learn a shared emitter that preserves the relevant payload distinction and
actually changes an uncopied generated answer when older evidence changes.**
Start with the existing 120-state, width-16 operator family and the fixed
reader/local prior. Correct the learner before interpreting capacity. Keep
ring/candidate bounds stable initially. Do not reopen the frozen S attribution
or extend the scalar-copy dose campaign.

Use a compact development fit and tune decision, then one untouched final
population after source, artifact, design and practical criteria are frozen.
Complete one output -> update maps -> output learning stage, or a better
bounded jointly coherent alternative. A source-level correction alone is not
this run's deliverable.

### One numerical and causal contract

Build examples from the same target-free causal prefix boundary used by
inference. Targets and intended-source annotations enter only training loss
and evaluation, never read selection or inference inputs. Record the actual
selected occurrence, payload position/value, relation, q0 and q1. Evaluate
end-to-end correctness AND the correct-source stratum; do not replace the
end-to-end denominator with an oracle-selected subset.

Set `shift` before training. Convert integer logits with the actual `f_bits`.
Check a small nonzero residual against the float/hard forward, not only a
zero-map example. Declare whether gradients optimize nats (with reports converted to bits) or bits
(with the corresponding 1/ln(2) gradient factor). Keep tie handling,
clamp/saturation and active row support
identical. The known incumbent counterexample must remain fixed. Recompute
the committed objective after every accepted block so score and parameters
cannot silently diverge.

Train for the deployed quantized model: use ternary forward with offline
latent weights/gradient estimates, or a correctly implemented discrete method
with adequate signal. CE or capped/hinge margins are defensible; unbounded
mean margin can reward already-correct rows while collapsing difficult pairs.
Show before/after hard losses, paired correctness and actual parameter changes.
Refit the shared output after update-map changes. Learning R at the existing
width is allowed if frozen random features are a demonstrated limitation.

Inspect value-code and updated-feature collisions at each stage. A codebook
initializer or regularizer that preserves distinctions is allowed when learned
from development data and declared; it must not encode the answer mapping by
hand. Do not force universal token injectivity into 120 states. The relevant
requirement here is that this task's observed incompatible outputs are not
silently collapsed. A lookup learned from labels is a comparator, not proof of
semantic geometry.

### Scope for an informed mechanism choice

You may replace the proposed optimizer, choose a capped loss, alternate the
fit differently, or learn existing-width features if a short derivation or
actual development counterexample supports it. Record the reason and compare
on the same declared inputs/budget. Do not follow a known-bad sketch literally.

The principal review identifies a conditional alternative: a state-difference
emitter `h(q*g)-h(q)` telescopes to zero over a complete finite orbit. If actual
source-correct examples require positive correction for the same target and
competitor around that orbit, widening cannot fix the obstruction. Save a
concrete witness before invoking this diagnosis. A smaller alternative is

```
d = inverse(q0) * q1
residual = h(d) - h(identity)
```

For C120 use `(q1-q0) mod 120`. This removes common-left frame nuisance and
preserves exact-zero NoRead. It may also remove useful absolute-frame content;
use it only with an explicit task/representation argument and matched control.
It is an existing finite-geometric bridge, not a new S7 memory or proof of
language invariance. If selected, compare at equal storage and report the
information change. Do not stack it with an unrelated dimension/reader sweep.

Width expansion is conditional on corrected, source-correct,
distinguishable-input evidence and an attainable-margin/feature restriction.
The crude max range is not such evidence. One bounded relaxation or concrete
collision/cycle witness is sufficient for the specific restriction it demonstrates;
a value-code collision justifies restoring distinctions, not widening a head.
No endless solver work.

## 4. Data, controls and a real decision

Retain old panels as development/regression. Make development/tune and final
combination boundaries explicit (for example source/query role × value
combinations with every required value/output class represented in training).
An arbitrary eight-value output mapping cannot be inferred for entirely unseen
values without additional structure: do not design an impossible holdout and
call it failed reasoning. State that this tests contextual binding and
transformation transfer, not learning an unseen arbitrary mapping.

The new final panel remains unevaluated until the design and fitted artifact
are fixed. A repeated seed after a change is no longer final. Use one fresh
draw; preserve failures rather than manufacturing a more favorable final.

Retain at least these useful comparisons on identical rows:

- Frozen local and scalar-copy parents.
- Actual ReadDisabled and UpdateDisabled full-logit parity with local.
- H4 versus C120 with matched initial bytes, capacity, observations and fitting
  effort; compare paired losses/answers, not just different sample totals.
- A development-fitted constant and a small ordinary categorical
  selected-value emitter using the same reader and observed inputs. Learn its
  table from development answers; do not feed it intended values at serving.
  This exposes whether geometry adds transfer beyond finite value association.
- Changed relevant source, changed distractor, and absent/incorrect-source
  cases; report correct exact source separately from correct payload value.

Before touching final data, state practical thresholds and their rationale.
A reasonable default component screen is at least 50% **both-members-correct**
on new combinations plus a clear paired improvement over the frozen copy/local
controls; also report against the learned categorical/value and C120 controls.
This is an engineering screen, not a theorem or alpha threshold. You may choose
a better prospective margin from development difficulty and costs. Preserve a
useful partial mechanism below it with explicit limitations; do not rewrite
historical pass/fail or lower it after seeing final results. Measure the frozen
reader's correct-source ceiling alongside the screen. If a concrete selection
error prevents it, retain the end-to-end failure and permit one narrowly
justified selector/input-contract repair, or identify that as the successor.
Do not make the emitter compensate for wrong sources by adding width.

Run actual loaded-artifact greedy continuations at equal horizons and score
the first token correctly. A correct first token is still not a coherent
response. Show complete short trajectories and failures. If proposing that
this operator replace or integrate with the existing language path, replay
ordinary text, present/absent/conflict and prior action controls against the
same parent; otherwise label integration/text preservation NOT_RUN rather
than imply it. No general language or energy claim from this authored task.

## 5. Reproducible artifact and receipts without a new framework

Use the existing report claim/seal/verify machinery. Claim each new output
root exclusively before loading models; never write below a sealed root.
Retain a compact row file with split/pair/source identities, observed prefix,
correct intended value/target, actual read and payload reference, q0/relation/
q1, feature signature, emitted IDs, target/competitor logits, calibrated loss,
control outputs and parameter/data/source hashes. Keep enough local logits or
immutable replay inputs to independently reproduce the principal comparisons.
Do not retain huge redundant vocabulary arrays where compact exact data suffice.

Bind E/S/reader/tokenizer/group, algebra, scale, code maps, row support, seed,
splits, training configuration and all changed source modules. Derive active
rows from the loaded output artifact. Evaluate and generate using independently
loaded objects, with actual before/after-load logit/read/state equality. Keep
serialization equality distinct from behavioral parity and from determinism
across different hardware. Include artifacts for both algebras and negative
candidates. Report actual byte storage, not theoretical ternary packing.

## 6. Reuse research with a concrete purpose

Read the principal review's first-principles argument and references. Relevant
primary sources are [LSQ](https://arxiv.org/abs/1902.08153),
[ParetoQ](https://arxiv.org/abs/2502.02631), and
[2026 ternary adaptation](https://arxiv.org/abs/2608.24469).
They motivate consistent quantization-aware learning, not adopting their
transformer architectures. The multiplicative 2026 adapter cannot reactivate
a zero coefficient; do not graft it onto this zero-map initializer unchanged.
Inspect actual code if borrowing a mechanism. Search further only where it can
resolve a specific uncertainty in this fit.

Use current/retired project donors: exact source ownership, finite signed
H4/Spin inverse/composition, QueryTrainer fixed-point learning, shared typed
Read/Update/Emit/Stop and historical dependent-language controls. Keep retained
Hopf fiber, role/scope state, paired-H4/icosians, normalized E8/S7 and harmonic
channels available for a witnessed distinction or lifetime bottleneck. Harmonic
orthogonality is not automatic zero crosstalk in finite quantized memory; hash
or prime identity is not a semantic metric. No supersymmetry/quantum analogy
replaces an executable learned operation.

## 7. Resources and delivery

Read the **absolute live** ledger at
`/Users/casey.allard/uor-r4/.uor-models/native-joint-learning-2026-09-04/model-time.json`
and current storage receipts, then record the complete preparation/build/fit/
controls/evaluation/retry/checkpoint projection before execution. Do not copy
a historical balance from this prompt. Necessary local allowance extensions
are owner-authorized: record reason, increment and full updated limit BEFORE
use, preserving cumulative debits. Update the actual JSON, not just prose.
No paid/external compute follows from that authorization.

Keep two build workers, one fit worker and an 8 GiB RAM ceiling unless a new
machine-based projection explains a change. Preserve the 36766079385-byte
physical reserve plus 128 MiB stop margin; allowance extensions do not create
free disk. Reuse binaries/caches where valid. Clean only confirmed disposable
inactive cache; preserve source, unique artifacts, research, intentional dirty
state and Downloads. Record before/after free bytes and exact removed paths.
Stop/checkpoint before limits. A small step-time probe is enough to bound the
new fit; do not turn timing into a separate campaign.

Run formatting, focused real-risk module/runner tests, offline touched-package
checks, actual generated behavior and claim-wording checks. Queue compatibility
checks do not execute tests. Preserve failed candidates and their exact causes.

Update current-state, canonical plan, result/evidence and README/ROADMAP where
claims change, plus the owning live issues #973/#820 and relevant #1139/#962/
#963/#964. Keep partial responsibilities open. Refresh project items if present;
do not invent a project-board update where no item exists. Use a protected PR,
never direct-push main/admin-bypass. Verify the actual merge and file/tree
identity. Import concise revision-pinned knowledge with supersession links.

End with what changed, what actually ran, exact artifacts/rows, scoped outcome,
controls and failures, physical storage, reconciled cumulative resources,
delivery verification and **one evidence-supported successor**. Useful
single-read emission precedes dependent scheduling; if this corrected fit
fails, identify the concrete observed failure rather than automatically adding
width or restarting generic scalar calibration.
