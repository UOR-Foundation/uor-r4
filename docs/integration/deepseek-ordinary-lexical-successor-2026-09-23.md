# DeepSeek next research step: diagnose and improve the shared lexical state

You are a substantive research partner on the UOR-R4 Geometric Language Model. Work from the live protected repository, not this dated brief alone. Refresh `origin/main`, read `AGENTS.md`, `docs/integration/agent-execution-policy.md`, `DECISIONS.md`, `project-track.md`, `current-state.md`, the [principal review of PRs #1353/#1354](ordinary-lexical-principal-review-2026-09-23.md), original [result](ordinary-lexical-result-2026-09-23.md), and the relevant source, sealed receipts and issue #820/#973. Use the project knowledge store and prior mechanism/evidence records when a design choice depends on them. Reconcile any newer merged work before execution. Preserve the owner's checkout and all unique/sealed artifacts; use an isolated full worktree and protected PR delivery.

The owner objective remains a useful transformerless geometric model for conversation, durable memory, reasoning and Rust coding on M1-class hardware, ultimately with lower measured energy. Rust training may use floating point and matrix products. Serving may use bounded low-bit additive linear maps under D0-b, but geometric address/state/selective work is the preferred architecture; do not disguise dense transformer operations as lookup. Exact occurrence/version identity and source ownership remain explicit. No current result qualifies general prose, chat, geometric superiority or whole-path energy savings.

## Why this step is different

PR #1353 provides a single 4096-token Generate/Copy/Stop learner that beats unigram but loses 7.3722 to 5.1217 bits/target against a tuned exact two-token count predictor on open development text, and greedy continuations collapse. This gap does not diagnose missing long-range geometry. The current 64-coordinate recurrence sees an eight-token prefix but may not retain exact local IDs. Its final 300 training steps are grounded-only. Its post-copy preflight changes evidence fingerprint and copied feedback together; the learned role of emitted-token feedback is unproved. The original runner evaluates most headline panels before deserializing the artifact; the separate-process replay covers only 36 fitted training cases. The principal review also corrected a served bias-overflow validator defect. Do not build a more elaborate H4/S7 reader solely from a claimed “context/order gap” until a local failure is witnessed and the causal/lifecycle instruments are sound.

## First deliverable: decisive instrumentation on the existing path

1. Load the *saved* artifact through `from_bytes` in a fresh process for every qualification panel. Compare action logits, selected actions, emitted tokens and state digests against the original quantized instance for the 32 temporal training cases, both class cases, the three exposed compositions, prose rows, generation and each mixed phase. Record tested source tree and executable hashes as well as artifact/tokenizer/data identities. Preserve the old roots and old results; put new derived evidence under a newly claimed root. Distinguish independent rollouts from a mutable session, and do not label exposed/fitted cases fresh held-out acceptance.
2. Make the post-copy causal test genuinely single-variable. Hold selected-source fingerprint `m`, typed facts, prompt, copy legality and chosen Copy action fixed; intervene on only the token passed to `transition` after Copy, then measure the next vocabulary logits/action. Cross it with the converse `m`-only intervention while feedback stays fixed, plus no-copy and source-disabled controls. The desired result is not a particular score: truthfully classify whether the learned policy uses emitted identity, source fingerprint, both, or neither. Do not allow the first blinded action to change from Copy to Generate and still call the next action “post-copy.” If the learned signal is absent, train it within the shared path with exact observed feedback, not an answer override.
3. Report full-action NLL, token-conditional NLL given Generate, and Stop mass/quality separately. The current count/donor references are token-only; make the comparison denominator explicit. Recompute at document level with the pinned same targets, and retain the current count reference as a strong competence floor. Measure or bound all coefficient inspections, table bytes, allocation and whole-path latency separately from the rollout-only timer; energy stays UNAVAILABLE unless physically measured.

## Second deliverable: one informative model successor

Use a small diagnostic to choose where to spend fit time: recover the checkpoint before the grounded-only final phase if available (or a faithful bounded rerun), stratify loss by context frequency and prefix position, measure hidden-state clamp saturation and last-two-token decodability/counterfactual swaps. Compare to the exact-ID count reference. One strong hypothesis is that a direct bounded last-two-token channel to the readout, while keeping the recurrent state and exact memory, will recover local identity/order at little cost. Fit an equal-data, equal-budget ordinary control with that channel or another better-founded local diagnostic you can justify; preserve the baseline artifact and candidate separately. Report a fresh source-separated text evaluation after design selection, actual continuations/termination, the same grounded controls, and paired uncertainty. If optimization or curriculum is the cause, fix that rather than claiming architecture wins. If the local channel succeeds, retain it as the local-recency component; exact memory and geometric relational state can then handle structure/distance. If it fails, diagnose why before widening context or representation.

You may depart from the suggested control or mechanism if a stronger first-principles argument and inexpensive falsification show a better path. State the alternative, its predicted observable signature, a matched nongeometric comparator, and what result would reject it *before* the expensive fit. Do not let bookkeeping conventions or arbitrary session timers truncate necessary authorized work. Project wall time, CPU, RAM, temporary/retained bytes and physical reserve from the live cumulative ledger before each costly run; record any necessary local extension prospectively under the owner's standing authorization. Never incur paid compute or delete unique research. Checkpoint when an actual machine ceiling is approached.

## Geometry and roadmap decision

After local competence is measured, a signed H4/shared relative-transport operator is the first geometric candidate for a witnessed order/role or interference failure. Give it the same causal inputs, parameter/storage/compute envelope and fit exposure as an ordinary state or role-bank comparator. Preserve both sign and ordered relation; do not quotient away an answer-relevant distinction. A state update must alter a later uncopied vocabulary decision, not only select a payload. If a finite or categorical control ties or wins, retain the useful mechanism but do not promote unique geometric advantage. Hopf S3/S2 with retained fiber, quaternionic S7/S4, E8-root codebooks, harmonic channels and phase/spin remain conditional tools if relative H4 and explicit role banks fail a specific measured distinction. Revisit them if your analysis discovers an actual injectivity, capacity, interference or transition need.

Integrate useful advances into the same native model/session rather than a parallel demo. Sequence after a competent local learner: changed-source and versioned memory, role/scope structural persistence, one dependent read or computation that changes a noncopy output, then broader prose/chat and Rust coding, followed by full laptop quality/cost/energy qualification. The roadmap is evidence-driven: you have latitude to reorder when a decisive experiment warrants it, and must explain the revised dependency.

Deliver source, focused executed Rust tests, new sealed evidence with complete file set, artifact/executable/data lineage, truthful positive/negative/NOT_RUN claims, updated current-state/roadmap/README where claims changed, cumulative ledger and owning GitHub issues. Use `References #820` / `#973`; close nothing without its complete acceptance. Push through protected PR, verify actual merge/tree or report it open. In the handoff, lead with what changed in the model's measured ability, what failed, why you chose the next mechanism, and the one remaining decision for principal review. Use your long-horizon judgment; do not stop at a diagnostic if you can safely produce and evaluate the coherent successor.
