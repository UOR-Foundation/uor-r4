# DeepSeek execution brief: observed-text memory answers in one reusable session

September 21, 2026. Principal-directed successor to reviewed PR #1341. Codex retains principal mathematics, ML and systems architecture and holistic evidence review. You are an autonomous research contributor: choose the concrete representation, learning method and development sequence, troubleshoot unexpected failures, and complete the executed milestone and delivery. This is not a short-timer script or a request for compiled helpers.

## Objective and authority

Build the **UOR-R4 Geometric Language Model**, a native transformerless geometric state model with exact addressed memory and learned typed operators, toward useful conversation/memory and coding/reasoning on consumer M1-class laptops. The immediate milestone is **complete observed-text memory answers with evidence-dependent continuation**, through one reusable owned session. Broad prose, general reasoning, frontier capability and whole-path energy savings remain unqualified.

Current owner instructions, AGENTS.md, DECISIONS.md D0-b/D1/D2/D3 and the stable execution policy govern. Offline Rust training may use floating point, gradients and matrix multiplication. Final serving uses bounded geometric routing, typed state and integer/table operations; D0-b permits bounded at-most-four-bit additive maps under its declared numerical contract. Geometric mechanisms remain the preferred research path. Do not hide a dense transformer, provider answer or teacher at serving. Preserve scoped frozen contracts and existing evidence.

You may choose a simpler or better mechanism, alter the illustrative task wording, use declared intermediate supervision, diagnose and correct a learning failure, or extend necessary local time/storage prospectively under the standing owner authorization. Explain consequential choices using source, mathematics and development evidence. No arbitrary short cutoff, one-retry rule or demand to spend all remaining allowance. Preserve real hardware reserve, cumulative charges, unique artifacts and the prohibition on paid external compute. Practical thresholds may be revised prospectively with reasons; preserve historical outcomes. Causal independence, accurate metrics and fresh-final separation cannot be waived by relabeling a result.

## Recover the reviewed state, not just the attachment

Refresh origin/main. Verify PR #1341's corrected head and merge status against actual files. If protected delivery is still pending, inspect the exact reviewed head and base explicitly; do not use the original `84ba72ce` as if it included principal repairs. PR #1340 merged as `d9d896e8`, with the complete tree equal to reviewed `5821c48d`. Use an isolated full worktree; preserve the owner's checkout and negative/positive artifacts.

Read completely:

- [Principal relational-session review](relational-session-review-2026-09-21.md), this brief and revised [result](relational-session-result-2026-09-21.md).
- The original and corrected `relational-session-principal-*` and `relational-session-corrected-replay-*` evidence/check records, plus the actual retained report manifests.
- README → [canonical plan](project-track.md) → latest [current state](current-state.md) → [direction](model-direction-2026-09.md) → [project map](../PROJECT_MAP.md).
- [Mechanism synthesis](geometric-attention-mechanism-synthesis-2026-09-20.md), [Hopf/structural-memory direction](structural-memory-hopf-direction-2026-09-20.md), [grounded-session review](grounded-session-review-2026-09-21.md), relevant [architecture source audit](architecture-2026-09/README.md).
- Live cumulative model-time JSON and physical storage, policy and decisions, live issues #820/#973/#1139/#962/#963/#964.

Use the source-bound knowledge store for efficient recovery: search `PR1341 principal`, `observed-text memory session`, `relational session fixed offset`, `owned evidence`, `query participation` and `Hopf structural memory`. Fetch the full relevant sources and verify active claims against current repository bytes. Query the principal-review source's related records to find current issues, checks and next brief. A historical snapshot is evidence, not live authority. Inspect the specific donor source rather than repeating a broad archaeology survey.

## What the last run actually established

The submitted module learned relation/role code correspondence and a finite follow map from development traces. Exact entity admission plus matching picks the requested role among plausible facts. A changed first value changes the next entity, selected occurrence and answer. That is useful dependent memory behavior.

The original128/128 primary labels were independently reconstructed, but its continuation table started correctly and its training feature was copied from the action label. All original final panels were reused; relation identity alone predicted depth. The H4 test used central minus identity, not identity. Fixed score coefficients were called learned. Resume used hidden host-loop locals, origin checking used value equality alone, and second-read events were omitted. The principal correction repairs these boundaries, versions the model/frame semantics, retains all-arm evidence and adds a small same-request content diagnostic. Read actual corrected results and limitations before designing the next fit.

The corrected learned policy uses a **supplied entity registry**. That registry is legitimate typed input for the current fixture, but does not learn what an ordinary sentence means. The original140-byte model also excludes shared assets and session state. The corrected frame uses versioned JSON and bound identities; no compact allocation-free serving claim follows. Step/finish currently live in the runner and should become a reused library boundary for this milestone.

For a fixed group element `c`, `a^-1*z=c` iff `z=a*c`. Original `c=-1`, corrected `c=1`: this is paired-label compatibility. Categorical/cyclic ties are expected. Do not run a capacity/seed campaign to make that fixed-equality problem appear uniquely geometric. Use fuller relative/ordered geometry if a real contextual alias requires it, and accept a matched categorical tie when the integrated behavior is useful.

The grounded Q8 factorizer remains a reusable exact computation component; it solves an observed regular eight-state action, not arbitrary world semantics. Do not force office/ownership/redirect facts into bijective group transport. Keep exact facts and typed nonlinear actions alongside geometry.

## Build one meaningful language-facing task

Prefer a small domain with two ordinary factual goals, such as office and project assignment, and multiple plausible facts for each entity. Each goal can be answered by a direct assertion or by following explicitly expressed information references while retaining the goal. Illustrative observed text:

```text
Mara works in Cedar Annex.
Mara's office information follows Ivo.
Ivo's office information follows Nila.
Nila works in Cedar Annex.
Cedar Annex is managed by Oren.
Question: Where does Mara work?
```

Use coherent worlds: the direct and redirect forms above illustrate alternatives, not an unqualified contradictory memory. You may choose clearer wording or another domain. Document/newline/record delimiters may be declared input scaffolding. The serving input must not include gold semantic role labels, subject/object/answer extents, gold relation IDs, follow pointers, next-source indices, depth, fixture family or target. A parser with authored semantic rules can serve as a comparator, not the claimed learned system. Declared bounded span proposals and exact string/span identity are legitimate mechanisms when their assumptions and costs are visible.

Required distinctions:

- The **same exact request and entity** must answer after one read, after two reads, and through an unseen familiar three-read composition in different supplied worlds. Development may expose the relevant primitive actions; final composition must be separate after design selection. Do not make request length or relation ID determine depth.
- An answer phrase may also be a key/subject elsewhere. “Cedar Annex” can be a terminal office answer even when it has other records. Continue/Emit must depend on the selected record's meaning and current goal, not token membership in an entity bank.
- A redirect updates the entity while preserving the requested goal. A request for another goal must select different evidence from the same world.
- Missing required evidence produces an explicit unresolved outcome; cycles or a bounded execution cap produce a distinct exhausted/cycle outcome. Neither is a successful answer.
- Multiword entities and answers retain full exact span identity and occurrence/version, not just their final BPE token. Include repeated words or shared suffixes to expose identity collapse.
- Emit the complete selected answer phrase and stop/EOS through the native runtime. An authored answer wrapper is optional presentation and cannot supply the answer or the success claim. Open-ended prose is not required at this stage.

Vary world assignments and a modest set of ordinary request/source renderings. Keep development open, select the design, then draw a separate final panel with new worlds and familiar structures in withheld combinations. Report novelty axes accurately: new names, new assignments, new wording and new compositions are different claims. A repeated panel can remain a regression but does not become fresh because its report directory is new.

## Reuse a single session and learned observation boundary

Extract or adapt the corrected `rel_step`/`rel_finish` and `RelFrame` responsibilities into a small reusable Rust boundary. Request observation, candidate/source-role representation, selection, owned capture, goal/entity update, continuation and emission must execute through that boundary. Teacher-forced traces, normal generation, interventions, timing, independently loaded models and resumed sessions must call the same causal implementation. Do not write another separate host-driven answer loop.

At any pause, the frame must retain the goal, exact current query/span identity, owned source/result, pending action, read count and emission cursor needed to continue. Restoring it must perform remaining work without rereading an earlier source merely to reconstruct hidden locals. Bind model/geometry/tokenizer and session/world identity; distinguish namespace from evolving record version. Current-reference dereference checks the actual occurrence/version; an owned immutable captured span remains usable after overwrite. Candidate admission must carry the selected exact occurrence directly, including duplicate equal-value records, rather than rediscover identity by payload equality.

Prepare immutable model/geometry/tokenizer bindings once in the loaded runtime context, then validate the small session identity against them. The current experiment recomputes serialization/hashes during each step; preserve the checks without treating that scaffolding as the final efficient kernel. Report preparation separately from per-action work and avoid a new optimization campaign before useful behavior.

Use one observation extractor for fitting and serving. Gold action/span/answer labels may define offline loss; they must not manufacture input features. Consider supervised action/span learning first, then output credit where needed, instead of an unnecessarily fragile joint optimizer. Report what actually changes from initialization, pre/post behavior on the same units, train data and seed/configuration, and any fixed components. Fit every learned comparator fairly against the same observations and supervision.

## Source donors and how to use them

Inspect these interfaces before inventing replacements. Reuse responsibilities and missing distinctions, not incompatible historical artifacts or an authored grammar presented as learning.

| Donor | Useful responsibility | Boundary to adapt |
| --- | --- | --- |
| `learner/relational_session.rs` and runner `rel_step`/`rel_finish` | Bound owned state, learned correspondence, evidence-dependent action phase, actual restore | Typed token registry and supplied triples become learned observations over current text; move shared execution out of experiment-only code |
| `source_routing.rs::SourceRouting::encode/score` | Ordered two-lane H4 accumulation and full relative-rank scoring | Existing feature vocabulary/artifact differs; equality mode alone adds no richer geometry |
| `dependent_language/occurrence_role.rs::source_context/query_context/fit_paired` | Contextual role separate from exact canonical identity; paired source/query fitting | Historical grammar and unknown-neighbor aliases remain explicit |
| `query_participation.rs::route/allow_update/required_mask/replacement_mask` | Required evidence and legal replacement are separate roles | Do not collapse them into one role bit |
| `dependent_language/runtime.rs::updates_with_window/splice_with_window` | Candidate query updates and boundary-preserving entity replacement | Old ASCII/byte and small-record assumptions need explicit BPE/span adaptation |
| `span.rs::candidates`, `span_learning.rs::prepare/fit/refine_boundary`, `span_boundary.rs::learn_from_compatible/complete_run` | Bounded phrase candidates, source-compatible boundary credit and complete extent | Fixed three-word/fifty-byte limits and global lexical barriers are historical, not semantic truth |
| `scheduling.rs::observe_routed_updates/execute/generate` | Causal action observations, legality, selection and persistent control | Authored clause counters/continuation logic cannot become an undeclared oracle; reuse in the one current loop |
| `recurrent_text/runtime.rs::execute_with_update` | Read/Emit/Stop, pending query, emitted cursor and EOS | Byte symbol256 EOS is not the current BPE contract |
| `addressed_attention/objects.rs::ObjectSession` acquisition/check/prepare methods | Exact provenance, private ownership and safe action preparation | Import only necessary semantics, not a redundant full object framework |
| `learner/grounded_session.rs` | Exact shared Q8 action where a task requires ordered computation | Memory assertions and redirects need not be invertible; arithmetic is not compulsory in this milestone |

If a donor is a poor fit, explain why and implement a smaller alternative. A no-op integration that loads an old model but never executes its mechanism is not reuse.

## Falsify the proposed explanation, then advance

Before a costly fit, show on a few transparent development examples that the task genuinely needs relation selection and evidence-dependent control and that the candidate pool can admit the answer. Diagnose admission, semantic representation, learning, control, source ownership and emission separately. A later scorer cannot recover an erased span or missing source.

Use a competent nongeometric learned comparator with the same accessible observations, span/candidate pool, supervision and comparable parameter budget. A relation-only fixed-depth controller should be evaluated as a specific shortcut control, not the sole baseline. For geometry-specific claims, compare ordered/full-relative features against appropriately fitted categorical or order-blind controls. Do not hold all useful progress behind a unique geometric win.

Save actual loaded generation and item-level actions, source occurrences, observations, state updates, complete emitted token sequences and terminal reasons. Include the causal controls that answer the mechanism's question:

- Change exactly one first selected source while keeping downstream records fixed; independently derive which quantities must change and which must remain invariant. Verify the resulting entity, selected source, depth and answer as appropriate, including preservation of the active goal across redirects. Different paired cases may isolate different consequences.
- Change request goal with the same entity/world; include irrelevant records and repeated names.
- Remove a required later fact, disable reads, disable the selected-content query update, and replace learned continuation with a capable fixed-depth rule. Preserve legal behavior in controls and label what each lesion removes.
- Demonstrate direct terminal answers whose text also appears as a key. Demonstrate one unfamiliar composition of familiar redirects and ordinary direct facts.
- Save a mid-read/control and mid-emission checkpoint, drop live state, reload and continue only pending work; interleave distinct sessions. Check same-value version replacement and an owned payload after source eviction.
- Verify actual-used loaded models, tokenization and source/configuration hashes; preserve complete report seals and all-arm raw events. Do not add files beneath a sealed attempt or reuse a report root after failure.

Choose meaningful final practical margins prospectively after development diagnosis. Report complete-answer counts and per-world differences, action/depth/span errors, unknown/missing/cycle outcomes and whole response behavior. Repeated requests within one world are not independent worlds. If the whole milestone fails but an operator helps, retain it with the exact limitation and select a causally justified successor; avoid endless tiny gate/threshold refinements.

## Wider mathematics and sequencing

The active need is contextual correspondence and goal-preserving state/action selection. Signed H4/Spin and retained Hopf fiber are candidates for ordered role/scope distinctions; exact prime/UOR addresses preserve identity; typed nonlinear maps handle replacement and nonbijective facts. Finite scalar/harmonic fields can support salience or separation if there is a measured interference problem. Orthogonal functions do not supply learned role assignment, infinite capacity or automatic selective recall.

Paired-H4/icosian E8 or S7 may supersede a smaller representation when a concrete lost distinction or equal-cost sharing opportunity is identified. Preserve the coupled companion, orientation, glue/maps and inverse witnesses. Do not confuse a normalized E8 vector with a freely independent S7 state or discard radius/fiber unnoticed. No quantum, supersymmetry or twin-prime capability claim follows from an analogy. The [principal review](relational-session-review-2026-09-21.md) links current primary research on context-dependent geometry, structural generalization and input/state interaction; consult actual source and additional relevant primary literature when deciding a mechanism.

After this observed-text memory path works, broaden the **same engine** to durable updates/current-versus-previous information, source-separated prose, and generated Rust that is actually executed. Scale routing, pages or spectral summaries when useful workload evidence identifies their bottleneck; profile complete latency, RAM and memory traffic throughout. Physical energy remains UNAVAILABLE until measured. This is a coherent roadmap toward a language model, not a declaration that these small tasks establish frontier capability.

## Resources, documentation and protected delivery

Refresh the authoritative cumulative JSON and free space before costly work. Project complete data preparation/build/fit/development/diagnostic retries/final evaluation/checkpoint/delivery, CPU/threads, peak RAM, temporary/retained storage and stop margin. Use standing authorization for necessary local allowance increments and record reason, increment and updated limit before use; do not ask again for this already authorized class. Charge cumulative work once with measured versus estimated components named. Prefer valid caches/binaries and preserve all unique artifacts, research, other agents' work and Downloads. Remove only verified regenerable material when useful. No paid external compute.

Compile and exercise the changed Rust path. Run focused tests for meaningful causal, arithmetic, source-identity, serialization, emission and interface risks, appropriate format/claim checks and only relevant broader checks. Five compatibility status names are not executed tests. Preserve warnings and UNAVAILABLE checks accurately.

Update the current-state/result/EVIDENCE and canonical plan as outcomes warrant; refresh README/front doors where claims changed, live owning issues and the source-bound knowledge store. Use References, not Closes, while programme acceptance is incomplete. Commit named paths, push a branch, use a protected PR and verify source/tree contents after actual merge. Preserve the owner checkout. Final handoff must state what the model emitted, what genuinely learned, useful failures and causal conclusions, exact reports/artifacts/source hashes, executed validation, resource balance/free space, delivery status and the single best next milestone. Finish the complete authorized work; a compiled helper or a tool result with no exercised session is partial.
