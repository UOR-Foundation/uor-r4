# DeepSeek execution prompt — first corrected real-text prior learning curve

> Historical execution prompt. PR #1300 executed the real-text fit. Use the [subsequent review](frozen-prior-review-2026-09-20.md) and [current evaluation-only prompt](deepseek-frozen-prior-step-2026-09-20.md) to recover the retained result; do not repeat this training run merely to repair its evaluation.

Work on **UOR-R4 Geometric Language Model**, repository `UOR-Foundation/uor-r4`. Read this whole prompt and `docs/integration/realtext-prior-review-2026-09-20.md`. The audited implementation is `a3c5403587bd27b32399622ec0bebc010447b9f2` (PR #1298); refresh `origin/main` and include the protected delivery of this prompt.

**Deliver the thin real-text runner and execute its bounded learning curve in this run.** The corrected V4 fitting gate already passed. Integrate the specific continuation/integer-trace repairs below, run their focused tests, then proceed into real text. Do not return only another “ready to train” report if the checks pass and measured resources permit. The floating comparator was conditional on small-gate failure and is not required now. Do not build another learner module or add a memory/geometry arm to this experiment.

## 1. Authority, recovery and complete projection

Read AGENTS.md → `docs/integration/DECISIONS.md` → execution policy → canonical plan/current state → parent review. Current owner D0-b/D1/D2 override older prohibitions: Rust preparation/training/export/inference; offline floats, gradients and matrix multiplication allowed; serving bounded <=4-bit/ternary add/subtract/shift/table maps, no floating/transcendental computation and no multiplier instruction in the declared numerical kernel. Geometric routing/state remains the goal. No transformer backbone, runtime teacher/provider or Python model. Frozen R4G1 retains its own scope.

Fetch origin, inspect relevant open PRs and #973/#820/#963/#964, checkout/worktree state, retained artifact hashes and resource/storage receipts. Reuse a clean isolated full worktree on `codex/…`; preserve the owner's checkout and other branches. Assign active work appropriately. No broad history census or old qualification replay.

Retained positive gate: owner `.uor-models/prior-learning-gate-2026-09-20/attempt-1`; CPL2 SHA256 `c020b1e131a615bc3ce2b2cf857d83b7465fe3c7c50002528bd85732cc6b928b`, checkpoint SHA256 `b90eff2df5898aca35712c8bbc18097fcd924a03d14e17dd43d523fd127519ef`. Preserve its sealed root. It is a toy artifact, not real-text initialization. Preserve CPR1 and other model families separately.

Snapshot shared ledger: owner `.uor-models/native-joint-learning-2026-09-04/model-time.json` = `151038565 / 154400000 ms`, remaining 3,361,435 ms. Refresh it. Proposed complete **3,000,000 ms (50 minute) tranche**: 900 s repairs/build/focused checks; 300 s data/reference/probe preparation; 1,000 s fitting; 500 s artifact evaluation/controls/generation/checkpoint I/O; 300 s evidence/delivery/stop reserve. These are estimates, not charges. Record the complete projection and revise it from the new-path probe. If necessary local work exceeds remaining allowance, use standing owner authorization: record reason, increment, new cumulative limit and complete revised projection **before** use. Do not ask again for this class of extension. Do not spend the reserve blindly.

One model worker; at most four Cargo jobs; <=8 GiB process peak RSS; <=1 GiB incremental build/data/checkpoint storage on a reused checkout/cache; retain the protected 128 MiB storage margin. Measure with `scripts/project_storage_inventory.py`, not just git status/free disk. Account separately for a new full checkout if unavoidable. Charge actual preparation/build/model/controls/retries/checkpoint work without double-counting overlapping elapsed intervals. No deletion, paid compute or external GPU. Distinguish local machine limits from your session/token limits in any stop report.

## 2. Finish the exercised primitive while building the runner

Use `native_geometric/learner/prior_learning.rs`, `lowbit_core.rs`, existing tokenizer/backoff/report helpers. Add one thin binary, for example `prior-learning-realtext`; refactor a small reusable helper if necessary. Do not copy the defective `cold-prior-pilot` training/CE/accounting implementation.

**Exact continuation:** CPCK v2 omits the shuffled `perm`, then `resume_from` clears it. Mid-pass reload consequently reshuffles and resets cursor. Persist the active permutation or sufficient state to reconstruct it exactly; include dataset length, cursor, pass and shuffle state. Version incompatible checkpoint changes. Restore/verify seed and the complete optimizer/numerical/frozen-bias identity. Bind one nonzero experiment identity to source/config, derived tokenizer and immutable data manifest; a sidecar verified by the loader/runner is acceptable if clearly specified. Never treat writing then ignoring a digest as verification.

Add focused continuation checks at **mid-pass**, at the partial final batch and across a pass boundary. Compare consumed window IDs, next batches, counters, masters, both moments, numerical/optimizer settings, bias and serialized continuation state, before and after subsequent updates. Use nondefault betas/decay. A saved final toy checkpoint that never used the scheduler does not establish corpus resume. Reject identity/config/bias/seed mismatches and invalid/nonfinite optimizer fields or moments before modifying live state; assert rejected-load equality of the whole state, not one vector. Validate stored permutation cardinality/range/uniqueness and cursor bounds.

**One integer forward:** `trace` currently computes floating masks/normalization credit on the greedy path. Retain the single shared integer score/hidden computation, storing mask bool/u8 and normalization shift u32. Perform float conversions and inverse-shift credit only in offline training. Preserve integer logits and the retained artifact's predictions exactly under the refactor. No parallel floating predictive definition. Keep allocating scratch explicit; a no-allocation claim is not required for this pilot.

**Focused mathematical check:** the intended STE is with respect to effective quantized weights, with discrete quantizer/shift choices frozen: `gZ=(p-onehot)*2^-F/ln(2)`, `gWo[r,j]=gZ[r]*h[j]`, `gE[j]=sum_r(gZ[r]*Wo_effective[r,j])*active[j]*2^-norm_shift`, followed by one batch target-count normalization. Add an independently hand-computed test for both embedding tables and output credit before Adam/clipping, with nonzero residual, nonflat bias, nonunit shifts, active normalization and active/inactive clamp coordinates. Assert the intended conditions actually occur. Do not finite-difference a discontinuous quantizer and label it this surrogate.

**Valid active envelope:** correct/restrict norm_bits=0, prevent overflow in the sum of shifted embeddings before clamp, and apply shared validation to constructed and reloaded cores. Reject unsupported shapes/shifts/codes and invalid prompt token IDs at the runner boundary. A checked wider temporary sum followed by bounded integer clamp is acceptable. Keep scope small. Correct misleading comments about output gradients: `(p-y)*h` is output-weight credit, while embedding credit is zero when the exported output weights are all zero. The current asymmetric subthreshold output masters are a fixed initialization choice.

Run the focused existing/new numerical, optimizer, format and continuation tests together to avoid unnecessary full rebuild cycles. Verify retained toy integer-score/greedy parity after this semantic-preserving refactor. Do not schedule another independent toy-learning/comparator campaign unless a concrete regression appears. Then continue to section 3 in the same run.

## 3. Immutable, bounded real-text data

Claim a unique report attempt with `uor_r4_core::report_output::claim` immediately after argument validation and before loading models/preparing data. Use a new derived-input attempt or place derived inputs in the currently unsealed attempt; never write beneath the old sealed root. Persist enough exact bytes/token IDs/offsets to reconstruct evaluation even after checkout docs change. Seal and verify the complete file set at completion; reject/report nonempty unlisted files.

Primary population: a **bounded technical-prose pilot from tracked repository `docs/` at a pinned commit**, max 4 MiB selected raw UTF-8 text. Do not read changing worktree documentation while generating results. Enumerate eligible whole documents, record exclusions and content hashes, group exact duplicates before splitting, and select by a stable seeded/hash rule spread across document lengths rather than a sorted-path prefix or the first large document. Do not cut a document in the middle to fill the byte cap. Related documents may still share prose; label this limitation. No instruction-following, natural general-language or coding benchmark claim.

Use fixed content-group fit/tune/development partitions (e.g. 80/10/10), one split seed. Freeze all selected IDs before model results. The primary corpus remains one domain/one arm. Do not add a second code-corpus fit in this task. Leave final held-out qualification for after design selection.

Tokenizer: the existing SmolLM2 source tokenizer was previously SHA256 `9ca9acddb6525a194ec8ac7a87f24fbba7232a9a15ffa1af0c1224fcd888e47c`. Verify it; derive V=4096 through `transformerless::bpe_derive` and `HfBpeTokenizer`. Save original/derived hashes and both tokenizer JSON files. This is a fixed pretrained vocabulary-prefix derivation, **not a tokenizer fit on these documents**. Reject out-of-range emitted IDs. Do not silently substitute tokenizer families.

Use within-document length-64 windows, with the shared n−1 target rule and explicit offsets/reset policy. Preserve shorter tails with at least two tokens where supported, or record their exclusion. Full windows score 63 targets; PAD denotes only the missing second history token at the window's first prediction. Never cross documents. All learners/references/control records use identical scored positions; do not drop the first prediction or fabricate a target at a window boundary. Enumerate all eligible windows in the selected corpus and use the repaired no-replacement shuffle. Save consumed IDs.

Development should cover short/medium/long documents and positions throughout long documents. Suggested bound: up to 48 documents and 96 windows total, with a deterministic stratified choice recorded before outcomes; report actual document/window/target counts and sampling weights. If the corpus permits fewer, report it without widening after seeing results. The primary estimand is this declared sampled population, not all repository bytes. Keep a fixed small fit diagnostic slice, and a distinct tune slice only for baseline smoothing; neither is called final held out.

Raw unigram counts start at zero. Count exactly the declared fit target population, assert `sum(counts)==total`, then apply add-one smoothing **once**. The gate runner's counts-one/total-zero pattern is harmless only for its flat toy and must not be copied. Freeze the exact marginal and its quantized bias before optimization. For each distribution, assert normalization in the offline reference and record the quantized-vs-exact loss gap on fit/development. Do not assume an old 0.10-bit calibration bound applies.

## 4. One learned arm and actual timing

Model: V=4096, dv=128, norm_bits=6, F=10, bias shift=10; two token-position tables, existing bounded ReLU, ternary output and frozen fit-only bias; **memory disabled**. Use seed 13, output-master bound 0.45 and the repaired default Adam settings (currently lr .05, beta1 .9, beta2 .999, weight_decay 0, grad_clip 1; verify and record the actual values). Keep one configuration; no learning-rate/width/seed sweep. Step 0 must export zero residual and match the frozen quantized bias loss exactly. This real-text arm starts fresh; never enlarge/reuse the V4 answer fixture's trained weights.

Persist/export/reload and evaluate step 0 before any updating timing probe. Measure several complete warm training steps, quantize/export, serialized checkpoint write/read, held-out predictions/controls and generation through this runner. Count probe work in the actual training step/exposure total if it updates the real arm, or explicitly discard the probe state and charge it. The earlier geometric-memory trainer timing is only an estimate. Project all remaining checkpoints, full-vocabulary readout, controls and I/O before proceeding. If a measured allocation/harness bottleneck dominates, make one semantics-preserving repair and check parity rather than silently raising the model size or changing scoring.

Primary checkpoints are **0, 256, 512**; step 512 owns the development gate. No replacement within a pass. If fewer than 512 batches exist, stop at one pass and label the reduced dose before fitting; do not secretly repeat to reach the label. In that case the primary step-512 gate is NOT_RUN and the reduced-dose endpoint is descriptive, not a substitute pass. Report actual steps, sequence tokens, scored targets, distinct windows/documents, consumed fraction, token-row coverage, cumulative timings and RSS. For 512 full batches of 8×64 the scored-target dose is 258,048.

Persist each checkpoint and its exported serving artifact. Reload each artifact for every reported checkpoint evaluation and generation. Verify metadata against the attempt manifest. Save an actual disk checkpoint mid-pass and demonstrate exact continuation of the next batch/update against a clone; do not needlessly fit a second full arm. Record source commit plus dirty patch/hash if training precedes the implementation commit, binary/compiler/config, optimizer, data/tokenizer, dose and schema identities.

At checkpoints, retain fit/development loss, exported/master update and code-change summaries, activation occupancy/saturation and logit-margin summaries. These are diagnostic aggregates, not enormous per-step tensors. Keep the complete consumed IDs and per-document loss sums/counts; do not present master-weight movement alone as exported learning.

## 5. References, interventions and predetermined decision

Use uncapped stable offline log-sum-exp **bits per scored target** from the same exported integer logits scaled by `2^-F`. Validate uniform V4096 =>12 bits, common-offset invariance and an extreme wrong score. Do not copy old clipped nats, a mismatched bias path or a running count before observing the correct current target.

Evaluate on the same development records:

1. Exact add-one fit-only unigram, `p(t)=(count(t)+1)/(N+V)`.
2. The identical frozen quantized constant, also the same artifact with contextual residual disabled. This must match step 0 exactly.
3. The exported contextual model, plus both single-position knockouts as diagnostics.
4. A fixed **whole context-pair permutation**, holding targets fixed, without refitting. Prefer shuffling `(prev,cur)` pairs within each development document and PAD/non-PAD stratum. Save the mapping/seed, preserve the exact context and target marginals, and verify changed context-target associations. Keep the same mapping for every checkpoint. Non-permutable strata are reported, not silently dropped from the primary population; report the intervention on its explicit eligible subset and its unpermuted counterpart. This blocks a mere document-topic/marginal correction from masquerading as local-context learning.
5. Corrected static one-/two-token interpolated backoff references using the same target iterator and tune-only smoothing selection. Adapt `Cond`/`Fit` and interpolated `family_p`/tuning from `geometric-realtext-ceiling`; its current evaluator starts at target index 2 and floors probabilities, so that evaluation loop is not reusable unchanged. Do not copy the old pilot's misaligned count code. Ensure proper probabilities, finite unseen-event loss and explicit normalization fixtures. Label full-fit count references versus a counts-only reference over the model's exact consumed windows at each dose. For matched contextual exposure, back consumed conditional counts with the same full-fit unigram already supplied to the learner; label this explicitly. A wholly consumed-only marginal would have less information and must be separately labeled. Counts may be accumulated during exposure; no extra learned arm. Report the metric on pairs unseen in consumed fit separately, with support counts. These are statistical references, not empirical Bayes ceilings and not called Kneser–Ney unless actually implemented/verified.

Freeze the **step-512 primary gate before fitting**: contextual CE is at least 0.10 bits/target below both exact full-fit unigram and identical quantized constant; paired nominal 95% intervals for each gain exclude zero; permutation penalty is positive with its paired interval excluding zero. Use paired document-cluster bootstrap resampling of saved loss sums/counts with a fixed seed and 2,000 draws. Freeze practical support minima in the manifest before results: at least 20 scored development content groups and 1,024 targets for the primary gain, and at least 20 eligible groups and 1,024 eligible targets for the permutation penalty. These are screening rules, not a statistical independence guarantee. For token-micro, recompute the ratio from resampled sums/counts; report document-macro separately. Resampling a token as independent would be wrong here. Correlated repository documents and repeated development use limit interpretation; do not call this a final qualification or calibrated universal guarantee. Too few documents/eligible strata yield INCONCLUSIVE, not manufactured confidence.

Report all checkpoints. Beating the quantized bias alone can be marginal calibration; a context-independent residual is invariant to the fixed pair permutation. Backoff superiority is not a required primary gate, but report the gap plainly. Neither positive average loss nor the V4 toy establishes fluent generation.

Optional dose continuation is predeclared: if step512 fails the gate yet exported **fit** CE improved by at least .05 bit from256, is finite, and measured resources cover complete evaluation, continue the same arm to at most step1024 or one completed shuffled pass, whichever comes first. It is exploratory; retain the original512 decision. Do not select a best checkpoint after looking at every score. If flat/divergent or a correctness defect appears, inspect the saved code/gradient/exposure/scale diagnostics and apply at most one causally justified bounded repair with a separately identified attempt; no blind repetition or hyperparameter sweep. A negative short dose is not a proof against low-bit learning or geometry.

## 6. Actual generated behavior and mechanism scope

Before training, freeze three prefixes from three different development documents/length strata, each at least two tokens. For step0/256/512 (and any exploratory endpoint), generate 64 greedy new tokens with the reloaded artifact and record full prompt/output IDs plus decoded bytes, alongside the identical context-disabled constant control. Keep repetitive or malformed outputs; do not select fluent examples afterwards. Greedy serving uses integer argmax; floating probability evaluation remains offline. Report tie policy and prompt/reset behavior.

Report actual serialized model/checkpoint size, cold load and warmed end-to-end generation time, training/evaluation time and peak RSS. Account for V×dv readout work (524,288 ternary coordinate visits per prediction before overhead) and parameter-byte traffic rather than calling the decoder sparse. Scope operation claims to the numerical score/argmax boundary actually inspected; if inspecting optimized instructions, identify the exact binary/symbol. No full-path energy claim without a physical measurement; energy/token remains UNAVAILABLE otherwise. Do not let an energy instrumentation campaign displace this pilot.

The modulo-120 table is currently metadata, not a predictive geometric operation. The two-token prior is the local distribution component. Do not relabel it a learned geometric language success. Subsequent state work must distinguish histories with the same final two tokens and different relevant prefixes; its candidate action/read needs learned contribution and capacity/cost controls. Preserve exact occurrence/version memory separately from value buckets and preserve the typed golden/Galois companion relationship. No new state channel is implemented in this run.

## 7. Verification, protected delivery and final report

Run `~/.cargo/bin/cargo fmt --check`, the touched-package offline check and focused numerical/gradient/format/scheduler/resume/runner tests appropriate to the edits. Exercise the actual release runner and generation, save exact commands/results, and run `python3 scripts/check_claim_wording.py` for claim edits. No blanket historical suite. Queue compatibility statuses are not tests.

Deliver named paths through a protected PR; no main/admin bypass. Verify the actual merge and tree/content equality. Update current-state, EVIDENCE, README, continuation pointers, canonical dependency status and cumulative resource/storage receipts; preserve original raw evidence. Update #973/#820 and relevant #963/#964 with actual results and remaining acceptance; keep broad issues open and update actual linked project items only. Do not declare every prerequisite complete from a test count.

Final report must include: source/binary/data/tokenizer identities; exact fixed configuration and exposure; every checkpoint's fit/development/reference/control result; primary gate versus exploratory result; generated bytes and collapse; actual manifest/artifact/checkpoint paths/hashes; numerical/continuation fixes and executed assertions; actual resource charges, remaining allowance and any prior-recorded extension; delivery identity; and **one evidence-supported next action**. If local limits or session limits prevent completion, seal partial evidence and preserve a tested resumable checkpoint, saying which limit was actually reached. Do not call “runner implemented” or “tests passed” a completed real-text experiment.
