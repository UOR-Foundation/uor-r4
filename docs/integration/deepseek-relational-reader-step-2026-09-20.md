# DeepSeek execution prompt: learn a contextual geometric reader

**Completed historical prompt:** PR #1319 attempted this task. Read its [principal review](relational-reader-review-2026-09-20.md); the single current successor is [competitive query-dependent reading](deepseek-competitive-reader-step-2026-09-20.md). Do not rerun this older prompt as the active task.

**Owner-steered architectural priority, after the completed PR #1314 handoff.** This is the single next implementation task. The owner reconfirmed the full geometric-attention synthesis: the next experiment must test a meaningful geometric bridge, not become an abstention-only repair. Learn a bounded contextual relational reader over exact memory. A repaired and loss-calibrated version of PR #1314 is its comparator and integration reference. No successor fit has run.

You are a research contributor. Select the smallest causal context encoder and directed finite geometric comparison that can improve useful memory access; explain the mathematical and source-based choice. You may change the contextual representation, shared ranker, NoRead and bounded copy-strength policy within the complete projection. Preserve useful current components and original evidence. Do not merely relabel the existing eight-class equality as geometry, and do not build all proposed geometry variants at once.

## Recover authority and current state

Refresh origin/main, live #973/#820 and relevant PRs, the actual model/resource ledger, retained report seals and free space. Work in an isolated full worktree; preserve owner and other active checkouts. Read AGENTS.md, DECISIONS.md D0-b/D1/D2/D3, canonical project-track/current-state, then these two documents completely:

1. [Correcting principal audit of PR #1314](occurrence-reader-audit-2026-09-20.md).
2. [Mechanism synthesis and geometric-attention roadmap](geometric-attention-mechanism-synthesis-2026-09-20.md).

PR #1314 merged as `fbf542aa4c1f7ed336cd028c6b7b3502cba4118c`; later commits may carry this prompt and corrections. Inspect the specific source and design/result receipts. Obtain additional history/research context as needed, using primary papers and original source. Historical authored grammar, old artifacts and dense reference mechanisms are donors, not interchangeable components of the current BPE model.

The goal remains a learned transformerless geometric language model for conversation/memory and coding/reasoning on consumer hardware. Rust training may use float/matmul. D0-b serving permits bounded <=4-bit additive/shift/table maps with no numerical-kernel multiplier or floating/transcendental work. Preserve prime/zeta/R4/S3/H4/icosian priorities while measuring each actual contribution. Do not adopt a transformer backbone, hidden teacher responses, a new authored parser or a generic architecture sweep.

## What the latest result actually says

Retain frozen E plus S's query-only local contribution with no S history fold. Retain the occurrence-reader-4 artifact and all earlier negative roots. The new ring has exact session/sequence identity, 128 token positions and at most 24 exact-key candidates, each carrying an observed successor.

The constructed fresh panel has reader accuracy 411/443 versus latest 279/443 on reader-relevant positions. On the authored slot-equivalence subset it is 99/120 versus latest 64/120. This is a useful selection result. Its geometry is eight-class token-slot equality, not yet directed H4 transport. Targets were constructed using the same partition, and the coefficient ablation changes exact-match weight as well as slot information.

Raw-text selection is **5 correct reads and 83 wrong reads out of 88**: five errors occur among ten covered positions, 78 among uncovered positions. The earlier 10/10 rank-hit claim was a counter defect. Synthetic all-position CE worsens +0.6713 bits/token; raw probe +2.7891. Thus ranking, abstention and integration strength are all legitimate learning targets. The fixed residual `2^14` at ten fractional bits is a 16-nat boost. Preserve these negative outcomes and the positive construction gain without promoting this candidate.

Construction training already had **737 read and 515 NoRead labels** among 1,252 examples. The full candidate-bearing fit population was 737 covered and 1,031 uncovered before negative subsampling. Therefore do not report missing NoRead supervision or an impossibility of learning it from synthetic data. Natural-text fit is intended to address distribution/feature/objective mismatch, not to add the first negative label. The synthetic margin-derived amplitude was a useful selector diagnostic; its successful source can overcome local margins, but its false-read cost was not optimized.

## Repair the instruments as part of the constructive task

Correct the specific defects in the audit; do not launch a broad replay campaign. Reaggregate saved vectors where sufficient and write a new correcting attempt, leaving sealed originals unchanged. In particular:

- Separate target-payload availability, correct selected payload, correct occurrence when identifiable, Read/NoRead, final token correctness and language loss. Multiple equal payloads need output-compatible credit; do not invent a unique source label.
- Make a real future-token intervention and actually analyze it. Compare prior scores, selected reference and payload. Compare ReadDisabled independently with the retained S-query-only scorer.
- Validate declared construction semantics; classify same-slot decoy ambiguity instead of asserting unique solvability. Use consistent denominators and paired comparisons against latest as well as local.
- Preserve exact identity information when disabling a nonidentity feature. A matched categorical partition/control must have targets defined independently of its code assignment.
- Hash the executable bytes properly. Compare complete exported/reloaded decisions, not only Read versus NoRead; bind source, compiler/config, data, tokenizer and artifact. Keep source sensitivity distinct from emitted-output sensitivity.
- Time a real baseline with no unused ring/admission/feature work and a complete reader path; divide by actual predictions. Report preprocessing, resident tables, scratch, ring, model bytes and memory traffic separately. Do not claim energy from a clock or an assumed power value.

## Primary experiment: learned contextual geometric access

The current admission requires `x_j == x_i` and only offers `x_(j+1)`. Its query context is current/previous/two-back token, and its features are mostly exact or eight-slot equality and recency. Calibration cannot admit an excluded source or restore a distinction those observations erase. This is why the next primary hypothesis is richer contextual relational access, not a further threshold campaign. Earlier Q/S compressed-prefix negatives do not test this mechanism: this reader selects among separately retained exact occurrences.

Use the following conceptual form, with bounded finite representations and an explicit action convention:

```
q_t  = Q_theta(causal query context)
k_j  = K_theta(observed source context)
r_tj = inverse(q_t) * k_j
s_tj = Phi_theta(r_tj, exact/contextual evidence)
a_t  = NoRead or (exact source reference, bounded copy strength)
```

Learn the contextual query/source descriptions and shared selection from the task objective. Two ordered H4 lanes or a small ordered-root tuple are reasonable starting choices; choose the simplest supported design. Preserve exact token/occurrence identity outside these descriptors. A new fixed hash-to-root assignment or a relabeling of frozen S slots does not meet this contextual-learning objective. Do not compress the entire prefix into one 120-valued register and assume its 6.91-bit capacity changed. Retain distinctions the task actually requires, and state which parameters are learned and which primitives are fixed.

Inspect and reuse the operator patterns in `native_geometric/source_routing.rs` (ordered two-lane composition, relative landmark scoring, NoSource), `relational_attention/runtime.rs` (ordered roots and directed relation packets), and `hamming_refinement/runtime.rs` (selected context and exact-reference read/refine interface). Their artifacts, byte/word grammars and supplied record boundaries are not the BPE learner. Do not transplant an authored role parser. Source descriptors must be derived from observations already available at their declared write/update point; query construction uses only the current prefix. Keep the selected relative element/context available for a later state update rather than reducing everything permanently to one scalar score. The trained dependent-read scheduler is a successor, not mandatory additional scope for this one-read pilot.

This directly tests the reduced Hopf/spin bridge: finite signed quaternion relations already supply the equal-magnitude quaternionic Hopf special case. Preserve the distinction between `q^-1*k` under common-left action and `q*k^-1` under common-right action. Retained phase is available when a specific observed alias motivates it, but all sixteen cross-lane products, full S7/E8 state and a harmonic coefficient bank are not prerequisites. Role/scope observations belong in the contextual representation if needed; persistent role banks are a later retention intervention unless actual eviction or scope ambiguity makes them necessary here.

## Comparators, support and learning objective

Use one shared harness with frozen E plus S-query-only local emission and exact payload semantics:

- Local-only/always-NoRead, with no unused reader work in its cost measurement.
- A repaired PR #1314-style exact-recurrence selector with a competently fitted gain/NoRead policy.
- The learned contextual geometric reader, receiving the same loss-based integration opportunities and matched training exposure.

Do not compare the new reader's calibrated gain against the old destructive 16-nat gain and attribute the difference to geometry. Keep the original artifact as historical evidence. Compare ranking on identical admitted pools. When practical, an equally small nongeometric contextual selector can distinguish the value of richer context from the particular group structure; without it, claim useful native relational access at its measured scope, not unique H4 superiority. Focused direction/relative-feature interventions show use of the mechanism but are not substitutes for a trained matched comparator when making that stronger claim.

Start with the 128-token ring and at most 24 final candidates. First decide whether exact-key admission can support the intended relation test. For nonidentical source/query cues it often cannot. If required, select **one** causal bounded mixed proposal pool (for example exact-key candidates plus contextual/geometric proposals); do not insert gold sources. Retrain/evaluate both selectors on that same pool for conditional-ranking attribution. Expose exact source/query token agreement to every comparator: the old reader obtained this fact implicitly from admission, so broadening its pool without adding the indicator would unfairly blind it. Preserve other information previously implicit in admission. Keep the original exact-only baseline as a support comparison and report which gains came from admission. Count every scanned record, descriptor update, table read and page touch; a 24-candidate result after scanning 128 records is not 24 operations. No admission/capacity sweep is selected.

Use fit-only natural text plus a compact binding/relation construction whose answers are defined independently of the model's root/slot assignment. Include both useful and non-useful reads, causal context changes, role/order reversals and competing sources. Hold out combinations of roles, cues and payloads. Do not make a target correct merely because it shares the model's own geometric slot. The existing eight raw windows and old fresh panel are inspected regressions, not new held-out evidence. Separate fit, tune, known regression and new evaluation by document/assignment before scoring.

Before committing to a fit, measure target-payload coverage over the complete intended population and relation-critical subset, and check whether the proposed causal observations separate the intended cases. Unsupported positions are valid NoRead examples; require source support for cases used to claim learned retrieval, not 100% coverage of arbitrary natural text. A witnessed complete observation collision justifies one small contextual correction; more optimization cannot resolve it. Let this check guide the representation rather than mandate a large phase/sphere/scope subsystem.

Learn source choice and bounded strength, including zero, against actual post-injection next-token loss or a justified aligned surrogate. For a payload with local probability `p`, true next token `y` and boost `a` in nats, the one-step action cost relative to local is

`log(1 + p*(exp(a)-1)) - a*1[payload == y]`.

All these one-step candidate/strength costs are available offline from the observed next token; no new reinforcement-learning or reward-model system is needed. Multiple occurrences with the same payload share lexical credit. Source identity for later dependent reads remains a separate responsibility; do not invent unique gold occurrences from equal output bytes.

The audit derives `a_opt = clip(logit(r)-logit(p),0,A)` for true selection-conditioned correctness `r`, showing why utility depends on what the local predictor already believes. This is an offline reference, not a requirement to serve normalized probabilities/logarithms or to estimate an oracle `r`. A finite learned additive/LUT action policy is acceptable under D0-b. Check actual quantized hard-forward loss, clipping and saturation. A score margin is a possible feature, not automatically a calibrated probability.

A globally fitted gain is a cheap comparator. A small causal-stratum gain table is allowed if it earns its cost; strata may use candidate count, score separation, local payload scores or learned context. Coverage, correct/incorrect source labels and corpus/evaluation-family identity are never serving inputs. Calibration must follow selection and any ranker change; use held-out/cross-fitted predictions where needed. In a coarse stratum with varying `p`, optimize actual average action loss rather than subtracting logits of averaged probabilities. Report natural-text and construction tradeoffs separately and account for negative subsampling/mixture weighting.

Choose the optimizer, fitting dose and finite action set yourself, based on source and measured cost. Repair the named instruments within this constructive task, then fit and exercise the relational reader. Do not stop at a calibrated copy baseline, another literature survey or a physics design document. If a concrete representation/admission barrier blocks the pilot, preserve the useful baseline and report that barrier with a bounded successor instead of forcing a positive geometry claim.

## Decide from behavior and cost

Before seeing new evaluation, record the intended practical tradeoff and outcome decision. Retain the old thresholds as historical reporting columns; any new tolerance needs a prospective justification. Correctness, causality, exact references and honest evidence are not negotiable performance tolerances.

Compare frozen local, competent latest/exact-prefix, calibrated exact-recurrence and the learned relational reader on matched positions and candidate support. Report coverage, conditional ranking, read precision, NoRead behavior, per-document paired loss, payload availability, generated continuations, and corrected direct-path cost. Include identical-recent-tail/different-older-binding cases, nonidentical source/query cues supported by the declared admission, ordered role changes, changed payloads, repeated keys, absent answers, conflicting contexts and held-out role/payload combinations. Show when the model follows a distant binding and when it leaves local generation alone. Teacher-forced gain without useful generated behavior remains a limited result.

Do not count collapse to always-NoRead as an attention gain. It is a useful baseline and may be the honest negative outcome, but a successful candidate must retain demonstrated causal contextual benefit as well as the declared natural-text tradeoff. Conversely, do not impose an arbitrary high-precision threshold: a bounded probability correction can be useful without winning every token's argmax. Report both loss and actual source-sensitive generated behavior. Any oracle choice made after viewing the next token is an opportunity bound only, not a learned result; use such a diagnostic only if it resolves the next implementation choice.

Use focused arithmetic/causality/export/continuation tests and compile/exercise the changed Rust path. Validate the actual trainer's disk continuation when you change its state. Do not repair unused legacy CPQK machinery unless you use QueryTrainer. No blanket historical suite is required for this unpromoted component; an actual model replacement would require relevant retained controls.

The primary falsifier is whether the learned contextual relation adds useful source-sensitive behavior beyond a competently calibrated comparator at the declared natural-text/cost tradeoff. If gains come only from a larger candidate pool or a smaller amplitude, record that contribution without attributing it to geometric ranking. If the candidate helps only one subset, preserve and report that useful component and its limitation. If it is negative, record the smallest witnessed bottleneck and one justified successor. A few meaningful corrected attempts are permitted within the budget; stop unproductive refinement rather than accumulating threshold sweeps. Do not present another audit alone as completion of this constructive task.

## Resources, preservation and delivery

At principal review the ledger was 181238565/191300000 ms, with about 167.7 minutes left. This is a recovery pointer, not a fresh allowance. Read the live JSON before work. A reasonable initial planning ceiling is 90 minutes for the entire preparation/build/fit/controls/evaluation/corrections/checkpoint block, one model worker, <=8 GiB RSS, <=512 MiB new data/reports plus <=2 GiB incremental build, and the existing 128 MiB protected storage margin. Revise that complete projection from a small step-time measurement before committing to a longer fit; do not spend it merely because it exists.

Necessary local extensions are already owner-authorized: record the reason, increment, complete updated projection and cumulative limit before use. Retain charges across sessions and failures. No paid/external compute, deletion of unique research/checkpoints or owner checkout mutation follows from this authorization. Do not clear shared build caches routinely.

Claim each new report root exclusively, seal attempts and verify all files. Keep original artifacts and reports intact. Stage named paths, push a branch, create a protected PR with `References #973`, and verify the actual merge/source content before saying delivered. Synchronize README, canonical current state/plan where changed, evidence, continuation instructions, owning issues and any existing GitHub project items. A queue compatibility acknowledgement is not an executed test. Close no broad capability issue from this bounded result.

Conclude with the exact implementation choice and rationale; artifact/source/data identities; measured improvements and harms; actual outputs and controls; complete resource debit/balance; delivery receipts; and one evidence-supported next step in the geometric-attention roadmap. Be a research contributor: propose a better bounded mechanism when the data warrants it, while preserving the evidence that led there.
