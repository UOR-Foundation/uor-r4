# UOR-R4 Geometric Language Model — complete continuation handoff

Prepared September 7, 2026 for a new Codex window. Read this document in full.
It is a dated continuity record. Refresh live sources before execution; do not
infer that the artifact in the latest merged PR is the accepted model.

## Your role and the owner's destination

Take over as continuing lead for project management, architecture and native
Rust model development. Build the **UOR-R4 Geometric Language Model**: an
experimental autoregressive geometric state model with exact addressed memory
and learned typed operators. It should learn useful prose, conversation,
memory, reasoning and Rust coding, ultimately targeting frontier-level ability
on ordinary consumer laptops such as the M1. The reason for local execution is
to reduce energy and wasted compute associated with data-centre matrix products.
This is the objective, not a claim of current capability or efficiency.

Training time is secondary to execution quality, latency and energy. The owner
wants submillisecond execution if possible; state whether a measurement covers
a decision, token, ingestion, or full response. Current warm-kernel results do
not establish that complete-model goal. Evaluate useful work and quality per
task alongside memory traffic, power/energy and thermal behavior.

Use Rust throughout preparation, training, artifact construction and inference.
Offline learning can use floats, gradients and matrix multiplication, including
uor-matmul. Final serving must execute no mathematical matrix products,
including lookup/add contractions, and no transformer backbone. A dense
transformer hidden behind a table or a provider authoring responses is excluded.
Deterministic geometric address/page-table selection is allowed. Shared typed
operators are current. The owner later said expert gates may eventually be
necessary: preserve that conditional option, without silently adopting MoE or
sparse learned gating as the current architecture.

Prime addresses/ordered n-lets, fixed zeta-zero phases, R4/S3/H4 state and
transport, exact Z[phi], orientation/chirality/polarity, typed paired-H4/icosian
geometry and UOR identity are primary mechanisms. Name their implemented roles
and missing bridges. Structural priority does not prove predictive advantage.
Use existing mechanisms where they solve a real representation/computation need;
do not attach every mathematical field to every token merely because it exists.

The final product goal is one complete native capability API, then the accepted
native artifact executing through WASM in the already developed GitHub Pages AI
Studio. A static shell, reference backend or separately downloaded transformer
does not satisfy that goal. The companion repository is
https://github.com/Casey-allard/uor-r4-wasm-chat; its historically inspected local
checkout is `/Users/casey.allard/Downloads/uor-r4-project`. Refresh it before work.

## Authority and source-reading instructions

Start with these complete current documents, in order:

1. `README.md`, `AGENTS.md`, `CONTRIBUTING.md`.
2. `docs/integration/project-track.md`, `docs/integration/current-state.md`.
3. `docs/integration/model-direction-2026-09.md`, `docs/PROJECT_MAP.md`.
4. `docs/integration/agent-execution-policy.json` and its readable companion.
5. `docs/integration/architecture-2026-09/README.md`, `engines.md`,
   `mathematics.md`, `imports.md`, and the `source-inventory.json` scope/catalog.
6. The latest relation-start and reverse-span result records and JSON evidence;
   the source-context and span-context donor records; actual Rust source listed
   below. Read historical/result files in full when adopting their mechanism.

The installed workflow is `/Users/casey.allard/.codex/skills/uor-project-workflow/SKILL.md`;
the repository copy is `tools/skills/uor-project-workflow/SKILL.md`. Use the
current repository policy if a stale installed copy disagrees. Historical
fifteen-minute windows, one-retry quotas, one-issue stops, broad proof gates and
old stage orders do not override owner scope. A whole-plan request authorizes
necessary successive work. Do not repeat the broad archive census for every edit.

For the full earlier canonical recovery, also read these files completely:

- `/Users/casey.allard/Documents/Codex/2026-09-06/uor-r4-canonical-geo-transformer-handoff.md`
- `/Users/casey.allard/Documents/Codex/2026-09-06/uor-r4-canonical-handoff-verification.json`

They contain additional exact source/history/resource preservation pointers.
Their `e7c14c99` checkpoint, PR #1159 and old source/NoRead next action are now
historical; the lineage below supersedes their current-state sections. A dated
handoff or cached KB source is evidence, not authorization to regress the plan.

## Live verification before choosing execution

The GitHub repository is `UOR-Foundation/uor-r4`. At this documentation task's
start, PR #1171 was merged and `origin/main` was
`bc03f2d7ffde99608da370808eca542360e54508`. Its tested source head was
`0802d8e14d25afd3249dedd99d7c2e6244e779ae`; the merged tree matched that tested tree.
This documentation reorganization is delivered by a later protected PR; refresh
its actual merge rather than assuming this base is still main.

On the original host, the reusable isolated worktree is
`/Users/casey.allard/.codex/worktrees/r4-typed-value/uor-r4`.
The documentation branch is `codex/geometric-project-direction`.
Inspect status before using it. Start a clean `codex/` branch/full worktree from
refreshed `origin/main`, or safely reuse a clean worktree. Do not overwrite
another active task or intentionally dirty source. The original checkout
`/Users/casey.allard/uor-r4` is not the development branch and is intentionally
preserved without resetting/fast-forwarding its work.

Refresh Git status, worktree list, `origin/main`, relevant PR states and the live
#820 roadmap. Verify current model/candidate bytes and SHA-256 against committed
evidence. Inspect the corresponding delivery receipt and actual source hashes,
not only its filename. Check cumulative model/storage limits, cache/toolchain
and disk/RAM state. Reuse valid earlier results; a documentation-only change
does not need a repeat model run. If live evidence moved, adapt the plan and
state exactly why. Inspect the same artifact through the actual interface being
claimed before calling a new surface qualified.

PR/merge-group CI has five historical required names that are echo-only
compatibility acknowledgements after owner-requested PR #1163. No formatting,
Clippy or model tests execute there. Actual validation comes from focused local
commands. Manual native/legacy release jobs remain available only when relevant.
Use protected PR delivery, no direct main push, admin bypass or fabricated
statuses. Verify actual merge plus source/tree identity. Partial results use
`References #N`, not `Closes #N`.

## Retained artifact and newest candidate

Artifact root on this host:
`/Users/casey.allard/uor-r4/.uor-models/native-typed-value-2026-09-05/`.

**Retain the reverse-span model, not the relation-start candidate:**

- Path: `reverse-span-angular/model.json` under that root.
- CID: `blake3:321e990f218899aa2668c2530caed5589c614cc58d1bb1de0b7145a64aee9563`.
- Bytes: 11,631,560.
- SHA-256: `e094a0b511bba8747cee49867997e908940a85c9147b0537ecbdc1e297ea56ab`.
- Delivery: PR #1170, merged `3dd7180206b31a91998e9052e02eeed334655c3d`.

**Preserve the failed successor:**

- Path: `relation-start-angular/model.json` under the same root.
- CID: `blake3:bb6b8ba41f65b0c9d5c7ddf453e765a52b5ea8b169e50141ead05b3b5cefad79`.
- Bytes: 11,634,162.
- SHA-256: `3d6c1c1f535e989885969b19415aa81c855c4f27517836d5eb994609cb530d4d`.
- Delivery: PR #1171 at the verified source/base above.
- Verdict: `PRESERVE_CANDIDATE_TRANSFER_NEGATIVE_KEEP_321E990F`.

Read `docs/native_geometric_reverse_spans_1140.md`,
`docs/native_geometric_relation_start_1140.md`, their `docs/evidence/` JSON files
and local `reverse-span-delivery.json` / `relation-start-delivery.json`.
Artifact identity and runtime source identity are distinct: a runtime correction
can change behavior without refitting identical parent parameters.

Reverse-span retention reused the learned writer endpoint and existing H4
Continue/Finish operator. It admits earlier starts that reach the final value
word exactly and selects the longest admitted start. The terminal WordAtom keeps
its exact identity; span.start is separate. Linker/owner words cannot enter the
value. Bounded caps include 512 context tokens, 16 relation records, 16 span
words, 28 payload bytes and 32 response tokens at this experiment's scope.

That retained path improved construction 3/6→6/6, passed open 6/6, short 3/3 and
fresh 6/6 after selection. It preserved all 663 prior construction answers,
18 earlier forward turns, 28 long-window reads, 48 dependent reads, prior
source-span panels and selected other behavior. Parent/start mutation rejection,
terminal identity, checkpoints and zero-allocation checks passed at the named
actual-artifact scope. This is bounded familiar-template memory, not a general LM.

The subsequent start learner uses 20 codes, two signed-H4 lanes and seven shape/
gap/pair/singleton feature families. It improved construction 2/8→8/8 and open
2/8→6/8, preserving the selected earlier answers and complete parent parameters.
Its selection required all eight open answers and failed. **Fresh cases and
post-selection diagnostics remain NOT_RUN.** Do not promote it because the PR
merged or construction fit perfectly. A singleton persistent-reader correction
is included in the delivered runtime and must not be lost.

Only predecessor-shape codes became nonidentity: lowercase→[24,119], title→
[119,77], identity root 119. `A ledger says Pearl Cove holds tesvi` emits
` says Pearl Cove.\n`; the singleton Pearl case also fails. Both starts follow
lowercase words, score equally and the longer candidate wins. The correct value
is admitted. This is selection/representation-use failure, not missing storage.
Prior diagnostics `Records show Orin Grove holds nelra` overextend the intro;
`Orin  Grove holds nelra` truncates across a two-space gap. Separator bytes are a
separate representation limitation, not something phrase ranking alone repairs.

## The recommended next implementation and why

**Extend the existing phrase-start selector with bounded ordered lexical/prime
and role context, together with construction contrasts.** Then advance shared
contextual transitions and lexical emission. Do not launch an unchanged fit.

The previous immediate suggestion was better contrasts using shape-pair features.
The post-result source review refined that: true shape-feature aliases also
exist. If `says quiet river` and `quiet river` are both admitted in `the report
says quiet river holds mira`, they can share first/next/predecessor shapes, gaps,
shape pairs and non-singleton status. Identical features cannot receive distinct
scores. This is a **source-derived conditional example, not another model run**.
More construction alone can fix the observed title-case shortcut but cannot
solve the general alias.

Use exact predecessor records, construction-bound prime identities, writer
owner/value context and the existing signed-H4 code/landmark learner. Expose
distinctions already present in retained source state. Shape remains one feature
family. Prime magnitudes are addresses, not a semantic metric. The closest
supported donor is source-span context `419ba3a7`, which pairs the next word
with the original source cue. This repaired the same-word `holds` role collision.

Concrete sequence:

1. Read current `relation_start.rs` and training, `relation_span.rs`, writer and
   source-span contextual features. Check exact feature aliases on newly authored
   cases using a cheap focused helper/check in the implementation, not a new
   benchmark or proof framework.
2. Add bounded lexical/role context with an explicit artifact version/compatibility
   story if fields change. Keep endpoint/admission/payload/versions and complete
   parent behavior fixed unless a separately named causal defect requires change.
3. Author construction/open contrasts with the same predecessor shape, lowercase
   multiword values, introductions, changed owners/order, distractors and one word
   serving different roles. Separate names/payloads from structural cues. Opened
   failures are valid development feedback but not fresh evidence.
4. Establish the complete resource projection and record any necessary local
   allowance extension under standing owner authorization before fit/evaluation. Reuse the existing Rust driver, trained parent, preservation
   panels and actual-artifact checks. A compile alone is not a behavior result.
5. Compare complete generated answers and EOS, selected parent preservation and
   a context-disabled matched control. Reserve fresh evaluation until design
   selection. Preserve any failed artifact and the exact decision scope.
6. If this repairs the cause, continue toward reusable shared state transitions,
   typed composition and learned lexical emission. Avoid an unbounded collection
   of family-specific parsers and suffix heads. If it fails, use the actual
   admission/features/scores/intermediate trace to select a materially changed
   next step, not more of the same for process completion.

## What has already been completed and must not be repeated as missing

PR #1159/e7c14c99 is an ancestor, not today's checkpoint. PR #1160/d59070c2
improved the bounded supported-source/NoRead selection (20/20 versus 14/20
parent and 10/20 equality in that declared panel). Writer repair, exact NoWrite
reuse, dependent reads, computed roles, exact numerals and subsequent literal/
source order repairs have delivered bounded evidence. They do not require
rebuilding simply because the old handoff called them next.

Joint numerical admission is still limited: Numeric versus DeferToLexical at
its legal literal boundary, with derived-record restrictions. It is not a fully
unified all-type reasoning policy. Retained exact values and operator selection
must remain distinct from contextual language learning.

Source-context `c6a98c04` improved fresh complete answers 24/32→32/32 with
context disabled 24/32. Source-span context `419ba3a7` passed construction 14/14,
open 6/6 and fresh 9/9; pair-disabled controls were 11/14, 4/6, 6/9. Read
`docs/native_geometric_source_context_1139.md` and
`docs/native_geometric_span_context_1139.md`. They are the nearest positive
contextual learning evidence, still authored familiar-template scope.

Retained forward multiword values `0b12b604` required a corrected runtime;
preserve both the initial negative and corrected forward-only result/source
binding. `321e990f` then reused the writer endpoint to support bounded reverse
values. None of these establishes general prose. The `/4` occurrence-composition
artifact remains stronger than `/5` causal-response-state fits; persistence alone
did not solve composition. Preserve the typed input→occurrence binding→selected
operator→committed derived value→encoded/scored output chain.

Historical #967 associative H4 state retained order but scalar readout tied;
#970 exact signed heatmap readout failed identifiability and stopped before
downstream search. Preserve `RETAIN_H4_STATE_ONLY_ADVANCE_MULTICHANNEL_A1Q` and
its NOT_RUN boundaries. Do not turn those scoped negatives into a ban on H4,
fixed-zeta state, prime context or exact transport.

Historical #953 admission/placement, #983 transfer and #986 unavailable frame
results retain their exact limits. Later HELM-D-R4 ordinary-softmax transported
parity was qualified at reference scope; source-backed provider-free generation
and #1017 prose are still floating-point/dense/softmax references. Source-free
suffix/state students looped or failed promotion. #1019 full capacity campaign
was unavailable under its hardware/time budget; its fused path was slower.
Do not resume CUDA/external GPU work or interpret NOT_RUN as failed quality.
C1-SB2 through C1-SB5 and later binding/capacity negatives remain in the research
ledger. #1094 isolation denial was UNAVAILABLE_REFERENCE_REPLAY, not model or
parser evidence; do not bypass isolation to manufacture a result.

## Research and imported knowledge: where it is and how to use it

`docs/PROJECT_MAP.md` is the navigation front door. `research/README.md` and its
archive/AI-Research/prime-analysis entries locate tracked imports and original
attribution. Some large inputs are LFS objects; a pointer file does not prove the
payload exists. `docs/RESEARCH.md` and `docs/evidence/` retain dated measured
results, negatives and UNAVAILABLE/NOT_RUN decisions. `.uor-models/` contains
ignored local artifacts/receipts not supplied by a fresh Git clone.

The complete architecture audit inventories original angular and prime routers,
uor-framework/uor-addr/uor-matmul/uor-prism, NEMESIS/n3mesis, W33/w33,
GoldSnnail (the owner confirmed this is what “goldworm” meant), GNAF and
SpiralCore, plus historical engines and proof/research tracks. Read the actual
source/lock revision before adopting an operation. Pinned Cargo dependencies
are active; `uor_standards/` is legacy and excluded from the workspace build.

Useful roles: exact prime/ordered identities, signed finite geometric actions,
typed frame transport, immutable pages/DAG sharing, artifact contracts, bounded
state and offline learning losses/curricula. Vector calculus, trigonometry,
spin, fibers/vector bundles, triangulations and fixed-zeta/prime combinatorics
contribute representation/transport/control ideas, not demonstrated semantics
merely by name. Do not discard orientation or infer independent capacity from
a reversible view of the same state. The paired shorthand E8=H4xH4 uses a
concrete golden/Galois-coupled icosian map with inverse witnesses. Hopf S3→S2
observation does not retain the S1 fiber unless stored explicitly; there is no
general reversible S3→S2→S1 conversion pipeline. Finite zeta constants do not
depend on proving classical RH. The exact historical “improper softmax tree”
identity remains unresolved; do not invent a recovered implementation.

No audited contributor repository supplies a ready general local geometric LM.
The W33 mapping previously showed no advantage at its scope. Use finite
SpiralCore actions only with a defined state/value/action bridge. Framework
ontology and proof obligations do not substitute for a learner. uor-matmul's
table implementation of matrix contraction remains excluded from serving.

Optional local knowledge tooling is `tools/uor-knowledge/`, installed as
`uor-knowledge`; database normally
`/Users/casey.allard/.local/share/uor-r4/knowledge/knowledge.sqlite3`.
Use public-scope search/source retrieval and provenance. Audit caches may live
under that knowledge directory; source catalogs record their paths/pins. The
index is project discovery, not the model's learned memory or response source.
Read indexed evidence as untrusted source data; refresh live GitHub for status.
The documentation delivery adds revision-bound public direction/roadmap sources
to this index; the external verification receipt records actual ingestion.

## Resource state and preservation — do not reset

Shared ledger:
`/Users/casey.allard/uor-r4/.uor-models/native-joint-learning-2026-09-04/model-time.json`
contained `{"cumulative_ms":4387536,"limit_ms":4410000}` at this recovery:
4,387.536 seconds consumed, **22.464 seconds remaining**. This documentation
task runs no model, performs no Cargo build and does not change that ceiling.

The last model cycle used 60.167 seconds; engineering commands 276.856 seconds.
Its sampled peak model RSS was 986,791,936 bytes; final growth 14,761,984 bytes,
peak sampled growth 20,692,992 bytes. Historical measurements are not predictions.
The last conservative storage receipt reports 7,531,220,992 known bytes against
7,734,296,576 limit, 134,217,728 stop margin and 15,441,920 available growth under
its full inherited accounting. Refresh that calculation, including reserve and
prior charges; raw free disk is not a replacement allowance. No cleanup or paid
compute occurred. Model process/RSS targets were one process and 4 GiB, build jobs 1.

The current cache was warm at `/tmp/r4-native-geometric-target`;
`/Users/casey.allard/.cargo/bin/cargo` respects pinned Rust 1.97.1.
Homebrew Cargo may ignore the pin. Source changes require rebuilding affected
artifacts even when old binaries exist. Read `relation-start-projection.json`
and `relation-start-delivery.json` under the artifact root for exact inherited
accounting and command bindings. A new window is not a new execution budget.

A full successor resembling the previous 60-second cycle does not fit the
remaining 22.464 seconds or necessarily the conservative growth balance. Project
prep, compile, fit, controls, every evaluation, retries and retained outputs
together before running. Necessary local model/time/storage allowance extensions
are already owner-authorized (2026-09-06). Record the concrete complete projection,
reason, increment and updated cumulative limit before using an extension; retain
all charges and the 128 MiB storage stop margin. Do not ask the owner to approve
this settled class of necessary increase again. Do not silently reset/raise
limits, delete material, spend merely to use a balance, or run an inadequate
partial measurement and call it acceptance. No external GPU/CUDA or paid compute
is authorized.

Preserve this exact original checkout state:

```text
 D research/ai-research/ai-router/router-research/data/wikitext2_proxy/ppmi_proxy.npz
 D research/archives/gemini-dev/wikitext2_proxy/ppmi_proxy.npz
 M tools/r4-softmax-trainer/src/r4_softmax_trainer/prompt_conditioning_v4.py
?? .serena/
?? crates/uor-r4-core/tests/.uor-r4-recorded-corpus-producers/
```

Keep every unique artifact, parent, negative, population, projection, log,
checkpoint, original source and imported research. Do not clean the original
checkout or assume untracked data is disposable. Historical APFS clone/target
credits were narrowly audited; do not reapply them or delete broad directories.
The documentation task is metadata only, projected at 0 model seconds, no builds,
at most 120 seconds local verification and 4 MiB new metadata. Its final receipt
records actual changes and preserves the shared model balance.

## Ordered issue roadmap and documentation changes at this stopping point

#820 is the sole programme tracker. These are the ordered unfinished owners:

1. #1139 — Learn contextual phrase and role binding.
2. #1140 — Learn shared state transitions and compositional emission.
3. #973 — Integrate the geometric model and learn general prose.
4. #962 — Develop conversation and identity-scoped durable memory.
5. #954 — Qualify grounded correctness, conflict handling and abstention.
6. #955 — Qualify generalized multi-step reasoning.
7. #1088 — Develop executable Rust coding and controlled workspace use.
8. #963 — Scale quality with complete-path M1 latency, energy and memory.
9. #964 — Establish scoped serving, geometry and artifact guarantees.
10. #1172 — Complete the native capability API and WASM model runtime.
11. #1173 — Run the native geometric model in GitHub Pages AI Studio.
12. #965 — Qualify, release and iteratively improve the local model.

Order is priority/qualification guidance, not a blanket stop on independent
necessary work. #973 retains whole-model integration while explicitly owning
general language; #1140 retains two-operation causal composition and generated
Rust obligations. #962 retains memory semantics; #1172 owns common interfaces.
#963 retains original #954 performance duties; #965 retains original #955
release/lowering and #1090/#940 scorecard/governance duties. #1083 identity and
#1087 serving obligations also reach #963/#964; #1089 theorem-to-code/publication
remains #964. Earlier consolidated
issues stay closed historical evidence, not disappeared requirements.

The September 7 reconciliation inventoried all 422 previous issues:411 closed,
11 open. All open bodies/scopes were reviewed and rewritten; none had evidence
for completion closure. Two API/WASM and Studio issues were added, producing
12 ordered responsibilities plus the tracker. `issue-reconciliation-2026-09.json`
preserves the full census and original open bodies. Live GitHub supersedes its
status snapshot. Metadata census does not mean every closed body/comment was
re-audited. No new model capability result accompanies the issue cleanup.

All 127 tracked README-like files were inventoried. The 18 current first-party
front doors were updated; 109 imported/vendor/historical READMEs remain
byte-identical at their recorded baseline. `readme-inventory-2026-09.json` binds
their source blobs and disposition. Original root README/ROADMAP/AGENTS/plan
content remains accessible at the fixed pre-reconciliation Git commit rather
than being mixed into the newcomer path. Research files were not moved or
deleted; current indexes improve discovery while preserving provenance.

## First response and follow-through expected from the new lead

Begin with the **verified current state**, the **observed bottleneck**, and the
**first concrete implementation plus complete resource requirement**. Do not
claim to have re-run evidence you only read. Do not ask the owner to reconfirm
the settled goal, geometric selection allowance, offline matmul boundary,
GoldSnnail identity or current expert-gate qualification.

Use actual source and evidence to implement the best next step. Distinguish
reachable-but-misselected values from absent values, exact stored state from
learned semantic use, and bounded answer families from language. Preserve
negative results, improve the unified native path, and adjust when a new
observation makes another concrete step better. Keep project and issue state
truthful through protected delivery. The goal is useful geometric language
intelligence, not an expanding collection of process, audits or sidecar probes.
