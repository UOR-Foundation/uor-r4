# DeepSeek research brief: grounded computation in an owned dependent session

September 21, 2026. Successor to PR #1339 and its [principal review](shared-transition-review-2026-09-21.md). Read both completely, including the corrected replay/check receipt. Own a complete constructive research milestone, not only helpers or another static fixture score.

## Objective, leadership and autonomy

Build a native **resumable read → grounded computation → dependent read → answer/stop session**. Preserve the useful shared-action recurrence, tie useful computed states to their learned outcomes, and keep exact selected evidence independent from the instruction stream. The first computed result must be able to change the next query, selected occurrence and final answer. This advances geometric attention toward conversation/memory and coding/reasoning.

Codex remains principal mathematics/ML/systems architect and returned-run reviewer. You own substantive choices of learning method, finite representation, supervision, diagnostics and justified follow-ons. Explain disagreements with actual source, mathematical assumptions or measured behavior. Necessary local time/storage extensions are already authorized when recorded prospectively; no arbitrary short time window or fixed retry quota should dictate the science. Preserve physical limits, unique research and cumulative accounting. No paid/external compute is authorized.

Use the current stable Rust/native policy. Offline floating point, gradients and matrix multiplication are allowed. Serving targets bounded geometric routing, exact integer/table state updates and D0-b permitted low-bit additive maps. Keep the frozen runtime's separate contract. No hidden transformer/provider responses, authored answer interpreter, Python model implementation or silent MoE adoption.

## Recover enough context to make good decisions

Refresh origin/main and verify PR #1339's actual reviewed merge, including corrections, not merely your original submitted branch. If queued, locate the reviewed head and avoid duplicate delivery. Preserve the owner's original checkout; use an isolated full worktree.

Read README → canonical plan → current-state → model-direction → PROJECT_MAP, then AGENTS, execution policy and DECISIONS. Read this review/brief, original shared-transition result with corrections, the prior derived-state decoder review, and the geometric-attention and structural-memory/Hopf syntheses. Search the UOR knowledge store for the PR1339 principal review, corrected replay and owned dependent-session direction; verify current facts from the repository and artifacts. The latest knowledge record should point to actual source revisions. Historical negatives and superseded instructions retain their scope.

Inspect `learner/shared_transition.rs`, `learner/result_decoder.rs` and the actual shared-transition runner. Reuse specific donors from `addressed_attention/objects.rs`, `operation_transition.rs`, `shared_operator_refinement.rs`, `dependent_language/scheduling.rs`, `hamming_policy/policy.rs` and `shared_core.rs` as useful. The older lease, commit, phase and scheduler mechanisms are real source; their authored byte grammars and old artifacts are not a learned BPE solution. Do not repeat a broad census unless a concrete missing dependency requires it.

Owning work remains #973 integration, #820 programme, #1139 binding, #962 memory, #963 cost and #964 scoped invariants. Assignment means active work, not future ownership. Do not close broad issues after a finite component result.

## Start from the reviewed evidence

Original H4 counts reconstruct: 186/288 development, 48/128 length four and 10/64 reversal. Length four consists of 32 programs × four values, all sharing their first two primitives. The four showcased responses are four values on one program. These are exposed, narrow program-completion results.

Every reversal example selects the correct source; do not attribute the 10/64 reversal failure to reader confounding. Length four selects correctly in 124/128, so source ownership alone cannot solve the remaining computational errors. The input/operation coupling is still a real interface concern, but diagnose causes from the actual rows.

The so-called sequence-disjoint probe is value-index parity: values 0/2 calibrate and 1/3 form the supervised outer objective, while all programs occur in both. The final decoder refits all development observations. Preserve this scope and use actual program identities when designing a new split.

The original dictionary memorizes whole programs and emits one token on an unseen key. Its zero complete length-four score is not an advantage over shared transitions. Development already supplies all 32 first-step `(payload, primitive)` transitions and all 64 recurrent `(typed outcome, primitive)` transitions with consistent labels. The corrected Rust shared finite-state comparator tests that directly. The corrected loaded finite table reaches 288/288 development, 124/128 length four and 64/64 reversal while H4 remains 186/48/10. The four length-four misses are incorrect reads. Actual changed-source/order controls now succeed, but the noncommuting latent witness still misses the reversed final answer. Read the executed corrected audit; do not repeat the old cache comparison as the main claim.

The original stop labels are `remaining == 0`, and exhaustion forces return. This is supplied-program completion, not learned open-ended stopping or a learned hop count. The repaired artifact distinguishes explicit exhaustion and preserves legacy behavior. Source-causal controls, no-gold fallback, validated observed instructions and actual all-arm events are now the baseline; inspect any remaining limitations in the corrected receipt.

## Part A: make the learned computation coherent

Retain the same primitive action across occurrences. Fix observed transition grounding as part of the session milestone; avoid a separate indefinite toy-fitting campaign. A useful candidate is a learned injective map `Z[typed outcome]` with constraints

`Z[next outcome] = A[observed primitive] * Z[current outcome]`.

The first step joins `E[selected payload]` to the same outcome representation. Learn value/action/outcome assignments from declared development observations. You may use the development-fitted transition graph, closed paths, inverses, cycle consistency or a finite constraint search to identify a compact group action instead of repeating unchanged random restarts against a separately moving decoder. A gauge choice fixing one outcome's state to identity is representation normalization, not a hidden semantic label.

The [observed-action audit](../evidence/shared-transition-observed-action-audit-2026-09-21.json) already identifies the development graph: eight distinct bijective primitive permutations form a closed regular action with the Q8 order spectrum. The initial payload grounding is consistent across all observed primitives. Preferred constructive method: build the transition permutations from the declared training labels, identify a group isomorphism into a verified quaternion subgroup of the served table, obtain outcome coordinates by a reference-state orbit, and derive initial coordinates as `inverse(A[p]) * Z[first_target(x,p)]`, checking agreement across primitives. Normalize by a common right action if useful. Export the learned token-to-action, payload-to-state and state-to-outcome maps. Reconstruct this in Rust from training observations; the audit is a mathematical guide, not a parameter file to copy. Test relabelled token/outcome identities so hidden generator coding cannot accidentally supply the answer.

Do not import the fixture's true Q8 indices, operation powers, gold intermediate state or final targets into served inputs. Transition supervision is allowed offline when declared. Match that supervision for the ordinary finite-state comparator. It is legitimate to learn a finite transition graph and then factor it geometrically; it is not evidence of reduced training supervision or broad language unless measured.

A sufficient typed computational outcome can be a valid recurrent state. A repeated word token usually is not: context, scope, referent and phase may differ. Do not force every occurrence of the same BPE token into one computational state. If observed transitions are noninvertible or the action is nonfree, an injective embedding into the left-regular H4 action may not exist. Retain needed context or use an appropriate typed nonlinear/partial operator; do not invent an H4 capacity diagnosis from a failed fit.

Keep the direct finite-state model as a competent control and possible declared offline constraint source. Prefer the geometric representation where it gives useful sharing, compositional transfer or measured resource advantage. Do not hide a lookup baseline inside a geometric headline. Capacity changes are allowed with a reason and measurements; merely memorizing more complete programs is not the intended advancement. Count shared multiplication/inverse tables, the selector/local model, token maps and serialized overhead when comparing total cost; a small per-candidate artifact is not the entire inference footprint.

## Part B: own evidence and computed state across calls

Introduce or reuse a compact session/frame with exact source occurrence/version and selected payload ownership, valid geometric result, observed instruction/control position, response phase and terminal status. An integer absolute position alone is not a durable reference once a ring can wrap. Pin/copy the necessary payload or retain a valid lease; detect stale references explicitly.

Expose a real incremental operation such as `step(observation, frame) -> typed action/effect`. A normal uninterrupted run and a paused/resumed run must share the same state transition. Emitting a token must not reset the query, reread generated text as instructions, or lose the result. Different concurrent/interleaved sessions must not share mutable result state accidentally. Bind the artifact to geometry, learned parameters, tokenizer/configuration and any required state format.

Keep one source of truth for serving inputs. If a prototype accepts an explicit primitive program or instruction span, call it supplied structure and validate it against the observed request where applicable. Targets and intended operands belong to evaluation/training records, not the serving boundary. Preserve missing/unknown/conflicting cases separately from legitimate identity or terminal state.

Reversible transport and noninvertible control were already intentionally separate in the parent brief. Continue that architecture: geometric actions perform suitable computation; read, commit, replace, emit and stop have typed contracts. There is no requirement to turn the whole language engine into a group.

## Part C: a genuinely dependent read and meaningful continuation

Use an actual first selected payload/result to form the second query. A changed first source must change the computed key, the selected second occurrence and the final answer in the expected way. Do not receive the second reference, intended payload or gold number of hops from the fixture at serving.

Choose a small language-facing memory/relation task whose result depends on older evidence while the recent request stays fixed. Ground known primitive meanings and outcome lexicalizations in development, then hold out combinations, source assignments or renderings. Keep exactly which structure is supplied explicit. Include at least one derived answer absent from all source payloads when claiming noncopy computation.

Supplied-program completion can remain a valid control. For a learned continuation claim, choose actions from causal observations of result, query, memory availability and phase. Evaluate cases with the same request/program length but different valid continuations or termination determined by the retrieved content. A `remaining` counter, fixed two-hop schedule or gold answer length cannot stand in for that evidence. A safety cap may stop execution, but report cap exhaustion separately from a successful learned Stop.

You may pursue the parts in an order justified by implementation dependencies, run independent development diagnostics, and revise the method. The intended output is a complete owned, learned computation/access path and its measured limitations. A useful partial result is retained honestly; do not claim the full session solely because the explicit primitive executor works.

## Evaluation that can decide the next architectural choice

Before final design selection, state observable inputs, training and auxiliary labels, split identities, comparator information, and a practical useful-effect criterion. Use development openly. If an instrument is wrong, repair it and retain the old result at its scope. Do not relabel inspected final populations as fresh. Use independent program/source problems, not repeated renderings counted as independent trials.

Compare the retained geometric recurrence, the corrected shared finite-state transition table, the current local/NoRead path and relevant matched geometric/additive alternatives. A whole-program cache remains a memorization reference only. Report conditional computation success given correct retrieval and total end-to-end success separately. C120 final-state invariance is specific to additive actions; intermediate emitted sequences may differ. For claimed order-sensitive computation, test expected answers, not merely different latent states.

Exercise actual loaded artifacts through the same boundary for scoring, generation, interventions and timing. Save actual per-arm response and ending events from the scoring pass. Retain observed instructions, source/version/payload identity, pre/post state and validity, action, emitted token, ending reason and expected answer. Derive aggregate claims from those events; do not rerun only the winner to populate an unrelated log.

Meaningful controls include:

- Identical query/program with changed older source, plus an irrelevant distractor change.
- Changed operation/order with evidence held fixed; report any selection change separately from a fixed-selected-operand computation probe.
- Actual source removal or demonstrated eviction, separately from disabling a reader.
- Unknown input, missing decoder state, valid identity, explicit exhaustion and learned terminal decision.
- Pause/resume and interleaved sessions with retained state; an actual output perturbation if claiming state independence from emissions.
- First-source interventions that change the second query, selected occurrence and final output; disabled read/update/control effects through the same session.

Once useful, expose one complete relation/memory answer beyond the arithmetic fixture. Broader prose and executed Rust remain programme responsibilities; do not become trapped polishing a supplied finite circuit indefinitely.

## Wider geometry and research

The Q8 subgroup already makes the owner's spin/fiber idea concrete: signed quaternion actions retain distinctions lost by quotienting `q` and `-q`. A Hopf base-only observation can also discard that sign. Preserve it when task semantics distinguish ordered products. This is classical exact computation, not physical quantum hardware. A cocycle/bit representation can be explored as a correctly accounted implementation alternative; do not install hidden fixture labels or claim a new benefit without a matched experiment.

Relative H4/Spin, retained Hopf fiber, prime/ordered-n-let identities, fixed zeta phases, structural role/scope banks and finite spectral features remain available for their actual jobs. Exact identity is not semantic distance. A general S7 field needs coefficients and explicit update/read cost; it is not one normalized eight-dimensional point. Paired-H4/E8/S7 follows a concrete missing distinction, not a requirement to use every mathematical tool in every token.

Consult [Sequential Group Composition](https://arxiv.org/html/2602.03655v2), [On the Induction Bias in Sequence Models](https://arxiv.org/html/2602.18333v2), [Unexplored recurrent states and length generalization](https://arxiv.org/html/2507.02782v2), and [intermediate-state supervision](https://proceedings.mlr.press/v162/dan22a.html). These motivate reusable state transitions and careful reachable-state training, not a guarantee for our learner or permission to import their dense serving. Search additional primary sources when they change an actual design choice. State clearly what is a derivation, a measured result or a hypothesis.

## Resources and protected delivery

Refresh `/Users/casey.allard/uor-r4/.uor-models/native-joint-learning-2026-09-04/model-time.json`, free space and active worktrees. Use the absolute shared ledger, not a worktree copy. Project preparation, builds, fitting, evaluation, retries, snapshots and final delivery, including workers/RAM/new storage. Record necessary standing-authorized local increments before use; preserve cumulative charges and the physical reserve of 36,766,079,385 bytes plus 128 MiB unless a later owner instruction changes it. No arbitrary short timer or fixed retry quota; no paid compute or unsafe deletion.

Compile and exercise changed Rust paths with focused checks for real state, ownership, serialization, arithmetic and interface risks. Run actual loaded complete behavior. Debug timings are functional evidence, not optimized serving cost. Measure the full useful inference path before claiming efficiency; energy remains UNAVAILABLE without measurement.

Deliver named files through a protected PR, verify actual merge/source equality, and preserve the owner checkout. Update current-state, canonical plan, relevant README/front-door claims, evidence, resources and owning issues. Ingest source-bound knowledge and verify retrieval. Return actual learned/exported/generated behavior, meaningful controls and comparator results, limitations, resource receipts, justified deviations and one coherent successor. Do not stop after compiling helpers or completing another isolated score table.
