# Independent review and delivery checks — #1085

**Disposition: accept `CLAUSE_SEGMENTATION_SPECIFIED` for protected delivery.**
An independent Codex reviewer, separate from the contract author and the source
auditors, read live #1085/#1082/#1094, the #1082 independent review, actual #1077
reader/data and #1079 R4 source, committed artifact identities, and the final
specification/source-audit/roadmap/claim diff. No specification blocker remains.

The contract preserves the accepted reader/core, lexical codec and query,
four-fact context, complete soft mixtures, continuous valid-token R4 frames and
full-vocabulary output. Exact request/result/refusal records do not forward
grammar captures, roles or answers. Independent group partitioning, immutable
preparation, typed refusal controls, exact adapter/oracle fidelity, source
isolation, bounded accounting and divergent actions precede future observation.

The first review requested four corrections, all verified in the accepted text:

1. Complete field names/types and ordered hash framing.
2. Make artifact verification a wrapper startup prerequisite, with fixed request
   refusal precedence after readiness.
3. Add `CLAUSE_ADAPTER_PREFLIGHT_MISS` for authoring mismatch, preserving failures
   and keeping withheld evaluation `NOT_RUN`.
4. Keep preflight model-free and include all row forwards in the bound. Full
   tensor copies would consume at least 134,604,800 bytes (128.369 MiB) before
   inputs/receipts; streamed byte comparison, digests and bounded failure details
   satisfy the frozen 128 MiB output cap.

Reviewed specification SHA256:
`85f928fec94fa0f6793cff4c35e1fc8c9cba691739d34db272465766c7c9dab1`.
Reviewed source-audit SHA256:
`83f6533c7296f5b3c1edefd4f182eafac83edc71f2de9cb67203504ca62880e4`.
The original external-download verification belongs to the separate source
auditor; this reviewer checked its stated constraints against the contract.

Live #1094 is an open, unassigned #973 child with #1085 as its blocker. It owns
later implementation and immutable input/runtime binding. Before closing #1085,
pin the ledger sources and #1094 handoff to the accepted commit. The new claim
correctly remains `Unproven` / `SPECIFIED_NOT_RUN`. Acceptance permits delivery
of this specification; it is not empirical run admission or raw-text capability.

## Named validation and storage review

- Claim-wording check passed; `git diff --check` clean.
- All 33 local links in new/changed prose resolved; all seven claim IDs were
  unique and all evidence references resolved. New source SHA256 entries matched
  their actual bytes. Immutable source URLs are completed after the content
  commit exists; no source assertion is revised by that pinning.
- No parser implementation, model load, population preparation, fitting,
  evaluation, replay, broad QA or mathematical proof ran. CI/queue compatibility
  statuses carry transport acknowledgement only.
- The isolated worktree occupied 1,164,432 KiB at inventory, with no Cargo target
  or trainer environment created. The existing knowledge directory occupied
  974,656 KiB before this task's ingestion. The volume had about 52 GiB available.
  This documentation task needs no model download or build cache. Retain the
  worktree until protected delivery and index verification; do not evict cited
  source snapshots or rebuild the unaffected code graph.
- The original checkout's two tracked deletions and untracked `.serena/`, test
  producer artifacts and `tools/` were unchanged. No worktree, model, corpus,
  diagnostic evidence, sealed file, user material or cache was deleted.

#1079's weak-control terminal and #1082's descriptive findings remain intact.
#973 remains open and #954 blocked. The next task is the parked #1094 comparison
under the merged specification, starting with implementation and independent
input preparation rather than a new fit.
