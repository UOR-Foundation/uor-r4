# DeepSeek execution prompt — recover the frozen prior's result and diagnose its loops

Work on **UOR-R4 Geometric Language Model**, repository `UOR-Foundation/uor-r4`. Read this whole prompt and `docs/integration/frozen-prior-review-2026-09-20.md`. Audited base is `74fef0886ca3b14ff90943c8677d6b7815064198` (PR #1300); refresh origin/main and include the protected delivery of these instructions.

**Deliver and execute a corrected evaluation-only mode for the existing step-512 artifact.** Preserve the trained parameters. The reported sign fix is insufficient: observations are collapsed by the permutation map and windows are bootstrapped as documents. Repair those concrete defects, reconstruct the original population, replay the frozen artifact, add the missing count references, and diagnose the observed greedy loops. Do not retrain the model to repair an evaluator or reconstruct an unsaved intermediate checkpoint. Changing the corpus to 4 MiB would create a new experiment, not validate #1300.

## 1. Authority, recovery and cost

Read AGENTS.md, `docs/integration/DECISIONS.md`, execution policy, current state, canonical plan and the parent review. Rust data/model/evaluation lifecycle; offline float/matmul allowed. D0-b permits bounded <=4-bit/ternary add/subtract/shift/table maps at serving, no floating/transcendental computation or multiplier instruction in the declared numerical kernel. Geometric routing/state remains the priority. No transformer backbone, runtime teacher/provider, Python model, paid compute or deletion. Frozen R4G1 keeps its own contract.

Fetch origin; inspect active PRs and #973/#820/#963/#964, original checkout, worktrees and shared ledger/storage. Reuse a clean isolated full worktree on `codex/…`, preserve other work and assign active work appropriately. Reuse the prior architecture/import maps; no broad census.

Required retained inputs in owner `/Users/casey.allard/uor-r4/.uor-models/realtext-prior-2026-09-20/`:

- `attempt-1/prior_realtext.cpl2`,454,788 bytes, SHA256 `cd5a3aa1b804ccc3582a4a25090e25c862489e0bc4e5bb86c1d1f363a7338a00`.
- `attempt-1/prior_realtext.ckpt`,19,192,104 bytes, SHA256 `1c3dfedb2248ec76c21f320b7ecec585c7a522a2d47e35ed5dc9eaca2454bff2`.
- Saved source/derived tokenizer JSON; derived SHA256 `a7ac75b68aa997fe7cc338d25d843157f2dc9d4265fb2f958ee779bb04828d6f`, source SHA256 `9ca9acddb6525a194ec8ac7a87f24fbba7232a9a15ffa1af0c1224fcd888e47c`.
- Sealed result/generation/manifest and sibling `inputs/docs`, whose 850 files match Git corpus commit `e9c04e80`. Verify rather than reading mutable current docs.

Current recorded ledger: owner `.uor-models/native-joint-learning-2026-09-04/model-time.json` = `152238565 /154400000 ms`, remaining 2,161,435 ms. Proposed complete **1,800,000 ms (30 minutes)**: 600 s evaluator/focused fixes/build/tests, 300 s input/identity/reference preparation, 450 s frozen scoring/controls, 150 s loop diagnostics/generation, 300 s evidence/delivery/stop reserve. Record the refreshed complete projection before work, measure the new path and revise before overruns. Necessary local allowance extensions are already authorized when reason/increment/new limit are recorded before use; do not ask again. Charge actual preparation/build/evaluation/retries without double-counting elapsed intervals.

One model worker, at most 4 Cargo jobs, <=8 GiB peak RSS, <=512 MiB new retained/temporary data plus <=1 GiB incremental reused build output; preserve 128 MiB protected storage margin. Use `scripts/project_storage_inventory.py` and account for overlapping paths. No new corpus download or full checkout is needed. A session-token stop is distinct from local wall-time exhaustion; preserve completed evidence if one is reached.

## 2. Small implementation changes, no new learner

Add `--evaluate-only` or a thin sibling evaluator sharing the corrected helpers in `prior-learning-realtext.rs`; load the existing CPL2. Do not invoke Adam, change weights/bias/scales, extend training or start a V4 gate/comparator. Keep the legacy training mode from silently using the broken evaluation helpers after refactoring.

Claim a fresh report attempt immediately after argument validation and before loading models/preparing data. Keep the original sealed attempt immutable. Persist recovered immutable data and new results under the new unsealed root, check every required write Result, verify the expected file set, then seal/verify with zero unlisted files. Reject/report incomplete output rather than declaring success from a partial manifest.

Make the two value-preserving primitive corrections identified in review: replace widened ternary `weight * (1i64<<shift)` with signed shifts, and call shared `PriorCore::validate` from core construction as well as loading. Before these primitive edits, capture integer-score and greedy-output fixtures using the pre-change inference code and the loaded retained artifact on a fixed small context set, including recorded loop states. Repeat with the same artifact after editing and require exact equality. Artifact-byte equality alone is not inference-code parity. Keep operation claims scoped; inspect the relevant optimized numerical symbol if practical, but no broad release/energy campaign.

Do not claim complete checkpoint identity from v3. This task need not resume training: read the final checkpoint's metadata/permutation for exposure recovery only. Do not silently overwrite its legacy identity or treat its composite digest as a tokenizer SHA. Record unresolved training-source/binary and continuation identity limits; future actual continuation must validate seed, expected nonzero data identity, cursor, frozen bias and complete on-disk state first. No additional training update is authorized by this evaluation task.

## 3. Recover occurrence identity and original exposure

Materialized archive is 850 files / 88,016,240 bytes; the actual eligible corpus is 429 `.md` documents/7,609,837 UTF-8 bytes. Hash-bucket split yields 355 fit, 38 tune, 36 development-pool docs. Preserve the old collection/split order to reconstruct its population. The unselected tune docs are available for tuning count-reference smoothing. The old 4 MiB bound was exceeded; disclose actual population, do not shrink it retroactively.

Persist a recovered manifest with corpus Git commit, file path/content hash/bytes, duplicate group, split, tokenizer hashes, actual token IDs, window offsets/lengths and target records. Use a unique observation ID such as `(content_id, window_token_offset, target_offset)`. Two identical context pairs with different occurrences or targets must remain distinct observations. Report exact duplicate-content groups across splits; preserve the legacy population and disclose any overlap rather than silently relabeling contaminated records as held out. If overlap is found, add a clearly labeled non-overlapping analysis where support permits; a new clean refit is a later decision, not this run. Use the shared n−1 iterator with explicit PAD-first-prediction semantics. Reproduce the old length>=3 window filter for the legacy panel and record discarded tails; do not call it the general desired tail policy.

Reconstruct the **legacy scored panel**, 32 docs×3 opening windows at offsets 0/64/128, 96 windows/6,048 targets. Source selected 36 docs but global truncation discarded four; verify actual counts from tokenization. Link each saved old loss row to its actual document/window. Regroup rows to confirm original aggregate arithmetic before invoking the model. The old result has no full logits or saved 0/256 artifacts. Preserve those historical curve points as recorded, without claiming to replay absent weights.

Recover optimizer exposure from the final CPCK: step 512, cursor 4096, pass 0, permutation length 38,987. Map the first 4,096 unique IDs to the reconstructed ordered fit-window list; assert the range/uniqueness and sum actual n−1 targets. Distinguish this exposure from the 2,446,208 targets used to fit the frozen marginal and from the four discarded-probe updates. Build full-fit counts and consumed-conditional counts without updating model parameters. Verify frozen bias codes against recovered full-fit counts with smoothing applied once.

The old composite identity hashes source/derived tokenizer SHA strings and corpus revision text, not this complete manifest. Preserve that interpretation and bind old artifact SHA, recovered manifest SHA, new evaluator source/binary/compiler and numerical config in the new report. Do not pretend this retroactively creates a missing historical binary receipt.

## 4. Correct the evaluator with falsifying fixtures

Represent every scored record explicitly: document/window/target ID, `(prev_or_PAD,cur)`, fixed target. Group records into true document clusters by content ID. Keep a deterministic key alignment across scorers; fail on missing/misaligned records rather than truncating to a common minimum.

**Permutation:** for each document and PAD/non-PAD stratum, construct a seeded Fisher–Yates bijection over occurrence indices. At recipient record i, supply donor context `(prev,cur)` from record pi(i) and keep target_i fixed. Persist the complete map. Do not key observations by context values or mutate targets through a deduplicating HashMap. Include repeated contexts with differing targets in a fixture: the old implementation must fail it. Assert exact target-vector identity, occurrence count, context multiset per stratum, bijection and actual changed-context/changed-association counts. Prediction caching by context is allowed; merging observations is not.

Singleton strata stay unmodified and explicitly non-permutable. Multiple windows of the same document can provide multiple PAD records, so PAD is not universally non-permutable. Report full-panel intervention and the matched eligible-subset penalty separately. Reuse one saved permutation per panel for all scorers; never search permutation seeds for significance.

**Constant invariance:** the frozen bias-only predictor must have identical per-occurrence losses with original/permuted contexts because targets never move. Assert exact equality where arithmetic ordering is identical, otherwise a justified tight tolerance. The exact unigram must also be invariant. The old step0 CE 9.13163 versus permuted 9.1364 is a failed control, not a learned effect. Include this assertion before evaluating the headline model.

**Aggregation and uncertainty:** sum all a document's window losses/counts before macro/interval calculation. Token-micro is sum(loss)/sum(count); document-macro is the mean of document means. Test unequal windows/counts across at least two documents so an accidental window-macro cannot pass. Compute paired gains as reference minus model and penalties as permuted minus unpermuted. Include a tiny known-sign reference fixture.

Bootstrap aligned document clusters with 2,000 fixed-seed draws, recomputing ratios of resampled sums/counts for micro. Save scorer sums/counts for every document and the bootstrap seed/method; min support is 20 scored documents / 1,024 targets and 20 eligible documents / 1,024 eligible targets for the intervention. Below support or failed invariants  => INCONCLUSIVE/INVALID, not PASS. Nominal intervals on correlated repository docs are screening evidence, not final held-out qualification.

Run these focused fixtures, then execute the evaluator in this same run. A new test count is not the deliverable.

## 5. Replay the frozen artifact and add the missing references

Load `prior_realtext.cpl2` through its real loader, validate dimensions/config and identities, and keep its bytes unchanged. First reproduce the legacy panel's unpermuted micro CE≈7.14247215 and frozen-bias CE≈9.13162977 within a declared numerical tolerance. If they differ materially, diagnose tokenization, offsets, artifact semantics and source identity before publishing a corrected gate. Do not retrain to force agreement.

Evaluate the same frozen artifact on:

1. The reconstructed legacy panel, with corrected document aggregation and occurrence permutation. Report corrected vs historical columns and which historical quantities were invalid.
2. A **separately named spread-position development panel**, frozen before any new scores: use all 36 original dev-pool documents, up to two distinct windows per doc, deterministic first/last or evenly spaced positions spanning the document rather than only openings (<=72 windows). Short documents contribute what exists; record that policy. Save all IDs, support and overlap with the legacy panel. This is additional open development evaluation, not fresh final held out; never substitute it silently for the old population.

For both panels report uncapped stable bits/target from integer logits scaled by 2^-F, exact full-fit add-one unigram, the identical frozen quantized bias, contextual model, whole-context permutation and both position knockouts. Validate uniform V4096=>12 bits and score-offset invariance using existing checks. Knockouts remain potentially out-of-distribution diagnostics.

Add corrected interpolated one-/two-context count references using the **same records including PAD**. Adapt the verified count structures/tuning rule from `geometric-realtext-ceiling`, not its index2/floored evaluator or the old pilot. Assert valid normalization and finite unseen-event probabilities. Tune a small fixed smoothing grid on the 38 tune documents with a bounded, preselected spread of windows, never on either dev panel. Save selected hyperparameters. Fit full-fit conditional counts and separately consumed-only conditional counts backed by the same full-fit unigram supplied to the learner. These are diagnostic references, not model-fitting arms, Bayes ceilings or Kneser–Ney unless actually implemented and verified.

Report each model/reference on all targets and on context pairs unseen in consumed fit; include counts/support and distinguish full-fit unseen from consumed-fit unseen. Freeze stratum membership from each original recipient context. Keep those same observation IDs and denominators across references, knockouts and permutation; do not recompute membership from donor or knocked-out contexts. If a stratum is empty or too small, say so. Cache computations where useful without dropping observation multiplicity. Save per-document sums/counts for every reference/intervention, not only contextual losses.

Keep the historical threshold: at the frozen step-512 artifact, at least 0.10 bits/target gain over both exact unigram and quantized constant, paired intervals excluding zero, plus positive eligible-subset permutation penalty with its interval excluding zero and all identity/control/support checks passing. Report a **corrected legacy-panel decision** and a separate spread-panel decision. A corrected point estimate is not the old predeclared experiment fully executed: population/serialization deviations remain disclosed. No weight updates, new seeds or adaptive decoding to improve the verdict.

## 6. Diagnose greedy repetition with the same artifact

Reproduce the saved final outputs from the old three prefixes, correctly labeling them as one document. Then freeze three prefixes from **three distinct spread-panel documents** covering short/medium/long length strata by a deterministic rule before generation; verify distinct content IDs. Generate 64 greedy tokens with the loaded artifact and bias-only control. Save prompt/output IDs and decoded bytes, tie policy and exact contexts; keep all repetition and failures.

For each trajectory, record the visited ordered token pairs, chosen token, top-two integer scores/margin, number of tied maximum scores and first repeated pair. Report cycle entry and period only when witnessed within the bounded trajectory; do not enumerate all V² states. Inspect reached `(32,32)` and, where relevant, `(198,198)` explicitly, decoding IDs with the pinned tokenizer. Verify the fixed-point condition rather than inferring it from text formatting alone.

At loop-entry/fixed-point pairs report fit and consumed-fit support, top empirical successor counts, model top choices/margins and corrected backoff choices; add the model's teacher-forced loss on actual observed successors where available. Run the same bounded prompts through the chosen count baseline as a diagnostic. This distinguishes a model-specific unsupported loop preference from one shared by local statistics; it does not by itself prove an optimization, quantization or context-capacity cause.

Do not add repetition penalties, token blacklists, output overrides or a new sampler in this task. Literature shows that decoding matters, but sampling away a loop would not establish older-prefix reasoning or useful learned geometry. The theorem that a finite deterministic two-token map eventually cycles does not explain the observed short cycle or guarantee that adding a finite geometric state removes it.

## 7. Roadmap decision and delivery

The larger goal remains learned geometric state/routing with exact addressed memory and useful conversation/coding on M1. The current modulo-120 table is unused by the prior. A frozen two-token predictor cannot distinguish identical local tails; this motivates a subsequent learned prefix-state/read experiment, with disabled-state, same-tail/different-prefix, order and matched capacity/cost controls. Exact group transport avoids numerical drift but not wrong actions or finite-state collisions. Preserve orientation and the derived golden/Galois companion relationship.

Use this run to select one next seam from evidence. If corrected contextual gain survives across positions, retain this artifact as the local baseline and specify the smallest learned ordered prefix-state/read experiment; do not demand another unchanged prior training run. If gains or generation support fail, identify whether the observed gap is data/population, local-statistical learning or a concrete decoder/credit defect before selecting the change. A negative result does not mandate indefinite baseline tuning. Do not implement/fund a new geometry or decoder family in this evaluation run. The new RiLM distance-decoder paper is research-only pending matched evidence and integer lowering.

Run touched-package offline check and individually named, nontraining evaluator/serialization/static score-parity tests, `~/.cargo/bin/cargo fmt --check` and `python3 scripts/check_claim_wording.py` for claim edits. Exercise the actual evaluator and generation. Do not use the broad `prior_learning` test filter: existing tests include Adam update loops, which are outside this evaluation-only run. No blanket historical suite. Record elapsed time, peak RSS where available, report/storage sizes, actual model hash, evaluator source/binary identity and exact commands. No energy claim without physical measurement; queue acknowledgements are not tests.

Deliver named changes through a protected PR; no direct-main/admin bypass. Verify actual merge and content/tree equality. Update README, current-state, canonical dependency status, continuation pointers, EVIDENCE and resource/storage receipts; append corrections while preserving original raw results and sealed artifacts. Update #973/#820 and relevant #963/#964; keep broad issues open and update linked project items only if they exist.

Final report: the recovered corpus/exposure manifest; original vs corrected vs spread-panel results and decisions; applied permutation invariants and real document counts; all reference/stratum outcomes; old and three-new-document generated bytes plus loop diagnostics; primitive changes and score parity; artifact/report hashes; explicit absent intermediate artifacts and remaining continuation limits; actual cumulative resource charges and any previously recorded extension; delivery identity; and one evidence-supported next mechanism. Do not stop at a corrected sign, compiled evaluator or a proposed replay: execute the frozen evaluation and report its outcome.
