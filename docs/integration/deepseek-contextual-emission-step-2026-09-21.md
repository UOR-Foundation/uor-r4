# DeepSeek execution: genuine contextual transformation with shared low-bit emission

You are a research contributor to the UOR-R4 Geometric Language Model. Complete one learned Read -> geometric Update -> shared Emit primitive on a task that truly requires older context. The last run wired an update path and completed short confidence continuations, but its new instrument did not test the intended causal capability. Repair that question and train the shared residual in one bounded constructive run. Do not stop after compiling helpers, producing a design note or opening an unexecuted pass.

Use your own mathematical and engineering judgment. You may choose a smaller equivalent factorization, a sound optimizer, useful donor code and practical prospective criteria. Explain substantive choices before opening final outcomes. Keep geometry primary and distinguish an authored capability instrument from broad language or reasoning. Do not turn this into another scalar-confidence feature sweep, a wide architecture survey or an indefinite refinement campaign.

## Recover the actual state

Refresh origin/main. Verify PR #1334's actual merge and reviewed tree, including principal corrections after submitted head `bca3d224f1f4ece17d2c1ec9c69083d7e13cb3df`; the original PR was open when reviewed. Preserve the owner checkout and other agents' work; use an isolated full worktree and a codex branch. Read AGENTS.md, README, project-track/current-state/model-direction, DECISIONS.md D0-b/D1/D2 and the stable execution policy. Read these completely:

- [Principal review of this run](read-conditioned-review-2026-09-21.md) and [independent evidence audit](../evidence/read-conditioned-principal-review-2026-09-21.json).
- [Corrected read-conditioned result](read-conditioned-result-2026-09-21.md), the dated [design](read-conditioned-design-2026-09-21.md), and the prior [confidence review](reader-confidence-review-2026-09-21.md).
- [The previous comprehensive prompt](deepseek-read-conditioned-state-step-2026-09-21.md), now executed only in part; retain its intent, not its stale scheduling.
- [Current/retired mechanism synthesis](geometric-attention-mechanism-synthesis-2026-09-20.md) and [structural memory/Hopf direction](structural-memory-hopf-direction-2026-09-20.md).

Use project knowledge search/get_source for scoped continuity and actual source inspection for mechanism decisions. Read live issues #820/#973/#1139/#962/#963/#964 and any project items. Relevant primary research and a bounded independent reviewer are useful; no tool or paper's existence justifies an unrelated campaign. Rust preparation/training may use floating point and matrix multiplication; serving follows owner-adopted D0-b's bounded at-most-four-bit additive/shift/table operators, with no multiplier/floating numerical kernel and no hidden transformer/provider response. Historical skill language does not override D0-b.

## What survives and what does not

The frozen confidence artifacts actually produce six-token continuations on seven prompts. H4 reads 23/42 steps, categorical 22/42, and all 45 source/payload references verify; ReadDisabled equals local 7/7. This is useful short rollout execution, not demonstrated complete useful responses or full changed-source/tie/logit qualification. Prose remains degenerate. Old panels/artifacts repeat exposed `reader-confidence-3` evidence; the corrected own-parent CE/absence failures still stand.

The new instrument has several independent defects:

- Its target is the fixed partner of `qrole`, already in the recent query suffix; random older payloads do not determine the answer. An output head could solve it without a read.
- Tokens end in `[qrole,qkey]` with no answer sentinel, but extraction and generation stop before the final query key. The retained 22 value-domain IDs are query keys, not the intended source values.
- Accuracy-only coordinate search cannot learn when the finite row ceiling is zero everywhere. Both saved maps exactly equal their initializations. Their NLL differences and state changes are seeded behavior, not learning gains.
- The alleged categorical arm is the identical H4 update with another initialization.
- The reported update first-token accuracy actually scores the fourth emission, while local scores the first; only four update trajectories were retained.
- RLRC v1 reload objects were compared then discarded; parameters do not bind parent/reader/tokenizer/geometry/fit identities. The original source inventory has three files and omits the new module.

Principal source corrections repair the full-prefix helper, first-emission indexing/equal horizons, honest alternate-H4 labels, use of fail-closed reloaded objects and future module hashing, plus bounded canonical parsing. Focused tests qualify those interfaces. They neither rerun the model nor make the historical role-partner task context-required. Preserve every original sealed report and the initialized artifacts. Do not describe a new result as a replay of old semantics.

## Establish the causal task before fitting

The decisive question is: can the SAME query use DIFFERENT relevant older evidence to produce DIFFERENT uncopied answers?

Use paired prefixes. Keep the entire recent suffix, source position/order, distractors and every unrelated token fixed. Change only the relevant older payload, and require a different target in the paired example. Put targets in a disjoint output-token bank so each answer is absent from every prefix token and every admitted payload. Maintain several plausible same-key sources so selecting the right occurrence matters. A minimal example is:

```
prefix A: ... (source_role, key, value_a) ... (query_role, key)
answer A: learned_output_class(value_a)
prefix B: ... (source_role, key, value_b) ... (query_role, key)
answer B: learned_output_class(value_b), different from A
```

The generator may know the intended source and transformation offline. The predictor must see only the actual prefix, learned representations and its selected owned occurrence. No gold source index, semantic class, evaluator stratum, operation ID, expected next query or hidden word parser enters inference. A fixed authored decoder that implements the desired transformation is not a learned shared emitter.

Train each arbitrary value-to-output mapping somewhere in development, then hold out key x value x role-family bindings, source locations and distractor configurations. A new random seed alone is not an unseen composition, and a never-trained arbitrary label mapping cannot be inferred without structure. Name the first result a learned contextual value transformation. An operation-dependent variant f(value,operation) is welcome if the operation is actually present in causal model inputs and the split tests compositional transfer; it is not required to demonstrate the first primitive.

For each pair verify identical local logits, different targets, target absence, actual source availability and no shortcut through record length/order. Include the frozen local and scalar-copy controls. A deterministic suffix-only predictor cannot answer both different targets from identical local inputs; with absent outputs, positive copy boosts cannot repair that deficiency. Include an appropriately fitted suffix-only control if query remapping could otherwise masquerade as contextual learning.

Inspect collisions in actual served signatures, e.g. `(q0, relation, payload_code)` and the emission features. An impossibility claim from a collision must also account for the unchanged local logits: distinctions can survive in z_local even if update inputs alias. The current `q0=local.query_state(last_token)` is a coarse final-key code, while directed relations can erase absolute role identity. If a required operation or value distinction is lost, retain one bounded causal descriptor from the actual query/value, matched in controls, rather than pretending a head can recover erased information. This is a targeted interface repair, not permission for a vocabulary/width sweep.

## One shared causal boundary

Use the complete observed prefix, including its final query key, for extraction, scoring, changed-source interventions and generation. Keep target/expected-answer metadata outside the prediction function. Use the same selector/read_step and update/emission implementation for all paths, including loaded artifacts. Never seek an earlier candidate-bearing position when the intended pool is empty; retain that example as NoRead with its true denominator.

First run a cheap finite-row scan on the correctly indexed DEVELOPMENT positions. Its scope is `argmax(z_local-u(q0)+u(q))` for each of 120 frozen rows. Save the sufficient row/target margins so the result is independently reconstructible. Zero means that frozen row family cannot emit those targets at those positions; it does not prove selection or input representation adequate. Do not spend a coordinate-search campaign on a zero ceiling. Preserve the frozen-row diagnostic as a baseline and proceed to the learned shared residual within this same run.

Check selected-source correctness separately. A source-correct/oracle-source diagnostic may localize learning problems, but ordinary end-to-end results and interventions must use the model's own read. Source correctness cannot be assumed because candidates exist or a group product executes.

## Learn a small shared emission residual

Keep source admission, ring/candidate bounds, local baseline and frozen selector fixed initially. A suitable model is

```
q0 = bounded causal query state
r  = directed relation to the selected actual occurrence
v  = bounded learned descriptor of its actual payload
q1 = (q0 * T_theta[r]) * V_theta[v]
z  = z_local + W_theta (R_theta[q1] - R_theta[q0])
NoRead or UpdateDisabled: q1=q0, residual exactly zero
```

Use exact signed-H4 composition with declared multiplication order/orientation. Preserve q0 and exact occurrence/version ownership outside q1. Do not infer equivariance from the presence of group products; derive a frame transformation law before making that claim.

Inspect `learner/query_read.rs::row_scores`, shared residual arithmetic, `QueryTrainer`'s nonconstant seeded state rows/zero-output initialization, hard export and prediction-loss gradients. Adapt the smallest useful existing Rust path. A shared state-row table or another equally bounded factorization is allowed if its parameter bytes, inference operations and capacity matching are explicit. Do not add a vocabulary head per source/occurrence, a hidden dense backbone or a special answer-only serving path.

Learn under NLL or a finite logit-margin objective, rather than requiring an immediate integer accuracy improvement. Discrete transport search and/or offline gradients are allowed. Record initial versus final parameter hashes, accepted coordinate changes and hard-path loss to demonstrate that learning actually occurred. With nontrivial update features and a seeded nonconstant R, zero W gives an exact baseline start while W can learn. Identity updates plus flat features/output can deadlock; show one finite sensitivity/learning step on development before investing in the fit.

Check the residual's attainable score range against the frozen local target-to-winner margin. Low-bit weights plus an inherited small shift can make a correct argmax unattainable even with useful features. For width d, coefficient bounds |W|<=w and |R|<=r, and total shift s, the pairwise residual adjustment is bounded by `4*d*w*r*2^s` integer logit units (64*2^s for ternary width16). A larger local target deficit cannot be overturned; compare in the same f_bits units and handle the declared argmax tie rule. This loose bound is necessary, not sufficient; q1=q0 gives exactly zero regardless of scale. Select and justify one bounded fixed scale/shift from development before final evaluation; preserve wide intermediate arithmetic and exact no-op behavior. An unconstrained floating head may be a declared diagnostic, never substituted for the exported hard-serving result. Do not make this a broad dose/width sweep.

For V=4096 and width 16, two-bit packed R/W coefficients are about 16864 bytes before metadata; caching 120 full-vocabulary i32 rows is 1966080 bytes plus their construction cost. Report actual width/precision, packed and materialized sizes, per-prediction row accesses and startup work. A table representation still has parameter traffic and finite memory capacity.

## A real comparison and the controls that matter

Use one truly different update algebra, with identical information, state count, learned map count, shared-emitter capacity and learning budget. One concrete comparator is cyclic categorical C120:

`q1=(q0+T[r]+V[payload]) mod120`.

Its fixed table can match the H4 table footprint; it is abelian and not an H4 relabeling. Name it specifically. Sharing the frozen H4 reader means this compares update algebras conditional on that reader, not all geometry versus all nongeometric computation. Another justified matched ordinary finite-state operator is welcome. Different initializations of the same algebra are seed controls only.

Required comparisons on identical rows:

- Frozen local/NoRead, scalar-copy parent and the finite frozen-row diagnostic.
- Learned H4 update plus shared emitter, and the matched alternate operator.
- ReadDisabled, UpdateDisabled and a direct suffix-only control where needed.
- Original versus changed selected SOURCE OCCURRENCE, with admission, selection, state, logits and full generated continuation recomputed.
- If source selection fails, a separately labeled source-correct diagnostic; never merge it into the serving headline.

Score the actual first continuation at `tokens[prefix_len]` for every arm, with equal declared generation horizons. Retain all small-task trajectories and exact source/payload references. Report whether both members of a changed-source pair are correct, not only aggregate accuracy. An abstract relation-index perturbation is a sensitivity check, not a replacement for a real causal context intervention. Verify disabled integer-logit parity on the actual loaded models, including empty pools and NoRead.

Keep the old present/absent/text screens against each arm's named parent, with both counts and CE and their complete-stream denominators. Label old panels as exposed replays. The new capability test and ordinary-text preservation are different claims; report all failures, and retain useful scoped progress without promoting an integrated language model. Prospective practical margins may change for a sound reason with effect sizes and uncertainty, never retroactively erase a failure.

## Selection, evidence and execution discipline

Use development and tune for design/selection; inspect the actual learned hard artifact before opening final outcomes. Freeze one candidate per declared comparison and evaluate one held-out binding population. Use genuinely eligible source-separated text if making a new text-generalization claim; the old 36 Dev documents are reader-exposed. Disclose local-prior exposure separately.

Save compact sufficient statistics for fit AND tune: complete sequences/prefix boundaries, offline target/source annotations, served occurrence/reference, q0/r/v/q1, pre/post local and residual margins or logits, parameter changes, each criterion and its unit, cluster identities and all short generated continuations. Retain enough information to independently reconstruct oracle and outcome counts; a JSON aggregate or report seal does not replace it. Use paired sequence/family uncertainty with honest small-sample limits.

Bind actual executed source modules and binary, tokenizer, local prior, source reader, exact group/operator table, emitter/transport parameters, fit documents/sequences/seed/config and format version. Record the last edited source hashes at execution. Consume the independently loaded returned object throughout prediction/evaluation/generation, and exercise negative identity/version/truncation cases. RLRC v1 is parameters-only; provide a complete expected-dependency manifest or a versioned artifact, without overwriting historical files.

Build a targeted pass/runner for this experiment or reuse verified frozen artifacts. Do not rerun every legacy fit/panel merely to reach a new pass near the bottom of a large harness. A report/metadata repair should not require fitting again. Claim each report root exclusively before loading a model, seal completed attempts, preserve failures and never append derived material beneath an old sealed root.

Continue through implementation, actual fit, controls, generated behavior and delivery within the recorded budget. If a diagnostic reveals a small correctable defect, fix it before final evaluation. If the bounded operator fails, retain the result and distinguish admission/selection, input collision, residual range, optimization and output generalization using the saved traces. Do not automatically add dimensions, reopen scalar confidence calibration or run an endless retry campaign.

## Roadmap, resources and delivery

A useful one-read transformation is the prerequisite for dependent Read -> Update -> Read/Emit with learned stopping. Structural roles/lifetimes and retained Hopf fiber support witnessed older-context aliases; they are not abandoned and need not block this primitive. Prime/exact occurrence identity, signed H4/Spin, typed paired-H4/icosian geometry and shared operators remain central. S7/E8/harmonic fields are conditional on capacity/interference evidence and equal-bit controls, not guaranteed orthogonal syntax. Broader prose/conversation/executed Rust and quality-matched cost/energy follow actual capability.

Read and WRITE the authoritative absolute ledger at `/Users/casey.allard/uor-r4/.uor-models/native-joint-learning-2026-09-04/model-time.json`; a worktree-relative absent file or prose-only allowance is not the shared ledger. Principal review reconciled the prior 3200000-ms debit and 4000000-ms increment; refresh current check charges. Necessary local extensions are already owner-authorized: record complete projection, reason, increment and updated live cumulative limit BEFORE using it. Include preparation/build/extraction/fit/controls/retries/checkpoints/report/delivery. Do not incur paid/external compute.

Refresh physical free space and the reserve 36766079385 bytes plus 128 MiB stop margin. Principal cleanup removed only inactive old-worktree static-library/metadata intermediates, recovering about 0.871 GB before checks. Project build/report growth in bytes and compare it with actual headroom; a paper extension does not create storage. Use `CARGO_INCREMENTAL=0`, valid shared builds, two compiler workers and a bounded model process. Preserve models, evidence, research, source, executables and others' work. Stop/checkpoint before limits, and measure time instead of silently leaving the live JSON stale.

Compile/exercise meaningful focused prefix, arithmetic, disabled parity, artifact and generated-behavior checks; run formatting, touched-package check and wording gate. No blanket suite or theorem package. Queue compatibility statuses execute no tests; physical energy remains UNAVAILABLE until measured.

Deliver through a protected PR and verify actual merge plus reviewed content. Update README/ROADMAP/current-state/project-track/CONTINUE/EVIDENCE/model-direction/PROJECT_MAP and active mechanism pointers; keep historical records with explicit corrections. Synchronize all six owning issues and existing project items, close none without complete acceptance. Import compact revision-pinned knowledge and verify retrieval. End with your choices/disagreements, learned versus initialized behavior, exact causal outputs, each failed criterion, retained artifacts, source/merge receipts, resource/free-space accounting and one evidence-supported next operation.
