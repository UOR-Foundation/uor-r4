# DeepSeek next task: learn the available geometric relation

You are a research contributor to the UOR-R4 Geometric Language Model. Read this prompt and the [principal PR #1323 review](contextual-utility-review-2026-09-21.md) completely, then the [mechanism synthesis](geometric-attention-mechanism-synthesis-2026-09-20.md). Recover whatever additional repository, knowledge-store, history or primary research context helps your implementation. You have discretion over the learner and bounded tests. State material changes and their mathematical reason before final held-out evaluation. Deliver one constructive learning result, including the repairs needed to interpret it; do not spend the run only re-auditing the parent.

## Authority and recovery

Refresh origin/main and verify the review's delivery, then read AGENTS.md, README, canonical plan/current state, D0-b/D1/D2/D3 and execution policy. Inspect `native_geometric/learner/relational.rs`, `bin/competitive-reader.rs`, the exact group tables, parent design/result, review evidence and relevant older routing/scheduling code. Refresh live #820/#973/#1139/#962/#963/#964 and attached project items, artifacts, worktrees, shared model-time JSON and free space. Work in an isolated full worktree; preserve the owner's checkout and every unique/negative artifact. Do not reset the ledger at an issue/session boundary.

Parent PR #1323 is merged as `29f680fb5c80d9d00af9f9f2fb1e4c3e6205303d`. `contextual-utility-2` is sealed with 23 files and no unlisted files; keep attempt 1 and older roots untouched, including their known historical unlisted helpers. Claim a new report root immediately after argument validation, before model loading, and seal/verify the complete file set. Write derived inputs in their own claimed root.

Offline Rust training may use floating point, gradients and matrix products. Target serving uses learned geometry and bounded <=4-bit additions/subtractions/shifts/table reads under D0-b, no floating/transcendental numerical serving or multiplier instruction in the declared kernel. No dense Transformer backbone or runtime teacher/provider. Report any unfinished surrounding numerical path honestly; selector compliance is not whole-model compliance. Use Rust for model/data/fit work.

## What the parent established

Contextual utility improves over its frozen global-strength H4 parent by **−0.257644 bits/candidate position [−0.327088,−0.182968]**. Preserve this narrow positive. On 119 present final queries, correct emitted tokens improve **8→35**, correct payload reads **11→49**. On 21 absent queries reads worsen **5→10**. The categorical/global-utility arm emits **47/119** correctly; it has the best aggregate point estimate but its difference from contextual H4 is not statistically resolved by the post-hoc paired interval. A categorical+contextual comparator has not been run.

The 12.8× ranking diagnostic measured the global arm and conflated NoRead with ranking; first-match occurrence counting undercounted correct payloads. Do not build a capacity diagnosis on those numbers. The new reader fitted the same eight text documents it evaluated, and still harms those 488 positions by +0.218875 bits/token. Generation is degenerate. Useful strength control is real; ranking, absence, language integration and unique geometric advantage remain open.

## One constructive hypothesis and a cheap witness

The existing signed 120-element relation has enough capacity for this fixture, but the training schedule fits scores, changes codes and never refits those scores. Improve **learning useful source relations in the present representation**, with the contextual controller retained and fairly fitted. Keep frozen local E plus S-query-only, the same causal admission pool, 128-token ring and <=24 candidates initially. Do not reopen frozen S attribution or widen state as a default.

First add a tiny test-only expressivity witness: fourteen symmetric partner pairs can occupy distinct antipodal classes in 2I, `Q(a_i)=g_i`, `Q(b_i)=−g_i`, excluding the identity class; unrelated tokens map to identity. Both partner directions have relative element −1. The review gives a signed-four-bit rank/key/NoRead configuration that separates present/absent final cases. Verify group identity, sign, exact-key and strict NoRead tie behavior against the actual tables/fixture. This is a source-selection/NoRead instrument and capacity test only; tied strength biases choose the weakest boost and do not guarantee correct emitted answers. **Never initialize the experimental learner from gold partner assignments or expose family/partner/target labels to serving.** No parameter of the final learned artifact may come from this oracle witness. If it fails, diagnose the exact assumption before expanding capacity.

Choose a small effective learning change. A recommended starting point is bounded alternating scalar-scorer fit → discrete descriptor refinement → scalar refit, with selection based on the actual exported decision quality on development data. The previous schedule lacks the last refit. A source-sensitive, duplicate-aware listwise auxiliary may be useful alongside observed-token action risk; use it only with a stated objective/weight and held-out selection. Available correct payload occurrences form a set, not one distinguished index. If exact source identity is supervised, report that separately. Prefer a few justified passes to a broad hyperparameter search. Keep the incumbent, strict improvement/tolerance, deduplicated affected observations and reproducible ordering.

Inspect whether final relational queries are diluted by intermediate repetition. Declare the fit mixture/stratum weighting using training data, including absent cases and a meaningful natural-text contribution. Fit labels may define training losses/weights; final-query flags, partner labels, gold coverage/correctness and source IDs must never become inference features. A static token map can learn this bounded fixture, but do not call it learned contextual syntax. If a causal descriptor extension is necessary, demonstrate the collision it resolves and make one costed change with the same observations supplied to the comparator.

You may choose a better bounded optimizer or use one directed-pair diagnostic if justified by evidence. A 120×120 table has 120× the rank entries and different inductive bias; it is not a mandatory successor or inherently matched cost. State parameter access, capacity and learning opportunity. Arbitrary unseen pairings cannot be inferred without training or prefix evidence; do not manufacture an impossible generalization gate.

## Fair learning and honest populations

Compare the retained parent with newly fitted **H4+contextual utility** and **categorical+contextual utility**, using the same observations, candidate support, action set, data, fit/tune decisions, code-search/refit opportunities and bounded budget. Keep a competent exact reader and local NoRead reference. Reuse common positions/local scores and avoid a Cartesian product of arms. A geometry-channel lesion is a reliance check, not query blindness. Report map movement, objective before/after each phase, evaluations and exported decisions. Give each arm the same contextual fitting procedure; freezing `ctx` bytes while changing its scorer-dependent bucket does not isolate source selection.

Use real document/source-hash separation for reader fit, tuning and final text evaluation. Old eight documents and all already inspected construction outcomes are development/regression only. Disclose the frozen prior's older corpus exposure separately. Verify disjoint documents and contents before fitting, bind exact window selection/tokenizer, and declare fit-token and candidate-position weights. Use existing local text; avoid large downloads. If truly fresh external data is unavailable, run a clearly scoped reader-held-out local evaluation, not an invented externally unseen claim.

Construction evaluation must expose the relation: same key with multiple different-role competitors, balanced recency, true absent partners, changed payloads, unchanged recent tail/different older binding, and fixed memory/different query. Freeze final seeds/splits after development selection, before scoring. Report final present/absent queries, intermediate role/key/value, candidate count and coverage separately. Fresh keys/payloads/combinations within learned role pairs qualify only that transfer; stronger role/scope claims need observable relation evidence and an appropriate holdout.

## Repair attribution as part of the run

For each actual exported arm, save the top source **before** NoRead/strength, exact reference, payload, bucket, selected action and emitted token. Count any correct payload as correct prediction; distinguish intended-source provenance and duplicates. Separate admission coverage (including a full causal-memory oracle only offline), source ordering, abstention and dose. All target-using quantities stay outside inference.

A useful exact decomposition uses losses relative to local: let `c*` be the served ungated top source, `L+(c)` the best positive finite-action loss, `L*(c)=min(0,L+(c))`, and `Lpool=min_c L*(c)`. Ranking regret is `L*(c*)−Lpool`. Gate regret is `(L+(c*) if read else 0)−L*(c*)`. Dose regret is `actual_loss−L+(c*)` for a read, zero otherwise. These nonnegative terms sum to `actual_loss−Lpool` for this factorized source ordering; verify edge cases, ties and no-candidate observations. If you change that ordering/action architecture, derive the corresponding decomposition instead. `Lpool` cannot diagnose a source excluded from admission. Use correct nats/bits units and no first-matching-index proxy.

Keep the true one-logit action loss `log(1+p*(exp(a)−1))−a*I[payload=y]`; optimize/measure the actual clipped or quantized kernel if it differs. The soft expectation surrogate is not the deterministic exported objective. Record floating versus exported decisions/losses so optimization, quantization and integration failures can be distinguished. A two-pass coordinate result is not a proven global optimum.

## Actual causal boundary, export and cost

Evaluation, generation, interventions and timing must call the same target-free observed-prefix path, with ring state strictly before the current input and real occurrence/payload references. Exercise the **independently reloaded new contextual arms**, not just the old global arm. A loaded struct comparison is narrower than predictions from the loaded artifact.

Use focused checks for zero-controller parity, future-prefix invariance including the predecessor of a changed token, reset/stale references, learned-code export and actual-used fitter continuation. Bind full fit/tune/eval source/window identities, candidate construction, configuration, seeds, parameters, geometry, parent and format versions in the artifact or cryptographically bound manifest. Include contextual phases and ordering/cursor in genuine continuation, or checkpoint at completed phase boundaries and state that exact scope; do not repair unused historical CPQK.

For a selected-source intervention, choose a source payload outside the fixed recent query context, change only that payload, preserve the query and admission conditions, and rerun the same policy. Report all pairs including lost-support/NoRead cases, an unrelated-source control and read-disabled behavior. Do not silently replace the selected occurrence with the first target-bearing occurrence. Capture finite generated answers and natural-text continuations, including loops and failures.

Measure optimized active-candidate latency with representative candidate counts and observed read/NoRead actions, plus a genuine no-reader-work local baseline. Include admission, rank/bucket work, table traffic and local emission in the relevant complete-path measurement. The old zero-candidate probe is not this cost. Report resident entries/metadata/capacity separately from serialized bytes (current table 256 i32-entry bytes versus 64 serialized bytes); energy stays UNAVAILABLE without physical measurement.

## Prospective decision and discretion

Before the final draw, write practical margins/endpoints using development evidence: present-query actual hard loss and correct emitted outputs, absent-query harm, document-held-out text loss, and matched categorical behavior. Use sequence/document-cluster paired intervals with units and denominators. Keep a component-learning result distinct from integrated usefulness. A learned source improvement with unresolved prose should be retained, not promoted as chat; an all-NoRead collapse cannot count as attention progress. Unique geometric benefit needs the matched categorical comparison, not only a lesion. A useful near miss may justify a subsequent prospective criterion adjustment with evidence, but never overwrite the frozen rule after results.

If a cheap diagnosis reveals a fixable implementation or optimization problem, correct it within the projected run and preserve the attempt. Choose one additional bounded correction only when its expected information or capability gain justifies the cost. Do not require arbitrary passes through every historic control, endless precision refinements, or a full new architecture to finish this step. If the compact learner still cannot learn the witness, deliver the exact optimization/quantization/observation failure and one justified next action; do not automatically escalate to S7 or a giant pair table.

## Resources, delivery and the larger goal

At review the shared ledger is **186736749/194900000 ms**, about **136.05 min** remaining; refresh it. Project complete preparation/build/fit/control/evaluation/retry/checkpoint time, threads, RAM and peak/new/retained storage before execution, then charge actual work once with measured/estimated components. Necessary local extensions are already owner-authorized when reason, increment and new limit are recorded before use. The last extension's stated 90-min-versus-106-min rationale was arithmetically wrong; do not repeat it or erase charges. No paid/external compute. Preserve the 128 MiB stop margin and machine reserve; reviewed free space was 43.06 GB, reserve 36.77 GB. Reuse valid binaries and avoid unnecessary cold full builds. Delete no unique research.

Run appropriate focused Rust checks and actual changed-path generation; local executed checks carry validation, queue acknowledgements do not. Stage named files, protected PR, no admin/main bypass; verify merge/tree/content. Update current-state, canonical roadmap, README/front doors where their claims change, result/receipt/resource records and live owning issues/project status. Broad issues remain open; use References. Make a compact revision-pinned knowledge import and verify retrieval; do not duplicate the complete historic corpus every run. Finish with implementation choice, evidence, limitations, retained paths, resource totals and one successor.

Our five stages remain useful access/utility → role/scope persistence → dependent reads/shared composition → broader language/conversation/executed Rust → qualified scale and energy. The owner's new mathematics is active: signed spin/H4 relations in this step; scalar action utility already productive; retained Hopf fiber, explicit structural banks and conditional S7/E8/harmonic state for demonstrated aliasing/retention needs. Exact identity is separate from learned semantics; orthogonality is not automatic crosstalk immunity. Reuse helpful current or retired mechanisms at the boundary they solve. The goal is a learned transformerless geometric language model, not a succession of authored retrieval fixtures.
