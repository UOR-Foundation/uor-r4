# DeepSeek next task — learn when a geometric read helps language

You are a research contributor to the UOR-R4 Geometric Language Model. Read this prompt, the [principal PR #1321 review](competitive-reader-review-2026-09-20.md) and [mechanism synthesis](geometric-attention-mechanism-synthesis-2026-09-20.md) completely. Recover any further context you need from the repository, retained evidence and knowledge store. Make justified implementation choices; explain material deviations before observing final held-out results. Your task is to build and fit useful contextual read integration, not to reproduce a prescribed positive label or run another standalone audit.

## Recover current state and authority

Refresh origin/main and verify that the principal review/source repairs are merged. Read AGENTS.md, README, canonical plan/current state, D0-b/D1/D2/D3, execution policy, the changed `learner/relational.rs` and `bin/competitive-reader.rs`, original result and correcting evidence. Refresh live #820/#973/#1139/#962/#963/#964, active project items, artifact identities, worktrees, shared model-time JSON and storage guard. Work in an isolated full worktree and preserve the owner checkout, all unique research, negative artifacts and original sealed roots. A historical next-action paragraph is not current authority.

PR #1321 merged as `a1fadd1f`. Its constructed aggregate gain survives: H4 minus exact **-0.179540 bits/candidate position, cluster interval [-0.270206,-0.093226]** on 1,946 positions. The old -2.4956 is bits/sequence. Natural text worsens **+1.2908 bits/token** on 488 positions; `positive=false` remains. The categorical map did not learn; the purported query-blind control was a relation-channel lesion; generation/evaluation used different ring conventions; the intended partner/absence construction was defective; most covered observations were intermediate keys. Do not repeat a unique-H4, ranking-solved, or completed-runtime claim from the old report.

The principal repair supplies mode-aware descriptor optimization, RLR2 domain/vocabulary validation, RLRK v2 mode/code continuation, corrected prefix conventions, truthful fixture metadata and reporting fixes. Inspect its executed validation receipt. These fixes are not a new trained model and do not retroactively validate old quality/control claims. The preserved `competitive-reader-1` and `relational-reader-1` roots include unlisted helper scripts; do not edit, clean or reseal them. New work claims its own root immediately after argument validation and seals/verifies its complete file set.

Offline Rust fitting may use floats, gradients and matrix multiplication. Serving targets bounded learned geometry plus integer/add/subtract/shift/table operations under D0-b. No transformer backbone, runtime teacher or floating/transcendental served policy. Any remaining floating reporting/reference operations must be outside the declared numerical kernel and named. Do not claim whole-path compliance from a low-bit selector alone.

## One constructive hypothesis

The current selector factorizes `score(c,a) = candidate_score(c) + sb[a]`; the same positive strength therefore wins everywhere. All arms selected eight nats, which is harmful when copying is unreliable. Learn a **small action-dependent causal interaction including NoRead**, and fit it on a declared source-separated natural-text/construction mixture.

Retain frozen E plus S-query-only local emission, exact occurrence/version memory, the 128-token ring and <=24 shared admitted candidates. Start by freezing the learned source ranker and pool, so changed strength has an attributable effect. Compare its global fitted gain with its contextual gain under the same loss/actions/data. Do not widen memory, sweep widths, reopen S attribution or build a scheduler in this run.

You may choose a sparse finite action-cost table, an action-specific low-bit term, or another small justified interaction. Useful causal observations include source rank/margin, selected payload's local score gap, exact-context flags, candidate agreement and the learned relative element. Choose a compact sufficient set, not every available feature. Fit/tune selects bucket boundaries, shrinkage and sparse/unseen fallbacks. Coverage, target correctness, final-query labels, synthetic family IDs and future tokens are forbidden serving features. Identical causal observations with conflicting desired behavior are a representation diagnostic, not permission to insert an authored role parser.

The true one-step cost of boosting selected payload c by a nats is `log(1+p_c*(exp(a)-1)) - a*I[c=y]`. Optimize actual per-example costs or a justified surrogate, report hard exported loss separately, and include zero. Do not replace nonlinear per-example costs with a formula at average p. For fixed causal context, small positive gain needs correctness probability r > local p; a finite eight-nat action needs a stronger, explicit utility condition. For continuous `a` in `[0,A]`, the ideal boost is `clip(logit(r)-logit(p),0,A)`. For finite actions, evaluate their expected costs and choose the minimum, including zero; nearest numerical rounding need not minimize loss. The exact reference assumes only the selected logit changes, without saturation/overflow. If the exported kernel clips or rounds, evaluate its actual action costs. A score margin is not automatically p or r. Direct empirical action costs avoid needing to claim probability calibration.

A cheap offline opportunity diagnostic may evaluate the best finite action for the already selected payload and for all admitted payloads. It separates strength loss, ranking loss and unavailable support. Keep these target-using bounds out of inference and out of model-quality claims. If they show ranker/admission/observation error dominates, use your judgment for one minimal justified correction or bounded joint fit and report its contribution separately. The review does not declare ranking solved. Do not force an all-NoRead policy to count as attention progress.

## Fit once on an honest population, with competent comparators

Use locally available pinned text, Rust preparation and document/source hash separation. Declare overlap with the old local predictor's training corpus. “Source-separated” for this new fit does not erase historical exposure. Keep fit/tune separate from final evaluation; all inspected PR #1321 panels are development/regression evidence. If there is no genuinely fresh source population, say so and run an honest bounded development experiment rather than invent independence or downloading a large corpus.

Use the repaired construction with real competing partner sources, absence, query switches, payload changes and balanced recency. Validate its truth without learned codes before fitting. Report final-query, intermediate key/role/value, present/absent and multi-candidate strata alongside the all-position total. Include equal recent-tail/different older-binding pairs and fixed memory/different query pairs. Absence means the required source is actually absent; changing the target while leaving the source is not absence. Preserve exact selected occurrence and payload reference, including session identity.

Compare local/NoRead, a competent exact-context reader, H4 and one learned categorical alternative with the same candidate support and action choices. Apply the repaired discrete code-learning dose fairly when fitting map-based arms; verify meaningful optimization on a tiny witness and report map movement/evaluations/objective change. A map may legitimately stay unchanged at an optimum, but an empty acceptance branch is not learning. Equal parameters alone do not equalize optimization. Retaining useful H4 behavior does not require unique-H4 superiority; if the generic alternative matches it, report that and keep the useful component.

Use a fitted global-strength baseline and contextual controller for the central integration comparison. Avoid a comparator explosion: reuse shared positions/local scores for offline fitting, and exercise the exported causal path for final decisions. A post-fit channel lesion may diagnose reliance; name it correctly. At least one actual query/source counterfactual must demonstrate useful selection rather than merely degrading an input channel.

## Exercise the actual inference boundary

Teacher-forced evaluation, autoregressive generation, interventions and timing must call the same target-free inference path with the same state convention. The ring contains observations strictly before the current input; the current input and previous token are supplied explicitly. Verify the next-write order and exact occurrence/payload references. No-candidate positions emit local predictions. Never infer parity from calling the same function twice on one precomputed observation.

Before the bounded fit, run the supplied focused tests plus any tests needed by the new controller. After export independently reload every scored arm, compare actual predictions/actions/references and check the actual-used trainer's disk continuation including learned code map, controller state, ordering/cursor and optimizer state. Do not repair unused CPQK just because it exists.

Future-input intervention requires invariance at every prediction whose current observed prefix is unchanged, including the prediction immediately before the changed token. It must not require invariance once that token becomes current. Altered-source tests should pair the same query/policy on the original and changed selected source, track lost support/NoRead rather than drop them, and report selected payload plus emitted output. Preserve unrelated-source/no-read controls where they explain the claimed causal effect.

Capture actual finite generations and constructed final-query outputs from the reloaded artifact. Report errors and repetitive loops. A teacher-forced CE gain is not general prose/chat/reasoning; it can still justify a useful incremental mechanism. Benchmark a local path that truly bypasses all ring/reader work and an active-candidate path, count actual predictions/scanned records, and report serialized and resident bytes separately. Use optimized serving timing if making a serving comparison; keep physical energy UNAVAILABLE without measurement.

## Decide prospectively, without an arbitrary near-miss treadmill

Before final evaluation, specify a practical loss/useful-output/cost tradeoff from fit/tune requirements. Freeze meaningful margins and dataset identities; keep the old failed rule/history unchanged. Use document/sequence cluster intervals with the ratio of resampled loss sums to resampled position counts. Report total positions, candidate positions, admitted support, reads, correct reads, covered decision success, conditional precision and emitted correctness explicitly. No filtering away harmful NoRead/uncovered positions from natural-text totals.

Success is useful contextual attention with a tolerable language tradeoff, not merely suppression of every read. If text harm falls but constructed source-sensitive outputs disappear, record that tradeoff. If the contextual policy improves useful behavior with a small uncertainty-bound near miss, preserve it and recommend a prospective decision adjustment with its cost/risk; do not silently relabel a failed preregistered result. If a representation collision blocks utility learning, retain the working controller/interface and state the smallest missing observation. Stop repeated threshold/width experiments when they cease to answer a new question.

## Larger roadmap and contributor freedom

We are in Stage 1: useful learned contextual access plus utility. Stage 2 adds structural role/scope persistence when aliasing/retention requires it; Stage 3 learns dependent query updates, Read/Emit/Stop and derived outputs; Stage 4 develops broader prose/chat and executed Rust; Stage 5 qualifies scale and full laptop cost, with profiling throughout. Reuse historical modules at their actual evidence scope.

Prime/UOR identities own exact references. Relative H4 transports supply directed geometry; learned scalar utility controls influence. Retained fiber/spin and paired-H4/icosian structure remain explicit optional state. S7/harmonic banks are candidates for measured representation or lifetime limits, not automatic substitutes for contextual utility. Harmonic orthogonality does not promise collision-free finite memory. You may propose a better bridge when it addresses a concrete observed bottleneck with a smaller or more informative test; explain the reason and retain controls rather than treating this prompt as a ban on invention. No current result justifies a full wave-memory or two-hop pivot.

## Resources and complete delivery

Refresh `.uor-models/native-joint-learning-2026-09-04/model-time.json` before projection; the principal review also charges its complete bounded repair/build/test work. A starting complete block is <=90 minutes, <=2 compiler workers/one model worker, <=8 GiB RSS, <=512 MiB new data/reports and <=2 GiB incremental reused build. Measure step time before committing the longer fit. Adjust the complete projection to live resources. Necessary local extensions are owner-authorized: record reason/increment/new cumulative limit before use, retain all prior charges, the 128 MiB storage stop margin and machine reserve. No paid/external compute or unique-data deletion is authorized.

Compile and exercise changed Rust paths; execute focused causal/arithmetic/serialization/interface tests and actual generated behavior. Charge preparation/build/fit/controls/evaluation/retries/checkpoints once with receipts. Use one protected PR, stage named files, verify actual merge and source/tree content. Update current state, README/front doors, canonical plan, evidence, continuation, owning issues and any actual project items; broad issues remain open until full acceptance. Update the local knowledge store with pinned public review/result/prompt sources and verify retrieval; keep reports small and do not recursively duplicate the entire historical corpus. Conclude with implementation rationale, actual outputs, evidence/control limits, artifact/source/data identities, free space, resource balance, delivery and one justified successor.
