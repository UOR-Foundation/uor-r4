# DeepSeek execution prompt — recover trustworthy prior learning

You are implementing the next bounded step for the **UOR-R4 Geometric Language Model** in `UOR-Foundation/uor-r4`. Read this entire prompt and `docs/integration/prior-learning-review-2026-09-19.md`. The audited source is `c0ca7482` (merged #1294); refresh origin/main and include this prompt's protected delivery. The previous cold-context prompt is historical.

**Objective:** repair the numerical/data experiment and obtain a saved, interpretable prior-only learning curve on the exact exported computation. Do not run the unchanged multi-epoch command. This task includes implementation and a conditional learning run, not just a new design document.

## Authority and constraints

Read AGENTS.md, DECISIONS.md, agent-execution-policy.json, project-track.md, newest current-state, the parent review and the #1294 receipt. The broader takeover and import audits are reusable context, not a task to repeat. Current owner D0-b/D1/D2 override old skill wording.

Use Rust for data preparation, model/training, export and inference. Offline floats, gradients and matrix products are allowed. Serving has no floating point/libm transcendentals and no multiplier instruction in its declared numerical kernel; <=4-bit/ternary coefficients with explicit bounded add/shift/lookup execution are permitted. Geometric addressing/state/transport, prime/zeta/R4/S3/H4/2I/icosian mechanisms remain the programme priority. No transformer backbone, runtime teacher/provider or Python model implementation. Frozen R4G1 retains its separate contract. Preserve the older exact-memory/dependent-language and TinyStories .rgm families; their tests do not qualify this new core.

## Corrections that govern this run

The last pilot's 6.4019/6.4522/6.7011/6.8941 losses are **clipped nats**, mislabeled bits. Converted rounded values are approximately 9.2360/9.3086/9.6676/9.9461 clipped bits. The warm loss increase 4.4072 nats is approximately 6.3582 clipped bits. These conversions are not new evaluations. Keep the negative point-estimate direction, but withdraw claims of correct units, proven healthy activation scale, proven undertraining cause and proven aliasing cause.

The two arms trained on the first 2,048 fit windows in document order; full-fit count references saw all 23,559. Each arm scored 126,976 targets, not 131,072: current target indices are 1..=62 for a 64-token window. Development sampled document openings, with 18 scored documents despite 19 split members. The two-token count query is one position behind the correct context. The bootstrap labeled 90% uses roughly 95% endpoints. The arbitrary <10% warm-coverage rule is not a precision calculation.

Training uses unquantized float bias; export rounds to i32. CPR1 is a serving snapshot, not an optimizer checkpoint. Only the joint artifact is retained at `/Users/casey.allard/uor-r4/.uor-models/cold-prior-2026-09-19/cold_prior_joint.cpr`, SHA256 `fb780ff6ff19eec58f861135004250ae8874107099e2a071f7bfd1b4318b5a39`. `/tmp/cp6` contains the same artifact. Do not claim faithful continuation or replay of the missing prior-only arm. Do not invent a missing manifest from a later corpus snapshot.

## Stage 1 — one correct numerical and data contract

1. **Recover and project before builds.** Fetch source/live #973/#820 and relevant #963/#964, confirm active PRs, hash artifacts/tokenizer, inspect shared ledger and storage. Reuse a suitable clean isolated full worktree on a new codex/ branch without losing historical branches/material. Preserve the owner's checkout. Record the complete tranche before any preparation/build/model execution. See the budget below.

2. **Make report preservation executable.** Validate CLI limits, exclusively claim the attempt directory using the existing report API before loading models or collecting data, and write durable manifest/config/raw-output paths as the run proceeds. No late `create_dir_all` with overwrite-permitted fixed filenames. Verify and seal completed attempts; checkpoint failed attempts with an honest disposition. Never append beneath a sealed report root. Keep this a small experiment runner, not a new framework.

3. **Use one target iterator and probability implementation.** Default new runs to target indices `1..n` (all 63 next-token predictions at length 64): prediction position `i` ranges over `0..n-1` exclusive, context ends at `tokens[i]`, and target is `tokens[i+1]`. Use the absent-prefix row on the first prediction. Share the target iterator across training, integer evaluation, coverage and references. Independently test first/last targets using distinguishable tokens. Preserve the old 62-target mask only in explicitly labeled historical replay. Correct the two-token reference to `(w[i-1],w[i]) -> w[i+1]` and permit one-token conditioning at i=0. Reuse the existing corrected backoff estimator rather than another add-one trigram. Fit/tune/report populations must be explicit.

4. **Compute stable uncapped NLL.** For integer scores Z and artifact-bound common exponent F, offline logits are `z=Z*2^-F`; use `logsumexp(z)-z[target]`, then divide by `ln(2)` for bits/target. Float training loss in nats is allowed if labeled; gate comparisons must be bits. Remove probability flooring as a metric substitute. Test uniform V4096 ->12 bits, common-offset invariance, extreme confident error beyond the old cap, and agreement of loss/gradient conventions. Count old floor activations when replaying legacy metrics.

5. **Unify training-forward and serving-forward.** Quantize all served parameters, including bias, before training-forward; use the same integer forward primitive or require exact per-position logits against it. Define a <=4-bit bias coefficient representation with bounded power-of-two scale metadata and explicit common fixed-point logit units. An unrestricted i32 learned bias is not compliant merely because it is integer. Choose the convention once from training-only calibration and freeze it before development scoring. Export/version all numerical metadata; apply `2^-F` once when converting the softmax error into the derivative with respect to integer score Z; downstream propagation already carries it. Greedy serving computes `argmax(Z)` directly, requiring no runtime scale multiplication. The fixture with channels off and master biases `(0.49,-0.49,0)` must catch the old mismatch. Keep legacy CPR1 readable for evidence under its old semantics; do not relabel it compliant.

6. **Qualify the new backward path, not just nonzero gradients.** Add an independent tiny analytic reference for both prior tables, output, value and any trainable bias, before Adam/clipping, with nonunit row scales, actually active normalization, saturated and unsaturated clamp/ReLU cases and repeated-write suffix credit. Freeze quantization/normalization choices for the declared effective-weight STE; do not finite-difference a discontinuous quantizer and call it the surrogate. Assert tested branches are active. Retain causal/reset/equal-residue/order/absent-marker controls. Fix only demonstrated defects.

7. **Bound and bind loading.** Validate dimensions, checked size arithmetic, element ranges, channel flags, shift/code ranges, numerical overflow envelope and EOF/trailing bytes before large allocations. Return source/tokenizer/config metadata for verification, and use one invalid-token policy across training/serving. Preserve valid CPR1 bytes. Add a few focused malformed-artifact cases and exact new-format roundtrip tests; no general security campaign.

## Stage 2 — recover evidence and establish learnability cheaply

Create a durable document/window/target manifest before editing collected data. Bind source snapshot, source tokenizer hash (expected `9ca9acddb6525a194ec8ac7a87f24fbba7232a9a15ffa1af0c1224fcd888e47c`), derived tokenizer/config identity, per-document paths/content hashes, exact-duplicate groups, token data, split membership, window offsets and scored targets. Use deterministic fit/tuning/development document separation. Sample a small fixed development set across each document's length, not only its beginning. These remain open internal development data, never final holdout or an instruction benchmark.

If the old corpus/target manifest can be recovered exactly, rescore the retained joint artifact under that declared historical mask and corrected unclipped bits. Otherwise, label the replay as a **new-population diagnostic**, preserve the old digest as unrecovered and do not silently compare deltas across snapshots. The missing prior-only artifact is UNAVAILABLE, not reconstructed evidence.

Use the retained joint artifact for full/no-memory/prior-disabled/bias-only diagnostics on common masks. On warm targets, separate exact-context recurrence from alias-only hits, prior-write count/successor conflicts, prior/memory norms, separate logit contributions, centered spreads, target margins, entropy, max probability, zero/clamp rates and loss tails. A small warm fraction does not by itself make a paired effect unresolved. Report contributing warm documents and specify how zero-warm bootstrap resamples are excluded/count them; never turn an undefined denominator into a zero effect. Do not retrain memory now. If a measured amplitude imbalance warrants it, one declared fixed dyadic attenuation diagnostic is allowed; it is calibration evidence, not a model promotion or a sweep.

Before real-text fitting, run a tiny **balanced contextual learning fixture** where a constant marginal cannot solve the task, both context positions matter and the exported prior must change its prediction with context. Freeze the expected mapping and success/control conditions first. Merely lowering loss on a repeated single target can be explained by bias. Require a nonzero learning path and exported training/serving parity. If the low-bit fixture fails, use one bounded offline Rust floating-forward comparator of the same small architecture to distinguish a quantization/optimization gap; it must not become a served path. Diagnose and make one supported repair within the projected budget before retrying. Do not spend a full-corpus dose while this cheap gate fails.

## Stage 3 — one prior-only learning curve

Keep V4096, dv128, length64, batch8, exact token IDs, two position tables and bounded nonlinearity. Disable memory for training. **Freeze the same fit-only quantized unigram bias in the contextual model and its constant control**, so improvements must pass through the learned residual; useful contextual association still requires the permutation control below. Fit this marginal only on the declared fit targets; disclose that the full-fit marginal is available from initialization. It is a baseline, not a geometric contribution. Construct bias from log probabilities: `B[r] ≈ 2^F * (ln p_fit[r] + c)`, with declared smoothing and common offset c, followed by the selected low-bit/dyadic quantization. Its constant distribution is `softmax(2^-F * B)`, generally different from the exact unigram. Report the fit-only quantization gap before fitting and both references on development.

Prefer zero exported contextual residual at step zero, making the candidate initially identical to that constant control. Keep nonzero context features and verify the output gradient can move latent output weights across quantization thresholds, followed by credit to the prior tables. Zero exported output weights initially give zero prior-table gradients by design; require the output masters to cross the actual quantizer thresholds (currently approximately ±0.5 times the row scale) and then deliver context-dependent feature credit. Check this under the selected F and optimizer. Do not create an all-zero, gradient-dead model. Record the initialization, calibration and numerical changes as a new experiment; do not call it a continuation of the lost arm A.

Use a seeded deterministic shuffled schedule over all fit windows, without replacement per pass; preserve its seed, cursor and consumed IDs. Consume the last partial batch once and normalize by its actual size; do not wrap to the next pass to fill the batch. Record document coverage and input/scored-target counts. Provide both consumed-exposure and full-fit count references, using matched target conventions and no development data in fitting. Report the distinct information regimes rather than saying all models had equal training exposure.

Measure complete new-path step timing first. Evaluate fixed checkpoints at steps 0, 256 and 512; extend to 1,024 and at most one full shuffled pass only if correctness holds, the balanced fixture passed, loss remains finite and the measured learning curve justifies it within budget. Freeze one optimizer configuration; no learning-rate/width/seed sweep. Checkpoints may reuse a small fixed fit slice and development slice. Record train/dev/context-intervention loss, gradient/quantized-code changes, bias identity and logit diagnostics. A flat or divergent curve needs a named diagnosis, not an automatic longer run. One full pass was approximately 2,945 steps/28 minutes at the old rate; that is a projection, not a new timing.

Save the **prior-only serving artifact and a real resumable training checkpoint** at declared optimizer-step boundaries, including masters, moments, step, configuration, schedule/RNG cursor and data identity. A tiny split-run versus uninterrupted-run test must establish same-host resume equality. Save raw metrics before producing prose. Keep selected final artifacts and negative checkpoints under unique names and hashes.

## Metrics, gates and successor

Use bits/scored target as the main metric. Record token-micro and document-macro summaries separately. For uncertainty in a token-micro delta, resample paired documents with their loss sums/counts and recompute the ratio; use declared 95% endpoints. Report contributing documents and targets. Do not infer training-seed uncertainty from document resampling. Intervals after repeated checkpoint inspection/conditional dose extension are nominal descriptive intervals, not a 5% familywise error guarantee. Report every declared checkpoint and the selection rule; later qualification requires a fresh fixed confirmation population.

Before real-text results, freeze gate 1: at least 0.10 **bits/target** gain over both the exact fit-only unigram and its quantized constant, with a separate paired 95% interval for each gain excluding zero. Also require a positive context-permutation penalty with its paired interval excluding zero: permute whole two-token context tuples independently of their targets under a fixed seed and within absent-prefix/non-prefix strata, preserving the context marginal, targets and masks. Score the same trained prior without refitting. This distinguishes useful context–target association from a residual marginal correction. Separately zero each position lookup contribution before the shared nonlinearity, keeping weights, bias, scales and target masks fixed. These knockouts measure sensitivity and may be out of distribution; they are not standalone proof of contextual usefulness. Report fit-unseen pairs whose individual tokens were seen, including counts and uncertainty; an empty/small stratum is unavailable/limited evidence, not success. Use the same masks for comparisons. Warm-memory gates are deferred in this prior-only run; do not change their definitions to manufacture a result.

Decode and save complete prompt/output bytes and token IDs for at least three fixed development prompts from different documents, with constant and context-disabled controls. Include repeated/failed continuations. Integer greedy generation is sufficient; do not add a floating sampler. Measure actual export/checkpoint I/O, generation timing boundary, peak RSS and storage. Inspect the changed numerical path for prohibited operations; whole-path/binary qualification remains unclaimed unless actually performed. Energy is UNAVAILABLE without physical measurement.

Choose exactly one supported successor:

- If the corrected exported prior learns context and clears the gate, retain it; use frozen memory diagnostics to propose one controlled memory/address/scale change or one concrete geometric interaction, not a broad expansion.
- If it improves but is unconverged, report the measured slope and train/dev gap and propose a costed continuation from the **real** checkpoint. More dose is then evidence-based.
- If the floating tiny comparator learns but low-bit does not, propose a bounded quantization-aware curriculum or scale repair. No floating serving substitution.
- If train fit improves while development does not, investigate distribution/generalization before more epochs. If neither learns, diagnose credit/representation using the cheap fixtures. No universal impossibility or geometry-retirement claim.

The parent review refreshes calibration, LSQ, BitNet, continual quantization-aware pretraining, Sparse-BitNet and CAT-Q primary sources. These motivate diagnostics, not imported benchmark claims. In particular, quantizing a pretrained model cheaply does not account for learning its original weights. Consult actual source before adopting any new component; do not import a transformer or a new external teacher in this task. Distinctive prime/zeta/H4/2I contribution remains a later controlled question, not a label earned by lookup alone.

## Resources and delivery

Refresh the owner's `.uor-models/native-joint-learning-2026-09-04/model-time.json`. Review snapshot: `149038565 / 154400000 ms`, leaving 5,361,435 ms (89.36 min). Proposed complete tranche **3,600,000 ms**: 900 s repairs/builds/manifest/cheap fixtures, 1,800 s maximum prior fitting, 500 s artifact diagnostics/evaluation/checkpoints/generation, 400 s reserve. These are projections, not charges. If the work cannot fit, revise the complete projection **before** exceeding it; necessary local allowance extensions are already owner-authorized when reason/increment/new limit are recorded. Do not ask again for that settled class. No paid/external compute or destructive deletion.

One model worker, at most 4 Cargo jobs, peak RSS <= 8 GiB, <= 1 GiB incremental build/data/checkpoint storage when reusing the full worktree/cache, and 128 MiB stop margin. Account for any new checkout separately before creation. Use existing storage inventory tooling without unlocking sealed material. Charge actual preparation/build/model/control/retry work, including failed runs, with a consistent elapsed/overlap convention; do not turn estimates into measured receipts.

Run focused unit/gradient/causal/artifact tests, affected-package offline check, fmt and claim-wording gate. No unrelated historical model campaign. Deliver through a protected PR with named staged paths, no direct main push/admin bypass. Verify actual merge/tree equality. Update #973/#820, current-state, EVIDENCE, ledger, README and continuation pointers; #963/#964 when their scope changes. Keep broad issues open and update only actual linked project items. Queue acknowledgements are not tests.

Final report: source and artifact/checkpoint hashes, complete manifest location, exact loss units and target mask, actual exposure/learning curve/context controls, generated bytes, executed checks, actual resource charges, honest limitations and one next action. State omissions as NOT_RUN or UNAVAILABLE. Correctness repairs alone are not learned language; a failed or inconclusive learning experiment is an acceptable honest outcome.
