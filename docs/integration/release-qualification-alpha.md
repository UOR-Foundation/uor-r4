# Alpha qualification withdrawn — September 8, 2026

**Status: UNQUALIFIED / PRE-ALPHA.** The `v0.1.0-alpha` publication at `032f255dec33de23a313d8cc653e6a9715d14086` does not establish alpha capability. Both capability groups have not met independent acceptance on the same delivered artifact.

The prior [dossier at the audited revision](https://github.com/UOR-Foundation/uor-r4/blob/f8e9fa32/docs/integration/release-qualification-alpha.md) is preserved as historical evidence of the withdrawn assertions. [The recovery audit](recovery-2026-09-08.md) identifies the checks that failed to support them.

The former twelve-axis scorecard passed prose for emitting one token, reasoning and coding for nonempty text, Studio for metadata flags, and security through literal `true` fields. Its timestamps, verified operation counts and manifest flags were supplied constants. These are not independent capability, security or runtime measurements. Current report helpers emit `NOT_RUN`, missing numerical values and an unqualified verdict until actual evidence is provided.

Power, energy, sustained thermals and dense-model speed/memory comparisons are **UNAVAILABLE**. The diagnostic profiler has no sensor input, matched comparator or completed-task quality criterion. It can report selected in-process timings with their exclusions; it cannot establish complete deployed-path efficiency. Zero matmul is a serving constraint and scoped source/runtime obligation, not evidence of lower energy.

The retained model remains `blake3:79710468e3b75905550c15fbea62079e4267df774dbf4bc90ea45114826e4bfd` at its [contextual writer scope](../native_geometric_writer_refinement_1139.md). No release helper promotes those bounded results to general prose, generalized reasoning, broad coding or alpha.

Recovery's actual API and Chrome/WASM checks establish bounded artifact loading, response parity, checkpoint and cancellation behavior. They do not qualify alpha. Experimental `b2da9f9a` passes its 183 construction / 12 open / 12 fresh panel and recorded preservation but is not promoted: a third independent repeated 13+4 query returns 35 after earlier results 17 and 18, whereas retained `79710468` abstains. Checkpoint parity reproduces the same wrong response; a parity PASS is not semantic acceptance. The separate retained-model generated-chain diagnostic fails at 3/6. See [the complete recovery evidence](recovery-2026-09-08.md#shared-role-experiment-and-retention-decision).

Future release acceptance remains [the canonical plan](project-track.md#alpha-acceptance): exact artifact/revision, independent generated behavior across both capability groups, actual API/browser behavior, installation/resume/rollback, scoped runtime contracts and complete-path laptop cost. Preserve the failed publication and prior candidates; correct claims without rewriting experimental history.
