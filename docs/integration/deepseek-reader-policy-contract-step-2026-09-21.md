# DeepSeek next task: complete the correctly indexed reader-policy experiment

You are a research contributor to the UOR-R4 Geometric Language Model. Read this document, the [PR #1328 principal review](reader-utility-review-2026-09-21.md), and the [current/retired mechanism synthesis](geometric-attention-mechanism-synthesis-2026-09-20.md) completely. Recover additional source, project history, knowledge-store records and primary mathematics/research as needed. Use your judgment to improve implementation and select a bounded scientific follow-up; explain material deviations before final evaluation. The immediate objective is to **execute the intended learned finite-influence experiment correctly**, retaining useful source learning and testing actual language/relational behavior. This is not an audit-only assignment.

## Recover live authority and the correct result

Refresh origin/main and verify the merged principal review and PR #1328 (`a57524767390f0491e7fa2af65fa335d715a9b30`). Read AGENTS.md, README, canonical plan/current state, execution policy and D0-b/D1/D2/D3. Inspect `native_geometric/learner/relational.rs`, `bin/competitive-reader.rs`, the parent design/result/receipt, previous direct-policy prompt, and the new saved-data review. Refresh #820/#973/#1139/#962/#963/#964 and any project items, worktrees, artifacts, model-time JSON and storage. Use an isolated full worktree; preserve the owner's checkout, unique research and every sealed or superseded run.

PR #1328's exported policy gives a small measured teacher-forced text gain: H4 −0.0195 bits/token, eight reader-disjoint final documents / 1,952 positions, nominal document interval below zero; categorical −0.0243. Preserve that behavior. H4 text emitted accuracy remains 406/1,952, equal to local, and generation remains degenerate. Full construction emitted correctness drops 362→29 while selected payload correctness rises 465→517. Those are full-stream counts, not final-query counts. No integrated-model promotion follows.

**Do not adopt the old representation diagnosis.** The merged source builds events from legacy parents with zero gap thresholds, then assigns negative tune-derived thresholds to the exported selectors. Saved fit support strongly corroborates this defect; the recorded runner hash differs from merged source, so exact executed-source correspondence remains unresolved. All fitting support is in buckets 0–7; serving addresses 0–31. The intended gap-conditioned policy was never fitted at its served addresses. Serialization parity does not validate the training interface. A different observation, more state or S7 cannot be justified by this failure until the implementation confound is removed.

Offline Rust may use float/matmul; serving remains bounded <=4-bit add/subtract/shift/table arithmetic with no numerical multiplier/float/transcendental in the declared kernel. Geometric routing and typed operators are preferred, no dense Transformer backbone or runtime teacher/provider. Report unfinished full-path operations without confusing a compliant selector with a compliant complete model.

## Implement one feature contract before fitting

Initially freeze the improved H4 and categorical source maps/rankers, E+S local predictor, ring 128, current candidate pool <=24, existing four actions and equal-total-weight construction/text mixture. Keep the existing support rule and features for the first corrected fit so the comparison isolates the repair. Do not refit geometry, widen admission, sweep bins or change thresholds to rescue final results.

Choose/freeze the gap cutoffs on development data as previously specified. Configure the selector or a small explicit immutable feature descriptor **before** creating either construction or text `PolicyEvent`s. Use that configured representation for event extraction, fitting, independent reload and serving. Bind threshold values, units, comparison/tie convention, context bits, margin convention and action encoding. The observations must remain causal and target-free; never use correctness, coverage, synthetic domain/family IDs, query-type labels or future input at serving.

Add the missing high-value contract test: on the SAME observation and local logits, the stored fit-event bucket and selected occurrence must equal the independently reloaded selector's bucket and occurrence. Include legacy-v3 input artifacts, several gap bands, threshold ties, one/zero candidates and bounded integer extremes. Widen/check subtraction instead of relying on unchecked i32 arithmetic. Verify complete fit-event occupancy against independently reconstructed served occupancy; do not require every theoretically possible bucket to be populated. Distinguish unobserved bins, below-minimum-support bins and supported bins. Record both raw examples and declared weighted support; weighted events are not independent samples.

The direct per-example action cost remains:

`Δ(a)=log(1+p_payload*(exp(a)-1))−a*I[payload==target]`, with NoRead cost zero.

Fit means of individual costs, not the nonlinear formula at mean p. Evaluate the rounded/clipped integer action actually served; name any numerical gap from the analytical reference. The policy is an empirical finite-policy optimizer with a fallback, not a global language optimum. If you improve the implementation with an explicit feature-config type or an action-decision record, keep it local to the real boundary rather than creating a new framework.

## Repair the metrics that will choose the next architecture

Make `eval_stream` consume the actual `read_step`/`choose_with` source/action. The old regret helper calls `choose`, which returns None for a populated policy. It therefore reports NoRead regret for an executed read. Pass the actual action into a policy-aware decomposition and compare its `actual` against the loss change from the actual predicted logits. Assert the ranking/gate/dose identity relative to `Lpool`; verify actual selected-reference equality rather than treating None as an observed match. Empty pools should give valid zero local/pool read contributions.

Add per-stratum `reads`, `NoRead`, action counts, correct selected payloads and **correct emitted tokens**. Store per-sequence construction loss/counts and document text loss/counts, plus bounded per-position decision traces sufficient to audit bucket, selected reference, action, output and controls without another model pass. The final schema must describe the actual result structure; validate that structure using a small preflight before the expensive final pass. Do not merely compare two hand-authored field lists.

Evaluate the declared categories directly:

- Relational preservation uses final-present **emitted correctness** and loss versus the retained parent, with the previously declared practical margins unless you prospectively justify a different future criterion.
- Absence improvement requires actual absent-query **read counts and loss** no worse than the parent under the parent declaration. Unknown absent answers are not expected to be guessed. A reduced wrong-read dose can improve loss while the number of inappropriate reads rises; report both instead of merging them into one positive.
- Harm containment, useful text probability transfer, improved generated behavior and general language competence are separate outcomes. Keep same-artifact source-dependent outputs and fresh data in the report. Preserve all historical positives/negatives at their original scope.

Compute both candidate-bearing and complete-stream admission diagnostics with explicit counts. For every position, including an empty admitted pool, `admission_regret=Lpool−Lring>=0`; an empty pool has `Lpool=0`. The parent wrongly omitted empty-pool terms and divided by all positions. The saved corrected complete-stream means are approximately 0.831657 construction and0.529129 text nats/position; candidate-only 0.200636/0.649680. The old0.25 trigger was candidate-only. These target-using oracle opportunities are diagnostics, never serving observations or promised achieved gains. They do not displace the current feature repair. Leave admission unchanged for the first corrected policy comparison.

Keep the repaired four-condition intervention: reader enabled/disabled crossed with original/changed source, fixed local query inputs. Verify disabled-before/after invariance and compare enabled/disabled on the same prefix. The changed payload should be absent elsewhere when possible. Count exact selected-reference following and payload response separately. Check whether the original occurrence remains eligible/admitted; existence of an `observe_full` row does not prove support. Count pool and neighboring-feature changes and include NoRead/support loss rather than discarding them. Retain relevant future-prefix/reset/stale-reference checks.

## Bind and resume the artifact actually used

The old report seal is valid, but the model has zero data digest and its self-hashed sidecar does not bind new artifact bytes or loaded inputs. Complete a simple **acyclic** binding: bind the feature/action/config/input manifest identity into the artifact or a verified loader contract, then bind artifact byte hashes in a surrounding delivery manifest. Verify actual parent, group, tokenizer, data/window, config and model identities **before prediction**. Do not make artifact and manifest hashes cyclic. Exercise rejection of a changed threshold, swapped policy and mismatched input identity through the real loader.

The recorded `competitive-reader.rs` SHA differs from the merged source; the library SHA matches. The review could not reconstruct the difference as only the added unit test. Reconcile this provenance if an exact source snapshot exists, otherwise preserve it as unresolved historical source binding and make the next source/executable identities exact. Never rewrite a sealed old receipt to match current code. Capture actual source bytes/configuration and the executable used, rather than relying only on an environment-supplied revision label.

For this direct fitter a saved completed-stage artifact can be the natural checkpoint. Use it for evaluation/control/report-only corrections when valid; do not rerun an identical complete fit because of a report field or seal filename. Create each report root exclusively immediately after argument validation, write derived results to a new root, seal and verify its actual complete contents. Preserve `reader-utility-1` unsealed and `reader-utility-2` sealed exactly as they are.

## Deliver the constructive comparison, with bounded discretion

First run the corrected H4 and categorical finite-policy fit, matched in observations/capacity/fitting opportunity. Compare local, frozen strong parent, retained misindexed policy, and a fixed one-nat comparator over the same ungated source. If you also compute the best globally fitted action from already available cost sums, name it as a separate diagnostic; it does not replace the fixed one-nat comparator. Reuse valid local/source evaluations where possible. This cheap constant comparator tells us whether a fitted table helps beyond a generic small boost; it must not be confused with a new model path. Do not manufacture many redundant arms.

Treat all already inspected construction/text panels as regression/development data. Reserve genuinely new reader-final documents and construction seed before any development-dependent modification, with path/content and tokenizer/window hashes. The local model's prior corpus exposure remains separate. Use available local data and a measured cost projection. If no fresh source-disjoint text remains, say so and report a regression result instead of relabelling a reused panel as fresh. Do not download a large corpus by default. Keep fixed full-stream denominators and document/sequence-cluster intervals; serialize the underlying sums/counts.

Measure both probability and actual outputs. For teacher-forced construction retain complete-stream and final-query counters separately. Generate bounded natural-text continuations and actual constructed answers through the independently reloaded shared causal path, reporting loops and wrong outputs. No repetition suppression, output override, hidden grammar or teacher answer should stand in for model improvement.

After the clean first corrected fit, use development evidence to choose **at most one consequential extension** if needed and affordable. You have discretion; do not blindly follow the previous representation story:

1. If the corrected policy preserves useful relational emission and text benefit, retain it and formulate the next structural-context or dependent-composition step.
2. If a weighted-CE choice trades away emission, inspect per-bucket/per-action costs and emitted-correct counts by fit/tune stratum. With fixed sources and actions these are additive. A bounded feasibility/Pareto check can distinguish an objective tradeoff from insufficient observations. Do not assert impossibility unless established; the absence of one successful scalarized fit is insufficient. A prospectively declared constrained utility objective on the same features can be the one extension if the evidence supports it.
3. If a causal distinction remains absent despite correct indexing/support, identify it concretely. The same gap can conceal different local normalization mass; the one-bit source margin discards relation/score information. One existing causal confidence/relation observation may suffice. If instead identical current observations require different behavior after an older scope change, a small learned H4/shared typed contextual transition is justified. Explain the information added, use matched ordinary-code capacity, avoid authored syntax rules and evaluate new contexts.
4. If target support is the actual development bottleneck, distinguish retention, admission and generation. A single bounded matched admission complement may be proposed as the one extension, justified by recoverable development utility and measured costs. Do not select it from final labels or conflate oracle headroom with learnable gain.

Do not launch a sequence of bucket, width, strength, corpus or threshold sweeps. If the intended experiment remains negative after this bounded work, report the real unresolved distinction and one evidence-supported next task. Useful subcomponents may be retained without claiming that all goals pass. Near misses can motivate prospective practical-margin changes with reasons; never retroactively erase a declared result or relax causality/provenance correctness.

## Keep the larger geometric invention coherent

The roadmap is useful contextual access/influence → structural role/scope persistence → dependent read/update/emit composition → broader language, conversation and executed Rust → qualified scale/energy. Profiling occurs throughout. Do not spend the next run building a full role parser or wave engine merely because the last prose summary recommended it.

Signed H4/Spin compatibility, exact occurrence/version memory and scalar utility are active ingredients. Existing ordered n-lets, prime identities and fixed zeta phases have addressing/transport roles, not automatic semantic distances. A future contextual-state experiment can reuse finite group actions and typed operators. Retain Hopf fiber when projection would erase a necessary distinction; compare S7/E8/harmonic representations only against equal-resource simpler controls for that need. A scalar gate cannot create an absent target, and a copy reader cannot by itself make the weak local predictor a general prose model. Reuse existing dependent-read and shared-scheduler lessons when extending to derived outputs absent from source payloads.

Primary reading, as needed: [proper scoring rules](https://sites.stat.washington.edu/people/raftery/Research/PDF/Gneiting2007jasa.pdf), [retrieval quality-dependent influence](https://aclanthology.org/2022.findings-emnlp.218/), [perplexity versus generation](https://aclanthology.org/2023.emnlp-main.929/), [PaTH](https://arxiv.org/abs/2505.16381), [GRAPE](https://arxiv.org/abs/2512.07805). These supply distinctions and hypotheses; they do not validate our artifact or authorize dense serving.

## Resources, verification and protected delivery

At principal review the shared ledger is **191016749/194900000 ms**, leaving **3883251 ms (~64.72 min)**. Inventory shows **40.57 GB free** against **36.77 GB reserve**; refresh both. Project complete preparation/build/fitting/evaluation/controls/retries/checkpoint work, CPU workers, RAM and new/temporary/retained storage before execution. Necessary local extensions are already owner-authorized if projected, justified and recorded before use; preserve all cumulative charges and the 128 MiB stop margin. No external spend or destructive cleanup follows.

Run focused Rust tests for the actual defects and compile/exercise the changed path; use existing shared builds and relevant checks, not a blanket suite. Record actual commands/results. The previous passing tests missed the train/serve orchestration and wrong metric dispatch. Measure reader cost with enough candidate-bearing positions, alternating/randomized local-reader order and retained paired samples. Include bytes/config/preparation costs; no cache campaign is required. Keep unresolved incremental timing and energy UNAVAILABLE if the instrument cannot resolve them. A no-multiplier selector is not whole-path qualification.

Stage named paths, push and deliver through a protected PR. Verify actual merge and content/tree identity. Update current-state, roadmap/README claims, result/receipt/ledger, owning issues and project items where present; keep broad issues open. Add a compact revision-pinned knowledge import and verify retrieval. Return chosen mechanism, repairs, measured behavior and limitations, resource charge, retained root and one evidence-supported successor. Finish the constructive run rather than stopping after recognizing the bug.
