# DeepSeek execution prompt — repair, prove parity, learn, then advance

Work on the **UOR-R4 Geometric Language Model**, repository `UOR-Foundation/uor-r4`. This prompt supersedes the immediate execution ordering of the previous prior-learning prompt. Read this entire document and `docs/integration/learning-contract-review-2026-09-19.md`. Audited base is `f4fc1c60` (PR #1296); refresh origin/main and include this prompt's protected delivery.

**Deliver an implementation and an informative bounded experiment.** Correct the concrete defects below, run a valid small learning gate, and continue into the selected real-text pilot when its prerequisites pass and resources permit. Do not stop merely because one compile/test stage passes. Do not run a floating comparator against the old contradictory fixture. Preserve all original negative evidence.

## Authority and scope

Read AGENTS.md, DECISIONS.md, agent-execution-policy.json, project-track.md, newest current-state and the parent review. Reuse the takeover/architecture/import maps for specific questions; no new broad census. D0-b/D1/D2 are owner decisions: Rust preparation/training/export/inference; offline float/gradients/matmul allowed; serving bounded <=4-bit/ternary add/subtract/shift/lookup maps, no floating/transcendental served arithmetic and no multiplier instruction in the declared numerical kernel. Geometric routing/state/transport remains the objective. No transformer backbone, runtime provider/teacher or Python model. Frozen R4G1 retains its own contract.

Use the existing `prior_learning.rs` and shared low-bit primitives; **do not create a fourth competing prior/training implementation**. Preserve legacy cold_prior/CPR1 and the older exact-memory and TinyStories families. Keep mathematical constructions, learned results and unrun hypotheses distinct.

## Recovery and budget before execution

Fetch origin/main, inspect active PRs and live #973/#820/#963/#964, original checkout, retained hashes and shared model/storage ledger. Use a suitable clean isolated full worktree on a new codex/ branch; preserve other branches and user material. Retained joint artifact SHA256 is `fb780ff6ff19eec58f861135004250ae8874107099e2a071f7bfd1b4318b5a39`; no new trained CPL2 artifact or real-text checkpoint was delivered by #1296.

Review snapshot: owner `.uor-models/native-joint-learning-2026-09-04/model-time.json` is `149838565 / 154400000 ms`, leaving 4,561,435 ms. Proposed **complete 3,600,000 ms tranche**: 1,200 s repairs/builds/independent fixtures/checkpoint-schedule checks; 300 s small learning experiments and conditional comparator; 1,500 s conditional real-text training/evaluation/generation; 600 s evidence/delivery/stop reserve. This is a proposal, not a charge. Measure and revise before overruns; necessary local extensions are already owner-authorized when reason/increment/new limit are recorded. No paid compute, external GPU or deletion.

One model worker, at most four Cargo jobs, peak RSS <=8 GiB, <=1 GiB incremental build/data/checkpoint storage using a reused checkout/cache, plus the protected 128 MiB stop margin. Account for any new full checkout separately. Charge actual preparation/build/model/controls/retries, preserve cumulative history and distinguish estimates from measured elapsed work.

## A. Repair the numerical contract before fitting

1. **One predictive forward, not parallel definitions.** Currently `accumulate_bits` uses `z_train=R+b*2^(s-F)` while served evaluation uses `z=(R+b*2^s)*2^-F`; F=10 makes the contextual term 1,024 times too large in training. The backward nonetheless applies `2^-F`. Make training obtain the exact predictive integer score and intermediate trace from the served primitive. Use stable offline log-sum-exp loss in bits. Apply `2^-F/ln(2)` once at score credit and divide by the actual number of scored targets once. Greedy serving uses integer argmax.

2. **Non-vacuous parity and gradient tests.** Compare exact integer scores, scaled probability logits and losses with nonzero contextual output, nonflat bias, F>0, nonunit row shifts, active normalization and active/inactive ReLU/clamp boundaries. A zero-residual or flat-bias argmax fixture alone is insufficient. Demonstrate that omitting the residual factor fails the test. Verify independent analytic effective-weight STE derivatives before Adam/clipping for both embedding tables and output, freezing discrete scale/round choices and declaring boundary derivatives. Do not finite-difference a discontinuous quantizer and call it the declared surrogate.

3. **Repair the optimizer's hard small-gradient cutoff.** `lowbit_core::adam_update` skips the update when `v_hat<=1e-12`. With g=1e-7, lr=.05, default betas, zero decay and zero moments, ordinary epsilon-regularized Adam gives about −.04545, whereas current code gives zero. Use a stable independently checked Adam/declared-decay update, including small/zero gradients and carried moments. Run focused checks for affected shared callers; preserve old receipts without claiming their outcomes unchanged. Record the removed threshold and actual update/gradient magnitudes.

4. **Finish the exercised artifact bounds.** Validate dimensions and checked sizes before allocations, ternary code/shift ranges and metadata identities. Derive a safe bound for embedding sums, readout accumulation/shift and bias addition; a shift <=30 alone is insufficient for i32. Either reject unsupported envelopes or use checked wider accumulators under an explicit format/contract. Replace the `is_err() || true` test: it always passes and mutates byte 60 of the digest, not a shift. Locate real serialized field offsets and assert rejection. Choose one invalid-token/empty-prompt policy with Results at recoverable boundaries. Keep work focused on this active format/path.

Fix stale comments: the implemented bias uses `round(ln p+7)` with bias shift F by default, not the obsolete divide-by-two/F+1 formula. The previous 0.10-bit gap is an authored sequence result, not a universal calibration guarantee. Also correct the initialization comments: zero output masters with nonzero features can receive class-dependent gradients `(p_r - 1[r=t])*h`; randomized subthreshold masters are a choice, not mathematically required for output credit. Keep the chosen learning initialization fixed rather than adding an initialization sweep. Return seed/tokenizer/config metadata for verification; writing then discarding identities does not bind a loaded model to its inputs.

## B. Validate the task and representability, then test learning

Use an exclusive claimed report root for the small experiment too, before loading models or collecting data. Persist source/config/seed, target list/mask, curves, raw predictions and gate status; seal and verify with the existing report API. Unit-test success and the research gate must be separate reported facts.

**Correct task:** all 16 triples `[x,y,(x+y)%4]`, x,y in 0..4, scored **only at target index 2**. Implement an explicit fixture target mask shared by training, loss and accuracy. Assert every scored context has exactly one target, all four targets are equally frequent, and either single input alone leaves a uniform target distribution. Real-text defaults stay n−1 targets; do not drop prefix targets globally. Addition is commutative, so keep any order-sensitive interface test separate. The old repeated-triple task remains historical; its training labels mix addition/subtraction and its 0.375 result is not a valid quantization diagnosis.

**Authored capacity witness, kept separate from learned weights:** 16 hidden coordinates indexed by (a,b). Set E_old(x)[a,b]=+1 if x=a, else −1; E_new(y)[a,b]=0 if y=b, else −1; PAD row −1. ReLU then produces one-hot(x,y). Set W[r,a,b]=+1 if r=(a+b)%4 else 0, embedding shifts 0, output shift 12, F=10, norm_bits=6, flat bias; unused dv=32 coordinates are zero. Export/reload should give all 16 correct, margin 4, p_correct=1/(1+3exp(−4)), CE=log2(1+3exp(−4))≈0.0772 bits. Verify numerically. This proves finite representability only; never initialize the learning arm from the answer-coded witness or promote it as a learned geometric result.

**One corrected learning arm:** V=4, dv=32, original seed 13, zero exported contextual residual with nonzero features, same declared bounded ReLU and frozen flat bias. Start from the prior recorded initialization/configuration except the proven repairs and consistent score mask. Record F/scales, code changes, layer gradient/update norms, activation occupancy, score margins, train/export loss parity and all 16 predictions at steps 0, 128, 512 and 2,000. The final 2,000-step checkpoint owns the gate; earlier checkpoints diagnose the curve and are not selected to manufacture a pass. Freeze these conditions before results: exported accuracy ≥0.9 (at least 15/16), finite mean CE at least 0.10 bit below the 2-bit constant, and a positive loss penalty under a fixed independently permuted context-target pairing, evaluated without refitting the same frozen artifact. Permute complete context tuples, preserving their marginal and the target list. Preserve the actual permutation and assert that it changes target associations; an accidentally label-preserving permutation is not a negative control. Show both position-knockout diagnostics. This is an all-pairs fitting/instrument gate, not a fresh generalization or grokking claim.

If it fails **after parity/gradient/optimizer/task checks pass**, execute the already authorized one tiny offline floating-forward comparator in this same run, within the projection. Use the same two-table/ReLU/readout architecture, targets, frozen bias, seed/initialization convention and fixed dose; change only the declared quantization/rounding forward treatment. Keep scale/normalization conventions explicit and verify its ordinary smooth gradients away from boundaries. Never serve the comparator. A float success supports a gap in the quantized learning path, not proof of a particular STE or scale cause. A float failure despite the capacity witness calls for objective/credit/conditioning diagnosis, not a capacity impossibility claim.

One evidence-supported small repair/retry is permitted after that diagnosis (for example an explicitly justified scale/initialization or quantization curriculum change), with its before/after condition preserved and reprojected if needed. No lr/width/seed sweep, imported teacher or broad model rewrite. If failure remains, seal it, give the exact unresolved seam and stop corpus fitting; do not make the learning test pass by weakening its bar.

## C. Make continuation real before admitting real text

Use an actual seeded Fisher–Yates permutation or proven bijection, not independent hash-modulo indices. Assert every window appears once per pass, return a partial last batch and never wrap to fill it. Handle empty input/batch bounds explicitly. Preserve consumed IDs, pass/cursor/RNG or reproducible permutation state.

Complete checkpoint identity: all masters, moments, step, beta1/beta2/lr/weight_decay/grad_clip and any epsilon/scale settings, numerical config, frozen bias, source/tokenizer/data identities, seed and schedule state. Verify expected identities on load; reject nonfinite/invalid fields and failed loads without partially mutating the live trainer. Test split versus uninterrupted **full training state and next batches/updates**, with nondefault optimizer settings, a pass boundary and a partial batch. Equality of exported ternary codes alone is insufficient. Serialize to disk/reload at least one small fixture checkpoint and serving artifact under its report root; no lost-arm continuation claim.

Keep the serving-format version stable only if semantics/encoding remain compatible; version incompatible checkpoint or numerical conventions. Retained CPR1 is evidence under its old convention. Its earlier training bias mismatch does not prevent a served-artifact diagnostic; missing original data prevents claiming exact population replay. Defer that diagnostic if it would displace the active learning correction.

## D. Conditional real-text continuation, not a new approval stop

When A–C pass and the corrected small learned gate passes, continue the selected prior-only real-text experiment rather than returning only “ready to train.” Build the thin runner around the repaired common primitive and existing tokenizer/backoff/report code. Do not reuse the uncorrected cold-prior-pilot metrics/references or create another learner fork.

V=4096, dv=128, length=64, batch=8; frozen fit-only quantized unigram bias; memory disabled. Bind the original tokenizer hash (previously `9ca9acddb6525a194ec8ac7a87f24fbba7232a9a15ffa1af0c1224fcd888e47c`) and derived tokenizer, document content hashes/exact duplicates, separate fit/tune/development documents, window offsets and target masks in a durable manifest. Sample development across document lengths. Use all 63 next-token targets per full window and the verified shuffled fit schedule. Fit bias only on fit data and record its exact/quantized marginal gap; select numerical scale from fit-only information once. No instruction-corpus claim for repository text.

Measure complete new-path timing; start at checkpoints 0/256/512 and extend to 1,024 or at most one shuffled pass only if the measured learning curve and complete remaining cost justify it. One optimizer configuration, no automatic multi-epoch successor. Preserve the prior handoff's exact/quantized unigram and corrected tuned-backoff references, consumed-exposure/full-fit labels, fixed context-permutation control, position knockouts and unseen-fit pairs. Keep real-text permutation evaluation-only on the same frozen artifact, permuting whole contexts within prefix/non-prefix strata while preserving targets/marginals; retain and check the actual association-disrupting permutation. Distinguish token-micro from document-macro. Use paired document loss sums/counts for nominal 95% intervals; report all checkpoints and adaptive selection limitations.

Freeze the development gate before fitting: ≥0.10 bits/target gain over both exact unigram and quantized constant, each paired interval excluding zero, plus positive permutation penalty with its paired interval excluding zero. Empty/small strata are unavailable/limited evidence. Save actual artifacts/checkpoints and complete IDs/decoded bytes for three fixed prompts from different documents with constant/context-disabled controls. Include collapsed outputs. Training success does not establish useful prose; energy remains UNAVAILABLE without physical measurement.

## Roadmap, verification and delivery

The roadmap is: correct shared learner -> representative local-context baseline -> older-prefix-sensitive geometric state/read plus exact memory -> integrated conversation/memory and executed coding/reasoning on one artifact -> complete M1 cost and API/WASM/Studio/alpha. A two-token prior cannot distinguish older histories with identical tails. Do not scale it indefinitely as the final architecture. A geometric operator must earn a specific predictive-sharing/selected-compute role with matched controls; no new operator is justified by the flawed #1296 fixture.

Run focused Rust numerical/gradient/optimizer/task/artifact/schedule/resume checks, affected-package offline check, fmt and claim wording. Inspect changed serving operations; scope multiplier/allocation claims to actual checks. No dormant broad historical campaign. For each completion claim give its executed assertion/command and retained result; fix or mark missing items instead of declaring all of Stage 1 complete from a test count.

Deliver named paths through a protected PR; never direct-push main/admin-bypass. Verify merged content/tree equality. Correct current-state, EVIDENCE, README, canonical plan/continuation pointers and the ledger; preserve raw #1296 receipt and failed candidates. Update #973/#820 and relevant #963/#964 findings; keep broad issues open and update only actual linked project items. Queue acknowledgements are not tests.

Final report: corrected source identities, which prerequisites passed, actual small learned/comparator outcomes, whether conditional real text executed and why, manifest/artifact/checkpoint hashes, generated bytes when run, exact resource charges and one evidence-supported next action. A valid negative result is useful; a passing test that does not assert its headline is not qualification.
