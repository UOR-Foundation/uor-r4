# Historical GitHub issue #973 before native recovery

Source: https://github.com/UOR-Foundation/uor-r4/issues/973

This preserves the complete issue body retrieved on 2026-09-04. Its old current/next instructions are historical. New work follows [the canonical plan](../project-track.md) and [current implementation](../current-state.md).

---

## Current authority — #1096 delivery (2026-09-03)

#1096 completed **ISOLATED_RUNTIME_READY** through protected [PR #1098](https://github.com/UOR-Foundation/uor-r4/pull/1098), merge `6f21fc5f4c40b9620c9fec5e95a39097f812ae73`. The delivered tree matches the independently reviewed head. The one actual readiness-only attempt completed in 2.058100333 seconds with all four harmless probes denied, verified fixed runtime/source/artifact identities, null states and zero model loads/row or batch forwards/optimizer updates. Conservative combined peak RSS was 704,806,912 bytes. [Complete record and exact receipts](https://github.com/UOR-Foundation/uor-r4/blob/6f21fc5f4c40b9620c9fec5e95a39097f812ae73/docs/r4_isolated_runtime_readiness_1096.md); [independent delivery review](https://github.com/UOR-Foundation/uor-r4/blob/6f21fc5f4c40b9620c9fec5e95a39097f812ae73/docs/r4_isolated_runtime_readiness_1096_delivery_review.md).

This qualifies one pinned runtime startup and the tested harmless-read restrictions. It does not identify the precise original denial cause, prove general isolation or qualify raw-text model behavior. #1094's original UNAVAILABLE_REFERENCE_REPLAY, exact authoring results (320/320 valid, 16/16 refusal), source/binding/profile and sealed mode-000 population remain preserved. No corpus preparation, withheld access, fit, model comparison or replay occurred under #1096. #1079 LANGUAGE_R4_PRESERVED_CONTROL_WEAK and #1082 descriptive limits remain unchanged; #973 stays open and #954 blocked.

The one next action is **#1094's separate frozen preparation contract**, using the accepted source/profile/runtime handoff, explicit accounting for its prior consumed budget and independent release review before withheld access or comparison. #1094 remains open, parked and unassigned until activated. No further task was executed in this delivery.

## Earlier authority — #1085 delivery (2026-09-03)

#1085 completed CLAUSE_SEGMENTATION_SPECIFIED through PR #1095 at `3e894820c520f3b7803a48c6a2eeeb5b7d7021c5`. Next: parked #1094 implements the sole raw-text adapter and independently binds its comparison before observation. Reader/core, known lexicon/query and four facts remain fixed. No parser/population/fit/evaluation ran. #1079 preservation/weak-control and #1082 descriptive results stand. #973 remains open; #954 blocked. [Delivered contract and DoD](https://github.com/UOR-Foundation/uor-r4/issues/1085#issuecomment-5520624391). [Scope/original opening](https://github.com/UOR-Foundation/uor-r4/issues/973#issuecomment-5520112973). Below is dated historical context.

## Exact-spin global operator prototype

Higher-scope operator influence consumes the candidate set accepted by #953; it never participates in admission. The existing coarse adjacent-spin rows remain retrieval fallback/diagnostic data, not operator coefficients.

For the first bounded-global subprobe:

- reuse the existing exact signed-S3/Hopf/fiber/torsion `shared_class_kappa`; Hopf direction alone is insufficient and `q` / `-q` remain distinct;
- bind one immutable class result to global root/epoch kappa, operator kappa, chart/table identities, and exact class, then share or precompute it once per admitted class without changing route, payload, or kappa bytes;
- keep the final candidate score query-specific;
- bind either a new exact `SpinTorsionState` relative relation or an explicit spin-to-H4 map when direction matters—the existing H4 relative witness is prime-derived route state;
- use a construction-independent multi-unit global snapshot with a witnessed non-identity transition;
- include at least two references in one exact class plus a different exact class occupying the same current `SpinSector`;
- build any later finite neighbor operator independently over exact classes and already-admitted candidates, never from the coarse retrieval rows or by all-to-all broadcast; and
- before a scalar readout, enumerate transformed candidate-state classes and stop if one retained class requires incompatible outcomes.

Compare matched real, identity-disabled, and deterministic class/operator-permuted arms. Candidate rows, admitted addresses, payloads, lower-scope state, ceilings, and work remain byte-identical. The real arm must change the predeclared candidate and exact decoded output; disabled and permuted arms must not reproduce that effect. Trace base local cost, exact class, operator input/result/contribution, transformed cost, reuse count, selection, abstention, and output separately. One fan-out witness shows one class evaluation reused by multiple references with byte-identical overlay state.

Call the prototype harmonic only when its operator identity also binds basis, mode order, coefficients, quantization, and transition law. H4 group action alone is not that evidence. A positive global result cannot close #973; paragraph and conversation still require their own matched load-bearing qualification or an explicit native scope revision.


## Sequenced corpus induction

#973 owns the multiscale receptive-mechanism gate, corpus-induction ladder, and
final requalification as separate ordered parts of one terminal.

1. First, the frozen paragraph, conversation, and global probes must each cause
   a candidate-relative decoded-output change under matched disabled and
   permuted controls, unless an explicit native revision changes the required
   scope.
2. Then freeze document/conversation/task partitions, operator family/schema,
   basis/mode order, objective, quantization, scope semantics, neighborhood
   contract, and induction rule. Construction data may supply causal
   prefix-to-observed-next-route examples; validation/test continuations and
   runtime future data may not tune compilation or inference. Only declared
   corpus-derived statistic/coefficient values may vary under a new
   artifact/operator kappa per rung.
3. At every rung, real and control arms preserve identical admitted support and
   work. Coverage/support changes between rungs are reported separately as
   capacity/recall; an operator-only scale question freezes admission across all
   rungs. Every structural or placement-epoch change reruns the bounded matched
   #973 gate before scale evidence is read.
4. Before an expensive run, post the repository run contract with reachability
   ceiling, cheap-instrument verdict, exit rule, divergent next actions, and
   cost.

The corpus never supplies exact full-history continuations, actual future
events, target outputs, evaluation answers, teacher outputs, or provider text to
inference. Increased hit rate, table density, or trace activity is not
attention. Promotion requires the real arm to change a predeclared held-out
anti-recall route and decoded output while the matched scope-disabled,
order-shuffled, or operator-permuted controls do not reproduce the effect.
Current procedural spin placement remains identity structure unless a versioned
induced placement epoch itself passes those controls.

## Native programme position

- Child of programme root #820.
- Future stage; intentionally unassigned.
- Natively blocked by accepted #969/A1Q-L and accepted #953/I1.
- Native blocker of #954/C1.
- #954 remains additionally blocked by #953.
- Downstream order remains #954 -> #955 -> #962 -> #963 -> #964 -> #965.

#969 is closed at `PROCEED_TO_I1_WITH_CAUSAL_R4_PATH_ATTENTION`. It qualified only an identity-derived local causal path selector, not semantics, sentence attention, grammar, or coherent language. #953 must now qualify the decoded grammar/sentence loop before this issue is eligible.

During #953, paragraph, conversation, and global states may remain serialized and incrementally updated, but they may not influence candidate admission, ranking, abstention, termination, or emitted output.

## Qualification contract

Reuse the exact #969-qualified local path plus #953-qualified decoded grammar/sentence loop. Do not treat an internal-state probe, distinct serialized state, trace activity, or retrieval as attention.

For each higher scope, freeze a disjoint independently identified fixture before outcomes are opened. Vary only the named scope while holding lower scopes, natural candidate support, payloads, source partition, candidate/row ceilings, and work fixed.

Require:

- incompatible predeclared choices under matched histories;
- strict intended candidate selection and exact payload decoding;
- distinct decoded autoregressive output;
- deterministic termination;
- no target, future-route, exact-continuation, source-weight, teacher, provider, or hidden-support leak;
- a scope-disabled control;
- a deterministic scope-permutation control;
- the frozen #969 full-path/last-only/state-disabled controls where still applicable; and
- complete support, work, ties, abstentions, exact hits, decode, update, and cycle reporting.

Global qualification uses a construction-independent bounded snapshot permutation rather than mutating evaluation conversation history. Candidate relabeling, prime-assignment permutation, and geometry permutation must remain equivariant.

## Terminal decision

- `PROMOTE_FULL_HIERARCHY_TO_C1`: only when paragraph, conversation, and bounded-global interventions each change matched candidate choice and exact decoded output through the accepted #953 loop; every required control is exercised and weaker; support/work are equal within each rung; bytes and report kappa reproduce; no leakage occurs; the final induced artifact re-passes the bounded #973 gate; and its candidate-relative effect transfers to held-out anti-recall histories rather than merely increasing lookup density.
- A partial, tied, inert, unavailable, or invalid result retains only directly established state, keeps #954 blocked, and requires a narrow public repair/requalification successor before closure.

## Nonclaims

A positive result establishes only that the full hierarchy is causally load-bearing in the bounded accepted decoded loop. It does not establish factual correctness, grounding, contradiction handling, reasoning, general coherence, performance advantage, chat, or release readiness. Those remain #954, #955, #962, #963, #964, and #965 responsibilities.

## Post-#983 local-qualification boundary (2026-08-28)

Closed-negative #983 is followed by fresh local qualifier #986, which now blocks #953. #973 remains blocked by #953; no native dependency change is required here.

#986 owns only bounded document/CID-disjoint local semantic-placement/value, self-plus-six harmonic link-state, and signed-transport qualification. It does not take paragraph, conversation, bounded-global, exact-spin global-operator, or later corpus-scale ownership from #973. Those scopes remain dormant until either the full #986 mechanism or a separately qualified table-value successor is applied unchanged and qualifies the real #953 decoded loop.

The later #973 corpus-scale ladder still freezes its own partitions, operator family, scope semantics, placement epoch, identities, and matched controls. A positive local #986 result is a prerequisite, not inherited higher-scope evidence.

## Dormant behind B0 and #953 (2026-08-28)

This issue remains on hold. #989 must first establish the working source-free table baseline, and #953 must then show that one geometric intervention adds held-out and decoded-output value beyond that frozen baseline. Until both are complete, paragraph, conversation, global influence, geometric corpus induction, and all higher-scope qualification remain NOT_RUN.

Corpus size, lookup density, route activity, or exact algebra cannot bypass the working-baseline and incremental-value sequence.


## B0 complete; higher scopes remain blocked (2026-08-28)

Protected PR #990 closed #989 at `ESTABLISH_TABLE_NATIVE_LEXICAL_BASELINE` and froze the working non-geometric table reference. This does not unblock #973. Exactly one matched #953 geometric intervention must first demonstrate causal incremental value against that unchanged corpus/support/decode/work reference. Paragraph, conversation, global influence, corpus-scale expansion, correctness, reasoning, chat, optimization, formal closure, and release remain dormant here.

## Authoritative current state and successor after PR #997 (2026-08-28)

This final section supersedes earlier present-tense blocker/status/next-action
prose in this body. Historical frozen contracts and results remain evidence.
#973 is the active, assigned child of #820; its native blockers #969 and #953
are closed; it remains the blocker of #954.

Retained bounded evidence is: prior-prefix PR #992 real 2/2 versus disabled 1/2
and permuted 0/2; paragraph PR #993 real 2/2 versus disabled 1/2 and permuted
0/2; conversation PR #994 real 2/2 versus disabled 1/2 and permuted 0/2;
bounded-global V1 PR #995 target-free negative; and bounded-global V2 PR #996
real 2/2 versus disabled 1/2 and class/operator-permuted 0/2. These are bounded
synthetic causal witnesses, not natural semantic transfer.

PR #997's first document placement passed a target-free gate with 36,533
operative anti-recall positions and one decoded all-control contrast. On 35,028
construction-fitted held-out known targets, real componentwise-Frechet
placement scored 2,931 = 8.367592%, unchanged #953 scored 4,281 = 12.221651%,
order-shuffled scored 2,934 = 8.376156%, and operator-permuted scored 2,966 =
8.467512%. Terminal:
`RETAIN_BOUNDED_GLOBAL_ONLY_REDESIGN_CORPUS_SPIN_PLACEMENT`. This rejects that
placement objective, not geometric state, transport, routing, or the programme
goal. More scale/table density is not authorized.

### Active mechanism: `GeometricGatedDeltaRetentionR4V1`

The pre-outcome scientific audit corrected one false dependency in the earlier
text. `PredictiveConnectionRetentionGate0V1` is only an optional diagnostic for
one immutable fixed-route feature channel. It is not a necessary-condition test
for the learned mechanism and cannot block it. Its ignored corpus run remains
`NOT_RUN`; focused checks do not count as delivery of the full cell.

The immediate #973 implementation is the smallest faithful construction-only
`GeometricGatedDeltaRetentionR4V1`:

- immutable route/prime/spin/payload identities remain unchanged;
- separately learned token-local key, value, and candidate-query placements;
- four bounded local/short/scope/long recurrent memory banks;
- declared exact-route-derived connection transport between successive frames;
- distinct retention gates plus key-specific delta overwrite after the observed
  route joins the causal prefix;
- candidate-relative scoring over exactly the unchanged #953 admitted support;
  and
- matched full-geometric, plain-delta, no-delta, transport-permuted,
  order-shuffled, and last-only controls.

Gate-0 outcome, if later run on a separately bound construction-only bundle,
selects only whether the fixed-route auxiliary channel is retained, dropped, or
left undecided. Positive, negative, and unavailable Gate-0 branches all proceed
to the learned full cell.

The full cell must first qualify on an independently frozen construction-only
fit/validation contract with strict causal-input, support, parameter/work,
associative-recall, next-route, replay, and geometry-destroying-control evidence.
Only that actual recurrent mechanism may authorize the protected D3 join. D3
targets, decoded generation, exact/table lowering, #954, and every downstream
claim remain blocked until then.

A full-cell negative falsifies only the frozen learned mechanism and its
representation/update law. It redirects #973 to that seam; it does not authorize
more corpus volume, route families, or lookup density.


## Authoritative current state after PR #999 (2026-08-29)

This final section supersedes earlier present-tense active-mechanism and next-action prose in this body. Historical contracts and frozen results remain evidence.

PR #999 delivers and freezes two bounded results. `GeometricGatedDeltaRetentionR4V1` is structurally exercised but shows no advantage on its sealed synthetic fixture: full geometric is `16/28` next-token and `55/112` association, while matched plain delta is `23/28` and `98/112`. Its verdict is `PASS_STRUCTURAL_SMOKE` plus `NO_ADVANTAGE_ON_THIS_FIXTURE`.

The direct dense causal Q/K/V/O attention reference then establishes a working learning/softmax/value path without Ollama or Gemma. Historical V2 is `NON_PROMOTABLE_BUDGET_MISMATCH`. Fresh equal-manifold-budget V3 returns full geometric `3/12`, plain fixed-tangent `12/12`, seed-disabled `7/12`, current-only `6/12`, alternative-connection diagnostic `10/12`, key-isometry `7/12`, order-shuffled `5/12`, and value-permuted `8/12`. Construction replay is full `13/16`, plain `16/16`, seed-disabled `16/16`, and current-only `8/16`.

The binding conclusion is narrow. Dense causal attention and training work. The tested mixed-gauge H4 projection/connection/optimizer combination does not. The exact H4 group action remains algebraically valid, and the `10/12` alternative is diagnostic only because it was swapped at inference time rather than trained independently.

The only active #973 rung is now `ConnectionGaugeCovarianceV4`. It must use explicit three-coefficient local Q/K/V/O coordinates, separately train H4, alternative-tangent, and fixed-tangent arms from identical coordinates, publish all 120 frames and a fresh 24-case disjoint freeze before labels, verify finite-difference gradients and gauge-covariant logits/weights/scores/update deltas, and include current-only, order-shuffled, value-permuted, and deliberate gauge-mismatch controls. Main arms must first reach `16/16` construction parity; validation requires at least `18/24`, current-only at most `12/24`, destructive controls at least six cases below, zero future reads, and byte-identical replay.

Paired-H4/E8 hierarchy input remains `NOT_IMPLEMENTED`; corpus qualification, resonance-sieve replacement, positive-kernel recurrent factorization, H4/Q29/integer lowering, autonomous generation, and #954 remain `NOT_RUN` or blocked. Their order is conditional: V4 -> paired-H4/E8 and fiber qualification -> corpus oracle -> positive multi-resonance replacement -> bounded recurrence -> exact lowering. A V4 negative redirects only the failing coordinate/connection/training seam; it does not authorize more route families, corpus volume, or lookup density.

Frozen V3 artifact: `blake3:136f0bac7361ca77a30946d8f120843b2d42eacccedbb1f64ab604af3cdf50f3`. Frozen V3 experiment: `blake3:8ff5c7f584f82c8fc7cf39ebbd28274140f4de3faf05eff370bc22e7aa429785`. #973 remains open and assigned; #954 remains blocked.

## Authoritative HELM-D-R4 parity result (2026-08-29)

This final section supersedes earlier present-tense next-action prose without rewriting any frozen V2, V3, or V4 evidence.

Pinned HELM-D source provenance is recorded at commit `7501deca8f413848bfef804be64ce874b72a3cd7`; no upstream checkpoint parity is claimed. The local SmolLM2 donor and a complete 30-layer, 9-head R4/Spin gauge lift ran on a deterministic held-out SimpleWiki document with every learned Q/K/V projection, RoPE, complete causal prefix, stable softmax, value aggregation, `W_o`, residual/FFN block, and LM head retained.

The frozen result is `PASS_HELM_D_R4_GAUGE_SOFTMAX_FULL_DECODER_PARITY_ADVANCE_TO_INTRINSIC_R4`, result CID `blake3:05eaad210198fbe39a0645c25b0c890c55d5f3d3dd8a1710472e976a637e2a07`:

- 196,608 full-vocabulary logits compared;
- maximum/mean absolute donor-R4 delta `1.049041748046875e-5` / `2.2742100540540378e-6`;
- 3/3 held-out top-1 decisions and all target losses within the frozen tolerance;
- donor and coherent-R4 replays exact;
- donor and R4 decoded `, and`;
- source-frame-permuted control decoded `[[` with maximum logit delta `23.08442449569702`;
- 2,700 key and 2,700 value transports, 1,080 query and 1,080 output transforms, and zero future reads; and
- 103,680 R4 blocks were encoded; the control executed 77,760 source-frame block permutations; implementation-owned policy/frame/audit evidence passed;\n- the raw 12,539,119-byte corpus matched its frozen BLAKE3 CID; and\n- the exact donor used eight workers through `available_parallelism`, not a single-core run.

This establishes bounded full-decoder ordinary causal attention in coherent UOR R4/Spin frames. It is not an R4 predictive advantage, intrinsic distance/centroid attention, softmax removal, transformerless serving, correctness, reasoning, or scale evidence.

**Now:** train one separately frozen intrinsic R4 distance plus geometric weighted-centroid attention arm while retaining this donor/reference behavior and comparing against equal-budget Euclidean/plain and geometry-destroying controls. **Still blocked:** multi-resonance replacement, recurrence, exact lowering, scale, E8 expansion, #954, and downstream product claims.

## Authoritative Attempt 02 result and successor (2026-08-29)

This final section supersedes earlier present-tense active-mechanism and next-action prose without rewriting any frozen result.

`HELM-D-R4` remains a bounded `PASS` for ordinary full-prefix causal softmax in coherent transported R4/Spin frames. That establishes preliminary ordinary attention at this scope, not curvature-specific advantage, transformerless serving, correctness, or reasoning.

`IntrinsicLorentzR4AttentionV1` Attempt 02 completed construction fitting and construction validation at `a348d7ec31f75524906a39cc04327bbcbdd47a56`, then terminated `UNAVAILABLE_INTRINSIC_LORENTZ_R4_STOP_BEFORE_HELD_OUT`. Result CID: `blake3:da2a63323d6211b8d581e5a4ed75d788eb919ff0f210d2e3beb8a749ee1bc64f`. Barycenter covariance was `9.121400701417315e-8` against the frozen `1e-8` ceiling. Diagnostic curved NLL was `3.9044978436394797`, versus donor `2.6513639557648623` and flat `3.6955705255518287`. Replay, work/shape, donor-trace identity, liveness, zero faults, and zero future reads passed.

D3 remained sealed: `heldout` is null and no reveal marker exists. There is no held-out intrinsic-attention verdict, no tolerance-only rerun, and no Attempt 03 under this freeze. The sole active successor is one fresh construction-only source-faithful HELM-D learned-manifold Q/K/V qualifier with the declared Lorentz score/scale/bias, a stable equivariant centroid, an equal-capacity Euclidean arm, and fresh non-D3 validation. Multi-resonance, recurrence, exact lowering, scale, #954, correctness, and reasoning remain blocked. PR #1003 publishes the exact result and synchronized repository direction.

## Authoritative learned-manifold V2 result after PR #1006 (2026-08-30)

This final section supersedes earlier present-tense next-action prose without rewriting any frozen contract or result. Protected PR #1006 merged at `dda9c37db8e8ff9778dbe464c814a10c9d55c6e0`.

`HELM-D-R4` remains the qualified bounded ordinary causal-softmax reference in coherent R4/Spin frames. `HelmDLearnedManifoldR4ConstructionV2` Attempt 02 is a valid non-D3 construction-validation negative at `FAIL_HELM_D_MANIFOLD_CONSTRUCTION_REVISE_PROJECTION_SCORE_CENTROID_OR_TRAINING`, result self-CID `blake3:9144913380c6ebdeebb5848138bc8e6642c1e7020d8e7a097aa3cd73cb829020`, checkpoint self-CID `blake3:dd04bfc1cf15e5dd2c6c8be5afa363ecb452386f54632cd120bce56018444789`. Donor/gauge parity and all destructive-control separations passed; learned Lorentz NLL `7.71061809923296` failed donor retention (`3.667626465210025`) and matched Euclidean parity (`4.483153905078387`). The controls establish intervention sensitivity only. D3 remains `NOT_RUN`.

The sole active #973 action is the separately frozen `HelmDScoreCentroidLocalizationR4V1`: use only the prior 16 construction-fit documents, split 8/8 for the new fit/audit; exclude the revealed V2 validation documents; cross Lorentz/Euclidean score with normalized-Lorentz/tangent value readout; and stop at the two-document fail-fast gate unless tangent readout lowers aggregate MSE by at least 10% on both. The longest authorized path is 66–70 minutes after build. Resonance, recurrence, E8 expansion, exact/table lowering, scale, autonomous generation, #954, correctness, and reasoning remain blocked. #973 remains open and assigned.

## Authoritative native R4 softmax generation result after PR #1008 (2026-08-30)

This final section supersedes earlier present-tense next-action prose without rewriting any frozen mechanism contract or result. Protected PR #1008 merged through the queue as `184bdb939ec8b14ba16c87de749b51735fecd960`.

The separately frozen score/readout localization attempt stopped at `REJECT_TANGENT_READOUT_SELECT_SCORE_PREFLIGHT`: tangent value readout was worse on both untouched documents, with pooled MSE ratio `1.0643688804269025`. The fit, remaining audit, paired decoder, V2 validation, and D3 were not run. Intrinsic score/readout, score-radius, resonance, recurrent factorization, and exact lowering are parked.

`R4SoftmaxReferenceGeneratorV1` is now implemented as a native CLI reference. It retains the pinned SmolLM2 embeddings, RoPE, learned Q/K/V, ordinary stable causal softmax, value aggregation, residual/RMSNorm/MLP, final normalization, and LM head while transporting all 30 decoder attention layers through coherent R4/Spin frames. The five-prompt gate passed its frozen 4/5 quality threshold in both passes; G0-P3 honestly failed by substituting sunset for morning. All 5/5 prompt reports replayed exactly after deleting only timing. Every causal, projection, and R4 observed record equaled its expected record, and future reads were zero. G0-P1 matches the donor response and token prefix through EOS; G0-P2 through G0-P5 match all 32 retained donor token IDs.

Terminal: `PASS_R4_SOFTMAX_REFERENCE_GENERATION_ADVANCE_NATIVE_PRODUCT_BRIDGE`. Record BLAKE3: `blake3:34b6d7ddcdb858092ec7b192338fc236e5dfc3768fd8f4fb05dbe4ff2a23e82e`. Aggregate BLAKE3: `blake3:580360f44fe7163e697043ab8d6f801f578a5e7ed3d905209a166e7d303af6d0`.

HELM-D is credited as the MIT architectural reference at upstream commit `7501deca8f413848bfef804be64ce874b72a3cd7`; no HELM checkpoint or HELM generation implementation executed. The language stack is UOR's existing pinned SmolLM2 `HuggingFaceLlamaOracle`.

This establishes provider-free native source-backed ordinary attention and bounded generation through the R4/Spin seam. It does not establish geometry advantage, softmax removal, source-free/table-native or multiplication-free inference, correctness, reasoning, frontier quality, release readiness, or static-WASM model hosting. #973 remains open and #954 remains blocked.

**Now:** qualify one explicit opt-in native HTTP/dashboard bridge of this exact policy with no default-engine change. Optimize or qualify latency only as needed for one real end-to-end prompt. No tag, release, hosted promotion, or static-WASM claim is authorized by this result.

## Authoritative current state after PR #1009 (2026-08-30)

This final section supersedes earlier present-tense bridge and next-action prose without rewriting any frozen negative or positive evidence.

Protected PR #1009 merged through the queue as 28acc8278c2c02b6f923b86c23eb5b728ab96bc0. The dedicated, opt-in, loopback-only native HTTP endpoint now passes the frozen G0-P1 eight-token HTTP-to-CLI identity contract:

- HTTP 200 in 183.876699 seconds;
- decoded Plants need sunlight to undergo photosynthesis, a;
- generated IDs [34246, 737, 8118, 288, 11205, 19173, 28, 253];
- decision CID blake3:e1843e45e62ab9c5872a26e808e43449841350aaef7c848d80a5a5ef9fbabbd2;
- persistent-state CID blake3:438436331a5030cb04438eb791baed8313b245e0fc2de4943bf20a034a5feacd;
- all 30 decoder layers with exact causal, projection, and R4 audits and zero future reads; and
- requested/effective/maximum active workers 8/8/8.

The route is disabled by default, request bounded, process-wide single-flight, source-path/prompt scrubbed on success and error responses, and separate from /api/chat, OpenAI-compatible routes, default-engine selection, router state, and static/WASM execution. Dashboard wiring/native-readiness and static/WASM-isolation checks pass. Browser interaction/E2E is NOT_RUN; the model canary directly exercised the HTTP endpoint.

HELM-D remains a credited MIT architectural reference at 7501deca8f413848bfef804be64ce874b72a3cd7. No HELM checkpoint or upstream generation code executed and no upstream paper result is inherited. The executed language model remains UOR's pinned SmolLM2 HuggingFaceLlamaOracle source-decoder path.

Terminal: PASS_R4_SOFTMAX_REFERENCE_HTTP_BRIDGE. Structured-result BLAKE3: blake3:9795c2e68f175037fa4cf3227697541b050a4db784a297b239c3a14482cdfbc8.

This establishes faithful native HTTP access to the already-qualified source-backed R4/Spin ordinary causal-softmax reference. It does not establish source-free, table-native, multiply-free, transformerless, browser-WASM, geometry-advantaged, correct, reasoning-capable, frontier, hosted-product, tag, or release readiness.

The only active #973 successor is the proposed R4SoftmaxTeacherTraceV1 plus R4SoftmaxTraceCompilerV1 rung. It must capture construction-only causal layerwise token/Q/K/V/attention/value/logit traces from this exact oracle, bind source and policy identities, compile the first source-free student/attention-state artifact, and compare decoded tokens plus next-token loss under causal-input and matched-control audits. Both names remain NOT_IMPLEMENTED and NOT_RUN. Intrinsic/readout, resonance, softmax replacement, recurrence, E8 expansion, exact lowering, scale, and product promotion remain parked behind that baseline.

#973 remains open and assigned; #954 remains blocked.


## Authoritative group-addressed construction terminal and successor (2026-09-01)

This final section supersedes every earlier present-tense active-mechanism and
next-action statement while preserving all frozen contracts and results as
history.

The independently frozen `R4GroupAddressedRetentionLMV1` construction gate
terminated `UNAVAILABLE_FRAME_POPULATION_OR_LOCAL_BUDGET`. Result CID:
`blake3:72ed93ec7e1356091cb40bdc6f35d89cf6520f4262778149908817a514832018`.
Exact geometry and 120/120/120 generated coverage, the 256/64 natural-language
population, label-free reachability, byte-identical initialization, equal
operation/read ledgers, finite nonzero gradients, MPS memory, and held-out
sealing passed. Stationary/direct recurrence parity was separately verified by
focused logit, final-state, and gradient tests; the official envelope records
that parity as a required contract marker rather than an in-run measurement.

The official timing projection was `796.4167 s`, above the frozen `720 s`
admission ceiling. More importantly, the sole disposable smoke barely learned:
exact H4, C120, and scrambled H4 reduced CE by about `0.13%` rather than the
required `80%`, and exact-H4 state-off worsened CE by only `0.0003223` nats
rather than `0.05`. The miss is therefore learning capacity, not unreachable
geometry or a disconnected retained path.

No main authorization was emitted. Main optimization and held-out model scoring
are `NOT_RUN`; held-out training reads remain zero. There is no
`ATTENTION_PASS`, H4-advantage, generation, correctness, or reasoning verdict.
Do not retry, tune, or scale this exact embedding-plus-one-cell model. Preserve
its reusable exact group exporter, stationary-frame recurrence, population
freezer, structural census, and MPS gate as substrate and controls.

The sole active #973 action is now to scientifically select and independently
freeze a fuller source-free decoder block. The candidate must add the missing
language-model machinery around group-addressed retained attention—residual
stream, normalization, depth, nonlinear channel mixing, and an output head—then
compare the retained path with state-off and matched non-geometric controls
before any held-out reveal or exact-runtime lowering. #954 remains blocked and
C1-SB6 remains unauthorized.


## Post-terminal delivery/provenance correction (2026-09-01)

The exact started and result envelopes are being preserved in the protected
code/documentation delivery. The started envelope binds trainer tree
`blake3:126df4d82a020ada3898dff0d61d17f71beca4dd614cbc97a017b80227877ab9`.
The safer shipping tree intentionally differs after the terminal: an
unimported/unexecuted draft main harness was deleted, package/help prose was
updated, and the unused held-out reveal helper was removed because it did not
verify real fitted artifacts plus positive authorization. No experiment was
rerun. The terminal package exposes no held-out-open API; the official directory
remains mode `000`, with no reveal marker and zero reads.


## Authoritative CPU-recovery terminal and next action (2026-09-01)

This final section supersedes every earlier present-tense next-action statement while preserving all frozen contracts and results as history. Protected PR #1032 merged as `bf2d8d86ef07d610f34aee6b42605d911012fe84`.

`R4GroupAddressedRetentionDecoderV1CpuRecovery` completed all 512 construction optimizer steps in `438.117083` seconds on deterministic Apple Accelerate CPU with one process, sequential arms, and four configured PyTorch/Accelerate threads. Result CID: `blake3:68355ad2f61d02dc73dbf22de4c24834815a23069ed5735630dc365081cf91db`.

On the disjoint construction-validation partition, state-off lost `0.967227` nats and 182 of 4,096 top-1 decisions. This is bounded causal retained-attention component evidence. The exact 3.17M-parameter, two-block, data/dose recipe did not satisfy its frozen full-decoder generalization criterion: aggregate validation CE moved `8.371911 -> 8.976155`, so the terminal verdict is `RETAINED_DECODER_FAIL` and this complete decoder is not promoted or scaled.

Formal H4 specificity remains `NOT_EVALUATED`. Diagnostically, scrambled-transport CE was `0.033049` nats better while exact H4 led top-1 by four decisions, below threshold. No H4-specific advantage is established.

The sole active #973 direction is now an independently frozen, data-supported language-path generalization mechanism that retains the bounded attention primitive as a component hypothesis and compares it with an ordinary matched non-geometric decoder. It must not open heldout data, claim coherent generation or reasoning, promote #954, lower to exact runtime, or authorize a release before its own gates pass.

Substantial offline jobs must predeclare a small set of materially plausible deterministic backend/thread/worker plans, benchmark one representative unit from each, and select the measured-fast stable plan. Neither one core nor maximum workers is a silent default; CUDA remains out of scope unless a future issue explicitly authorizes it. The deployed target remains CPU/table-native.

#973 remains open and assigned. #954 remains blocked.

## Authoritative paired-H4 prompt-capacity terminal and readout successor (2026-09-01)

This final section supersedes every earlier present-tense next-action statement while preserving all frozen contracts and results as history. Protected PR #1034 merged through the queue as `22218652fcd9bddf76f0a574e67111a9ad4bdd99`.

Qualified `R4RetainedLanguagePathV1` remains the accepted bounded source-free attention cell. Its independently frozen paired-address successor, `R4PairedH4PromptCapacityV1`, completed `PAIRED_H4_PROMPT_CAPACITY_FAIL` at result CID `blake3:508a4ff352f1e533d669d9616f65b972b0f13e8efe35867b7b095281ad940274`.

The paired construction reduced simultaneous cumulative-route repeats from 1,937,864 to 47,522, a 97.5477% construction-only reduction. On 247,920 fresh held-out decisions, candidate NLL/top-1 were 3.883229/29.7802%, slightly better than frozen V1 at 3.890115/29.7064%. The decision-bearing prompt-swap criterion did not improve: candidate G was 0.0062477543 versus V1 0.0063672952, delta -0.0001195409, with 282/512 wins below the frozen absolute 0.043321699 and 308/512 win floors. Both state-off arms collapsed exactly to zero; causal, forbidden-read, artifact-before-reveal, CID-binding, and replay checks passed. A fresh-process audit reproduced all prompt and held-out scores and trace CIDs.

The canonical 256-pair population CID is `blake3:c11a7c935139ca169460b90c01392d7c9e0929e4c10710e76e6c8f74cbdf0340`. A provisional `9e04...` feasibility scan was rejected before public freeze and before model scoring because it omitted canonical whitespace normalization and mixed canonical train/dev/test stories.

Reject paired addressing as the prompt-capacity successor. Preserve V1. Do not tune or retry the revealed population, run candidate generation, add route families, expand the corpus, or begin exact lowering from this result. Attention remains established at V1's bounded scope; prompt-conditioned coherence, reasoning, H4-specific superiority, exact/table-native runtime, browser readiness, and release readiness remain unestablished.

The sole active #973 action is one independently frozen prompt-state-to-logit readout experiment that holds the qualified V1 recurrence, representation, data, optimizer, dose, and budget fixed, and varies only how retained prompt state reaches candidate logits. It must retain matched V1, state-off, causal, replay, and forbidden-read controls. #973 remains open and assigned; #954 remains blocked and no C1-SB6 is authorized.


## Authoritative direct-readout terminal and sole successor after PR #1035 (2026-09-01)

This final section supersedes every earlier present-tense #973 next-action statement while preserving frozen contracts and results as history. Protected PR #1035 merged through the queue as `d36c16b8217dae7eb68cf8d32483d4f786ceb2d1`.

`R4DirectRetainedReadoutLanguagePathV1` held qualified V1's exact-H4 recurrence, representation, learned parameters, state, training data/order, seed, optimizer, 2,730-step dose, and one tied vocabulary matmul fixed. It changed only the final head input from `N(h)` to `N(h) + g*N(a1+a2)`, fixed candidate `g=1` versus matched V1 `g=0`.

On the independently sealed V2 prompt population, candidate gain was `0.0215897894` nats/token versus V1 `0.0076304198`, delta `+0.0139593696`, with `343/512` versus `313/512` directional wins. Own-prompt NLL improved from `3.7415367661` to `3.5521331251`. On 247,920 fresh held-out language decisions, candidate NLL/top-1 were `3.7374367989` / `31.542433%`, versus V1 `3.9010778353` / `29.632946%`. State removal cost `1.1234286047` nats and 20,179 correct decisions; both state-off prompt contrasts collapsed exactly to zero.

The wins, language, state-off, causal, forbidden-read, artifact/reveal-binding, replay, and independent-verification gates passed. The absolute prompt-gain floor `0.0433216988` and incremental floor `0.0253415693` did not. Terminal: `DIRECT_RETAINED_READOUT_PROMPT_CAPACITY_PARTIAL`, result CID `blake3:71dd85e610dcc50b74cb2bb2068e5a1a433ac5df5db2a4f8fde22fb41735889c`; independent verification CID `blake3:b8ad3b6fa6d6ab9e429b3bd8d2a5060215d15230cd272e7272f27b7eef54785b`. A second read-only audit reproduced 90 provenance, arithmetic, hash, and mode checks with zero discrepancies.

Freeze the artifact. Do not generate, retry, widen, lower, or tune gain after reveal. This is directional positive evidence, not prompt-capacity qualification, coherent generation, reasoning, H4 superiority, exact/table runtime, browser readiness, or release readiness.

The sole active #973 successor is one fresh independent freeze of `R4LayerwiseNormalizedRetainedReadoutLanguagePathV1`: `E @ [N(h) + (g/sqrt(2))*(N(a1)+N(a2))]`, fixed `g=1` versus `g=0`, zero new learned parameters/state, unchanged recurrence/data/init/order/2,730-step dose/work/gates, and a CID/story-disjoint V3 prompt population plus fresh held-out slice. V2 may never be scored or tuned again. If any unchanged prompt, language, state-off, causal, or replay gate misses, stop the parameter-free readout ladder and pivot to learned associative binding/readout. There is no scalar tweak or third normalization attempt.

#973 remains open and assigned. #954 remains blocked; no C1-SB6 is authorized.


## Authoritative layerwise-normalized readout terminal and learned-binding successor (2026-09-01)

This final section supersedes every earlier present-tense #973 next-action statement while preserving frozen contracts and results as history.

The sole `R4LayerwiseNormalizedRetainedReadoutLanguagePathV1` trajectory completed on Apple Accelerate CPU with four threads: 2,730 optimizer steps, 5,241,600 presentations, and 1,447.764 seconds. It held the qualified V1 recurrence, persistent state, data, deterministic order, seed, optimizer, dose, parameter count, and one tied vocabulary projection fixed. The only candidate change was `E @ [N(h) + (1/sqrt(2))*(N(a1)+N(a2))]`, with equal-work `g=0` control and zero new parameters or state.

On independently sealed V3, candidate prompt gain was `0.0286980210` nats/token versus V1 `0.0073316237`, delta `+0.0213663973`, with `339/512` directional wins and improved own-prompt NLL. On 247,920 fresh held-out language decisions, candidate NLL/top-1 were `3.7126411677` / `31.661826%`, versus V1 `3.8850003883` / `29.728138%`. Removing retained state cost `1.3495375637` nats and 20,595 correct decisions. Every language, state-off, causal-input, replay, forbidden-read, artifact-before-reveal, and CID-binding gate passed.

The two decision-bearing capacity floors missed: absolute prompt gain `0.0286980210 < 0.0433216988`, and candidate-minus-V1 gain `0.0213663973 < 0.0253415693`. Terminal: `LAYERWISE_NORMALIZED_RETAINED_READOUT_PROMPT_CAPACITY_PARTIAL`. Result CID: `blake3:35396bd6e64fc2c0bc7d86a84cc9e212ed913ce28e5353f5f2b8212b4cf2c532`. A separate process with fresh model instances, no optimizer, no training batches, and zero optimizer steps reproduced all 13/13 comparisons; verification CID: `blake3:3f316541dbab8061ed5ba891bf6a47ef22c55bca21fba01f6f97dbb3cb8497aa`.

Freeze this candidate as evidence; do not promote it. End the parameter-free readout ladder. No retry, gain tuning, `g=2`, third normalization placement, widening, generation, or lowering is authorized. Qualified `R4RetainedLanguagePathV1` remains the bounded source-free attention baseline.

The sole active #973 successor is one freshly frozen learned associative binding/readout. It must give retained states learned candidate-relative binding/readout capacity under a predeclared, independently sealed prompt population and matched V1/non-geometric/state-off/causal/replay controls. Its exact architecture, budget, data boundary, thresholds, and divergent actions must be frozen before implementation or outcome access.

Generation, coherent prompt following, reasoning, H4-specific superiority, exact/table-native lowering, browser readiness, and release readiness remain `NOT_RUN` or unestablished. #973 remains open and assigned. #954 remains blocked; C1-SB6 is unauthorized.



## Authoritative predictive block-delta binding freeze (2026-09-01)

This final section supersedes earlier present-tense successor prose without rewriting historical contracts or results.

`R4LearnedCandidateLeafAssociativeReadoutV1` completed terminal `LEARNED_ASSOCIATIVE_READOUT_NO_CAPACITY` and was delivered through protected PR #1037 at `3e768fa520bfb2fbc811610cd5b746cf89b41413`. Geometric gain was `0.0063767854` with `299/512` wins; the independently learned pooled control gained `0.0102632346` with `324/512` wins. Both missed the frozen absolute and incremental capacity floors. Geometry attribution also failed. Qualified V1 state remained load-bearing and pooled values improved fresh language, so the narrow diagnosis is failed candidate/address binding, not absent retained information.

The read-only write/address audit found that V1 writes only the identity slot after transport while 4,096 candidate tokens collapse onto 35 fixed leaves. The true target leaf was occupied for only `35.9645%` of construction decisions and `35.9265%` of validation decisions.

**Now:** implement the publicly frozen `R4PredictiveBlockDeltaBindingV1` contract in `docs/r4_predictive_block_delta_binding_prompt_capacity_973.md`. It keeps qualified V1 immutable and adds one bounded four-bank R4 matrix memory that binds the previous causal context key to the newly observed token value. Only four `48 x 48` maps and twelve bank gates are trainable: 9,228 values; added recurrent state is 816 f32 values. Canonical H4 token-leaf/current-frame anchors are compared against an independently fitted equal-budget identity arm, fixed connection permutation, live additive no-delta-overwrite, and state-off controls.

No V5 target may be created until the implementation passes mechanics plus one disposable first-32-V4-pair expressivity gate within 256 steps. Failure rejects this version without a full fit. Passing freezes the implementation CID and authorizes exactly one new jointly sealed, story-disjoint V5/fresh-language campaign. Generation remains unauthorized until terminal prompt capacity passes; geometry attribution remains a separate comparison. #973 stays open and assigned and continues to block #954. CUDA remains out of scope.

## Authoritative V5 predictive-binding terminal (2026-09-01)

This final section supersedes every earlier present-tense #973 mechanism and next-action statement while preserving all frozen contracts and results as history. Protected PR #1038 merged as `6c7354c2b935a11f78daef9074c42f3442845685`.

The independently frozen `R4PredictiveBlockDeltaBindingV1` campaign completed validly after a scoring-only recovery repaired unequal final-batch work-ledger comparison without changing any target, fitted artifact, optimizer state, threshold, or result. All `2,066` windows were scored; post-reveal optimizer steps were zero. The first harness stop remains preserved as `NOT_RUN`. Recovery/result/verification CIDs are `blake3:7b76e36e44798bebf184ece08fdd8a2065bdd370106b5d64d5fae4c59dc6d88b`, `blake3:6c67544d675eafcb8eb9c0dabb93617e3f6c3295af812e8acbb687107c010a74`, and `blake3:567cf336eb05c3ec562aef7135f6fb35b580d02c758b0e79f2508ca21fba01f6f97dbb3cb8497aa`.

Terminal: `PREDICTIVE_BINDING_NO_TERMINAL_CAPACITY`. Geometric prompt gain was `0.03896945868086732` with `375/512` wins, missing the frozen `0.04332169878499658` capacity floor by `0.00435224010412926`. It separated from transport-permuted geometry by `0.03181032686529761`, but missed the required plain-arm incremental floor and had worse own NLL than plain. The independently fitted additive arm was language-valid and stronger in gain; delta-overwrite attribution therefore failed. Fresh-language retention and every causal/integrity/replay gate passed.

Binding action: `STOP_WITHOUT_GENERATION`. Retire only this V5 write/binding law. Do not retry, tune, widen, lower, generate from it, or use it for correctness/reasoning. This result does not negate the qualified `R4RetainedLanguagePathV1` bounded source-free attention baseline or the established ordinary R4/Spin dot-product/stable-softmax attention reference. The practical coherent-generation path remains #1017 `r4 generate`; any further #973 research mechanism requires a separately authorized fresh contract. #973 remains open and assigned and continues to block #954. C1-SB6 remains unauthorized.

## Current programme checkpoint after #1059 (2026-09-02)

This section supersedes older present-tense next-action/status prose; frozen historical contracts and results remain evidence. Protected PR #1060 merged as `83e1e90d9fae8e92457e32f16223996f5389f4ed`.

**Completed:** `ZoologyCoherentR4InferenceV1` preserved #1050's learned recall through native coherent R4 attention. Plain and R4 both scored **11,900/12,000 = 99.1667%** with all 12,000 top-1 decisions identical. The deliberately inconsistent source-frame transport control scored **2,307/12,000 = 19.225%**, a **79.9417 percentage-point loss**, at matched causal support and work. Max logit difference was `2.002716064453125e-05`, attention difference `8.940696716308594e-07`, and NLL difference `8.900960288271698e-09`; all frozen criteria passed.

Unchanged learned tensors/tied head, full 8,192-token mapping, zero optimizer updates, and exact fresh-process replay passed. Inference reached 24 of 120 H4 frames. Combined run/replay was **27.49 seconds**, peak RSS **1.844 GiB**, on four CPU/Apple Accelerate threads. No #1057 model or checkpoint was opened. This is retained-open-population integration and transport sensitivity, not H4 superiority, new sealed generalization, softmax removal, source-free/table lowering, English context binding, or generation. Broad QA remained dormant; required protected-queue jobs acknowledged delivery.

Result CID: `blake3:bdf5a440562bf31a6c0d6d53cef0454270638b87508f0a758aaf9eb3a0031f7d`. Replay CID: `blake3:458f6f8817203e57089580d851971d7d32234c5d9e4edf96967984097bd7f181`. Complete implementation, preparation, raw result, replay and operator instructions are in the merged [#1059 record](https://github.com/UOR-Foundation/uor-r4/blob/83e1e90d9fae8e92457e32f16223996f5389f4ed/docs/r4_zoology_coherent_inference_1059.md).

**Next recommended action:** separately scope inference-only reuse of the same adapter on #1057's preserved final block-40 artifact and original exact-data population, with the retained **8,071/8,192 = 98.5229%** plain reference and its own newly frozen transport control. This checks transfer to UOR serialization/longer binding context before a language-context application. Preserve #1057's original near-target result and unrun historical control. No third training window or geometry expansion is implied. Additional coherent frame labels alone change coordinates, not learned capacity; a later expansion must name an actual representation, memory, update-law, or score change.

#973 remains open and assigned as the active programme lane and continues to block #954. Correctness, reasoning, chat, exact/table runtime, and release remain unestablished. Prior ordinary/R4 and qualified source-free retained-attention positives remain intact.

## Current programme checkpoint after #1061 (2026-09-02)

This section supersedes older present-tense next-action/status prose while retaining historical contracts and measurements. Protected PR #1062 merged as `62c64ccf2cba7674e27628775e78fb81c44baee0`; main's file tree matches the reviewed delivery.

**Completed:** `ZoologyExactDataCoherentR4InferenceV1` applied the unchanged #1059 adapter and native map to #1057's preserved final block-40 model and original exact-data population. Plain and R4 both scored **8,071/8,192 = 98.5229%**, with every prediction ID identical. The new inconsistent-transport control scored **1,009/8,192 = 12.3169%**, an **86.2061 percentage-point loss**, with identical causal support and work. Maximum logit/attention/NLL differences were `2.5272369384765625e-05`, `2.2351741790771484e-06`, and `7.450580596923828e-09`; all frozen preservation criteria passed.

Learned tensors and tied weights stayed unchanged. Exact fresh-process replay passed. Combined run/replay took **33.79 seconds** on four CPU/Apple Accelerate threads, with **2.411 GiB** peak RSS. No training, checkpoint/optimizer/evaluation-RNG loading, historical physical-binding-control read, native rebuild/export, or old QA ran. Vocabulary 4,096, sequence length 120, eight queries per row were covered by the unchanged 8,192-token native map; 24 H4 frames were reached. #1057's original near-target training terminal and unrun physical-binding control remain unchanged. This is preservation and transport sensitivity on previously observed development, not new sealed generalization or geometric superiority.

Result: `blake3:ac2ec4d533ac47d25f8eb9dfd7a41147147d73c0e2d9531352d9f9fb2eb84e58`. Replay: `blake3:af6c239ec2d0e11f26f50f74150c992dea345ec21257141fcec1096a573e708e`. Full preparation, raw result, replay and operator commands are linked in the [#1061 record](https://github.com/UOR-Foundation/uor-r4/blob/62c64ccf2cba7674e27628775e78fb81c44baee0/docs/r4_zoology_exact_coherent_inference_1061.md).

**Next recommendation:** freeze one small English supplied-context binding curriculum under #973 using the working attention architecture and R4 adapter. Hold the question fixed while supplied facts change, add distractors and swapped/missing-history controls, and bind lexical encoding, learning dose, and disjoint construction/development populations before fitting. These MQAR checkpoints are not English models; zero-shot English failure is not an attention-existence test. Preserve both learned artifacts. Further learning and geometry expansion are outside this completed inference-only delivery.

#973 remains open and assigned, blocking #954. Correctness, reasoning, chat, exact/table runtime and release remain unestablished. Prior ordinary/R4 and qualified source-free retained-attention positives remain intact.

## Current programme checkpoint after #1063 (2026-09-02)

This final section supersedes older present-tense next-action/status prose while preserving every historical contract and result.

Protected PR #1064 merged as 6df75837e55a4ffbbc760bb2bd9650ab27fea433 and closed #1063. The merged tree exactly matches reviewed head 311db033f566a7f094abd4f269ebf12dad7378ba (tree 3e4ae3f54999273d0d93c6568dc2c8a1fa559ff1).

The one frozen English curriculum completed all 3,920 updates and 2,007,040 answer presentations, but returned `ENGLISH_BINDING_CONSTRUCTION_MISS`. Final construction supported accuracy was 2,396/8,192 (29.2480%), held-out supported accuracy 218/1,024 (21.2891%), complete counterfactual groups 0/256 (0/128 for each question type), and missing-binding accuracy 37/256 (14.4531%). Changing the question with the facts fixed changed only 12/512 predictions; location swaps changed 106/512. These output counts do not establish an internal cause or a capacity ceiling.

Exact fresh-process replay passed. Combined fit, scoring and replay took 249.3656 seconds, maximum RSS 0.4856 GiB, on eight Apple Accelerate threads. The optimizer received zero development decisions. Model, checkpoint and exact data remain retained at the local issue-1063-zoology-english-binding evidence root.

- Final model: blake3:a4eb5ef76c387ca6ebe9f185b1a5ad023c81291ce4cc9000bb5d23248aaef282
- Result: blake3:aaca100c5c2b8abfb126937523c5cce44bb7e6ca2eb8d48260f42e9281606e0f
- Replay: blake3:dd5984c22d507faa1e2cea0f9b0c8051fbd3ec923cf53c896768e62708295e02

Conditional R4 inference and transport control are `NOT_RUN_ENGLISH_BINDING_MISS`. #1061 still preserves all 8,192 prediction IDs while scoring 8,071 correct, with its 86.2061-point inconsistent-transport loss. #1059 and #1057 artifacts and historical controls are unchanged.

Next: one separately frozen, construction-only diagnostic on the retained English model, with zero training. Classify target/same-owner/same-object/unrelated-location choices and question/location-swap responses by fact slot before choosing a narrowly targeted lexical/readout change. This diagnostic is proposed and unrun. The English surface requires gathering two query words into a constant colon readout, unlike MQAR's direct query-key readout; the missed fit does not establish that the architecture cannot learn those extra compositions. More geometry remains deferred.

#973 stays open and assigned; #954 remains blocked. General English understanding, H4 superiority, reasoning, chat readiness, softmax removal and integer/table lowering remain unestablished. Broad QA stays dormant; queue acknowledgements were transport only.

[Binding record and raw evidence](https://github.com/UOR-Foundation/uor-r4/blob/6df75837e55a4ffbbc760bb2bd9650ab27fea433/docs/r4_zoology_english_binding_1063.md).

## Current programme checkpoint after #1065 (2026-09-02)

This is the current checkpoint and next recommendation; earlier sections remain historical evidence. #1065 completed the construction-only diagnostic of #1063's retained final English model, delivered through #1066. Terminal: `CONSTRUCTION_DIAGNOSTIC_COMPLETE`; descriptive focus: `QUESTION_READOUT`.

The entire #1063 construction score reproduced exactly in all 13 fields: 2,396/8,192 correct (29.2480%), NLL, predictions, full selected logits and attention. Of 4,096 fixed-history question pairs, 3,974 (97.0215%) retained the same prediction and only 20 were both correct. Fixed-target question-logit contrasts were positive in 2,040 pairs and negative in 2,056. Supplied-location outputs were 6,905/8,192; the remaining 1,287 were UNKNOWN. No absent-location or other-vocabulary output occurred.

No overall displayed-slot or pooled q0 attribute-confound majority fired. Maximum slot share was 27.7625% with balanced 25% target exposure; q0 in-history errors were same-owner 841, same-object 834, unrelated 578. Smaller position and type-specific attribute effects remain visible, so this does not prove their absence or identify the constant colon as an internal cause. Location swaps changed 1,134/4,096 predictions, with 247 both correct. The two mean contrast summaries are algebraically linked, not independent evidence.

All 11 named focused checks and independent source/evidence/documentation review passed. Fresh-process replay reproduced complete evidence exactly. Combined execution was 3.433826875 s; peak RSS 0.774902344 GiB on eight CPU Apple Accelerate threads. Training updates, new development decisions, development/checkpoint/frame payload reads, new rows and geometry changes were zero. Queue statuses acknowledge transport only.

Result CID: `blake3:65b23631b10fe62b215411932cd9fe45f76b43d6b8503d0f2e74dc3d256c9b61`. Replay CID: `blake3:7222a680c300552ab097ce184500c90c0e44ede8248c4c3f752aa09f4232c0ca`. Record: `docs/r4_zoology_english_diagnostic_1065.md` and its preparation/result/replay JSON.

**Next recommendation:** one separately frozen fresh fit changing only the supervised answer readout from constant colon position 40 to query-object position 37, with matched attention cell, construction inputs/labels, seed, sampler, optimizer and 3,920-update dose. Report both question types; direct object access does not remove the need to distinguish owners. The literal next input token at 37 is `?`; this is an explicit answer-readout experiment. A new transfer claim requires previously unexamined frozen development data. No such fit ran in #1065.

Preserve #1063's negative and #1059/#1061's attention positives. More geometry, generation, reasoning, chat readiness, softmax removal and integer/table lowering remain deferred or unestablished. #973 remains open and #954 blocked.

Protected delivery: [PR #1066](https://github.com/UOR-Foundation/uor-r4/pull/1066), merged as `e1d3c431ce520a59572e9158828fecef50e4f793`. Current main tree `2cb0214692cf9da51b576b0f680bd96344362e47` matches the reviewed head exactly.

## Current programme checkpoint after #1067 (2026-09-02)

This checkpoint supersedes earlier next recommendations; earlier measurements remain historical evidence. #1067 completed one matched fresh fit moving the supervised answer readout from colon40 to query-object37, with all 41 input tokens, original construction/vocabulary bytes, cell, seed, sampler/dropout shapes, optimizer and 3,920-update dose preserved.

Terminal: QUERY_OBJECT_READOUT_CONSTRUCTION_MISS, with substantial partial progress. Construction rose from 2,396/8,192 (29.2480%) to 3,735/8,192 (45.5933%): +1,339 correct / +16.3452 percentage points. NLL fell by 0.229062326 nats. Object-changing question pairs were both correct in 447/2,048 cases (previously 6), while owner-changing pairs reached 47/2,048 (previously 14). Prediction changes were 1,413/2,048 for object changes and 193/2,048 for owner changes. Wrong-owner/same-object selections account for 1,439/2,131=67.5270% of q0 in-history errors; descriptive focus OWNER_DISAMBIGUATION. Position bias remains: slots 2+4 receive 74.2686% of in-history selections despite balanced targets. These localize remaining behavior, not its internal cause.

The 8,111/8,192 construction gate was missed, so fresh development has zero model decisions, NOT_RUN_CONSTRUCTION_MISS. R4 is NOT_RUN_SEPARATE_INFERENCE_STEP. The fit completed the exact 2,007,040-presentation ledger, including 1,846,452 supported and 160,588 unknown. Fit/evaluation/replay took 290.31 seconds, peak 0.7764 GiB. Eight named focused checks and independent source/preparation/evidence/documentation reviews passed; exact separate-process replay reproduced the complete evidence. Broad QA remains dormant; queue acknowledgements are transport only.

**Next recommendation:** one separately frozen fresh matched fit retaining readout 37 and adding the owner-token embedding from 35 to the object embedding at 37 before unchanged embedding dropout/attention: x37=E(token37)+P37+E(token35). No additional parameters or random draws; keep full construction rows/labels, seed, optimizer and 3,920-update dose. Owner-changing both-correct improvement is the primary decision, with preservation of overall accuracy and object-changing both-correct behavior; report position effects separately. Freeze exact criteria and a new unscored development population before fitting. The owner was already causal; this tests direct joint-query access. No additional fit ran in #1067. More geometry remains deferred.

Preserve #1063/#1065 history and #1059/#1061 attention positives. General English, reasoning, chat readiness, H4 superiority and softmax removal remain unestablished. #973 remains open and assigned; #954 remains blocked.

Result CID: blake3:c6dfcb3a856963ab4493c3d26bf729f6d9cad70147316ef2b9b62e87c3116369. Replay CID: blake3:98c799c7844e36d68b56c6948824c1dacb53fb36e9f38bc7c052ec6fe0873fac.

Protected delivery: [PR #1068](https://github.com/UOR-Foundation/uor-r4/pull/1068), merged as `385fa32dcaa33e1a34c253e96edea69ee7b93da4`. Main tree `01a085cf7002b0bb054fe7e8fc596c49990e7e9a` matches the reviewed head exactly. [Binding record and raw evidence](https://github.com/UOR-Foundation/uor-r4/blob/385fa32dcaa33e1a34c253e96edea69ee7b93da4/docs/r4_zoology_query_readout_1067.md).

## Current programme checkpoint after #1069 (2026-09-02)

This checkpoint supersedes earlier next recommendations; prior measurements remain historical evidence. #1069 completed one fresh matched owner-plus-object encoding fit: add E(owner35) to E(object37)+P37 before unchanged source dropout/attention. Full 41-token inputs, construction/vocabulary bytes, labels, parameters, seed and 3,920-update dose remain matched.

Terminal JOINT_QUERY_PRESERVATION_MISS. Construction rises 3,735→4,118/8,192 (45.5933%→50.2686%, +4.6753 points); NLL falls 0.1386159509 nats. Owner-changing both-correct pairs rise 47→338/2,048 (+291/+14.2090 points), passing 150. Object-changing both-correct pairs fall 447→376/2,048 (−71/−3.4668 points), failing preservation even though that family's individual correct answers rise 2,029→2,083/4,096. Preserve both gains and the pair regression; #1067 remains the accepted reference under the frozen decision.

Position dependence is substantial: target-slot accuracies 35.8887%,36.0352%,29.4922%,99.6582%. Slot 4 gains 690 correct answers; slots 1–3 collectively lose 307 (38.8021%→33.8053%). Neither DISTRIBUTED_BINDING nor a false single-slot majority establishes position independence. The restricted fourth-slot result is retained evidence, not general positional retrieval.

The exact 3,920-update/2,007,040-presentation ledger completed. Fit/evaluation/exact fresh-process replay took 286.891776 seconds, peak 0.785583 GiB. All 12 named focused methods plus independent source/preparation/evidence/documentation review passed. Construction missed 8,111/8,192; new development has zero model decisions, NOT_RUN_CONSTRUCTION_MISS. R4 is NOT_RUN_SEPARATE_INFERENCE_STEP. No extra dose, geometry change or generation. Broad QA remains dormant; queue statuses are transport acknowledgements.

**Next recommendation:** one separately frozen cyclic fact-order augmentation fit using plain #1067 readout 37, without the owner residual. Retain the four-fact task, all 41 tokens, labels, fresh seed/model/optimizer and 3,920-update budget. Rotate complete fact blocks through four offsets across successive traversals, keeping all variants of a world aligned and auditing actual exposures. Unique owner-object keys preserve label/absence semantics. Compare the final candidate and retained reference on canonical construction and all four cyclic rotations. Freeze both question-type improvement criteria plus an explicit worst-slot criterion before fitting; full binding and new frozen development remain separate. Four cyclic orders do not cover all 24 permutations. No augmentation fit occurred in #1069.

Preserve #1063/#1065/#1067 history and #1059/#1061 qualified attention. General English, fresh transfer, reasoning, chat readiness, H4 superiority and softmax removal remain unestablished. #973 remains open/assigned; #954 blocked. More geometry deferred.

Result CID: blake3:bc1066eb0e9bbf08304ab296ca0c1681b7e8af4b0ea9026945ebef83c7fb9d53. Replay CID: blake3:6a6ad3ce5ef9e9541fec4994006b39d7a15ce52432a1af3cff351f0b9d96fcf2.

Protected delivery: [PR #1070](https://github.com/UOR-Foundation/uor-r4/pull/1070), merged as `87c1610a7e58980e82eaf14d1ab1852bfc021fac`. Main tree `4cf574357512041de34d9522c70eacdb3d2d240f` matches reviewed head `b232cd59c1320f2bca4fe88b5f287c91beae87e4` exactly. [Binding record and raw evidence](https://github.com/UOR-Foundation/uor-r4/blob/87c1610a7e58980e82eaf14d1ab1852bfc021fac/docs/r4_zoology_joint_query_1069.md).


## Current checkpoint — #1071 (2026-09-02)

#1071 / #1072 completed `CYCLIC_FACTS_PRESERVATION_MISS`. Canonical 45.5933%→20.7764%; all four augmented orders 20.7764–21.0083% vs reference 40.8569–45.5933%. Both paired question types regress; every slot misses 50%; no complete candidate quartet in any order. Retain #1067. Exact 3,920-update dose and fresh replay; 315.59s/.78595GiB. Fresh development 0; R4 NOT_RUN. Fifteen focused checks/reviews pass; broad QA dormant, queue transport only.

Next: separately freeze a fact-level learned owner–object Q/K and location-V softmax prototype, shared across slots with learned null/full vocabulary head; fixed grammar roles, no equality matcher or target routing. Keep four-fact controls and bounded dose. Structured binding would not establish learned English parsing. More geometry deferred; #973 open, #954 blocked; prior attention positives preserved.

[Complete record and raw evidence](https://github.com/UOR-Foundation/uor-r4/blob/88b084450067aa770099072f85348b00adbc1e6b/docs/r4_zoology_cyclic_facts_1071.md). Protected merge `88b084450067aa770099072f85348b00adbc1e6b`; main tree matches reviewed head.


## Compound binding checkpoint (2026-09-02)

#1073/#1074: `COMPOUND_BINDING_FRESH_PASSED`. All four orders: construction 8192/8192 supported + 2048/2048 absent; fresh 1024/1024 + 256/256. Value cycling follows reassigned locations with attention unchanged; exact replay. Fixed grammar supplies roles. Next: unchanged-R4 preservation of the full four-fact-plus-null mixture; geometry deferred. #973 open; #954 blocked. [Evidence and delivery](https://github.com/UOR-Foundation/uor-r4/issues/973#issuecomment-5518534791).

#1079 completion: LANGUAGE_R4_PRESERVED_CONTROL_WEAK. [Evidence and next action](https://github.com/UOR-Foundation/uor-r4/issues/973#issuecomment-5519385959). Next: construction-only token exposure diagnostic. #973 open; #954 blocked.

---

End of preserved issue body.
