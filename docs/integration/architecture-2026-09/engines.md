# UOR-R4 engine and historical track reconciliation

Read-only review, 2026-09-07. Main inspected worktree W=`/Users/casey.allard/.codex/worktrees/r4-typed-value/uor-r4`, source HEAD `05f265ad4736c362aee126f0d555c5e365396939`. Studio S=`/Users/casey.allard/Downloads/uor-r4-project`, HEAD `5a10305126df62e838cadfec5fd509e0c9705fa7`, remote `https://github.com/Casey-allard/uor-r4-wasm-chat.git`. Studio has existing modified `results/benchmark_data.json`, preserved. No builds, model loads, fits, inference, browser runs, external costs, git mutations or source edits in this review. Main active `native_geometric` assessed by lead separately.

## Scope and evidence method

Read AGENTS, current-state, lifecycle and API entry points; searched source/document names repository-wide; inspected actual execution seams and owning result records below. This is a family-level comprehensive engine map, not a claim that every line of every archive or test was individually read. `/tmp/uor-engine-files.txt` holds 5,753 path matches for engine/router/softmax/etc. These include duplicate archives, scripts, receipts and supporting mechanisms, not 5,753 distinct engines. Actual pinned source overrides stale archive prose. Review did not rerun any historical experiment.

User clarification governing recommendation: deterministic geometric address/page lookup may select bounded work; shared typed operators are the current priority, with expert gates retained as a conditional future option. Offline Rust training may use matrix multiplication, including uor-matmul; final inference must execute no matrix products. Existing artifacts that violate this remain useful references without being proposed serving models.

## 1. Historical angular and prime engines

### Angular/Poincare/Hopf original archive

Source: `research/ai-research/ai-router/router-research/README.md`, `hyperbolic_router_so8.py`, `hopf_routing_demo.py`, `_inc0171_analysis.py`, `_inc0172_analysis.py`, `_inc0173_analysis.py`; detailed increment corpus under `docs/research/increments/` and archive `docs/research/ACTIVE_STATE.md`.

Implemented family: fixed shell/sector partitions; Poincare radial interpretation; learned SO(d) chart/scaling; local EMA bucket memory and growth; complex-pair Hopf coordinates chi/delta/alpha and phase transport; native angular concentration and cache locality proxies. Most pre-INC0171 results use PPMI-SVD proxy and EMA, not language-model backpropagation. INC0167/0168 archive reports normalized vectors had inaccessible outer shell thresholds: observed concentration comes from angular partitioning, not radial capacity. INC0169 froze fixed partition law; INC0170 expanded K to 5000. These are useful routing/topology/locality donor mechanisms, but no direct frontier language capability follows.

Actual INC0171 model is a 2-layer standard transformer, width128,4 heads, vocabulary5000, K64 two-layer dense experts. `BaselineMoEFFN.forward` line~215 computes Linear+softmax then argmax; expert output is not weighted by that probability and no task-loss gradient crosses argmax to gate parameters. Hopf is fixed partition -> selected dense FFN. Permuted control merely relabels the same partitions. Recorded PTB PPL152.26 baseline versus164.54 Hopf is retained at implemented-system scope. `eff_buckets` is exp occupancy entropy, not measured executed operations; every token still executes one expert. September5 correction appended to INC0171 explicitly withdraws learned-gate/null/speed interpretation. Other archive pages still contain older publication claims and must be read under correction.

INC0172 really adds a load-balance auxiliary gradient to the gate; it does not make the task-loss path differentiable. It stopped KILL after auxiliary loss drove occupancy eff_b62.77/64. INC0173 repeats original setup on WikiText2; recorded mean ratio1.081 is historical, with same baseline/permuted-control limitations. The source correction must carry across interpretation of this family. Do not count archived copies as independent replications. MoE and transformer FFN design conflict with owner scope; retain partition/transport routines and locality lessons, not this full engine.

### Prime transport train-soft/infer-hard archive

Source: `research/prime-analysis/research/routing-infrastructure/ai-router/router-research/geometric_routing_architecture_summary.md`; `tools/prime_transport/run_router_trainsoft_inferhard_v1.py` beneath that root.

`RouterAngularHybrid` learns token embedding, routing MLP W1/W2, attention W_attn/v_attn, readout W_pred, and token injection. Converts finite one-hot phase blocks to cos/sin pairs. Soft training blends six transition operators with Gumbel-softmax. Hard inference selects one candidate by angular proximity and bypasses routing MLP, but still executes dense state transformations, attention scoring, full softmax over positions, weighted aggregate, and matrix output projection (`einsum` and `@ W_pred` around311/425). Script records D24 perfect synthetic accuracy and ~2.32x soft/hard timing scope; those are not LLM-quality or matrix-free inference results.

Valuable architecture insight survives: separate immutable routing manifold, transported state field and learned retrieval/key placement. The archive H^4 is hyperbolic4-space; it must not be confused with finite binary icosahedral H4 or the exact coupled E8 representation. Historical projected10x–100x benefits are hypotheses. Expert/sparse activation portions are not today's target.

### Gemini prime-router and current exploratory Rust router

Archive `research/archives/gemini-dev/prime-router/server.py` actually builds grammatical first-/second-order word transition maps, prime-seeded zeta vectors, normalized state drift, cosine retrieval, Hopf telemetry and corpus index. `generate_geometric_response` around964 and trajectory version1055 choose words using transition maps plus geometry/frequency terms. Separate `ollama_generate` around2085 and `engine==ollama` around2175 can author final text. Archive README names both modes; no Ollama quality transfers to geometric mode.

Rust counterpart `crates/uor-r4-router/src/lib.rs` (`UorR4Router`) implements manifold indexing, per-identity state/persistence, corpus retrieval, geometric Markov generation, thought-stream telemetry, and wasm-bindgen surface. It allocates owned vectors/maps and uses f64 geometry. `ThoughtStream.activated_experts` and expert count telemetry are not evidence of learned expert-model computation. `geometry.rs::SpectralGeometry` is explicitly heuristic: query prime/zeta vectors reconnect content to state, projected windows yield weighted routes; current grounding is not a learned language understanding engine. Reuse identity isolation, source/tokenizer-bound memory, useful view/UI telemetry only after preserving separation from model state. Avoid arbitrary identity hash proximity as semantic metric.

## 2. Compiler, graph runtime and dormant execution families

### TLA/TLS1 historical compiled engine

`crates/uor-r4-core/src/transformerless/compiler.rs`, `runtime.rs`, `region_store.rs`, `transitions.rs`, tokenizers and conversion. Offline teacher-derived embedding compression + signature/store construction; runtime reconstructs compressed codebook rows on demand via table reads/adds and combines bit signatures/XOR/popcount. `OpKernel` counts allowed primitive operations. Historical purpose was compiled next-token approximation, not native learned geometric LM. Compiler execution/byte determinism and multiplication-free primitive contract do not imply faithful model quality. Lifecycle records poor generated text/degenerate loops for specific historical bundles.

Reuse compressed borrowed storage, codec provenance, operation accounting and exact table lowering techniques. Do not revive corpus/teacher compilation as current primary training plan or copy historical container-demo predictions (0% held-out in the cited demo) as intelligence.

### R4G1 actual graph runtime

`crates/uor-r4-graph-runtime/src/engine.rs::R4G1Runtime` with `GraphView`, `PatchChain`, optional VP-tree indexes, bounded signature routing, context/suffix and session routing; `predict_served_candidates` is historical normative candidate/token authority. Source constants: shortlist8, context8, skipmix row64 and maxprobes32. Sign affinity computes masked XOR-popcount, integer shift/add. Scoring sources distinguish Base and Skipmix; context rows resolve trigram/bigram/unigram. Runtime is no_std + forbidunsafe with allocation-free steady state (indexes/load may allocate; `extern crate alloc` is present). `uor-r4-api::engine::R4Engine` wraps byte loading, tokenizer, D4 token-free permit/widen/abstain policy and scratch.

`graph-format` supplies validated packed bytes, sections, fixed layouts and CID binding; `graph-compiler` includes observation, multiresolution induction, pack, score fitting, segment/skipmix, transitions. `graph-certify` supplies separate reference scoring and quality/evidence. These are valuable hardware/storage infrastructure. They are not a replacement for learning contextual relationships and typed operations. No automatic promotion from compile success.

### Packed route attention and MSA

`graph-runtime/src/route_attention.rs`: real bounded masked XOR/popcount distance, top-M insertion and selected integer contribution accumulation, borrowed inputs/fixed scratch, P4 scanned; reference+wire support in certify/format. Source says DORMANT and no serving callers. `model/ledger.toml` #604/#605/#804: synthetic fitting ladder worked; real SmolLM2-360M support-overlap fitted0.396 vs permuted0.192 but temporally shifted null0.292 made119/120 heads vacuous under contract, so no activation. Reuse selection/census code only if it fits a real native operator, not teacher-support reproduction as automatic goal.

`graph-runtime/src/msa_selector.rs`: real fixed classification-table lookup and bounded top-M accumulation. **No query argument**; same instance selects same candidates. MSA ranks only three residues from original theorem, other eight via owner's explicit extension. Dormant no serving caller. Useful arithmetic/table dispatch substrate, not contextual language attention by itself.

`graph-runtime/src/packed_kernels.rs`: contrary to broad header, ROUT bytecode evaluator `evaluate_routing_program` returns constant0 (declared placeholder); typed transition stub likewise ledger-declared. Other bounded frontier/shortlist/kernel pieces exist. Do not tell user full routing VM exists because module compiles.

### Typed planning

`graph-runtime/src/plan.rs`, graph-format plan sections, `R4G1Runtime::plan_bounded`: real fixed-capacity P4-only BFS/iterative deepening/best-first beam with constraints, rule tables, capacity decline and replayable witness. `graph-compiler/src/future_state_planner.rs` is f32/heap bounded graph search; `compositional_planning.rs` defines task/witness/reference semantics. Historical #843 is LIMITED (12/20 joint-split cells clear effect; greedy solves8), #846 untouched final partition NOT_RUN and REASONING NOT ESTABLISHED. Model ledger confirms no production token-serving caller reaches plan_bounded. Reuse plan scratch/rule/precondition/effect/witness semantics after model learns to select/ground operations. No need a new planning framework; missing bridge is raw language -> correct state/goal/operator then returned result -> language.

`core/src/semantic/reasoning.rs` is earlier route/constraint/operator registry scaffold, not general reasoning; `ReasoningPlanV1::execute` follows supplied operator IDs, path-prefix constraints and evidence floor. Some witness strings are descriptive (`blake3:clause_proof_...`) rather than genuine digest addresses. Prefer typed later planner/new active model lineage, don't inherit these as formal proofs.

### Other dormant names are retained, not lost

Entire `model/ledger.toml` inspected. Families: Cayley-Dickson syntactic morphisms (executed0/1998 positions before scoring removal), tropical max-plus route composition (#626 1hop/2hop top1 .0044 vs .0620 null; legal add/compare operation remains reusable), Lie-Jordan/endomorphism operator algebra, Bott-Fock context fold (shipped ceiling +.16pp about one standard error), quantum cover, standalone alternative graph construction, holographic reconstruction, predictive-sufficiency/rate-distortion metrics, shortlist evaluator, anti-degeneracy/perturbation, fairness/provenance, floating reference compiler IR, monograph validator/behavioral probes, target-operator certificates. All documented off-serving/open; their names are not hidden working LMs.

Patch chain is a field of actual R4G1Runtime; PTCH compiler/lifecycle source supports precedence/fork rejection/compaction, but ledger says no deployed caller invokes try_push_patch or emits epochs. FMM `graph-certify/src/fmm.rs` implements low-rank prototype/emission factorization via eigendecomposition and floating matrix factors, off-serving; would conflict with no serving matrix products if lifted unchanged. These support future storage/measurement, not immediate semantic fix.

## 3. Core geometric attention / language engine inventory

All the following public families were inspected at source entry and owning record. Distinguish library mechanism from complete LM.

| Family/source under core/src | Actual operation | Owning record / evidence boundary | Recommended use |
|---|---|---|---|
| `source_free_table.rs` | Geometry-free fitted count ngram backoff with lexical+byte fallback; optional fixed-count R4 tie overlay | #989 table22.261404% held-out vs5.413561% unigram; #953 overlay23.211797%, matched support/work; lifecycle + source-free table records | Retain simple language baseline and emission/codec, not endpoint-quality proof |
| `local_geometric_generation.rs` | Thin source-free decode/render/append loop over already-admitted #969 path candidates; sliding bounded context | `docs/local_geometric_attention_969.md`, local generation records. Earlier natural agreement remained `still run` vs disabled `still runs`; path loop itself exists | Reuse bounded causal decode/trace, not grammar claim |
| `recursive_geometric_attention.rs` | A1 ordered-state/reachability probe; A1R repaired associative summary then bounded readout | `recursive_geometric_attention_a1_952.md`, `associative_ordered_route_summaries_a1r_967.md`: order repaired but scalar Cayley energy tied6/6; state retained | Preserve exact ordered state; reject scalar readout sufficiency |
| `higher_scope_geometric_attention.rs` | PriorSentenceCountRadius adds exact prefix candidate occupancy to already-tied count candidates | `prior_sentence_count_radius_attention_973.md`: retained bounded exact-candidate prior-prefix copy | Reuse evidence access and copy signal, not broad semantic attention |
| `paragraph_entity_spin_path_attention.rs` | Two-fact typed entity resolves recurrent descriptor, lexicographic H4-shell/fiber/torsion path distance | same-name docs #973: bounded fixture keeps H4 shell tied; phase/fiber load-bearing; construction-bound descriptor recurrence | Reuse role binding + orientation preservation |
| `conversation_entity_spin_path_attention.rs` | Same candidate support, bind entity across two completed turns, stored-spin descriptor path | same-name #973: bounded cross-turn positive, not general conversations | Preserve scope isolation and typed occurrences |
| `bounded_global_exact_spin_attention.rs` | Fold independently supplied global snapshot; compute per exact class result; V1 commuted away contrast, V2 noncommuting repair | same-name #973 has first target-free negative, later V2 positive; fixed global-snapshot scope only | Shared immutable exact-class reuse, not approximate universal cache |
| `construction_causal_return_attention.rs` | Candidate-relative prototype return from existing observed causal histories | same-name #983: pure construction classes transfer0/6, `UNAVAILABLE_ZERO_CONSTRUCTION_TRANSFER`, no generation | Preserve negative; don't revive unchanged readout |
| `corpus_induced_spin_placement.rs` | Construction-derived placement from exact immutable prime/spin leaves | document placement #973:8.367592% vs12.221651% unchanged #953 and destructive controls | Learn actual role-relative placement; avoid same marginal-center formula |
| `geometric_gated_delta_retention.rs` | Offline f64 four multirate4x4 banks, transported gated delta write after observation, learned token-local K/V/Q | `geometric_gated_delta_retention_973.md`: construction smoke only, trailed plain delta; no artifact from_bytes decoder | Causal write discipline useful; not table native or qualified advantage |
| `direct_causal_geometric_attention.rs` | Offline dense Q/K/V/O tangent placements, H4 frame transport, stable softmax, value aggregation | same-name #973: V3 H4 3/12 vs plain12/12, coherent alternative10/12; V4 covariance passed but heldout13/24 all main arms/control weak | Parity/geometry transport oracle only |
| `helm_d_r4_attention.rs` | Ordinary softmax with coherent R4 Spin gauge encode/transport/decode; separate intrinsic Lorentz candidate | `helm_d_r4_softmax_decoder_973.md`, intrinsic record: ordinary parity positive; Lorentz covariance unavailable then learned Lorentz retention/parity negative, tangent preflight rejected | Training/reference oracle, frame consistency; unchanged transformer banned for serving |
| `r4_group_addressed_retention.rs` | Exact H4 and C120 tables + prime->leaf map + transport control; **owns no LM arithmetic** | group-addressed retention family docs | Reuse group artifact, identity and controls |
| `r4_softmax_trace_student.rs` | Fixed-point suffix table distilled from teacher construction traces; source-free artifact only, no runtime softmax | #973 bounded distillation but autonomous loops | Avoid another suffix-only compression rung |
| `r4_softmax_trace_state_student.rs` | Four f32 4x4 banks/278-byte state + recurrent projection, artifact-only runtime | #1011 exact replay but decision/material-control/nonlooping gates failed; #1012 coverage insufficient | Preserve loss diagnosis; not integer-only or useful language model |

Prime route foundations/canonical ingestion/SpiralCore and exact heatmap math are covered by other reviewer, not omitted. Broad attention names do not all indicate independent neural engines.

## 4. Softmax, memory, binding and nonlinear training history

The likely intended 'improper but working softmax tree' is the broader R4 softmax lineage, **identification unresolved**. Repository-wide searches for hierarchical-softmax, softmax_tree, softmax-tree, softmax tree and tree-softmax found no explicitly named implementation. There is no basis to invent a hierarchical softmax tree. Lead also checked knowledge FTS.

Actual `tools/r4-softmax-trainer/src/r4_softmax_trainer/model.py` is ordinary Llama architecture: embeddings, RMSNorm, RoPE, dense Q/K/V/O, causal `torch.matmul`, ordinary softmax, weighted `matmul`, SwiGLU, tied output `F.linear`, cross-entropy. This is not mathematically 'improper softmax'; it is a valid ordinary transformer/reference path inconsistent with final target. Root `r4_softmax_local_generation.rs` and model-source executor run these trained weights with R4 gauge transport. UOR's HELM-derived seam credits `Graph-and-Geometric-Learning/helm@7501deca...`; SmolLM2 source supplies rest of original decoder. Geometry reparameterization numerical parity does not remove dense compute.

Key retained outcomes (owning docs are `docs/r4_softmax_*` and `docs/MODEL_LIFECYCLE.md`):
- #973 HELM-D-R4 provider-free-at-execution/source-backed 30-layer generation4/5 quality,5/5 exact replay and zero future read; loopback HTTP canary parity. Source-backed transformer, not native matrix-free language model.
- #1014 newly trained7.15M path proved attention load-bearing: attention-off worsens2.67739nats; quality NLL2.1274 and scene3/5 missed.
- #1017 continuation149,995,520 cumulative tokens gave NLL1.572752 vs strict1.5 target, scene5/5 and Rust parity/replay. Working bounded story continuation reference, not general chat.
- #1039 explicit local HTTP raw continuation; #1041 fresh narratives2/3 and both history retrieval probes fail; `KEEP_RAW_CONTINUATION_ONLY`.
- #1019 12layers13,130,784params preflights passed, safety projection20.66h exceeds8h; full training NOT_RUN. Fused AdamW path4.485223s vs3.491307s signed slower. Hardware stop isn't model-quality negative.

Other actual Python tracks, sources inspected:
- `group_retention_decoder.py`, `language_path_generalization.py`, `paired_h4_language_path.py`, direct/layerwise/learned-associative readouts. Group decoder CPU fit state-off loses.967227nats/182hits, but aggregate CE worsens8.371911->8.976155; scrambledtransport better. Qualified retained-language V1 is bounded source-free language baseline; paired-H4 capacity fails; direct/layerwise PARTIAL; associative no-capacity. Don't describe retained-attention signal as nonexistent, or this dense LM as serving target.
- `predictive_block_delta_binding.py`: four48x48maps plus12 gate logits, four banks each12 independent4x4 matrices, post-observation key/value correction. V5 terminal `PREDICTIVE_BINDING_NO_TERMINAL_CAPACITY`: own NLL3.54197; promptgain.03897 below.04332floor; no geometry/additive overwrite attribution; fresh language passed but generation not run. This particular matrix write law failed, not all memory geometry.
- `position_kv_binding.py` #1043 kept all causal positions distinct, same252,160param ordinary model; finite120token cache92160bytes. Terminal INVALID_POSITION_KV_BINDING under exact frozen mechanical/numerical criterion. Preserve invalidity, not count outcome as valid quality ranking.
- `fixed_recurrent_kv_binding.py`:8 exact recent K/V +4 H4-framed means via binary carry; old records individually unavailable after pooling;13 maximum sources inclcurrent. Retains denseQKVO,softmax,SwiGLU/head. Mechanical fixed-memory execution positive, useful language unestablished. **The binary carry tree is another possible meaning of user's softmax tree**, but no source explicitly labels it that.
- `sparse_geometric_kv_binding.py`: actual fixed signedS3 shell/maximin geometric metadata selection12->8 persistent before gather, then current=9. This is deterministic geometry, not learned sparse gating, but retained downstream dense attention fails serving goal. Mechanical execution positive no utility. The shell/selection implementation can inspire permitted bounded page selection.
- `quaternion_cube_nonlinear.py`: parameter-free f32 H4-frame normalized quaternion cube residual replaces dense SwiGLU only. Uses frame matrix actions, products/reciprocal and still dense attention/head. No-fit text worse. `r4_quaternion_cube_fit_973.md`: both attempts reached backward and8updates then projectionstop; no final fitted artifact, no128update result.18 tensors215296 active;36864 unreachable denseweights frozen. Historical resource stop is not architecture falsification.
- role-tagged associative/contextual retained/prompt conditioning families provide binding diagnostics, not accepted complete LMs. C1-SB2 independent source-relative head fit positives12/12/negatives20/20/copies6/6 but sealed5/12,14/20,0/6. C1-SB3 rank8QKVO adaption produced attention transfer but exact56/63; SB4 full-record margin70/126fit35/63sealed; SB5 paired-query56/56fit14/28sealed. Keep these negatives; don't retry unchanged.

### Zoology and controlled-language sequence

Source directories `zoology_control`, `zoology_release`, `zoology_transfer`, `zoology_clock`, `zoology_continuation`, `zoology_r4_inference`, `zoology_exact_r4_inference`, `zoology_english_binding`, `zoology_english_diagnostic`, `zoology_query_readout`, `zoology_joint_query`, `zoology_cyclic_facts`, `zoology_compound_binding`, `zoology_compound_r4`, `zoology_language_interface`, `zoology_language_r4`, `zoology_token_exposure`, `text_clause_adapter` all inventoried. Owning docs are corresponding `docs/r4_zoology_*_{issue}.md` #1047..#1082 and #1094 records.

Progression retained: #1047 preflight; #1049 scaled calibration miss; #1050 stock source reproduction positive; #1053 stock cell transfer miss; #1055 clock-matched transfer miss; #1057 continuation; #1059 R4 integration preserved; #1061 exact-dataR4preserved; #1063 English binding then#1065 descriptive diagnostic; #1067 query-object construction miss; #1069 joint-query preservation miss; #1071 cyclic facts preservation miss; #1073 compound-binding core; #1075 compoundR4; #1077 learned role reader; #1079 languageR4preserved/controlweak; #1082 exposuredescriptive only; #1094 clauseadapterpreserved; #1102 nativecomparisonpreserved. Exact dates/thresholds remain in owning records, no reruns.

Actual #1077 reader uses Embedding4096x32 -> Conv1d32x64 k5 -> GELU -> Linear64x3 then full-token softmax within supplied5clauses. Core has dense query/key/value,4fact+null attention, output norm/tied4096head. Source `learned_reference/numerics.rs` ports scalar f32/f64 operations with explicit mul/add and R4 frame matrices. #1094 rawtext parser removes supplied segmentation only on fixed controlled4fact population1600valid+96 refusal/boundary; #1102 comparison320/320 plus16refusals. Those are real bounded learned bindings and numerical preservation, but not unrestricted parsing/prose or matrix-free inference. Valuable lesson: conjunctive owner/object keys, relation roles and true values matter; weak token-frame effects warn against interpreting geometry sidecar as semantic cause.

## 5. Root reference/mixer/grounded surfaces

`src/geometric_decoder.rs` #950 can replace only final layer29 attention on SmolLM2; remaining29layers and restdense. G0 `PROMOTE_TO_G1` was one seam/persistence/reachability, not full modelreplacement. Initial layer0 treatment failedloop preserved. `src/geometric_mixer_qualification.rs` #951 trained existing small mixer over source traces, returned `REDESIGN_REPRESENTATION`; preserve negative checkpoint.

`src/r4_grounded_answer.rs`, `r4_source_span_pointer.rs`, `r4_source_relation_head.rs` are exact span copy/typed abstention/conflict wrappers over #1017 six-layer denseR4/Spin source states. Pointer/source-relative/LoRA control families match historical C1 failures above. They do not supply an accepted free-language model. Reuse typed failure/selected occurrence/output-byte contracts, avoid new query-template parser/extra special-case head as architecture default.

## 6. API and Studio path

### Main repository

`crates/uor-r4-api/src/lib.rs` explicitly reexports core `native_geometric`, same model/session as `r4 geometric`. This is currentdevelopmentAPI. Historical compile/engine/production-envelope/capability-suite APIs remain separate and include real structural checks, not all model qualities. Root large `src/server.rs` older profiles/cascades and opt-in references remain; current native service is a separate core path (lead reviewing). Never silently select a transformer fallback for desired native response.

`crates/uor-r4-workbench` exists (authority/base64/comparison/host/http/intake/ipc/launch/lifecycle/strictJSON/wire/worker). Header identifies CPUfloatingresearchhost and source-only qualification distinction. #1107 `WORKBENCH_CANDIDATE_SOURCE_FROZEN_UNBUILT` remains no model/HTTP/browser result. Do not finish this old4facthost merely because it resembles product plan; use lightweightcurrentmodelAPI and reuse isolation/lifecycle pieces only if needed.

### Separate Studio source is not UOR-R4 native model

S HEAD above verified. README contains broad claims (100%local,1500+tok/s, frontier-likecode) that **do not establish native-model capability**.

- Browser `assets/js/uor_model_worker.js` imports Transformers.js(localbundle/CDNfallback), allows remote models/cache, registry Qwen0.5/1.5B ONNX, pipeline text-generation q4WebGPU/WASM. 'glm5.3-flash' registry actually sources Qwen2.5-0.5B under another label. It is a transformer authoringpath.
- `index.html` lines5964+ worker construction,6080-6085 ingest generated chunks into wasm session for geometrictelemetry,6411+ prompt telemetry,6527+ actualworker request. Geometric visualization does not author browser prose.
- Tauri `src-tauri/src/lib.rs::run_native_inference`225-258 first shells out Ollama, estimates tokens=len/4. On failure `synthesize_native_ai_response`46+ uses hardcoded promptkeyword branches/cannedRustcode; returns engine label 'Native Apple Silicon Substrate', latency min5ms and approximate/clampedtokencount. These paths do not measure learned geometric inference or justify1500tok/s. `get_native_hardware_info` hardcodes memory16GB and treats macOS as metalsupported; telemetry must be corrected before productperformanceclaims.
- WASM `src/lib.rs` header claims multiplicationfree, but `VsaVector::project_to_8d_with_matrix`185-209 computes actual8x512 integer matrix-vector products. Wider dynamic codebook uses projection, E8snap,CORDICHopf,centroid dot products; word response branch1380+ contains hard-coded intents and phrase synthesis. AllocatingString/Vec/HashMappaths mean wholeinterface not zeroallocation. Toy constants vocabulary12/max8 tokens coexist with laterdynamicengine; don't mistake fixedtoycall for fullengine.
- `src/bin/uor_server.rs::generate_dynamic_response`257+ and `synthesize_user_response`308+ has cannedcode/intentbranches; compatibility endpoint `/v1/chat/completions` and many managementroutes exist, not evidence of fullmodelintelligence. UI/workerqueue/chat storage/Monaco/workspace diff andindexing useful as application substrate.

No browser/runtime recertification performed; current remote deployment unverified here. To deliver eventual GitHubPagesAIstudio, compile accepted matrix-free core to WASM, load its exact artifact/codec, substitute nativeworker generation behind explicit modelidentity, preserve supported streaming/state/cancel/error API, and remove transparent provider/cannedfallback authority. WebGPU is not required for intendedmodel. A completeAPI declares implemented capabilities+limits; attention/prose/reasoning require learnedbehavior, not merely endpoint names.

## Recommended synthesis for lead

Best available road is the active Rust geometric model plus stronger contextual binding/typed operator selection, then composition over retained exact occurrences and deterministic geometry-addressed pages. Geometry should organize learned semantic placement and transported finite state; exact payload identity remains recoverable. Use existing packed-table, bounded-planner, lexicalcodec and frame artifacts to implement only needed operator execution. Offline dense references can teach/debug task binding, but no servingmatrix products or expertstack and no dense transformer compiled wholesale into lookup.

Do not respond to every research import by adding every scoring term. Several were inert/aliased/negative; ensure one role and information-preserving bridge for each incorporated mechanism. Current repeatedly observed bottleneck is values/roles/order becoming inaccessible or misselected before output; a new UI, proofcampaign, genericgraphVM, universalmatmulcompiler or another scalargeometryreadout does not solve it. Numeric-vs-word upstream choice remains a sensible concrete next decision, but broadarchitecture should first consolidate it into a single typed joint source/operator law rather than endless appendedspecial-caseheads.

Reuse planning's exact operator/precondition/effect/witness and geometric finite group transport when task requires multi-stepderivedstate; reuse page/indexlookup when existinglinearwork limits measured scaling. Train generalizable lexical/role/transition features with varied construction and actual completegeneratedresponses, keep independent final heldout separate. Then measure quality/work/storage growth as context/vocabulary increases on M1. Frontier capability is objective with no current evidence supporting a capacity/speedprojection; do not infer it from algebra correctness or fixed tinyfixtures.

## Owner steering received after review

The user subsequently said expert gates may eventually be necessary. Preserve gate research as a conditional later option if a concrete behavior/cost bottleneck and matched evidence motivate it. This does not adopt the historical MoE transformer as the present architecture, erase its source/control limitations, or relax the final serving matrix-product prohibition. The current recommendation remains deterministic geometric addressing and shared typed operators. References above to MoE/expert designs being excluded describe the current chosen direction, not permanent retirement of all gate research.

## Additional source verification of dormant algebra and measurement helpers

The lead directly inspected these implementations after the family inventory. This extends source coverage; it executes none of them and changes no model result.

| Source | What the implementation does | Reuse boundary |
|---|---|---|
| `crates/uor-r4-core/src/transformerless/bott_fock.rs` | Fixed 256-cell i16 state; each append rotates/XORs a 16-component embedding, applies quarter decay and saturating injection | A real bounded lossy recurrence with no matrix product in append. The comments' Bott-periodicity interpretation does not by itself establish a faithful Clifford tensor contraction or language memory. Retain as a recurrence/control candidate, not lossless context. |
| `crates/uor-r4-core/src/transformerless/endomorphism.rs` | Actual f32 16x16 matrix products, transposes, commutators, anticommutators and vector application | Offline algebra/reference. The unchanged operations violate the serving matrix-product constraint. |
| `crates/uor-r4-core/src/transformerless/lie_jordan.rs` | Symmetric/skew-symmetric split and continuous product through the matrix implementation; separate `universal_product_u8` returns rotated XOR or AND | The tiny integer branch has no demonstrated encode/decode correspondence to the continuous universal product in the inspected source. Bitwise legality does not establish algebraic equivalence. Prefer exact finite SpiralCore action tables for a declared operator domain. |
| `crates/uor-r4-graph-compiler/src/quantum_cover.rs` | Diagonal density distribution and next-token count entropy; weighted entropy gain over a proposed partition | Actual offline entropy-based cover selection. The diagonal von Neumann entropy equals ordinary Shannon entropy in nats here; no quantum acceleration or new semantic learner follows. |
| `crates/uor-r4-graph-compiler/src/rate_distortion_compression.rs` | `analyze_rate_distortion` synthesizes base loss and observation count from `corpus_id.len()`, then generates inverse-depth error curves | A deterministic illustrative report generator, **not measured model compression or empirical rate-distortion**. Do not reuse its `is_certified` output as evidence of useful model efficiency. |
| `crates/uor-r4-graph-certify/src/predictive_sufficiency.rs` | Computes actual KL/cross-entropy/top-k metrics on supplied distributions, then combines supplied bytes/op counts in a rate curve | A distinct useful numerical evaluator. Results depend on genuine input measurements and the declared cost weighting. It does not independently execute or certify a model. |
| `crates/uor-r4-graph-certify/src/holographic_encoding.rs` | Projection-family validation, supplied recovery distributions, ablation/progressive-fidelity reports and bound certificates | Measurement/serialization helpers, not a demonstrated universal holographic compressor. |
| `crates/uor-r4-proof-model/src/lib.rs`, `proof_matrix.rs`, `structural_guarantees.rs` | Feature-gated structural checks, finite verifiers and explicit proof-status registry | Useful scoped verification machinery. A registry's populated status is not a fresh proof run or language capability. Kani/release/proof workflows were not executed in this review. |
