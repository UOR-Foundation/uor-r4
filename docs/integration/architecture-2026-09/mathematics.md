# Mathematical mechanisms and historical routing inventory

Review date: 2026-09-07 UTC. Read-only review in `/Users/casey.allard/.codex/worktrees/r4-typed-value/uor-r4`. No build, model execution, new mathematical experiment, proof compilation or external compute was performed. Paths below are relative to that worktree unless stated. Results are retained receipts, not new measurements. Parent agent owns live GitHub and current retained-artifact reconciliation.

## Architectural answer

The useful road is a causal, learned finite geometric transducer with content-addressed memory and typed operators. Keep prime identities and ordered occurrences, finite signed H4 group state, artifact-bound fixed-zeta phase channels, typed value/role records and deterministic address/page lookup. Learn which state transitions and local operators should execute; compile their finite actions and comparisons into bounded integer/table operations. A geometric location must identify useful data or an operator, and its state must retain the distinctions the task needs. Do not infer predictive value from a rich coordinate system alone.

The strongest immediate mathematical lesson across the histories is **transition-sufficient state with orientation and value provenance**. Commutative prime products, scalar distance, absolute magnitudes, a finite-horizon future-word class, or a single mean summary can each collapse distinctions needed for the next action. The current native occurrence/value state and exact noncommutative H4 fold already address parts of that problem. Reuse them before building another broad routing family.

The user's clarified target permits deterministic geometric address/page selection and offline Rust matmul while prohibiting serving matrix products. Shared typed operators are the current priority; expert gates remain a conditional future option under the later owner clarification. The original transformer-with-geometric-expert-gate implementation is not the current product route. It still permits finite group composition by table lookup, finite signed-permutation actions, fixed phase update tables, address decoding, copy/commit operations and typed arithmetic operators. The main review distinguishes learned geometric placement and typed source/action choice from learned sparse expert dispatch; the current source router has no neural expert bank.

## Mechanism map

| Mechanism | Actual implementation / capability | Evidence boundary | Reuse / missing bridge |
|---|---|---|---|
| Prime atom registry and ordered n-lets | `crates/uor-r4-core/src/prime_route_attention.rs:177-330`: validates prime atoms, keeps ordered prime vector plus lossless sorted factor multiset, computes exact overlap; optional product is diagnostic. Semiprime edges retain `p^2` self-loops as distinct valid repeats. | Exact identity/address mechanics; factor overlap does not define semantic similarity or word order. | Keep as stable content/role occurrence addresses and shared-factor indexes. Do not multiply an unbounded context into a giant integer or discard occurrence order. |
| Fixed zeta phase deltas | Same source, `zeta_phase_delta` around895, fixed512-grid constants near25; ADR0003 defines `theta_j(p)=wrap(gamma_j log p)` and exact log-ratio phase delta. | A finite immutable coordinate basis; no proof of RH needed to execute it. Current native task controls do not universally establish zeta predictive benefit. | Compile phase deltas offline; runtime bounded modular updates/lookups. Keep channels only with declared roles, separate architectural retention from measured benefit. |
| Exact golden radial algebra | `prime_route_attention.rs:381` `ZPhi`, exact checked coefficient addition/multiplication and Galois conjugation. | Real `Z[phi]` algebra; these compiler helpers do contain integer multiply and floating phase conversion elsewhere. They are not automatically frozen-kernel-compatible. | Build finite transition tables for selected radial/operator laws. Do not infer semantics from divisor count, which often mainly reflects context length. |
| Signed S3 / Hopf S2 / fiber / torsion | `prime_route_attention.rs` UnitS3Q30, UnitS2Q30, SpinTorsionState; validation checks Hopf observation agrees with S3. | S2 is an observation quotient; fiber information cannot be thrown away when the task uses it. | Preserve signed quaternion state, fiber and torsion as typed fields. S3 state is not an arbitrary4-vector merely renamed. |
| Hopf polar angular address engine | `crates/uor-r4-core/src/lib.rs:297-370` actually computes `(rho1,rho2,chi,theta1,theta2,delta,alpha)`, transported fiber `alpha + .5*lambda*cos(2chi)*delta`, allocates triplet bins and returns sector. | Floating exploratory router code with atan2/asin/sqrt/cos, allocations; not the final integer runtime. | Use as an offline geometric chart/address compiler; lower fixed finite charts into lookup. No automatic learned LM semantics. |
| Native H4 group fold | `canonical_lexical_ingestion.rs:1339-1450`: exact scaled Z[phi] quaternion roots; 120-state binary-icosahedral closure; table multiplication, inverse, identity and ordered fold. | A finite noncommutative state, not lossless arbitrary-length history. A single120-state summary inevitably collides; order sensitivity alone is not language sufficiency. | Keep exact composition and bounded earlier prefix keys/occurrences. Larger state must earn predictive capacity rather than multiply arbitrary table dimensions. |
| Paired H4 / icosian / E8 representation | `canonical_lexical_ingestion.rs:2577-2616`, `icosian_coordinate_at_index`: declared integral `(a_i,b_i)` basis, quaternion `a+b phi`, golden conjugate and phi-scaled companion, exact inverse witness; root membership tied to120-root table. | Source explicitly says no scalar-domain norm equality is inferred. This is a fixed golden-coupled coefficient representation, not two independent learned spaces, a general E8 lattice engine, or a proved isometry E8→R4. | Preserve storage/canonical inverse role. Any new semantic metric on this representation requires explicit task-linked operator and controls. |
| Chirality / cosine polarity / pole chart | `canonical_lexical_ingestion.rs:2650-2720`: sine sign, cosine sign, quarter-turn orientation at pole, fiber/torsion transport and inverse witness; no tangent evaluation at the singular chart. | Real typed chart bookkeeping. Initial lexical assignments use deterministic unit-id/prime-based fixtures (`spin_for_binding`), not learned semantic embedding. | Keep orientation and typed complementary charts, avoid magnitude-only flattening. Do not treat signs alone as a semantic axis. |
| Seven-row prime-route attention | `prime_route_geometric_attention.rs` reads3 direct rows,1 divisor row,3 adjacent spin rows; integer/table costs rank fixed bounded candidate support. | #958 `RETAIN_STORAGE_RECALL_ONLY`; stage3 mechanics only. Later #969/#973 add narrower positive paths; dated missing-CLI remarks in #958 must not be treated as current missing work. | Reuse deterministic admission/index structure if compatible with current user constraints; omitted candidates cannot be rescued by better ranking. |
| Windowed exact causal path | `prime_route_geometric_attention.rs:46-194`:1..4096 prefix keys, cumulative fold survives eviction, observation one group composition; selection O(candidates×window). | Bounded workspace and state mechanics, not constant-time attention or full-history recall. | Good substrate for page-resident memory/prefix keys with explicit work count and eviction semantics. |
| Recursive hierarchy / scalar readout | `recursive_geometric_attention.rs`: A1.0 summaries lose earlier order; A1R exact fold repairs order; candidate-relative Cayley scalar tied. A1P exact heatmap classes inspected. | #967 6/6 distinct ll/rr relative states but both candidate energies2/ties6/6. #970 enumerated14400 pairs,120 relative rows,45 signed heatmap classes,480 typed-null pairs; no-class-splitting ceiling10/12, transfer0/6, hard stop. | Keep exact state. Reject reusing the same single scalar as if representation richness implied separability. Multichannel candidate-relative readout needs learned task labels and fresh task transfer. |
| Paragraph/conversation stored-spin selectors | `paragraph_entity_spin_path_attention.rs`, `conversation_entity_spin_path_attention.rs`, matching docs #973. Exact descriptor grammar parses entity bindings and ranks preadmitted tied candidates through stored paths. | Positive synthetic two-history contracts, not arbitrary prose parser or general conversation model. Candidate words absent from prompt but construction prototypes bind them. | Reuse explicit binding/causal separation idea; current native learned record path has moved beyond these fixtures. Do not repeat as missing attention. |
| Bounded global exact spin | `bounded_global_exact_spin_attention.rs`, corresponding #973record. Exact global fold and class fanout; initial commuting relation failed, later noncommuting repair retained in history. | Exact-class sharing was repeated references to one address, not broad semantic equivalence across distinct tokens. | Keep noncommuting/global relative-state lesson; no hashed or coarse class substitution for exact state. |
| Exact SpiralCore v63 | `spiralcore_operator.rs`: oriented Fano octonion multiplication, separate left/right Cl(0,6) generators,15 bivectors,64-state finite composition group, inverse table/Killing form and six-prime J(6,2) chart. | `CHART_TRANSPORT_STATUS=NOT_ESTABLISHED`, `OPERATOR_SEMANTIC_STATUS=OPTIONAL_CONTROL_PENDING`.8D signed operator chart does not identify itself with R4/H4/E8. | Strong finite operator library for composition once a learned task operator and explicit typed bridge are defined. Runtime use can be finite composition/permutation lookup rather than8×8 contraction. Do not add it as a decorative scoring feature. |
| Cayley-Dickson named prototype | Entire `cayley_dickson.rs` inspected:16 coordinates from a multiplicative token hash; cyclic neighboring-coordinate products for named Lie/Jordan operations; integer dot-like centralizer score; identity operator contains zero vectors. | Names do not establish full Cayley-Dickson algebra, Clifford relations, a centralizer theorem or a semantic metric. Distinct from audited SpiralCore. | Preserve as historical prototype; unsuitable foundation without replacement by exact algebra/task qualification. |
| Dense transported R4 calculus oracle | `direct_causal_geometric_attention.rs`, `helm_d_r4_attention.rs`: tangent projections, quaternion-derived frame connections, transported Q/K/V/O, stable softmax, analytic derivatives and finite-difference/gauge controls. | Offline dense reference. V3 H4 projection/connection/optimizer negative; V4 construction covariance passed but H4/alternative/plain all13/24 held-out, current-only12/24; later HELM-D-R4 ordinary attention parity passed. Gauge reparameterization does not remove matrix products or prove geometric superiority. | Reuse correctness of typed frame transport, gradient equations for offline learning, and parity controls. Do not carry dense softmax/linear maps into serving. |

## Original angular and hyperbolic routing tracks

`research/ai-research/ai-router/router-research/docs/research/MATH_REVIEW_H4_GEOMETRY_20260305.md` and `MATH_REVIEW_DYNAMIC_H4_STATE_20260306.md` distinguish static shell/sector laws from dynamic state. Their H4 notation generally means continuous hyperbolic **H^4**, not current finite H4 roots. They explored paired complex phase4d, Hopf latitude/fiber, Poincare origin exp/log charts, phi/log shell laws, shell-dependent angular allocation, alpha-core regularization, tangent flow and product H^4×H^4. The dynamic pilot explicitly lacked arbitrary-point logarithmic maps/general parallel transport and used a past-flow origin-ball surrogate. This is useful design history, not a finished manifold-native serving implementation.

INC0144 retained a two-seed semantic-proxy discrimination result: fixed Hopf pmax mean.0874 original versus.0638 column-permuted; kmeans.0722 versus.0698. Those numbers measure a chosen PPMI-SVD subspace and occupancy/discrimination, not language capability or physical efficiency. The earlier prose overstates what a fixed-vs-adaptive comparison proves.

Actual `_inc0171_analysis.py` has a two-layer transformer, ordinary multihead attention, dense ExpertFFNs and dense output head. Baseline softmax is consumed only by argmax, so its gate weights have no shown downstream differentiable loss. Hopf/permuted arms dispatch one dense expert per token. Retained PPL152.26 baseline,164.54 Hopf,164.41 relabeled Hopf; occupancy entropy gives45.96 effective buckets of64, but does not establish1.39× saved work. September5 source-interpretation correction appended to INC0171 already preserves these distinctions. Under current user constraints this is historical comparison/learning evidence, not a product candidate.

## Prime combinatorics and spin history

`research/ai-research/prime_research_summary.md` defines a real modular admissibility dynamical system: wheel W, phase j, n(j)=5+6j modW, admissible prime tuple A tested by gcd(n+a,W)=1 for every a. Adding a prime extends the wheel by a local residue stencil and affine lift. Base/fiber/scale `(b,phi,r)` is an exact CRT-style chart; transport is an odometer. This is prime-tuple admissibility, not a primality oracle or a language transition law.

The exact finite-horizon spin object is a length-H future admissibility word. `run_spin_h_c2_bridge.py:55` constructs it from the cyclic full admissibility array. Its deterministic keyed mean predictor targets retained two-complex-coordinate quotient data. Best H8 spin within-scale relative error.99869 and cross-scale1.00298 is weak; grouped-packet baseline.98655/1.0 likewise weak. This provides no completed bridge from exact combinatorics to learned language.

Do not blindly inherit the summary assertion that a finite-horizon spin class always has a deterministic successor: `research/ai-research/spin_operator_diagnostics_summary.csv` reports deterministic-row fraction.83333 at H8 and.87234 at H21, with positive transition entropy. A finite future prefix can agree while its next unseen symbol branches. The transferable design question is what causal extra state makes the transition sufficient. A full orbit state has a deterministic successor; its quotient needs to be verified.

The extended/core spin lines are real source prototypes, not just names. `geometry_native_spinH_core_v6.py` implements a coupled-family holonomy residue and direct sigma laws; v4 replaced diagnostic profile tables as the parent sigma carrier with direct transported current/fiber/radial modes and a regressive phase. Docs record45 primary states,38315 reached parent states, zero parent collisions and bounded recursive consistency1.0. `spinH_extended_v2` added a twist/holonomy bit that removed504 branching states. They explicitly say full exact spin_H is **not** present; predictive words, depth and lawful operator surface remain bounded. The symbol kappa here is a holonomy bit, not the content CID called kappa elsewhere. Do not conflate them.

This suggests a useful research donor: compile state refinement around witnessed branching instead of adding scalar scores. For language, all runtime state must be computed from observed text/committed memory. The prime experiment's precomputed future word cannot be used as causal language state.

## Riemann-hypothesis work and vector-calculus connection

There are at least three distinct historical lines:

1. `research/riemann-lean`:23 hand-written Lean files plus extensive A1–A4/O1–O5/K1/tau/majorant/proof-ledger Python scripts. Core `PrimeRiemannBridge.lean` uses rational surrogate contracts; `RH_Equivalent_Implication` at87 is literally an alias for its `EndpointClass`, not classical RH. Later mathlib/formal files reach actual RH-shaped statements but retain nontrivial provider hypotheses. `PrimeRiemannBridgeFinalTargetEquivalence.lean` proves equivalences between RH and existence of strict-tail/oscillation providers; it is not a construction of such a provider. `PrimeRiemannBridgeSpinningTopFrontier.lean:934-967` declares required `theorem_term` provider fields and conditional provider-to-provider instances. README identifies the unresolved non-circular candidate-shape theorem term. No new Lean build was run.
2. `research/prime-analysis/research/topological rh.md`: a manuscript claiming a topological RH proof via invariant tangent regularization and phase torque. The manuscript's boundary-zero-torque step is an additional unestablished premise, not derived by changing charts. Its displayed isolated-pair torque is also nonzero at delta=0 whenever t differs from t0, so the stated cancellation criterion does not distinguish off-line from on-line zeros. Preserve as an attempted argument and a source of phase/branch-cut questions; do not use its QED as a mathematical foundation or runtime correctness guarantee.
3. Imported Washburn `external/riemann` material beneath formal-math-lean: far-field Pick/ARB certificate manuscripts, energy/Carleson near-field and conditional cutoff tracks. README makes a strong all-height half-plane claim; the inspected near-field notes explicitly retain a height-dependent logarithmic budget obstruction and conditional closure. Reading shipped certificate assertions is not an independent verification of their analytic infinite-tail bridge. `TAO_REVIEW_NOTES.md` says “Reading as Terence Tao would”; it is not a review authored by Tao. These are imported claims/research artifacts, not project-certified RH evidence.

`spinning_top_r4_bridge.py` is an actual vector/phase calculus prototype. It defines F(u)=sum exp(i gamma u)/(1/2+i gamma), its derivative F'(u), and R4 point `(ReF,ImF,ReF',ImF')`; reports precession/nutation/wobble and centroid separation of prime/composite samples. `prime_geometry_loop.py` implements modular complex embeddings, spectral powers, geometry diagnostics and permutation controls. Those are finite numerical investigations, with centroid evaluation using the same points to define centroids in the inspected helper, not learned language or an all-height theorem.

The architectural zeta basis is nevertheless usable now. ADR0003 explicitly permits RH as coordinate-design assumption and says product correctness must not depend on proving it. A finite manifest of supplied gamma constants, fixed log-phase evaluation and exact compiled transition tables are well-defined computational objects independently of whether the historical RH programme closes. Prime combinatorics and zeta phases motivate a structured basis; learning and causal task evidence must supply the semantic bridge.

## Vector calculus and triangulations: honest coverage

Literal searches for vector calculus, triangulation, discrete exterior calculus, Hodge, gradient/divergence/curl across native core/router, repository docs and named research source families did not reveal a complete native R4 field-calculus/triangulation engine. Relevant implemented pieces are frame transport/projection/analytic-gradient oracles, complex phase derivatives, signed angular charts and finite group state. Dense tangent vector actions remain offline mechanisms.

The imports agent independently found the pinned UOR-Framework constraint-nerve Betti routine in `foundation/src/enforcement.rs:7836` (registry variant7861): at most8 constraints/sites, enumerated edges/triangles and modular boundary rank. That is actual bounded constraint topology, not semantic R4 triangulation. Its `kernel/op.rs` derivative names and `kernel/convergence.rs` Hopf/R/C/H/O vocabulary need to be distinguished from an executable general vector field. The imports agent owns pinned-source provenance.

Remaining coverage: this was a bounded source-linked family scan, not line-by-line verification of thousands of duplicated research files, every PDF, all Lean external dependencies or every historical generated CSV. Repeated `router-research` and formal-math-lean copies share lineage; they are not independent replications. Further PDF-only vector-calculus or triangulation work can be indexed without assuming it is executable. No tree-softmax mechanism was conclusively identified by exact phrase search in this subtask; main agent owns the broader softmax/engine inventory.

## Source set actually read or inspected

Authority: current-state keyword/state sections, project-track mechanisms/sequence, AGENTS carried in task, geometric_intelligence_programme opening/current architecture and historical boundary, ADR0003 first150lines.

Rust: prime_route_attention first210lines and selected actual algebra/zeta/factor/Hopf definitions; prime_route_geometric_attention first220lines; canonical_lexical_ingestion wire/profile/state sections900–1050,1330–1450,2577–2720; entire cayley_dickson; spiralcore_operator first180lines and finite composition/validation API; zeta_projection construction and project_state; zeta_zeros fixed declaration; core lib Hopf functions295–370; router geometry first190lines; direct_causal_geometric_attention module contract and public mechanisms; helm_d_r4_attention opening; recursive_geometric_attention module/report interfaces.

Receipts/docs: #958 qualification entirety; Hopf306 and zeta246 entirety; paragraph/conversation/global973 opening contracts; V4 result/claim section via targeted search; angular INC0144, INC0171 with September correction; March H4 geometry/dynamic-state reviews; spin-first reset, phase-fiber vs spin, spinH corev4, extendedv2, component diagnosis; prime summary and spin diagnostic CSV/notes.

Historical code/math: `_inc0171_analysis.py` actual gate/expert/attention definitions inspected via structural search; `geometry_native_spinH_core_v6.py` function/update/API inspection; `run_spin_h_c2_bridge.py` construction/fit definitions; spinning_top_r4_bridge first170lines; prime_geometry_loop function inventory; riemann-lean README/formal README; core Lean first115lines; full FinalTargetEquivalence; provider section SpinningTopFrontier; rh_endpoint_gap_audit first180lines; topological rh manuscript; deepermath7 opening; complex tangent manuscript opening; imported riemann README and near-field/TAO-read-as notes selected sections.

# Supplement: trigonometry, S3/S2/S1, fibers and vector bundles

Owner steering after the first report explicitly invites combining useful pieces and regards expert gates as a possible later conditional option, rather than a permanent categorical prohibition. The main recommendation remains deterministic geometric memory/operator selection first; the report's earlier exclusion language records the prior clarification and is not a permanent ban on an explicitly evaluated later design.

## Exact source APIs and their mathematical roles

`prime_route_attention.rs:467–617` contains real normalization and Hopf APIs:

- `UnitS3Q30::from_r4([a,b,c,d])` validates finite nonzero input, normalizes by Euclidean radius, rounds to Q1.30 and retains sign. It rejects the zero direction. This loses radius unless radius is stored separately.
- `UnitS3Q30::hopf()` at533 computes, for `z1=a+ib`, `z2=c+id`,
  `h(q)=(2(ac+bd), 2(bc-ad), a²+b²-c²-d²)`.
  This is `h(z1,z2)=(2 Re(z1 conj(z2)),2 Im(z1 conj(z2)),|z1|²-|z2|²)`.
- `UnitS2Q30::from_r3` normalizes and quantizes that observation; `raw/to_r3` expose the result.
- `UnitS3Q30::rotate_common_fiber(phase)` at543 applies the common S1 action `(z1,z2) -> exp(i phase)(z1,z2)` with actual sine/cosine calls, then normalizes/quantizes. The Hopf base is invariant mathematically; quantized equality is tested on the declared fixture, not promised for arbitrary floating phase/input combinations.
- `SpinTorsionState::new` at627 stores signed S3, its derived-and-checked Hopf observation, explicit fiber phase and explicit torsion phase. The supplied fiber field is not reconstructed from q or checked as a unique q-fiber coordinate; retaining q itself prevents loss. Do not claim this struct implements an inverse from arbitrary `(S2,fiber)` to S3.

`crates/uor-r4-core/tests/prime_route_attention_958.rs:415–453` checks a fixed-zeta phase delta, manifest identity, q vs -q distinction, equal Hopf observation of antipodes, common quarter-turn invariance, unit S2 norm and rejection of zero R4. These are existing tests inspected, not rerun in this review.

This is correctly pictured as an S1 fiber over an S2 observation of S3, not a reversible dimensional chain `S3 -> S2 -> S1`. The global Hopf bundle is not the global product S2×S1. Locally, a chart plus a fiber coordinate reconstructs a point; chart transitions matter. The source already protects the key engineering principle by keeping full signed S3 rather than treating the base coordinate as complete route identity.

## The original angular chart already exposes two useful circle coordinates

`crates/uor-r4-core/src/lib.rs:297–345` and historical `_inc0171_analysis.py:148–193` implement the same family:

`rho1=|z1|`, `rho2=|z2|`, `chi=asin(rho2/sqrt(rho1²+rho2²))`,
`theta1=atan2(b,a)`, `theta2=atan2(d,c)`,
`delta=wrap(theta1-theta2)`, `alpha=wrap((theta1+theta2)/2)`.

For unit q, the Hopf base is equivalently
`X=sin(2chi)cos(delta)`, `Y=sin(2chi)sin(delta)`, `Z=cos(2chi)`.
Thus delta is a local S1 azimuthal observation of S2: away from the poles it can be recovered as `atan2(Y,X)`. At `X=Y=0` this azimuth is undefined; reducing S2 to this S1 drops latitude. No dedicated universal `UnitS2Q30 -> UnitS1` API was found. The existing code computes delta from the original complex pair instead.

Alpha carries a common-phase/fiber chart coordinate and changes under the common complex rotation; it is not obtainable from the S2 base. Averaging separately principal-valued theta1/theta2 introduces chart/branch conventions. It must not be treated as a global continuous fiber coordinate. The source's phase wrap does not by itself solve branch-cut topology or the zero-complex-component cases.

`hopf_phase_transport_components_scalar` implements the local chart law
`transported_alpha=wrap(alpha + (lambda/2)*cos(2chi)*delta)`.
`assign_sector_hopf_transport_scalar` then quantizes `chi_u=sin²chi`, delta and transported alpha, with an explicitly bounded triplet-bin product. This is a usable deterministic geometric address recipe. It does not prove that the chosen connection coefficient is a globally induced Levi-Civita connection or that those bins are semantically optimal.

A practical combination is to let a Hopf base address a page/neighborhood, retain fiber/orientation for within-page transport or comparison, and retain exact object/occurrence identity to resolve collisions. That is a proposed combination of existing pieces; current sources do not establish a deployed semantic page hierarchy from this recipe.

## Trig sign, magnitude, singularity and table boundaries

`native_geometric/anchors.rs:24–125` compiles exact root coefficients, paired/Galois companions, squared projection magnitude, signed orientation and equality classes. Its comment `sin=q0, cos=q1` names the existing projected heatmap convention. A general H4 root's q0²+q1² need not equal1, so these projected values are not automatically sine and cosine of a normalized single circle angle. The code correctly retains projection radius and a null-projection flag. Full root radius is constant; changing projected radius is not a different full-root radial shell.

`canonical_lexical_ingestion.rs:2650–2720` uses a typed complementary chart at cosine=0, keeps sine/cosine signs and quarter-turn orientation, transports fiber/torsion and checks an inverse witness. It avoids directly evaluating singular tangent. This can supply the chart discipline for combining trig mechanisms; keep null, pole and sign cases explicit rather than inventing a numeric answer to an undefined direction.

Current `native_geometric/training.rs:108–165` already performs a concrete offline-to-table step: token primes map to8 fixed-zeta phase channels quantized to16-bit turns; exact H4 products/inverses and orientation anchors are compiled into the model. Inference can update phase by modular integer addition and act on finite H4 indices. By contrast, `PhaseQ29::wrapping_add` in the older substrate converts to floating radians and back. Integer-shaped artifact fields alone do not imply the implementation is a pure integer kernel.

## Vector bundles and transport: what is real now

`helm_d_r4_attention.rs:459–706` supplies:

- `R4RegisteredSpinFrame::encode_model_block`: `local=Fᵀ model`.
- `decode_local_block`: `model=F local`.
- `canonical_registered_h4_spin_frames`: enumerates all120 registered frame matrices, while explicitly avoiding a claim all are reachable under a particular lexical mapping.
- `R4SpinFrameAtlas::transport_local_block`: refuses future source positions and computes `local_i=(F_iᵀ F_j)local_j`.
- `decode_query_block`: returns the transported aggregate to the model basis.

This is an implemented, causal, discrete frame-atlas/gauge transport pattern. Coherent source/query frames preserve the ordinary vector computation up to its measured floating tolerance; source-frame-permutation controls detect inconsistency. `H4FrameConnection` in `direct_causal_geometric_attention.rs:237–267` exposes apply, inverse_apply and ordered composition, currently via4×4 matrix actions. The module explicitly disclaims a fitted or uniquely induced Levi-Civita connection.

These pieces can support vector-bundle-inspired architecture without importing dense serving: records have base/page address, local typed state and frame identity; a transport operator moves compatible local states into a query frame; chart transitions preserve the represented value. However, the current atlas is a finite discrete frame mechanism. It does not demonstrate a general smooth tangent-bundle engine, curvature-induced parallel transport on arbitrary learned manifolds, or a global trivialization of the Hopf bundle. The S1 principal fiber and a tangent/associated vector fiber are different types and should have different APIs even if both are informed by the same geometric source.

## How to combine them without serving matrix products

A concrete buildable adaptation is:

1. Preserve exact prime/occurrence addresses and typed value/role records in pages.
2. Retain signed finite H4 state plus declared phase/orientation channels for each record/context.
3. Learn state-to-operation behavior offline; reuse the correct frame/gradient calculations as training oracles where useful.
4. Compile **finite group-valued state transport** into relative-index/inverse/composition lookup; compile only the selected bounded local state/operator domain into an action table. Generic arbitrary real vectors cannot be exactly replaced by a finite table without restricting or quantizing their domain.
5. Use the Hopf/chart view to choose a deterministic page/neighborhood or diagnostic projection, while retaining information discarded by that view in the record.
6. Apply a typed copy, relation, numeric, record-update or selected nonlinear operator; commit the resulting value; render through the existing model lifecycle.

Some arbitrary common S1 rotations leave the finite H4 root set. Therefore a table architecture must explicitly choose a closed finite action set, keep phase as a separate finite channel, or define and measure quantization. Do not claim the existing120-root group is closed under every fiber angle.

For an exact 120-state H4 value acted upon by120 relative group elements, the group table already has14400 entries (about28.8KB at u16), illustrating why this specific representation is cheap. That does not project the cost of a cross-product table over arbitrary vector values, multiple phase channels or all language contexts. Page design should avoid a Cartesian-product explosion and record the actual bytes and work before execution.

A direct first test for an added geometric transport piece should hold addressed records and candidate support fixed, vary only the relevant orientation/frame/phase binding, and require correct changed generated behavior. This can be integrated into the current native record/operator path without starting another standalone routing framework or mathematical proof campaign.

Supplement source coverage: complete selected UnitS3/UnitS2/SpinTorsion implementations467–665; exact test415–453; angular Python148–193; native anchors1–125; geometry compilation108–165; direct frame237–267; HELM-D registered and causal atlas459–518,621–706. No execution was performed.
