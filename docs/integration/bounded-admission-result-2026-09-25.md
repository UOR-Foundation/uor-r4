# D8 bounded admission: completed paired continuation

**Both orthant64 candidates fail the frozen source-retention gate. The implementation and full paired comparison are complete; neither candidate is promoted.**

## Decision and project position

Likelihood retention, combined read/copy effect, noncollapse and mechanical/export checks pass in both arms. First-noun losses are11/8 against the immediate parents and11/9 against rung1, beyond the maximum2. Both indexed-complement utility discriminators pass: the index supplies17 additional complete answers over recent32 and lowers late-position NLL by0.110267/0.105152. That utility cannot override the retention failure. Restoring full access on the same final weights recovers10/8 complete answers, localizing much of the loss to the admission policy rather than an inability of those weights to use the information. Recent64 recovers the same27/25 complete answers with lower NLL than either tested indexed proposer. Exact-cache yields23/18, so it is not selected as the next bounded reference. These are same-weights interventions on one paired seed, not independently trained policy superiority.

The accepted learned-code parents remain preserved. This result changes the admission implementation and supplies its measured outcome; it does not reopen rounding, replace the geometric model goal, or qualify integer serving. D8 rung3 remains open until bounded behavior and transport satisfy their declared requirements.

## Fixed comparison

Each arm receives256 updates /1,048,576 fitted target visits at B16/T256, from actual retained packed codes. The same bounded rule runs during learning and incremental generation. A new declared AdamW learns all21 parameter tensors with fixed grids and full-strength STE. Original alpha/optimizer parents and the inherited quantizer clock remain intact.

The two sealed segments are166+90 updates, consuming sampler8860..9115 exactly once. Actual processed-data hashes match across both arms in each segment. All final policies use the same final weights and whole-prefix intervention. The full-access parent receives no equal extra training, so this is not an equal-compute superiority comparison. No intermediate checkpoint is selected.

| Comparison-tail NLL, nats/target | Quaternion | Householder pair |
|---|---:|---:|
| Retained learned-code parent | 2.114226169 | 2.110879800 |
| Frozen bounded entry | 2.170559173 | 2.171663554 |
| **Final bounded orthant64** | 2.129595124 | 2.124890468 |
| Whole-prefix NoRead | 2.570332863 | 2.559377317 |
| Same final weights, full | 2.080040745 | 2.069662849 |
| Same final weights, recent64 | 2.119169266 | 2.108628005 |
| Same final weights, recent32 | 2.217738285 | 2.208398679 |
| Same final weights, exact_cache64 | 2.146801669 | 2.136447411 |

| Frozen retention criterion | Quaternion | Householder pair |
|---|---|---|
| NLL ≤ own hard parent+0.05 and below count/cache | PASS | PASS |
| NoRead effect ≥ half own parent | PASS | PASS |
| Five noncollapsed continuations | PASS | PASS |
| At most2 first-noun losses against each anchor | **FAIL** | **FAIL** |
| Mechanical/source/export checks | PASS | PASS |

The bounded-minus-parent gaps are +0.015368955/+0.014010668. Combined read/copy NoRead penalties are +0.440737739/+0.434486849 nats. These are policy effects over the whole causal prefix, not isolated final-read effects.

For cumulative numerical transparency, bounded minus the historical step8,348 continuous reference is +0.039076625/+0.060487247. The prospective admission gate uses the retained hard parent; it does not rewrite the earlier discretization comparison or erase cumulative cost.

## Actual answers and the access intervention

| Complete source answers, out of32 | Quaternion | Householder pair |
|---|---:|---:|
| Retained learned-code parent | 28 | 24 |
| Frozen bounded entry | 21 | 17 |
| Final bounded orthant64 | 17 | 17 |
| Final weights, full | 27 | 25 |
| Final weights, recent64 | 27 | 25 |
| Final weights, recent32 | 0 | 0 |
| Final weights, exact_cache64 | 23 | 18 |
| Final weights, NoRead | 0 | 0 |

### quaternion: retained-row changes

- Against immediate_parent: first-noun losses 11, gains 0; complete-answer losses 11, gains 0.
- Against rung1: first-noun losses 11, gains 1; complete-answer losses 12, gains 1.

### householder_pair: retained-row changes

- Against immediate_parent: first-noun losses 8, gains 0; complete-answer losses 9, gains 2.
- Against rung1: first-noun losses 9, gains 0; complete-answer losses 9, gains 3.

Every loss and gain is retained separately in the machine-readable result, including older intermediate artifacts. Gains never cancel losses. All ten primary free continuations avoid constant/short-token-cycle collapse, but exhibit entity and role drift, implausible events, repetition and malformed words. For example, the quaternion bike story changes to a dog and ice cream, and the ordinary cake story puts a bandage on an oven. Noncollapse does not establish coherent prose or useful conversation.

## Indexed complement utility and cost

| Same final weights: orthant64 vs recent32 | Quaternion | Householder pair |
|---|---:|---:|
| Complete-answer count gain | 17 | 17 |
| NLL gain at comparison positions64..255 | 0.110267455 | 0.105152335 |
| Orthant64 NLL minus recent64 | 0.010425858 | 0.016262463 |
| Frozen utility discriminator | PASS | PASS |

Utility and retention are separate. A positive complement discriminator cannot override source-retention failure. These are conditional interventions on one trained policy; none of the control policies received matched independent learning. No general index or geometry-superiority conclusion follows.

| Actual orthant64 audit, evaluation plus generated panels | Quaternion | Householder pair |
|---|---:|---:|
| Read queries | 252497 | 252501 |
| Causally available events | 31964685 | 31966660 |
| Unique selected candidate scores | 10425283 | 10494283 |
| Maximum candidates in one read | 64 | 64 |
| Selected events older than32 | 2880243 | 2949115 |
| Posting overwrites | 278103 | 275035 |
| Maximum logical index bytes/lane | 19160 | 19160 |

The proposer unions recent32 with at most32 postings from two16-bucket sign tables, depth8, two probes each. It performs no full-history search. Exact occurrence IDs remain separate; scoring uses all64 read coordinates, actual occurrence ages and NoRead. Selected keys/values/copy tokens are fetched directly in incremental sessions. The offline evaluator/trainer still uses dense histories and mass layouts. Deduplication, insertion, posting eviction, cache links and all control-index storage are reported in the full audit. Logical index bytes are not measured process RAM.

Hard admission is stopped-gradient. Selected Q/K/V/state/output paths retain language gradients, but there is no direct estimator for cross-bucket membership. Eight sign coordinates of dense read projections are not designated R4 state lanes. Untied query/key maps and magnitude-sensitive scores have no recall guarantee under this partial-sign partition. An overwritten posting is not proof that an exact event is absent.

## Recommended next step

**Superseded after owner review, September 25:** the original recommendation
below to train recent64 is withdrawn before execution. The owner challenged the
context/access reduction and required a system to prevent experiment loops.
[D9](DECISIONS.md#d9--prevent-experiment-loops-and-preserve-the-context-contract)
and the [active contract](current-state.md#active-execution-contract) now retain
the accepted learned-code parents, matched 256-token contexts and full causal
access while implementing quantized transport and the integer execution bridge.
Pruning is a deferred optimization. The completed measurements, original gates,
negative decision and machine-readable evidence above are unchanged.

<details>
<summary>Original recommendation preserved as history; do not execute</summary>

**Use recent64 as the chosen bounded reference for the next matched learning step.** It is already implemented in the shared Rust graph and, on the final weights here, retains27/25 complete answers with lower NLL than either tested indexed policy. Its same-weights first-noun losses are2/0 against the immediate parent and1/1 against rung1. These diagnostic controls were not trained under recent64 and are not silently promoted to accepted artifacts.

Complete one paired recent64 continuation from the accepted learned-code parents, retaining the same fixed grids, optimizer recipe, B16/T256, 256 updates and sampler8860..9115 used here. That supplies a matched policy comparison without another implementation/build programme or new hash/width/scale sweep. Freeze its admission-policy retention gates prospectively, preserve all current negatives, and inspect loaded answers before promotion. The existing binary and checkpoints are reusable; no broad re-audit or test expansion is the next task.

After a bounded baseline retains behavior, continue the same model through observable quantized R4 transport and measured state/output drift, then integer/add/subtract/shift/lookup execution. Keep the ordinary transport arm and account for dense parameter access separately. A recency baseline does not solve retrieval beyond64 or qualify D8 rung3 in full.

The longer-range geometric routing research should target **candidate recall with explicit learning credit**, using train-only full-access parent evidence and a declared address/membership objective. Conditional gradients through already-selected events cannot directly correct every missed event. Preserve exact occurrence/word identity and capacity/eviction semantics; compare learned older access with cost-matched ordinary access on genuine dependencies beyond64 before claiming a geometric benefit. The new objective is a design dependency, not an implemented or measured remedy. This result supplies no reason to remove the geometric recurrent core.

</details>

## Validation, resources and evidence

The release build succeeded on its first attempt. All62 focused optimized Rust tests passed in4.79seconds, including shared batch/session causality, selected gradients, packed policy reload and constant index-query bounds. Actual packed full-context reload probability delta is exactly zero for both final candidates, with seeded token/distribution parity. Principal arithmetic checks every249,856 target record per evaluated mode, identities, partition means, normalization reports, complete source outputs and all preserved continuation segments. Five historical queue acknowledgements execute no tests.

RDC ran two initial concurrent DeepSeek source/mathematics and implementation sessions, a third integrated-source review during learning, and both Rust fits on this same M1. No Kimi model or external training hardware was used. Two sequence workers/model and nested backend threads1 use the previously measured hardware arrangement. Paired training supervision totals24.67minutes; release build and focused-check compilation total7.55minutes. The original physical-reserve guard checkpointed at166; the prospective512MiB storage extension preceded the exact resume. No artifact, owner material or worktree was deleted. Whole-cycle unique-wall accounting includes preparation, all reviews, build, stops, comparisons, analysis and delivery.

Executed Rust source: `78fde5711ea01f80783eb9911e01197a61406d1a`. Executable SHA256: `3d6bea5024b59119a8d74d79986f926501f113d132af0b58fea1a2b1ef8d2669`. Final candidates: `fit-{quaternion,householder_pair}-bounded-2/packed-model` under `/Users/casey.allard/uor-r4-investigations/bounded-admission-20260925`. Both are retained at the decision scope above.

- [Prospective plan and review adjudication](bounded-admission-plan-2026-09-25.md)
- [Full arithmetic, changes and sealed bindings](../evidence/bounded-admission-result-2026-09-25.json)
- [Principal decision](../evidence/bounded-admission-decision-2026-09-25.json)
- [Every generated continuation and source response](bounded-admission-outputs-2026-09-25.md)
- [Immutable recipes and exact resume](../evidence/bounded-admission-recipes-2026-09-25.json)
- [DeepSeek review receipts and principal adjudication](../evidence/bounded-admission-reviews-2026-09-25.json)
- [Diagnostic policy retained-row changes](../evidence/bounded-admission-control-retention-2026-09-25.json)
- [Executed local checks](../evidence/bounded-admission-checks-2026-09-25.json)
- [Complete-cycle resource closeout](../evidence/bounded-admission-closeout-2026-09-25.json)

One paired seed and previously exposed development remain explicit; no final holdout was opened. Final computation still uses F32 dense parameter maps and allocates. Integer execution, quantized group closure, unbounded context, D5 parameter sparsity, geometric advantage, Hamiltonian dynamics, useful general language/coding and energy savings remain unqualified.
