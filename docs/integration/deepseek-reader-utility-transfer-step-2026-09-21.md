# DeepSeek next task: make learned geometric reads useful on language

You are a research contributor to the UOR-R4 Geometric Language Model. Read this prompt, the [principal PR #1325/#1326 review](relational-learning-review-2026-09-21.md), and the [mechanism synthesis](geometric-attention-mechanism-synthesis-2026-09-20.md) completely. Recover any additional source, project history, knowledge-store records and primary research needed. You have discretion to improve the bounded design; explain material deviations and their evidence before final evaluation. Deliver one constructive utility-transfer result, integrating the necessary instrument repairs, rather than another audit-only run.

## Recover authority and preserve the useful result

Refresh origin/main; verify the principal review and parent merges PR #1325 (`f80fdbdf`) and #1326 (`44788008`). Read AGENTS.md, README, canonical plan/current state, D0-b/D1/D2/D3 and execution policy. Inspect `learner/relational.rs`, `bin/competitive-reader.rs`, their shared causal path, new design/result/receipt and review evidence. Refresh #820/#973/#1139/#962/#963/#964, attached project items, artifact identities, worktrees, shared ledger and storage. Use an isolated full worktree and preserve the owner's checkout, all unique research, and every sealed parent/superseded attempt.

The delivered parent is `.uor-models/realtext-prior-2026-09-20/relational-learning-4`. Its manifest has 30 listed files, all verified; the original 27-file prose count was incomplete. Its learned H4 reader improves present-query correct emitted next tokens **35→83/119**, correct payload reads **49→103/119**, and CE **9.08857→4.0912 bits/query** on the retained construction. Preserve this progress. H4 beats exact recurrence by −0.524968 bits/candidate position; categorical+contextual beats H4+contextual by −0.124149 bits. The new controller's marginal effect is unresolved, −0.012093 bits with interval crossing zero. Neither that nor the categorical result erases earlier scoped positives or establishes general language capability.

The old “fresh” population is a byte-identical replay, not a new post-selection draw. It is useful regression data. The eight reader-held-out text documents were separated correctly in this run, but are now inspected development data. Their H4-contextual regression **+0.274243 bits/token** against local is the important unresolved behavior. No general prose, chat, reasoning or energy claim follows from the synthetic result.

Offline Rust preparation/training may use float and matrix products. Serving targets bounded <=4-bit add/subtract/shift/table arithmetic under D0-b, without numerical float/transcendentals/multiplier instructions in the declared kernel. Geometric routing and shared typed operators remain preferred. No dense Transformer backbone, runtime teacher/provider or authored serving grammar. Report unfinished surrounding operations honestly; a conforming selector is not a conforming whole model.

## One constructive hypothesis

**Learn whether, and how strongly, an already selected source should influence the local predictor using a direct hard finite-action objective and sufficient causal observations.** Freeze the improved H4 and categorical source maps/rankers initially, along with E+S-query-only local emission, the 128-token ring, current admission rule and <=24 candidates. This reuses the useful source-learning result and isolates influence. Start from the ungated top source, so NoRead does not hide it during training.

The current 16 buckets see a handful of exact-context/recency flags and a one-bit source margin, omit the selected payload's local score, and fit a soft expectation before hard deployment. Only 214 of 3,183 candidate-bearing fit positions are natural text; phase selection is synthetic. Changing two sweep counts on that same population is not the selected experiment.

A recommended implementation is a small table selecting `NoRead` or one of the existing strengths `{0.0625,1,8}` nats. Its index can combine a few quantized **causal integer** observations: selected-payload gap from local top score, source score/margin or agreement, and a useful exact-context/recency observation. Choose the compact features and bins from fit/tune evidence, and show which conflicting decisions the added observation can separate. Do not concatenate every feature, or smuggle target correctness, coverage, synthetic family IDs, source-domain labels, query-type metadata or future input into serving.

Train each finite action from its actual per-example loss:

`Δ(a)=log(1+p_payload·(exp(a)−1))−a·I[payload==target]`, with NoRead cost zero.

Average those example costs under the declared fit weights; do not apply the nonlinear formula at average probability. A table's empirical minimizer can be computed directly per bucket, with tune-selected shrinkage/minimum support and an explicit sparse/unseen fallback. This is an empirical finite-policy solution, not a Bayes-optimal or globally optimal language policy. Offline probabilities are allowed; an integer local logit gap at serving is an observation, not a probability estimate. Evaluate the actual rounded/clipped integer action if it differs from the reference equation. No hidden probability mixture requiring floating serving is adopted.

The direct action output may be a tiny opcode lookup rather than artificial scores pushed through softmax training. Version the artifact/interface when needed. Use the same causal observations, table capacity, fitting opportunity and fit population for geometric and categorical source arms. Retain frozen current global/contextual policies and local NoRead as references, reusing predictions/positions where valid rather than constructing many redundant arms.

You may choose another small hard-objective policy with a clearer reason, or one causal descriptor correction if development observations prove an alias the proposed features cannot resolve. A material change must have a named capability purpose and bounded cost. Do not expand H4 into a pair table or S7 merely because the comparator is better.

## Put language transfer and absence into the actual fit

Use document-disjoint reader fit/tune/final text, with path and content hashes and pinned tokenizer/window identities. Reserve final documents before design selection. The frozen local model's historical corpus exposure is a separate disclosure; this is reader transfer, not necessarily externally unseen language. Existing local corpora are sufficient for a bounded first test. Use several useful windows/positions per document if affordable instead of drawing conclusions from eight opening windows alone. Select the size from a measured preparation/score-time probe, with a declared minimum information target and machine budget; do not download a large corpus by default.

Give natural text meaningful declared weight relative to synthetic queries, rather than allowing intermediate fixture repetition to determine utility. Weighting may use fit strata; serving may not use those labels. Include genuine absent-source queries and varied ordinary text where copying is unnecessary. Judge intentionally unavailable answers by inappropriate reads and loss relative to local; do not require guessing an unknowable absent value. Keep regression on the parent's 119 present queries and use a **new frozen construction seed** and held-out combinations for final assessment. New arbitrary partner rules without observable training/prefix evidence are not a fair generalization demand. Static token codes still do not qualify contextual syntactic roles.

Use one fixed **full prediction stream**, including empty-pool positions with local predictions, for every pool/policy comparison. The parent scored 1,813 candidate-bearing positions and omitted 608 others. Saved-data reconstruction gives 712 eligible-ring targets and 646 admitted in the scored subset; all 119 present final queries are admitted. Across the full 2,421 positions there are 929 eligible-ring targets and 283 available-but-excluded cases. The known finite-action admission opportunity on the scored subset is 0.271329 nats/position; opportunity on omitted rows is not yet measured. Thus admission might help some predictions, but 646/1813 did not prove it was the dominant loss.

As a cheap development diagnostic, measure full causal-ring eligibility, admitted support and the best finite-action losses on the same full stream, especially natural text. Keep target-using oracles out of inference. If this reveals a substantial recoverable exclusion loss that blocks the utility task, you may select **one** causal bounded admission complement at the same ring/candidate budget, matched across both source arms. State the diagnostic and selection before final evaluation, include its scanning/indexing cost, and compare on unchanged denominators. This is conditional freedom, not a mandatory admission campaign.

## Evaluate what the policy actually does

Serialize per-document loss sums/counts or outcomes so document intervals can be independently reproduced; use document and bits/token labels. Report all-position hard CE, source availability/admission, ungated source correctness, read/NoRead/strength, selected payload and emitted token. Separate present/absent final queries, intermediate fixture rows, real-text copy-available and unavailable rows, and candidate-count strata. Duplicate correct payloads count as correct predictions; exact intended-source identity is a separate provenance metric.

For current factorized source ordering, preserve the repaired ranking/gate/dose identity and verify it for both new arms. If source and action become coupled, derive the right decomposition. For minimum finite-action losses including NoRead, nonnegative admission regret is `Lpool − Lring >= 0` (admitted-pool loss minus full-ring loss); an empty pool alone does not diagnose admission. A target absent from causal memory requires the local generator or a derived operator, not better copying.

Declare practical margins, task units, fit weights, final seeds/documents and success categories prospectively. Main question: does the new policy reduce document-held-out harm or produce net benefit against local while preserving useful source-dependent relational behavior and improving absence handling? Compare old/new policies paired on the same documents/sequences; use cluster intervals. Check correct emitted results as well as CE. Keep matched categorical behavior visible; geometry-specific benefit requires that comparison, while a useful geometric component can be retained without winning it.

An all-NoRead policy is a reference, not attention progress. Distinguish **harm containment** (text falls back to local), **useful transfer** (held-out text actually benefits from correct causal reads), and **general language capability** (still a larger unmet goal). Preserve near misses with their exact symptoms rather than relabel a frozen failure. If no meaningful language read survives, report that limitation instead of claiming success because text CE returned to baseline.

## Correct the causal controls and artifact boundary inside this run

Exercise independently reloaded arms through the same target-free `read_step`/`predict_next` path for evaluation, generation and timing. Preserve real occurrence/version/payload references and the ring-before-current-input convention. Add focused tests for direct hard-action fitting, empty/unseen buckets, integer ties/overflow, serialization and changed observation indexing; retain relevant existing causal/group tests without rerunning every historical campaign.

For selected-source intervention, compare enabled original, enabled changed, disabled original and disabled changed. Disabled-before/after must be invariant when its local inputs are unchanged; enabled/disabled on the same prefix measures reader contribution. The parent incorrectly compared disabled-after with enabled-before. Record source-reference identity as well as payload response. Prefer a replacement absent elsewhere, keep the query fixed, inspect changes to neighboring records/admission, and count support loss/NoRead rather than dropping them. Do not assert a fixed candidate pool without checking it. Include unrelated-source and changed-query controls appropriate to the claimed behavior, future-prefix invariance and reset/stale-reference checks.

Bind the actual parent artifacts, complete fit/tune/final source/window identities and observations, group-table bytes, feature/bin definitions, action semantics, parameters and configuration through a versioned artifact or a manifest digest the loader verifies. The parent report seal associates its sidecar and artifacts, but embedded digests are still abbreviated and `ExactGroupTable::build()` is not a group hash. Do not retrofit or reseal old evidence. For this small direct fitter, a completed fit-stage artifact may be the natural checkpoint; exercise actual load/continue behavior if continuation is claimed. In-memory snapshots are not disk resume.

Validate the report schema and expected fields before the expensive final pass. Reuse retained fitted artifacts for reporting/control-only corrections in a new claimed derived root when possible; do not repeat the entire identical fit merely to add an omitted timing vector. Claim every output root immediately after argument validation, before model load; seal/verify complete contents. Preserve failed and superseded attempts honestly.

Capture actual constructed answers and finite natural-text generations from reloaded artifacts, including loops. No repetition suppression, output overrides or hidden teacher responses should masquerade as a model improvement. The old local predictor is weak; a safe copy policy alone cannot supply all prose competence.

## Costs, delivery and the next architectural branch

At principal review the ledger is **189266749/194900000 ms**, **5633251 ms (~93.89 min)** remaining; refresh it. Project complete preparation/build/fit/control/evaluation/retry/checkpoint cost, worker counts, RAM and peak/new/retained storage before running. Necessary local extensions are already authorized when reason, increment and new limit are recorded before use; do not ask again for that class of increase, silently reset charges or incur external spend. Reviewed free space is **42.47 GB** against **36.77 GB reserve**; preserve the 128 MiB margin and unique material. Reuse valid shared builds.

Measure paired interleaved same-stream optimized local/reader timings with enough active candidates to resolve useful differences, plus bounded selector-only accounting if helpful. Include local-score feature scans, admission work, tables and resident/serialized bytes. A token-pair cache is optional; if added, include its bytes/construction and quality-equivalent baseline. Avoid making a cache campaign the next task. Physical energy is UNAVAILABLE without measurement; scoped integer policy arithmetic is not whole-path compliance.

Deliver through a protected PR, stage named files, verify actual merge and tree/content identity, update current-state, roadmap/README claims, result/receipt/ledger and owning issues/project items. Keep broad issues open with References. Add a compact revision-pinned knowledge import and verify it; do not duplicate the entire archive. Report the chosen mechanism, fit population, actual results, errors, resource charge and one evidence-supported successor.

This run should settle a useful architectural question, not begin endless gate tuning. If one bounded objective/observation repair transfers, advance to learned structural role/scope persistence and then dependent read/update/emit composition using the existing typed mechanisms. If it cannot separate useful from harmful reads, expose the missing causal distinction and make that a contextual-representation learning task; if target support is genuinely missing, distinguish retention, admission and generation. Signed H4/Spin relations remain active; PaTH/GRAPE suggest future content-dependent finite transport, and Hopf fiber/S7/E8/harmonic state remain available for demonstrated aliasing or retention needs. Keep exact identity separate from learned semantic compatibility. The goal is one useful transformerless geometric language model, not a permanently growing collection of authored retrieval tests.
