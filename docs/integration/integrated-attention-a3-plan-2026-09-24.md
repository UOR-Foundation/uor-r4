# A3: learn the integer routing decision inside the integrated language model

September 24, 2026. References #973 and #820. Continues D7 Milestone A and open PR #1387.

**Executed:** the [A3 result](integrated-attention-a3-result-2026-09-24.md) reports actual integer admission progress (1→17/24), failed selection/complete language, retained regressions and the next coupled candidate-or-NoRead decision. The text below remains the prospective protocol; it is not a second current-state authority.

## Authority and observed failure

The owner directed continuation of the [A2 recommendation](integrated-attention-a2-result-2026-09-24.md#next-make-hard-admission-learn-in-the-integrated-model). Refreshed main remains the protected A1/programme delivery `b020f34a`; A2 head `d235fb60` is on open PR #1387. Extend that PR from its clean isolated worktree and preserve the A2 source-bound artifacts.

The corrected A2 pair admits the right source on 1/24 new first decisions and completes 0/12 read-enabled correction answers per arm. Its dedicated context-token bank has zero net exported integer changes. The soft overlap objective and hard exact-code/page admission differ. Correct forced evidence improves new decision-token probabilities offline, while actual output remains degenerate. Neither parameter mutation nor forced-source scoring can substitute for actual generated behavior.

## Implemented objective to pursue

Reuse the same causal contextual representation, V4096 tokenizer, pinned natural/grounded data, finite algebra, relative-energy ranker and language head. Change the coarse address learning mechanism. Serving code and the A2 envelope remain unchanged; A3 denotes the training/report version.

Offline collection runs the actual session only through each raw prompt. Retain its query input, the delayed positive source's key input, causal committed keys in insertion order, and explicitly annotated wrong-entity source anchors. Two worlds that change only the source value form a pair. A training-only entity group ties the same subject across fit styles. Group labels and positive indices never enter inference. Same-entity versions are not false negative coarse targets; unrelated raw tokens impose page pressure without being assigned semantic-negative labels.

For each group, choose a latent coarse code minimizing integer hinge violations across its queries and positive keys. The code must beat alternatives by a declared margin. Wrong-entity anchors must prefer a different code. Add pressure for actual bounded posting occupancy/admission using the chronological causal keys. A globally collapsed bucket cannot reliably retain a stream exceeding its 64-posting capacity.

Optimize selected content-row coefficients with deterministic bounded signed-four-bit coordinate edits. Accept an edit only when the specified hard balanced objective strictly decreases; report exactly whether the acceptance objective is global or minibatch and how interference is replayed. Keep the shared role and State rows separate. Direct integer edits avoid treating sub-quantization master movement as hard routing progress. They do not establish a global optimum; failed local descent can be an optimizer or feature limitation.

Then freeze **coarse** address learning during the same eight-epoch joint language fit. The fine contextual lanes, relative energy, State, head and utility gates remain trainable. This staged continuation preserves A2's evidence-language learning and prevents its old soft coarse objective from silently replacing the integer fit. Bind that policy, route data and all optimizer limits to the artifact/report. Export/reload before judging behavior.

## Research basis and limits

[Supervised Discrete Hashing (CVPR 2015)](https://www.cv-foundation.org/openaccess/content_cvpr_2015/papers/Shen_Supervised_Discrete_Hashing_2015_CVPR_paper.pdf) explicitly distinguishes direct discrete optimization from relaxation followed by quantization and uses cyclic coordinate descent for its own binary-code objective. That motivates examining the served discrete decision. Its classifier objective and analytical subproblem solution are different from this 120-way shared-row router; its guarantees and retrieval results are not transferred to A3.

The serving geometry remains finite 2I composition with an empirical relative-energy score, compared with C120 under matched data and limits. No physical Hamiltonian flow, geometric predictive advantage, unbounded exact history or electrical-energy result follows from this optimizer. Prime/zeta/ordered-n-let and typed span mechanisms remain programme resources with separately specified roles; this change specifically learns the coarse finite contextual address.

## Execution, accounting and decision

The [prospective full-cycle budget](../evidence/integrated-attention-a3-budget-2026-09-24.json) records the standing-authorized local time extension before builds/runs, preserving all earlier cumulative charges. It includes design, implementation, compilation, route collection/optimization, joint fitting, controls, replay and delivery. Route fitting is bounded by 300 seconds, 5,000,000 candidate evaluations and 4,096 accepted edits per arm. Initial joint fitting uses eight epochs (1,124,776 tokens per arm), at most 900 seconds per arm, with 6,000,000 total observed training tokens across this cycle. Context maxima correctly include natural fit 1,024, natural development 768 and authored episodes 512. One Cargo process, two CPU jobs, 8 GiB RSS target and the physical reserve plus 128 MiB stop margin remain in force.

Collect before/after hard scores, coefficient/code changes, occupancy and actual admission on fit and the existing development set. Use identical route and joint-fit logical limits for C120/2I; if wall time ends one early, preserve the partial result and do not call the workloads matched. Retain the route checkpoint before joint learning. Count all routing work separately from language-token exposure. No new final held-out draw is introduced.

The development decision sequence is:

1. Does integer learning change content rows and improve actual fit admission without an overloaded-page substitution?
2. Does paired new-world development admission improve over A2's 1/24, and do source value edits preserve useful access?
3. Does the same loaded model rank/select the correct source and improve actual complete uncopied answers against NoRead/source edits, while preserving prior rows rather than hiding regressions in averages?

All three remain visible in the same run. Only the last reaches the language endpoint; intermediate progress stays a scoped mechanism result. If hard learning operates but generalization fails, use the existing ordered span/role and exact byte/BPE contracts for the next representation change. If sources are retrieved but output still fails, address that measured evidence-to-language interface. Do not run a rate/epoch sweep or a broad test campaign in place of those decisions.

The existing long-session snapshot posting-rebuild limitation remains recorded. These short episodes do not qualify saturation plus ring eviction or training resume. That repair belongs before persistent-session qualification, not as a hidden assumption in A3 results.
