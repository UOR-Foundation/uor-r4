# DeepSeek next milestone: learn ordinary continuation and grounded copying in one native path

Read the [principal V3 review](transferable-lexical-principal-review-2026-09-23.md) and its [independent checks](../evidence/transferable-lexical-principal-checks-2026-09-23.json) completely. They qualify PR #1352's [submitted result](transferable-lexical-result-2026-09-22.md). The [canonical plan](project-track.md) owns sequencing and [current state](current-state.md) owns the active result. Verify the actual protected PR merge and tree before developing against it; a document's presence is not delivery proof.

## Objective and your research discretion

Complete the still-open **transferable lexical-learning milestone**: one native learned state/output path should generate ordinary text and grounded responses with optional exact owned copying. Train vocabulary, continuation and Stop on a source-separated real-text population as well as grounded supervision. Use the same actual emitted-token feedback across generated and copied tokens. Exercise loaded generation, causal source changes, held-out source/relation cases, mixed memory/computation sessions, and complete-path M1 costs. The result may be a substantive negative if the representation cannot reach useful quality; preserve it honestly rather than switching to another supplied response template.

You are a substantive architect and investigator. Choose the representation, corpus scale, tokenizer treatment, optimizer, objective, training schedule, donor adaptation and geometric candidate based on measured reduced diagnostics. Challenge this brief when source or a falsifying experiment supports a better approach, and explain the alternative. There is no arbitrary short wall-time gate or retry quota. Work remains within the standing local resource authorization: record cumulative projection and any necessary extension **before** use, preserve physical reserve and unique artifacts, and use no paid compute.

## Recover the full relevant context

Refresh origin/main, PR #1352 merge state and contents, owner and worktree status, live resource ledger and free space. Read AGENTS.md, README, DECISIONS D0-b/D1/D2, execution policy, canonical plan, current state, model direction, project map, this brief and the principal review. Refresh owning issues #820/#973/#1139/#962/#963/#964 and retrieve the new review/brief and their predecessors from the knowledge store. The principal brief lists exact knowledge IDs after ingestion. Read source where the responsibility actually resides: `learner/state_lexical.rs`, `scoped_memory.rs`, `observed_text_session.rs`, `lexical_realization.rs`, `contextual_emission.rs`, `prior_learning.rs`, `query_read.rs`, and the relevant `competitive-reader.rs` runner paths. Use the [architecture audit](architecture-2026-09/README.md) for the broader mechanism map without repeating its entire survey.

Retain the history: #1346 scoped versioned memory; #1347 signed consumed computation with a competent finite tie and sign-loss witness; #1348 combined observation/intent support; #1349 four-word lexical interface with false tense; #1350 correction/review; #1351 seven-slot ternary recurrence and misfitted finite reference; #1352 exact typed V3 observation and emitted-token feedback on seven authored worlds. Source-separated prose learning, truthful independent relation language, and equal-information geometric advantage have not been delivered by those results.

## Falsify the current shortcuts before a large fit

V3's 31/31 contains 23 fitting cases and eight non-computed held-out cases in familiar forms. All computed/post-copy cases are fitting. The 791/791 teacher steps are in-sample export parity. The submitted 4/4 pair changes operation and typed `key_changed` along with payload, so it does not isolate content use. The post-copy `now` is selected by the learned policy but is the authored answer for every changed-key case. An operation that follows a different key is not a committed temporal change. The retained finite reference sees less information and its 20/31 is not evidence of a neural/geometric advantage. E/S component probes are not a matched scoped language comparison. Ordinary-text fit was NOT_RUN.

Start with two cheap, decisive preflights integrated into the actual training design. First, construct independent causal meaning cases that separate changed key/same value, same key/different value, no commit and real committed correction. Author accepted meaning from typed source state once; do not reuse the learner's renderer as its evaluator. Second, make the target post-copy vocabulary decision depend on which source token or relation was emitted while holding the request, derived/history/commit facts, copy length and nonsemantic prefixes fixed. Compare the loaded model with actual feedback, with token identity blinded, and with source disabled. Avoid widening a channel that loses the required distinction.

## Learn language through one shared interface

The serving responsibility can be written as:

```text
c        = pinned request meaning + selected evidence/result + exact provenance
h_0      = learned initialization(c)
a_t      = learned Generate(token) / Copy(owned occurrence) / Stop decision
x_t      = token actually emitted by a_t
h_(t+1)  = learned update(h_t, representation(x_t), event, c)
```

Exact ownership, version authority and copy cursor stay outside the compressed state. Ordinary prose has no required owned span; grounded responses may have one. Do not hardwire Copy for a benchmark population and then claim to have learned the choice. Permit a shared generation vocabulary large enough to express the selected text population, with a declared OOV/copy strategy; eight authored words are not a general language head. For non-injective low-bit token embeddings, expose collisions and test whether they affect the held-out decision. Keep legal action support, duplicate Generate/Copy surface mass, EOS and normalized probabilities coherent if reporting CE or bits/token. A hard-policy result is acceptable when labeled as such; teacher agreement is not a generation result.

Use Rust for data preparation, fitting, artifact construction and inference. Offline float, gradients and matrix products are permitted. Final serving should use bounded integer/low-bit routing, updates and table reads under D0-b. Report a dense additive map as a mathematical dense map with its traffic and cost; retain geometric routing and shared typed operators as the preferred research direction. Do not add a transformer or runtime teacher/provider response source. Compare ordered signed H4 transport or a shared geometric residual where it can improve useful prediction, role separation or measured cost; allow a competent ordinary low-bit baseline with identical observations and supervision. E/S is a loaded full-vocabulary local reference and S a query-read component; assess donor-conditioned vocabulary emission only through an actual compatible interface, or measure why adaptation loses to another source of lexical learning. S7/Hopf/E8/harmonic channels remain candidate tools for a witnessed interference/capacity need.

Select feasible ordinary text by independent source/document groups. Pin corpus identities, licenses where relevant, tokenizer, deduplication, reset boundaries, all attempted seeds/configurations and selection rule. Keep the authored grounded world separate from ordinary prose data, then train a shared path or a justified phased curriculum that carries learning into the same served artifact. Measure next-token quality against at least a competent local count/recurrent baseline and E where comparable, using one tokenizer and denominator; evaluate loaded greedy/sample continuations and document-level variation. A local small corpus does not establish general conversation, but a model that only reproduces supplied response grammar has not completed this milestone. Require a held-out source/relation composition case after design selection and disclose every earlier exposure.

Run a cheap loaded mixed session early: observe, correct, compute, ask current and historical questions, copy an owned span, continue vocabulary after it, save during each phase and restore in another process. A pinned answer retains its causal view while a new answer sees the correction. Compare full sessions, remaining effects, bytes and terminals. Bind the **same learned realizer** to prior memory/intent/computation controls; historical `LegacyWords` rows with no realizer bytes are not same-candidate preservation. Keep independently held-out final evaluation separate from development and freeze its practical screen after choosing the design. A mild quality shortfall may justify a prospective threshold revision; false temporal claims, source leaks and broken ownership remain defects.

Report complete path latency, initialization, peak RAM, table/parameter traffic, allocations and measured energy when available on the named M1 machine. Energy stays UNAVAILABLE if not measured. Use an exclusive report root, sealed manifest and source/executable/data/artifact hashes. Preserve failed roots and original artifacts. Compile and exercise touched Rust, run focused causal/serialization/arithmetic checks and actual complete generated answers; queue compatibility acknowledgements do not count as tests.

Finish the result as a reviewable protected PR. Update the canonical plan, current state, relevant README/navigation/evidence claims, all owning issue bodies/status, knowledge-store records and verified retrieval. Leave issues open until their full acceptance is met. Provide the learned capability, held-out quality, failures, any unique geometric gain against equal-information controls, complete costs and one evidence-supported architectural next step. You have discretion to do the necessary successive work rather than stopping after a small preflight or a compiled helper.
