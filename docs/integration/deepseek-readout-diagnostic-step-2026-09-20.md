# DeepSeek execution prompt — recover the prefix negative and isolate readout learning

> Executed by PR #1306 with the corrections in [the principal review](readout-result-review-2026-09-20.md). Preserve this historical protocol; its next action is superseded by the [head-projection prompt](deepseek-head-projection-step-2026-09-20.md). In particular, the executed smoothed teacher differs from the selection prescribed here.

Work on **UOR-R4 Geometric Language Model**, `UOR-Foundation/uor-r4`. Refresh origin/main beyond audited base `88e7122122717333aed7ee79049854337cf60ead` (PR #1304) and read this document plus `docs/integration/prefix-pilot-review-2026-09-20.md` completely. They supersede the prior run's demand to approach the count reference before any further geometric state research.

**Deliver one bounded causal experiment: freeze the existing integer context features and compare empirical versus smoothed conditional-target training of the existing ternary output head. First recover and replay the saved negative prefix candidates without retraining them, correcting the specific artifact/generation/statistic gaps.** Execute the readout experiment and make one next decision from its outcome. Do not stop after writing a module or designing a study.

## 1. Authority, preservation and complete projection

Read AGENTS.md, DECISIONS.md D0-b/D1/D2, execution policy, canonical plan and current state. Use Rust for preparation, fitting, artifacts and inference. Offline float/gradients/matmul are permitted; serving follows D0-b: <=4-bit weights, add/subtract/shift/table numerical operations, no multiplier instruction or floating-point served numerical computation. Geometry remains primary; no transformer backbone or runtime provider. Frozen R4G1 is separately scoped.

Use an isolated full `codex/…` worktree from refreshed origin/main. Preserve owner checkout and all unique research and sealed attempts. Refresh #973/#820/#963/#964 and assign active work appropriately. Root `/Users/casey.allard/uor-r4/.uor-models/realtext-prior-2026-09-20/` contains:

- `attempt-1/prior_realtext.cpl2`, SHA256 `cd5a3aa1b804ccc3582a4a25090e25c862489e0bc4e5bb86c1d1f363a7338a00`, 454,788 bytes.
- `attempt-1/prior_realtext.ckpt`, SHA256 `1c3dfedb2248ec76c21f320b7ecec585c7a522a2d47e35ed5dc9eaca2454bff2`, 19,192,104 bytes. Use for recovered exposure and verified output-master initialization, not as a qualified continuation.
- `prefix-pilot-2`, 12 sealed members, `result.json` SHA256 `d228bd57eaf4d5e61202d2bdac8a95daf90cb6328cf5d2d36c8562a51f38ea0a`; three `arm*.ckpt` CPXS files and their `.cpl2.json` descriptors. Preserve `prefix-pilot-1` and probe too.
- `derived-corrections-1`, corrected fit indices; `derived-corrections.json` SHA256 `134d6b5b36090c1e9a1a6f16677733a85edb8ff37c16c20c729928b9ffb11c67`.
- `eval-replay-2`, corrected frozen baseline, SHA256 of result `6db96a1c7f9cccefe13b9c07a610bbb02559ece6e3762a7c36f35ba9dcc6f661`; original parity roots; pinned `inputs/docs` at corpus commit `e9c04e80`.
- Derived tokenizer SHA256 `a7ac75b68aa997fe7cc338d25d843157f2dc9d4265fb2f958ee779bb04828d6f`.

Refresh owner `.uor-models/native-joint-learning-2026-09-04/model-time.json`; expected **159438565 /159800000 ms**, remaining **361435 ms**. Preserve the recorded 5,400,000 ms previous charge, while labeling its wall-time basis unverified/conservative: a projection is not a measured duration. No automatic refund or duplicate charge.

Before any build/model/preparation, record full projection and storage inventory. Proposed **5,400,000 ms**: 900 s artifact/generation/statistic recovery; 1,500 s implementation/build/focused checks; 300 s actual-path probe/data preparation; 1,500 s two hard fits and evaluation; 600 s conditional floating diagnostic; 600 s evidence/delivery/checkpoint/stop reserve. Record the necessary already-authorized **+5,400,000 ms** limit increment BEFORE use: proposed limit **165200000 ms**, headroom **5761435 ms**. Refresh actual values and revise before overruns. One worker, <=4 Cargo jobs, <=8 GiB RSS, <=512 MiB new retained/temporary data plus <=1 GiB incremental reused build output, preserve 128 MiB storage margin. No deletion, paid/external compute or new corpus.

Use monotonic elapsed intervals and persist execution timing; count retries/builds/tests/evaluation once and avoid overlapping charges. Do not charge the entire reservation merely because it was allocated. A token/session stop should leave a resumable checkpoint and exact next command, not an invented completed fit.

## 2. Recover the negative without fitting it again

The prefix numerical fit is valid at its recorded scope: primary 7.575603687 bits/target versus parent 7.558429403, fixed-action 7.723495502 and local-tail 7.576439157. Do not restart any of the three prefix fits. Source `learner/prefix_state.rs` and `bin/prefix-state-pilot.rs` has the following material gaps.

**Real hard artifact.** Add a small versioned prefix artifact with packed reader/output coefficients and fixed shifts, nibble-packed actions, exact product/inverse table and identity, canonical signed root-order/mapping, palette, arm semantics, H64/F10/dimensions and parent/tokenizer identities. Either bind and verify the external parent by full file digest or embed it; name the choice. A JSON file containing hashes without weights is a descriptor, not an executable artifact. Keep new format distinct from existing CPL2 and legacy CPXS.

Reconstruct final hard parameters OFFLINE from each pinned CPXS. Validate lengths/finiteness/ranges/configuration and legacy parent binding. The old `parent_sha256` field actually contains SHA256 of the hex parent-hash string; validate its historical convention, then store the raw parent-file digest in the new format. Require recovered packed reader/output hashes and the reconstructed RAW one-byte-per-token action-array hash to equal the old descriptors before evaluating anything. The old action hash is over 4,096 unpacked bytes, not nibble-packed bytes. Then pack actions for the new artifact, verify unpacking reproduces the original array exactly, and record the new packed hash separately.

Use the existing exact canonical construction (`native_geometric/training.rs::geometry`, `validate_h4_binary_icosahedral_closure`) to precompile table data. Existing `group_table()` uses lazy floating classification and may use a different root order. Recreate the historical enumeration in `learner/embedding.rs` with exact twice-scaled Z[phi] coordinate tuples (axis coordinates ±2, half roots ±1, golden entries ±phi and ±(phi−1)); match those tuples against the sorted exact roots in `canonical_lexical_ingestion.rs`. This gives an explicit bijection preserving q versus -q without nearest-float matching. Verify all 14,400 products. Prefer storing the exact table in historical state order to preserve reader rows/action IDs; otherwise transport reader-state rows, palette ELEMENTS, identity and table consistently and verify score parity. Token action codes are palette-slot IDs 0..7 and remain unchanged when only root order changes. If the tables differ non-isomorphically, stop this conversion and report the first counterexample; do not silently change the learned function or rerun training. The new loader/served state path must read the bound table without floating nearest-root construction. Test malformed dimensions/codes/shifts/table/index/parent identity and overflow at the touched boundary.

**Correct generation and common state selection.** Centralize arm-aware state selection used by training/evaluation/generation. The local-tail arm currently calls `state_older` during generation even though its CE uses `state_tail`; fix both library and runner. Scan ALL 4096 output probabilities for count-reference argmax with lowest-ID tie handling, not only `{unigram mode, prev, cur}`. Include a small fixture where the true maximizer is outside that set. Preserve the same six prompts and decoding; record full token/byte outputs and distinguish pair repetition from sufficient-state cycles. Parent pair is sufficient; older-prefix arm needs the bounded ring and length/reset state. Handle the local-tail availability mask/initialization explicitly; using full ring repetition is a safe sufficient certificate even when not minimal.

Reload each new hard artifact in an evaluation-only path. Check integer parity against recovered checkpoint hard cores on aligned contexts and the complete saved development panel. Reproduce the source-consistent CE/control results to declared numerical tolerance, then save per-occurrence and per-document parent/arm/permuted/reversed/disabled losses, targets, states and fixed donor IDs. Preserve the 288-window panel and existing exact `(prev,cur,older-prefix length)` donor map; do not redraw the intervention or retune to change its outcome. Recompute all intervals from those saved rows. Correct generation from reloaded artifacts. One recovery replay suffices; no duplicate fit to change labels or reporting code.

**Scope labels accurately.** Probe was two batch updates /16 windows. Reported action norm was pre-Jacobian palette credit; name it and record actual logit-gradient norm in future fits. Final cache was 21,251 entries /348,176,384 bytes, not the initial 9,891 /162 MB. Standardize gains as `CE_reference - CE_candidate`; old reference `gain_vs_parent` has reversed sign. A failed main screen can contain a passed component (the primary beat fixed actions). Distinguish changed final action IDs from the total number of transitions during learning.

Do not assert old CPXS complete mid-pass continuation: it lacks schedule/cursor/config binding and does not reject data-identity mismatches. Recovery is final-weight reconstruction. Do not build a new prefix continuation system for this rejected candidate. Preserve the original files and record corrections in a new exclusively claimed/sealed root.

## 3. The readout hypothesis and frozen boundary

The hypothesis is: **smoothed distributional supervision improves the existing output head beyond equally dosed empirical-CE refitting on identical frozen integer features.** It is not an established diagnosis, a new serving architecture or a requirement to solve all local prediction before geometric context work.

Use the original parent CPL2, V4096/d128/F10. Freeze `e_old`, `e_new`, their row shifts, bounded ReLU/normalization and bias codes/scale. Compute `h(prev,cur)` through the shared parent feature primitive; change only `w_o`. No prefix residual, new bias, new hidden layer, width change, tokenizer/corpus change, decoder intervention or lookup-table serving fallback.

Reuse the common prior forward/loss/quantizer and small reusable count/evaluation helpers. Add a focused output-only update path and one thin runner; avoid copying another complete training/evaluation stack. Assert frozen-field equality after every checkpoint/export.

Initialize both hard arms with the SAME preserved CPCK output masters, after verifying checkpoint hash, shape/finiteness, and that `TernaryLinear::quantize` reproduces every parent output code and shift. Reset moments, update age and schedule: this is a NEW controlled output-only fit. Freeze features from CPL2, never from unverified checkpoint re-quantization. Step-zero integer logits/artifact must equal parent exactly.

If verified output-master recovery is impossible, use the predeclared fallback `master=1.25*code*2^shift` for nonzero coefficients and zero for zero codes, shared by both arms, with exact export parity. Label the changed surrogate initialization. Avoid exact `code*2^shift` reconstruction, which puts row maxima on the `floor(log2(amax))` scale boundary and, for positive shifts, can halve a row on an infinitesimal decrease (shift 0 is clamped). Record first-update and checkpoint row-shift/code changes separately. If step-zero parity fails, diagnose before fitting; do not proceed with a changed baseline.

Keep the existing adaptive dyadic ternary quantizer for both hard arms. Declare its effective-weight identity STE exactly: with effective master weight w and frozen quantizer choices, `dL/dw[v,j] = dL/dZ[v] * h[j]`. Do not multiply by the output row scale again. Export uses the original integer `PriorCore::trace`; training must call that same hard forward, not floating masters as predictions.

## 4. Matched targets, data and dose

Use exactly the recovered first 4,096 consumed windows from the legacy permutation, with corrected indices, 257,113 n−1 targets and the original reset/PAD policy. Save exact token/window identities and ordering. No new documents. Construct conditional counts `C2[(prev,cur),v]`, totals N_c and one-token counts C1 from THESE occurrences; the full-fit add-one unigram U remains the declared shared marginal used by the prior/reference family.

Arm E: `p_emp(v|c)=C2[c,v]/N_c` for every fit context. Arm S: existing normalized `family_p` interpolation

`q(v|prev,cur) = lambda2*p2(v|prev,cur) + (1-lambda2)*(lambda1*p1(v|cur)+(1-lambda1)*U(v))`,

where p1/p2 are their empirical conditional distributions and back off to U when unseen, matching the existing reference definition. Use the saved 4,096-reference lambdas if retained and unambiguously bound. Otherwise deterministically reconstruct its declared 0.0..0.9 grid using only the named <=76 first/last windows over all 38 tune documents, record the tie rule, and check the resulting reference CE against the old result. Do not choose teacher/mixing/temperature on development data. Temperature is 1, no additional hard/soft mixture coefficient or sweep.

BOTH arms train against complete conditional distributions on the same occurrence-weighted context schedule. The empirical arm has exactly the token-CE population objective because `sum_c N_c H(p_emp,p)= -sum_(c,v)C2[c,v]log2 p(v|c)`. This replaces token sampling by its conditional expectation in both arms; do not claim minibatch trajectories equal historical one-hot training. Never average unique contexts uniformly. Within-batch repeated contexts may be combined with their occurrence multiplicities and a checked weighted-loss/gradient identity.

Check full-vocabulary normalization/nonnegativity/finiteness, absent-context backoff and source provenance. Teacher counts come only from fit; smoothing selection comes only from tune. All V4096 teacher mass must participate: no top-k, argmax labels, dropped tail or per-target-only probability masquerading as a distribution. The shared full-fit marginal is extra information beyond the 4096 conditional-count pool and must stay visible in exposure claims.

One seed13, fresh Adam beta1=.9/beta2=.999/epsilon1e-8, weight_decay0, lr0.05, global gradient clip1.0 after actual-target averaging. **512 batch-eight updates per arm: ONE pass over the same 4096-window population**, with a saved seed13 Fisher–Yates order over those selected window IDs. Do not accidentally take the first 512 of 38987 again or train four passes. Evaluate/save steps0/128/256/512; step512 is the predeclared primary. No best-dev checkpoint, new seeds or LR sweep. Both arms have equal data/order/updates/init/optimizer and inference parameter budget. Report actual occurrence counts and compute.

For either target distribution t, `dZ[v]=(softmax(Z*2^-F)[v]-t[v])*2^-F/ln2`. Mean over scored occurrences ONCE. The frozen feature path receives no updates. Independently finite-difference the corresponding floating linear-head loss and test one-hot/histogram/weighted-batch identities, missing teacher mass detection, nonzero-score train/serve parity and gradient scale.

Before full fits, time actual output-only forward/backward/export. Probe states must be either the named preserved first updates or explicitly discarded and charged. Cache only exact integer features keyed by parent/config and `(prev,cur)`; do not allocate a giant context×vocabulary probability table. Assemble teacher rows batchwise from common unigram mass and sparse conditional counts. Fit arms sequentially, reuse buffers, and project complete remaining evaluation/checkpoint work.

## 5. Measurements and conditional floating relaxation

Freeze the existing 36-document/288-window development panel and all metrics before new fitting. Reuse parent and reference populations; old prefix primary stays a labeled rejected comparator, not a new baseline. Record:

- final hard-artifact true-label CE on fit, tune and dev, with micro/macro and saved per-document sums/counts;
- BOTH objective values on the fixed fit population for both arms, especially occurrence-weighted teacher KL/soft CE, not only raw online batch-loss averages;
- KL reduction from step0, learning curves, output-gradient norms, row-scale and ternary-code changes, decision flips and frozen-field equality;
- paired 2,000-draw document intervals on the same dev observations; these are nominal open-development comparisons on correlated repository documents;
- original parent, empirical count distribution on its fit domain, and 512/4096/full-fit smoothed references with their actual exposure and sign conventions.

Predeclare primary practical improvement as >=0.10 bits/target gain over the parent with paired lower bound>0. Attribute a benefit to smoothed-target training only if Arm S also beats Arm E with paired lower bound>0 on true-label dev CE. Report objective/teacher improvements even when this screen fails; better teacher imitation is not itself language improvement. No count-gap-matching threshold is a condition for returning to geometric research.

**Conditional diagnostic:** if Arm S fails that combined screen, and a material teacher-fit gap remains (final fit KL>0.10 bits/target), run at most ONE floating-output relaxation on the same frozen integer features and soft targets, same512 updates/schedule/optimizer. It is an offline comparator. Initialize floating effective weights from the parent's served codes/shifts to reproduce parent logits; state clearly that hard arms use verified CPCK masters behind the quantizer whereas this relaxation starts at the same predictive function. Do not change embeddings, bias or F. Persist its weights/checkpoint as diagnostic-only and never load it in product serving.

Compare hard versus float on the SAME soft objective and true-label populations. The fixed-feature softmax objective is convex in the float head, but a finite optimizer run is not a certified optimum or capacity lower bound. If float improves strongly, the relaxed head/optimization helps; this does not distinguish ternary representability from STE difficulties. If neither improves or curves are still progressing at the stop, report unresolved optimization/feature approximation, not impossibility. If the branch condition is false, leave it NOT_RUN. If timing projection does not fit, record the necessary standing-authorized extension before use or save an exact checkpoint; do not silently change dose.

## 6. Artifact, continuation, behavior and delivery

For the NEW readout diagnostic, bind parent/data/tokenizer/teacher/count/tune/panel identities, quantizer/configuration, initialization source/fallback, seed, complete order/cursor/pass, optimizer constants, masters/moments/age and source/compiler/binary identities. Keep a real resumable checkpoint, not only an exported core or hash. Test runner mid-pass continuation against uninterrupted updates including next selected batch and full state; wrong data/teacher/parent/config must be rejected, not adopted.

Write complete CPL2 candidates, reload them independently and evaluate/generate from the reload. Confirm frozen embeddings/bias/config and step-zero parent parity, score bounds and deterministic tie rule. Save 64-token greedy outputs for the same six prompts for parent and both hard heads; save corrected full-vocabulary count-reference outputs from recovery. All bytes/IDs/failures remain visible. No repetition penalties, samplers, blacklists or output overrides. Neither a loss gain nor changed generation qualifies broad prose/coding.

Claim each report directory exclusively after argument validation and before loading/preparation, check every required write, seal and verify expected member sets. Persist per-occurrence or sufficient per-document vectors for EVERY reported comparison/control so CIs can be reconstructed without another model run. Historical sealed roots remain unchanged. Report recovery, implementation fixes and actual new fitting separately.

Run fmt, touched-package offline checks and focused meaningful tests, actual recovery replay, the two trained hard exports and the conditional comparator only as specified. No blanket historical suite. Update claim wording and run its script. Measure timing/RAM/storage and scope instruction/allocation claims to tested functions; no invented energy estimates or queue-check-as-test claims.

Deliver named changes through a protected PR; verify actual merge and source/tree identity. Update README, current-state, canonical dependencies, model direction, project map, CONTINUE, evidence corrections, shared JSON/prose/storage receipts and #973/#820/#963/#964. Update project items only if they exist. Preserve broad acceptance and raw historical receipts.

The final answer must state what was recovered versus originally measured; the actual two hard-head outcomes and floating-branch status; whether soft targets help beyond empirical refitting; remaining count gaps without causal overstatement; raw generation outcomes; timings and actual cumulative charges; verified hashes/delivery; and ONE next action. A future local-conditioned geometric read, selective state update or representation change must be chosen from the observed bottleneck, not excluded until a count reference is matched. Do not claim geometric superiority, useful general language or complete-path energy savings from this experiment.
