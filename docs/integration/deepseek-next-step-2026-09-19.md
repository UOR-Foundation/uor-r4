# DeepSeek handoff — qualify the real-text experiment before scaling

You are implementing the next bounded research step for the **UOR-R4 Geometric Language Model** in `UOR-Foundation/uor-r4`. Read this entire prompt before acting. The parent review is `docs/integration/takeover-review-2026-09-19.md`. The source audited there was `127c0eaf`; fetch live main and incorporate the protected documentation reconciliation before editing. Do not check out an obsolete unmerged #1284 branch: #1284 and #1290 were merged.

## Goal and owner authority

Build a native Rust geometric language model for useful conversation/memory and coding/reasoning, ultimately frontier capability on consumer M1-class laptops with less wasted compute. Preserve prime/zeta/R4/H4/2I/icosian research and exact addressed memory. Geometric routing is the preferred direction. No general chat, reasoning, coding or energy advantage is currently qualified.

The owner explicitly reconfirmed on September 19 that all recorded DECISIONS are owner decisions. **D0-b is adopted:** offline Rust training may use floating point, gradients and matrix products; serving may use bounded <=4-bit/ternary linear maps implemented with additions/subtractions/shifts/table reads. No floating-point arithmetic or libm transcendentals in served computation, and no multiplier instruction in the declared numerical kernel. Additive contractions remain mathematical linear maps; label them accurately. Do not revive D0-a or an old blanket ban on additive linear maps. Do not import a transformer backbone or runtime provider. Existing frozen R4G1 contracts remain intact.

Read in order: AGENTS.md; docs/integration/DECISIONS.md; agent-execution-policy.json; project-track.md; the newest current-state entry; takeover-review-2026-09-19.md; EVIDENCE.md and the specific raw receipts. Older handoffs/design reviews retain historical findings, but their next steps and universal impossibility claims are superseded where corrected in the review.

## What exists and what the last run actually measured

There are three separate paths: historical retained exact-memory/dependent-language models; the TinyStories `.rgm` prose path; and the new `learner/geometric_attention.rs` core. Do not transfer qualification between them. The older 9,984-case driver does not test a new geometric-core artifact.

The new order-2 core addresses by `120*(previous_previous_token % 120)+(previous_token % 120)`, and stores a sum of learned successor values from the observed prefix. It reads this mutable bucket, normalizes by a power-of-two shift and applies a ternary readout. State resets between training sequences. Group composition exists separately; positional ordered-pair addressing does not itself exercise group multiplication, zeta or chirality.

Your last tool `geometric-realtext-ceiling.rs` reported source/prose static residue-order-2 scores of **5.0019/5.9546 bits/token**, versus token-order-2 **4.1573/4.8955** on two internal repository corpora. Preserve those receipts. They are finite fitted static backoff baselines, **not information-theoretic ceilings**. The recurrent model also sees prefix-dependent state; even with fixed information, an estimated held-out cross-entropy is not the Bayes optimum.

Two prompts `[0,1,2,0,1]` and `[0,1,3,0,1]` share a tail but write different values. Conversely, an unwritten query bucket has zero vector and default no-bias logits zero, so it costs **12 bits/token at V=4096**. The current whole-sequence mean loss is at least `p_empty*12`, where `p_empty` must be measured before the current target is written. A global count model contains training-corpus priors absent from that empty recurrent bucket.

## Your bounded objective

Deliver a trustworthy causal real-text experiment and an evidence-based go/no-go for a small fit. Do not start a 2,000-step run first. Implement the correctness repairs and coverage instrument below, then the measured timing probe, then a small pilot only if the earlier results make it informative. This is one implementation task; avoid a broad new archive audit or repeated full-suite campaigns.

1. **Recover and project.** Use an isolated full worktree from refreshed origin/main. Refresh #820, #973, #963, #964 and any new owning child issue, artifact/tokenizer identities, shared ledger and storage inventory. Assign only the issue actively worked. Preserve the original checkout, research, all negative artifacts and sealed data. Do not reset the budget at a session boundary.

2. **Qualify the learner's arithmetic and causality.** In `geometric_attention.rs`, investigate `tail_word`, `sequence_loss` and trainer `word_at`: `len+k-order` / `i+k+1-order` underflows on short prefixes. Define explicit padding/BOS behavior and check empty, one-token and order-2 prefixes with overflow checking enabled. Add deterministic same-tail/changed-prefix, no-write, future-target exclusion, document-reset and empty-bucket fixtures. Compare batch teacher-forced reads with an independent incremental observe/predict trace on tiny fixtures. Make any float-training versus integer-serving rounding differences explicit.

3. **Check the declared mean-loss STE independently.** `g=p*inv` already normalizes at audited line 976, while `gkernel += acc*inv` and `gwv += dscratch*inv` appear to normalize again at 1025/1041. Use an independent small analytic reference for the stated surrogate derivative, freezing quantization/normalization choices and checking output/value/kernel gradients at two lengths. Exercise both an order-2 exact read and an order-1 nonzero active kernel with repeated writes and nonzero read credit; a zero kernel gradient in order 2 is not a kernel-gradient test. Require nonzero reference gradients in each tested group and compare accumulation before optimizer/clipping. Fix the extra factor if confirmed. Do not finite-difference the discontinuous quantized forward and call that an STE check. Do not attribute all old negative results to this defect; measure any behavioral effect with a paired tiny run if needed.

4. **Repair the baseline instrument without erasing prior output.** Relabel it in output/docs as static backoff comparison; keep the binary name if changing it adds compatibility noise. CE and top-1 must use the same interpolated normalized distribution; lambda=0 must exactly yield the unigram distribution and argmax. Tune interpolation with target-probability CE only, then compute final argmax once per scored position; avoid a full-vocabulary argmax for every candidate interpolation setting. Resolve the claimed full-training refit versus actual fit-only behavior with one explicit protocol. Align the scored token/byte convention, including document boundaries, and verify probability normalization on tiny fixtures. More conditioning cannot increase population conditional entropy, but can worsen a finite estimator's test loss. Do not claim otherwise. Keep shuffled refit and add corpus/split/tokenizer/source hashes. Previously opened data are development data, never relabeled final holdout.

5. **Measure causal coverage before fitting.** Reuse the pinned 4,096-vocabulary derivation and source/prose document populations where recoverable; if source edits change collection, create and hash a new snapshot rather than silently reusing the old digest. Freeze document boundaries and train/development splits, scored positions and reset policy. For 64-token windows (and a small coverage-only comparison at 128/256 if cheap), report before each target: empty query routes, previously written routes, exact-context matches versus modulo collisions, and distinct successors. Token-only coverage establishes unwritten buckets; vector cancellation needs pinned value weights. Measure cancellation with a named initialized/artifact state or in the pilot, explicitly separating initialization-specific behavior from learned behavior. Report by position and document, with `p_empty*12` as a scoped bound for the current default path. Compare a declared causal prefix-count reference, a global train-only count baseline, no-write and matched changed-prefix controls. They use different information regimes; label them separately. Never write the current/future target before scoring it.

6. **Time complete training steps only after correctness passes.** Use V=4096, dv=128, order=2 and actual proposed batch/window settings. Include both table quantizations, touched-bucket resets, initial allocation, forward, backward, Adam, evaluation and checkpoint overhead. Report cold and warmed samples, token definition, step distribution and peak RSS. Trainer reset is touched-bucket, not whole-state on every sequence. The old 1,800-second and 3–10x figures are unmeasured; replace them with a projection from actual timing. Bound the probe by a pre-recorded wall/RAM/storage cap and interrupt safely at it.

7. **Use an explicit decision branch.** If the cold-route loss floor already exceeds the frozen development target, or useful reads are too rare to train the intended function, stop scaling this configuration. Report the missing learned-prior/context path and propose the smallest geometric intervention with matched controls; do not bolt on the global count model and relabel it geometric learning. If coverage and correctness admit useful learning, run only a projected small pilot, compare held-out next-token loss and actual integer-path generations with no-write/changed-prefix controls, then recommend the next dose or mechanism. Show complete raw continuations, including failures. A pilot pass is not a chat claim. If empty-route learning is the obstacle, prefer a learned prior or broader causal state over blind increases of dv/steps; learned remapping cannot inject 4,096 identities into 120 single roots.

## Acceptance and cost

Freeze the pilot metric, minimum practical gain, context/control comparisons and failure branch after the coverage measurement but before fitting; keep final held-out evaluation separate until design selection. Fresh development data for instrument checks are allowed and must remain labeled development data. Correctness fixtures and distribution/gradient checks must pass. The result may legitimately be “do not train this configuration”; the task succeeds by delivering a correct instrument and a supported next decision, not by forcing a positive model score.

At takeover the shared ledger in the owner's checkout was `146438565 / 154400000 ms`, leaving **7,961,435 ms (132.69 min)**. Refresh it. The prose ledger contains historical discrepancies: its JSON is the recorded operational balance, not a certified sum. Preserve charges and record corrections separately. Necessary local allowance extensions are already authorized: record the full projection, reason, increment and updated limit before using one. Do not ask for the same approval again. No paid/external compute or destructive deletion is authorized.

Plan an initial correctness/coverage/probe tranche of at most **1,200,000 ms** including builds, data preparation, controls and retries; this is a proposed task cap, not a deduction already made. Use one model worker and at most four Cargo build jobs, peak RSS <=8 GiB, <=1 GiB new build/data/artifact storage, checkpoints and 128 MiB stop margin. Verify those are feasible on the actual host before execution. If a valid cached build makes the tranche smaller, use less. If a necessary build cannot fit, re-project and record before continuing; do not conceal uncharged work. Any pilot requires its own complete projection and must leave evaluation/checkpoint capacity. Record engineering and model time separately with a clear cumulative accounting convention; do not silently exclude builds already covered by the standing policy.

Original `.rgm` SHA256: `7023c4507039acc01653b2dd6a41cf4329635767c66811a17d76591a4f356d96`.
Stripped `.rgm`: `a25a87c4c5cdd5f7e6af1479273e29cc996d3cc78a1984315af8cf9b70a10a1b`.
Source tokenizer at `.uor-models/sources/smollm2-135m-instruct/tokenizer.json`: `9ca9acddb6525a194ec8ac7a87f24fbba7232a9a15ffa1af0c1224fcd888e47c`.
Refresh each before using it. The new core has no promoted real-text artifact to inherit.

## Research and delivery discipline

Use relevant source and primary papers when an actual mechanism decision arises. The takeover review maps linear/fast-weight memory, DeltaNet, low-bit kernels, structured recurrence, H4/2I, W33, NEMESIS, SpiralCore and UOR interfaces. Avoid universal conclusions from one failed optimizer run. D2 protects promising geometry from retirement on a small ablation; it does not let an irrelevant control prove geometric contribution.

Run `~/.cargo/bin/cargo fmt --check`, a touched-package offline check and the named meaningful tests. Use both overflow-checked boundary tests and optimized timing. Do not rerun every historical model panel for an isolated learner repair. If you change an integration boundary, run its relevant retained controls. Use claimed unique report roots and seal complete attempts under the repository's existing report APIs; never reuse a sealed root or write derived inputs underneath it.

Stage named paths, deliver through a protected PR, inspect actual merge/source equality and preserve all receipts. Update the owning issue, #973 and #820 as appropriate, current-state, EVIDENCE, ledger and README only where their live claims change. Use “References #N” for partial work; do not close broad capability issues. Queue acknowledgement statuses do not execute model tests. Finish with source/artifact IDs, actual commands, coverage/gradient/timing results, generated examples if run, cumulative resource charge, limitations and **one recommended next action**.
