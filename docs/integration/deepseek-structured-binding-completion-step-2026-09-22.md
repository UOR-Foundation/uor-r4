# DeepSeek completion brief: make structured argument binding work on ordinary forms

September 22 UTC, 2026. Codex remains principal mathematics/ML/systems investigator. You are a capable autonomous research contributor responsible for implementation, diagnosis, evaluation and delivery. The objective is a complete useful learned interface, not literal compliance with an illustrative sketch. Make reasoned improvements when evidence supports them and explain your decisions.

## Recover before choosing a base

Read this brief and [the principal PR1344 review](structured-argument-binding-review-2026-09-22.md) completely. Then read the previous [full design brief](deepseek-structured-argument-binding-step-2026-09-22.md), the linked original/corrected audits and checks, and the latest canonical plan/current state. The review corrects errors in the returned narrative; do not reason only from its summary.

Refresh origin/main and live PR #1344. It was submitted as draft `9cb02f6e77eeae6d026e830f7128625c135f7e26`; the principal review updates that draft's source separately from the review/documentation delivered to main. Do not assume a merged documentation PR means the draft model merged. The principal corrected source head is `1df229178c6b56ae6dbf95273c9e4eae3282a66e`. Verify the actual live draft head, source content, model/session versions and parent before creating an isolated full worktree. Preserve the owner checkout, unique original/negative models and all report roots. Continue the existing draft rather than opening a competing implementation PR. Bring current main's reviewed documentation into that draft with an actual merge when needed, and verify both retained model source and inherited review files; do not repeat a single-descendant rebase that drops its unmerged dependency.

Recover source-linked knowledge with `PR1344 principal`, `structured binding completion`, `cue role 47/48`, `byte identity`, `binary icosahedral interval` and the current review's revision. Follow original sources and correction edges. Historical snapshots guide navigation; GitHub/source bytes and the live ledger own current status. Read AGENTS.md, DECISIONS.md D0-b and stable execution policy. Offline Rust floats/matmul are allowed; final serving targets bounded geometric/integer/lookup operators and permitted <=4-bit additive maps. Whole-path compliance is not established by Rust syntax or a table name.

## What is retained and what actually failed

The submitted model correctly fit 48/48 argument extents on the old fixture but cue semantics were only 47/48. `Mara project follows Ivo` was classified Emit rather than Continue, causing 23/24 answers in each primary panel. Its aligned-byte path was already exercised: actual source, fit and question bytes tile correctly. The missing test is joining across different tokenizations of the same lexical name. Its source-edit/cycle instruments changed tokens while leaving stale text/lengths, mixing identity regimes and yielding zero-read Unresolved. Five red tests included genuine supervision/loader/restore regressions, not merely obsolete assertions.

The corrected draft restores those contracts and records their executed outcomes in the principal receipts. Read those outcomes before repeating repairs. The segment decoder is still staged before cue-role fitting, background is explicitly fixed zero, and cue spans are bounded by the actual four-token cue feature support. Original artifacts and misleading claims remain navigable with corrections. No new ordinary-form fixture, fresh final or interpretable categorical/H4 language comparison has been completed by the review.

## One complete milestone

Produce complete evidence-dependent answers from learned subject/object/query spans across a modest variety of readable sentence and question structures, with exact lexical identity across normal BPE context differences, through the same loaded runtime.

This is the observation interface needed for the broader UOR-R4 Geometric Language Model: native conversation/memory, shared computation/reasoning and Rust generation on consumer hardware. Complete it before expanding into durable corrections, a broad grammar engine or prose in this run. Those later components remain part of the same roadmap.

### Start from actual candidate support and the scored objective

Use a few transparent development examples to show that the correct subject/query and object spans are admitted without gold candidate selection. Include a source whose object precedes its subject, a genuine question, and trailing material outside the answer. For example, `Cedar Annex is Mara's office`, `Where does Mara work?`, and a work-location statement with a trailing time phrase. Choose clearer or more token-budget-appropriate forms if useful. These are illustrative text, not serving parser rules.

Inspect the current 24-token clause/four-token cue support. A question may have semantic evidence around its subject; do not force an impossible contiguous cue label or quietly add a host grammar that finds it for the model. Select minimal contextual cue spans, use surrounding observations, or deliberately extend the structured hypothesis if the real examples require it. Report what is fixed versus learned. Candidate bounds may change when justified by observed support and measured cost; they are not sacred historical thresholds.

The eight-mask semi-Markov approach is a sound small representation. You may retain it, use bounded span tuples, or improve its joint scoring if evidence warrants. The critical criterion is that fitting and inference optimize the declared same scores. A staged span model plus cue classifier can be adequate if both learned quantities and complete answers work; perfect span accuracy must not hide wrong Continue/Emit semantics.

If learning background, use an unambiguous objective. Singleton background transitions yield a sum of named span potentials plus uncovered-token potentials, so the structured perceptron update can include the exact feature difference scored by inference. Alternatively keep background explicitly fixed zero. A claim of learned background requires actual fitting. Diagnose objective/representation failures before widening or blind fitting retries.

Precompute interval features/potentials once when that simplifies the implementation. There are only 300 intervals at 24 tokens. Cache-free O(8*n*L*R) recurrence after preparation is enough; account separately for preparing potentials. Do not build an unrelated performance framework. Verify the DP once against exhaustive legal assignments on tiny examples if changing its recurrence or objective.

### Exact identity and observed bytes

Construct clauses from actual original text through the pinned tokenizer, saving raw bytes/tokens and verified alignment. The corrected runner compares decoded bytes to text as well as length totals. `Clause::of` is explicit legacy token-only input; do not silently downgrade malformed aligned input. Tagged lexical and token domains are distinct.

Demonstrate the same exact name in sentence-initial and internal positions with different BPE token sequences. The join must use its declared lexical key while emitting the actual selected source surface. Preserve case, interior bytes/spaces, exact prefix distinctions (`Cedar` versus `Cedar Annex`) and raw source/version/offset provenance. The present lexical policy excludes only exterior ASCII whitespace. Broader normalization, aliases and learned coreference are separate capabilities and cannot be silently added.

Keep byte fragments and Unicode reconstruction honest. Tokenizer normalization or synthetic prefix-space insertion may require real original-input offsets rather than an assumed token-length sum. Adapt the small tokenizer boundary if the chosen actual inputs need it; do not special-case entity names or rewrite all sentences to avoid different BPE boundaries. The executed principal key-equivalence test is an interface check, not a learned cross-boundary full-answer result.

Source interventions must update text, tokens and alignment consistently. Derive their expected semantic result independently. A valid redirect to an existing alternative person should produce the corresponding new path/answer; a broken reference should be unresolved. These are different tests. Preserve exact occurrence identities when deleting a later source.

### A meaningful geometric comparison

Use the current categorical representation as a competent baseline, adapted to the same structured support and information. Its interior feature is a bag. Add bounded order observations such as relative-position tokens or bigrams if the H4 hybrid receives ordered interiors; otherwise an improvement conflates new order information with geometry.

The H4 feature donor uses the 120-element binary icosahedral group 2I, not the full H4 reflection group. Its associative prefixes satisfy

`P_j=g(x_0)...g(x_{j-1}); inverse(P_i)*P_j=g(x_i)...g(x_{j-1})`.

This gives a finite ordered summary through product/inverse tables. Current H4 terms augment categorical terms, so call this arm a hybrid unless you deliberately choose another comparison. Initialize the code map before fitting scored weights. If codes change, evaluate the same exported hard forward and preserve an incumbent; alternate/refit weights or use a better coherent offline objective when needed. Exact-hit-only coordinate search can plateau and is not the only allowed optimizer. You may use margins, structured loss, gradients offline or a justified discrete procedure.

Bind actual code maps, role potentials and group product/inverse convention to the artifact. Inspect lazy group initialization, all coefficient widths, table/code accesses and preprocessing before claiming D0-b, cache residency or efficiency. A group ID is not a <=4-bit learned linear coefficient, and table initialization using floats is not a multiplier-free end-to-end path. Geometry can be retained for a useful equal-quality cost or transfer result; no arbitrary dominance bar should discard a helpful component. Report a negative comparison at its exact scope rather than demoting the whole geometry architecture.

### Execute complete behavior and preserve causal instruments

Use the existing canonical ObservedTextRuntime and `ob_serve` adapter for primary/control generation, independent reload, interventions, timing and checkpoint resumption. Do not replace it with an abbreviated host loop. No gold role, argument extent, action, pointer, expected depth, answer or fixture identity may enter primary inference. Source boundaries and explicit bounded candidate admission can be declared scaffolding.

Author readable development worlds with at least two familiar goals, exact multiword names/answers and plausible same-goal competitors. Include actual shared BPE material between syntax and names, meaningful word-order changes/paraphrases, and the same question answered after one/two/three content-dependent reads in different worlds. A terminal value can also be a key elsewhere, so membership cannot decide stopping. Continue must preserve the requested goal.

Preserve read-disabled, read-cap and membership controls and relevant update-disabled evidence. For a genuinely different-BPE task, give membership the same lexical identity as the primary, or explicitly label its weaker identity boundary; the current control uses token vectors. Distinguish required later evidence from permissible replacement, cycle/exhaustion from absence and exact complete answer from any nonempty output. Every arm needs actual row outputs, source traces and final frames.

Retain all-phase serialized continuation checks and source/model/tokenizer/control binding. The current snapshot validates internal source-chain/phase consistency; it does not authenticate an omitted external original request. Do not broaden that claim. Preserve failing reports by sealing their data and returning failure for violated instrument/runtime contracts; remove neither the evidence nor its failure status.

Use open development while choosing the design. Old panels are exposed regressions. After design selection, make a genuinely new composition/assignment/form combination draw with familiar primitive meanings learned during development. If final data informs another change, retain its result and label that data exposed. Report new lexical values, new structures and longer compositions separately. Estimate uncertainty by independent world where requests share facts; do not count correlated rows as independent evidence.

Complete lawful answers are the decision quantity. Report span support, learned span/role accuracy, identity joins, action/depth and full output separately so one auxiliary score cannot hide another failure. A useful partial may be retained, but compiling helpers or measuring the old layout again is not completion of this milestone.

## Donors, primary sources and broader synthesis

Inspect the current observed-text module and `ob_serve`; `source_routing.rs::encode/score`; `group_table.rs` and exact table conventions; `HfBpeTokenizer::encode/decode_bytes/token_byte_lengths`; occurrence_role and span_learning/span_boundary for context/boundary credit; query_participation/dependent-language scheduling for required-evidence versus replacement. Reuse responsibilities rather than importing old ASCII bounds, authored role rules or incompatible serialized artifacts.

Relevant primary sources, to read when changing the corresponding component:

- [Collins2002 structured perceptron](https://aclanthology.org/W02-1001/): consistent scored feature updates.
- [Semi-Markov Models for Sequence Segmentation](https://aclanthology.org/D07-1067/) and [Semi-Markov CRFs](https://papers.nips.cc/paper_files/paper/2004/hash/eb06b9db06012a7a4179b8f3cb5384d3-Abstract.html): bounded segment outputs and inference.
- [Span Selection for Semantic Role Labeling](https://arxiv.org/abs/1810.02245): span/role decomposition, not a dense serving requirement.
- [Tokenizer Encoding](https://huggingface.co/docs/tokenizers/en/api/encoding) and the actual Rust adapter: original-input offsets versus token content.
- [SLOG](https://aclanthology.org/2023.emnlp-main.194/): distinguish familiar primitive meanings from withheld structural composition.

Refresh external primary research when it can change a concrete choice; no broad literature survey is required before every edit. Neither publication recency nor a geometric name substitutes for an actual mechanism advantage.

The roadmap after this interface is durable roles/scopes/corrections plus grounded Q8 outputs consumed by the same runtime; then retained E/S language prediction integrated into prose/conversation and executed Rust; then complete artifact/API and machine qualification. Structural banks/Hopf fiber/signed Spin-H4, golden-coupled paired-H4/E8, S7 and harmonic/scalar fields remain possible responses to a witnessed interference, orientation or sharing problem. Learned scalar span/role potentials already provide a concrete field-like object; harmonic parameterization is a testable alternative basis, not an automatic need for supersymmetry, twin primes or physical spin.

## Autonomy, resources and delivery

Choose the useful implementation and resolve unexpected problems with scientific judgment. There is no arbitrary short timer or one-retry quota. Necessary local model/time/storage extensions remain owner-authorized when projected and recorded before use. Preserve cumulative accounting across preparation, builds, fitting, controls, failures, retries and delivery. The previous run omitted its debit; the principal receipt restores an explicitly estimated charge. Read the live JSON rather than copying this document's historical balance.

Keep CPU/threads, peak RAM, temporary/retained storage, physical reserve and 128 MiB stop margin configured. Preserve unique research, original and negative models, AI histories and Downloads. Reuse release dependencies and remove only identified inactive disposable material within authorization. No paid/external compute. A resource estimate is not measured runtime; energy stays UNAVAILABLE until physically measured.

Compile and exercise the changed Rust path, focused algorithm/arithmetic/identity/serialization risks, actual loaded answers and relevant formatting/claim checks. No blanket suite/proof dossier is required. Queue statuses acknowledge compatibility, not tests. Update the draft PR with the completed implementation, evidence, limitations and self-review; make it ready for protected delivery only when the claimed milestone is actually exercised. No direct main push, admin merge or manufactured checks.

Update current state, canonical roadmap responsibilities when changed, README claims, live owning issues #820/#973/#1139/#962/#963/#964 and source-linked knowledge. Verify the actual PR head and merged contents when reporting delivery; a review/docs merge does not merge a separate model draft. Preserve your meaningful disagreements and original negative outcomes. Return your selected mechanism, what learned/fixed, complete-answer and control evidence, matched representation result, dataset/source/artifact identities, checks, resources/storage, delivery state and one coherent next step in the full roadmap.
